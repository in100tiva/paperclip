---
phase: 10-mensagens-dos-agentes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/agents.json
  - ui/src/i18n/locales/en-US/agents.json
  - ui/src/components/transcript/RunTranscriptView.tsx
  - ui/src/components/RunChatSurface.tsx
  - ui/src/components/LiveRunWidget.tsx
  - ui/src/components/IssueRunLedger.tsx
  - ui/src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx
autonomous: true
requirements:
  - AGENT-MSG-02
must_haves:
  truths:
    - "Usuário com locale=pt-BR abre uma run e vê labels de transcript em pt-BR — 'Em execução'/'Concluído'/'Com erro'/'Entrada'/'Resultado'/'Aguardando resultado'/'Executando comando'/'Comando executado'/'Ferramenta falhou'/'Falhou com código de saída {code}'"
    - "Aria-labels de toggle (Recolher/Expandir detalhes da ferramenta/comando/saída padrão) traduzidas — leitores de tela em pt-BR"
    - "IssueRunLedger renderiza 'Registro de execuções', 'Trabalho derivado', 'Execução mais recente', 'X ativos, Y concluídos, Z cancelados' (com plurals), '+N a mais' em pt-BR"
    - "RunChatSurface emptyMessage 'Aguardando saída da execução…'/'Nenhuma saída de execução capturada.' em pt-BR"
    - "LiveRunWidget renderiza 'Execuções ao vivo', 'Parar'/'Parando…', 'Abrir execução' em pt-BR"
    - "Toggle para en-US devolve transcript/ledger/widget para inglês imediatamente"
    - "Detector missing-keys CI=true exit 0 após migração"
    - "i18next plural ativo em more-children (+N a mais) e child-summary"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/agents.json"
      provides: "Sub-trees transcript, run-ledger, live-runs (estensão coordenada com plano 10-01 — sub-trees disjuntos)"
      contains: "transcript"
    - path: "ui/src/i18n/locales/en-US/agents.json"
      provides: "Mirror en-US, paridade 100% para os sub-trees adicionados"
      contains: "transcript"
    - path: "ui/src/components/transcript/RunTranscriptView.tsx"
      provides: "Transcript renderer migrado para t()"
      contains: "useTranslation"
    - path: "ui/src/components/IssueRunLedger.tsx"
      provides: "Run ledger seção (linhas 416-708) migrada; preserva watchdog toast (será migrado em 10-03)"
      contains: "agents:run-ledger"
    - path: "ui/src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx"
      provides: "RTL probe assertindo labels do transcript pt-BR ↔ en-US toggle"
      min_lines: 50
  key_links:
    - from: "ui/src/components/transcript/RunTranscriptView.tsx"
      to: "agents:transcript.* sub-tree"
      via: "useTranslation([\"agents\", \"common\"])"
      pattern: "useTranslation\\(\\[\"agents\""
    - from: "ui/src/components/IssueRunLedger.tsx"
      to: "agents:run-ledger.* sub-tree (+ pluralização _one/_other)"
      via: "t(\"agents:run-ledger.more-children\", { count })"
      pattern: "agents:run-ledger"
    - from: "ui/src/components/LiveRunWidget.tsx"
      to: "agents:live-runs.* sub-tree"
      via: "useTranslation([\"agents\", \"common\"])"
      pattern: "agents:live-runs"
---

<objective>
Traduzir a camada de **summaries e relatórios apresentados em UI** (não output do modelo) — `RunTranscriptView` (1526 LOC, labels de tool blocks/command blocks/status/result), `IssueRunLedger` seção de run summary (linhas 416-708), `RunChatSurface` (emptyMessage), `LiveRunWidget` (live runs panel) — de inglês hardcoded para `useTranslation`-driven pt-BR/en-US. Estende `agents.json` com sub-trees `transcript` (15+ keys + aria), `run-ledger` (8 keys + plurals), `live-runs` (6 keys). Plano 10-02 corre em paralelo com 10-01 (Wave 1) — file sets disjuntos exceto pelo namespace `agents`. Cada plano edita sub-trees distintos do mesmo JSON (10-01: title/actions/status/role/detail/panel/new-agent-dialog/config; 10-02: transcript/run-ledger/live-runs). Cobre AGENT-MSG-02 (summaries/relatórios).

Purpose: Fecha a fatia de "summaries" da Fase 10 — usuário em pt-BR abrindo um run vê transcript inteiro traduzido (executando comando, ferramenta falhou, aguardando resultado, entrada, resultado), accessibility labels traduzidas (recolher/expandir), e em issues vê o run ledger ("Registro de execuções", "Trabalho derivado", "X ativos, Y concluídos") em pt-BR. Live run widget no dashboard com "Execuções ao vivo", "Parar"/"Abrir execução" em pt-BR.

Output: agents.json com 3 sub-trees adicionados (~30 leaf keys × 2 locales) + 4 arquivos TSX migrados + 1 RTL test file novo.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mensagens-dos-agentes/10-CONTEXT.md
@.planning/phases/10-mensagens-dos-agentes/10-RESEARCH.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-04-SUMMARY.md

# Source files (read first)
@ui/src/components/transcript/RunTranscriptView.tsx
@ui/src/components/RunChatSurface.tsx
@ui/src/components/LiveRunWidget.tsx
@ui/src/components/IssueRunLedger.tsx
@ui/src/i18n/locales/pt-BR/agents.json
@ui/src/i18n/locales/en-US/agents.json
@ui/src/i18n/__tests__/missing-keys.test.ts

# Reference patterns from prior phases
@ui/src/pages/CompanySettings.tsx
@ui/src/i18n/locales/pt-BR/common.json

<interfaces>
<!-- Linhas críticas de RESEARCH.md §"Phase Requirements" para AGENT-MSG-02 — strings exatas a substituir. -->

From ui/src/components/transcript/RunTranscriptView.tsx:
- Line 287: "Executing command" → t("agents:transcript.executing-command")
- Line 292: "Executed command" → t("agents:transcript.executed-command")
- Line 298: "Tool failed" → t("agents:transcript.tool-failed")
- Line 300: "Waiting for result" → t("agents:transcript.waiting-result")
- Line 515: "Completed" → t("agents:transcript.completed")
- Line 651: "Failed" → t("agents:transcript.failed")
- Line 709-712: "User"/"Running"/"Errored"/"Completed" → t("agents:transcript.{slug}")
- Line 789: aria-label "Collapse/Expand tool details"
- Line 820-822: "Input"/"Result" labels
- Line 1033/1038/1045: "Run failed"/"Failed with exit code {code}"/"Failed"
- Line 1473/1487: emptyMessage "No transcript yet."
- Aria-labels: collapse/expand for tool, command, stdout (6 variants)

From ui/src/components/IssueRunLedger.tsx:
- Line 391: pushToast title "Watchdog decision not recorded" — DEFER to plan 10-03 (toast)
- Line 444: "Run ledger" → t("agents:run-ledger.title")
- Line 449-450: "Waiting for the first run record."/"No runs linked yet." → t("agents:run-ledger.waiting-first-run") / t("agents:run-ledger.no-runs-linked")
- Line 458: "Latest run" → t("agents:run-ledger.latest-run")
- Line 466-470: "Child work" + summary "X active, Y done, Z cancelled" / "all N terminal" → t("agents:run-ledger.child-work") + t("agents:run-ledger.child-summary-active", { active, done, cancelled }) / child-summary-terminal
- Line 484-487: "+N more" → t("agents:run-ledger.more-children", { count })

From ui/src/components/RunChatSurface.tsx:
- Line 63: emptyMessage default "Waiting for run output..."/"No run output captured." → t("agents:live-runs.waiting-output")/t("agents:live-runs.no-output")

From ui/src/components/LiveRunWidget.tsx:
- Line 95-99: "Live Runs"/"Uses the shared chat-style run surface from issue activity." → t("agents:live-runs.title")/t("agents:live-runs.subtitle")
- Line 133: "Stop"/"Stopping…" → t("agents:live-runs.stop")/t("agents:live-runs.stopping")
- Line 140: "Open run" → t("agents:live-runs.open-run")
</interfaces>

<wave1_coordination>
**CRÍTICO — coordenação Wave 1 com Plano 10-01:**

Ambos os planos editam `ui/src/i18n/locales/pt-BR/agents.json` e `en-US/agents.json`. Para evitar merge conflicts:
- Plano 10-01 (paralelo) cria/edita TOP-LEVEL keys: `title`, `actions`, `status`, `role`, `detail`, `panel`, `new-agent-dialog`, `config`.
- Plano 10-02 (este) cria/edita TOP-LEVEL keys DISJUNTOS: `transcript`, `run-ledger`, `live-runs`.
- Tarefa 0 deste plano executa AFTER (ou em ordem mecânica/lockstep) Tarefa 0 do plano 10-01: 10-01 commit primeiro o bootstrap completo de agents.json com sub-trees vazios para `transcript`/`run-ledger`/`live-runs` (objetos vazios `{}`), 10-02 popula esses sub-trees.

Alternativa equivalente: ambos plans editam o mesmo arquivo via append-only com sub-trees disjuntos; merge conflict trivial resolvível por agrupamento estrutural durante execução. **Convenção:** se ambos planos paralelos colidem em JSON puro (não há código compartilhado), o segundo a mergeer faz append do seu sub-tree e re-roda detector missing-keys.
</wave1_coordination>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 0 (Wave 0): Estender agents.json com transcript/run-ledger/live-runs sub-trees + failing RTL test</name>
  <files>
    ui/src/i18n/locales/pt-BR/agents.json,
    ui/src/i18n/locales/en-US/agents.json,
    ui/src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx
  </files>
  <behavior>
    - agents.json (pt-BR + en-US) ganha 3 novos sub-trees TOP-LEVEL: `transcript`, `run-ledger`, `live-runs`.
    - Paridade estrutural 100% entre pt-BR e en-US.
    - Sub-trees disjuntos com os do plano 10-01 (zero overlap nas top-level keys).
    - RunTranscriptView.i18n.test.tsx skeleton com `it.todo` declarados — promove a `it` na Tarefa 1.
    - Detector missing-keys CI passa após bootstrap (sem keys órfãs porque o código TSX ainda não consome).
  </behavior>
  <action>
    1. **Estender `ui/src/i18n/locales/pt-BR/agents.json`** com os 3 sub-trees (Pattern 4 do RESEARCH.md, páginas 19-20):

    ```json
    {
      "transcript": {
        "user": "Usuário",
        "executing-command": "Executando comando",
        "executed-command": "Comando executado",
        "tool-failed": "Ferramenta falhou",
        "waiting-result": "Aguardando resultado",
        "waiting-result-ellipsis": "Aguardando resultado…",
        "completed": "Concluído",
        "running": "Em execução",
        "errored": "Com erro",
        "run-failed": "Execução falhou",
        "failed-with-exit": "Falhou com código de saída {{code}}",
        "failed": "Falhou",
        "input-label": "Entrada",
        "result-label": "Resultado",
        "empty": "Ainda sem transcrição.",
        "aria": {
          "collapse-tool": "Recolher detalhes da ferramenta",
          "expand-tool": "Expandir detalhes da ferramenta",
          "collapse-command": "Recolher detalhes do comando",
          "expand-command": "Expandir detalhes do comando",
          "collapse-stdout": "Recolher saída padrão",
          "expand-stdout": "Expandir saída padrão"
        }
      },
      "run-ledger": {
        "title": "Registro de execuções",
        "waiting-first-run": "Aguardando o primeiro registro de execução.",
        "no-runs-linked": "Ainda não há execuções vinculadas.",
        "latest-run": "Execução mais recente",
        "child-work": "Trabalho derivado",
        "child-summary-active": "{{active}} ativos, {{done}} concluídos, {{cancelled}} cancelados",
        "child-summary-terminal": "todos {{total}} terminais ({{done}} concluídos, {{cancelled}} cancelados)",
        "more-children": "+{{count}} a mais",
        "more-children_one": "+{{count}} a mais",
        "more-children_other": "+{{count}} a mais"
      },
      "live-runs": {
        "title": "Execuções ao vivo",
        "subtitle": "Usa a superfície de chat compartilhada da atividade da tarefa.",
        "stop": "Parar",
        "stopping": "Parando…",
        "open-run": "Abrir execução",
        "waiting-output": "Aguardando saída da execução…",
        "no-output": "Nenhuma saída de execução capturada."
      }
    }
    ```

    Estes sub-trees DEVEM ser adicionados como TOP-LEVEL keys em adição aos sub-trees do plano 10-01. Se o arquivo já tem `title`/`actions`/`status`/etc do plano 10-01 (Tarefa 0 daquele plano), apenas merge dos novos top-level keys mantendo formatação JSON consistente. Se o plano 10-01 ainda não populou, este plano cria o arquivo com APENAS os 3 sub-trees deste plano — o plano 10-01 fará merge subsequente.

    2. **Estender `ui/src/i18n/locales/en-US/agents.json`** com mirror estrutural — mesmas 3 sub-trees com valores em inglês (extraídos das strings hardcoded do RESEARCH.md §"Phase Requirements" AGENT-MSG-02). Exemplo:
    ```json
    "transcript": {
      "user": "User",
      "executing-command": "Executing command",
      "executed-command": "Executed command",
      "tool-failed": "Tool failed",
      "waiting-result": "Waiting for result",
      "waiting-result-ellipsis": "Waiting for result…",
      "completed": "Completed",
      "running": "Running",
      "errored": "Errored",
      ... (todas as 15+ keys com valores inglês originais),
      "aria": { "collapse-tool": "Collapse tool details", ... }
    }
    ```

    3. **Criar `ui/src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx`** skeleton:
       - Importar render do `@testing-library/react`, I18nextProvider e i18n config (reusar pattern de `CompanySettings.i18n.test.tsx` da Fase 9-01)
       - 3 describe blocks com `it.todo`:
         * `describe("transcript labels")` it.todo: render com mock entries; assert "Executando comando"/"Concluído"/"Com erro"/"Entrada"/"Resultado" presentes em pt-BR.
         * `describe("aria labels for collapse/expand")` it.todo: assert botão tem `aria-label="Recolher detalhes da ferramenta"`.
         * `describe("locale toggle")` it.todo: render em pt-BR, assert pt-BR string; `i18n.changeLanguage("en-US")`; assert en-US string.

    4. **Verificar parity programaticamente** rodando o detector pré-existente: `pnpm --filter @paperclipai/ui test:run -- missing-keys`. Detector deve passar (parity 100% e — neste momento — nenhum `t("agents:transcript|run-ledger|live-runs")` callsite ainda existe em produção; só keys nos JSON).

    Implementa AGENT-MSG-02 (bootstrap dos summaries keys) — Pattern 4 do RESEARCH.md.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/i18n/__tests__/missing-keys.test.ts src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx</automated>
  </verify>
  <done>
    - agents.json (pt-BR + en-US) tem sub-trees transcript/run-ledger/live-runs com paridade 100%
    - RunTranscriptView.i18n.test.tsx skeleton declarado com it.todo
    - Detector missing-keys CI=true exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 1: Migrar RunTranscriptView.tsx (transcript labels + aria) + RunChatSurface + LiveRunWidget</name>
  <files>
    ui/src/components/transcript/RunTranscriptView.tsx,
    ui/src/components/RunChatSurface.tsx,
    ui/src/components/LiveRunWidget.tsx,
    ui/src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx
  </files>
  <behavior>
    - RunTranscriptView (1526 LOC, ~70 strings) renderiza labels traduzidas em todos os blocos: tool, command, status (Running/Completed/Errored/Failed), result, input, empty.
    - Aria-labels de toggle (collapse/expand) traduzidos para acessibilidade pt-BR.
    - Plurals e interpolação `{{code}}` em "Falhou com código de saída {code}" funciona via i18next.
    - RunChatSurface emptyMessage default traduzido.
    - LiveRunWidget renderiza "Execuções ao vivo"/"Parar"/"Abrir execução" em pt-BR.
    - RunTranscriptView.i18n.test.tsx 3 describe blocks GREEN.
    - Toggle pt-BR ↔ en-US devolve transcript para inglês imediatamente.
  </behavior>
  <action>
    **Read first:** `ui/src/components/transcript/RunTranscriptView.tsx` (1526 LOC inteiro — focar nas 17 linhas listadas no `<interfaces>` block; também rodar grep para detectar strings adicionais: `grep -n '"[A-Z][a-z][a-zA-Z ]\{3,30\}"' ui/src/components/transcript/RunTranscriptView.tsx`), `ui/src/components/RunChatSurface.tsx` (70 LOC), `ui/src/components/LiveRunWidget.tsx` (160 LOC).

    1. **Migrar `ui/src/components/transcript/RunTranscriptView.tsx`** (1526 LOC):
       a. Adicionar `useTranslation(["agents", "common"])` no top-level component
       b. Substituir as ~17 strings listadas no bloco `<interfaces>`:
          - linha 287 "Executing command" → `t("agents:transcript.executing-command")`
          - linha 292 "Executed command" → `t("agents:transcript.executed-command")`
          - linha 298 "Tool failed" → `t("agents:transcript.tool-failed")`
          - linha 300 "Waiting for result" → `t("agents:transcript.waiting-result")` (verificar elipse — pode ser variante `.waiting-result-ellipsis`)
          - linha 515 "Completed" → `t("agents:transcript.completed")`
          - linha 651 "Failed" → `t("agents:transcript.failed")`
          - linha 709-712 "User"/"Running"/"Errored"/"Completed" → `t("agents:transcript.{user|running|errored|completed}")`
          - linha 789 + aria-labels análogas: `t("agents:transcript.aria.{collapse|expand}-{tool|command|stdout}")` em todos os pontos onde aparece o pattern (provavelmente 6 spots)
          - linha 820-822 "Input"/"Result" → `t("agents:transcript.input-label")` / `t("agents:transcript.result-label")`
          - linha 1033 "Run failed" → `t("agents:transcript.run-failed")`
          - linha 1038 "Failed with exit code {code}" → `t("agents:transcript.failed-with-exit", { code })` (interpolação)
          - linha 1045 "Failed" → `t("agents:transcript.failed")`
          - linha 1473/1487 emptyMessage "No transcript yet." → `t("agents:transcript.empty")`
       c. Grep adicional: rodar `grep -n '"[A-Z][a-z][a-zA-Z ]\{3,30\}"' RunTranscriptView.tsx` e revisar matches; cada string user-facing nova → adicionar key ao agents.json `transcript.*` (estender ambos pt-BR e en-US, paridade obrigatória).
       d. NÃO traduzir `payload.message`, `tool.output`, output de comando — são output do agente (Pitfall RESEARCH §"Anti-Patterns").
       e. NÃO traduzir IDs encurtados ou nomes de comando técnicos (e.g., `bash`, `Read`, `Edit`).

    2. **Migrar `ui/src/components/RunChatSurface.tsx`**:
       a. `useTranslation(["agents", "common"])` no component
       b. linha 63 emptyMessage default `"Waiting for run output..."` → `t("agents:live-runs.waiting-output")`; `"No run output captured."` → `t("agents:live-runs.no-output")` — passados como prop `emptyMessage` para `IssueChatThread` ou similar.

    3. **Migrar `ui/src/components/LiveRunWidget.tsx`**:
       a. `useTranslation(["agents", "common"])` no component
       b. linha 95 "Live Runs" → `t("agents:live-runs.title")`
       c. linha 99 subtitle "Uses the shared chat-style run surface from issue activity." → `t("agents:live-runs.subtitle")`
       d. linha 133 "Stop" → `t("agents:live-runs.stop")`; "Stopping…" → `t("agents:live-runs.stopping")`
       e. linha 140 "Open run" → `t("agents:live-runs.open-run")`

    4. **Promover `it.todo` → `it` em `RunTranscriptView.i18n.test.tsx`** com assertions reais:
       - `describe("transcript labels")`: render `<RunTranscriptView entries={[mockToolBlock, mockCommandBlock, mockResultBlock]} />` em I18nextProvider locale pt-BR; `expect(screen.getByText("Executando comando")).toBeInTheDocument()`; idem para "Concluído", "Com erro", "Entrada", "Resultado".
       - `describe("aria labels")`: render com tool block; `expect(screen.getByLabelText("Recolher detalhes da ferramenta")).toBeInTheDocument()` (após click no toggle).
       - `describe("locale toggle")`: render em pt-BR; assert "Concluído"; `await i18n.changeLanguage("en-US")`; rerender (ou use react-testing-library's `act` para changeLanguage); assert "Completed".

    5. **Verificar grep final** em RunTranscriptView.tsx: nenhuma string em inglês UI-facing user-faced (excluindo nomes de comando, IDs, técnico).

    Implementa AGENT-MSG-02 (transcript view + chat surface + live widget).
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx src/i18n/__tests__/missing-keys.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - RunTranscriptView.tsx 17+ strings via t() incluindo aria-labels
    - Interpolação `{{code}}` em failed-with-exit funciona
    - RunChatSurface.tsx emptyMessage default via t()
    - LiveRunWidget.tsx 5 strings via t()
    - RunTranscriptView.i18n.test.tsx 3 describe blocks GREEN com toggle pt-BR ↔ en-US
    - Detector missing-keys CI=true exit 0
    - typecheck preserva apenas baseline
    - Suite UI completa GREEN (sem regressões)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar IssueRunLedger.tsx run summary section (linhas 416-708) com plurals</name>
  <files>
    ui/src/components/IssueRunLedger.tsx
  </files>
  <behavior>
    - IssueRunLedger renderiza "Registro de execuções"/"Aguardando o primeiro registro de execução."/"Ainda não há execuções vinculadas."/"Execução mais recente"/"Trabalho derivado" em pt-BR.
    - Sumário de child work em pt-BR com interpolação: "{active} ativos, {done} concluídos, {cancelled} cancelados" / "todos {total} terminais ({done} concluídos, {cancelled} cancelados)".
    - "+N a mais" pluralizado via i18next `_one`/`_other`.
    - Watchdog toast (linha 391) PRESERVADO em inglês — será migrado em plano 10-03 (toast).
    - Existing tests GREEN.
    - Toggle pt-BR ↔ en-US devolve ledger section para inglês.
  </behavior>
  <action>
    **Read first:** `ui/src/components/IssueRunLedger.tsx` (708 LOC inteiro — focar em linhas 416-708 para a seção de run summary; PRESERVAR linha 391 `pushToast({ title: "Watchdog decision not recorded", ... })` — esta toast vai ser migrada no plano 10-03).

    1. **Migrar `ui/src/components/IssueRunLedger.tsx`** seção run summary:
       a. Adicionar `useTranslation(["agents", "common"])` no component (se já não tiver — verificar; se já tem, estender namespace para `["agents", "common"]`)
       b. linha 444 "Run ledger" → `t("agents:run-ledger.title")`
       c. linha 449-450 "Waiting for the first run record." → `t("agents:run-ledger.waiting-first-run")`; "No runs linked yet." → `t("agents:run-ledger.no-runs-linked")`
       d. linha 458 "Latest run" → `t("agents:run-ledger.latest-run")`
       e. linha 466 "Child work" → `t("agents:run-ledger.child-work")`
       f. linhas 467-470 "X active, Y done, Z cancelled" → `t("agents:run-ledger.child-summary-active", { active, done, cancelled })`; "all N terminal" → `t("agents:run-ledger.child-summary-terminal", { total, done, cancelled })`
       g. linhas 484-487 `\`+${count} more\`` → `t("agents:run-ledger.more-children", { count })` — JSON tem `more-children_one`/`more-children_other` (Pitfall 6 do RESEARCH).
       h. **PRESERVAR linha 391** `pushToast({ title: "Watchdog decision not recorded", body: message, tone: "error" })` intacta — comentário inline `// TODO(10-03): migrate to t("common:toast.watchdog-decision-not-recorded")` opcional, ou simplesmente deixar para o plano 10-03 sem sinalização.
       i. NÃO tocar em outras seções fora de 416-708 (issues/comments etc.). Se tests existentes usam `screen.getByText("Run ledger")`, atualizar para variante traduzida + verificar pt-BR mode.

    2. **Verificar grep final**: regex `>[A-Z][a-z][a-zA-Z ]{4,}<` em linhas 416-708 retorna 0 matches relevantes (excluindo toasts ainda hardcoded).

    3. **Estender tests existentes** se houver `IssueRunLedger.test.tsx` — adicionar caso pt-BR onde rendera `<IssueRunLedger>` com I18nextProvider pt-BR e assert `screen.getByText("Registro de execuções")`. Se test file não existe, OK — RTL coverage segue do RunTranscriptView + missing-keys detector.

    Implementa AGENT-MSG-02 (run ledger summary).
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/i18n/__tests__/missing-keys.test.ts && pnpm typecheck && pnpm exec vitest run src/components/__tests__/IssueRunLedger</automated>
  </verify>
  <done>
    - IssueRunLedger.tsx linhas 416-708 traduzidas via t()
    - Plural _one/_other resolve em more-children
    - Linha 391 watchdog toast PRESERVADA inglês (delegada a 10-03)
    - Detector missing-keys GREEN
    - typecheck baseline preserved
    - Tests existentes GREEN (zero regressão)
  </done>
</task>

</tasks>

<verification>
**Per task:** comando `<automated>` específico passa.

**Phase plan gate:**
- `cd ui && CI=true pnpm exec vitest run -- missing-keys` exit 0
- `cd ui && pnpm exec vitest run src/components/transcript/__tests__/RunTranscriptView.i18n.test.tsx` 100% GREEN
- `cd ui && pnpm typecheck` apenas pre-existing baseline
- `cd ui && pnpm exec vitest run` suite completa GREEN
- `pnpm -r typecheck` apenas pre-existing
- grep `agents:transcript\.|agents:run-ledger\.|agents:live-runs` em arquivos modificados retorna 25+ matches (cobertura)
- grep raw English strings em arquivos modificados (regex `>[A-Z][a-z][a-zA-Z ]{4,}<` no JSX) retorna 0 matches relevantes em RunTranscriptView/RunChatSurface/LiveRunWidget; em IssueRunLedger preserva apenas linha 391 toast (delegado)

**Anti-regression:** consumidores externos de RunTranscriptView/IssueRunLedger compilam via typecheck.
</verification>

<success_criteria>
- AGENT-MSG-02 satisfeito ao código-nível: summaries (transcript view, run ledger, live run widget, chat surface emptyMessage) renderizam em pt-BR via t()
- agents.json estendido com 3 novos sub-trees (transcript: 15+ keys + aria 6 keys; run-ledger: 8 keys + plurals; live-runs: 6 keys) — pt-BR + en-US paridade 100%
- 4 arquivos TSX migrados (RunTranscriptView, RunChatSurface, LiveRunWidget, IssueRunLedger seção)
- 1 arquivo de teste novo (RunTranscriptView.i18n.test.tsx) GREEN
- Pluralização _one/_other ativa em run-ledger.more-children
- Interpolação `{{code}}` em transcript.failed-with-exit funciona
- Linha 391 IssueRunLedger toast PRESERVADA — handoff explícito para plano 10-03
- HUMAN-UAT-10-02 (visual scan) preserved para fase de fechamento
</success_criteria>

<output>
After completion, create `.planning/phases/10-mensagens-dos-agentes/10-02-SUMMARY.md` documentando:
- Final key count em agents.json após esta extensão
- Lista de arquivos TSX migrados + LOC delta
- Strings adicionais detectadas via grep (delta vs Pattern 4 do RESEARCH)
- Status do detector missing-keys CI
- Handoff explícito para plano 10-03 sobre IssueRunLedger toast linha 391
- HUMAN-UAT-10-02 checklist routing
</output>
