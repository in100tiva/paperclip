---
phase: 10-mensagens-dos-agentes
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - ui/src/i18n/locales/pt-BR/common.json
  - ui/src/i18n/locales/en-US/common.json
  - ui/src/context/LiveUpdatesProvider.tsx
  - ui/src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts
  - ui/src/pages/AgentDetail.tsx
  - ui/src/components/IssueRunLedger.tsx
autonomous: true
requirements:
  - AGENT-MSG-04
must_haves:
  truths:
    - "Toast de run failed renderiza 'Execução de {nome} falhou' em pt-BR via buildRunStatusToast"
    - "Toast de agent.error renderiza '{nome} com erro' em pt-BR via buildAgentStatusToast"
    - "Toast de join-request renderiza 'Um agente quer entrar' / 'Alguém quer entrar' + body 'Uma nova solicitação de entrada aguarda aprovação.' em pt-BR"
    - "Toasts de activity (issue.created/updated/comment_added) renderizam '{actor} criou {ref}'/'{actor} atualizou {ref}'/'{actor} comentou em {ref}' em pt-BR"
    - "Action labels nos toasts ('Ver agente'/'Ver execução'/'Ver inbox'/'Ver {ref}') traduzidos"
    - "AgentDetail.tsx linha 1606 'Save failed' toast traduzido para 'Falha ao salvar' via t('common:toast.save-failed')"
    - "IssueRunLedger.tsx linha 391 'Watchdog decision not recorded' toast traduzido"
    - "Mudança de idioma (i18n.changeLanguage) NÃO reabre o WebSocket — closeSocketQuietly NÃO é chamado"
    - "tRef pattern: t é capturado via useRef + useEffect dependency on [t]; useEffect do socket NÃO depende de t"
    - "Toggle pt-BR ↔ en-US devolve toasts para inglês imediatamente em próximo evento"
    - "Activity log entries de agente continuam renderizando via Fase 8-05 actionKey + paramsJson (sem mudança server-side)"
    - "Detector missing-keys CI=true exit 0"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/common.json"
      provides: "Sub-trees toast.agent.*, toast.run.*, toast.join-request.*, toast.activity.* — preservando 144 keys da Fase 7-9 byte-by-byte"
      contains: "toast.agent"
    - path: "ui/src/i18n/locales/en-US/common.json"
      provides: "Mirror en-US, paridade 100% para os novos sub-trees"
      contains: "toast.agent"
    - path: "ui/src/context/LiveUpdatesProvider.tsx"
      provides: "tRef pattern + 4 toast builders refatorados para receber t como último param"
      contains: "tRef"
    - path: "ui/src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts"
      provides: "Vitest probe: (a) buildRunStatusToast retorna title pt-BR, (b) buildAgentStatusToast pt-BR, (c) buildActivityToast pt-BR, (d) buildJoinRequestToast pt-BR, (e) language change NÃO reabre socket"
      min_lines: 80
  key_links:
    - from: "ui/src/context/LiveUpdatesProvider.tsx (LiveUpdatesProvider component)"
      to: "useTranslation([\"common\", \"activity\"]) + tRef = useRef(t)"
      via: "Pattern 3 do RESEARCH — t capturado via ref para evitar reconexão de socket"
      pattern: "tRef = useRef\\(t\\)"
    - from: "buildAgentStatusToast / buildRunStatusToast / buildActivityToast / buildJoinRequestToast"
      to: "t no último param + chama t(\"common:toast.{agent|run|join-request|activity}.*\")"
      via: "Function signature inclui `t: TFunction` no último param"
      pattern: "t: TFunction"
    - from: "handleLiveEvent (callback do socket.onmessage)"
      to: "tRef.current passado para builders"
      via: "Closure read via tRef.current em momento de invocação"
      pattern: "tRef\\.current"
---

<objective>
Traduzir a camada de **notificações e toasts de eventos de agente** — quartel onde todos os builders puros (`buildActivityToast`, `buildJoinRequestToast`, `buildAgentStatusToast`, `buildRunStatusToast` em `LiveUpdatesProvider.tsx:430-607`) emitem strings inline em inglês via template literals com interpolação. Refactor desses 4 builders para receber `t: TFunction` como último parâmetro e consumir keys de `common.json` `toast.agent.*` / `toast.run.*` / `toast.join-request.*` / `toast.activity.*`. Aplica o **tRef pattern** (Pattern 3 do RESEARCH + Pitfall 2): `t` capturado via `useRef` no `LiveUpdatesProvider` component evitando reconexão do WebSocket a cada mudança de idioma. Migra também 2 toasts isolados (AgentDetail.tsx:1606 "Save failed", IssueRunLedger.tsx:391 "Watchdog decision not recorded" — handoff do plano 10-02). Estende `common.json` com 4 novos sub-trees (~25 leaf keys × 2 locales). Sem mudanças no server (Fase 8-05 já cobre activity log). Sem mudanças no `activity.json` — verificado RESEARCH §"AGENT-MSG-04": **sem gaps detectados**, todos os 8 eventos de agente já cobertos. Cobre AGENT-MSG-04.

Purpose: Fecha a última fatia de Fase 10 — usuário em pt-BR recebe toasts de eventos de agente (run succeeded/failed/timed-out/cancelled, agent error, join-request, activity issue.created/updated/comment_added) em pt-BR. Activity log de agente já estava OK (Fase 8-05); este plano apenas traduz toasts. Evita reconexão de socket via tRef pattern — usuário troca idioma e não perde eventos live.

Output: common.json com 4 sub-trees novos (~25 leaf keys × 2 locales) + LiveUpdatesProvider.tsx refatorado (tRef + 4 builders com TFunction) + 2 toasts isolados migrados em AgentDetail/IssueRunLedger + 1 vitest probe new com 5 cases.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mensagens-dos-agentes/10-CONTEXT.md
@.planning/phases/10-mensagens-dos-agentes/10-RESEARCH.md
@.planning/phases/10-mensagens-dos-agentes/10-01-SUMMARY.md
@.planning/phases/10-mensagens-dos-agentes/10-02-SUMMARY.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-04-SUMMARY.md

# Source files (read first)
@ui/src/context/LiveUpdatesProvider.tsx
@ui/src/i18n/locales/pt-BR/common.json
@ui/src/i18n/locales/en-US/common.json
@ui/src/i18n/locales/pt-BR/activity.json
@ui/src/pages/AgentDetail.tsx
@ui/src/components/IssueRunLedger.tsx

# Reference patterns
@ui/src/context/__tests__/Toast.translation.test.tsx
@ui/src/lib/api-error.ts

<interfaces>
<!-- Tipos críticos para refactor do LiveUpdatesProvider — Pattern 3 do RESEARCH.md -->

From ui/src/context/LiveUpdatesProvider.tsx (current — lines 406-408, 430-607):
```typescript
const ISSUE_TOAST_ACTIONS = new Set(["issue.created", "issue.updated", "issue.comment_added"]);
const AGENT_TOAST_STATUSES = new Set(["error"]);
const RUN_TOAST_STATUSES = new Set(["failed", "timed_out", "cancelled"]);

// Builders (signatures atuais):
function buildActivityToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
  queryClient: QueryClient,
  companyId: string,
): ToastInput | null;

function buildJoinRequestToast(
  payload: Record<string, unknown>,
): ToastInput | null;

function buildAgentStatusToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
  queryClient: QueryClient,
  companyId: string,
): ToastInput | null;

function buildRunStatusToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
): ToastInput | null;
```

Target post-refactor (Pattern 3 — t como último param):
```typescript
import type { TFunction } from "i18next";

function buildActivityToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
  queryClient: QueryClient,
  companyId: string,
  t: TFunction,                     // NEW
): ToastInput | null;

function buildJoinRequestToast(
  payload: Record<string, unknown>,
  t: TFunction,                     // NEW
): ToastInput | null;

function buildAgentStatusToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
  queryClient: QueryClient,
  companyId: string,
  t: TFunction,                     // NEW
): ToastInput | null;

function buildRunStatusToast(
  payload: Record<string, unknown>,
  nameOf: (id: string) => string | null,
  t: TFunction,                     // NEW
): ToastInput | null;
```

LiveUpdatesProvider component (line 902 area) — tRef pattern:
```typescript
import { useTranslation } from "react-i18next";

export function LiveUpdatesProvider({ children }: { children: ReactNode }) {
  const { t } = useTranslation(["common", "activity"]);
  const tRef = useRef(t);
  useEffect(() => {
    tRef.current = t;
  }, [t]);
  // ...

  // dentro do socket.onmessage handler:
  handleLiveEvent(
    queryClient, liveCompanyId, pathnameRef.current, parsed,
    pushToast, gateRef.current, currentActorRef.current,
    tRef.current,            // NEW — passar t corrente via ref
  );

  // CRÍTICO (Pitfall 2): useEffect que monta o socket NÃO depende de [t].
  // useEffect deps continuam exatamente como hoje (sem adicionar t).
}

function handleLiveEvent(
  /* args existentes */,
  t: TFunction,             // NEW
) {
  // ... passa t para buildAgentStatusToast(..., t) etc.
}
```

Common.json extension (Pattern 5 do RESEARCH.md, página 22):
```json
{
  "toast": {
    "saved": "Salvo",
    "save-failed": "Falha ao salvar",
    "...": "(keys 7-9 preservadas byte-by-byte)",

    "agent": {
      "started": "{{name}} iniciado",
      "errored": "{{name}} com erro",
      "view": "Ver agente"
    },
    "run": {
      "succeeded": "Execução de {{name}} concluída",
      "failed": "Execução de {{name}} falhou",
      "timed-out": "Execução de {{name}} expirou",
      "cancelled": "Execução de {{name}} cancelada",
      "trigger-prefix": "Gatilho: {{detail}}",
      "view": "Ver execução"
    },
    "join-request": {
      "agent-wants": "Um agente quer entrar",
      "someone-wants": "Alguém quer entrar",
      "body": "Uma nova solicitação de entrada aguarda aprovação.",
      "view-inbox": "Ver inbox"
    },
    "activity": {
      "issue-created": "{{actor}} criou {{ref}}",
      "issue-updated": "{{actor}} atualizou {{ref}}",
      "issue-commented": "{{actor}} comentou em {{ref}}",
      "issue-reopened-commented": "{{actor}} reabriu e comentou em {{ref}}",
      "issue-commented-updated": "{{actor}} comentou e atualizou {{ref}}",
      "view-ref": "Ver {{ref}}"
    },
    "watchdog-decision-not-recorded": "Decisão do watchdog não registrada"
  }
}
```

Note: NÃO renomear ou mover keys existentes (`saved`, `save-failed`, `member-*`, `environment-*`, etc. da Fase 9-04). Apenas APPEND novos sub-trees.
</interfaces>

<dependencies>
**Wave 2 dependency on Wave 1:**
- Plano 10-01 já fechou: `agents.json` populado com `actions/status/role/detail/...` (não consumido aqui, mas evita conflict ao editar JSONs)
- Plano 10-02 já fechou: IssueRunLedger refatorado para a seção 416-708 mas **PRESERVOU linha 391 toast** ("Watchdog decision not recorded") — este plano migra essa linha como handoff explícito.
- Plano 10-01 ou 10-02 já tem `useTranslation([...])` em arquivos compartilhados; este plano adiciona `LiveUpdatesProvider`-level useTranslation (separado).

**No conflict expected:** common.json append-only (sub-trees novos), LiveUpdatesProvider.tsx é cross-cutting mas NÃO foi tocado por planos anteriores.
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 0 (Wave 0): Estender common.json com toast.agent/run/join-request/activity sub-trees + failing vitest probe</name>
  <files>
    ui/src/i18n/locales/pt-BR/common.json,
    ui/src/i18n/locales/en-US/common.json,
    ui/src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts
  </files>
  <behavior>
    - common.json (pt-BR + en-US) preserva keys 7-9 byte-by-byte; APPEND apenas 4 novos sub-trees: toast.agent, toast.run, toast.join-request, toast.activity, e leaf key toast.watchdog-decision-not-recorded.
    - Paridade estrutural 100% entre pt-BR e en-US.
    - LiveUpdatesProvider.toast.i18n.test.ts skeleton com 5 it.todo (1 por builder + 1 para no-reconnect).
    - Detector missing-keys CI passa.
  </behavior>
  <action>
    1. **Estender `ui/src/i18n/locales/pt-BR/common.json`** preservando keys atuais byte-by-byte. APPEND ao sub-tree `toast` os 4 novos sub-sub-trees (`agent`, `run`, `join-request`, `activity`) e a leaf key `watchdog-decision-not-recorded`. Conforme bloco `<interfaces>` Pattern 5 acima. Manter as 26 keys existentes do `toast.*` (saved, save-failed, deleted, copied, member-*, environment-*, draft-probe-*, environment-ready, unknown-error, member-removed-with-reassignment + plurals).

    Estrutura final esperada `toast` (pt-BR):
    ```
    toast:
      saved, save-failed, deleted, ... (26 keys preservadas Fase 9-04)
      agent: { started, errored, view }                       (3 keys novas)
      run: { succeeded, failed, timed-out, cancelled, trigger-prefix, view }   (6 keys novas)
      join-request: { agent-wants, someone-wants, body, view-inbox }           (4 keys novas)
      activity: { issue-created, issue-updated, issue-commented,
                 issue-reopened-commented, issue-commented-updated, view-ref } (6 keys novas)
      watchdog-decision-not-recorded                          (1 leaf nova)
    ```

    Total novos: 3 + 6 + 4 + 6 + 1 = **20 leaf keys novos**.

    2. **Estender `ui/src/i18n/locales/en-US/common.json`** mirror estrutural — mesma forma de árvore, valores em inglês:
    ```json
    "agent": {
      "started": "{{name}} started",
      "errored": "{{name}} errored",
      "view": "View agent"
    },
    "run": {
      "succeeded": "{{name}} run succeeded",
      "failed": "{{name}} run failed",
      "timed-out": "{{name}} run timed out",
      "cancelled": "{{name}} run cancelled",
      "trigger-prefix": "Trigger: {{detail}}",
      "view": "View run"
    },
    "join-request": {
      "agent-wants": "Agent wants to join",
      "someone-wants": "Someone wants to join",
      "body": "A new join request is waiting for approval.",
      "view-inbox": "View inbox"
    },
    "activity": {
      "issue-created": "{{actor}} created {{ref}}",
      "issue-updated": "{{actor}} updated {{ref}}",
      "issue-commented": "{{actor}} commented on {{ref}}",
      "issue-reopened-commented": "{{actor}} reopened and commented on {{ref}}",
      "issue-commented-updated": "{{actor}} commented and updated {{ref}}",
      "view-ref": "View {{ref}}"
    },
    "watchdog-decision-not-recorded": "Watchdog decision not recorded"
    ```

    3. **Criar `ui/src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts`** skeleton:
       - Importar `__liveUpdatesTestUtils` exportado de `LiveUpdatesProvider.tsx` (verificar se existe export — se não, plano deve adicionar export named `__liveUpdatesTestUtils = { buildActivityToast, buildJoinRequestToast, buildAgentStatusToast, buildRunStatusToast, handleLiveEvent }`)
       - Importar i18n config de `ui/src/i18n/index.ts` ou test helpers
       - 5 describe blocks com `it.todo`:
         * `it.todo("buildAgentStatusToast returns 'X com erro' in pt-BR")`
         * `it.todo("buildRunStatusToast returns 'Execução de X falhou' in pt-BR")`
         * `it.todo("buildActivityToast returns '{actor} criou {ref}' in pt-BR")`
         * `it.todo("buildJoinRequestToast returns 'Um agente quer entrar' in pt-BR")`
         * `it.todo("language change does not trigger socket reconnect (closeSocketQuietly NOT called)")`
       - Tarefa 1 promove a `it` real.

    4. **Verificar parity** rodando detector: `pnpm --filter @paperclipai/ui test:run -- missing-keys`. Detector deve passar (parity 100%; nenhum `t("common:toast.{agent|run|join-request|activity}")` callsite em produção ainda — só keys nos JSON).

    Implementa AGENT-MSG-04 (bootstrap dos toast keys) — Pattern 5 do RESEARCH.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/i18n/__tests__/missing-keys.test.ts</automated>
  </verify>
  <done>
    - common.json (pt-BR + en-US) tem 4 novos sub-trees + 1 leaf nova com paridade 100%
    - 26 keys existentes Fase 9-04 byte-by-byte preserved
    - LiveUpdatesProvider.toast.i18n.test.ts skeleton declarado com 5 it.todo
    - Detector missing-keys CI=true exit 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 1: Refactor LiveUpdatesProvider — tRef pattern + 4 builders recebem TFunction + handleLiveEvent passa tRef.current</name>
  <files>
    ui/src/context/LiveUpdatesProvider.tsx,
    ui/src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts
  </files>
  <behavior>
    - LiveUpdatesProvider component obtém `t` via `useTranslation(["common", "activity"])` e captura via `tRef = useRef(t)` + `useEffect(() => { tRef.current = t; }, [t])`.
    - useEffect que monta WebSocket NÃO inclui `t` ou `tRef` em deps array — preserva conexão estável.
    - 4 builders recebem `t: TFunction` como último param e consomem keys de `common:toast.{agent|run|join-request|activity}.*`.
    - `handleLiveEvent` recebe `t` e passa para os builders.
    - `__liveUpdatesTestUtils` (se existir) atualizado para nova assinatura; caso contrário, criar export named para enable testing.
    - 5 describe blocks GREEN: 4 toast builders retornam strings pt-BR + 1 case "language change não dispara closeSocketQuietly".
    - i18n.changeLanguage("en-US") em runtime altera próximo toast emitido para en-US.
  </behavior>
  <action>
    **Read first:** `ui/src/context/LiveUpdatesProvider.tsx` inteiro (focar nas seções: linhas 406-408 enum sets, 430-607 builders, 778-846 handleLiveEvent, 902-1018 LiveUpdatesProvider component + socket effect).

    1. **Adicionar `useTranslation` + tRef pattern no `LiveUpdatesProvider` component (~linha 902)**:
       ```typescript
       import { useTranslation } from "react-i18next";
       import type { TFunction } from "i18next";

       export function LiveUpdatesProvider({ children }: { children: ReactNode }) {
         const { t } = useTranslation(["common", "activity"]);
         const tRef = useRef<TFunction>(t);
         useEffect(() => {
           tRef.current = t;
         }, [t]);
         // ... resto do component
       }
       ```

    2. **Refatorar `buildAgentStatusToast`** (linha ~542) para receber `t: TFunction` no último param:
       - title: `status === "running" ? t("common:toast.agent.started", { name }) : t("common:toast.agent.errored", { name })`
       - body: `agent?.title ?? undefined` (preservar — não é tradução nossa, é título arbitrário do user)
       - action.label: `t("common:toast.agent.view")`

    3. **Refatorar `buildRunStatusToast`** (linha ~572):
       - status normalization: `status === "timed_out" ? "timed-out" : status` (mapping para key kebab)
       - title: `t(\`common:toast.run.${normalizedStatus}\` as const, { name })`
       - body: se `error`, preservar (output do agente, NÃO traduzir — Pitfall RESEARCH §"Anti-Patterns"). Se `triggerDetail`, `body = t("common:toast.run.trigger-prefix", { detail: triggerDetail })`.
       - action.label: `t("common:toast.run.view")`
       - tone calculation preservada: succeeded → success, cancelled → warn, else → error

    4. **Refatorar `buildJoinRequestToast`** (linha ~519):
       - title: `t("common:toast.join-request.{agent-wants|someone-wants}")` baseado no payload (agente vs humano)
       - body: `t("common:toast.join-request.body")`
       - action.label: `t("common:toast.join-request.view-inbox")`

    5. **Refatorar `buildActivityToast`** (linha ~430) — mais complexo (lógica condicional de combinações de events):
       - Para issue.created: `t("common:toast.activity.issue-created", { actor, ref })`
       - Para issue.updated: `t("common:toast.activity.issue-updated", { actor, ref })`
       - Para issue.comment_added: `t("common:toast.activity.issue-commented", { actor, ref })`
       - Para reopened+comment combo: `t("common:toast.activity.issue-reopened-commented", { actor, ref })`
       - Para comment+update combo: `t("common:toast.activity.issue-commented-updated", { actor, ref })`
       - action.label: `t("common:toast.activity.view-ref", { ref })`
       - "reopened from {state}" body fallback: deferir para v2 ou usar key específica `t("common:toast.activity.reopened-from", { state })` — adicionar ao JSON se necessário (Tarefa 0 cobre os casos base; estender se grep revelar mais combinações).

    6. **Atualizar `handleLiveEvent`** (linha ~778) para receber `t: TFunction` como param e passar para cada builder:
       ```typescript
       function handleLiveEvent(
         queryClient: QueryClient,
         companyId: string,
         pathname: string,
         parsed: LiveEvent,
         pushToast: PushToast,
         gate: Gate,
         currentActor: ActorInfo | null,
         t: TFunction,                  // NEW
       ): void {
         // ...
         const toast = buildAgentStatusToast(payload, nameOf, queryClient, companyId, t);
         // ...
         const runToast = buildRunStatusToast(payload, nameOf, t);
         // ...
         const activityToast = buildActivityToast(payload, nameOf, queryClient, companyId, t);
         // ...
         const joinToast = buildJoinRequestToast(payload, t);
       }
       ```

    7. **Atualizar callsite no socket.onmessage callback** (~linha 983) para passar `tRef.current`:
       ```typescript
       socket.onmessage = (event) => {
         const parsed = JSON.parse(event.data);
         handleLiveEvent(
           queryClient,
           liveCompanyId,
           pathnameRef.current,
           parsed,
           pushToast,
           gateRef.current,
           currentActorRef.current,
           tRef.current,                // NEW — read current t via ref
         );
       };
       ```

    8. **CRÍTICO (Pitfall 2):** O `useEffect` que monta o socket (~linha 934-1018) deve continuar com EXATAMENTE as mesmas deps que tem hoje — NÃO adicionar `[t]` nem `[tRef]`. tRef.current é lido em momento de invocação dentro do callback `socket.onmessage`. Validar via teste: language change → re-render do `LiveUpdatesProvider` → `tRef.current` atualiza → MAS o useEffect do socket NÃO re-executa.

    9. **Verificar/criar `__liveUpdatesTestUtils` export** (linha ~887 referenciada pelo RESEARCH):
       - Se já existe export named `__liveUpdatesTestUtils`, atualizar suas funções para nova assinatura.
       - Se não existe, adicionar:
         ```typescript
         export const __liveUpdatesTestUtils = {
           buildActivityToast,
           buildJoinRequestToast,
           buildAgentStatusToast,
           buildRunStatusToast,
           handleLiveEvent,
         };
         ```
       Este export permite testes invocarem builders diretamente passando `t` mockado ou real.

    10. **Promover `it.todo` → `it` em `LiveUpdatesProvider.toast.i18n.test.ts`** com 5 cases reais:
        - **case 1 — buildAgentStatusToast pt-BR:** invocar com payload `{ agentId, status: "error" }`, mock `nameOf` retorna "Bot", real `t` de i18n locale pt-BR; assert `result.title === "Bot com erro"`, `result.action.label === "Ver agente"`.
        - **case 2 — buildRunStatusToast pt-BR:** invocar com `{ runId, agentId, status: "failed", triggerDetail: "manual" }`, mock `nameOf` retorna "Bot"; assert `result.title === "Execução de Bot falhou"`, `result.body === "Gatilho: manual"`, `result.action.label === "Ver execução"`.
        - **case 3 — buildActivityToast pt-BR:** invocar com `{ actor: "Alice", ref: "TASK-123", actionKey: "issue.created" }`; assert `result.title === "Alice criou TASK-123"`, `result.action.label === "Ver TASK-123"`.
        - **case 4 — buildJoinRequestToast pt-BR:** invocar com payload `{ kind: "agent" }`; assert `result.title === "Um agente quer entrar"`, `result.body === "Uma nova solicitação de entrada aguarda aprovação."`, `result.action.label === "Ver inbox"`.
        - **case 5 — language change does not reopen socket:** mock `WebSocket` global; render `<LiveUpdatesProvider>` em I18nextProvider; capture initial socket count (1 instance); `await i18n.changeLanguage("en-US")`; capture socket count post-change (still 1 instance, same instance — `closeSocketQuietly` NOT called between renders). Use vi.spyOn em `closeSocketQuietly` se exportado, ou track via mock `WebSocket.close` count.

    11. **NÃO traduzir** valores arbitrários como `agent.title`, `payload.message`, `error` body — output de modelo/usuário (Pitfall RESEARCH §"Anti-Patterns").

    Implementa AGENT-MSG-04 core (toast builders + tRef pattern) — Pattern 3 do RESEARCH.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts src/i18n/__tests__/missing-keys.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - LiveUpdatesProvider component usa tRef pattern; useEffect do socket não depende de t
    - 4 builders recebem `t: TFunction` como último param
    - handleLiveEvent recebe e passa `t` corretamente
    - 5/5 cases GREEN no LiveUpdatesProvider.toast.i18n.test.ts
    - Case 5 valida no-reconnect on language change
    - typecheck preserva apenas baseline (ActivityRow:42 + recovery/service:459)
    - Suite UI completa GREEN (sem regressões em testes existentes do LiveUpdatesProvider, se houver)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar 2 toasts isolados em AgentDetail.tsx:1606 + IssueRunLedger.tsx:391</name>
  <files>
    ui/src/pages/AgentDetail.tsx,
    ui/src/components/IssueRunLedger.tsx
  </files>
  <behavior>
    - AgentDetail.tsx linha 1606: `pushToast({ title: "Save failed", body: message, tone: "error" })` → `pushToast({ title: t("common:toast.save-failed"), body: message, tone: "error" })`. Usa key existente da Fase 9-04 — sem nova key. Hook `useTranslation(["common"])` provavelmente já existe nesse component (Fase 9-04 + 10-01 já adicionaram); se sub-component dentro de AgentDetail.tsx precisa do hook, adicionar inner-component pattern.
    - IssueRunLedger.tsx linha 391: `pushToast({ title: "Watchdog decision not recorded", body: message, tone: "error" })` → `pushToast({ title: t("common:toast.watchdog-decision-not-recorded"), body: message, tone: "error" })`. Usa nova key adicionada na Tarefa 0.
    - Toggle pt-BR ↔ en-US devolve esses toasts para inglês imediatamente.
    - Body do toast (`message`) preservado (output do server / API error — translateApiError pode ter sido aplicado anteriormente; este plano NÃO altera body).
  </behavior>
  <action>
    **Read first:** `ui/src/pages/AgentDetail.tsx` linhas 1590-1620 (focar contexto do callsite linha 1606); `ui/src/components/IssueRunLedger.tsx` linhas 380-410 (focar contexto da linha 391, verificar que useTranslation já existe — Plano 10-02 Tarefa 2 deve ter adicionado).

    1. **Migrar `ui/src/pages/AgentDetail.tsx` linha 1606**:
       a. Verificar se o callsite está dentro de um componente que já tem `useTranslation` (Plano 10-01 + 09-04 já adicionaram em múltiplas localizações). Se não, adicionar `const { t } = useTranslation(["common"])` no escopo apropriado (pode ser inner component como `RunDetail` no padrão 09-04 — RunDetail tem seu próprio useTranslation).
       b. Substituir `title: "Save failed"` por `title: t("common:toast.save-failed")`.
       c. Preservar `body: message` (mensagem de erro do server — não tradução nossa).

    2. **Migrar `ui/src/components/IssueRunLedger.tsx` linha 391**:
       a. Verificar que `useTranslation(["agents", "common"])` já está no scope (deve ter sido adicionado pelo Plano 10-02 Tarefa 2 — confirmar grep). Se não, adicionar.
       b. Substituir `title: "Watchdog decision not recorded"` por `title: t("common:toast.watchdog-decision-not-recorded")`.
       c. Preservar `body: message`.

    3. **Verificar grep final** em arquivos modificados: `pushToast.*"[A-Z][a-z]` (procurando string title hardcoded em pushToast) retorna 0 matches em AgentDetail.tsx e IssueRunLedger.tsx (excluindo arquivos out-of-scope que serão tratados em fases futuras — IssueDetail/RoutineDetail/AdapterManager/PluginManager continuam com toasts hardcoded; documentar como known-gap).

    Implementa AGENT-MSG-04 (toasts isolados — handoff explícito do plano 10-02).
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/i18n/__tests__/missing-keys.test.ts src/__tests__/confirm-strings.lint.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - AgentDetail.tsx:1606 toast title via t()
    - IssueRunLedger.tsx:391 toast title via t() (handoff Plano 10-02 fechado)
    - Body de ambos toasts preservado
    - Detector missing-keys GREEN
    - typecheck baseline preservado
  </done>
</task>

</tasks>

<verification>
**Per task:** comando `<automated>` específico passa.

**Phase plan gate:**
- `cd ui && CI=true pnpm exec vitest run -- missing-keys` exit 0
- `cd ui && pnpm exec vitest run src/context/__tests__/LiveUpdatesProvider.toast.i18n.test.ts` 5/5 GREEN
- `cd ui && pnpm typecheck` apenas pre-existing baseline
- `cd ui && pnpm exec vitest run` suite completa GREEN
- `pnpm -r typecheck` apenas pre-existing
- grep `common:toast\.(agent|run|join-request|activity)` em LiveUpdatesProvider.tsx retorna 8+ matches
- grep template literal patterns `\\\`.*started.*\\\`` ou `\\\`.*run.*failed\\\`` em LiveUpdatesProvider.tsx retorna 0 matches (nenhum string interpolado bruto remanescente nos builders)
- grep `useEffect.*\\[.*t.*\\]` em LiveUpdatesProvider.tsx — verificar que useEffect do socket NÃO inclui `t` nas deps (Pitfall 2)

**Anti-regression:**
- Tests existentes do LiveUpdatesProvider (se houver) continuam GREEN
- Confirms lint test (Fase 9-04) continua GREEN — esse plano não toca confirms
- Toast.translation test (Fase 9-04) continua GREEN — esse plano não altera ToastProvider

**Phase 10 closure check (após este plano):**
- Todos os 4 reqs (AGENT-MSG-01..04) cobertos: 10-01 (01+03), 10-02 (02), 10-03 (04)
- agents.json populado total: ~140 leaf keys × 2 locales (10-01: ~110; 10-02: ~30 sub-trees adicionais; 10-03: zero — só common.json estendido)
- common.json estendido +20 leaf keys × 2 locales
- 0 strings hardcoded UI-facing nos arquivos in-scope
- HUMAN-UAT-10-01..03 documentados em Fase 10 closure SUMMARY
</verification>

<success_criteria>
- AGENT-MSG-04 satisfeito ao código-nível: 4 toast builders refatorados para receber TFunction; emitem strings traduzidas via common:toast.{agent|run|join-request|activity}.*
- 2 toasts isolados (AgentDetail:1606 + IssueRunLedger:391) migrados — handoff explícito do Plano 10-02 fechado
- LiveUpdatesProvider tRef pattern ativo; mudança de idioma NÃO reabre WebSocket — validado via teste case 5
- common.json estendido com 4 sub-trees novos + 1 leaf key (~20 leaf keys × 2 locales) preservando keys 7-9 byte-by-byte
- 1 vitest probe novo (LiveUpdatesProvider.toast.i18n.test.ts) com 5 cases GREEN
- Activity log de agente já estava OK (Fase 8-05) — sem mudanças server-side ou em activity.json (verificado RESEARCH: nenhum gap)
- HUMAN-UAT-10-03 (visual scan toasts) preserved para fase de fechamento
- Phase 10 ready to close as `complete-with-pending-UAT` (precedent Phases 3-9)
</success_criteria>

<output>
After completion, create `.planning/phases/10-mensagens-dos-agentes/10-03-SUMMARY.md` documentando:
- Final key count em common.json após esta extensão
- Refactor stats LiveUpdatesProvider.tsx (LOC delta, função-by-função: 4 builders + handleLiveEvent + LiveUpdatesProvider component)
- Pitfall 2 verification: useEffect deps array do socket sem t
- 5 test cases coverage map (1 por builder + 1 no-reconnect)
- 2 toasts isolados migrados (handoff fechado)
- HUMAN-UAT-10-03 checklist routing
- Phase 10 closure verdict (`complete-with-pending-UAT` se HUMAN-UAT pending; `complete` se UAT documented inline)
- Known gaps: out-of-scope toasts em IssueDetail/RoutineDetail/AdapterManager/PluginManager preserved (precedent Fase 9-04 known-gap)
</output>
