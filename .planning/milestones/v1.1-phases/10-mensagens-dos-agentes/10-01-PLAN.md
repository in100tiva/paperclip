---
phase: 10-mensagens-dos-agentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/agents.json
  - ui/src/i18n/locales/en-US/agents.json
  - ui/src/lib/agent-keys.ts
  - ui/src/components/StatusBadge.tsx
  - ui/src/components/__tests__/StatusBadge.label.test.tsx
  - ui/src/pages/AgentDetail.tsx
  - ui/src/pages/Agents.tsx
  - ui/src/pages/NewAgent.tsx
  - ui/src/components/AgentConfigForm.tsx
  - ui/src/components/AgentProperties.tsx
  - ui/src/components/AgentActionButtons.tsx
  - ui/src/components/NewAgentDialog.tsx
  - ui/src/components/agent-config-primitives.tsx
  - ui/src/components/ActiveAgentsPanel.tsx
  - ui/src/pages/__tests__/AgentDetail.i18n.test.tsx
autonomous: true
requirements:
  - AGENT-MSG-01
  - AGENT-MSG-03
must_haves:
  truths:
    - "Usuário com locale=pt-BR navega /agents e vê título 'Agentes', filtros 'Ativo'/'Pausado'/'Erro' e CTA 'Novo agente' em pt-BR"
    - "Usuário com locale=pt-BR abre /agents/{ref} e vê tabs 'Painel'/'Instruções'/'Configuração'/'Execuções'/'Orçamento', breadcrumb 'Agentes' e ações 'Atribuir tarefa'/'Pausar'/'Retomar'/'Copiar ID do agente'/'Reiniciar sessões'/'Excluir agente' em pt-BR"
    - "StatusBadge renderiza 'em execução'/'pausado'/'ativo'/'erro'/'aguardando aprovação'/'terminado'/'ocioso' quando label prop traduzido é passado pelo consumidor"
    - "Role labels Engineer/Designer/Researcher/General mostram 'Engenheiro'/'Designer'/'Pesquisador'/'Geral' em pt-BR; siglas CEO/CTO/CMO/CFO/PM/QA/DevOps preservadas"
    - "AgentConfigForm e NewAgentDialog renderizam labels, hints (help dictionary) e CTAs em pt-BR"
    - "ActiveAgentsPanel mostra 'Agentes', 'Ao vivo agora', 'Iniciado {time}', 'Finalizado {time}', plurals 'X execução/execuções a mais' em pt-BR"
    - "Toggle para en-US devolve todas superfícies de painel/configuração de agente para inglês imediatamente"
    - "Detector missing-keys CI=true exit 0 após migração (sem chaves órfãs)"
    - "StatusBadge consumidores legacy (issues/runs/workspaces/DesignGuide) que NÃO passam label continuam funcionando via fallback status.replace(/_/g, ' ')"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/agents.json"
      provides: "Sub-trees title, actions, status, role, detail, panel, new-agent-dialog, config (incluindo config.help com 32 chaves)"
      contains: "engineer"
      min_lines: 100
    - path: "ui/src/i18n/locales/en-US/agents.json"
      provides: "Mirror en-US estrutural, 100% paridade de keys"
      contains: "engineer"
      min_lines: 100
    - path: "ui/src/lib/agent-keys.ts"
      provides: "AGENT_STATUS_KEY + AGENT_ROLE_KEY lookups (Pattern 1 + Pattern 2)"
      exports: ["AGENT_STATUS_KEY", "AGENT_ROLE_KEY"]
    - path: "ui/src/components/StatusBadge.tsx"
      provides: "Refactor com prop opcional label preservando fallback"
      contains: "label"
    - path: "ui/src/components/__tests__/StatusBadge.label.test.tsx"
      provides: "RTL test cobrindo (a) label prop renderiza, (b) sem label fallback ao status.replace(/_/g,' ')"
      min_lines: 30
    - path: "ui/src/pages/AgentDetail.tsx"
      provides: "Painel de agente migrado para useTranslation; consome AGENT_STATUS_KEY + AGENT_ROLE_KEY"
      contains: "AGENT_STATUS_KEY"
    - path: "ui/src/components/AgentConfigForm.tsx"
      provides: "Form de configuração migrado; help dictionary substituído por t() inline"
      contains: "useTranslation"
    - path: "ui/src/components/agent-config-primitives.tsx"
      provides: "help const removido; Field/HintIcon usam t() pelos consumidores"
    - path: "ui/src/pages/__tests__/AgentDetail.i18n.test.tsx"
      provides: "RTL probe assertindo header/tabs/actions/properties pt-BR ↔ en-US toggle"
      min_lines: 50
  key_links:
    - from: "ui/src/pages/AgentDetail.tsx"
      to: "agents:detail.* + agents:status.* + agents:role.* sub-trees"
      via: "useTranslation([\"agents\", \"common\"]) + AGENT_STATUS_KEY/AGENT_ROLE_KEY lookups"
      pattern: "useTranslation\\(\\[\"agents\""
    - from: "ui/src/components/StatusBadge.tsx"
      to: "consumidor passa label prop traduzida"
      via: "Refactor preservando comportamento atual quando label ausente"
      pattern: "label\\?:|label \\?\\?"
    - from: "ui/src/components/AgentConfigForm.tsx"
      to: "agents:config.help.* sub-tree"
      via: "t() inline em <HintIcon text={t(...)} />"
      pattern: "t\\(\"agents:config\\.help"
---

<objective>
Traduzir a primeira camada de mensagens dos agentes ao usuário — superfícies de painel/listagem/configuração — de inglês hardcoded para `useTranslation`-driven pt-BR/en-US. Popula `agents.json` (atualmente vazio `{}`) com sub-trees `title`, `actions`, `status`, `role`, `detail` (tabs/header/properties/empty), `panel`, `new-agent-dialog` e `config` (incluindo `help` com 32 chaves de tooltip). Cria módulo `ui/src/lib/agent-keys.ts` com `AGENT_STATUS_KEY` e `AGENT_ROLE_KEY` lookups (paralelos ao `STATUS_KEY` da Fase 8-03). Refactor mínimo de `StatusBadge` adicionando prop opcional `label` preservando fallback para consumidores legacy. Migra `AgentDetail.tsx` (4248 LOC, ~280 strings — evolução, NÃO scaffold; já tem 2 callsites `useTranslation` existentes), `Agents.tsx`, `NewAgent.tsx`, `AgentConfigForm.tsx` (1510 LOC, ~31 strings), `AgentProperties.tsx`, `AgentActionButtons.tsx`, `NewAgentDialog.tsx`, `agent-config-primitives.tsx` (apaga `help` const) e `ActiveAgentsPanel.tsx`. Cobre AGENT-MSG-01 (status messages dos agentes) e AGENT-MSG-03 (prompts UI dos painéis).

Purpose: Fecha duas das quatro fatias de Fase 10 — usuário em pt-BR usando o paperclip não vê mais inglês em listagem/painel/configuração de agente. Status badges traduzidos via lookup canônico. Forms de configuração com tooltips em pt-BR. CTAs e tabs traduzidos. Cobertura paralela em en-US para paridade de toggle.

Output: agents.json populado (~110 leaf keys × 2 locales) + ui/src/lib/agent-keys.ts (novo módulo) + StatusBadge refactor + 9 arquivos TSX migrados + 2 RTL test files.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mensagens-dos-agentes/10-CONTEXT.md
@.planning/phases/10-mensagens-dos-agentes/10-RESEARCH.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-04-SUMMARY.md

# Source files (read first)
@ui/src/pages/AgentDetail.tsx
@ui/src/pages/Agents.tsx
@ui/src/pages/NewAgent.tsx
@ui/src/components/AgentConfigForm.tsx
@ui/src/components/AgentProperties.tsx
@ui/src/components/AgentActionButtons.tsx
@ui/src/components/NewAgentDialog.tsx
@ui/src/components/agent-config-primitives.tsx
@ui/src/components/StatusBadge.tsx
@ui/src/components/ActiveAgentsPanel.tsx
@ui/src/components/PageTabBar.tsx
@ui/src/i18n/locales/pt-BR/agents.json
@ui/src/i18n/locales/en-US/agents.json
@ui/src/i18n/__tests__/missing-keys.test.ts

# Reference patterns from prior phases
@ui/src/pages/ClaudeAccounts.tsx
@packages/shared/src/constants.ts

<interfaces>
<!-- Tipos chave que o executor consome — extraídos da base de código. -->

From packages/shared/src/constants.ts:16-67:
```typescript
// AGENT_STATUSES (7 valores) — usar como union em AgentStatus
export const AGENT_STATUSES = [
  "active",
  "paused",
  "idle",
  "running",
  "error",
  "pending_approval",
  "terminated",
] as const;
export type AgentStatus = (typeof AGENT_STATUSES)[number];

// AGENT_ROLES (11 valores) — usar como union em AgentRole
export const AGENT_ROLES = [
  "ceo", "cto", "cmo", "cfo",
  "engineer", "designer", "pm", "qa", "devops",
  "researcher", "general",
] as const;
export type AgentRole = (typeof AGENT_ROLES)[number];

// AGENT_ROLE_LABELS — server-side, NÃO traduzir
export const AGENT_ROLE_LABELS: Record<AgentRole, string> = {
  ceo: "CEO", cto: "CTO", cmo: "CMO", cfo: "CFO",
  engineer: "Engineer", designer: "Designer", pm: "PM", qa: "QA", devops: "DevOps",
  researcher: "Researcher", general: "General",
};
```

From ui/src/components/StatusBadge.tsx (current — pre-refactor, 15 LOC):
```typescript
import { cn } from "../lib/utils";
import { statusBadge, statusBadgeDefault } from "../lib/status-colors";

export function StatusBadge({ status }: { status: string }) {
  return (
    <span
      className={cn(
        "inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium whitespace-nowrap shrink-0",
        statusBadge[status] ?? statusBadgeDefault
      )}
    >
      {status.replace(/_/g, " ")}
    </span>
  );
}
```

Target post-refactor signature:
```typescript
interface StatusBadgeProps {
  status: string;
  label?: string;  // NEW — opcional; se ausente, fallback ao comportamento atual
}
export function StatusBadge({ status, label }: StatusBadgeProps): JSX.Element;
```

From ui/src/lib/agent-keys.ts (NEW module to create):
```typescript
import type { AgentStatus, AgentRole } from "@paperclipai/shared";

export const AGENT_STATUS_KEY: Record<AgentStatus, `agents:status.${string}`> = {
  active: "agents:status.active",
  paused: "agents:status.paused",
  idle: "agents:status.idle",
  running: "agents:status.running",
  error: "agents:status.error",
  pending_approval: "agents:status.pending-approval",
  terminated: "agents:status.terminated",
};

export const AGENT_ROLE_KEY: Record<AgentRole, `agents:role.${string}`> = {
  ceo: "agents:role.ceo",
  cto: "agents:role.cto",
  cmo: "agents:role.cmo",
  cfo: "agents:role.cfo",
  engineer: "agents:role.engineer",
  designer: "agents:role.designer",
  pm: "agents:role.pm",
  qa: "agents:role.qa",
  devops: "agents:role.devops",
  researcher: "agents:role.researcher",
  general: "agents:role.general",
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 0 (Wave 0): Bootstrap agents.json + agent-keys.ts module + failing RTL test</name>
  <files>
    ui/src/i18n/locales/pt-BR/agents.json,
    ui/src/i18n/locales/en-US/agents.json,
    ui/src/lib/agent-keys.ts,
    ui/src/components/__tests__/StatusBadge.label.test.tsx,
    ui/src/pages/__tests__/AgentDetail.i18n.test.tsx
  </files>
  <behavior>
    - Bootstrap RED: keys de agents.json existem antes de qualquer migração de TSX consumir.
    - Module agent-keys.ts exporta AGENT_STATUS_KEY (7 entradas) + AGENT_ROLE_KEY (11 entradas) — Pattern 1 + Pattern 2 do RESEARCH.md.
    - StatusBadge.label.test.tsx (2 cases): (a) renderiza label prop quando passado; (b) cai para status.replace(/_/g, ' ') quando label ausente. Test RED no momento da criação porque StatusBadge ainda não aceita label.
    - AgentDetail.i18n.test.tsx (skeleton): import RTL + i18n test utils da Fase 7-9; describe blocks declarados mas com `it.skip` ou `it.todo` cobrindo (a) header pt-BR (status badge "em execução", role "Engenheiro"), (b) tabs em pt-BR ("Painel"/"Configuração"/"Execuções"/"Orçamento"/"Instruções"), (c) action buttons ("Pausar"/"Retomar"/"Copiar ID do agente"). Tarefa 1 vira `it.todo` em `it` quando a migração do AgentDetail.tsx pousar.
  </behavior>
  <action>
    1. **Popular `ui/src/i18n/locales/pt-BR/agents.json`** com a estrutura completa do RESEARCH.md §"Pattern 4" — sub-trees `title` ("Agentes"), `actions` (8 keys: new-agent, assign-task, run-heartbeat, run-now, pause, resume, copy-agent-id, reset-sessions, delete-agent), `status` (7 keys cobrindo AGENT_STATUSES — usar kebab-case `pending-approval` para enum `pending_approval`), `role` (11 keys cobrindo AGENT_ROLES; siglas CEO/CTO/CMO/CFO/PM/QA/DevOps idênticas pt-BR↔en-US), `detail` (sub-trees tabs/header/properties/empty), `panel` (active-agents/no-recent-runs/more-runs com `_one`/`_other`/live-now/finished-relative/started-relative), `new-agent-dialog` (header/ceo-recommendation/ask-ceo-cta/advanced-link/back/advanced-prompt/recommended-tag/ceo-issue-title/ceo-issue-description), `config` (save/saving/saved/save-failed) + `config.help` com 32 entradas (extrair lista canônica das chaves do `help` const em `agent-config-primitives.tsx:22-60` — ler arquivo primeiro, espelhar 1:1 cada chave). Conforme RESEARCH §"Pattern 4" páginas 16-21.

    2. **Popular `ui/src/i18n/locales/en-US/agents.json`** como mirror estrutural — paridade 100% (mesma forma de árvore, mesmas chaves leaf), valores em inglês ("Agents", "New agent", "active", "Engineer", etc.). Extrair valores originais dos arquivos fonte (AgentDetail.tsx, AgentConfigForm.tsx, NewAgentDialog.tsx, ActiveAgentsPanel.tsx, agent-config-primitives.tsx help dict) — fonte de verdade do en-US é o texto que está hoje hardcoded.

    3. **Criar `ui/src/lib/agent-keys.ts`** exportando AGENT_STATUS_KEY e AGENT_ROLE_KEY conforme bloco `<interfaces>` acima. Importar `AgentStatus` e `AgentRole` de `@paperclipai/shared`. NÃO duplicar com AGENT_ROLE_LABELS server-side (Pitfall: server constants permanecem inglês).

    4. **Criar `ui/src/components/__tests__/StatusBadge.label.test.tsx`** com 2 cases:
       - `it("renders label prop when provided", ...)` — render `<StatusBadge status="running" label="em execução" />`, assert `screen.getByText("em execução")`.
       - `it("falls back to status.replace when label absent", ...)` — render `<StatusBadge status="pending_approval" />`, assert `screen.getByText("pending approval")` (espaço em vez de underscore).

    5. **Criar skeleton `ui/src/pages/__tests__/AgentDetail.i18n.test.tsx`** importando `render` de `@testing-library/react`, `I18nextProvider`/i18n config (reusar pattern de `AgentDetail.test.tsx` ou `CompanySettings.i18n.test.tsx` da Fase 9-01). 3 describe blocks com `it.todo`:
       - `describe("header")` it.todo (status badge translates, role label translates, breadcrumb pt-BR)
       - `describe("tabs")` it.todo (PageTabBar shows Painel/Instruções/Configuração/Execuções/Orçamento)
       - `describe("actions")` it.todo (Pausar/Retomar/Copiar ID/Reiniciar sessões/Excluir agente)

       Tarefa 2 promove cada `it.todo` para `it` real após AgentDetail migrado.

    6. **Verificar parity programaticamente** rodando o detector pré-existente: `pnpm --filter @paperclipai/ui test:run -- missing-keys`. Detector deve passar (parity entre pt-BR/en-US e nenhuma chave órfã usada — neste momento NENHUM `t("agents:...")` callsite ainda existe no código de produção; apenas test files que usam mock).

    Implementa AGENT-MSG-01 (status keys + StatusBadge contract) + AGENT-MSG-03 (config/dialog/panel keys) — Pattern 4 do RESEARCH.md.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/components/__tests__/StatusBadge.label.test.tsx src/i18n/__tests__/missing-keys.test.ts</automated>
  </verify>
  <done>
    - `agents.json` (pt-BR + en-US) populado com paridade estrutural 100%
    - `agent-keys.ts` exporta AGENT_STATUS_KEY (7 keys) + AGENT_ROLE_KEY (11 keys)
    - `StatusBadge.label.test.tsx` 2 cases — case (a) RED (StatusBadge ainda não aceita label), case (b) GREEN
    - `AgentDetail.i18n.test.tsx` skeleton com `it.todo` declarados
    - Detector missing-keys GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 1: Refactor StatusBadge + migrar AgentDetail.tsx (painel + ações + status + tabs + breadcrumbs)</name>
  <files>
    ui/src/components/StatusBadge.tsx,
    ui/src/components/__tests__/StatusBadge.label.test.tsx,
    ui/src/pages/AgentDetail.tsx,
    ui/src/pages/__tests__/AgentDetail.i18n.test.tsx,
    ui/src/components/AgentProperties.tsx,
    ui/src/components/AgentActionButtons.tsx,
    ui/src/components/ActiveAgentsPanel.tsx
  </files>
  <behavior>
    - StatusBadge passa a aceitar prop opcional `label` preservando fallback `status.replace(/_/g, ' ')` para consumidores legacy.
    - StatusBadge.label.test.tsx 2/2 GREEN.
    - AgentDetail.tsx renderiza pt-BR completo: breadcrumbs ("Agentes"/"Execuções"/"Painel"/"Configuração"/"Execuções"/"Orçamento"/"Instruções"), status badge traduzido, role label traduzida, action buttons em pt-BR, properties panel em pt-BR, mais menu (Copiar ID/Reiniciar sessões/Excluir agente).
    - AgentDetail.i18n.test.tsx 3 describe blocks com it.todo promovidos a `it` real — assertions em ambos locales (pt-BR + en-US toggle via i18n.changeLanguage).
    - AgentProperties.tsx labels (Status/Role/Title/Adapter/Session/Last error/Last heartbeat/Reports to/Created) em pt-BR.
    - AgentActionButtons.tsx defaults "Run now"/"Resume"/"Pause" via t() — Pause/Resume já têm `label` props que deve ser configuráveis pelo callsite.
    - ActiveAgentsPanel.tsx defaults `title`/`emptyMessage` traduzidos; "Live now"/"Finished {time}"/"Started {time}" + plurals "X execução/execuções a mais" via i18next plural.
  </behavior>
  <action>
    **Read first:** `ui/src/components/StatusBadge.tsx`, `ui/src/pages/AgentDetail.tsx` (focar em linhas 622, 861-887, 932-1003, 947-952, 982-1003, 3037, 3422 — toda esta superfície precisa ser tocada), `ui/src/components/AgentProperties.tsx` (43-92), `ui/src/components/AgentActionButtons.tsx` (todo, 51 LOC), `ui/src/components/ActiveAgentsPanel.tsx` (41, 47, 110, 158), `ui/src/components/PageTabBar.tsx` (verificar interface — espera-se prop `tabs: { id, label }[]`), `ui/src/components/StatusBadge.test.tsx` (se existir, preserve).

    1. **Refactor `ui/src/components/StatusBadge.tsx`** conforme bloco `<interfaces>` — adicionar prop opcional `label`, render `{label ?? status.replace(/_/g, " ")}`. NÃO mudar tipo `status: string` (preserva consumidores em issues/runs/workspaces).

    2. **Migrar `ui/src/pages/AgentDetail.tsx`** (4248 LOC, ~280 strings) — operação cirúrgica:
       a. Estender `useTranslation` callsite existente (linha 622 e 3037) para namespaces `["agents", "common"]` (note: 3037 deve ser preservado para `common` — adicionar `agents` se necessário no callsite que renderiza ações de status/role/breadcrumb/tabs).
       b. Importar `AGENT_STATUS_KEY`, `AGENT_ROLE_KEY` de `../lib/agent-keys`.
       c. Substituir `<StatusBadge status={agent.status} />` por `<StatusBadge status={agent.status} label={t(AGENT_STATUS_KEY[agent.status as AgentStatus])} />`.
       d. Substituir labels de role hardcoded (`AGENT_ROLE_LABELS[agent.role]` server-side) por `t(AGENT_ROLE_KEY[agent.role as AgentRole])` — usado em header e organograma node se aplicável.
       e. Breadcrumbs (linhas 861-887): "Agents" → `t("agents:title")`, "Runs" → `t("agents:detail.tabs.runs")`, sub-rotas como `Run ${urlRunId.slice(0,8)}` → preservar id encurtado, traduzir só a label "Run". "Instructions"/"Configuration"/"Runs"/"Budget"/"Dashboard" → `t("agents:detail.tabs.{slug}")`.
       f. PageTabBar tabs prop: gerar `[{ id: "dashboard", label: t("agents:detail.tabs.dashboard") }, ...]` para os 5 tabs.
       g. Action buttons (linhas 932-1003): "Assign Task" → `t("agents:actions.assign-task")`; RunButton label override → `t("agents:actions.run-heartbeat")`; AgentActionButtons consumed via prop label override; "Live" badge (962) → `t("agents:detail.header.live-badge")`; "Copy Agent ID" → `t("agents:actions.copy-agent-id")`; "Reset Sessions" → `t("agents:actions.reset-sessions")`; "Delete agent" → `t("agents:actions.delete-agent")`.
       h. Status badge fallback: `agent.status` enum coverage exact — defensive lookup `AGENT_STATUS_KEY[agent.status as AgentStatus] ?? "agents:status.idle"`.
       i. NÃO tocar em `payload.message` ou `transcript.text` (output do modelo — Pitfall do RESEARCH).
       j. NÃO traduzir IDs encurtados (`run.id.slice(0,8)`).

    3. **Migrar `ui/src/components/AgentProperties.tsx`** (43-92): `useTranslation(["agents", "common"])` no component. Labels: Status/Role/Title/Adapter/Session/Last error/Last heartbeat/Reports to/Created → `t("agents:detail.properties.{slug}")`. Status enum value → `t(AGENT_STATUS_KEY[status])`.

    4. **Migrar `ui/src/components/AgentActionButtons.tsx`**: prop default `label = "Run now"` → no callsite passar `t("agents:actions.run-now")`; "Resume" e "Pause" → no callsite passar `t("agents:actions.resume")` / `t("agents:actions.pause")`. Mudança: tornar `label` REQUIRED ou aceitar `label?` com fallback inglês — preferir REQUIRED (consistência com pattern de `StatusBadge.label`).

    5. **Migrar `ui/src/components/ActiveAgentsPanel.tsx`**:
       - `useTranslation(["agents", "common"])` no component
       - defaults `title = "Agents"` → `t("agents:panel.active-agents")` (passado via prop ou default no component)
       - `emptyMessage = "No recent agent runs."` → `t("agents:panel.no-recent-runs")`
       - linha 158 "Live now" → `t("agents:panel.live-now")`; "Finished {time}" → `t("agents:panel.finished-relative", { time })`; "Started {time}" → `t("agents:panel.started-relative", { time })`
       - linha 110 plural "X more active/recent run(s)" → `t("agents:panel.more-runs", { count: hiddenRunCount })` — JSON tem `more-runs_one` / `more-runs_other` (Pitfall 6 — usar `_one`/`_other` direto, NÃO ternário).

    6. **Promover `it.todo` → `it` em `AgentDetail.i18n.test.tsx`** — escrever 3 describe blocks com assertions reais (RTL `screen.getByText("Painel")`, etc.) em pt-BR, depois `i18n.changeLanguage("en-US")` e re-assertar variantes inglês ("Dashboard"/"Configuration"). Reusar I18nextProvider + i18n config helpers da Fase 9 (`CompanySettings.i18n.test.tsx`).

    7. **NÃO tocar em** consumidores de `StatusBadge` fora do file list (issues/runs/workspaces/DesignGuide) — fallback preserva comportamento (Pitfall 1).

    Implementa AGENT-MSG-01 (status messages + role labels) + AGENT-MSG-03 (panel prompts UI parcial) — Patterns 1 + 2 do RESEARCH.md.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/components/__tests__/StatusBadge.label.test.tsx src/pages/__tests__/AgentDetail.i18n.test.tsx src/i18n/__tests__/missing-keys.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - StatusBadge aceita prop opcional `label`; 2/2 cases GREEN
    - AgentDetail.tsx renderiza header/tabs/breadcrumbs/actions em pt-BR via t()
    - AgentDetail.i18n.test.tsx 3 describe blocks GREEN com toggle pt-BR ↔ en-US
    - AgentProperties.tsx + AgentActionButtons.tsx + ActiveAgentsPanel.tsx migrados
    - Plural _one/_other resolve corretamente em ActiveAgentsPanel
    - Detector missing-keys GREEN; typecheck preserva apenas pre-existing baseline (ActivityRow:42 + recovery/service:459)
    - Suite UI completa GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar AgentConfigForm + agent-config-primitives + NewAgentDialog + Agents + NewAgent (forms + dialogs)</name>
  <files>
    ui/src/components/AgentConfigForm.tsx,
    ui/src/components/agent-config-primitives.tsx,
    ui/src/components/NewAgentDialog.tsx,
    ui/src/pages/Agents.tsx,
    ui/src/pages/NewAgent.tsx,
    ui/src/i18n/locales/pt-BR/agents.json,
    ui/src/i18n/locales/en-US/agents.json
  </files>
  <behavior>
    - AgentConfigForm renderiza ~31 form labels + save/saving/saved CTAs em pt-BR.
    - agent-config-primitives.tsx perde o const `help` (substituído por t() inline pelos consumidores) — eliminar export ou substituir export por factory que retorna `Record<string, string>` populado dinamicamente.
    - NewAgentDialog renderiza header "Adicionar novo agente" + recomendação CEO + advanced toggle + back CTA em pt-BR.
    - Agents.tsx (listing) renderiza title "Agentes", filter options "Ativo"/"Pausado"/"Erro", action "Novo agente".
    - NewAgent.tsx (wrapper) renderiza breadcrumb e header em pt-BR.
    - Toggle pt-BR ↔ en-US devolve forms e dialog para inglês imediatamente.
    - i18next plural (se houver) resolve corretamente para forms (improvável aqui — singulares).
  </behavior>
  <action>
    **Read first:** `ui/src/components/AgentConfigForm.tsx` (1510 LOC inteiro), `ui/src/components/agent-config-primitives.tsx` (especialmente const `help` linhas 22-60 e `adapterLabels` linha 64), `ui/src/components/NewAgentDialog.tsx` (210 LOC), `ui/src/pages/Agents.tsx` (408 LOC), `ui/src/pages/NewAgent.tsx` (335 LOC), `ui/src/components/HintIcon.tsx` (verificar interface).

    1. **Migrar `ui/src/components/AgentConfigForm.tsx`** (1510 LOC):
       a. Adicionar `useTranslation(["agents", "common"])` no top-level component
       b. Identificar todos os ~31 form labels via grep `<label\|<Label\|placeholder=\|aria-label=` no arquivo (read-and-replace cycle)
       c. Substituir cada label por `t("agents:config.{kebab-key}")` — kebab-keys derivadas do field name (e.g. `name` → `t("agents:config.field.name")`, `role` → `t("agents:config.field.role")`, `reportsTo` → `t("agents:config.field.reports-to")`, `capabilities` → `t("agents:config.field.capabilities")`, `adapterType` → `t("agents:config.field.adapter-type")`, etc.)
       d. **Decisão (a) sobre `help` const** (Pitfall 3): apagar o `help` const exportado e usar `t("agents:config.help.{key}")` em cada `<HintIcon text={t(...)} />`. Adicionar suas chaves correspondentes ao agents.json `config.help.*` se a Tarefa 0 não cobriu todas as 32 chaves — verificar contagem; estender JSON se faltar.
       e. Save CTAs: "Save changes" → `t("agents:config.save")`; "Saving…" → `t("agents:config.saving")`; "Saved" → `t("agents:config.saved")`; "Save failed" → `t("agents:config.save-failed")`.
       f. NÃO traduzir adapter types literais (`claude_local`, `process`, `http`) — são identificadores. NÃO traduzir display labels via `getAdapterDisplay` (Pitfall RESEARCH §"Anti-Patterns"). Verificar `adapterLabels` permanece inglês — não tocar.

    2. **Migrar `ui/src/components/agent-config-primitives.tsx`** (Pitfall 3):
       a. Apagar o const `help: Record<string, string>` exportado (linhas 22-60). Verificar primeiro se algum consumidor fora de `AgentConfigForm` o importa via `grep -r "help" ui/src/`; se sim, atualizar consumer também.
       b. Manter `Field`, `HintIcon`, `adapterLabels`, `getAdapterDisplay` intactos.
       c. Estender `agents.json` `config.help.*` com TODAS as 32 chaves do `help` original — extrair chaves e valores antes de apagar (snapshot mental do diff).

    3. **Migrar `ui/src/components/NewAgentDialog.tsx`**:
       a. `useTranslation(["agents", "common"])` no component
       b. Strings (linhas 118, 140-150, 158, 170-174, 193) → `t("agents:new-agent-dialog.{slug}")`:
          - "Add a new agent" → `t("agents:new-agent-dialog.header")`
          - CEO recommendation → `t("agents:new-agent-dialog.ceo-recommendation")`
          - "Ask the CEO to create a new agent" → `t("agents:new-agent-dialog.ask-ceo-cta")`
          - "I want advanced configuration myself" → `t("agents:new-agent-dialog.advanced-link")`
          - "Back" → `t("agents:new-agent-dialog.back")`
          - "Choose your adapter type for advanced setup." → `t("agents:new-agent-dialog.advanced-prompt")`
          - "Recommended" → `t("agents:new-agent-dialog.recommended-tag")`
          - CEO issue title → `t("agents:new-agent-dialog.ceo-issue-title")`
          - CEO issue description → `t("agents:new-agent-dialog.ceo-issue-description")`

    4. **Migrar `ui/src/pages/Agents.tsx`**:
       a. `useTranslation(["agents", "common"])` no component
       b. linha 112 setBreadcrumbs `[{ label: "Agents" }]` → `[{ label: t("agents:title") }]`
       c. linhas 133-135 filter options "Active"/"Paused"/"Error" → usar `t(AGENT_STATUS_KEY[status])` (importar de `../lib/agent-keys`)
       d. linha 212 action="New Agent" → `t("agents:actions.new-agent")`

    5. **Migrar `ui/src/pages/NewAgent.tsx`** (335 LOC, ~5 strings):
       a. `useTranslation(["agents", "common"])`
       b. Header e breadcrumb → `t("agents:title")` + `t("agents:actions.new-agent")` ou apropriado

    6. **Estender agents.json** se grep durante migração revelou strings não previstas no Pattern 4 do RESEARCH.md — adicionar como sub-key no sub-tree apropriado (ambos pt-BR + en-US, paridade obrigatória). Documentar no SUMMARY se houver desvio.

    7. **Verificar grep final** em arquivos modificados: regex `"[A-Z][a-z][a-zA-Z ]{3,30}"` ou similar não deve retornar strings UI-facing fora de literais técnicos (ids, labels de protocolo, `console.log`).

    Implementa AGENT-MSG-03 (forms + dialogs + listing) — completa pareamento com Tarefa 1.
  </action>
  <verify>
    <automated>cd ui && pnpm exec vitest run src/i18n/__tests__/missing-keys.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - AgentConfigForm.tsx ~31 strings via t() (incluindo 32 help dict entries via t())
    - agent-config-primitives.tsx const `help` removido (substituído por t() callsites)
    - NewAgentDialog.tsx 9+ strings traduzidas
    - Agents.tsx + NewAgent.tsx migrados com STATUS_KEY lookup
    - agents.json estendido se houve diff vs Pattern 4 — paridade pt-BR/en-US preservada
    - Detector missing-keys CI=true exit 0
    - typecheck preserva apenas baseline (ActivityRow:42 + recovery/service:459)
    - Suite UI completa GREEN
  </done>
</task>

</tasks>

<verification>
**Per task:** comando `<automated>` específico passa.

**Phase plan gate:**
- `cd ui && CI=true pnpm exec vitest run -- missing-keys` exit 0
- `cd ui && pnpm exec vitest run src/pages/__tests__/AgentDetail.i18n.test.tsx src/components/__tests__/StatusBadge.label.test.tsx` 100% GREEN
- `cd ui && pnpm typecheck` apenas pre-existing errors (ActivityRow.tsx:42 + recovery/service.ts:459)
- `cd ui && pnpm exec vitest run` suite completa GREEN (sem regressões)
- `pnpm -r typecheck` apenas pre-existing errors
- grep `"\bagents:status\.|agents:role\.|agents:detail\.|agents:panel\.|agents:config\.|agents:new-agent-dialog"` em `ui/src/pages/AgentDetail.tsx` retorna 8+ matches
- grep raw English strings em files migrados (regex `>[A-Z][a-z][a-zA-Z ]{4,}<` no JSX) retorna 0 matches relevantes (excluindo brand "Paperclip", IDs, technical tokens)

**Anti-regression:** consumidores legacy de `StatusBadge` (issues/runs/workspaces/DesignGuide) compilam e renderizam (typecheck + grep `<StatusBadge` cross-file).
</verification>

<success_criteria>
- AGENT-MSG-01 satisfeito ao código-nível: status messages dos agentes em painéis e listings em pt-BR via AGENT_STATUS_KEY lookup; StatusBadge contract atualizado preservando consumidores legacy
- AGENT-MSG-03 satisfeito ao código-nível: prompts UI dos painéis (botões/headers/labels/abas/forms/dialogs) renderizam via t()
- agents.json populado de `{}` para ~110 leaf keys (pt-BR + en-US, paridade 100%)
- ui/src/lib/agent-keys.ts criado e consumido por AgentDetail/Agents/AgentProperties
- 9 arquivos TSX migrados (AgentDetail, Agents, NewAgent, AgentConfigForm, AgentProperties, AgentActionButtons, NewAgentDialog, agent-config-primitives, ActiveAgentsPanel)
- 2 arquivos de teste novos (StatusBadge.label.test.tsx + AgentDetail.i18n.test.tsx) GREEN
- Pluralização _one/_other ativa em panel.more-runs
- HUMAN-UAT-10-01 (visual scan) preserved para fase de fechamento
</success_criteria>

<output>
After completion, create `.planning/phases/10-mensagens-dos-agentes/10-01-SUMMARY.md` documentando:
- Final key count em agents.json (pt-BR + en-US)
- Lista de arquivos TSX migrados + LOC delta
- Decisões de granularidade (sub-trees JSON, opcional vs required em StatusBadge.label, decisão (a) help const)
- Auto-fix deviations (Rule 1/3 blockers se houver)
- Status do detector missing-keys CI
- HUMAN-UAT-10-01 checklist routing
</output>
