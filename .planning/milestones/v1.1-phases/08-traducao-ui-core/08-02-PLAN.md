---
phase: 08-traducao-ui-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/projects.json
  - ui/src/i18n/locales/en-US/projects.json
  - ui/src/pages/Projects.tsx
  - ui/src/pages/ProjectDetail.tsx
  - ui/src/components/NewProjectDialog.tsx
  - ui/src/components/ProjectProperties.tsx
  - ui/src/components/ProjectWorkspacesContent.tsx
  - ui/src/components/ProjectWorkspaceSummaryCard.tsx
  - ui/src/pages/__tests__/Projects.translation.test.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Usuário com locale=pt-BR navega em /projects e vê 'Adicionar projeto', empty states e dialog 'Novo projeto' em português"
    - "ProjectDetail.tsx renderiza tabs (Tarefas/Visão geral/Workspaces/Configuração/Orçamento), labels (Status/Data alvo) e mensagens de toast (archive/unarchive) em pt-BR"
    - "NewProjectDialog renderiza form completo (nome, descrição, repo URL, pasta local, status enum) e tooltips em pt-BR"
    - "Toasts de archive/unarchive interpolam name corretamente via t('projects:toast.archived', { name }) — RESEARCH Pitfall 3"
    - "Detector missing-keys.test.ts permanece GREEN com CI=true após edits"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/projects.json"
      provides: "Dicionário pt-BR do namespace projects com ~50 chaves"
      contains: "title"
    - path: "ui/src/i18n/locales/en-US/projects.json"
      provides: "Dicionário en-US do namespace projects espelhado"
      contains: "title"
    - path: "ui/src/pages/__tests__/Projects.translation.test.tsx"
      provides: "RTL test asserindo render pt-BR e en-US da página Projects"
      min_lines: 30
  key_links:
    - from: "ui/src/pages/Projects.tsx"
      to: "ui/src/i18n/locales/pt-BR/projects.json"
      via: "useTranslation(['projects', 'common']) + t('projects:...')"
      pattern: "useTranslation\\(\\[\"projects\""
    - from: "ui/src/components/NewProjectDialog.tsx"
      to: "ui/src/i18n/locales/pt-BR/projects.json"
      via: "t('projects:new-project.*') incluindo enum status via map estático"
      pattern: "t\\(\"projects:new-project"
    - from: "ui/src/pages/ProjectDetail.tsx"
      to: "setBreadcrumbs com t('projects:title')"
      via: "Pitfall 2 — breadcrumb traduzido na página, não em BreadcrumbBar"
      pattern: "setBreadcrumbs.*t\\("
---

<objective>
Traduzir a superfície de Projects (UI-02) para pt-BR e en-US, cobrindo `Projects.tsx` (lista, 87 LOC), `ProjectDetail.tsx` (711 LOC com 5 tabs), `NewProjectDialog.tsx` (447 LOC, dialog completo com 5 status enum + tooltips), `ProjectProperties.tsx`, `ProjectWorkspacesContent.tsx` e `ProjectWorkspaceSummaryCard.tsx`. Substituir strings hardcoded por `t()` no namespace `projects`, popular dicionários `pt-BR/projects.json` + `en-US/projects.json` com ~50 chaves, e adicionar teste RTL.

Purpose: UI-02 cobre o segundo fluxo mais frequente (criar/abrir/configurar projetos). Modal de criação tem alta densidade de strings (form + tooltips + status enum) e toasts com interpolação são pitfall comum (RESEARCH Pitfall 3).

Output: 2 arquivos JSON populados, 6 arquivos TSX migrados, 1 arquivo de teste RTL.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-traducao-ui-core/08-CONTEXT.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/08-traducao-ui-core/08-VALIDATION.md
@ui/src/pages/ProfileSettings.tsx
@ui/src/i18n/index.ts
@ui/src/i18n/__tests__/missing-keys.test.ts

<interfaces>
From ui/src/pages/ProfileSettings.tsx:3,26:
```typescript
import { useTranslation } from "react-i18next";
const { t } = useTranslation(["projects", "common"]);
```

From RESEARCH §Pattern 2 (toast interpolation example for ProjectDetail.tsx:339):
```typescript
// CORRETO:
pushToast({ title: t("projects:toast.archived", { name }) });
// JSON: "archived": "\"{{ name }}\" foi arquivado"

// ERRADO (concatenação manual quebra interpolação):
pushToast({ title: t("projects:toast.archived") + ` "${name}"` });
```

From RESEARCH Pitfall 1 (status enum em arrays JS):
```typescript
const projectStatusValues = ["backlog", "planned", "in_progress", "completed", "cancelled"] as const;
{projectStatusValues.map((v) => (
  <Item key={v}>{t(`projects:status.${v.replace("_", "-")}`)}</Item>
))}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Bootstrap dicionários projects (pt-BR + en-US)</name>
  <files>ui/src/i18n/locales/pt-BR/projects.json, ui/src/i18n/locales/en-US/projects.json</files>
  <read_first>
    - ui/src/i18n/locales/pt-BR/projects.json (Phase 7 deixou {})
    - ui/src/i18n/locales/en-US/projects.json (Phase 7 deixou {})
    - ui/src/pages/Projects.tsx:1-87 (Add Project, Select a company to view projects., No projects yet.)
    - ui/src/pages/ProjectDetail.tsx:1-712 (tabs Issues/Overview/Workspaces/Configuration/Budget; Loading workspaces...; Paused by budget hard stop; placeholders Add a description.../Project; labels Status/Target Date)
    - ui/src/components/NewProjectDialog.tsx:42-444 (status enum, form fields, tooltips, error/loading states)
  </read_first>
  <action>
    Conforme RESEARCH §Pattern 2 (Projects surface mapping) e D-01 (extração manual).

    Criar `ui/src/i18n/locales/pt-BR/projects.json` com bootstrap mínimo cobrindo as áreas mapeadas:

    ```json
    {
      "title": "Projetos",
      "actions": {
        "add": "Adicionar projeto",
        "create": "Criar projeto",
        "creating": "Criando…",
        "create-failed": "Falha ao criar projeto.",
        "archive": "Arquivar",
        "unarchive": "Desarquivar"
      },
      "status": {
        "backlog": "Backlog",
        "planned": "Planejado",
        "in-progress": "Em andamento",
        "completed": "Concluído",
        "cancelled": "Cancelado"
      },
      "tabs": {
        "issues": "Tarefas",
        "overview": "Visão geral",
        "workspaces": "Workspaces",
        "configuration": "Configuração",
        "budget": "Orçamento"
      },
      "labels": {
        "status": "Status",
        "target-date": "Data alvo",
        "description": "Descrição"
      },
      "new-project": {
        "title": "Novo projeto",
        "name-placeholder": "Nome do projeto",
        "description-placeholder": "Adicionar descrição…",
        "repo-url-label": "URL do repositório",
        "repo-url-placeholder": "https://github.com/org/repo",
        "local-folder-label": "Pasta local",
        "local-folder-placeholder": "/caminho/absoluto/para/workspace",
        "optional": "opcional",
        "tooltip-repo": "Vincule um repositório GitHub para que agentes possam clonar, ler e fazer push de código deste projeto.",
        "tooltip-local-folder": "Defina um caminho absoluto nesta máquina onde agentes locais lerão e escreverão arquivos.",
        "goal-add": "+ Meta",
        "goal-none": "Sem meta",
        "goal-all-selected": "Todas as metas já selecionadas.",
        "target-date-placeholder": "Data alvo"
      },
      "empty-state": {
        "no-projects": "Nenhum projeto ainda.",
        "select-company": "Selecione uma empresa para ver projetos."
      },
      "loading": {
        "workspaces": "Carregando workspaces…"
      },
      "toast": {
        "archived": "\"{{ name }}\" foi arquivado",
        "unarchived": "\"{{ name }}\" foi desarquivado",
        "archive-failed": "Falha ao arquivar projeto",
        "unarchive-failed": "Falha ao desarquivar projeto"
      },
      "paused-by-budget": "Pausado por limite de orçamento"
    }
    ```

    Estender com chaves adicionais conforme strings descobertas no grep das tarefas seguintes — alvo total ~50 entradas.

    Espelhar TODAS as chaves em `ui/src/i18n/locales/en-US/projects.json` com strings em inglês (Projects, Add project, Create project, Creating…, Failed to create project., Backlog, Planned, In Progress, Completed, Cancelled, Issues, Overview, Workspaces, Configuration, Budget, etc.).

    Validar JSON válido em ambos os arquivos.

    NÃO traduzir "Paperclip", URLs, `tsx`, `json`, identificadores técnicos (D-DISCRETION).
  </action>
  <verify>
    <automated>node -e "['pt-BR','en-US'].forEach(l => JSON.parse(require('fs').readFileSync(`ui/src/i18n/locales/${l}/projects.json`,'utf8'))); console.log('OK 2 valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/i18n/locales/pt-BR/projects.json contains "title" (grep-checkable)
    - ui/src/i18n/locales/pt-BR/projects.json contains "new-project" (grep-checkable)
    - ui/src/i18n/locales/pt-BR/projects.json contains "status" with sub-keys backlog/planned/in-progress/completed/cancelled (grep-checkable)
    - ui/src/i18n/locales/pt-BR/projects.json contains "{{ name }}" (interpolation placeholder) (grep-checkable)
    - ui/src/i18n/locales/en-US/projects.json contains "title"
    - 2 arquivos JSON válidos
    - pt-BR e en-US têm o mesmo conjunto de keys
  </acceptance_criteria>
  <done>2 dicionários projects populados com ~50 chaves cada; JSON válido; placeholders {{ name }} presentes para interpolação dos toasts</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar Projects.tsx + ProjectDetail.tsx + NewProjectDialog.tsx + propriedades para t()</name>
  <files>ui/src/pages/Projects.tsx, ui/src/pages/ProjectDetail.tsx, ui/src/components/NewProjectDialog.tsx, ui/src/components/ProjectProperties.tsx, ui/src/components/ProjectWorkspacesContent.tsx, ui/src/components/ProjectWorkspaceSummaryCard.tsx</files>
  <read_first>
    - ui/src/pages/ProfileSettings.tsx:3,26 (padrão canônico)
    - ui/src/pages/Projects.tsx (87 LOC — strings simples)
    - ui/src/pages/ProjectDetail.tsx (711 LOC — atenção a setBreadcrumbs e linhas 339-348 toasts)
    - ui/src/components/NewProjectDialog.tsx (447 LOC — atenção array de status enum em ~linhas 42-48)
    - ui/src/components/ProjectProperties.tsx
    - ui/src/components/ProjectWorkspacesContent.tsx
    - ui/src/components/ProjectWorkspaceSummaryCard.tsx
  </read_first>
  <action>
    Conforme D-01 e RESEARCH Pitfalls #1, #2, #3.

    Para cada um dos 6 arquivos:

    1. Adicionar import:
       ```typescript
       import { useTranslation } from "react-i18next";
       ```

    2. Hook no início do componente:
       ```typescript
       const { t } = useTranslation(["projects", "common"]);
       ```

    3. Substituir strings hardcoded por `t()`:
       - JSX text content: `<h1>Projects</h1>` → `<h1>{t("projects:title")}</h1>`
       - Atributos: `placeholder="Add a description..."` → `placeholder={t("projects:new-project.description-placeholder")}`

    4. **Pitfall 2 — Breadcrumbs:** `Projects.tsx` provavelmente tem `setBreadcrumbs([{ label: "Projects" }])`. Substituir por:
       ```typescript
       setBreadcrumbs([{ label: t("projects:title") }]);
       ```
       e atualizar dependency array para `[setBreadcrumbs, t]`. ProjectDetail.tsx tem breadcrumbs com nome dinâmico do projeto (passthrough literal — `{ label: project.name }` permanece como está; só a estrutura precedente é traduzida se houver "Project" como crumb).

    5. **Pitfall 3 — Toast interpolation em ProjectDetail.tsx:339-348:**
       ```typescript
       // ANTES:
       pushToast({ title: `"${name}" has been archived` });
       // DEPOIS:
       pushToast({ title: t("projects:toast.archived", { name }) });
       ```
       Aplicar mesma transformação para unarchived/archive-failed/unarchive-failed.

    6. **Pitfall 1 — Status enum array em NewProjectDialog.tsx:42-48:**
       Refatorar de:
       ```typescript
       const projectStatuses = [
         { value: "backlog", label: "Backlog" },
         { value: "planned", label: "Planned" },
         // ...
       ];
       ```
       Para:
       ```typescript
       const PROJECT_STATUS_VALUES = ["backlog", "planned", "in_progress", "completed", "cancelled"] as const;
       // no render do dropdown:
       {PROJECT_STATUS_VALUES.map((v) => (
         <Option key={v} value={v}>{t(`projects:status.${v.replace("_", "-")}`)}</Option>
       ))}
       ```
       (regex de missing-keys aceita `projects:status.backlog` mas não `projects:status.in_progress` — kebab-case obrigatório).

    7. Tooltips do NewProjectDialog (linhas ~219-444, RESEARCH §Pattern 2): traduzir mesmo nesta fase porque são parte da superfície core de Projects (não são tooltips inline em ações que ficam para Fase 9). Exceção semântica clara — RESEARCH explicitamente lista esses tooltips em scope.

    8. Strings dinâmicas complexas: marcar com `// i18n: review — complex template`, cobrir caso simples.

    9. Para CADA chave nova adicionada, GARANTIR existência em pt-BR e en-US JSON.

    Validar a cada arquivo grande:
    ```bash
    cd ui && pnpm typecheck
    cd ui && CI=true pnpm test:run -- missing-keys
    ```
  </action>
  <verify>
    <automated>cd ui && pnpm typecheck && CI=true pnpm test:run -- missing-keys</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/Projects.tsx contains "useTranslation" (grep-checkable)
    - ui/src/pages/Projects.tsx contains 't("projects:' (grep-checkable, 2+ occurrences)
    - ui/src/pages/ProjectDetail.tsx contains 't("projects:tabs.' (grep-checkable)
    - ui/src/pages/ProjectDetail.tsx contains 't("projects:toast.archived"' (grep-checkable, with interpolation)
    - ui/src/components/NewProjectDialog.tsx contains 't(`projects:status.${' OR 't(\"projects:status.' (grep-checkable)
    - ui/src/components/NewProjectDialog.tsx contains 't("projects:new-project.' (grep-checkable, 5+ occurrences)
    - `cd ui && pnpm typecheck` exits 0
    - `cd ui && CI=true pnpm test:run -- missing-keys` exits 0
  </acceptance_criteria>
  <done>6 arquivos migrados; setBreadcrumbs usa t(); toasts usam interpolação via t(key, { name }); status enum refatorado para values + lookup; missing-keys CI verde</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: RTL test Projects.translation.test.tsx</name>
  <files>ui/src/pages/__tests__/Projects.translation.test.tsx</files>
  <read_first>
    - ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx (provider wrapping pattern)
    - ui/src/pages/Projects.tsx (props mínimas e route context necessário)
    - ui/src/i18n/locales/pt-BR/projects.json (chaves para asserir)
  </read_first>
  <action>
    Conforme VALIDATION.md §Wave 0 Requirements (linha 57), criar `ui/src/pages/__tests__/Projects.translation.test.tsx`.

    Estrutura espelhada do padrão de Phase 7:

    ```typescript
    // @vitest-environment jsdom
    import { describe, it, expect, beforeEach } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
    import { I18nextProvider } from "react-i18next";
    import { MemoryRouter } from "react-router-dom"; // se Projects.tsx usar router
    import i18n from "@/i18n";
    import { Projects } from "@/pages/Projects";

    function renderWithProviders(ui: React.ReactElement) {
      const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
      return render(
        <QueryClientProvider client={queryClient}>
          <I18nextProvider i18n={i18n}>
            <MemoryRouter>{ui}</MemoryRouter>
          </I18nextProvider>
        </QueryClientProvider>
      );
    }

    describe("Projects page — translation render (UI-02)", () => {
      beforeEach(async () => {
        await i18n.changeLanguage("pt-BR");
      });

      it("renderiza 'Adicionar projeto' em pt-BR", async () => {
        renderWithProviders(<Projects />);
        expect(screen.getByText(/Adicionar projeto|Projetos|Selecione uma empresa/i)).toBeInTheDocument();
      });

      it("renderiza 'Add project' em en-US", async () => {
        await i18n.changeLanguage("en-US");
        renderWithProviders(<Projects />);
        expect(screen.getByText(/Add project|Projects|Select a company/i)).toBeInTheDocument();
      });
    });
    ```

    Se Projects.tsx requer providers adicionais (CompanyContext, BreadcrumbContext), inspecionar imports e mockar os contextos via stub mínimo. Buscar pattern em `ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx` que já lidou com mesmo problema.

    Garantir que o teste passa GREEN antes de finalizar.
  </action>
  <verify>
    <automated>cd ui && pnpm test:run -- Projects.translation</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/__tests__/Projects.translation.test.tsx exists
    - Test file contains "i18n.changeLanguage" (grep-checkable)
    - Test file contains "pt-BR" AND "en-US" (grep-checkable)
    - `cd ui && pnpm test:run -- Projects.translation` exits 0
  </acceptance_criteria>
  <done>Arquivo de teste criado com 2+ casos; teste GREEN; cobertura UI-02 truth #1 fechada em automatizado</done>
</task>

</tasks>

<verification>
- `cd ui && pnpm typecheck` — exit 0
- `cd ui && CI=true pnpm test:run -- missing-keys` — exit 0
- `cd ui && pnpm test:run -- Projects.translation` — exit 0
- `cd ui && pnpm test:run` — full UI suite GREEN
- grep manual em Projects.tsx + ProjectDetail.tsx + NewProjectDialog.tsx por strings capitalizadas em JSX que NÃO sejam dentro de t(...) — zero matches
</verification>

<success_criteria>
- 2 arquivos JSON populados (pt-BR/projects, en-US/projects) com ~50 chaves espelhadas
- 6 arquivos TSX migrados para `useTranslation` + `t()`
- Toasts de archive/unarchive interpolam name via `t(key, { name })` corretamente (Pitfall 3)
- NewProjectDialog status enum usa lookup via map sobre values constantes (Pitfall 1)
- setBreadcrumbs em Projects.tsx usa `t("projects:title")` (Pitfall 2)
- 1 teste RTL novo cobrindo render em ambos locales
- Detector missing-keys CI=true GREEN
- UI-02 satisfeito (truth #1: usuário em pt-BR vê projects sem strings em inglês)
</success_criteria>

<output>
After completion, create `.planning/phases/08-traducao-ui-core/08-02-SUMMARY.md`.
</output>
