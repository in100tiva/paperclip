---
phase: 08-traducao-ui-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/inbox.json
  - ui/src/i18n/locales/en-US/inbox.json
  - ui/src/i18n/locales/pt-BR/common.json
  - ui/src/i18n/locales/en-US/common.json
  - ui/src/pages/Inbox.tsx
  - ui/src/components/IssuesList.tsx
  - ui/src/components/IssueRow.tsx
  - ui/src/components/IssueFiltersPopover.tsx
  - ui/src/components/IssueColumns.tsx
  - ui/src/components/IssueGroupHeader.tsx
  - ui/src/components/SwipeToArchive.tsx
  - ui/src/components/inbox/__tests__/InboxList.translation.test.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Usuário com locale=pt-BR navega em /inbox e vê tabs, filtros, ações em massa, search placeholder, empty state e linhas de issue todos em português"
    - "IssuesList.tsx (1311 LOC compartilhado entre Inbox/ProjectDetail/Issues page) renderiza strings via t() em ambos os locales sem strings hardcoded em inglês"
    - "Modal 'Mark all as read?' renderiza confirmação em pt-BR via t()"
    - "IssueFiltersPopover renderiza headers (Filters, Quick filters, Status, Priority, Assignee, Creator, Project, Labels, Workspace, Visibility) traduzidos"
    - "Detector missing-keys.test.ts permanece GREEN com process.env.CI=true após edits"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/inbox.json"
      provides: "Dicionário pt-BR do namespace inbox com ~120 chaves"
      contains: "title"
    - path: "ui/src/i18n/locales/en-US/inbox.json"
      provides: "Dicionário en-US do namespace inbox espelhado"
      contains: "title"
    - path: "ui/src/components/inbox/__tests__/InboxList.translation.test.tsx"
      provides: "RTL test com pt-BR ativo asserindo strings PT presentes"
      min_lines: 30
  key_links:
    - from: "ui/src/pages/Inbox.tsx"
      to: "ui/src/i18n/locales/pt-BR/inbox.json"
      via: "useTranslation(['inbox', 'common']) + t('inbox:...')"
      pattern: "useTranslation\\(\\[\"inbox\""
    - from: "ui/src/components/IssuesList.tsx"
      to: "ui/src/i18n/locales/pt-BR/inbox.json"
      via: "useTranslation hook chamando t('inbox:...')"
      pattern: "useTranslation\\(\\[\"inbox\""
    - from: "ui/src/components/IssueFiltersPopover.tsx"
      to: "ui/src/i18n/locales/pt-BR/inbox.json"
      via: "t('inbox:filters.*')"
      pattern: "t\\(\"inbox:filters"
---

<objective>
Traduzir a superfície de Inbox (UI-01) para pt-BR e en-US, cobrindo a página principal `Inbox.tsx` (2563 LOC) e seus componentes filhos compartilhados (`IssuesList.tsx`, `IssueRow.tsx`, `IssueFiltersPopover.tsx`, `IssueColumns.tsx`, `IssueGroupHeader.tsx`, `SwipeToArchive.tsx`). Substituir todas as strings hardcoded por chamadas `t()` no namespace `inbox`, popular dicionários `pt-BR/inbox.json` + `en-US/inbox.json` com ~120 chaves, e adicionar teste RTL que valida render em pt-BR.

Purpose: UI-01 é a superfície de maior tráfego do paperclip; tradução de Inbox entrega valor visível imediato a usuários com locale=pt-BR. `IssuesList.tsx` é shared component (Inbox + ProjectDetail + Issues page) — traduzir aqui evita re-edição em planos futuros (RESEARCH §Open Questions #2).

Output: 4 arquivos JSON populados, 6 arquivos TSX migrados para `t()`, 1 arquivo de teste RTL novo cobrindo InboxList em pt-BR.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-traducao-ui-core/08-CONTEXT.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/08-traducao-ui-core/08-VALIDATION.md
@ui/src/pages/ProfileSettings.tsx
@ui/src/i18n/index.ts
@ui/src/i18n/resources.ts
@ui/src/i18n/__tests__/missing-keys.test.ts

<interfaces>
<!-- Padrão canônico de useTranslation extraído de ProfileSettings.tsx:3,26 -->

From ui/src/pages/ProfileSettings.tsx:
```typescript
import { useTranslation } from "react-i18next";
const { t, i18n } = useTranslation(["settings", "common"]);
// ns padrão = primeiro do array; outros acessíveis com prefixo "ns:"
// Uso: t("settings:language.title"), t("common:save")
```

From ui/src/i18n/__tests__/missing-keys.test.ts:6 (regex aceitável):
```typescript
const T_CALL_RE = /\bt\(\s*["'`]([a-z0-9.\-]+(?::[a-z0-9.\-]+)?)["'`]/g;
// Aceita: kebab-case dot-notation com ns opcional ("inbox:filter.all")
// REJEITA: camelCase ("inbox:filterAll")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Bootstrap dicionários inbox + extensões common</name>
  <files>ui/src/i18n/locales/pt-BR/inbox.json, ui/src/i18n/locales/en-US/inbox.json, ui/src/i18n/locales/pt-BR/common.json, ui/src/i18n/locales/en-US/common.json</files>
  <read_first>
    - ui/src/i18n/locales/pt-BR/inbox.json (estado atual — Phase 7 deixou {})
    - ui/src/i18n/locales/en-US/inbox.json (estado atual — Phase 7 deixou {})
    - ui/src/i18n/locales/pt-BR/common.json (estado atual — Phase 7 deixou app-name/loading/save/cancel)
    - ui/src/pages/Inbox.tsx (grep `>[A-Z]` e `placeholder=`/`aria-label=`/`label=` para mapear ~120 strings)
    - ui/src/components/IssuesList.tsx (mesma extração)
    - ui/src/components/IssueFiltersPopover.tsx (Filters, Quick filters, Status, Priority, Assignee, Creator, Project, Labels, Workspace, Visibility, Live runs only, Hide routine runs)
    - ui/src/components/IssueRow.tsx (Mark as read, Dismiss from inbox)
  </read_first>
  <action>
    Conforme D-01 (Estratégia: manual via grep + edição inline) e RESEARCH §Pattern 1.

    Popular `ui/src/i18n/locales/pt-BR/inbox.json` com sub-trees mínimos cobrindo as áreas mapeadas em RESEARCH §Pattern 1 (sample bootstrap):

    ```json
    {
      "title": "Inbox",
      "tabs": {
        "mine": "Meus",
        "team": "Equipe",
        "everything": "Tudo"
      },
      "search": {
        "placeholder": "Buscar inbox…",
        "issues-placeholder": "Buscar tarefas…",
        "assignees-placeholder": "Buscar responsáveis…"
      },
      "categories": {
        "all": "Todas as categorias",
        "join-requests": "Pedidos de entrada",
        "approvals": "Aprovações",
        "failed-runs": "Execuções falhadas",
        "alerts": "Alertas"
      },
      "filters": {
        "title": "Filtros",
        "quick-filters": "Filtros rápidos",
        "status": "Status",
        "priority": "Prioridade",
        "assignee": "Responsável",
        "creator": "Criador",
        "project": "Projeto",
        "labels": "Etiquetas",
        "workspace": "Workspace",
        "visibility": "Visibilidade",
        "live-runs-only": "Apenas execuções ao vivo",
        "hide-routine-runs": "Ocultar execuções rotineiras"
      },
      "actions": {
        "mark-as-read": "Marcar como lido",
        "mark-all-as-read": "Marcar tudo como lido",
        "mark-all-as-read-confirm": "Marcar tudo como lido?",
        "dismiss": "Dispensar",
        "dismiss-from-inbox": "Dispensar do inbox",
        "archive": "Arquivar"
      },
      "approval-status": {
        "all": "Todos os status",
        "actionable": "Requer ação",
        "resolved": "Resolvido"
      },
      "empty-state": {
        "title": "Nenhum item no inbox",
        "no-failed-runs": "Sem execuções falhadas",
        "no-approvals": "Sem aprovações pendentes"
      },
      "failed-run": {
        "label-with-agent": "Execução falhada — {{agentName}}",
        "label": "Execução falhada"
      },
      "group": {
        "today": "Hoje",
        "yesterday": "Ontem",
        "this-week": "Esta semana",
        "earlier": "Anteriores"
      },
      "row": {
        "swipe-to-archive": "Deslizar para arquivar",
        "loading": "Carregando…"
      }
    }
    ```

    Estender com chaves adicionais conforme strings descobertas no grep das tarefas seguintes — alvo total ~120 entradas. Estrutura sub-namespace fica a critério (kebab-case dot-notation obrigatório por causa do regex em missing-keys.test.ts:6).

    Espelhar TODAS as chaves em `ui/src/i18n/locales/en-US/inbox.json` com strings em inglês (Inbox, Mine, Team, Everything, Search inbox…, etc.).

    Estender `ui/src/i18n/locales/pt-BR/common.json` (e en-US/) com ~10 chaves de botões compartilhados que aparecem no Inbox (RESEARCH estimativa):
    ```json
    {
      "actions": {
        "select-all": "Selecionar todos",
        "deselect-all": "Limpar seleção",
        "apply": "Aplicar",
        "clear": "Limpar",
        "filter": "Filtrar"
      }
    }
    ```
    NÃO duplicar chaves já existentes em common.json (Phase 7 deixou `app-name`, `loading`, `save`, `cancel`).

    Validar que cada JSON é válido: `node -e "JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/inbox.json','utf8'))"` (e idem para os outros 3).

    NÃO traduzir "Paperclip" (D-NAV: nome do produto, hardcoded brand string).
  </action>
  <verify>
    <automated>node -e "['pt-BR','en-US'].forEach(l => ['inbox','common'].forEach(n => JSON.parse(require('fs').readFileSync(`ui/src/i18n/locales/${l}/${n}.json`,'utf8')))); console.log('OK 4 valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/i18n/locales/pt-BR/inbox.json contains "title" (grep-checkable)
    - ui/src/i18n/locales/pt-BR/inbox.json contains "filters" (grep-checkable)
    - ui/src/i18n/locales/pt-BR/inbox.json contains "categories" (grep-checkable)
    - ui/src/i18n/locales/en-US/inbox.json contains "title" (grep-checkable)
    - 4 arquivos JSON válidos (parse sem erro)
    - inbox.json em pt-BR e en-US têm o mesmo conjunto de keys (espelhamento)
  </acceptance_criteria>
  <done>4 dicionários (pt-BR/inbox, en-US/inbox, pt-BR/common, en-US/common) populados/estendidos; JSON válido; conjuntos espelhados entre pt-BR e en-US</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar Inbox.tsx + componentes filhos para t()</name>
  <files>ui/src/pages/Inbox.tsx, ui/src/components/IssuesList.tsx, ui/src/components/IssueRow.tsx, ui/src/components/IssueFiltersPopover.tsx, ui/src/components/IssueColumns.tsx, ui/src/components/IssueGroupHeader.tsx, ui/src/components/SwipeToArchive.tsx</files>
  <read_first>
    - ui/src/pages/ProfileSettings.tsx:1-50 (padrão canônico de import + useTranslation)
    - ui/src/pages/Inbox.tsx (todos os 2563 LOC — strings principais nas linhas 278, 292, 1881, 2034, 2067-2090 conforme RESEARCH §Sources)
    - ui/src/components/IssuesList.tsx (1311 LOC — header de grupo, headers de coluna, search placeholders, aria-labels)
    - ui/src/components/IssueRow.tsx:1-168
    - ui/src/components/IssueFiltersPopover.tsx (372 LOC — popover completo)
    - ui/src/components/IssueColumns.tsx (column picker, headers de coluna)
    - ui/src/components/IssueGroupHeader.tsx
    - ui/src/components/SwipeToArchive.tsx (~150 LOC — aria-labels mobile)
  </read_first>
  <action>
    Conforme D-01 (extração manual) e D-COVERAGE (incluir empty states, loading states, child components profundos, modais).

    Para cada um dos 7 arquivos:

    1. Adicionar import no topo:
       ```typescript
       import { useTranslation } from "react-i18next";
       ```

    2. Dentro do componente, no início (antes de hooks dependentes):
       ```typescript
       const { t } = useTranslation(["inbox", "common"]);
       ```
       (em IssuesList.tsx use ["inbox", "common"] já que é shared component — RESEARCH §Open Questions #2 confirma cobrir o componente inteiro mesmo que use também em ProjectDetail/Issues)

    3. Substituir cada string literal hardcoded encontrada via grep:
       - Strings JSX texto (`<h1>Inbox</h1>` → `<h1>{t("inbox:title")}</h1>`)
       - Atributos label/placeholder/aria-label/title (`placeholder="Search inbox…"` → `placeholder={t("inbox:search.placeholder")}`)
       - Confirmações modal (`window.confirm("Mark all as read?")` → `window.confirm(t("inbox:actions.mark-all-as-read-confirm"))`)
       - Strings com interpolação (RESEARCH Pitfall 3): `\`Failed run — ${agentName}\`` → `t("inbox:failed-run.label-with-agent", { agentName })`

    4. Para strings em arrays JS (RESEARCH Pitfall 1 — `IssueColumns.tsx` provavelmente tem column metadata array com labels):
       Refatorar para que array contenha apenas IDs estáveis e a label seja resolvida no render via `t()`:
       ```typescript
       const COLUMN_IDS = ["status", "priority", "assignee"] as const;
       // no render:
       {COLUMN_IDS.map(id => <Header key={id}>{t(`inbox:columns.${id}`)}</Header>)}
       ```

    5. Strings de domínio técnico (`tsx`, `json`, URLs literais como `https://...`) — NÃO traduzir (CONTEXT §Discrição).

    6. Strings dinâmicas complexas (templates compostos com 3+ partes condicionais): adicionar comentário `// i18n: review — complex template`, cobrir o caso simples agora, deferir refator agressivo para v2 L10N-03.

    7. Para CADA chave nova adicionada via `t()` no código, GARANTIR que existe em `pt-BR/inbox.json` e `en-US/inbox.json` (Tarefa 1 ou append) — RESEARCH Pitfall 6 alerta que detector falha CI com 200+ keys missing.

    8. NÃO mexer em `BreadcrumbBar.tsx` para traduzir labels de breadcrumb — labels vêm de `setBreadcrumbs([{ label: "..." }])` chamadas pelas páginas. Inbox.tsx provavelmente tem `setBreadcrumbs([{ label: "Inbox" }])` — substituir por `setBreadcrumbs([{ label: t("inbox:title") }])` com dependency array `[setBreadcrumbs, t]` (Pitfall 2).

    Validar typecheck e missing-keys após cada arquivo grande:
    ```bash
    pnpm --filter @paperclipai/ui typecheck
    CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys
    ```
  </action>
  <verify>
    <automated>cd ui && pnpm typecheck && CI=true pnpm test:run -- missing-keys</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/Inbox.tsx contains "useTranslation" (grep-checkable)
    - ui/src/pages/Inbox.tsx contains 't("inbox:' OR "t('inbox:" (grep-checkable, 5+ occurrences)
    - ui/src/components/IssuesList.tsx contains "useTranslation" (grep-checkable)
    - ui/src/components/IssueFiltersPopover.tsx contains 't("inbox:filters.' (grep-checkable)
    - ui/src/components/IssueRow.tsx contains "useTranslation" (grep-checkable)
    - `cd ui && pnpm typecheck` exits 0
    - `cd ui && CI=true pnpm test:run -- missing-keys` exits 0 (sem chaves órfãs em pt-BR ou en-US)
  </acceptance_criteria>
  <done>7 arquivos migrados para `t()`; nenhum string em inglês hardcoded restante (auditável via grep manual de strings capitalizadas); typecheck verde; missing-keys.test.ts CI=true verde</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: RTL test InboxList.translation.test.tsx</name>
  <files>ui/src/components/inbox/__tests__/InboxList.translation.test.tsx</files>
  <read_first>
    - ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx (padrão de provider wrapping de Phase 7 — QueryClientProvider + I18nextProvider)
    - ui/src/i18n/index.ts (singleton i18n instance para uso em testes)
    - ui/src/components/IssuesList.tsx (props shape esperado)
    - ui/src/i18n/locales/pt-BR/inbox.json (chaves disponíveis para asserir)
  </read_first>
  <action>
    Conforme VALIDATION.md §Wave 0 Requirements (linha 56), criar `ui/src/components/inbox/__tests__/InboxList.translation.test.tsx`.

    Criar diretório `ui/src/components/inbox/__tests__/` se não existir (mkdir -p equivalente).

    Estrutura do teste (espelhada de ProfileSettings.locale-toggle.test.tsx — confirmar provider order via Read antes):

    ```typescript
    // @vitest-environment jsdom
    import { describe, it, expect, beforeEach } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
    import { I18nextProvider } from "react-i18next";
    import i18n from "@/i18n";
    import { IssuesList } from "@/components/IssuesList";
    // OR Inbox page se IssuesList tiver props complexos demais

    function renderWithProviders(ui: React.ReactElement) {
      const queryClient = new QueryClient({
        defaultOptions: { queries: { retry: false } },
      });
      return render(
        <QueryClientProvider client={queryClient}>
          <I18nextProvider i18n={i18n}>
            {ui}
          </I18nextProvider>
        </QueryClientProvider>
      );
    }

    describe("InboxList — pt-BR translation render (UI-01)", () => {
      beforeEach(async () => {
        await i18n.changeLanguage("pt-BR");
      });

      it("renderiza tabs em português quando locale=pt-BR", async () => {
        renderWithProviders(<IssuesList {/* mock props mínimas */} />);
        // Asserções concretas baseadas nas chaves de inbox.json:
        // pelo menos 1-3 strings PT visíveis
        expect(screen.getByText(/Filtros|Buscar tarefas|Carregando/i)).toBeInTheDocument();
      });

      it("renderiza em inglês após changeLanguage('en-US')", async () => {
        await i18n.changeLanguage("en-US");
        renderWithProviders(<IssuesList {/* mock props mínimas */} />);
        expect(screen.getByText(/Filters|Search issues|Loading/i)).toBeInTheDocument();
      });
    });
    ```

    Se `IssuesList` requer props complexos (Issue[], handlers, etc.) que tornam o setup invasivo, substituir por renderizar `Inbox` page com mocks via MSW ou stub direto de hooks (consultar pattern em testes existentes do paperclip via `find ui/src -name "*.test.tsx" | head`).

    Se preferir testar componente menor: alvejar `IssueFiltersPopover.tsx` que tem strings densas e props simples.

    O teste deve validar duas verdades: (a) chaves resolvem para pt-BR após `changeLanguage("pt-BR")`, (b) chaves resolvem para en-US após `changeLanguage("en-US")`. Confirma cobertura UI-01 truth #1.
  </action>
  <verify>
    <automated>cd ui && pnpm test:run -- InboxList.translation</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/inbox/__tests__/InboxList.translation.test.tsx exists
    - Test file contains "i18n.changeLanguage" (grep-checkable)
    - Test file contains "pt-BR" (grep-checkable)
    - Test file contains "en-US" (grep-checkable)
    - `cd ui && pnpm test:run -- InboxList.translation` exits 0 (test passes)
  </acceptance_criteria>
  <done>Arquivo de teste criado com 2+ casos (pt-BR + en-US render); teste GREEN; cobertura UI-01 truth #1 fechada em automatizado</done>
</task>

</tasks>

<verification>
- `cd ui && pnpm typecheck` — exit 0
- `cd ui && CI=true pnpm test:run -- missing-keys` — exit 0 (zero chaves órfãs)
- `cd ui && pnpm test:run -- InboxList.translation` — exit 0
- `cd ui && pnpm test:run` — full UI suite GREEN (sem regressões)
- grep manual em `ui/src/pages/Inbox.tsx` por strings capitalizadas em JSX (`>[A-Z][a-z]+`) deve retornar zero matches que NÃO sejam dentro de `t(...)`.
</verification>

<success_criteria>
- 4 arquivos JSON populados (pt-BR/inbox, en-US/inbox + extensões common em ambos locales) com ~120 chaves espelhadas
- 7 arquivos TSX migrados para `useTranslation` + `t()` calls; zero strings hardcoded em inglês restantes na superfície Inbox (validável via grep)
- 1 arquivo de teste RTL novo cobrindo render em ambos locales
- `IssuesList.tsx` traduzido cobre 3 superfícies (Inbox + ProjectDetail + Issues page) — custo marginal baixo para 08-02
- Detector missing-keys CI=true permanece GREEN
- UI-01 satisfeito (truth #1: usuário em pt-BR vê inbox sem strings em inglês)
</success_criteria>

<output>
After completion, create `.planning/phases/08-traducao-ui-core/08-01-SUMMARY.md`.
</output>
