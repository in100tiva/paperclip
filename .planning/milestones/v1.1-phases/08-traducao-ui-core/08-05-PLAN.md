---
phase: 08-traducao-ui-core
plan: 05
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - packages/db/src/schema/activity_log.ts
  - packages/db/src/migrations/0074_add_activity_action_key.sql
  - packages/db/src/migrations/meta/_journal.json
  - packages/db/src/migrations/meta/0074_snapshot.json
  - packages/shared/src/index.ts
  - packages/shared/src/validators/activity.ts
  - server/src/services/activity-log.ts
  - server/src/routes/issues.ts
  - server/src/routes/projects.ts
  - server/src/routes/agents.ts
  - server/src/routes/goals.ts
  - server/src/routes/companies.ts
  - server/src/routes/approvals.ts
  - server/src/routes/costs.ts
  - server/src/services/budgets.ts
  - server/src/services/claude-accounts.ts
  - server/src/services/heartbeat.ts
  - server/src/services/recovery/service.ts
  - ui/src/i18n/locales/pt-BR/activity.json
  - ui/src/i18n/locales/en-US/activity.json
  - ui/src/components/ActivityRow.tsx
  - ui/src/components/__tests__/ActivityRow.actionKey.test.tsx
  - server/src/__tests__/activity-log-action-key.test.ts
autonomous: true
requirements:
  - UI-09

must_haves:
  truths:
    - "Migration 0074_add_activity_action_key.sql aplica idempotente e adiciona colunas action_key text NULL + params_json jsonb NULL em activity_log"
    - "ActivityEvent type em @paperclipai/shared inclui campos opcionais actionKey e paramsJson; LogActivityInput aceita os mesmos"
    - "36 callsites prioritários (de activity-format.ts:24-63) emitem actionKey + paramsJson; entries antigas (sem actionKey) usam fallback formatActivityVerb() com label '(legado)' em itálico"
    - "Cliente ActivityRow.tsx renderiza via t(`activity:${actionKey}`, paramsJson) quando actionKey presente; fallback legacy quando ausente"
    - "Dicionários activity.json (pt-BR + en-US) populados com chaves para os 36 actions prioritários, todas em kebab-case"
    - "Detector missing-keys.test.ts permanece GREEN com CI=true após edits"
    - "Sem data migration — backwards-compat resolvida em runtime no ActivityRow"
  artifacts:
    - path: "packages/db/src/migrations/0074_add_activity_action_key.sql"
      provides: "DDL idempotente: ALTER TABLE activity_log ADD COLUMN action_key text + ADD COLUMN params_json jsonb"
      min_lines: 2
    - path: "packages/db/src/schema/activity_log.ts"
      provides: "Schema Drizzle estendido com actionKey + paramsJson columns"
      contains: "actionKey"
    - path: "packages/shared/src/validators/activity.ts"
      provides: "Tipo ActivityEvent atualizado + LogActivityInput exportado"
      contains: "actionKey"
    - path: "ui/src/i18n/locales/pt-BR/activity.json"
      provides: "Dicionário pt-BR para 36 actions prioritários (issue.*/agent.*/heartbeat.*/approval.*/project.*/goal.*/cost.*/company.*/claude-account-rotated) — todas as chaves em kebab-case"
      contains: "issue"
    - path: "ui/src/components/ActivityRow.tsx"
      provides: "Renderer refatorado: t() preferred, formatActivityVerb fallback, label '(legado)' quando actionKey ausente"
      contains: "actionKey"
    - path: "server/src/__tests__/activity-log-action-key.test.ts"
      provides: "Integration test: logActivity aceita actionKey + paramsJson e persiste; emite event com shape correto"
      min_lines: 30
    - path: "ui/src/components/__tests__/ActivityRow.actionKey.test.tsx"
      provides: "RTL test: render com actionKey (preferred) e fallback legacy"
      min_lines: 40
  key_links:
    - from: "ui/src/components/ActivityRow.tsx"
      to: "ui/src/i18n/locales/pt-BR/activity.json"
      via: "t(`activity:${event.actionKey}`, event.paramsJson)"
      pattern: "t\\(`activity:\\$\\{"
    - from: "server/src/services/activity-log.ts"
      to: "packages/db/src/schema/activity_log.ts"
      via: "db.insert(activityLog).values({ actionKey, paramsJson, ... })"
      pattern: "actionKey"
    - from: "ActivityEvent type"
      to: "ActivityRow consumer"
      via: "@paperclipai/shared barrel re-export"
      pattern: "actionKey\\?:"
---

<self_check>
**RISCO DE ESCOPO — LEIA ANTES DE COMEÇAR.**

Este plano modifica 23 arquivos (1 migration + 1 journal + 1 snapshot + 1 schema Drizzle + 2 arquivos shared + 1 service Express + 11 callsite arquivos server + 2 dicionários activity.json + 1 component UI + 2 testes). Esse volume é alto comparado a planos típicos da Fase 8 (08-01..08-04 ficam abaixo de 15 arquivos cada). O checker considerou splittar mas decidiu MANTER como plano único — splittar adicionaria coordination overhead (migration + schema + tipo shared + LogActivityInput + insert + live event + 36 callsites + UI render formam uma unidade coerente; quebrar em 2 planos exigiria depends_on serial + duplicação de contexto).

**Mitigações exigidas:**
1. Execute as 3 tarefas em ordem (1 → 2 → 3). Tarefa 1 estabelece a fundação (schema + tipo shared + LogActivityInput); Tarefa 2 backfill server + UI; Tarefa 3 testes.
2. Se contexto consumido exceder 60% antes da Tarefa 3 começar, PARE e reporte ao orquestrador. NÃO comprima Tarefa 3 (testes são must-have para fechar UI-09).
3. Use `read_first` rigorosamente — não exiba arquivos já lidos. Cada `read_first` lista exatamente o que precisa ser carregado.
4. Sub-passo 2.2 tem pre-step de validação por grep (ver Tarefa 2) — execute-o ANTES de editar callsites para garantir que `files_modified` cobre todos os 36 priorizados.
</self_check>

<objective>
Implementar UI-09: tradução do activity log via convenção `actionKey + paramsJson`. Adicionar migration `0074_add_activity_action_key.sql` (2 colunas nullable em `activity_log`), estender schema Drizzle + tipo compartilhado `ActivityEvent`, atualizar `LogActivityInput` em `server/src/services/activity-log.ts` e backfill 36 callsites prioritários (lista em `ui/src/lib/activity-format.ts:24-63`), refatorar `ActivityRow.tsx` para preferir `t(actionKey, paramsJson)` com fallback `formatActivityVerb()` + label "(legado)", popular dicionários `pt-BR/activity.json` + `en-US/activity.json` com 36 actions, e adicionar 2 testes (server integration + UI RTL).

Purpose: UI-09 é a integração mais complexa da Fase 8 — envolve schema DB + server emit + client render + backwards-compat. RESEARCH §Pattern 5 mapeou estratégia: schema diff trivial (2 ALTER ADD COLUMN), backwards-compat resolvida em runtime sem data migration, 36 actions priorizados (subset dos 169 totais) cobrem o formatter UI legado.

Output: 1 migration + journal/snapshot, 2 dicionários activity, 1 schema Drizzle estendido, 1 tipo shared estendido, 1 service Express estendido, 11 callsites server backfilled, 1 component UI refatorado, 2 testes.

**Wave 2 sequential rationale:** Wave 1 (08-01..08-04) traduz UI surfaces que NÃO tocam activity log infrastructure. Wave 2 (08-05) é sequencial APÓS 08-01..08-04 (declarado em `depends_on: ["01", "02", "03", "04"]`) para que o operador possa revisar o trabalho de tradução pura da Wave 1 antes do trabalho mais arriscado de migration de schema + 36 callsites server. Tecnicamente os arquivos são disjuntos (backend + ActivityRow + activity.json não colidem com Inbox/Projects/Settings/Nav), mas o ordering serializado dá ao operador uma fronteira clara de revisão.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-traducao-ui-core/08-CONTEXT.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/08-traducao-ui-core/08-VALIDATION.md
@.planning/phases/07-foundation-i18n-toggle-de-settings/07-RESEARCH.md
@packages/db/src/schema/activity_log.ts
@packages/db/src/migrations/0073_add_user_locale.sql
@server/src/services/activity-log.ts
@ui/src/components/ActivityRow.tsx
@ui/src/lib/activity-format.ts
@ui/src/pages/Activity.tsx
@ui/src/i18n/__tests__/missing-keys.test.ts

<interfaces>
From packages/db/src/schema/activity_log.ts:1-26 (current state):
```typescript
export const activityLog = pgTable("activity_log", {
  id: uuid("id").primaryKey().defaultRandom(),
  companyId: uuid("company_id").notNull().references(() => companies.id),
  actorType: text("actor_type").notNull().default("system"),
  actorId: text("actor_id").notNull(),
  action: text("action").notNull(),       // KEEP — fallback
  entityType: text("entity_type").notNull(),
  entityId: text("entity_id").notNull(),
  agentId: uuid("agent_id").references(() => agents.id),
  runId: uuid("run_id").references(() => heartbeatRuns.id),
  details: jsonb("details").$type<Record<string, unknown>>(),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
});
```

From server/src/services/activity-log.ts:80-90 (LogActivityInput shape — current):
```typescript
export interface LogActivityInput {
  companyId: string;
  actorType: "agent" | "user" | "system" | "plugin";
  actorId: string;
  action: string;
  entityType: string;
  entityId: string;
  agentId?: string | null;
  runId?: string | null;
  details?: Record<string, unknown> | null;
}
```

From ui/src/lib/activity-format.ts:24-63 (36 priority actions — canonical list, source strings em underscore):
```
issue.created, issue.updated, issue.checked_out, issue.released,
issue.comment_added, issue.comment_cancelled, issue.attachment_added,
issue.attachment_removed, issue.document_created, issue.document_updated,
issue.document_deleted, issue.commented, issue.deleted,
agent.created, agent.updated, agent.paused, agent.resumed,
agent.terminated, agent.key_created, agent.budget_updated,
agent.runtime_session_reset,
heartbeat.invoked, heartbeat.cancelled,
approval.created, approval.approved, approval.rejected,
project.created, project.updated, project.deleted,
goal.created, goal.updated, goal.deleted,
cost.reported, cost.recorded,
company.created, company.updated, company.archived, company.budget_updated,
issue.blockers_updated, issue.reviewers_updated, issue.approvers_updated
```

**actionKey kebab-case mapping rule (i18n):** O detector regex em `ui/src/i18n/__tests__/missing-keys.test.ts:6` aceita `[a-z0-9.\-]+` — underscore NÃO é aceito. Server emite `action: "issue.checked_out"` (string original/legacy preservada) MAS `actionKey: "issue.checked-out"` (kebab para `t()`). Tabela completa de transforms underscore→kebab em Tarefa 2 Sub-passo 2.2.

From ui/src/components/ActivityRow.tsx:31 (current render path):
```typescript
const verb = formatActivityVerb(event.action, event.details, { agentMap, userProfileMap });
```

DB-03 enforcement: `pnpm db:migrate` runs ONLY in `.github/workflows/db-migrate.yml`. Locally we use `pnpm --filter @paperclipai/db generate` to produce SQL, commit, PR — CI applies. NEVER run `db:migrate` against Supabase from dev machine.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Schema migration + tipo shared + LogActivityInput extension</name>
  <files>packages/db/src/schema/activity_log.ts, packages/db/src/migrations/0074_add_activity_action_key.sql, packages/db/src/migrations/meta/_journal.json, packages/db/src/migrations/meta/0074_snapshot.json, packages/shared/src/validators/activity.ts, packages/shared/src/index.ts, server/src/services/activity-log.ts</files>
  <read_first>
    - packages/db/src/schema/activity_log.ts (estado atual)
    - packages/db/src/migrations/0073_add_user_locale.sql (referência de sequência — última migration)
    - packages/db/src/migrations/meta/_journal.json (formato do journal e como appendar entry para 0074)
    - packages/shared/src/validators/activity.ts (se existir; ou descobrir onde ActivityEvent é definido — RESEARCH Open Q1 sugere `packages/shared/src/`)
    - packages/shared/src/index.ts (barrel exports)
    - server/src/services/activity-log.ts:80-150 (LogActivityInput + db.insert + live event payload)
    - .github/workflows/db-migrate.yml (DB-03 enforcement reference)
  </read_first>
  <action>
    Conforme RESEARCH §Pattern 5 (5a..5c) + Pitfall 7 (inspecionar drizzle generate output).

    **Sub-passo 1.1: Estender schema Drizzle (`packages/db/src/schema/activity_log.ts`):**

    ```typescript
    export const activityLog = pgTable("activity_log", {
      // ... existing columns intactas ...
      actionKey: text("action_key"),                                    // NEW — nullable
      paramsJson: jsonb("params_json").$type<Record<string, unknown>>(), // NEW — nullable
      // existing details, createdAt continuam após
    });
    ```
    NÃO mexer em `details` — fica para payload completo legacy. `paramsJson` armazena APENAS params de interpolação i18n (subset de details, idiomático).

    **Sub-passo 1.2: Gerar migration via Drizzle:**

    ```bash
    cd packages/db
    # Carregar env via mesmo padrão de Phase 6/7 (DB-03 — generate é local-safe, apply é CI-only):
    export $(grep -v '^#' .env.local | xargs 2>/dev/null) 2>/dev/null
    pnpm db:generate
    ```

    **CRÍTICO Pitfall 7:** Inspecionar o arquivo gerado `0074_*.sql` ANTES de commitar. Deve conter EXATAMENTE:
    ```sql
    ALTER TABLE "activity_log" ADD COLUMN "action_key" text;
    ALTER TABLE "activity_log" ADD COLUMN "params_json" jsonb;
    ```
    Se aparecer `RENAME` ou DDL adicional inesperado, abortar e investigar (drizzle pode inferir rename ambíguo).

    Renomear arquivo gerado para `0074_add_activity_action_key.sql` se o gerador tiver dado nome aleatório (consistente com naming `0073_add_user_locale.sql` da Phase 7).

    Atualizar `meta/_journal.json` para incluir entry de 0074 (drizzle-kit normalmente faz automaticamente, verificar).

    **Sub-passo 1.3: Estender tipo shared `ActivityEvent` (RESEARCH Open Q1):**

    Localizar definição de `ActivityEvent` em `packages/shared/src/`. Se em `validators/activity.ts`:
    ```typescript
    export interface ActivityEvent {
      id: string;
      companyId: string;
      // ... campos existentes ...
      action: string;
      actionKey?: string | null;                          // NEW
      paramsJson?: Record<string, unknown> | null;        // NEW
      details?: Record<string, unknown> | null;
      // ...
    }
    ```
    Garantir export via barrel (`packages/shared/src/index.ts`) — `export type { ActivityEvent } from "./validators/activity"` (verificar pattern de Phase 7 D-Locale-export para Locale type).

    **Sub-passo 1.4: Estender `LogActivityInput` em `server/src/services/activity-log.ts`:**

    ```typescript
    export interface LogActivityInput {
      companyId: string;
      actorType: "agent" | "user" | "system" | "plugin";
      actorId: string;
      action: string;                                    // mantém — fallback
      actionKey?: string | null;                         // NEW
      paramsJson?: Record<string, unknown> | null;       // NEW
      entityType: string;
      entityId: string;
      agentId?: string | null;
      runId?: string | null;
      details?: Record<string, unknown> | null;
    }
    ```

    Atualizar `db.insert(activityLog).values({...})` (linhas ~100-110) para incluir os novos campos:
    ```typescript
    .values({
      // ... existing ...
      actionKey: input.actionKey ?? null,
      paramsJson: input.paramsJson ?? null,
    })
    ```

    Atualizar live event payload (linhas ~112-125) para incluir `actionKey` + `paramsJson` que serão broadcast via WebSocket:
    ```typescript
    const event: ActivityEvent = {
      // ... existing ...
      actionKey: input.actionKey ?? null,
      paramsJson: input.paramsJson ?? null,
    };
    ```

    NÃO commitar com migration aplicada localmente (DB-03 viola). `pnpm db:generate` apenas gera SQL — apply é exclusivo do GitHub Actions workflow `.github/workflows/db-migrate.yml` push-to-main.

    Validar:
    ```bash
    pnpm -r typecheck
    ```
  </action>
  <verify>
    <automated>pnpm -r typecheck && test -f packages/db/src/migrations/0074_add_activity_action_key.sql && grep -c "ADD COLUMN" packages/db/src/migrations/0074_add_activity_action_key.sql | grep -q "^2$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/activity_log.ts contains 'actionKey: text("action_key")' (grep-checkable)
    - packages/db/src/schema/activity_log.ts contains 'paramsJson: jsonb("params_json")' (grep-checkable)
    - packages/db/src/migrations/0074_add_activity_action_key.sql exists
    - packages/db/src/migrations/0074_add_activity_action_key.sql contains exactly 2 'ADD COLUMN' statements (no RENAME)
    - packages/db/src/migrations/0074_add_activity_action_key.sql contains "action_key" AND "params_json"
    - packages/db/src/migrations/meta/_journal.json contains "0074" (entry appended)
    - packages/shared/src/validators/activity.ts (or wherever ActivityEvent lives) contains "actionKey?:" (grep-checkable)
    - packages/shared/src/index.ts re-exports ActivityEvent type with new fields
    - server/src/services/activity-log.ts contains "actionKey?:" in LogActivityInput (grep-checkable)
    - server/src/services/activity-log.ts contains "actionKey: input.actionKey" (insert binding) (grep-checkable)
    - `pnpm -r typecheck` exits 0
    - Migration NOT applied locally (no `pnpm db:migrate` run from dev machine)
  </acceptance_criteria>
  <done>Schema + migration + journal + snapshot + tipo shared + LogActivityInput estendidos; typecheck verde em todo workspace; migration arquivo committed mas NÃO aplicada (DB-03)</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Backfill 36 callsites server + dicionários activity + ActivityRow refactor</name>
  <files>server/src/routes/issues.ts, server/src/routes/projects.ts, server/src/routes/agents.ts, server/src/routes/goals.ts, server/src/routes/companies.ts, server/src/routes/approvals.ts, server/src/routes/costs.ts, server/src/services/budgets.ts, server/src/services/claude-accounts.ts, server/src/services/heartbeat.ts, server/src/services/recovery/service.ts, ui/src/i18n/locales/pt-BR/activity.json, ui/src/i18n/locales/en-US/activity.json, ui/src/components/ActivityRow.tsx</files>
  <read_first>
    - ui/src/lib/activity-format.ts:24-63 (lista canônica dos 36 actions priorizados)
    - ui/src/components/ActivityRow.tsx (todo — render path atual via formatActivityVerb na linha ~31)
    - ui/src/pages/Activity.tsx:46-160 (consumidor de ActivityRow)
    - ui/src/i18n/locales/pt-BR/activity.json (Phase 7 deixou {} ou stub)
    - ui/src/i18n/locales/en-US/activity.json (idem)
    - server/src/services/activity-log.ts (após Tarefa 1 — LogActivityInput estendido)
  </read_first>
  <action>
    Conforme RESEARCH §Pattern 5 (5d, 5e, 5f) + Pitfall 5 (server callsites omitidos).

    **Sub-passo 2.0 (PRE-STEP — coverage check antes de editar):**

    Antes de tocar qualquer callsite, executar pre-step de validação para garantir que `files_modified` cobre todos os 36 priorizados:

    ```bash
    grep -rn "logActivity(" server/src/ | tee /tmp/logActivity-callsites.txt
    grep -rn 'action: "' server/src/ | grep -E '(issue|agent|heartbeat|approval|project|goal|cost|company)\.' | tee /tmp/action-strings.txt
    ```

    Para CADA um dos 36 actionKeys priorizados (ver lista em `ui/src/lib/activity-format.ts:24-63`), verificar:
    1. Em qual arquivo o callsite vive (via grep `action: "<string>"` em server/src/).
    2. Se esse arquivo está em `files_modified` declarado no frontmatter deste plano.

    **Se algum callsite estiver em arquivo NÃO listado em `files_modified`** (ex: `services/hire-hook.ts`, `services/environment-run-orchestrator.ts`, `services/plugin-host-services.ts` — todos citados no RESEARCH §5d como production callsites mas potencialmente não em `files_modified` se não emitirem nenhum dos 36 priorizados):
    - Adicionar o arquivo a `files_modified` ANTES de editar (commit de frontmatter como step independente, ou integrar à task atual).
    - Reportar a expansão no SUMMARY ao final.

    **Se todos os 36 callsites priorizados estão cobertos pelos 11 arquivos já em `files_modified`:** seguir adiante. Os 130+ outros actions (não priorizados) ficam fora deste plano.

    **Sub-passo 2.1: Popular `ui/src/i18n/locales/pt-BR/activity.json` (kebab-case nas chaves):**

    O dicionário inteiro usa kebab-case nas keys porque o detector regex (`ui/src/i18n/__tests__/missing-keys.test.ts:6`) aceita apenas `[a-z0-9.\-]+`. NÃO há versão underscore das keys em parte alguma do JSON.

    ```json
    {
      "issue": {
        "created": "criou",
        "updated": "atualizou",
        "checked-out": "checou",
        "released": "liberou",
        "comment-added": "comentou em",
        "comment-cancelled": "cancelou comentário em fila em",
        "attachment-added": "anexou arquivo em",
        "attachment-removed": "removeu anexo de",
        "document-created": "criou documento em",
        "document-updated": "atualizou documento em",
        "document-deleted": "deletou documento de",
        "commented": "comentou em",
        "deleted": "deletou",
        "blockers-updated": "atualizou bloqueadores de",
        "reviewers-updated": "atualizou revisores de",
        "approvers-updated": "atualizou aprovadores de"
      },
      "agent": {
        "created": "criou",
        "updated": "atualizou",
        "paused": "pausou",
        "resumed": "retomou",
        "terminated": "terminou",
        "key-created": "criou chave de API para",
        "budget-updated": "atualizou orçamento de",
        "runtime-session-reset": "reiniciou sessão de"
      },
      "heartbeat": {
        "invoked": "invocou heartbeat para",
        "cancelled": "cancelou heartbeat para"
      },
      "approval": {
        "created": "solicitou aprovação",
        "approved": "aprovou",
        "rejected": "rejeitou"
      },
      "project": {
        "created": "criou",
        "updated": "atualizou",
        "deleted": "deletou"
      },
      "goal": {
        "created": "criou",
        "updated": "atualizou",
        "deleted": "deletou"
      },
      "cost": {
        "reported": "reportou custo para",
        "recorded": "registrou custo para"
      },
      "company": {
        "created": "criou empresa",
        "updated": "atualizou empresa",
        "archived": "arquivou",
        "budget-updated": "atualizou orçamento de"
      },
      "claude-account-rotated": "trocou conta Claude (de {{fromAccountName}} para {{toAccountName}}, motivo: {{reason}})"
    }
    ```

    **Mapeamento underscore→kebab (transforms aplicados nas keys do dicionário; NÃO altera a string `action` legacy emitida pelo server):**

    | action (legacy, underscore) | actionKey (kebab para `t()`) |
    |-----------------------------|------------------------------|
    | `issue.checked_out` | `issue.checked-out` |
    | `issue.comment_added` | `issue.comment-added` |
    | `issue.comment_cancelled` | `issue.comment-cancelled` |
    | `issue.attachment_added` | `issue.attachment-added` |
    | `issue.attachment_removed` | `issue.attachment-removed` |
    | `issue.document_created` | `issue.document-created` |
    | `issue.document_updated` | `issue.document-updated` |
    | `issue.document_deleted` | `issue.document-deleted` |
    | `issue.blockers_updated` | `issue.blockers-updated` |
    | `issue.reviewers_updated` | `issue.reviewers-updated` |
    | `issue.approvers_updated` | `issue.approvers-updated` |
    | `agent.key_created` | `agent.key-created` |
    | `agent.budget_updated` | `agent.budget-updated` |
    | `agent.runtime_session_reset` | `agent.runtime-session-reset` |
    | `company.budget_updated` | `company.budget-updated` |
    | `claude_account_rotated` | `claude-account-rotated` |

    Demais actions já são kebab-friendly e mapeiam 1:1 (`issue.created` → `issue.created`, etc.).

    **CRÍTICO sobre `claude_account_rotated`:** Este é um action TOP-LEVEL (sem namespace `entity.`). O dicionário usa key `"claude-account-rotated"` (kebab no top-level). O callsite em `server/src/services/claude-accounts.ts` (atualmente emite `action: "claude_account_rotated"` — a constante `ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED` em `server/src/services/activity-log.ts:28` MANTÉM underscore para retrocompat) deve ser atualizado para emitir `actionKey: "claude-account-rotated"` (kebab). A string legacy `action` permanece `"claude_account_rotated"` — não trocar essa constante; trocá-la quebraria `services/claude-accounts.test.ts:7,422,450` e `services/claude-accounts-swap.md`.

    Espelhar TODAS as chaves em `ui/src/i18n/locales/en-US/activity.json` em inglês (created, updated, paused, etc.) — também em kebab-case.

    **Sub-passo 2.2: Backfill 36 callsites server:**

    Para cada um dos 36 actions na lista RESEARCH §5d (canonical em `activity-format.ts:24-63`), localizar o callsite via `Grep "action: \"<action_string>\"" server/src/`. Atualizar para incluir `actionKey + paramsJson`:

    ```typescript
    // ANTES:
    await logActivity({
      companyId, actorType, actorId,
      action: "issue.checked_out",                         // legacy underscore
      entityType: "issue", entityId: issue.id,
      details: { issueTitle: issue.title, projectId: issue.projectId },
    });

    // DEPOIS:
    await logActivity({
      companyId, actorType, actorId,
      action: "issue.checked_out",                         // mantém — retrocompat (legacy underscore)
      actionKey: "issue.checked-out",                      // NEW — kebab para t()
      paramsJson: { issueTitle: issue.title },             // NEW — subset de details usado pelo template i18n
      entityType: "issue", entityId: issue.id,
      details: { issueTitle: issue.title, projectId: issue.projectId },  // mantém payload completo
    });
    ```

    `paramsJson` deve conter APENAS os campos referenciados pelo template em `activity.json` (ex: `{{issueTitle}}`, `{{fromAccountName}}`). NÃO duplicar todo `details`.

    Lista distribuída por arquivo (mapeamento heurístico — VERIFICAR via grep no Sub-passo 2.0):
    - `routes/issues.ts`: issue.created, issue.updated, issue.deleted, issue.commented, issue.checked_out, issue.released, issue.attachment_added, issue.attachment_removed, issue.document_created, issue.document_updated, issue.document_deleted, issue.comment_added, issue.comment_cancelled, issue.blockers_updated, issue.reviewers_updated, issue.approvers_updated
    - `routes/projects.ts`: project.created, project.updated, project.deleted
    - `routes/agents.ts`: agent.created, agent.updated, agent.paused, agent.resumed, agent.terminated, agent.key_created, agent.budget_updated, agent.runtime_session_reset
    - `routes/goals.ts`: goal.created, goal.updated, goal.deleted
    - `routes/companies.ts`: company.created, company.updated, company.archived, company.budget_updated
    - `routes/approvals.ts`: approval.created, approval.approved, approval.rejected
    - `routes/costs.ts`: cost.reported, cost.recorded
    - `services/budgets.ts`, `services/claude-accounts.ts`, `services/heartbeat.ts`, `services/recovery/service.ts`: heartbeat.invoked, heartbeat.cancelled, claude_account_rotated, e cost.* extras

    **Pitfall 5:** Validar via `grep -rn "actionKey:" server/src/` que pelo menos 36 linhas matcheiam (uma por callsite priorizado). Os 130+ outros actions (não mapeados em activity-format.ts) NÃO recebem actionKey — entram em fallback path no UI (legado).

    **Sub-passo 2.3: Refatorar `ui/src/components/ActivityRow.tsx`:**

    ```typescript
    import { useTranslation } from "react-i18next";
    import { formatActivityVerb } from "@/lib/activity-format";
    import type { ActivityEvent } from "@paperclipai/shared";

    function renderVerb(
      event: ActivityEvent,
      t: TFunction,
      ctx: { agentMap: ...; userProfileMap: ... }
    ): { verb: string; isLegacy: boolean } {
      // Preferred path: server emitted actionKey (kebab-case)
      if (event.actionKey) {
        const params = (event.paramsJson ?? {}) as Record<string, unknown>;
        const translated = t(`activity:${event.actionKey}`, { ...params, defaultValue: null });
        if (translated) return { verb: translated as string, isLegacy: false };
      }
      // Fallback path: legacy entries (sem actionKey) ou keys ainda não mapeadas
      return {
        verb: formatActivityVerb(event.action, event.details, ctx),
        isLegacy: true,
      };
    }

    export function ActivityRow({ event, agentMap, userProfileMap }: Props) {
      const { t } = useTranslation(["activity"]);
      const { verb, isLegacy } = renderVerb(event, t, { agentMap, userProfileMap });
      return (
        <li>
          {/* ... existing actor/timestamp render ... */}
          <span>{verb}</span>
          {isLegacy && (
            <span className="ml-1 text-xs italic text-muted-foreground">
              (legado)
            </span>
          )}
          {/* ... entity link ... */}
        </li>
      );
    }
    ```

    NÃO remover `formatActivityVerb` — mantém como fallback (Anti-pattern: tentar reescrever activity-format.ts é fora de escopo, Pitfall 4).

    Para CADA chave usada em `t(\`activity:${actionKey}\`, ...)`, garantir que existe em pt-BR/activity.json e en-US/activity.json (ambas em kebab-case). Como actionKeys são dinâmicos (string template), o detector pode não detectar — adicionar comentário `// i18n-allowlist: activity.* keys driven by actionKey` se necessário.

    Validar:
    ```bash
    pnpm -r typecheck
    cd ui && CI=true pnpm test:run -- missing-keys
    ```
  </action>
  <verify>
    <automated>pnpm -r typecheck && cd ui && CI=true pnpm test:run -- missing-keys && cd .. && grep -rcE "actionKey:" server/src/ | awk -F: '{s+=$2} END {if (s < 36) {print "FAIL only " s " callsites"; exit 1} else print "OK " s " callsites"}'</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/i18n/locales/pt-BR/activity.json contains "issue" with sub-keys created/updated/checked-out/comment-added (kebab-case, grep-checkable)
    - ui/src/i18n/locales/pt-BR/activity.json does NOT contain underscore in any key (grep-check: `grep -E '"[a-z]+_[a-z]+"' ui/src/i18n/locales/pt-BR/activity.json` returns 0 lines)
    - ui/src/i18n/locales/pt-BR/activity.json contains top-level "claude-account-rotated" (kebab) with "{{fromAccountName}}" placeholder (grep-checkable)
    - ui/src/i18n/locales/pt-BR/activity.json does NOT contain top-level "claude_account_rotated" (underscore) — grep returns 0
    - ui/src/i18n/locales/en-US/activity.json mirrors pt-BR keys (all kebab-case)
    - server/src/services/claude-accounts.ts emits `actionKey: "claude-account-rotated"` (kebab) while keeping `action: "claude_account_rotated"` (legacy underscore)
    - server/src/routes/issues.ts contains 'actionKey: "issue.' (grep-checkable, multiple occurrences)
    - server/src/routes/agents.ts contains 'actionKey: "agent.' (grep-checkable, multiple occurrences)
    - server/src/routes/projects.ts contains 'actionKey: "project.' (grep-checkable)
    - At least 36 lines matching `actionKey:` across server/src/ (verify command)
    - Pre-step coverage check executed: every priority callsite's parent file is in files_modified (no surprise file edits outside declared list)
    - ui/src/components/ActivityRow.tsx contains "useTranslation" (grep-checkable)
    - ui/src/components/ActivityRow.tsx contains "actionKey" (grep-checkable)
    - ui/src/components/ActivityRow.tsx contains "(legado)" OR "isLegacy" (grep-checkable)
    - ui/src/components/ActivityRow.tsx still imports formatActivityVerb (fallback retained)
    - `pnpm -r typecheck` exits 0
    - `cd ui && CI=true pnpm test:run -- missing-keys` exits 0
  </acceptance_criteria>
  <done>2 dicionários activity populados em kebab-case (sem underscore residual); 36 callsites prioritários backfilled com actionKey kebab; ActivityRow refatorado com fallback + label "(legado)"; typecheck e missing-keys verdes; pre-step de coverage validou files_modified</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Server integration test + UI RTL test</name>
  <files>server/src/__tests__/activity-log-action-key.test.ts, ui/src/components/__tests__/ActivityRow.actionKey.test.tsx</files>
  <read_first>
    - server/src/__tests__/auth-routes-locale.test.ts (Phase 7 — pattern de stubbed-drizzle integration test em server)
    - server/src/services/activity-log.ts (após Tarefas 1+2 — LogActivityInput aceita actionKey + paramsJson)
    - ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx (provider wrapping pattern)
    - ui/src/components/ActivityRow.tsx (após Tarefa 2 — render refatorado)
    - ui/src/i18n/locales/pt-BR/activity.json (chaves kebab-case para asserir)
  </read_first>
  <action>
    Conforme VALIDATION.md §Wave 0 Requirements (linhas 59-60).

    **Sub-passo 3.1: Server integration test `server/src/__tests__/activity-log-action-key.test.ts`:**

    Espelhar pattern de `auth-routes-locale.test.ts` (Phase 7 stubbed-drizzle):

    ```typescript
    import { describe, it, expect, vi } from "vitest";
    import { logActivity } from "../services/activity-log";
    // OU import { createServer } + supertest se preferir route-level test

    describe("activity-log: actionKey + paramsJson persistence (UI-09)", () => {
      it("logActivity aceita actionKey + paramsJson e os passa para db.insert", async () => {
        const insertSpy = vi.fn().mockReturnValue({ values: vi.fn().mockResolvedValue([{ id: "uuid-test" }]) });
        const dbStub = { insert: insertSpy };

        await logActivity(dbStub as any, {
          companyId: "comp-1",
          actorType: "user",
          actorId: "user-1",
          action: "issue.created",
          actionKey: "issue.created",
          paramsJson: { issueTitle: "Test issue" },
          entityType: "issue",
          entityId: "issue-1",
        });

        expect(insertSpy).toHaveBeenCalled();
        const valuesCall = insertSpy.mock.calls[0]; // confirme shape via inspeção real
        // assert que actionKey + paramsJson chegam ao .values(...)
      });

      it("logActivity sem actionKey passa null para colunas novas (retrocompat)", async () => {
        // ... similar, sem actionKey/paramsJson — assert null nos campos
      });

      it("live event payload inclui actionKey + paramsJson quando providos", async () => {
        // se logActivity emite evento via WebSocket/EventEmitter, capturar e asserir shape
      });
    });
    ```

    Adaptar shape do test ao pattern real de `activity-log.ts` — pode ser que a função aceite `db` como parâmetro injetado ou use closure. Ler pattern dos testes Phase 7 antes de assumir.

    **Sub-passo 3.2: UI RTL test `ui/src/components/__tests__/ActivityRow.actionKey.test.tsx`:**

    ```typescript
    // @vitest-environment jsdom
    import { describe, it, expect, beforeEach } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { I18nextProvider } from "react-i18next";
    import i18n from "@/i18n";
    import { ActivityRow } from "@/components/ActivityRow";
    import type { ActivityEvent } from "@paperclipai/shared";

    function makeEvent(overrides: Partial<ActivityEvent>): ActivityEvent {
      return {
        id: "evt-1",
        companyId: "comp-1",
        actorType: "user",
        actorId: "user-1",
        action: "issue.created",
        entityType: "issue",
        entityId: "issue-1",
        agentId: null,
        runId: null,
        details: { issueTitle: "Test" },
        actionKey: null,
        paramsJson: null,
        createdAt: new Date().toISOString(),
        ...overrides,
      };
    }

    function renderRow(event: ActivityEvent) {
      return render(
        <I18nextProvider i18n={i18n}>
          <ActivityRow event={event} agentMap={{}} userProfileMap={{}} />
        </I18nextProvider>
      );
    }

    describe("ActivityRow — actionKey + fallback (UI-09)", () => {
      beforeEach(async () => {
        await i18n.changeLanguage("pt-BR");
      });

      it("renderiza verb traduzido quando actionKey presente em pt-BR", async () => {
        renderRow(makeEvent({
          actionKey: "issue.created",
          paramsJson: { issueTitle: "Test issue" },
        }));
        // pt-BR/activity.json: "issue.created": "criou"
        expect(screen.getByText(/criou/i)).toBeInTheDocument();
        expect(screen.queryByText(/legado/i)).not.toBeInTheDocument();
      });

      it("renderiza fallback formatActivityVerb + label '(legado)' quando actionKey ausente", async () => {
        renderRow(makeEvent({
          actionKey: null,
          action: "issue.created",
          details: { issueTitle: "Legacy issue" },
        }));
        expect(screen.getByText(/legado/i)).toBeInTheDocument();
      });

      it("renderiza em en-US quando locale=en-US", async () => {
        await i18n.changeLanguage("en-US");
        renderRow(makeEvent({
          actionKey: "issue.created",
          paramsJson: { issueTitle: "Test" },
        }));
        // en-US/activity.json: "issue.created": "created"
        expect(screen.getByText(/created/i)).toBeInTheDocument();
      });
    });
    ```

    Adaptar props do ActivityRow ao shape real (`agentMap`/`userProfileMap` ou outros — ler ActivityRow.tsx atual).

    Validar:
    ```bash
    cd ui && pnpm test:run -- ActivityRow.actionKey
    cd server && pnpm test:run -- activity-log-action-key
    ```
  </action>
  <verify>
    <automated>cd ui && pnpm test:run -- ActivityRow.actionKey && cd ../server && pnpm test:run -- activity-log-action-key</automated>
  </verify>
  <acceptance_criteria>
    - server/src/__tests__/activity-log-action-key.test.ts exists
    - server/src/__tests__/activity-log-action-key.test.ts contains "actionKey" (grep-checkable)
    - server/src/__tests__/activity-log-action-key.test.ts contains "paramsJson" (grep-checkable)
    - ui/src/components/__tests__/ActivityRow.actionKey.test.tsx exists
    - ui/src/components/__tests__/ActivityRow.actionKey.test.tsx contains "i18n.changeLanguage" (grep-checkable)
    - ui/src/components/__tests__/ActivityRow.actionKey.test.tsx contains "legado" (test for fallback label) (grep-checkable)
    - `cd ui && pnpm test:run -- ActivityRow.actionKey` exits 0 (3 cases pass: pt-BR translated, en-US translated, fallback legacy)
    - `cd server && pnpm test:run -- activity-log-action-key` exits 0 (with-actionKey + without-actionKey cases pass)
  </acceptance_criteria>
  <done>2 arquivos de teste criados; 5+ cases totais (3 UI + 2 server); ambos GREEN; cobertura UI-09 fechada em automatizado (truth: server emit + UI render preferred + UI render fallback)</done>
</task>

</tasks>

<verification>
- `pnpm -r typecheck` — exit 0 em todos os workspaces
- `cd ui && CI=true pnpm test:run -- missing-keys` — exit 0
- `cd ui && pnpm test:run -- ActivityRow.actionKey` — exit 0
- `cd server && pnpm test:run -- activity-log-action-key` — exit 0
- `pnpm test:run` — full suite GREEN
- Migration `0074_add_activity_action_key.sql` existe com EXATAMENTE 2 ALTER ADD COLUMN, journal atualizado
- Migration NÃO foi aplicada localmente (DB-03 — apply é responsabilidade do CI workflow `db-migrate.yml` em push para main)
- grep `"actionKey:" server/src/` retorna ≥36 linhas (Pitfall 5 — coverage dos 36 priorizados)
- grep underscore em activity.json — 0 lines (kebab-case enforcement)
</verification>

<success_criteria>
- Migration 0074 idempotente, 2 colunas nullable adicionadas, journal/snapshot consistentes
- ActivityEvent type estendido em @paperclipai/shared (barrel re-export)
- LogActivityInput estendido em server/src/services/activity-log.ts; insert binding + live event payload incluem actionKey + paramsJson
- 36 callsites priorizados emitem actionKey kebab-case + paramsJson (subset de details usado pelo template); legacy `action` string com underscore preservada
- ActivityRow refatorado: t() preferred path + formatActivityVerb fallback + label "(legado)" para entries pré-Fase-8
- Dicionários activity.json populados com 36 actions em pt-BR/en-US (todas as chaves em kebab-case, sem underscore)
- 2 testes (server + UI) cobrindo: emit com actionKey, fallback sem actionKey, render pt-BR + en-US, label legado
- Detector missing-keys CI=true GREEN
- UI-09 satisfeito (truths: schema migration + server emit + UI render preferred + UI render fallback + sem data migration)
</success_criteria>

<output>
After completion, create `.planning/phases/08-traducao-ui-core/08-05-SUMMARY.md`.
</output>
</content>
</invoke>