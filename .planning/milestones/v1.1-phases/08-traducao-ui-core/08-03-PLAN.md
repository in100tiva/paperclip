---
phase: 08-traducao-ui-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/settings.json
  - ui/src/i18n/locales/en-US/settings.json
  - ui/src/pages/InstanceSettings.tsx
  - ui/src/pages/InstanceGeneralSettings.tsx
  - ui/src/pages/InstanceExperimentalSettings.tsx
  - ui/src/pages/ProfileSettings.tsx
  - ui/src/pages/ClaudeAccounts.tsx
  - ui/src/pages/__tests__/Settings.translation.test.tsx
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "Usuário com locale=pt-BR navega em /instance/settings/{profile,general,heartbeats,experimental} e vê todas as labels, descriptions e botões em português"
    - "ProfileSettings.tsx (já parcialmente traduzido em Phase 7 — language section) tem TODOS os outros campos (nome, avatar, etc.) também traduzidos"
    - "InstanceGeneralSettings.tsx (382 LOC, maior superfície) renderiza sections completas (Deployment/Censor/Shortcuts/Backup/Feedback/Sign out) em pt-BR"
    - "InstanceSettings.tsx (heartbeats) renderiza confirmação `window.confirm(...)` traduzida + plurals via i18next nativo (`{{count}} dia` / `{{count}} dias`)"
    - "ClaudeAccounts.tsx renderiza listing, register form, status badges, helper text em pt-BR"
    - "Detector missing-keys.test.ts permanece GREEN com CI=true após edits"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/settings.json"
      provides: "Dicionário pt-BR estendido — language.* (Phase 7) + general/heartbeats/profile/claude-accounts (~85 chaves novas)"
      contains: "general"
    - path: "ui/src/i18n/locales/en-US/settings.json"
      provides: "Dicionário en-US espelhado"
      contains: "general"
    - path: "ui/src/pages/__tests__/Settings.translation.test.tsx"
      provides: "RTL test asserindo render pt-BR e en-US"
      min_lines: 30
  key_links:
    - from: "ui/src/pages/InstanceGeneralSettings.tsx"
      to: "ui/src/i18n/locales/pt-BR/settings.json"
      via: "useTranslation(['settings', 'common']) + t('settings:general.*')"
      pattern: "useTranslation\\(\\[\"settings\""
    - from: "ui/src/pages/InstanceSettings.tsx"
      to: "ui/src/i18n/locales/pt-BR/settings.json"
      via: "t('settings:heartbeats.*') incluindo plurals (count)"
      pattern: "t\\(\"settings:heartbeats"
    - from: "ui/src/pages/ProfileSettings.tsx"
      to: "ui/src/i18n/locales/pt-BR/settings.json"
      via: "useTranslation já existente (Phase 7) — estender para campos não-locale"
      pattern: "t\\(\"settings:"
---

<objective>
Traduzir a superfície de Settings (UI-03) para pt-BR e en-US, cobrindo `InstanceSettings.tsx` (heartbeats, 283 LOC), `InstanceGeneralSettings.tsx` (geral, 382 LOC — maior), `InstanceExperimentalSettings.tsx` (experimental, 157 LOC), `ProfileSettings.tsx` (campos não-locale; language section já feita em Phase 7) e `ClaudeAccounts.tsx` (480 LOC). Estender `pt-BR/settings.json` + `en-US/settings.json` com ~85 chaves novas (preservando `language.*` da Phase 7).

Purpose: Settings inclui a própria seção de idioma (Phase 7) — UI-03 truth #1 demanda paridade completa em todas as outras subseções. InstanceGeneralSettings tem alta densidade de descriptions multi-linha que o detector precisa cobrir. Heartbeats requer pluralização via i18next built-in (`{{count}} agente` / `{{count}} agentes`).

Output: 2 dicionários estendidos, 5 arquivos TSX migrados, 1 arquivo de teste RTL.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-traducao-ui-core/08-CONTEXT.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/08-traducao-ui-core/08-VALIDATION.md
@.planning/phases/07-foundation-i18n-toggle-de-settings/07-RESEARCH.md
@ui/src/pages/ProfileSettings.tsx
@ui/src/i18n/locales/pt-BR/settings.json
@ui/src/i18n/index.ts

<interfaces>
From ui/src/i18n/locales/pt-BR/settings.json (Phase 7 existing — DO NOT REMOVE):
```json
{
  "language": {
    "title": "Idioma",
    "description": "...",
    "pt-br": "Português (Brasil)",
    "en-us": "English (US)"
  }
}
```

From ProfileSettings.tsx:26 (canonical pattern, namespace já carregado):
```typescript
const { t, i18n } = useTranslation(["settings", "common"]);
```

From RESEARCH §Pattern 3 (plurals via i18next built-in):
```json
{
  "heartbeats": {
    "company": "{{count}} empresa",
    "company_other": "{{count}} empresas",
    "days": "{{count}} dia",
    "days_other": "{{count}} dias"
  }
}
```
i18next 26.x default config tem plurals habilitados; usar via `t("settings:heartbeats.company", { count })` — resolve `_other` automaticamente para count !== 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Estender dicionários settings (preservando language.* da Phase 7)</name>
  <files>ui/src/i18n/locales/pt-BR/settings.json, ui/src/i18n/locales/en-US/settings.json</files>
  <read_first>
    - ui/src/i18n/locales/pt-BR/settings.json (CRÍTICO — Phase 7 deixou language.* populado; NÃO remover)
    - ui/src/i18n/locales/en-US/settings.json (idem)
    - ui/src/pages/InstanceSettings.tsx:36-280 (heartbeats — strings de count plurais)
    - ui/src/pages/InstanceGeneralSettings.tsx:34-368 (deployment, censor, shortcuts, backup, feedback, sign out — strings densas)
    - ui/src/pages/InstanceExperimentalSettings.tsx (experimental flags)
    - ui/src/pages/ClaudeAccounts.tsx (listing, register, status badges)
  </read_first>
  <action>
    Conforme RESEARCH §Pattern 3 e D-01.

    **CRÍTICO:** Preservar `language.*` sub-tree existente (Phase 7) — leitura obrigatória do estado atual antes de editar.

    Estender `ui/src/i18n/locales/pt-BR/settings.json` com:

    ```json
    {
      "language": { "...": "(MANTER intacto da Phase 7)" },
      "instance-settings": {
        "title": "Configurações da instância"
      },
      "general": {
        "title": "Geral",
        "description": "Configure preferências da instância: exibição de logs, atalhos de teclado, retenção de backup e compartilhamento de dados.",
        "deployment": {
          "title": "Implantação e autenticação",
          "auth-readiness": "Status de autenticação",
          "ready": "Pronto",
          "not-ready": "Não pronto",
          "setup-required": "Configuração necessária",
          "active": "Ativo",
          "none": "Nenhum",
          "bootstrap-status": "Status do bootstrap",
          "bootstrap-invite": "Convite de bootstrap"
        },
        "censor-username": {
          "title": "Censurar usuário em logs",
          "description": "Esconder o segmento de usuário em paths de home-directory exibidos em logs."
        },
        "keyboard-shortcuts": {
          "title": "Atalhos de teclado"
        },
        "backup-retention": {
          "title": "Retenção de backups",
          "daily": "Diário",
          "weekly": "Semanal",
          "monthly": "Mensal",
          "days": "{{count}} dia",
          "days_other": "{{count}} dias",
          "weeks": "{{count}} semana",
          "weeks_other": "{{count}} semanas",
          "months": "{{count}} mês",
          "months_other": "{{count}} meses"
        },
        "ai-feedback": {
          "title": "Compartilhamento de feedback de IA",
          "always-allow": "Sempre permitir",
          "dont-allow": "Não permitir",
          "read-tos": "Leia nossos termos de serviço"
        },
        "sign-out": {
          "title": "Sair"
        }
      },
      "heartbeats": {
        "title": "Heartbeats do agendador",
        "description": "Agentes com timer heartbeat habilitado em todas as suas suas empresas.",
        "loading": "Carregando heartbeats…",
        "empty": "Nenhum heartbeat corresponde aos critérios atuais.",
        "active": "ativo",
        "disabled": "desativado",
        "on": "Ligado",
        "off": "Desligado",
        "never": "nunca",
        "company": "{{count}} empresa",
        "company_other": "{{count}} empresas",
        "actions": {
          "disable-all": "Desativar todos",
          "disabling": "Desativando…",
          "enable-timer": "Ativar timer heartbeat",
          "disable-timer": "Desativar timer heartbeat",
          "confirm-disable-all": "Desativar timer heartbeats para os {{count}} agentes habilitados?"
        }
      },
      "experimental": {
        "title": "Experimental",
        "description": "Funcionalidades experimentais — habilite por sua conta e risco."
      },
      "profile": {
        "title": "Perfil",
        "name-label": "Nome de exibição",
        "name-placeholder": "Seu nome",
        "avatar-label": "Avatar",
        "avatar-upload": "Enviar imagem",
        "avatar-remove": "Remover avatar",
        "save": "Salvar perfil",
        "saving": "Salvando…",
        "save-failed": "Falha ao salvar perfil"
      },
      "claude-accounts": {
        "title": "Contas Claude",
        "description": "Conexões com Claude para spawn de agentes.",
        "register": "Adicionar conta",
        "remove": "Remover",
        "empty": "Nenhuma conta Claude configurada.",
        "status": {
          "active": "Ativa",
          "exhausted": "Esgotada",
          "cooldown": "Em cooldown"
        }
      }
    }
    ```

    Estender com chaves adicionais conforme strings descobertas no grep das tarefas seguintes — alvo total ~85 chaves novas.

    Espelhar TODAS as chaves novas em `ui/src/i18n/locales/en-US/settings.json` mantendo `language.*` que Phase 7 deixou.

    Validar JSON válido e que `language.*` permanece intacto:
    ```bash
    node -e "const d = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/settings.json','utf8')); console.log(d.language?.title ? 'OK language preserved' : 'FAIL language missing')"
    ```
  </action>
  <verify>
    <automated>node -e "const ptBr = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/settings.json','utf8')); const enUs = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/en-US/settings.json','utf8')); if (!ptBr.language?.title) throw 'pt-BR language.* missing'; if (!enUs.language?.title) throw 'en-US language.* missing'; if (!ptBr.general?.title) throw 'pt-BR general.* missing'; if (!ptBr.heartbeats?.title) throw 'pt-BR heartbeats.* missing'; console.log('OK preserved + extended');"</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/i18n/locales/pt-BR/settings.json contains "language" (preserved from Phase 7) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/settings.json contains "general" (new) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/settings.json contains "heartbeats" (new) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/settings.json contains "claude-accounts" (new) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/settings.json contains "_other" (plural marker) (grep-checkable)
    - ui/src/i18n/locales/en-US/settings.json mirror dos sub-trees
    - 2 arquivos JSON válidos
  </acceptance_criteria>
  <done>2 dicionários settings estendidos com general/heartbeats/experimental/profile/claude-accounts; language.* preservado; plurals via _other; espelhamento pt-BR/en-US</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar 5 arquivos de Settings para t()</name>
  <files>ui/src/pages/InstanceSettings.tsx, ui/src/pages/InstanceGeneralSettings.tsx, ui/src/pages/InstanceExperimentalSettings.tsx, ui/src/pages/ProfileSettings.tsx, ui/src/pages/ClaudeAccounts.tsx</files>
  <read_first>
    - ui/src/pages/ProfileSettings.tsx (Phase 7 — useTranslation já existente em linha 26; estender para campos não-locale)
    - ui/src/pages/InstanceSettings.tsx (linha 36-280 — heartbeats com plurals)
    - ui/src/pages/InstanceGeneralSettings.tsx (linhas 92-95, 113-119, 141-145, 159-163, 178-183, 280-283, 356-358 — descriptions)
    - ui/src/pages/InstanceExperimentalSettings.tsx (157 LOC)
    - ui/src/pages/ClaudeAccounts.tsx (480 LOC)
  </read_first>
  <action>
    Conforme D-01 e RESEARCH §Pattern 3.

    Para cada um dos 5 arquivos:

    1. Adicionar import (se ainda não existir):
       ```typescript
       import { useTranslation } from "react-i18next";
       ```

    2. Adicionar hook (se ainda não existir; ProfileSettings já tem):
       ```typescript
       const { t } = useTranslation(["settings", "common"]);
       ```

    3. **ProfileSettings.tsx**: já tem `useTranslation` (Phase 7). Estender — todos os campos NÃO-locale (nome, avatar, breadcrumb labels "Instance Settings" / "Profile") precisam migrar:
       ```typescript
       // Linha ~40 (setBreadcrumbs):
       setBreadcrumbs([
         { label: t("settings:instance-settings.title") },
         { label: t("settings:profile.title") },
       ]);
       // dependency array: [setBreadcrumbs, t]
       ```
       Strings de upload avatar, save button, error messages — todos via t().

    4. **InstanceSettings.tsx (heartbeats)** — substituir confirmação:
       ```typescript
       // ANTES:
       window.confirm(`Disable timer heartbeats for all ${enabledCount} enabled ${noun}?`)
       // DEPOIS:
       window.confirm(t("settings:heartbeats.actions.confirm-disable-all", { count: enabledCount }))
       ```
       E counts plurais:
       ```typescript
       // ANTES:
       <span>{count} {count === 1 ? "company" : "companies"}</span>
       // DEPOIS:
       <span>{t("settings:heartbeats.company", { count })}</span>
       ```
       (i18next nativo resolve `_other` automaticamente).

    5. **InstanceGeneralSettings.tsx** — section por section:
       - Headers: `<h2>General</h2>` → `<h2>{t("settings:general.title")}</h2>`
       - Descriptions multi-linha (linhas 92-95 etc.): cada bloco vira uma chave (ex: `t("settings:general.censor-username.description")`)
       - Backup retention strings com counts: `{count} days` → `t("settings:general.backup-retention.days", { count })`
       - "Always allow" / "Don't allow" → `t("settings:general.ai-feedback.always-allow")` / `t("settings:general.ai-feedback.dont-allow")`
       - Deployment mode descriptions (113-119): cada modo recebe sua chave.

    6. **InstanceExperimentalSettings.tsx**: section header + flag descriptions traduzidos.

    7. **ClaudeAccounts.tsx**: title, register button, remove button, empty state, status badges (Active/Exhausted/Cooldown), helper text. Não mexer em strings de erro de validação de form (UI-07, Fase 9) — escopo desta fase é label/title/header/empty.

    8. Strings dinâmicas complexas: marcar com `// i18n: review — complex template`.

    9. Para CADA chave nova adicionada via `t()`, garantir existência em pt-BR e en-US JSON (Tarefa 1 ou append).

    Validar a cada arquivo grande:
    ```bash
    cd ui && pnpm typecheck
    cd ui && CI=true pnpm test:run -- missing-keys
    ```
  </action>
  <verify>
    <automated>cd ui && pnpm typecheck && CI=true pnpm test:run -- missing-keys</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/InstanceGeneralSettings.tsx contains "useTranslation" (grep-checkable)
    - ui/src/pages/InstanceGeneralSettings.tsx contains 't("settings:general.' (grep-checkable, 5+ occurrences)
    - ui/src/pages/InstanceSettings.tsx contains 't("settings:heartbeats.' (grep-checkable)
    - ui/src/pages/InstanceSettings.tsx contains "{ count" (interpolation for plurals) (grep-checkable)
    - ui/src/pages/ProfileSettings.tsx contains 't("settings:profile.' OR 't("settings:instance-settings' (grep-checkable, breadcrumb update)
    - ui/src/pages/ClaudeAccounts.tsx contains 't("settings:claude-accounts.' (grep-checkable)
    - `cd ui && pnpm typecheck` exits 0
    - `cd ui && CI=true pnpm test:run -- missing-keys` exits 0
  </acceptance_criteria>
  <done>5 arquivos migrados; plurals via t(key, { count }); ProfileSettings.setBreadcrumbs traduzido; descriptions multi-linha cobertas; missing-keys CI verde</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: RTL test Settings.translation.test.tsx</name>
  <files>ui/src/pages/__tests__/Settings.translation.test.tsx</files>
  <read_first>
    - ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx (provider wrapping pattern + jsdom annotation)
    - ui/src/pages/InstanceGeneralSettings.tsx (props/context dependencies)
    - ui/src/i18n/locales/pt-BR/settings.json (chaves para asserir)
  </read_first>
  <action>
    Conforme VALIDATION.md §Wave 0 Requirements.

    Criar `ui/src/pages/__tests__/Settings.translation.test.tsx`. Estrutura espelhada de ProfileSettings.locale-toggle.test.tsx:

    ```typescript
    // @vitest-environment jsdom
    import { describe, it, expect, beforeEach } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
    import { I18nextProvider } from "react-i18next";
    import { MemoryRouter } from "react-router-dom";
    import i18n from "@/i18n";
    import { InstanceGeneralSettings } from "@/pages/InstanceGeneralSettings";
    // OU outro componente de Settings com setup mais simples

    function renderWithProviders(ui: React.ReactElement) {
      const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
      return render(
        <QueryClientProvider client={queryClient}>
          <I18nextProvider i18n={i18n}>
            <MemoryRouter>{ui}</MemoryRouter>
          </I18nextProvider>
        </QueryClientProvider>
      );
    }

    describe("Settings — translation render (UI-03)", () => {
      beforeEach(async () => {
        await i18n.changeLanguage("pt-BR");
      });

      it("renderiza General settings em pt-BR", async () => {
        renderWithProviders(<InstanceGeneralSettings />);
        // Esperado: Geral, Implantação, Censurar, Atalhos, Retenção
        expect(screen.getByText(/Geral|Implantação|Censurar/i)).toBeInTheDocument();
      });

      it("renderiza General settings em en-US", async () => {
        await i18n.changeLanguage("en-US");
        renderWithProviders(<InstanceGeneralSettings />);
        expect(screen.getByText(/General|Deployment|Censor/i)).toBeInTheDocument();
      });
    });
    ```

    Se InstanceGeneralSettings requer providers/contextos adicionais (BreadcrumbContext, CompanyContext) que dificultem o setup, alvejar componente menor de Settings (ex: InstanceExperimentalSettings ou ProfileSettings que já tem teste-base de Phase 7).

    Garantir GREEN antes de finalizar.
  </action>
  <verify>
    <automated>cd ui && pnpm test:run -- Settings.translation</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/pages/__tests__/Settings.translation.test.tsx exists
    - Test file contains "i18n.changeLanguage" (grep-checkable)
    - Test file contains "pt-BR" AND "en-US"
    - `cd ui && pnpm test:run -- Settings.translation` exits 0
  </acceptance_criteria>
  <done>Arquivo de teste criado com 2+ casos cobrindo render em ambos locales; teste GREEN; cobertura UI-03 em automatizado</done>
</task>

</tasks>

<verification>
- `cd ui && pnpm typecheck` — exit 0
- `cd ui && CI=true pnpm test:run -- missing-keys` — exit 0
- `cd ui && pnpm test:run -- Settings.translation` — exit 0
- `cd ui && pnpm test:run` — full UI suite GREEN (sem regressões em ProfileSettings.locale-toggle.test.tsx — Phase 7 baseline)
- `language.*` sub-tree em settings.json permanece intacto (verify automated da Tarefa 1)
</verification>

<success_criteria>
- 2 dicionários settings estendidos com ~85 chaves novas (preservando language.* da Phase 7)
- 5 arquivos TSX migrados (InstanceSettings, InstanceGeneralSettings, InstanceExperimentalSettings, ProfileSettings extended, ClaudeAccounts)
- Plurals via i18next built-in (`{{count}} dia` / `_other`)
- Confirmações `window.confirm()` traduzidas
- 1 teste RTL novo
- Detector missing-keys CI=true GREEN
- UI-03 satisfeito (truth #1: usuário em pt-BR vê toda Settings em português, language section preservada)
</success_criteria>

<output>
After completion, create `.planning/phases/08-traducao-ui-core/08-03-SUMMARY.md`.
</output>
