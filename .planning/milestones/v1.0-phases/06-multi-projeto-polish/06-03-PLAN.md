---
phase: 06-multi-projeto-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - server/src/services/claude-accounts.ts
  - server/src/services/claude-accounts.test.ts
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "selectActiveAccount lê companies.claudeAccountPoolMode antes de filtrar candidatos"
    - "Modo per_company filtra WHERE companyId=? AND scope='company'"
    - "Modo shared filtra WHERE (companyId=? AND scope='company') OR scope='shared'"
    - "Defaults preservam comportamento Fase 5: company sem coluna setada vira per_company; conta sem scope setada vira company"
    - "Cross-company isolation: Company B em modo per_company NUNCA vê contas da Company A nem contas shared"
    - "Tests cobrem ambos os modos + isolation cases"
  artifacts:
    - path: "server/src/services/claude-accounts.ts"
      provides: "selectActiveAccount pool-mode-aware"
      contains: "claudeAccountPoolMode"
    - path: "server/src/services/claude-accounts.test.ts"
      provides: "tests para per_company + shared + isolation"
      min_lines: 50
  key_links:
    - from: "server/src/services/claude-accounts.ts"
      to: "companies.claudeAccountPoolMode"
      via: "SELECT companies.claude_account_pool_mode WHERE id=?"
      pattern: "claudeAccountPoolMode|claude_account_pool_mode"
    - from: "server/src/services/claude-accounts.ts"
      to: "claude_accounts.scope"
      via: "WHERE clause incluindo scope"
      pattern: "claudeAccounts.scope|eq\\(claudeAccounts.scope"
---

<objective>
Atualizar `selectActiveAccount` para honrar `companies.claudeAccountPoolMode` (D-06). Quando modo='per_company' (default), só elege contas da própria company com scope='company'. Quando modo='shared', também eleva contas com scope='shared' independente do owner companyId.

Purpose: Destravar PROJ-02 success criterion #2 do ROADMAP. Defaults novos preservam semântica Fase 5 byte-por-byte (per_company + scope=company = comportamento atual). Sem opt-in operador, nada muda.

Output: Service atualizado + tests cobrindo ambos modos + isolation cases. Sem mudanças em UI (06-04 cuida) e sem mudanças em endpoints existentes (POST register precisa aceitar scope, mas isso fica no 06-04 onde a UI consome).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-projeto-polish/06-CONTEXT.md
@.planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md

@packages/db/src/schema/companies.ts
@packages/db/src/schema/claude_accounts.ts
@server/src/services/claude-accounts.ts
@server/src/services/claude-accounts.test.ts

<interfaces>
<!-- Estado atual de selectActiveAccount em claude-accounts.ts (linhas 145-235) -->
```typescript
async function selectActiveAccount(input: {
  agentId: string;
  companyId: string;
}): Promise<ClaudeAccount> {
  return withAgentLock(input.agentId, async () => {
    const now = new Date();
    const cooldown = cooldownSeconds();

    // Step 1: check binding for sticky/manual policy
    const bindingRows = await db
      .select()
      .from(agentAccountBindings)
      .where(eq(agentAccountBindings.agentId, input.agentId))
      .limit(1);
    // ... sticky/manual logic preservada ...

    // Step 2: lazy cooldown sweep
    await sweepCooldown(input.companyId, now);

    // Step 3: query candidates — ESTE BLOCO MUDA
    const cooldownThreshold = new Date(now.getTime() - cooldown * 1000);
    const candidates = await db
      .select()
      .from(claudeAccounts)
      .where(
        and(
          eq(claudeAccounts.companyId, input.companyId),  // ⬅ AMPLIAR para considerar pool mode
          eq(claudeAccounts.status, "live"),
          or(
            isNull(claudeAccounts.exhaustedUntil),
            lt(claudeAccounts.exhaustedUntil, now),
          ),
        ),
      )
      .orderBy(asc(claudeAccounts.lastUsedAt));

    // ... resto preservado (filter, ensure binding, return) ...
  });
}
```

<!-- Schema novo (após 06-01) -->
```typescript
// companies.claudeAccountPoolMode: text NOT NULL DEFAULT 'per_company'
// claude_accounts.scope: text NOT NULL DEFAULT 'company'
```

<!-- Drizzle import necessário adicional -->
```typescript
import { companies } from "@paperclipai/db";  // já provavelmente importado indireto; verificar barrel
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Atualizar selectActiveAccount para honrar pool mode + scope</name>
  <files>
    server/src/services/claude-accounts.ts,
    server/src/services/claude-accounts.test.ts
  </files>
  <read_first>
    - server/src/services/claude-accounts.ts (linhas 145-240 — selectActiveAccount completo; também imports linhas 1-17)
    - server/src/services/claude-accounts.test.ts (cases existentes — entender pattern de mock Db chainable; especialmente os cases de selectActiveAccount around ordering/sticky/cooldown filter)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-06 — query base por modo)
    - .planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md (confirmar nome final das colunas — claudeAccountPoolMode + scope)
    - packages/db/src/schema/companies.ts (estado atualizado pós-06-01)
    - packages/db/src/schema/claude_accounts.ts (estado atualizado pós-06-01)
    - packages/db/src/schema/index.ts (confirmar exports `companies` e `claudeAccounts` no barrel)
  </read_first>
  <behavior>
    Adicionar tests no MESMO arquivo `server/src/services/claude-accounts.test.ts` em um novo `describe("selectActiveAccount — pool mode (PROJ-02)")` block:

    - Test PM-1: Company com poolMode='per_company' (default) e 2 contas próprias scope='company' + 1 conta de outra company scope='shared' → seleciona apenas dentre as 2 próprias (a conta shared é IGNORADA)
    - Test PM-2: Company com poolMode='shared' e 1 conta própria scope='company' + 1 conta scope='shared' (de outra company) → seleciona dentre as 2 (round-robin lastUsedAt ASC entre ambas)
    - Test PM-3: Company com poolMode='shared' e 0 contas próprias + 1 conta scope='shared' → seleciona a shared
    - Test PM-4: Company com poolMode='per_company' e 0 contas próprias + N contas shared → throw NoAccountsAvailableError (shared não conta)
    - Test PM-5 (cross-tenant isolation crítico): Company B com poolMode='per_company' + N contas da Company A em qualquer scope → throw NoAccountsAvailableError (Company B nunca vê contas de Company A se elas têm scope='company'; nem mesmo se scope='shared' já que B está em per_company)
    - Test PM-6: Company com poolMode value desconhecido (corrupção de DB hipotética) → defensivo: tratar como per_company (fail-closed segura) OU log warn + per_company. Decisão: fail-closed silencioso para per_company.
  </behavior>
  <action>
    Conforme D-06 (06-CONTEXT.md):

    **Modificações em server/src/services/claude-accounts.ts:**

    1. Atualizar import de @paperclipai/db (linha 7-11) para incluir `companies`:
    ```typescript
    import {
      claudeAccounts,
      agentAccountBindings,
      agentStepExecutions,
      companies,
    } from "@paperclipai/db";
    ```

    2. DENTRO de `selectActiveAccount`, ANTES do Step 2 (sweepCooldown — linha ~183), ADICIONAR resolução de pool mode:
    ```typescript
    // Phase 6 / D-06 / PROJ-02: resolve pool mode for this company
    const companyRows = await db
      .select({ poolMode: companies.claudeAccountPoolMode })
      .from(companies)
      .where(eq(companies.id, input.companyId))
      .limit(1);
    const rawMode = companyRows[0]?.poolMode ?? "per_company";
    // Defensive: unknown values fail-closed to per_company (D-06 safest semantics)
    const poolMode: "per_company" | "shared" =
      rawMode === "shared" ? "shared" : "per_company";
    ```

    3. SUBSTITUIR o predicate `eq(claudeAccounts.companyId, input.companyId)` no Step 3 (linha ~192) por uma condição condicional ao poolMode:
    ```typescript
    // Phase 6 / D-06: scope filter depende do poolMode da company
    const scopeFilter =
      poolMode === "shared"
        ? or(
            and(
              eq(claudeAccounts.companyId, input.companyId),
              eq(claudeAccounts.scope, "company"),
            ),
            eq(claudeAccounts.scope, "shared"),
          )
        : and(
            eq(claudeAccounts.companyId, input.companyId),
            eq(claudeAccounts.scope, "company"),
          );

    const candidates = await db
      .select()
      .from(claudeAccounts)
      .where(
        and(
          scopeFilter,
          eq(claudeAccounts.status, "live"),
          or(
            isNull(claudeAccounts.exhaustedUntil),
            lt(claudeAccounts.exhaustedUntil, now),
          ),
        ),
      )
      .orderBy(asc(claudeAccounts.lastUsedAt));
    ```

    4. Atualizar `sweepCooldown` se necessário — verificar se faz sense também limpar contas shared. Decisão: sweepCooldown atual filtra por `companyId` (Step 2 linha ~183). Em modo shared, contas com scope='shared' de outra company NÃO serão swept aqui — mas isto é BENIGNO: a query Step 3 já tem `lt(exhaustedUntil, now)` como predicate alternativo que cobre o caso. Sweep é otimização para flippar status='exhausted' → 'live'; não bloqueia seleção. v1: NÃO modificar sweepCooldown (escopo per_company existing semantics).

    5. Atualizar `rowToAccount` se a função monta o objeto manualmente — CONFIRMAR que o select usa `select()` sem projection limitada (a função existe na linha ~133+; verificar). Se `rowToAccount` espera todos os campos do schema, e schema agora tem `scope`, então o tipo `ClaudeAccount` interface (linha 41-53) precisa ganhar:
    ```typescript
    export interface ClaudeAccount {
      // ... campos existentes ...
      scope: "company" | "shared";  // ADD
      // ...
    }
    ```
    E `rowToAccount` deve mapear `row.scope`. Se função usa spread `{...row}` automaticamente, nenhuma mudança é necessária além da interface — verificar qual padrão a 05-04 implementou.

    **Tests em server/src/services/claude-accounts.test.ts:**

    Adicionar describe block ao FINAL do arquivo (preservar todos os 21+ cases existentes):

    ```typescript
    describe("selectActiveAccount — pool mode (PROJ-02 / Phase 6)", () => {
      // 6 tests cobrindo PM-1 a PM-6 conforme behavior
      // Mock pattern: extend o existing chainable mock para responder à query
      // SELECT companies.claude_account_pool_mode WHERE id=? (primeiro select)
      // E retornar o array correto de candidates conforme cenário
    });
    ```

    Para cada test, mock Db deve responder em ordem:
    1. binding query → null (no sticky)
    2. NOVO: companies query → { poolMode: 'per_company' | 'shared' }
    3. sweepCooldown query → []
    4. candidates query → array conforme cenário
    5. update lastUsedAt
    6. binding insert/update

    Use o helper de queue do mock existente para enfileirar resultados.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "claudeAccountPoolMode" server/src/services/claude-accounts.ts && grep -c "scope" server/src/services/claude-accounts.ts && pnpm --filter @paperclipai/server test -- claude-accounts && pnpm --filter @paperclipai/server tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccountPoolMode\\|claude_account_pool_mode" server/src/services/claude-accounts.ts` >= 1
    - `grep -c "claudeAccounts.scope" server/src/services/claude-accounts.ts` >= 1
    - `grep -c "poolMode === \"shared\"" server/src/services/claude-accounts.ts` >= 1 (lógica condicional presente)
    - Tests existentes (21+ da Phase 5) continuam passando — verify count `pnpm test` antes/depois
    - 6 tests novos no describe `selectActiveAccount — pool mode` passing
    - `pnpm --filter @paperclipai/server tsc --noEmit` exit code 0
    - Interface `ClaudeAccount` exporta `scope: "company" | "shared"` (verify via grep)
  </acceptance_criteria>
  <done>selectActiveAccount honra poolMode + scope; 6 tests novos passing; zero regressão Fase 5; defaults preservam comportamento; fail-closed defensivo para poolMode unknown.</done>
</task>

</tasks>

<verification>
- selectActiveAccount com poolMode awareness: `grep -A 3 "rawMode" server/src/services/claude-accounts.ts` mostra parsing seguro
- Total tests claude-accounts.test.ts >= 27 (21 existentes + 6 novos)
- Sem regressão na Phase 5 ou Phase 6 plan 02 (cost-summary continua passando)
- TS compile clean em server
</verification>

<success_criteria>
- D-06 implementado literalmente (per_company filter vs shared filter)
- D-08 preservado (defaults garantem retrocompatibilidade)
- Cross-tenant isolation testada (Test PM-5)
- Plano 06-05 (smoke E2E) pode validar via SQL contra Supabase real
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-projeto-polish/06-03-SUMMARY.md` documentando:
- Diff conceitual: query antes vs depois (esquema textual)
- Decisão sobre fail-closed para poolMode unknown
- Decisão sobre sweepCooldown não-modificado (justificativa)
- Lista dos 6 test cases novos
- Confirmação de zero regressão (nº de tests antes vs depois)
</output>
