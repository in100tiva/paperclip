---
phase: 06-multi-projeto-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/shared/src/validators/company.ts
  - server/src/routes/claude-accounts.ts
  - ui/src/api/claude-accounts.ts
  - ui/src/pages/CompanySettings.tsx
  - ui/src/pages/ClaudeAccounts.tsx
  - ui/src/pages/ClaudeAccounts.test.tsx
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "CompanySettings.tsx tem toggle de claudeAccountPoolMode (per_company | shared)"
    - "Toggle persiste via PATCH /api/companies/:companyId existente"
    - "ClaudeAccounts.tsx form de registro tem radio scope (company | shared)"
    - "POST /api/companies/:companyId/claude-accounts aceita scope no body"
    - "Listagem ClaudeAccounts.tsx mostra badge 'shared' quando scope='shared'"
    - "Cost summary section consome GET cost-summary endpoint e renderiza tabela"
    - "Tests cobrem: toggle persistence, scope radio, badge render, cost table render"
  artifacts:
    - path: "ui/src/pages/CompanySettings.tsx"
      provides: "Pool mode toggle"
      contains: "claudeAccountPoolMode"
    - path: "ui/src/pages/ClaudeAccounts.tsx"
      provides: "scope field + cost summary section"
      contains: "scope"
    - path: "ui/src/api/claude-accounts.ts"
      provides: "costSummary helper + scope no create"
      contains: "costSummary"
  key_links:
    - from: "ui/src/pages/CompanySettings.tsx"
      to: "PATCH /api/companies/:companyId"
      via: "companiesApi.update with claudeAccountPoolMode body"
      pattern: "claudeAccountPoolMode"
    - from: "ui/src/pages/ClaudeAccounts.tsx"
      to: "GET /api/companies/:companyId/claude-accounts/cost-summary"
      via: "claudeAccountsApi.costSummary"
      pattern: "costSummary"
    - from: "ui/src/pages/ClaudeAccounts.tsx"
      to: "POST .../claude-accounts (com scope)"
      via: "claudeAccountsApi.create payload include scope"
      pattern: "scope.*company.*shared"
---

<objective>
Atualizar UI para suportar PROJ-02 (pool mode toggle + scope no registro) e PROJ-03 (cost summary section). Inclui patch no validator shared (`updateCompanySchema`) e na route POST de claude-accounts (aceitar scope no body).

Purpose: Fechar PROJ-02 do lado humano (operador configura modo via UI) e PROJ-03 visualização (cost dashboard). Resto dos backings já existe após 06-01/06-02/06-03.

Output: Validator shared estendido + route POST aceitando scope + 2 UI pages atualizadas + 1 helper API + tests UI atualizados.

Decisão de discrição (D-12 / 06-CONTEXT): Cost summary é uma SEÇÃO dentro de ClaudeAccounts.tsx (não nova page) — minimiza navegação, agrupa toda gestão de Claude Accounts num lugar. Toggle de pool mode fica em CompanySettings.tsx (D-07 — settings é o lugar canônico de configuração de empresa).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-projeto-polish/06-CONTEXT.md
@.planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md
@.planning/phases/06-multi-projeto-polish/06-02-SUMMARY.md

@packages/shared/src/validators/company.ts
@server/src/routes/claude-accounts.ts
@ui/src/api/claude-accounts.ts
@ui/src/pages/CompanySettings.tsx
@ui/src/pages/ClaudeAccounts.tsx
@ui/src/pages/ClaudeAccounts.test.tsx

<interfaces>
<!-- updateCompanySchema atual em packages/shared/src/validators/company.ts (linhas 16-28) -->
```typescript
export const updateCompanySchema = createCompanySchema
  .partial()
  .extend({
    status: z.enum(COMPANY_STATUSES).optional(),
    spentMonthlyCents: z.number().int().nonnegative().optional(),
    requireBoardApprovalForNewAgents: z.boolean().optional(),
    feedbackDataSharingEnabled: z.boolean().optional(),
    // ...
    brandColor: brandColorSchema,
    logoAssetId: logoAssetIdSchema,
    // ⬅ ADICIONAR: claudeAccountPoolMode
  });
```

<!-- POST create route atual em server/src/routes/claude-accounts.ts (linhas 30-36 + 105-141) -->
```typescript
const createAccountSchema = z.object({
  label: z.string().trim().min(1).max(100),
  configDirSlug: z.string().trim().regex(SLUG_REGEX, "..."),
  // ⬅ ADICIONAR: scope
});
```

<!-- ClaudeAccount interface em ui/src/api/claude-accounts.ts — precisa ganhar scope -->

<!-- ClaudeAccounts.tsx tem section "Register account" (linhas 177-220) e "Accounts" table (222-261) -->
<!-- ClaudeAccounts.tsx existe mas NÃO tem cost section ainda — adicionar nova <section> antes de "Rotation history" -->

<!-- CompanySettings.tsx pattern: ToggleField + onChange + onSave button + companiesApi.update -->
<!-- Linha ~185 tem state setBrandColor; pattern de useState + dirty check + handleSave; linha ~480 tem mutation update payload -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Estender updateCompanySchema (shared) e route POST claude-accounts (server) para suportar scope + poolMode</name>
  <files>
    packages/shared/src/validators/company.ts,
    server/src/routes/claude-accounts.ts
  </files>
  <read_first>
    - packages/shared/src/validators/company.ts (50 linhas)
    - server/src/routes/claude-accounts.ts (linhas 28-45 — schemas, e linhas 104-141 — POST create handler)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-04, D-05, D-07)
  </read_first>
  <action>
    **Modificações em packages/shared/src/validators/company.ts:**

    Estender `updateCompanySchema` (linhas 16-28). Adicionar campo:
    ```typescript
    claudeAccountPoolMode: z.enum(["per_company", "shared"]).optional(),
    ```

    Posicionamento: após `logoAssetId: logoAssetIdSchema,` (linha 27).

    **Modificações em server/src/routes/claude-accounts.ts:**

    1. Atualizar `createAccountSchema` (linha 30) para aceitar scope opcional:
    ```typescript
    const createAccountSchema = z.object({
      label: z.string().trim().min(1).max(100),
      configDirSlug: z.string().trim().regex(SLUG_REGEX, "..."),
      scope: z.enum(["company", "shared"]).optional().default("company"),
    });
    ```

    2. No handler POST (linha 105-141), passar `body.scope` no insert:
    ```typescript
    const [created] = await db
      .insert(claudeAccounts)
      .values({
        companyId: String(req.params.companyId),
        ownerUserId: userId,
        label: body.label,
        configDirSlug: body.configDirSlug,
        status: "live",
        scope: body.scope,  // ⬅ ADD
      })
      .returning();
    ```

    3. (Opcional) Atualizar `patchAccountSchema` para também permitir mudança de scope post-registro:
    ```typescript
    const patchAccountSchema = z.object({
      status: z.enum(["live", "disabled"]).optional(),
      label: z.string().trim().min(1).max(100).optional(),
      scope: z.enum(["company", "shared"]).optional(),  // ⬅ ADD (D-05 enable mid-life mudança)
    });
    ```
    Decisão: ADD por consistência com create — operador pode realizar promoção de uma conta existente para shared sem deletar/recriar.

    4. Garantir que handler PATCH propaga scope para `updates` object (linha 151-153):
    ```typescript
    if (body.scope !== undefined) updates.scope = body.scope;
    ```

    NÃO modificar:
    - Handler GET list, GET rotation-history, GET cost-summary (06-02)
    - assertCompanyOwnerOrAdmin
    - Outros validators (createCompanySchema fica intocado — pool mode é update-only)
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "claudeAccountPoolMode" packages/shared/src/validators/company.ts && grep -n "scope:" server/src/routes/claude-accounts.ts && pnpm --filter @paperclipai/shared tsc --noEmit && pnpm --filter @paperclipai/server tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccountPoolMode" packages/shared/src/validators/company.ts` retorna 1
    - `grep -c "z.enum(\\[\"per_company\", \"shared\"\\])" packages/shared/src/validators/company.ts` retorna 1
    - `grep -c "scope:" server/src/routes/claude-accounts.ts` >= 2 (createAccountSchema + insert + patchAccountSchema)
    - Tests existentes da route 06-02 (cost-summary) continuam passing
    - `pnpm --filter @paperclipai/shared tsc --noEmit` exit 0
    - `pnpm --filter @paperclipai/server tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Validator e route aceitam scope + poolMode; defaults preservam comportamento.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Atualizar UI api helper + CompanySettings (toggle pool mode) + ClaudeAccounts (scope field + cost section) + tests</name>
  <files>
    ui/src/api/claude-accounts.ts,
    ui/src/pages/CompanySettings.tsx,
    ui/src/pages/ClaudeAccounts.tsx,
    ui/src/pages/ClaudeAccounts.test.tsx
  </files>
  <read_first>
    - ui/src/api/claude-accounts.ts (todo — entender ClaudeAccount type e helpers existentes)
    - ui/src/pages/CompanySettings.tsx (linhas 1-100 + linhas 180-260 — section pattern de useState/dirty check/onSave; linhas 1130-1145 — exemplo de ToggleField)
    - ui/src/pages/ClaudeAccounts.tsx (todo — 358 linhas)
    - ui/src/pages/ClaudeAccounts.test.tsx (todo — entender pattern de mock api e setControlledInputValue helper)
    - ui/src/api/companies.ts (helper update — pattern de PATCH)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-07, D-12, D-13)
  </read_first>
  <action>
    **Arquivo 1 — ui/src/api/claude-accounts.ts:**

    1. Estender interface `ClaudeAccount` adicionando:
    ```typescript
    scope: "company" | "shared";
    ```

    2. Estender payload `create` para aceitar scope opcional:
    ```typescript
    create(companyId: string, input: { label: string; configDirSlug: string; scope?: "company" | "shared" }): Promise<{ account: ClaudeAccount }>
    ```

    3. Adicionar helper `costSummary`:
    ```typescript
    export interface CostSummaryRow {
      accountId: string;
      accountLabel: string;
      totalCostUsd: number;
      totalInputTokens: number;
      totalOutputTokens: number;
      stepCount: number;
    }

    // Dentro de claudeAccountsApi:
    costSummary(companyId: string, range?: { from?: string; to?: string }): Promise<{ rows: CostSummaryRow[] }> {
      const params = new URLSearchParams();
      if (range?.from) params.set("from", range.from);
      if (range?.to) params.set("to", range.to);
      const qs = params.toString();
      const url = `/api/companies/${companyId}/claude-accounts/cost-summary${qs ? `?${qs}` : ""}`;
      return apiClient.get(url);
    }
    ```

    **Arquivo 2 — ui/src/pages/CompanySettings.tsx (toggle pool mode — D-07):**

    1. Adicionar useState próximo aos outros (linha ~185):
    ```typescript
    const [claudeAccountPoolMode, setClaudeAccountPoolMode] = useState<"per_company" | "shared">("per_company");
    ```

    2. Hidratar via useEffect quando selectedCompany carrega (próximo a `setBrandColor(selectedCompany.brandColor ...)` linha ~197):
    ```typescript
    setClaudeAccountPoolMode((selectedCompany?.claudeAccountPoolMode as "per_company" | "shared") ?? "per_company");
    ```

    3. Atualizar dirty check (linha ~234) para incluir comparação:
    ```typescript
    || claudeAccountPoolMode !== ((selectedCompany.claudeAccountPoolMode as string) ?? "per_company")
    ```

    4. Atualizar payload do save (linha ~250 área `requireBoardApprovalForNewAgents:`):
    ```typescript
    claudeAccountPoolMode,
    ```

    5. Atualizar tipo do payload na declaração da signature `(linha ~240`):
    ```typescript
    claudeAccountPoolMode: "per_company" | "shared";
    ```

    6. Adicionar SECTION na UI (próximo às outras toggle settings — após `requireBoardApprovalForNewAgents` row, ~linha 1140). Padrão: dois radio buttons + descrição:
    ```jsx
    <div className="space-y-2">
      <label className="text-sm font-medium">Claude Account Pool Mode</label>
      <p className="text-xs text-muted-foreground">
        Controls which Claude accounts agents in this company can rotate across.
        <strong> per_company</strong>: only this company's own accounts.
        <strong> shared</strong>: also accounts marked as scope=shared from any company.
      </p>
      <div className="flex gap-4">
        <label className="flex items-center gap-2 text-sm">
          <input
            type="radio"
            name="claudeAccountPoolMode"
            value="per_company"
            checked={claudeAccountPoolMode === "per_company"}
            onChange={() => setClaudeAccountPoolMode("per_company")}
            data-testid="pool-mode-per-company"
          />
          per_company (default — isolated)
        </label>
        <label className="flex items-center gap-2 text-sm">
          <input
            type="radio"
            name="claudeAccountPoolMode"
            value="shared"
            checked={claudeAccountPoolMode === "shared"}
            onChange={() => setClaudeAccountPoolMode("shared")}
            data-testid="pool-mode-shared"
          />
          shared (uses shared pool too)
        </label>
      </div>
    </div>
    ```

    7. Atualizar Company type/api se houver (ui/src/api/companies.ts) para incluir `claudeAccountPoolMode?: string` no Company interface — verificar e adicionar se ausente.

    **Arquivo 3 — ui/src/pages/ClaudeAccounts.tsx (scope field + cost section — D-12):**

    1. Adicionar useState para scope no form (próximo a `setNewLabel` linha ~70):
    ```typescript
    const [newScope, setNewScope] = useState<"company" | "shared">("company");
    ```

    2. Atualizar createMutation handler para passar scope (linha ~96):
    ```typescript
    mutationFn: (input: { label: string; configDirSlug: string; scope: "company" | "shared" }) =>
      claudeAccountsApi.create(selectedCompanyId!, input),
    ```

    3. Reset scope no onSuccess (linha ~107) — após setNewSlug:
    ```typescript
    setNewScope("company");
    ```

    4. Atualizar handleSubmit (linha ~133) para passar scope:
    ```typescript
    createMutation.mutate({ label, configDirSlug, scope: newScope });
    ```

    5. Adicionar radio scope dentro do `<form>` (linha ~186 — adicionar célula extra na grid OU nova linha de form). Implementação: nova `<div>` abaixo do input slug, antes do `<div className="flex items-end">` do button:
    ```jsx
    <div className="space-y-1 text-sm sm:col-span-2">
      <span className="block text-xs font-medium text-muted-foreground">Scope</span>
      <div className="flex gap-4">
        <label className="flex items-center gap-2">
          <input
            type="radio"
            name="scope"
            value="company"
            checked={newScope === "company"}
            onChange={() => setNewScope("company")}
            data-testid="scope-company"
          />
          This company only
        </label>
        <label className="flex items-center gap-2">
          <input
            type="radio"
            name="scope"
            value="shared"
            checked={newScope === "shared"}
            onChange={() => setNewScope("shared")}
            data-testid="scope-shared"
          />
          Shared with all companies (in shared mode)
        </label>
      </div>
    </div>
    ```

    Ajustar grid: pode mudar `sm:grid-cols-[1fr_1fr_auto]` para layout que acomode o scope row em uma segunda linha — usar `grid grid-cols-1 sm:grid-cols-3 gap-3` e `sm:col-span-3` para o scope row.

    6. Adicionar coluna Scope na tabela `Accounts` (linha ~237 thead + ~248 tbody). Header `<th className="...">Scope</th>` e cell `<td>{account.scope === "shared" ? <Badge variant="secondary">shared</Badge> : <span className="text-muted-foreground text-xs">company</span>}</td>`.

    7. Adicionar useQuery para cost summary (próximo a `historyQuery` linha ~90):
    ```typescript
    const costsKey = ["claude-accounts", "cost-summary", selectedCompanyId ?? ""] as const;
    const costsQuery = useQuery({
      queryKey: costsKey,
      queryFn: () => claudeAccountsApi.costSummary(selectedCompanyId!),
      enabled: Boolean(selectedCompanyId),
    });
    ```

    8. Adicionar SECTION nova ENTRE `Accounts` table (linha ~261 fim) e `Rotation history` section (linha ~263):
    ```jsx
    <section className="rounded-xl border border-border">
      <div className="px-5 py-4">
        <h2 className="text-sm font-semibold">Cost summary</h2>
        <p className="text-sm text-muted-foreground">
          Aggregated spend per Claude account for this company. Default range: since current month start.
        </p>
      </div>
      {costsQuery.isLoading ? (
        <div className="border-t border-border px-5 py-8 text-sm text-muted-foreground">Loading cost summary…</div>
      ) : !costsQuery.data || costsQuery.data.rows.length === 0 ? (
        <div className="border-t border-border px-5 py-8 text-sm text-muted-foreground" data-testid="costs-empty">
          No usage recorded yet.
        </div>
      ) : (
        <div className="overflow-x-auto border-t border-border">
          <table className="min-w-full text-left text-sm" data-testid="costs-table">
            <thead>
              <tr className="border-b border-border">
                <th className="px-5 py-3 font-medium text-muted-foreground">Account</th>
                <th className="px-5 py-3 text-right font-medium text-muted-foreground">Cost (USD)</th>
                <th className="px-5 py-3 text-right font-medium text-muted-foreground">Input tokens</th>
                <th className="px-5 py-3 text-right font-medium text-muted-foreground">Output tokens</th>
                <th className="px-5 py-3 text-right font-medium text-muted-foreground">Steps</th>
              </tr>
            </thead>
            <tbody>
              {costsQuery.data.rows.map((row) => (
                <tr key={row.accountId} className="border-b border-border last:border-b-0">
                  <td className="px-5 py-3">{row.accountLabel}</td>
                  <td className="px-5 py-3 text-right">${row.totalCostUsd.toFixed(4)}</td>
                  <td className="px-5 py-3 text-right">{row.totalInputTokens.toLocaleString()}</td>
                  <td className="px-5 py-3 text-right">{row.totalOutputTokens.toLocaleString()}</td>
                  <td className="px-5 py-3 text-right">{row.stepCount}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </section>
    ```

    **Arquivo 4 — ui/src/pages/ClaudeAccounts.test.tsx:**

    Estender mock API com:
    - `costSummary` retornando `{ rows: [...] }` plantado
    - `create` mock aceitando scope no input

    Adicionar 4 test cases novos (manter os 5 existentes intactos):
    - "renders cost summary table when rows present" → mock costSummary retorna 2 rows; assert getByTestId('costs-table') e linhas
    - "renders empty cost summary state" → mock retorna []; assert getByTestId('costs-empty')
    - "submits create with selected scope" → seleciona radio scope-shared, submete form; assert mock create called com scope='shared'
    - "renders shared badge for shared accounts" → mock list retorna 1 conta com scope='shared'; assert badge 'shared' visível

    Reusar helper `setControlledInputValue` documentado no SUMMARY 05-07.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "claudeAccountPoolMode" ui/src/pages/CompanySettings.tsx && grep -n "costSummary" ui/src/api/claude-accounts.ts && grep -n "newScope" ui/src/pages/ClaudeAccounts.tsx && grep -n "costs-table" ui/src/pages/ClaudeAccounts.tsx && pnpm --filter @paperclipai/ui test -- ClaudeAccounts && pnpm --filter @paperclipai/ui tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccountPoolMode" ui/src/pages/CompanySettings.tsx` >= 4 (state + hidrate + dirty check + payload)
    - `grep -c "costSummary" ui/src/api/claude-accounts.ts` >= 1
    - `grep -c "newScope\\|setNewScope" ui/src/pages/ClaudeAccounts.tsx` >= 4 (declare + reset + submit + radios)
    - `grep -c "scope === \"shared\"\\|scope=\"shared\"" ui/src/pages/ClaudeAccounts.tsx` >= 1 (badge render)
    - `grep -c "costs-table\\|costs-empty" ui/src/pages/ClaudeAccounts.tsx` >= 2 (data-testid presentes)
    - 9+ test cases em ClaudeAccounts.test.tsx (5 existentes + 4 novos), todos passing
    - `pnpm --filter @paperclipai/ui tsc --noEmit` exit code 0
    - `pnpm --filter @paperclipai/ui test -- ClaudeAccounts` 0 fails
  </acceptance_criteria>
  <done>UI completa: pool mode toggle em CompanySettings, scope radio + badge + cost section em ClaudeAccounts; 4 tests novos passing; zero regressão.</done>
</task>

</tasks>

<verification>
- Validator shared aceita claudeAccountPoolMode → companies PATCH funciona end-to-end
- Route POST claude-accounts aceita scope → DB recebe scope correto
- UI CompanySettings persiste poolMode via existing PATCH /companies/:id
- UI ClaudeAccounts mostra cost data + permite registrar com scope
- Tests UI cobrem novas funcionalidades
</verification>

<success_criteria>
- D-04, D-05, D-07, D-12 implementados via UI funcional
- Todo o ciclo UI→API→DB→Service→UI valida-se em smoke (06-05)
- Sem regressão nos 5 tests existentes da Phase 5 (ClaudeAccounts)
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-projeto-polish/06-04-SUMMARY.md` documentando:
- Decisões de layout: cost section como 3ª seção em ClaudeAccounts (D-12), pool mode em CompanySettings (D-07)
- Patches no validator shared (linha exata do extend)
- Decisão sobre PATCH scope mid-life em claude-accounts route (incluído por consistência)
- Lista dos 4 test cases novos
- Quaisquer fricções com layout grid (form scope row span)
</output>
