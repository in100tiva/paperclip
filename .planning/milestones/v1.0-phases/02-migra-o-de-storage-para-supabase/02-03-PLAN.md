---
phase: 02-migra-o-de-storage-para-supabase
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/db/src/client.ts
  - packages/db/src/runtime-config.ts
  - packages/db/drizzle.config.ts
  - server/src/index.ts
  - .env.example
  - packages/db/package.json
autonomous: true
requirements: [INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06, DB-02]

must_haves:
  truths:
    - "`createDb(url)` em `packages/db/src/client.ts` passa `prepare: false`, `max: 5`, `idle_timeout: 20` quando `new URL(url).port === '6543'` (INFRA-02, INFRA-05)"
    - "`createDb(url)` mantém comportamento default (sem options especiais) quando porta != 6543 — embedded postgres / direct connection seguem funcionando como hoje"
    - "`packages/db/src/runtime-config.ts` privilegia `process.env.DATABASE_URL` Supabase como source com label específico (`DATABASE_URL`); embedded só ativa quando `PAPERCLIP_DB_MODE=embedded-postgres` está explícito (INFRA-03, INFRA-06)"
    - "`packages/db/drizzle.config.ts` usa `process.env.SUPABASE_DB_URL ?? process.env.DATABASE_URL` (session 5432 preferido para DDL, fallback para pooler)"
    - "Auto-migrations no startup ficam desabilitadas por default em `server/src/index.ts` — `ensureMigrations` chama `inspectMigrations` mas NÃO chama `applyPendingMigrations` se `pendingMigrations.length > 0`. Falha fast com mensagem acionável apontando `pnpm db:migrate` (DB-02)"
    - "Opt-in para CI: variável `PAPERCLIP_MIGRATION_AUTO_APPLY=true` continua funcionando (mantida do código existente para o pipeline)"
    - "`.env.example` lista DATABASE_URL (pooler 6543), SUPABASE_DB_URL (session 5432), SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (com warning), BETTER_AUTH_SECRET, PAPERCLIP_INSTANCE_ID=team-shared, PAPERCLIP_DB_MODE (opt-in embedded fallback) (INFRA-04)"
    - "dotenv-cli OU dotenv carregamento já existe ou é adicionado para que `pnpm db:migrate` carregue `.env.local` automaticamente"
    - "`pnpm typecheck` (escopo packages/db + server) continua passando após patches"
  artifacts:
    - path: "packages/db/src/client.ts"
      provides: "createDb com pool config Supavisor-aware"
      min_lines: 30
      contains: "prepare: false"
    - path: "packages/db/src/runtime-config.ts"
      provides: "Resolução privilegiando DATABASE_URL Supabase, embedded como opt-in"
      contains: "PAPERCLIP_DB_MODE"
    - path: "packages/db/drizzle.config.ts"
      provides: "drizzle-kit aponta para SUPABASE_DB_URL (session 5432) preferencial"
      contains: "SUPABASE_DB_URL"
    - path: "server/src/index.ts"
      provides: "ensureMigrations falha fast por default; opt-in via PAPERCLIP_MIGRATION_AUTO_APPLY"
      contains: "Refusing to start"
    - path: ".env.example"
      provides: "Template de env para devs novos"
      contains: "SUPABASE_DB_URL"
  key_links:
    - from: "packages/db/src/client.ts"
      to: "Supavisor transaction pooler porta 6543"
      via: "Detecção de porta + `prepare: false` + pool max:5"
      pattern: "6543.*prepare.*false|prepare.*false.*6543"
    - from: "packages/db/src/runtime-config.ts"
      to: "process.env.DATABASE_URL"
      via: "ResolvedDatabaseTarget.source = 'DATABASE_URL' quando env existe"
      pattern: "process\\.env\\.DATABASE_URL"
    - from: "packages/db/drizzle.config.ts"
      to: "process.env.SUPABASE_DB_URL"
      via: "Connection string preferencial para DDL (session mode)"
      pattern: "SUPABASE_DB_URL"
    - from: "server/src/index.ts"
      to: "applyPendingMigrations"
      via: "Chamada gateadа por PAPERCLIP_MIGRATION_AUTO_APPLY=true; falha fast caso contrário"
      pattern: "PAPERCLIP_MIGRATION_AUTO_APPLY"
---

<objective>
Patches cirúrgicos em `packages/db/` + `server/src/index.ts` para que o app rode contra Supabase remoto via Supavisor pooler com `prepare: false`, embedded postgres permaneça como fallback opt-in, e auto-migrations no startup sejam desabilitadas por default. Atualiza `.env.example` para refletir todas as env vars necessárias. Satisfaz INFRA-02..06 + DB-02.

Purpose: Sem essas mudanças, três coisas quebram. (1) postgres-js falha intermitentemente em Supavisor transaction mode com "prepared statement does not exist" (PITFALLS Armadilha 2). (2) 5+ devs apontando para Supabase compartilhado disputam o lock de migration no startup (Armadilha 3). (3) Devs novos não sabem quais env vars setar.

Output: Commit único atomico tocando 6 arquivos. Nenhum schema novo criado. Nenhum endpoint novo. Apenas configuração de runtime + template de env.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

@packages/db/src/client.ts
@packages/db/src/runtime-config.ts
@packages/db/drizzle.config.ts
@server/src/index.ts
@.env.example
@AGENTS.md

<interfaces>
<!-- Existing exports that this plan must NOT break -->

From `packages/db/src/client.ts`:
```ts
export function createDb(url: string): Db
export type Db = ReturnType<typeof createDb>
export async function getPostgresDataDirectory(url: string): Promise<string | null>
export async function applyPendingMigrations(url: string): Promise<void>
export async function inspectMigrations(url: string): Promise<MigrationState>
export async function reconcilePendingMigrationHistory(url: string): Promise<MigrationHistoryReconcileResult>
export async function migratePostgresIfEmpty(url: string): Promise<MigrationBootstrapResult>
export async function ensurePostgresDatabase(url: string, databaseName: string): Promise<"created" | "exists">
```
ALL of these must continue to be exported with the same signatures. The plan only modifies the BODY of `createDb`.

From `packages/db/src/runtime-config.ts`:
```ts
export type ResolvedDatabaseTarget = ... (already supports `mode: "postgres"` with source labels)
export function resolveDatabaseTarget(): ResolvedDatabaseTarget
```
The function ALREADY privileges `process.env.DATABASE_URL` as first-priority source (see lines 220-228). The patch is to (a) annotate the comment block, (b) gate embedded fallback behind `PAPERCLIP_DB_MODE=embedded-postgres` env var, (c) when `PAPERCLIP_DB_MODE` is unset and no DATABASE_URL → throw a clear error directing user to `.env.example`.

From `server/src/index.ts` lines 130-183 (function `ensureMigrations`):
Current behavior:
  - calls `inspectMigrations` → if `upToDate` returns immediately (good)
  - if pending → calls `promptApplyMigrations` (TTY prompt OR `PAPERCLIP_MIGRATION_AUTO_APPLY=true`)
  - on `apply: true` → calls `applyPendingMigrations`
Problem: TTY prompt defaults to TRUE if stdin/stdout not TTY (line 118: `if (!stdin.isTTY || !stdout.isTTY) return true`). This means in `pnpm dev` watcher (no TTY) it AUTO-APPLIES silently.

The patch: change `promptApplyMigrations` so that NO-TTY case returns FALSE by default (safer), unless `PAPERCLIP_MIGRATION_AUTO_APPLY=true` is set. This makes `pnpm db:migrate` (manual command) the explicit path.

From `.env.example` (current — minimal upstream paperclip):
```
DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret
```
Will be replaced wholesale with the team-shared template.
</interfaces>

<dependencies>
This plan depends on 02-01 (MIGRATION_AUDIT.md) for:
- Confirmation that `prepare: false` is the correct mitigation for the postgres-js + Supavisor pairing (Audit Section A.1)
- Confirmation that auto-migrations is HIGH risk (Audit Section F.1)
- Confirmation that no other findings (B,C,D,E,G,H) require additional patches in this plan
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Patch `packages/db/src/client.ts` — Supavisor-aware createDb</name>
  <files>
    packages/db/src/client.ts
    packages/db/src/__tests__/client-pool-config.test.ts
  </files>
  <behavior>
Tests primeiro (vitest spec em `packages/db/src/__tests__/client-pool-config.test.ts`):

- Test 1: `createDb` chamado com URL contendo `:6543/` → postgres() é instanciado com options `{ prepare: false, max: 5, idle_timeout: 20, connect_timeout: 10 }`
- Test 2: `createDb` chamado com URL contendo `:5432/` (session) → postgres() é instanciado COM `max: 5, idle_timeout: 20, connect_timeout: 10` MAS SEM `prepare: false` (session mode suporta prepared statements)
- Test 3: `createDb` chamado com URL contendo `:54329/` (porta default embedded) → postgres() é instanciado SEM options especiais (mantém comportamento legado para embedded)
- Test 4: `createDb` retorna um objeto que tem o método `.select` (sanity check de que ainda é Drizzle)

Use vitest mocking:
```ts
import { vi } from "vitest";
vi.mock("postgres", () => ({ default: vi.fn(() => ({ /* mock sql */ })) }));
```
Então inspecionar `vi.mocked(postgres).mock.calls[0][1]` para verificar options passadas.
  </behavior>
  <action>
Em `packages/db/src/client.ts`, substitua a função `createDb` (linhas 48-51) por:

```ts
export function createDb(url: string) {
  const sql = postgres(url, buildPostgresOptions(url));
  return drizzlePg(sql, { schema });
}

/**
 * Build postgres-js options based on the connection target.
 *
 * Supabase Supavisor pooler (port 6543, transaction mode):
 *   - `prepare: false` is REQUIRED — pooler reuses connections across transactions,
 *     so prepared statements get orphaned. Without this, queries fail intermittently
 *     with "prepared statement does not exist" under concurrency.
 *   - Pool sized small (`max: 5`) because 5+ devs * 5 conns = 25, well below
 *     Supabase free-tier limit of ~60 shared connections.
 *
 * Supabase direct/session pooler (port 5432):
 *   - Pool config still applies (don't blow free-tier limit) but `prepare` can
 *     stay default (true) — session mode keeps connection across statements.
 *
 * Embedded postgres (default port 54329) and any other target:
 *   - No special options — preserve legacy behavior. Pool defaults from postgres-js
 *     are appropriate for a localhost embedded instance.
 *
 * Reference: STACK.md (Supavisor pooler section); MIGRATION_AUDIT.md A.1; PITFALLS Armadilha 2.
 */
export function buildPostgresOptions(url: string): Parameters<typeof postgres>[1] {
  let port: string | null = null;
  try {
    port = new URL(url).port || null;
  } catch {
    // Malformed URL — let postgres-js error handling surface it.
    return undefined;
  }

  if (port === "6543") {
    // Supavisor transaction pooler.
    return { prepare: false, max: 5, idle_timeout: 20, connect_timeout: 10 };
  }
  if (port === "5432") {
    // Direct or session pooler — could be Supabase or self-hosted Postgres.
    // Apply pool sizing but keep prepared statements (session mode supports them).
    return { max: 5, idle_timeout: 20, connect_timeout: 10 };
  }
  // Embedded (54329) or anything else — preserve default behavior.
  return undefined;
}
```

Importante:
1. NÃO altere outras exports do arquivo (`getPostgresDataDirectory`, `applyPendingMigrations`, `inspectMigrations`, etc.).
2. NÃO altere `createUtilitySql(url)` (linha 13) — essa é só para queries utilitárias com `max: 1`, não precisa de pool config.
3. `buildPostgresOptions` é exportada para que os testes possam validar.

Crie o arquivo de teste `packages/db/src/__tests__/client-pool-config.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

// Mock postgres-js so we don't actually open connections.
vi.mock("postgres", () => {
  const mockSql = Object.assign(vi.fn(), {
    end: vi.fn(),
    unsafe: vi.fn(),
  });
  return {
    default: vi.fn(() => mockSql),
  };
});

// Mock drizzle so we don't try to introspect schema.
vi.mock("drizzle-orm/postgres-js", () => ({
  drizzle: vi.fn(() => ({ select: vi.fn() })),
}));

import postgres from "postgres";
import { createDb, buildPostgresOptions } from "../client.js";

describe("createDb pool config", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it("Test 1: pooler 6543 → prepare:false + max:5 + idle_timeout:20", () => {
    const url = "postgres://user:pwd@aws-1-sa-east-1.pooler.supabase.com:6543/postgres";
    expect(buildPostgresOptions(url)).toEqual({
      prepare: false,
      max: 5,
      idle_timeout: 20,
      connect_timeout: 10,
    });
    createDb(url);
    expect(vi.mocked(postgres).mock.calls[0][1]).toEqual({
      prepare: false,
      max: 5,
      idle_timeout: 20,
      connect_timeout: 10,
    });
  });

  it("Test 2: session 5432 → max:5 + idle_timeout:20 (no prepare:false)", () => {
    const url = "postgres://user:pwd@aws-1-sa-east-1.pooler.supabase.com:5432/postgres";
    expect(buildPostgresOptions(url)).toEqual({
      max: 5,
      idle_timeout: 20,
      connect_timeout: 10,
    });
  });

  it("Test 3: embedded 54329 → undefined (default postgres-js behavior)", () => {
    const url = "postgres://paperclip:paperclip@127.0.0.1:54329/postgres";
    expect(buildPostgresOptions(url)).toBeUndefined();
  });

  it("Test 4: createDb returns drizzle-shaped object", () => {
    const db = createDb("postgres://user:pwd@aws-1-sa-east-1.pooler.supabase.com:6543/postgres");
    expect(typeof (db as { select?: unknown }).select).toBe("function");
  });
});
```

Rode os testes:
```bash
cd D:/projetos/ddd && pnpm --filter @paperclipai/db exec vitest run src/__tests__/client-pool-config.test.ts
```
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && pnpm --filter @paperclipai/db exec vitest run src/__tests__/client-pool-config.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
- `packages/db/src/client.ts` exporta `createDb` (signature inalterada) e `buildPostgresOptions` (novo)
- 4 testes passam
- `createDb` com URL `:6543/` instancia postgres com `prepare: false`
- `createDb` com URL `:5432/` instancia postgres COM pool config MAS sem `prepare: false`
- `createDb` com URL `:54329/` (embedded) mantém options undefined (default behavior)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Patch `packages/db/src/runtime-config.ts` + `drizzle.config.ts`</name>
  <files>
    packages/db/src/runtime-config.ts
    packages/db/drizzle.config.ts
  </files>
  <action>
**Passo 1 — `packages/db/src/runtime-config.ts`:**

A função `resolveDatabaseTarget()` JÁ privilegia `process.env.DATABASE_URL` como primeira fonte (linhas 220-228). A mudança é tornar a transição de embedded → opt-in mais explícita.

Adicione no INÍCIO da função `resolveDatabaseTarget()` (depois de `const envPath = ...; const envEntries = ...;` e ANTES do bloco `if (envUrl) {...}`), o seguinte gate:

```ts
  // Phase 2: Supabase is the canonical backend. Embedded postgres remains as
  // an opt-in fallback (INFRA-06) — explicit `PAPERCLIP_DB_MODE=embedded-postgres`
  // is required to use it. Without that env var AND without DATABASE_URL,
  // surface a hard error pointing at .env.example so devs know what to do.
  const dbMode = process.env.PAPERCLIP_DB_MODE?.trim();
  const allowEmbeddedFallback = dbMode === "embedded-postgres";
```

Em seguida, no FINAL da função (a partir do bloco que retorna `mode: "embedded-postgres"`, atualmente linhas 254-266), envolva-o num gate:

```ts
  // Embedded fallback: only if explicitly opted-in via PAPERCLIP_DB_MODE.
  if (allowEmbeddedFallback) {
    const port = config?.database?.embeddedPostgresPort ?? 54329;
    const dataDir = resolveHomeAwarePath(
      config?.database?.embeddedPostgresDataDir ?? resolveDefaultEmbeddedPostgresDir(),
    );

    return {
      mode: "embedded-postgres",
      dataDir,
      port,
      source: `embedded-postgres@${port}`,
      configPath,
      envPath,
    };
  }

  // Neither DATABASE_URL nor embedded fallback configured — fail with actionable message.
  throw new Error(
    "No database target configured. Set DATABASE_URL in .env.local (Supabase pooler URL) " +
    "OR set PAPERCLIP_DB_MODE=embedded-postgres to use the local fallback. " +
    "See .env.example for the canonical template.",
  );
```

NÃO altere a parte que detecta `process.env.DATABASE_URL` (lines ~220-228) — essa permanece como primeiro path.

**Passo 2 — `packages/db/drizzle.config.ts`:**

O arquivo atual (10 linhas) só lê `DATABASE_URL`. Migrations DDL precisam de session mode (porta 5432); pooler 6543 não suporta certos DDLs (CREATE TYPE, etc.).

Substitua o conteúdo POR INTEIRO:

```ts
import { defineConfig } from "drizzle-kit";

// Drizzle-kit reads schema from compiled output; run `pnpm db:generate` to ensure
// `dist/schema/*.js` is up-to-date before running migrations.
//
// Connection string preference for `pnpm db:migrate`:
//   1. SUPABASE_DB_URL (session pooler 5432) — preferred for DDL (CREATE TYPE, CREATE FUNCTION,
//      cross-schema operations). Supavisor session mode keeps the connection across statements.
//   2. DATABASE_URL (transaction pooler 6543) — fallback. Most DDL works but some operations
//      (CREATE TYPE inside a transaction with prepared statements off) can be flaky.
//
// CI/GitHub Actions should always set SUPABASE_DB_URL.
const url =
  process.env.SUPABASE_DB_URL?.trim() ||
  process.env.DATABASE_URL?.trim();

if (!url) {
  throw new Error(
    "drizzle-kit requires SUPABASE_DB_URL or DATABASE_URL. " +
    "For local dev, ensure `.env.local` is loaded (e.g. `node --env-file=.env.local`). " +
    "See .env.example for the canonical template.",
  );
}

export default defineConfig({
  schema: "./dist/schema/*.js",
  out: "./src/migrations",
  dialect: "postgresql",
  dbCredentials: { url },
});
```

Note que `drizzle-kit` é invocado com `tsx` no script `migrate` do `packages/db/package.json`. Ele NÃO carrega `.env.local` automaticamente. Mas o `pnpm db:migrate` do root chama `pnpm --filter @paperclipai/db migrate`, e o nó `migrate.ts` (linha 2 do `packages/db/src/migrate.ts`) IMPORTA `resolveMigrationConnection` que já lê o `runtime-config.ts` patchado no Passo 1. Então o caminho `pnpm db:migrate` continua funcionando enquanto o `.env.local` for carregado externamente (ver Tarefa 4 do `.env.example`).

Em rounds onde o usuário roda `pnpm db:generate` (que NÃO usa migrate.ts mas drizzle-kit direto), eles precisam ter `SUPABASE_DB_URL` ou `DATABASE_URL` exportado no shell. Documentar no `.env.example` e no README.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const rt=fs.readFileSync('packages/db/src/runtime-config.ts','utf8');if(!rt.includes('PAPERCLIP_DB_MODE'))process.exit(1);if(!rt.includes('No database target configured'))process.exit(2);const dk=fs.readFileSync('packages/db/drizzle.config.ts','utf8');if(!dk.includes('SUPABASE_DB_URL'))process.exit(3);if(!dk.includes('process.env.DATABASE_URL'))process.exit(4);console.log('OK runtime-config:',rt.length,'chars; drizzle.config:',dk.length,'chars')"</automated>
  </verify>
  <done>
- `runtime-config.ts` requer `PAPERCLIP_DB_MODE=embedded-postgres` para fallback embedded
- `runtime-config.ts` lança erro acionável quando nem DATABASE_URL nem embedded-mode setado
- `drizzle.config.ts` prefere SUPABASE_DB_URL → DATABASE_URL com mensagem clara em ausência de ambos
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: Patch `server/src/index.ts` — disable auto-migrations on startup</name>
  <files>
    server/src/index.ts
  </files>
  <action>
Em `server/src/index.ts`, modifique a função `promptApplyMigrations` (linhas 115-129) para que TTY-ausente NÃO mais default-aprove auto-apply.

ANTES (linhas 115-129):
```ts
async function promptApplyMigrations(migrations: string[]): Promise<boolean> {
  if (process.env.PAPERCLIP_MIGRATION_AUTO_APPLY === "true") return true;
  if (process.env.PAPERCLIP_MIGRATION_PROMPT === "never") return false;
  if (!stdin.isTTY || !stdout.isTTY) return true;  // ← problema: silencioso aprova em watcher

  const prompt = createInterface({ input: stdin, output: stdout });
  try {
    const answer = (await prompt.question(
      `Apply pending migrations (${formatPendingMigrationSummary(migrations)}) now? (y/N): `,
    )).trim().toLowerCase();
    return answer === "y" || answer === "yes";
  } finally {
    prompt.close();
  }
}
```

DEPOIS:
```ts
async function promptApplyMigrations(migrations: string[]): Promise<boolean> {
  // Phase 2 (DB-02): explicit-only. Auto-apply happens ONLY when:
  //   - PAPERCLIP_MIGRATION_AUTO_APPLY=true is set (CI/GitHub Actions path), OR
  //   - User is interactively running `pnpm dev` in a TTY and types "y".
  // Non-TTY contexts (watcher, dev-runner, daemonized server) ALWAYS refuse —
  // ambient auto-apply against a shared Supabase is dangerous (5+ devs disputing
  // migration locks; un-reviewed schema changes shipping silently).
  if (process.env.PAPERCLIP_MIGRATION_AUTO_APPLY === "true") return true;
  if (process.env.PAPERCLIP_MIGRATION_PROMPT === "never") return false;
  if (!stdin.isTTY || !stdout.isTTY) return false;  // ← changed from `return true`

  const prompt = createInterface({ input: stdin, output: stdout });
  try {
    const answer = (await prompt.question(
      `Apply pending migrations (${formatPendingMigrationSummary(migrations)}) now? (y/N): `,
    )).trim().toLowerCase();
    return answer === "y" || answer === "yes";
  } finally {
    prompt.close();
  }
}
```

A diferença é UMA linha: `return true` → `return false` em non-TTY.

ADICIONALMENTE, na função `ensureMigrations` (linhas 130-183), aprimore a mensagem de erro para apontar `pnpm db:migrate` em vez de só `PAPERCLIP_MIGRATION_AUTO_APPLY`. Encontre as DUAS strings idênticas:

```ts
throw new Error(
  `${label} has pending migrations (${formatPendingMigrationSummary(state.pendingMigrations)}). ` +
    "Refusing to start against a stale schema. Run pnpm db:migrate or set PAPERCLIP_MIGRATION_AUTO_APPLY=true.",
);
```

Substitua AS DUAS por:

```ts
throw new Error(
  `${label} has pending migrations (${formatPendingMigrationSummary(state.pendingMigrations)}). ` +
    "Refusing to start against a stale schema (Phase 2 DB-02). " +
    "Apply migrations explicitly: `pnpm db:migrate` (requires SUPABASE_DB_URL set). " +
    "For CI: set PAPERCLIP_MIGRATION_AUTO_APPLY=true.",
);
```

Note que NÃO removemos `PAPERCLIP_MIGRATION_AUTO_APPLY` — esse permanece como opt-in para o pipeline GitHub Actions (Plano 02-04). Apenas garantimos que (a) non-TTY default é refuse, (b) mensagem é acionável.

NÃO altere os tests existentes em `server/src/__tests__/server-startup-feedback-export.test.ts` — eles mockam `inspectMigrations` para retornar `upToDate`, então não tocam neste código path.

Confirme typecheck:
```bash
cd D:/projetos/ddd && pnpm --filter @paperclipai/server exec tsc --noEmit 2>&1 | tail -20
```
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const c=fs.readFileSync('server/src/index.ts','utf8');const idx=c.indexOf('promptApplyMigrations');if(idx<0)process.exit(1);const slice=c.slice(idx,idx+1200);if(!slice.includes('!stdin.isTTY || !stdout.isTTY) return false'))process.exit(2);if(!c.includes('Phase 2 DB-02'))process.exit(3);if(!c.includes('Apply migrations explicitly'))process.exit(4);console.log('OK')"</automated>
  </verify>
  <done>
- `promptApplyMigrations` retorna `false` em non-TTY context (era `true`)
- `PAPERCLIP_MIGRATION_AUTO_APPLY=true` continua funcionando para CI
- Mensagem de erro em `ensureMigrations` cita `pnpm db:migrate` e Phase 2 DB-02
- Typecheck do server continua passando (sem regressões)
  </done>
</task>

<task type="auto">
  <name>Tarefa 4: Sobrescrever `.env.example` com template team-shared</name>
  <files>
    .env.example
  </files>
  <action>
Substitua TODO o conteúdo de `.env.example` por:

```dotenv
# DDD — Paperclip da Equipe — Environment template
#
# 📋 Setup (one-time per developer):
# 1. Copy this file: `cp .env.example .env.local`
# 2. Fill in the values marked TODO_FILL_ME below
# 3. NEVER commit .env.local — it's gitignored. NEVER paste real values into THIS file.
# 4. Pre-commit hook (.husky/pre-commit) will block commits with leaked JWTs anyway.
#
# Phase 2 references: INFRA-04, AUTH-02, AUTH-05.

# ─────────────────────────────────────────────────────────────
# Server
# ─────────────────────────────────────────────────────────────
PORT=3100
SERVE_UI=true

# Cookie prefix derives from this — all team devs MUST share the same value
# so sessions stay coherent across machines (AUTH-02).
PAPERCLIP_INSTANCE_ID=team-shared

# ─────────────────────────────────────────────────────────────
# Database (Supabase Postgres — shared team backend)
#
# Project: bxlczioxgizgvtznukwt (sa-east-1)
# Pooler hostname: aws-1-sa-east-1.pooler.supabase.com (verified empirically; see MIGRATION_AUDIT.md)
# Username pattern: postgres.<project-ref>
#
# DATABASE_URL  → port 6543 (Supavisor transaction pooler) for app runtime.
#                 packages/db/src/client.ts auto-detects port and applies `prepare: false`.
# SUPABASE_DB_URL → port 5432 (session pooler) for Drizzle migrations / DDL.
#                   `pnpm db:migrate` and `pnpm db:generate` use this preferentially.
#
# Direct connection (`db.<ref>.supabase.co:5432`) does NOT work in this env (free tier IPv6-only).
# Use pooler for both runtime AND migrations.
# ─────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres.bxlczioxgizgvtznukwt:TODO_FILL_ME@aws-1-sa-east-1.pooler.supabase.com:6543/postgres
SUPABASE_DB_URL=postgresql://postgres.bxlczioxgizgvtznukwt:TODO_FILL_ME@aws-1-sa-east-1.pooler.supabase.com:5432/postgres

# Optional: Supabase HTTP API endpoints (used only if/when UI talks directly to Supabase
# for Realtime/Storage; not required for v1 with Better Auth + Express API).
SUPABASE_URL=https://bxlczioxgizgvtznukwt.supabase.co
SUPABASE_ANON_KEY=TODO_FILL_ME

# ⚠️  SERVICE ROLE KEY — server-only. NEVER prefix with VITE_ (that exposes to client bundle).
# Pre-commit hook blocks commits adding this in ui/src/** or as VITE_*SERVICE_ROLE*.
SUPABASE_SERVICE_ROLE_KEY=TODO_FILL_ME

# ─────────────────────────────────────────────────────────────
# Authentication (Better Auth, preserved in v1; AUTH-01..04)
# ─────────────────────────────────────────────────────────────
# Generate fresh per environment: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`
# Do NOT reuse `paperclip-dev-secret` from upstream — it's a placeholder.
BETTER_AUTH_SECRET=TODO_FILL_ME

# Optional override; defaults to BETTER_AUTH_SECRET if unset.
# PAPERCLIP_AGENT_JWT_SECRET=

# ─────────────────────────────────────────────────────────────
# Database backend mode (INFRA-06)
# ─────────────────────────────────────────────────────────────
# Default: Supabase (DATABASE_URL must be set above).
# Opt-in fallback: set to `embedded-postgres` to spin up a local embedded Postgres for offline work.
# When embedded mode is active, DATABASE_URL is IGNORED and a local instance runs at port 54329.
# PAPERCLIP_DB_MODE=embedded-postgres

# ─────────────────────────────────────────────────────────────
# Migration policy (DB-02)
# ─────────────────────────────────────────────────────────────
# Default: startup REFUSES to apply pending migrations (5+ devs sharing one Supabase →
# auto-apply is unsafe). Run `pnpm db:migrate` explicitly when needed.
#
# CI ONLY: GitHub Actions sets this to `true` to apply migrations after PR merge to main.
# Local devs: leave UNSET.
# PAPERCLIP_MIGRATION_AUTO_APPLY=true

# ─────────────────────────────────────────────────────────────
# Logs
# ─────────────────────────────────────────────────────────────
LOG_LEVEL=info
```

Não modifique nenhum outro arquivo nesta tarefa. NÃO commit `.env.local` ou modifique-o (já existe e está gitignored com valores reais).
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const c=fs.readFileSync('.env.example','utf8');const required=['DATABASE_URL','SUPABASE_DB_URL','SUPABASE_URL','SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','BETTER_AUTH_SECRET','PAPERCLIP_INSTANCE_ID=team-shared','PAPERCLIP_DB_MODE','PAPERCLIP_MIGRATION_AUTO_APPLY','aws-1-sa-east-1','TODO_FILL_ME'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('missing:',missing);process.exit(1)}if(c.includes('paperclip-dev-secret')){console.error('still references paperclip-dev-secret');process.exit(2)}console.log('OK',c.split('\\n').length,'lines')"</automated>
  </verify>
  <done>
- `.env.example` lista todas as 9 env vars críticas
- Cita `aws-1-sa-east-1` (achado empírico)
- Não tem mais `paperclip-dev-secret` (placeholder upstream)
- Comenta as duas opt-in vars (PAPERCLIP_DB_MODE, PAPERCLIP_MIGRATION_AUTO_APPLY) com instrução clara
  </done>
</task>

<task type="auto">
  <name>Tarefa 5: Verificação integrada — typecheck + dev startup contra `.env.local`</name>
  <files></files>
  <action>
Esta tarefa NÃO modifica arquivos. Valida que os patches anteriores não quebraram nada.

**Passo 1 — Typecheck completo:**
```bash
cd D:/projetos/ddd && pnpm -r typecheck 2>&1 | tail -30
```
Espera-se que termine sem erros novos. Erros pré-existentes (se houver) devem ser idênticos antes/depois.

**Passo 2 — Vitest dos packages tocados:**
```bash
cd D:/projetos/ddd && pnpm --filter @paperclipai/db exec vitest run 2>&1 | tail -20
cd D:/projetos/ddd && pnpm --filter @paperclipai/server exec vitest run --reporter=verbose 2>&1 | tail -30
```
Espera-se: testes de pool config (Tarefa 1) PASSAM; testes existentes do server (`server-startup-feedback-export.test.ts`) continuam passando.

**Passo 3 — Smoke quick: startup com Supabase, migrations vão recusar:**

Como `.env.local` já está populado e `inspectMigrations` será chamado contra Supabase no startup, este passo VALIDA o gate DB-02. Comportamento esperado: server arranca, `inspectMigrations` retorna `pendingMigrations` (porque schema vazio no Supabase), e startup ABORTA com a mensagem nova "Apply migrations explicitly: `pnpm db:migrate`".

```bash
cd D:/projetos/ddd && timeout 30 node --env-file=.env.local --experimental-vm-modules \
  -e "import('./server/dist/index.js').then(m => m.startServer()).catch(e => { console.error('STARTUP FAILED:', e.message); process.exit(0); })" 2>&1 | tail -30
```

(Se `server/dist/index.js` não existe, rode `pnpm --filter @paperclipai/server build` antes; OU pule este passo se for proibitivo de tempo — testes unitários da Tarefa 3 já cobrem o promptApplyMigrations behavior.)

**Resultado esperado para o smoke:**
- Saída inclui a string "Refusing to start against a stale schema (Phase 2 DB-02)"
- OU saída inclui "has pending migrations" + "pnpm db:migrate"
- Exit code é 0 (porque o catch força exit 0)

Se o smoke não roda em 30s (tipicamente porque o build do server é demorado), pule e marque como done — testes unitários cobrem o caminho.

**Passo 4 — Confirmar fluxo embedded ainda funciona (regressão):**

NÃO precisa rodar de fato. Apenas confirmar via grep que o caminho está preservado:
```bash
cd D:/projetos/ddd && node -e "const fs=require('fs');const c=fs.readFileSync('packages/db/src/runtime-config.ts','utf8');if(!c.includes('embedded-postgres'))process.exit(1);if(!c.includes('PAPERCLIP_DB_MODE'))process.exit(2);console.log('embedded fallback path preserved')"
```
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && pnpm --filter @paperclipai/db exec vitest run --reporter=verbose 2>&1 | grep -E "(✓|PASS|FAIL|Test Files)" | tail -10</automated>
  </verify>
  <done>
- `pnpm -r typecheck` passa (ou tem só erros pré-existentes idênticos ao baseline)
- `pnpm --filter @paperclipai/db exec vitest run` inclui passes para `client-pool-config.test.ts`
- Smoke passou (ou foi skipped com motivo documentado)
- Embedded fallback path está preservado em runtime-config.ts
  </done>
</task>

</tasks>

<verification>
- INFRA-02 ✓ — `prepare: false` aplicado quando porta 6543 (Tarefa 1, testado)
- INFRA-03 ✓ — `runtime-config.ts` privilegia `process.env.DATABASE_URL` (já era o caso; reforçado com gate de embedded fallback)
- INFRA-04 ✓ — `.env.example` lista 9 env vars (Tarefa 4)
- INFRA-05 ✓ — pool `max:5, idle_timeout:20` aplicado (Tarefa 1)
- INFRA-06 ✓ — embedded só ativa via `PAPERCLIP_DB_MODE=embedded-postgres` opt-in (Tarefa 2)
- DB-02 ✓ — auto-migrations no startup desabilitadas por default; opt-in via env var (Tarefa 3)
- Sem regressão de typecheck nem testes existentes (Tarefa 5)
</verification>

<success_criteria>
- [ ] `packages/db/src/client.ts` exporta `buildPostgresOptions` e usa em `createDb`
- [ ] 4 testes em `client-pool-config.test.ts` passam
- [ ] `packages/db/src/runtime-config.ts` requer `PAPERCLIP_DB_MODE=embedded-postgres` para fallback
- [ ] `packages/db/drizzle.config.ts` lê `SUPABASE_DB_URL ?? DATABASE_URL`
- [ ] `server/src/index.ts` non-TTY default é `false` em `promptApplyMigrations`
- [ ] `.env.example` tem 9 env vars críticas + comentários acionáveis
- [ ] `pnpm -r typecheck` passa sem regressões novas
</success_criteria>

<output>
After completion, create `.planning/phases/02-migra-o-de-storage-para-supabase/02-03-SUMMARY.md` describing:
- Arquivos tocados (6) + linhas modificadas
- Comportamento Supavisor port-aware: 6543 → prepare:false; 5432 → pool only; outras → default
- Estratégia DB-02: opt-in only; CI continua funcionando via PAPERCLIP_MIGRATION_AUTO_APPLY=true
- Embedded fallback preservado mas opt-in
- Próximo plano: 02-04 (apply migrations) e 02-05 (auth wiring) — ambos podem executar em paralelo na Wave 3
</output>
