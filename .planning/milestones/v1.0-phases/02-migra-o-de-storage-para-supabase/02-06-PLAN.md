---
phase: 02-migra-o-de-storage-para-supabase
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - .planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md
  - scripts/phase-02-supabase-smoke.mjs
autonomous: false
requirements: [DB-01, AUTH-01, AUTH-02, AUTH-04]

must_haves:
  truths:
    - "`pnpm dev` arranca contra Supabase remoto sem erros (DATABASE_URL=$SUPABASE_DB_URL ou =DATABASE_URL pooler 6543)"
    - "Server log mostra: 'Database target: postgres (DATABASE_URL)' OU equivalente — confirma que NÃO está usando embedded"
    - "API health endpoint `/api/health` retorna 200 + body sane"
    - "Better Auth signup endpoint funciona: POST /api/auth/sign-up/email com {email, password, name} retorna 200 + cookie `paperclip-team-shared.session_token` setado"
    - "Após signup, GET /api/companies retorna ≥1 company (paperclip auto-cria default)"
    - "Cookie persiste em second request: GET /api/auth/get-session retorna user com mesmo id"
    - "USER VERIFICATION: dois 'users' (mesmo dev simulando ou dois devs reais em máquinas distintas) cadastram, e ambos veem mesma company OU veem que estado compartilhado funciona"
    - "Wart conhecido (stale runtime-services em Windows) NÃO bloqueia — se aparecer, documentar mas não bloquear smoke"
  artifacts:
    - path: ".planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md"
      provides: "Log empírico do smoke test E2E + screenshots/output do user verification"
      contains: "## User Verification Outcome"
    - path: "scripts/phase-02-supabase-smoke.mjs"
      provides: "Script Node ESM que automatiza signup + login + fetch contra server local"
      min_lines: 80
  key_links:
    - from: "scripts/phase-02-supabase-smoke.mjs"
      to: "http://localhost:3100/api/auth/sign-up/email"
      via: "fetch POST com { email, password, name } body, expects 200 + Set-Cookie header"
      pattern: "sign-up/email"
    - from: "SMOKE-TEST-LOG.md"
      to: "Supabase project bxlczioxgizgvtznukwt"
      via: "Verificação SQL pós-signup: SELECT count(*) FROM \"user\" should be ≥1"
      pattern: "bxlczioxgizgvtznukwt"
---

<objective>
Smoke test E2E que prova que TUDO da Fase 2 funciona em conjunto: server arranca contra Supabase, Better Auth signup grava em `public.user` no Supabase, sessão persiste via cookie `paperclip-team-shared`. Termina com checkpoint de verificação manual onde o usuário confirma que o app funciona no browser e (idealmente) que dois "devs" simulados/reais veem o mesmo estado.

Purpose: Os planos 02-01 a 02-05 são unidades isoladas (audit, hooks, patches, migrations, tests). O VALOR REAL da Fase 2 é o sistema integrado funcionando como descrito em ROADMAP.md Success Criterion #1: "Dois devs em máquinas diferentes fazem login com Better Auth e veem a mesma company". Sem este smoke test, "Fase 2 completa" é hipotético.

Output: SMOKE-TEST-LOG.md com evidência empírica + checkpoint de aprovação humana.

⚠️  Este plano tem `autonomous: false`. Inclui `checkpoint:human-verify` ao final pois validar "dois devs veem o mesmo estado" requer ação humana (abrir UI no browser, observar, aprovar).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_APPLY_LOG.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-03-SUMMARY.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-04-SUMMARY.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-05-SUMMARY.md
@.planning/phases/01-fork-hard-cerim-nia-de-corte/SMOKE-TEST-LOG.md

@package.json
@.env.local
@scripts/dev-runner.ts

<interfaces>
<!-- Endpoints relevantes do paperclip (verificáveis em server/src/routes/) -->

GET  /api/health                          → 200 { ok: true, ... }
POST /api/auth/sign-up/email              → Better Auth handler; body { email, password, name } → 200 + Set-Cookie
POST /api/auth/sign-in/email              → Better Auth handler; body { email, password } → 200 + Set-Cookie
GET  /api/auth/get-session                → Better Auth handler; reads cookie, returns user/session
GET  /api/companies                       → 200 [{ id, name, ... }]; requires auth in 'authenticated' mode

Cookie name: `${cookiePrefix}.session_token` (Better Auth default), so with prefix `paperclip-team-shared`
the cookie is `paperclip-team-shared.session_token`.

Server arranca via:
  pnpm dev      (alias para scripts/dev-runner.ts watch)
  pnpm dev:once (alias para scripts/dev-runner.ts dev — single run, sem watcher)

Para smoke automatizado, `pnpm dev:once` é melhor pq não rebuilda em mudança.

Mas dev-runner spawnea o server como child process e roda em foreground. Para smoke automatizado precisamos:
- Subir server em background
- Esperar `/api/health` ficar 200
- Rodar fluxo
- Matar server

Phase 1 SMOKE-TEST-LOG.md teve user verification visual no browser (UI dark-theme observada). Este plano segue padrão semelhante mas com signup REAL contra Supabase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Implementar `scripts/phase-02-supabase-smoke.mjs` — automated smoke flow</name>
  <files>
    scripts/phase-02-supabase-smoke.mjs
  </files>
  <action>
Crie o script ESM:

```js
#!/usr/bin/env node
// Phase 2 (Migração Supabase) E2E smoke test.
// Prerequisites:
//   - .env.local must be loaded (run with `node --env-file=.env.local scripts/phase-02-supabase-smoke.mjs`)
//   - `pnpm dev` (or `pnpm dev:once`) must be running in a separate terminal — THIS script does NOT start the server
//   - Server should be at http://localhost:3100 (or PORT from env)
//
// Flow:
//   1. GET /api/health — confirm server is up and reachable
//   2. POST /api/auth/sign-up/email — create user A
//   3. GET /api/auth/get-session — confirm cookie session works
//   4. GET /api/companies — confirm authenticated request returns data
//   5. Verify user row exists in Supabase (direct SQL query)
//   6. POST /api/auth/sign-up/email — create user B (different email)
//   7. Verify both users in Supabase
//
// Outputs structured JSON to stdout for the SMOKE-TEST-LOG.md to consume.

import { writeFileSync } from "node:fs";

const SERVER_URL = process.env.SERVER_URL ?? "http://localhost:3100";
const SUPABASE_DB_URL = process.env.SUPABASE_DB_URL ?? process.env.DATABASE_URL;

if (!SUPABASE_DB_URL) {
  console.error("ERROR: SUPABASE_DB_URL not set in env. Load .env.local first.");
  process.exit(1);
}

const log = (step, status, data) => {
  const entry = { ts: new Date().toISOString(), step, status, data };
  console.log(JSON.stringify(entry));
  return entry;
};

const TIMESTAMP = Date.now();
const USER_A = { email: `smoke-a-${TIMESTAMP}@ddd.test`, password: "TestPass123!@#", name: `Smoke A ${TIMESTAMP}` };
const USER_B = { email: `smoke-b-${TIMESTAMP}@ddd.test`, password: "TestPass123!@#", name: `Smoke B ${TIMESTAMP}` };

const results = [];

async function main() {
  // Step 1: Health check
  try {
    const r = await fetch(`${SERVER_URL}/api/health`);
    const body = await r.text();
    results.push(log("health", r.ok ? "pass" : "fail", { status: r.status, body: body.slice(0, 200) }));
    if (!r.ok) throw new Error(`Health check failed: ${r.status}`);
  } catch (e) {
    results.push(log("health", "fail", { error: String(e) }));
    process.exit(2);
  }

  // Step 2: Signup user A
  let cookieA = null;
  try {
    const r = await fetch(`${SERVER_URL}/api/auth/sign-up/email`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify(USER_A),
    });
    const body = await r.json().catch(() => ({}));
    cookieA = r.headers.get("set-cookie");
    results.push(log("signup-a", r.ok ? "pass" : "fail", {
      status: r.status,
      cookie_set: !!cookieA,
      cookie_prefix_match: cookieA?.includes("paperclip-team-shared") ?? false,
      body: body,
    }));
    if (!r.ok || !cookieA) throw new Error(`Signup A failed: ${r.status}`);
  } catch (e) {
    results.push(log("signup-a", "fail", { error: String(e) }));
    process.exit(3);
  }

  // Step 3: Get session with cookie
  try {
    const r = await fetch(`${SERVER_URL}/api/auth/get-session`, { headers: { cookie: cookieA } });
    const body = await r.json().catch(() => ({}));
    results.push(log("get-session", r.ok && body?.user?.email === USER_A.email ? "pass" : "fail", {
      status: r.status,
      user_email_matches: body?.user?.email === USER_A.email,
      body: body,
    }));
  } catch (e) {
    results.push(log("get-session", "fail", { error: String(e) }));
  }

  // Step 4: Authenticated GET /api/companies
  try {
    const r = await fetch(`${SERVER_URL}/api/companies`, { headers: { cookie: cookieA } });
    const body = await r.json().catch(() => null);
    results.push(log("get-companies", r.ok ? "pass" : "fail", {
      status: r.status,
      count: Array.isArray(body) ? body.length : null,
      first_company_id: Array.isArray(body) && body[0] ? body[0].id : null,
    }));
  } catch (e) {
    results.push(log("get-companies", "fail", { error: String(e) }));
  }

  // Step 5: Verify user row in Supabase
  try {
    const postgres = (await import("postgres")).default;
    const sql = postgres(SUPABASE_DB_URL, { max: 1 });
    const rows = await sql`SELECT email, name FROM "user" WHERE email = ${USER_A.email}`;
    await sql.end();
    results.push(log("supabase-user-a", rows.length === 1 ? "pass" : "fail", {
      found_count: rows.length,
      row: rows[0] ?? null,
    }));
  } catch (e) {
    results.push(log("supabase-user-a", "fail", { error: String(e) }));
  }

  // Step 6: Signup user B
  let cookieB = null;
  try {
    const r = await fetch(`${SERVER_URL}/api/auth/sign-up/email`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify(USER_B),
    });
    cookieB = r.headers.get("set-cookie");
    results.push(log("signup-b", r.ok ? "pass" : "fail", { status: r.status, cookie_set: !!cookieB }));
  } catch (e) {
    results.push(log("signup-b", "fail", { error: String(e) }));
  }

  // Step 7: Both users present in Supabase
  try {
    const postgres = (await import("postgres")).default;
    const sql = postgres(SUPABASE_DB_URL, { max: 1 });
    const rows = await sql`SELECT email FROM "user" WHERE email IN (${USER_A.email}, ${USER_B.email})`;
    await sql.end();
    results.push(log("supabase-both-users", rows.length === 2 ? "pass" : "fail", {
      found_count: rows.length,
      emails: rows.map(r => r.email),
    }));
  } catch (e) {
    results.push(log("supabase-both-users", "fail", { error: String(e) }));
  }

  // Final summary
  const passes = results.filter(r => r.status === "pass").length;
  const fails = results.filter(r => r.status === "fail").length;
  const summary = {
    total_steps: results.length,
    passes,
    fails,
    overall: fails === 0 ? "PASS" : "FAIL",
    user_a_email: USER_A.email,
    user_b_email: USER_B.email,
    timestamp: new Date().toISOString(),
  };

  // Write JSON output for SMOKE-TEST-LOG.md to consume
  const outPath = ".planning/phases/02-migra-o-de-storage-para-supabase/smoke-test-results.json";
  writeFileSync(outPath, JSON.stringify({ summary, results }, null, 2));
  console.log("\n=== SMOKE TEST RESULT ===");
  console.log(JSON.stringify(summary, null, 2));
  console.log(`\nFull results: ${outPath}`);

  process.exit(fails === 0 ? 0 : 1);
}

main().catch(e => {
  console.error("FATAL:", e);
  process.exit(99);
});
```

Não rode ainda — Tarefa 2 cuidará de orquestrar a execução com o server arrancado.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -c scripts/phase-02-supabase-smoke.mjs && wc -l scripts/phase-02-supabase-smoke.mjs</automated>
  </verify>
  <done>
- `scripts/phase-02-supabase-smoke.mjs` existe, parse-clean (sintaxe válida)
- ≥80 linhas
- Não foi executado ainda (próxima tarefa orquestra)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Executar smoke test e capturar evidência</name>
  <files>
    .planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md
  </files>
  <action>
**Passo 1 — Subir o server contra Supabase em background.**

Em uma sessão de terminal (ou via Bash run_in_background):
```bash
cd D:/projetos/ddd && \
  set -a && source .env.local && set +a && \
  pnpm dev:once 2>&1 | tee server-startup.log
```

(Se a Bash do Claude rodar em Windows pure, ajuste para PowerShell.)

NOTA: `pnpm dev:once` arranca server + UI single-shot e fica rodando em foreground. Use `run_in_background: true` na Bash tool. Depois espere ~30s para o server bootar.

**Passo 2 — Confirmar server está up:**

```bash
sleep 30 && curl -sf http://localhost:3100/api/health || echo "SERVER NOT READY"
```

Se "SERVER NOT READY", aguarde mais 30s (pode estar buildando UI primeiro). Se persistir após 90s, inspecionar `server-startup.log` — possivelmente DATABASE_URL não está sendo carregado, ou Supabase está bloqueando.

**Verificações esperadas no startup log:**
- "Database target: postgres" (NÃO "embedded-postgres")
- Ausência de "Refusing to start against a stale schema" (porque 02-04 aplicou migrations)
- Server listening em :3100

Se aparece "Refusing to start against a stale schema", significa que (a) 02-04 não foi completado, OU (b) o server está sendo apontado para uma URL diferente. Diagnosticar antes de prosseguir.

**Passo 3 — Rodar o smoke test:**

```bash
cd D:/projetos/ddd && \
  node --env-file=.env.local scripts/phase-02-supabase-smoke.mjs 2>&1 | tee smoke-output.log
```

Aguarde conclusão. Espera-se exit 0 e linha final `"overall": "PASS"`.

**Passo 4 — Limpeza dos test users no Supabase (opcional mas recomendado):**

Os usuários `smoke-a-{TIMESTAMP}@ddd.test` e `smoke-b-{TIMESTAMP}@ddd.test` ficam no DB para sempre se não limpar. NÃO é destrutivo deletá-los, mas POLLUI o banco compartilhado.

```bash
node --env-file=.env.local -e "
const postgres = require('postgres');
const sql = postgres(process.env.SUPABASE_DB_URL, { max: 1 });
sql\`DELETE FROM \"user\" WHERE email LIKE 'smoke-%@ddd.test'\`
  .then(r => { console.log('cleaned up:', r.count, 'smoke users'); return sql.end(); });
"
```

(Esta limpeza é opcional. Pode deixar para o user verification confirmar visualmente que os usuários existem antes de limpar.)

**Passo 5 — Escrever SMOKE-TEST-LOG.md** (lendo `smoke-test-results.json`):

```markdown
# Phase 2 Smoke Test Log — Supabase + Better Auth E2E

**Phase:** 02 (Migração de Storage para Supabase)
**Test type:** End-to-end automated smoke + manual user verification
**Run at:** {ISO-8601}
**Run by:** Claude (executor de plano 02-06) + manual verification by user

## Pre-conditions verified

- ✅ Plano 02-01: MIGRATION_AUDIT.md exists; documented 71 migrations strategy
- ✅ Plano 02-02: Pre-commit hook installed and protects ui/src/** from JWT leaks
- ✅ Plano 02-03: createDb uses prepare:false on port 6543; auto-migrations disabled
- ✅ Plano 02-04: Schema applied to bxlczioxgizgvtznukwt; ≥30 tables in public
- ✅ Plano 02-05: Better Auth wiring tests pass; cookie prefix = paperclip-team-shared

## Server startup

Connection target observed in logs:
```
{paste relevant lines from server-startup.log showing "Database target: postgres ..."}
```

Startup time to /api/health 200: {N seconds}
Migrations status at startup: upToDate (no pending) — confirms 02-04 outcome.

## Automated smoke results

```json
{paste contents of .planning/phases/02-migra-o-de-storage-para-supabase/smoke-test-results.json}
```

### Step-by-step interpretation

1. **health:** {pass/fail} — server reachable, body sane
2. **signup-a:** {pass/fail} — POST /api/auth/sign-up/email succeeded; cookie has prefix `paperclip-team-shared`: {true/false}
3. **get-session:** {pass/fail} — cookie roundtrip works; user email matches
4. **get-companies:** {pass/fail} — authenticated request returns {N} companies
5. **supabase-user-a:** {pass/fail} — direct SQL confirms user A row in `public.user`
6. **signup-b:** {pass/fail} — second user created
7. **supabase-both-users:** {pass/fail} — direct SQL confirms both rows persist

Overall: **{PASS|FAIL}**

## Empirical findings

{Document any surprises:}
- Was server startup faster/slower than Phase 1 baseline (~15min)?
- Cookie prefix observed in Set-Cookie header: `{exact value from log}`
- Any warnings in startup log? (e.g. "Wart: stale runtime-services")
- Latency observation: average request time during smoke (rough estimate)

## User verification (Pending — see Tarefa 3)

To be completed at the human-verify checkpoint.

## References

- `.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_APPLY_LOG.md`
- `.planning/phases/02-migra-o-de-storage-para-supabase/02-05-SUMMARY.md`
- ROADMAP.md Phase 2 Success Criterion #1

___END_LOG___
*Smoke complete: {ISO-8601}*
```

**Passo 6 — Cleanup logs intermediários** (smoke-output.log, server-startup.log podem ficar; smoke-test-results.json é referenciado pelo MD então mantenha):
```bash
rm smoke-output.log server-startup.log 2>/dev/null
```

**Passo 7 — Matar o server:**
- Se `pnpm dev:once` foi rodado em background (run_in_background), pode matar com KillShell
- Se foi rodado em terminal separado, lembrar usuário de fazer Ctrl+C ou `pnpm dev:stop`

NOTA importante sobre o wart Windows herdado da Phase 1: stale `~/.paperclip/instances/default/runtime-services/` pode persistir após kill — não é blocker.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const p='.planning/phases/02-migra-o-de-storage-para-supabase/smoke-test-results.json';if(!fs.existsSync(p))process.exit(1);const r=JSON.parse(fs.readFileSync(p,'utf8'));if(r.summary.overall!=='PASS'){console.error('smoke failed:',r.summary);process.exit(2)}const md=fs.readFileSync('.planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md','utf8');if(!md.includes('paperclip-team-shared')||!md.includes('bxlczioxgizgvtznukwt'))process.exit(3);console.log('OK passes:',r.summary.passes,'fails:',r.summary.fails)"</automated>
  </verify>
  <done>
- Server arrancou contra Supabase (NOT embedded)
- `smoke-test-results.json` existe; `summary.overall === "PASS"`
- 7 steps passam (health, signup-a, get-session, get-companies, supabase-user-a, signup-b, supabase-both-users)
- `SMOKE-TEST-LOG.md` redigido com evidências
- Cookie observado contém `paperclip-team-shared`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: Checkpoint — usuário verifica visualmente que app funciona contra Supabase</name>
  <action>Pause execution and wait for human to perform the verification steps in `<how-to-verify>` below. After human types "aprovado" or describes failures, resume.</action>
  <what-built>
- Server arrancando contra Supabase remoto (não mais embedded local)
- Better Auth signup grava em `public.user` no Supabase compartilhado
- Cookie de sessão tem prefix `paperclip-team-shared`
- API /api/companies retorna dados após login
- Smoke automatizado confirmou 7/7 steps via curl + SQL direto
  </what-built>
  <how-to-verify>
**O usuário deve fazer:**

1. **Visual confirmation no browser:**
   - Server ainda está rodando? Se não, executar `pnpm dev` (com `.env.local` carregado) em um terminal — esperar ~1min para subir.
   - Abrir http://localhost:3100 em um browser limpo (modo anônimo recomendado, para não conflitar com cookies de sessões anteriores).
   - Esperado: tela de login do paperclip, dark theme.
   - Fazer signup com email/password REAL (não os de smoke; podem ter sido limpos).
     - Email: seu email de trabalho ou `dev-{seu-nome}@ddd.local`
     - Password: qualquer (ex: `Test1234!`)
   - Após signup, deve aparecer dashboard com pelo menos uma company default.

2. **Cookie verification:**
   - Abrir DevTools → Application → Cookies → http://localhost:3100
   - Verificar: existe cookie `paperclip-team-shared.session_token` (ou similar com prefixo `paperclip-team-shared`)
   - SE NÃO TIVER esse prefixo: AUTH-02 falhou; reportar.

3. **Supabase verification:**
   - Abrir Supabase dashboard → projeto bxlczioxgizgvtznukwt → Table Editor → tabela `user`
   - Esperado: ver SUA row de signup com o email que você usou
   - Vai aparecer também rows de smoke se Tarefa 2 não fez cleanup — isso é OK

4. **(Opcional, se outro dev disponível) — Multi-dev verification (Success Criterion #1 da Fase 2):**
   - Outro dev em outra máquina clona o repo, copia `.env.example` → `.env.local`, preenche os valores (peça as credenciais).
   - `pnpm install` + `pnpm dev` — deve subir igual ao seu.
   - Outro dev faz signup com email diferente.
   - Outro dev DEVE ver a company que você criou (ou ambos veem mesma company default — depende do flow de invite que ainda é Fase 3).

5. **Aprovação:**
   - Se TUDO acima funcionou: digite `aprovado` no chat.
   - Se algo falhou ou está confuso: descreva o que aconteceu.

**O que NÃO precisa funcionar nesta fase (deferido):**

- Onboarding/invite formal (TEAM-01, Fase 3)
- README de setup polido (TEAM-02, Fase 3)
- Setup script `pnpm setup` (TEAM-03, Fase 3)
- Multi-dev cross-machine confirmado (TEAM-04, Fase 3 — single-dev signup contra Supabase já satisfaz DB-01..05 + AUTH-01..04 desta fase)
  </how-to-verify>
  <resume-signal>Digite "aprovado" para finalizar a Fase 2; ou descreva os problemas observados.</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 4: Atualizar SMOKE-TEST-LOG.md com aprovação humana</name>
  <files>
    .planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md
  </files>
  <action>
Após o checkpoint resolver com `aprovado` (ou com falhas que precisam de gap closure):

Caso "aprovado":

Adicionar ao SMOKE-TEST-LOG.md (substituir a seção "## User verification (Pending)") o conteúdo:

```markdown
## User Verification Outcome

**Approved at:** {ISO-8601}
**Approver:** {user identifier — seu email ou "owner"}

### Visual confirmations
- ✅ http://localhost:3100 carrega tela de login com dark theme
- ✅ Signup com email/password real funciona; redireciona para dashboard
- ✅ Dashboard mostra ≥1 company
- ✅ Cookie `paperclip-team-shared.session_token` presente em DevTools
- ✅ Row do user signup visível na tabela `public.user` do Supabase Studio

### Multi-dev confirmation (if performed)
{describe outcome — or "Single-dev only this phase; multi-dev validation deferred to Phase 3 TEAM-04"}

### Findings worth carrying forward
{capture any gotchas: e.g., server took longer than expected to boot, UI rendered slow, etc. — these inform Phase 3 TEAM-05 troubleshooting doc}

## Phase 2 Status

**Complete.** All 16 requirements satisfied:
- INFRA-01..06 ✓ (planos 02-01, 02-03)
- DB-01..05 ✓ (planos 02-04, 02-03 for DB-02)
- AUTH-01..05 ✓ (planos 02-05, 02-02 for AUTH-05)

Wart from Phase 1 still tracked: stale `~/.paperclip/instances/default/runtime-services/` on Windows. Defer to Phase 3 TEAM-05.

___END_LOG___
*Phase 2 verification complete: {ISO-8601}*
```

Caso "falhas observadas":

NÃO marcar a fase como completa. Adicionar seção "## User Verification — Failures" listando o que não funcionou. Disparar `/planejar-fase 2 --gaps` para criar plano de fechamento de lacunas.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const c=fs.readFileSync('.planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md','utf8');if(!c.includes('User Verification Outcome'))process.exit(1);if(!c.includes('Phase 2 Status'))process.exit(2);const positive=c.includes('All 16 requirements satisfied')||c.includes('approved');const negative=c.includes('User Verification — Failures');if(!positive&&!negative){console.error('Neither approval nor failures section present');process.exit(3)}console.log('OK',positive?'APPROVED':'FAILURES NOTED')"</automated>
  </verify>
  <done>
- SMOKE-TEST-LOG.md atualizado com outcome do checkpoint
- Se aprovado: seção "Phase 2 Status: Complete" + 16 requirements listados
- Se falhas: seção "Failures" + recomendação de `/planejar-fase 2 --gaps`
  </done>
</task>

</tasks>

<verification>
- DB-01 ✓ — schema aplicado validado por SQL direto contra Supabase (Tarefa 2 step 5, 7)
- AUTH-01 ✓ — Better Auth signup roundtrip contra Supabase (Tarefa 2 step 2-5)
- AUTH-02 ✓ — cookie prefix `paperclip-team-shared` observado (Tarefa 2 step 2 + checkpoint)
- AUTH-04 ✓ — email/password signup funcional (Tarefa 2 step 2)
- E2E integration confirmed: 7 automated steps + human visual confirmation
</verification>

<success_criteria>
- [ ] `scripts/phase-02-supabase-smoke.mjs` existe e roda
- [ ] `smoke-test-results.json` tem `summary.overall === "PASS"` (7/7 steps)
- [ ] `SMOKE-TEST-LOG.md` documenta startup + smoke + user verification
- [ ] Server logs confirmam "Database target: postgres" (não embedded)
- [ ] Cookie observado tem prefixo `paperclip-team-shared`
- [ ] User confirmou visualmente "aprovado" OU disparou gap closure
- [ ] Phase 2 status declarado: Complete (com 16/16 requirements) OU pending (com lista de falhas)
</success_criteria>

<output>
After completion, create `.planning/phases/02-migra-o-de-storage-para-supabase/02-06-SUMMARY.md` describing:
- Smoke automatizado: passou X/7 steps
- User verification outcome: aprovado | falhas (com lista)
- Phase 2 declarada complete | gaps remaining
- Wart Phase 1 ainda diferido (Phase 3 TEAM-05)
- Próxima fase: Phase 3 (Workflow de Equipe + Onboarding)
</output>
