---
phase: 05-multi-account-claude-code-swap-implementacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/adapters/claude-local/src/server/parse.ts
  - server/src/__tests__/claude-local-adapter-quota-detection.test.ts
autonomous: true
requirements: [MULTI-06]

must_haves:
  truths:
    - "detectClaudeQuotaExhausted retorna sub-tipo correto para cada um dos 6 tipos da taxonomia"
    - "Função reusa CLAUDE_TRANSIENT_UPSTREAM_RE existente como gate top-level (sem duplicação)"
    - "Ordem de discriminadores garante que weekly não é confundido com daily, e session_5h não é confundido com daily"
    - "Tests passam em CI (vitest) reusando fixtures do spike Phase 4"
  artifacts:
    - path: "packages/adapters/claude-local/src/server/parse.ts"
      provides: "Função detectClaudeQuotaExhausted exportada (MULTI-06)"
      contains: "export function detectClaudeQuotaExhausted"
    - path: "server/src/__tests__/claude-local-adapter-quota-detection.test.ts"
      provides: "Tests para os 6 sub-tipos + gate negativo"
      contains: "describe.*detectClaudeQuotaExhausted"
  key_links:
    - from: "packages/adapters/claude-local/src/server/parse.ts"
      to: "CLAUDE_TRANSIENT_UPSTREAM_RE (linha 13)"
      via: "reuse direto como top-level gate"
      pattern: "CLAUDE_TRANSIENT_UPSTREAM_RE\\.test"
    - from: "server/src/__tests__/claude-local-adapter-quota-detection.test.ts"
      to: ".planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/"
      via: "fs.readFileSync com path relativo"
      pattern: "readFileSync.*prototype/fixtures"
---

<objective>
Adicionar a função `detectClaudeQuotaExhausted` em `parse.ts` que classifica saídas do Claude Code CLI em 6 sub-tipos da taxonomia 429 (rpm_transient, tpm_transient, daily_quota, weekly_quota, session_5h, org_tier), reusando `CLAUDE_TRANSIENT_UPSTREAM_RE` existente (parse.ts:13) como gate top-level. Tests cobrem cada sub-tipo + gate negativo, reusando fixtures capturadas no spike Phase 4.

Purpose: Habilitar o serviço de rotação (MULTI-04) a saber QUE TIPO de exhaustão ocorreu, para popular `lastQuotaWindowsJson[type]` corretamente e calcular `exhaustedUntil` per-tipo (cooldowns distintos: 5h vs daily vs weekly).

Output: Função pública em parse.ts + suite de tests vitest passando.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md
@.planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md
@.planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts
@packages/adapters/claude-local/src/server/parse.ts

<interfaces>
<!-- Exports atuais relevantes de parse.ts (linhas 12-16) — serão REUSADOS, não duplicados: -->

```typescript
const CLAUDE_TRANSIENT_UPSTREAM_RE =
  /(?:rate[-\s]?limit(?:ed)?|rate_limit_error|too\s+many\s+requests|\b429\b|overloaded(?:_error)?|server\s+overloaded|service\s+unavailable|\b503\b|\b529\b|high\s+demand|try\s+again\s+later|temporarily\s+unavailable|throttl(?:ed|ing)|throttlingexception|servicequotaexceededexception|out\s+of\s+extra\s+usage|extra\s+usage\b|claude\s+usage\s+limit\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|usage\s+limit\s+reached|usage\s+cap\s+reached)/i;

const CLAUDE_EXTRA_USAGE_RESET_RE =
  /(?:out\s+of\s+extra\s+usage|extra\s+usage|usage\s+limit\s+reached|usage\s+cap\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|claude\s+usage\s+limit\s+reached)[\s\S]{0,80}?\bresets?\s+(?:at\s+)?([^\n()]+?)(?:\s*\(([^)]+)\))?(?:[.!]|\n|$)/i;
```

<!-- Função extractClaudeRetryNotBefore JÁ existe em execute.ts:640 e usa CLAUDE_EXTRA_USAGE_RESET_RE — NÃO duplicar; chamada externa pelo orchestrator. -->

<!-- Reference: prototype/detect-quota-exhausted.ts (Phase 4 spike) demonstra a arquitetura final: -->
<!-- - export type QuotaType com 7 valores (6 + 'unknown') -->
<!-- - detectClaudeQuotaExhausted recebe string, retorna { type, confidence, retryAt, source } -->
<!-- - Sub-discriminators na ordem: weekly → session_5h → daily → tpm → org → rpm — most-specific-first -->
<!-- - Caveat de ambiguidade: "claude usage limit reached" aparece em daily E session_5h; -->
<!--   exigir "5-hour" literal para classificar como session_5h, senão default daily. -->
</interfaces>

<fixtures_path>
.planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/
(arquivos .txt stubs com mensagens canônicas — substituídos por fixtures reais quando UAT-04-01 completar)
</fixtures_path>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar detectClaudeQuotaExhausted em parse.ts</name>
  <files>packages/adapters/claude-local/src/server/parse.ts</files>
  <read_first>
    - packages/adapters/claude-local/src/server/parse.ts (linhas 1-80 — entender exports existentes; APPEND no fim, NÃO modificar regex linhas 12-15)
    - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts (referência de arquitetura)
    - .planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md (tabela 6-tipos com tokens canônicos)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-14: shape da função; D-15: ordem dos discriminators OBRIGATÓRIA)
  </read_first>
  <behavior>
    - Test 1 (gate negativo): input string vazia ou sem token transient_upstream → `{ detected: false, type: null, retryAt: null, confidence: 'low', rawMatch: null }`
    - Test 2 (session_5h): input com "5-hour limit reached" → `type: 'session_5h'`, confidence `'high'`, retryAt extraído de "resets at"
    - Test 3 (weekly_quota): input com "weekly limit reached" → `type: 'weekly_quota'`, confidence `'high'`
    - Test 4 (daily_quota): input com "claude usage limit reached" + "resets at" SEM "5-hour" e SEM "weekly" → `type: 'daily_quota'`
    - Test 5 (tpm_transient): input com "tokens per minute" → `type: 'tpm_transient'`
    - Test 6 (rpm_transient): input com "rate_limit_error" / "429" / "too many requests" → `type: 'rpm_transient'`
    - Test 7 (org_tier fallback): input com "503" / "overloaded_error" / "service unavailable" SEM tokens RPM/TPM/usage → `type: 'org_tier'`, confidence `'low'`
    - Test 8 (ambiguidade): input "claude usage limit reached" SEM "5-hour" → roteia para `daily_quota` (não session_5h) — bug fix do protótipo
  </behavior>
  <action>
    **APPEND no final de `packages/adapters/claude-local/src/server/parse.ts`** (não modificar linhas 1-580 do arquivo existente):

    ```typescript
    // ============================================================
    // Quota exhaustion classifier (MULTI-06, Phase 5)
    // ============================================================
    // Reusa CLAUDE_TRANSIENT_UPSTREAM_RE (linha 13) como gate top-level.
    // Sub-discriminators determinam qual tipo da taxonomia 6-tipos foi atingido.
    // Ver: .planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md

    export type ClaudeQuotaType =
      | "rpm_transient"
      | "tpm_transient"
      | "daily_quota"
      | "weekly_quota"
      | "session_5h"
      | "org_tier";

    export interface ClaudeQuotaDetection {
      detected: boolean;
      type: ClaudeQuotaType | null;
      retryAt: Date | null;
      confidence: "high" | "medium" | "low";
      rawMatch: string | null;
    }

    // Sub-discriminators — ordem MOST-SPECIFIC-FIRST conforme D-15.
    // Ambiguidade resolvida: "claude usage limit reached" mapeia para daily_quota
    // a menos que "5-hour" esteja explicitamente presente.
    const SESSION_5H_RE = /5[-\s]?hour\s+limit\s+reached/i;
    const WEEKLY_RE = /weekly\s+limit\s+reached|weekly_quota/i;
    const DAILY_RE = /(?:out\s+of\s+extra\s+usage|extra\s+usage\b|usage\s+limit\s+reached|usage\s+cap\s+reached|claude\s+usage\s+limit\s+reached|daily\s+limit|daily\s+quota)/i;
    const TPM_RE = /tokens?[-\s]?per[-\s]?minute|tokens?\s*\/\s*min(?:ute)?|input\s+tokens\s+per\s+minute|\btpm\b/i;
    const RPM_RE = /requests?[-\s]?per[-\s]?minute|requests?\s*\/\s*min(?:ute)?|\brpm\b|rate_limit_error|too\s+many\s+requests|\b429\b/i;
    const ORG_TIER_RE = /organization|organisation\s+(?:limit|quota|tier)|\borg\s+tier\b|overloaded(?:_error)?|server\s+overloaded|service\s+unavailable|\b503\b|\b529\b|high\s+demand/i;

    function tryParseRetryAt(input: string): Date | null {
      const match = input.match(CLAUDE_EXTRA_USAGE_RESET_RE);
      if (!match) return null;
      const candidate = (match[1] || "").trim();
      if (!candidate) return null;
      const parsed = new Date(candidate);
      return Number.isNaN(parsed.getTime()) ? null : parsed;
    }

    /**
     * Classifica saída do Claude Code CLI (stdout + stderr concat) em sub-tipo de exhaustão.
     * Ordem dos discriminators é OBRIGATÓRIA — most-specific-first evita ambiguidade.
     * Ver D-15 em .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md.
     */
    export function detectClaudeQuotaExhausted(input: {
      stdout?: string;
      stderr?: string;
    }): ClaudeQuotaDetection {
      const blob = `${input.stdout ?? ""}\n${input.stderr ?? ""}`;
      const topLevelMatch = blob.match(CLAUDE_TRANSIENT_UPSTREAM_RE);
      if (!topLevelMatch) {
        return { detected: false, type: null, retryAt: null, confidence: "low", rawMatch: null };
      }
      const rawMatch = topLevelMatch[0];
      const retryAt = tryParseRetryAt(blob);

      // Ordem most-specific-first (D-15) — NÃO ALTERAR sem atualizar tests:
      if (SESSION_5H_RE.test(blob)) {
        return { detected: true, type: "session_5h", retryAt, confidence: "high", rawMatch };
      }
      if (WEEKLY_RE.test(blob)) {
        return { detected: true, type: "weekly_quota", retryAt, confidence: "high", rawMatch };
      }
      if (DAILY_RE.test(blob) && retryAt !== null) {
        // Daily exige timestamp de reset para confidence high; sem reset, fallback p/ medium.
        return { detected: true, type: "daily_quota", retryAt, confidence: "high", rawMatch };
      }
      if (TPM_RE.test(blob)) {
        return { detected: true, type: "tpm_transient", retryAt, confidence: "medium", rawMatch };
      }
      if (RPM_RE.test(blob)) {
        return { detected: true, type: "rpm_transient", retryAt, confidence: "medium", rawMatch };
      }
      if (ORG_TIER_RE.test(blob)) {
        return { detected: true, type: "org_tier", retryAt, confidence: "low", rawMatch };
      }
      // Top-level matched mas nenhum sub — best-effort rpm_transient + log (Finding 4).
      return { detected: true, type: "rpm_transient", retryAt, confidence: "low", rawMatch };
    }
    ```

    **Não modificar:**
    - Linhas 1-15 (regex existentes — usadas como referência interna)
    - `parseClaudeStreamJson` (linha 17+) — função separada, não relacionada à detecção de quota

    **Critério crítico (D-15):** ordem dos `if` matters. session_5h antes de daily porque "claude usage limit reached" aparece em ambos; weekly antes de daily pelo mesmo motivo. Comentário inline explica a invariante.
  </action>
  <verify>
    <automated>cd packages/adapters/claude-local && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function detectClaudeQuotaExhausted" packages/adapters/claude-local/src/server/parse.ts` retorna 1
    - `grep -c "export type ClaudeQuotaType" packages/adapters/claude-local/src/server/parse.ts` retorna 1
    - `grep -c "export interface ClaudeQuotaDetection" packages/adapters/claude-local/src/server/parse.ts` retorna 1
    - `grep -B1 "WEEKLY_RE.test" packages/adapters/claude-local/src/server/parse.ts | grep -q "SESSION_5H_RE"` (ordem session_5h ANTES de weekly... wait, need to verify the ORDER) — na verdade verificar: `grep -A30 "if (SESSION_5H_RE" packages/adapters/claude-local/src/server/parse.ts | grep "WEEKLY_RE.test" | head -1` retorna linha (i.e., session_5h vem antes de weekly no código)
    - `grep -E "CLAUDE_TRANSIENT_UPSTREAM_RE\\.|topLevelMatch" packages/adapters/claude-local/src/server/parse.ts` casa pelo menos 1 (gate top-level reusado)
    - `! grep "const CLAUDE_TRANSIENT_UPSTREAM_RE.*=.*\\/\\(" packages/adapters/claude-local/src/server/parse.ts | head -2 | tail -1` (apenas UMA declaração; sem duplicata)
    - `cd packages/adapters/claude-local && pnpm tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Função `detectClaudeQuotaExhausted` exportada de parse.ts; TypeScript compila; regex existentes intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Test suite vitest cobrindo 6 sub-tipos + gate</name>
  <files>server/src/__tests__/claude-local-adapter-quota-detection.test.ts</files>
  <read_first>
    - server/src/__tests__/claude-local-adapter.test.ts (pattern de import e setup vitest)
    - server/src/__tests__/claude-local-execute.test.ts (pattern de mock se necessário; provavelmente nem precisa de mock — função é pura)
    - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/ (ls — listar arquivos disponíveis; usar 6 fixtures: rpm.txt, tpm.txt, daily.txt, weekly.txt, session_5h.txt, org_tier.txt OU equivalente)
    - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts (pattern de tests do spike — referência)
    - packages/adapters/claude-local/src/server/parse.ts (linhas adicionadas pela Tarefa 1 — função sob teste)
  </read_first>
  <action>
    Criar `server/src/__tests__/claude-local-adapter-quota-detection.test.ts`:

    ```typescript
    import { describe, it, expect } from "vitest";
    import fs from "node:fs";
    import path from "node:path";
    import { detectClaudeQuotaExhausted } from "@paperclipai/adapter-claude-local/server";
    // Se o package não exporta o símbolo via barrel, importar do path direto:
    // import { detectClaudeQuotaExhausted } from "../../../packages/adapters/claude-local/src/server/parse.js";

    const FIXTURES_DIR = path.resolve(
      __dirname,
      "../../../.planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures",
    );

    function readFixture(name: string): string {
      const fullPath = path.join(FIXTURES_DIR, name);
      return fs.readFileSync(fullPath, "utf8");
    }

    describe("detectClaudeQuotaExhausted", () => {
      it("returns detected=false for empty input", () => {
        const result = detectClaudeQuotaExhausted({ stdout: "", stderr: "" });
        expect(result.detected).toBe(false);
        expect(result.type).toBeNull();
      });

      it("returns detected=false for unrelated text", () => {
        const result = detectClaudeQuotaExhausted({ stdout: "Hello world\nNo errors here", stderr: "" });
        expect(result.detected).toBe(false);
      });

      it("classifies session_5h when '5-hour limit reached' is present", () => {
        const fixture = readFixture("session_5h.txt"); // ajustar nome se diferente
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.detected).toBe(true);
        expect(result.type).toBe("session_5h");
        expect(result.confidence).toBe("high");
      });

      it("classifies weekly_quota for 'weekly limit reached'", () => {
        const fixture = readFixture("weekly.txt");
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.type).toBe("weekly_quota");
      });

      it("classifies daily_quota for 'usage limit reached' + reset timestamp (without 5-hour token)", () => {
        const fixture = readFixture("daily.txt");
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.type).toBe("daily_quota");
        expect(result.retryAt).not.toBeNull();
      });

      it("classifies rpm_transient for rate_limit_error", () => {
        const fixture = readFixture("rpm.txt");
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.type).toBe("rpm_transient");
      });

      it("classifies tpm_transient for tokens-per-minute", () => {
        const fixture = readFixture("tpm.txt");
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.type).toBe("tpm_transient");
      });

      it("classifies org_tier for 503/overloaded fallback", () => {
        const fixture = readFixture("org_tier.txt");
        const result = detectClaudeQuotaExhausted({ stdout: fixture, stderr: "" });
        expect(result.type).toBe("org_tier");
        expect(result.confidence).toBe("low");
      });

      it("disambiguates 'claude usage limit reached' as daily (not session_5h) when '5-hour' is absent", () => {
        const input = "Error: claude usage limit reached. Resets at 2026-04-26T20:00:00Z.";
        const result = detectClaudeQuotaExhausted({ stdout: input, stderr: "" });
        expect(result.type).toBe("daily_quota");
      });
    });
    ```

    **Notas:**
    - Antes de escrever os tests, **listar fixtures disponíveis**: `ls .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/` — adaptar os filenames se diferentes (ex: `5h_limit.txt` em vez de `session_5h.txt`).
    - Se algum fixture não existir, **substituir leitura por string inline literal** com mensagem canônica da taxonomy (ex: `"You've reached the 5-hour limit. Resets at 2026-04-26T18:00:00Z."`).
    - Import path do classifier: usar export do package (`@paperclipai/adapter-claude-local/server`); se barrel não expõe, fallback para path relativo `../../../packages/adapters/claude-local/src/server/parse.js`.
    - **NÃO copiar fixtures para o test dir** — manter referência por path relativo (D-16). Quando UAT-04-01 capturar fixtures reais, eles substituem in-place sem afetar tests.
  </action>
  <verify>
    <automated>cd server && pnpm vitest run src/__tests__/claude-local-adapter-quota-detection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/src/__tests__/claude-local-adapter-quota-detection.test.ts` existe
    - `grep -c "describe.*detectClaudeQuotaExhausted" server/src/__tests__/claude-local-adapter-quota-detection.test.ts` retorna 1
    - `grep -c "it\\(" server/src/__tests__/claude-local-adapter-quota-detection.test.ts` retorna pelo menos 8 (1 gate empty + 1 gate unrelated + 6 sub-tipos + 1 ambiguidade)
    - Comando `pnpm vitest run src/__tests__/claude-local-adapter-quota-detection.test.ts` (rodado de `server/`) exit code 0 com pelo menos 8 tests passing
    - `grep -E "fixtures|inline literal" server/src/__tests__/claude-local-adapter-quota-detection.test.ts` casa (estratégia de fixture documentada)
  </acceptance_criteria>
  <done>8+ tests vitest passando; classifier validado para os 6 sub-tipos + gate negativo + caso de ambiguidade.</done>
</task>

</tasks>

<verification>
- TypeScript compila no package claude-local
- Test suite passa para os 6 sub-tipos + gates
- Regex existente (`CLAUDE_TRANSIENT_UPSTREAM_RE` linha 13) intacto, sem duplicação
- Função exportada do package consumível por server/services (será usada em 05-06)
</verification>

<success_criteria>
1. ✅ `detectClaudeQuotaExhausted` exportada de parse.ts
2. ✅ Reusa `CLAUDE_TRANSIENT_UPSTREAM_RE` (parse.ts:13) como gate top-level — sem duplicação
3. ✅ Discriminators na ordem D-15: session_5h → weekly → daily → tpm → rpm → org_tier
4. ✅ Test suite com pelo menos 8 cases (6 sub-tipos + 2 gates + 1 ambiguidade) passando
5. ✅ Tests reusam fixtures do spike Phase 4 por path relativo (não duplicados)
6. ✅ Caso de ambiguidade "claude usage limit reached" sem "5-hour" classifica como daily_quota (bug fix do protótipo registrado em D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-02-SUMMARY.md` with:
- Linhas adicionadas em parse.ts (range — provavelmente após linha 580+)
- Test count e cobertura por sub-tipo
- Estratégia de fixture reuse (path relativo vs inline literal)
- Confirmação de que `CLAUDE_TRANSIENT_UPSTREAM_RE` original não foi modificado (Finding 1)
- Decisões de confidence (`high` para weekly/session_5h/daily-with-reset, `medium` para rpm/tpm, `low` para org_tier fallback)
</output>
