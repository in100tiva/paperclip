---
phase: 05-multi-account-claude-code-swap-implementacao
plan: 08
type: execute
wave: 4
depends_on: [05-06, 05-07]
files_modified:
  - .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md
  - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md
autonomous: true
requirements: [MULTI-11]

must_haves:
  truths:
    - "SMOKE-E2E.md documenta procedimento testável de fim-a-fim para multi-account swap (D-33): registrar 2 contas, forçar exhaustão, observar swap, validar attribution"
    - "Procedure inclui SQL de validação para confirmar agent_step_executions tem rows com accountId=A e accountId=B após swap"
    - "Procedure inclui SQL de validação para activity_log com claude_account_rotated entry"
    - "05-HUMAN-UAT.md lista UAT-05-01 (smoke real cross-account com exhaustão natural — D-34)"
    - "Procedure mapeia step-a-step com SQL/curl literais que operador pode copiar-colar"
    - "Forçar exhaustão documentado de 2 maneiras: SQL direta (mais rápido para automated smoke) E uso natural (HUMAN-UAT empírico)"
  artifacts:
    - path: ".planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md"
      provides: "Procedimento canônico de smoke E2E (MULTI-11; D-33)"
      contains: "SMOKE-E2E"
    - path: ".planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md"
      provides: "UATs pendentes que destravam validação empírica final"
      contains: "UAT-05-01"
  key_links:
    - from: "SMOKE-E2E.md"
      to: "agent_step_executions table (criada em 05-01)"
      via: "SQL queries de validação"
      pattern: "FROM agent_step_executions"
    - from: "SMOKE-E2E.md"
      to: "activity_log table"
      via: "SQL queries filtrando por action='claude_account_rotated'"
      pattern: "claude_account_rotated"
    - from: "05-HUMAN-UAT.md"
      to: "FINDINGS-FOR-PHASE-5.md (UAT-04-03)"
      via: "referência cruzada — UAT-05-01 destrava refinamento"
      pattern: "UAT-04-03"
---

<objective>
Criar dois documentos consolidando a verificação empírica da Phase 5: (1) `SMOKE-E2E.md` com procedimento canônico passo-a-passo para validar o fluxo completo multi-account (registrar contas, forçar exhaustão, observar swap, validar attribution); (2) `05-HUMAN-UAT.md` listando UAT-05-01 (smoke real com 2 contas Claude reais e exhaustão natural — não controlável pelo executor Claude, requer humano operacional).

Purpose: MULTI-11 exige smoke documentado. D-33 lista os 6 passos do procedimento canônico; D-34 explicita que validação real requer 2 contas reais e cabe a HUMAN-UAT (precedente de Phase 3 — `complete-with-pending-UAT`). Phase 5 fecha com artefatos que destravam adoção real.

Output: 2 documentos markdown em pt-br (consistente com convenção de artefatos `.planning/`), passos numerados, SQL/curl literais, tabelas pass/fail.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-06-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-01-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-04-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-06-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-07-PLAN.md

<interfaces>
<!-- Pattern de HUMAN-UAT (de 04-HUMAN-UAT.md): -->
<!-- Frontmatter YAML: type: human-uat / status: pending -->
<!-- Cada UAT tem: ID, objetivo, pré-requisitos, passos numerados, critérios pass/fail empíricos -->

<!-- Pattern de smoke (de 02-06-PLAN.md / Phase 2 SMOKE-TEST-LOG): -->
<!-- - Pré-requisitos explícitos (env vars, comandos, contas) -->
<!-- - Setup steps numerados -->
<!-- - Validation steps com SQL queries literais -->
<!-- - Cleanup notes -->

<!-- Schemas finais (de 05-01): -->
<!-- - claude_accounts (id uuid, companyId uuid, ownerUserId text, label, configDirSlug, status, lastQuotaWindowsJson jsonb, exhaustedUntil timestamp, lastUsedAt) -->
<!-- - agent_account_bindings (agentId uuid PK, activeAccountId uuid, rotationPolicy, lastRotatedAt) -->
<!-- - agent_step_executions (id uuid, runId uuid, stepId text, accountId uuid, inputTokens, outputTokens, costUsd, startedAt, completedAt, errorFamily, createdAt) -->

<!-- Activity log entry (de 05-03): -->
<!-- action='claude_account_rotated', details JSON com fromAccountId/toAccountId/reason/errorFamily/swapStrategy -->

<!-- API endpoints (de 05-07): -->
<!-- GET /api/companies/:companyId/claude-accounts -->
<!-- POST /api/companies/:companyId/claude-accounts -->
<!-- GET /api/companies/:companyId/claude-accounts/rotation-history -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar SMOKE-E2E.md com procedimento canônico</name>
  <files>.planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md</files>
  <read_first>
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-33 — passos 1-6 do procedure)
    - .planning/phases/02-migra-o-de-storage-para-supabase/02-06-PLAN.md (pattern de smoke E2E pt-br)
    - .planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md (pattern de procedure documentation)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-01-PLAN.md (schemas finais — confirma colunas para SQL queries)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-07-PLAN.md (UI/API endpoints disponíveis para smoke)
  </read_first>
  <action>
    Criar `.planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` em pt-br seguindo pattern de Phase 3:

    ```markdown
    # SMOKE-E2E: Multi-Account Claude Code Swap

    **Phase:** 5 (Multi-Account Implementação)
    **Requirement:** MULTI-11
    **Decisão fonte:** D-33 (.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md)
    **Linguagem:** pt-br (artefato interno; convenção .planning/)
    **Status:** Procedimento documentado; validação empírica em 05-HUMAN-UAT.md (UAT-05-01)

    ## Objetivo

    Validar de fim-a-fim que:
    1. O pool de contas Claude é registrado e listado.
    2. Quando uma conta atinge exhaustão, o sistema dispara swap automático para outra conta.
    3. Cada step é registrado em `agent_step_executions` com o `accountId` correto (cost attribution funcional).
    4. Cada swap emite `claude_account_rotated` no `activity_log` com payload completo.
    5. UI `/company/settings/claude-accounts` reflete o estado atualizado em tempo real.

    Este smoke tem **dois modos**:
    - **Modo A (forçado, automatizado):** força `exhaustedUntil` via SQL para induzir swap sem usar quota Claude real. Útil para CI/regressão.
    - **Modo B (real, HUMAN-UAT):** roda agente até atingir limite Claude real. Requer 2 contas Claude (Pro/Team/etc.) com quota distinta. Coberto em `05-HUMAN-UAT.md` UAT-05-01.

    Este documento cobre **Modo A**. Modo B fica para HUMAN-UAT.

    ## Pré-requisitos

    | Item | Como obter/verificar |
    |------|----------------------|
    | Phase 5 plans 01-07 completos | `git log --oneline | head -20` deve mostrar commits dos plans 05-01 até 05-07 |
    | Migration aplicada em Supabase | `psql $SUPABASE_DB_URL -c "\dt claude_accounts"` retorna a tabela |
    | Server rodando em modo authenticated | `PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev` (porta 3100) |
    | Login Better Auth válido | Cookie `paperclip-team-shared` presente na sessão do browser |
    | 2 diretórios de credenciais Claude | `mkdir -p ~/.paperclip/claude-accounts/a ~/.paperclip/claude-accounts/b` (NÃO precisa rodar `claude login` para Modo A — apenas estrutura) |
    | Acesso à UI | `http://localhost:3100/company/settings/claude-accounts` carrega |
    | Acesso ao Supabase Studio | https://supabase.com/dashboard/project/bxlczioxgizgvtznukwt/sql para queries de validação |

    ## Procedimento (Modo A — forçado)

    ### Passo 1: Registrar 2 contas via UI

    1. Navegar para `http://localhost:3100/company/settings/claude-accounts`.
    2. No formulário "Register new account":
       - Label: `Account A`
       - Slug: `a`
       - Submit. Confirmar toast de sucesso.
    3. Repetir:
       - Label: `Account B`
       - Slug: `b`
       - Submit.
    4. Validar tabela mostra 2 rows com status `live` (badge verde).

    **Validação SQL (Supabase Studio):**

    ```sql
    SELECT id, label, config_dir_slug, status, last_used_at
    FROM claude_accounts
    WHERE company_id = '<seu-companyId>'
    ORDER BY created_at;
    ```

    Esperado: 2 rows, ambas `status = 'live'`.

    Capturar os UUIDs de A e B para passos seguintes (substituir `<account-a-uuid>` / `<account-b-uuid>`).

    ### Passo 2: Spawnar agente que ativa selectActiveAccount

    Iniciar um agente qualquer da company (via `/agents` ou via heartbeat manual). Pode ser um agente trivial que apenas executa um ping curto.

    Conferir nos logs do server: linha `[paperclip] Multi-account: ...` ausente (sem erros) E request a `claude` CLI com env var `CLAUDE_CONFIG_DIR=~/.paperclip/claude-accounts/a` (assumindo Account A foi selecionada como round-robin primeira por `lastUsedAt ASC`).

    **Validação SQL:**

    ```sql
    SELECT * FROM agent_account_bindings WHERE agent_id = '<seu-agentId>';
    ```

    Esperado: 1 row com `active_account_id` = `<account-a-uuid>`, `rotation_policy = 'auto'`.

    ### Passo 3: Forçar exhaustão de Account A via SQL

    Em vez de aguardar quota Claude real, força via update direto:

    ```sql
    UPDATE claude_accounts
    SET status = 'exhausted',
        exhausted_until = now() + interval '1 hour',
        last_quota_windows_json = jsonb_set(
          coalesce(last_quota_windows_json, '{}'::jsonb),
          '{daily_quota}',
          jsonb_build_object(
            'exhaustedUntil', (now() + interval '1 hour')::text,
            'lastTriggeredAt', now()::text,
            'count', 1
          )
        ),
        updated_at = now()
    WHERE id = '<account-a-uuid>';
    ```

    Validar via UI: refresh da página `/claude-accounts` mostra Account A com badge `exhausted` (vermelho) e tooltip de `exhausted until`.

    ### Passo 4: Spawnar próximo step do agente — observar swap

    Trigger uma nova ação no agente (ex: enviar issue, comentário, etc. — qualquer coisa que cause heartbeat invocar adapter.execute).

    No log do server, observar linha indicando rotação. Esperado: agora `CLAUDE_CONFIG_DIR=~/.paperclip/claude-accounts/b`.

    **Validação SQL:**

    ```sql
    -- Binding deve agora apontar para B
    SELECT active_account_id, last_rotated_at
    FROM agent_account_bindings
    WHERE agent_id = '<seu-agentId>';
    ```

    Esperado: `active_account_id = <account-b-uuid>`, `last_rotated_at` recente.

    ```sql
    -- agent_step_executions deve ter rows para A (errored) E B (success)
    SELECT step_id, account_id, error_family, started_at
    FROM agent_step_executions
    WHERE run_id IN (
      SELECT id FROM heartbeat_runs WHERE agent_id = '<seu-agentId>'
      ORDER BY created_at DESC LIMIT 1
    )
    ORDER BY started_at;
    ```

    Esperado: pelo menos 2 rows — uma com `account_id = <account-a-uuid>` (com `error_family` ou null), outra com `account_id = <account-b-uuid>`.

    ### Passo 5: Validar activity_log entry

    ```sql
    SELECT id, action, details, created_at
    FROM activity_log
    WHERE company_id = '<seu-companyId>'
      AND action = 'claude_account_rotated'
    ORDER BY created_at DESC
    LIMIT 1;
    ```

    Esperado: 1 row com `details` JSON contendo:
    - `agentId` = `<seu-agentId>`
    - `fromAccountId` = `<account-a-uuid>`
    - `toAccountId` = `<account-b-uuid>`
    - `reason` = `"exhausted"`
    - `errorFamily` populado (provável `"transient_upstream"` ou similar)
    - `swapStrategy` = `"resume"` ou `"fallback_full_context"` (depende de UAT-04-03)

    ### Passo 6: Validar UI rotation history

    Refresh `/claude-accounts`. Seção "Rotation history" deve mostrar a entry recente: `Agent <id> rotated <a-id-curto> → <b-id-curto> (exhausted, transient_upstream, resume/fallback_full_context)`.

    ## Pass/Fail

    | Critério | Pass se | Fail se |
    |----------|---------|---------|
    | Lista de accounts | UI mostra 2 rows após Passo 1 | UI vazia ou erro |
    | Selection inicial | Agent binding aponta para A após Passo 2 | binding null ou aponta para B (round-robin quebrado) |
    | Exhaustão induzida | UI mostra A como `exhausted` após Passo 3 | UI continua mostrando A como `live` (refresh cache?) |
    | Swap automático | binding muda para B após Passo 4 | binding permanece em A (heartbeat não invocou orchestrate) |
    | Attribution | `agent_step_executions` tem rows com ambos accountIds | Só tem rows com A ou só com B |
    | Activity log | 1 entry `claude_account_rotated` com payload completo | Sem entry, ou payload incompleto |
    | UI history | Entry visível em "Rotation history" | Vazio ou stale |

    ## Cleanup

    Após smoke:

    ```sql
    -- Reset Account A para live (se quiser repetir o smoke):
    UPDATE claude_accounts
    SET status = 'live',
        exhausted_until = NULL,
        last_quota_windows_json = '{}'::jsonb,
        updated_at = now()
    WHERE id = '<account-a-uuid>';

    -- Opcional: limpar accounts de teste:
    DELETE FROM agent_account_bindings WHERE agent_id = '<seu-agentId>';
    DELETE FROM claude_accounts WHERE config_dir_slug IN ('a', 'b');
    ```

    Não rodar contra company de produção sem coordenação.

    ## Limitações

    Este smoke (Modo A) **não valida**:
    - Que o `claude` CLI realmente respeita `CLAUDE_CONFIG_DIR` em ambiente real (assumido por testes do adapter; coberto por UAT-05-01)
    - Que `--resume` cross-account funciona ou falha com pattern previsto (Strategy A vs B — UAT-04-03 dependente)
    - Que continuation summary preserva contexto suficiente (UAT-04-03 dependente)
    - Que cost attribution em USD é exato (cost calc não conectado a Anthropic real billing — v1 limitação)

    Estas validações ficam em `05-HUMAN-UAT.md` (UAT-05-01).

    ## Referências

    - `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md` (D-33)
    - `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` (UAT-05-01)
    - `.planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md` (UAT-04-03 dependente)
    - `server/src/services/claude-accounts-swap.md` (orquestração)
    - `packages/db/src/schema/{claude_accounts,agent_account_bindings,agent_step_executions}.ts` (schemas)
    ```

    **Convenções:**
    - pt-br consistente com convenção .planning/
    - SQL literais copiável-colável (com placeholders entre `<...>` para o operador substituir)
    - Tabela pass/fail explícita
    - Cleanup obrigatório (não deixar dados de teste em company shared)
  </action>
  <verify>
    <automated>test -f .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md && wc -l .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` existe
    - `wc -l < .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 100
    - `grep -c "## Passo" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 6 (6 passos do D-33)
    - `grep -c "FROM claude_accounts\\|FROM agent_step_executions\\|FROM activity_log\\|FROM agent_account_bindings" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 4 (queries de validação para todas tabelas-chave)
    - `grep -c "claude_account_rotated" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 1
    - `grep -c "Pass\\|Fail" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 2 (tabela explícita)
    - `grep -c "Cleanup\\|Limpar\\|Reset" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 1
    - `grep -c "MULTI-11" .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` >= 1
  </acceptance_criteria>
  <done>SMOKE-E2E.md ≥ 100 linhas pt-br com 6 passos numerados, SQL literais para 4 tabelas, pass/fail criteria, cleanup, limitations.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar 05-HUMAN-UAT.md com UAT-05-01</name>
  <files>.planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md (pattern de UAT — frontmatter, estrutura)
    - .planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md (pattern de pass/fail empírico)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md (de Tarefa 1 — Modo A; UAT é Modo B)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-34)
  </read_first>
  <action>
    Criar `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` em pt-br:

    ```markdown
    ---
    type: human-uat
    status: pending
    created: 2026-04-26
    requirements: [MULTI-11]
    references:
      - .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md
      - .planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md
      - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
    ---

    # 05 HUMAN-UAT — Multi-Account Implementação

    **Phase:** 5 (Multi-Account Claude Code Swap — Implementação)
    **Modo:** validação empírica que requer 2 contas Claude reais com quota distinta
    **Decisão fonte:** D-34 (`.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md`)
    **Precedente:** Phase 3 / Phase 4 (`complete-with-pending-UAT` — artefatos entregues, validação real pendente)

    Estes UATs **não podem ser executados pelo executor Claude** — exigem 2 contas Claude (Pro/Team/Max) reais separadas e tempo de uso real até atingir quota natural. São responsabilidade do operador humano.

    ## Status

    | UAT | Status | Última atualização |
    |-----|--------|--------------------|
    | UAT-05-01 | pending | 2026-04-26 |

    ---

    ## UAT-05-01: Smoke real cross-account com exhaustão natural

    **Cobre:** MULTI-11 (success criterion #1 do ROADMAP — agente roda → conta esgota → swap automático → continuidade preservada → cost atribuído por conta)
    **Status:** pending

    ### Objetivo

    Validar empiricamente, em condições reais, que:
    1. Agente Claude roda contra Account A; Account A atinge limite real (5h, daily, weekly, ou tier).
    2. Sistema detecta `transient_upstream` via classifier real (não regex stub).
    3. Swap automático para Account B; agente continua de onde parou via Strategy A (resume) ou Strategy B (fallback full-context).
    4. `agent_step_executions` registra cost attribution correto: tokens de A vs tokens de B distintos.
    5. UI `/claude-accounts` mostra Account A em `exhausted` com `exhaustedUntil` real (não forçado por SQL).
    6. Activity log entry tem `swapStrategy` reflectindo qual estratégia funcionou.

    ### Pré-requisitos

    | Item | Como preparar |
    |------|---------------|
    | 2 contas Claude separadas | Conta A: plano com quota baixa (Free, ou Pro com já-consumido). Conta B: plano fresh com quota cheia. Pode ser 2 logins distintos do mesmo plano se for plano que distingue por user. |
    | 2 diretórios isolados | `mkdir -p ~/.paperclip/claude-accounts/{a,b}` |
    | Login em cada dir | `CLAUDE_CONFIG_DIR=~/.paperclip/claude-accounts/a claude login` (e repetir para b com a outra conta) |
    | Server rodando authenticated | `PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev` |
    | Phase 5 implementação completa | git log mostra commits dos planos 05-01 até 05-07; migration aplicada em Supabase |
    | Agente longo-running | Agente de teste configurado para rodar tarefa longa (ex: 50+ steps de geração) — necessário para esgotar quota A naturalmente |
    | Tempo disponível | Estimar ~30min-2h dependendo do limite atingido (5h é mais rápido se já há contexto consumido; daily exige uso pesado) |

    ### Procedimento

    1. **Setup inicial:**
       - Registrar Account A e Account B via UI `/claude-accounts` (label + slug, sem digitar credenciais — credenciais já estão nos dirs).
       - Verificar UI mostra ambas como `live`.

    2. **Spawnar agente:** rodar agente longo na company de teste. Configurar para usar quota agressivamente (várias chamadas Claude consecutivas com prompts longos, ou loop de geração).

    3. **Aguardar exhaustão natural de Account A:**
       - Monitorar `claude_accounts.exhausted_until` via Supabase Studio ou refresh da UI.
       - Quando A atinge limite, sistema deve detectar via classifier e disparar swap.
       - **Sinal visível:** UI muda Account A de verde (`live`) para vermelho (`exhausted`); agent_account_bindings muda activeAccountId.

    4. **Observar continuidade:**
       - Agent não para — continua rodando contra Account B.
       - Verificar nos logs do server linhas com `MULTI-08` ou `orchestrateClaudeSwap`.
       - Server log deve indicar Strategy A tentada e (provável) failure detected → Strategy B (fallback full-context).

    5. **Validar attribution:**
       ```sql
       SELECT account_id, count(*) as steps, sum(input_tokens) as total_input, sum(output_tokens) as total_output
       FROM agent_step_executions
       WHERE run_id IN (SELECT id FROM heartbeat_runs WHERE agent_id = '<seu-agentId>')
       GROUP BY account_id;
       ```
       Esperado: 2 rows — uma para A com tokens "consumidos antes do esgotamento", outra para B com tokens "consumidos após swap".

    6. **Validar activity log:**
       ```sql
       SELECT details FROM activity_log
       WHERE action = 'claude_account_rotated'
         AND company_id = '<seu-companyId>'
       ORDER BY created_at DESC LIMIT 1;
       ```
       Conferir `errorFamily` (esperado: `'transient_upstream'`), `retryNotBefore` (esperado: timestamp real do reset Anthropic), `swapStrategy` (esperado: `'resume'` se cross-account session funcionou, `'fallback_full_context'` se houve fallback).

    ### Critérios Pass/Fail

    | Critério | Pass | Fail |
    |----------|------|------|
    | Detection real | Classifier identifica sub-tipo de exhaustão (rpm/tpm/daily/weekly/5h/org) na mensagem real do Claude CLI | Classifier classifica como `unknown` ou não detecta |
    | Swap automático | Agent continua sem intervenção; binding muda para B | Agent para com erro persistente |
    | Continuity | Output de B faz sentido em relação ao último output de A (não recomeça do zero) | Output de B começa "do zero", ignorando contexto |
    | Attribution | agent_step_executions tem ambos accountIds com tokens diferentes | Apenas A ou apenas B |
    | exhaustedUntil | Reflete timestamp real do reset Anthropic (extraído de retry-after / "resets at") | NULL ou data muito longe (fallback default) |
    | swapStrategy | Reflete a estratégia que funcionou (resume OU fallback_full_context) | NULL |

    ### Resultados Esperados (Findings dependentes)

    Se UAT-05-01 PASS:
    - Phase 5 satisfaz todos os success criteria do ROADMAP.
    - Strategy A pode ser refinada baseado em qual % das vezes funciona vs precisa de Strategy B.
    - Cost attribution V1 validado para Phase 6 PROJ-03.

    Se UAT-05-01 FAIL em "Continuity":
    - Strategy B tem gap — continuation_summary insuficiente para recriar contexto.
    - Refinement: incluir mais do estado interno do agente no prompt (issue body completo, last messages do thread, etc.).
    - Ticket criado em phase 6 ou v2.

    Se UAT-05-01 FAIL em "Detection":
    - Mensagem real do Claude CLI tem token novo não coberto pelo regex.
    - Atualizar `CLAUDE_TRANSIENT_UPSTREAM_RE` em parse.ts e taxonomy.
    - Possível extensão de discriminator em classifier.

    ### Como reportar resultados

    Após executar UAT-05-01, atualizar este arquivo:
    - Mudar `status: pending` → `status: passed | failed | partial`
    - Adicionar seção `## Resultado UAT-05-01 (executado em <data>)` com:
      - Operador (nome/email)
      - Conta A label + plano
      - Conta B label + plano
      - Tempo decorrido até exhaustão
      - Output do classifier (sub-tipo detectado)
      - Strategy que funcionou (resume vs fallback)
      - Tokens de A vs Tokens de B
      - Surprises / findings
    - Commitar atualização em branch separada para review.

    Se PASS: Phase 5 declared `complete`.
    Se FAIL: criar plan 05-09 ou abrir issue para fix targeted.

    ### Referências

    - `.planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md` (Modo A — forçado; este UAT é Modo B real)
    - `.planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md` (UAT-04-03 — dependência conceitual; pode ser executado primeiro ou em paralelo)
    - `server/src/services/claude-accounts-swap.md` (Strategy A/B docs)
    - `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md` (D-34)

    ---

    *Phase 5 fecha como `complete-with-pending-UAT`: artefatos entregues e validados via Modo A; validação Modo B (UAT-05-01) fica como trabalho contínuo do operador.*
    ```

    **Convenções:**
    - Frontmatter YAML com `type: human-uat` e `status: pending` (consistente com 04-HUMAN-UAT.md, 03-HUMAN-UAT.md)
    - pt-br
    - Critérios pass/fail explícitos
    - Forward link para SMOKE-E2E.md (Modo A) e 04-HUMAN-UAT.md (UAT-04-03 conceitual)
    - Instruções de como reportar (atualizar status, adicionar seção de resultado)
  </action>
  <verify>
    <automated>test -f .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md && head -10 .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` existe
    - `head -3 .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md | grep -q "^---"` (frontmatter YAML presente)
    - `grep -c "type: human-uat" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` retorna 1
    - `grep -c "status: pending" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` retorna 1
    - `grep -c "## UAT-05-01" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` retorna 1
    - `grep -c "Pass\\|Fail" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` >= 2 (tabela pass/fail)
    - `grep -c "MULTI-11" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` >= 1
    - `grep -c "Strategy A\\|Strategy B\\|fallback_full_context\\|resume" .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` >= 2
    - `wc -l < .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md` >= 80
  </acceptance_criteria>
  <done>05-HUMAN-UAT.md em pt-br com frontmatter pendente, UAT-05-01 detalhado com pré-requisitos, procedimento, pass/fail, e instruções de report.</done>
</task>

</tasks>

<verification>
- 2 documentos criados em `.planning/phases/05-multi-account-claude-code-swap-implementacao/`
- SMOKE-E2E.md cobre Modo A (forçado, automatizable)
- 05-HUMAN-UAT.md cobre Modo B (real, requer 2 contas Claude — HUMAN)
- Pass/Fail criteria explícitos em ambos
- Cleanup documentado em SMOKE-E2E.md
- Forward references corretas entre os 2 docs e para FINDINGS-FOR-PHASE-5.md
</verification>

<success_criteria>
1. ✅ SMOKE-E2E.md com 6 passos numerados (D-33), SQL literais para validation, pass/fail table, cleanup section
2. ✅ 05-HUMAN-UAT.md com frontmatter `type: human-uat status: pending`, UAT-05-01 detalhado
3. ✅ pt-br consistente com convenção .planning/
4. ✅ MULTI-11 endereçado como artefato (smoke documentado) + HUMAN-UAT routing (validação empírica empírica fica para humano com 2 contas reais)
5. ✅ Phase 5 fecha como `complete-with-pending-UAT` (precedente Phase 3/4)
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-08-SUMMARY.md` with:
- 2 docs criados (line counts)
- Decisão de routing: Modo A SMOKE-E2E (Claude executor pode validar) vs Modo B UAT-05-01 (HUMAN-UAT — requer 2 contas reais)
- Pass/fail criteria sumarizados
- Forward dependencies pós-Phase 5: UAT-05-01 + UAT-04-03 destrava refinamento empírico de swapStrategy
- Phase 5 status: complete-with-pending-UAT (artefatos entregues; convergência via uso real)
</output>
