---
phase: 03-workflow-de-equipe-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ONBOARDING.md
  - README.md
autonomous: true
requirements: [TEAM-02]

must_haves:
  truths:
    - "Dev novo lê `ONBOARDING.md` na raiz e tem caminho linear de clone até primeira sessão logada em <30 min"
    - "`README.md` tem nota visível no topo apontando para `ONBOARDING.md`"
    - "ONBOARDING.md contém os 7 passos canônicos da decisão D-06 (pré-req, clone, env, setup, dev, login, troubleshooting)"
    - "ONBOARDING.md está em pt-br (decisão specifics) e linka explicitamente TROUBLESHOOTING.md"
  artifacts:
    - path: "ONBOARDING.md"
      provides: "Guia de onboarding pt-br para devs novos do fork DDD"
      contains: "Pré-requisitos|Clonar|.env.local|pnpm setup|pnpm dev|invite|TROUBLESHOOTING"
      min_lines: 80
    - path: "README.md"
      provides: "README do paperclip preservado + nota de fork apontando para ONBOARDING.md"
      contains: "ONBOARDING.md"
  key_links:
    - from: "ONBOARDING.md"
      to: "TROUBLESHOOTING.md"
      via: "link markdown"
      pattern: "\\[.*\\]\\(.*TROUBLESHOOTING\\.md\\)"
    - from: "ONBOARDING.md"
      to: ".env.example"
      via: "instrução `cp .env.example .env.local`"
      pattern: "cp\\s+\\.env\\.example\\s+\\.env\\.local"
    - from: "ONBOARDING.md"
      to: "scripts/setup.ts"
      via: "instrução `pnpm setup`"
      pattern: "pnpm\\s+setup"
    - from: "README.md"
      to: "ONBOARDING.md"
      via: "link no topo"
      pattern: "ONBOARDING\\.md"
---

<objective>
Criar `ONBOARDING.md` na raiz do repositório com guia em pt-br para devs novos: pré-requisitos, clone, env vars, `pnpm setup`, `pnpm dev`, fluxo de invite/primeiro login, e link para `TROUBLESHOOTING.md`. Adicionar nota curta no topo do `README.md` (paperclip's) apontando para `ONBOARDING.md` (decisão D-08).

Purpose: Satisfazer TEAM-02. Critério de sucesso #1 do ROADMAP: "Um dev novo segue o README de setup local e tem o app rodando contra Supabase compartilhado em <30 minutos sem ajuda externa."

Output: Doc legível, escaneável, com comandos copy-paste prontos. Política de fork hard preservada — `README.md` raiz continua sendo o doc do paperclip público (decisão D-05); o fork tem seu próprio onboarding doc separado.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md
@.env.example
@README.md
@CONTRIBUTING.md
@UPSTREAM_REFERENCE.md

<interfaces>
<!-- Comandos e referências canônicas que o doc precisa citar literalmente -->

Comandos canônicos:
- `git clone <repo-url>`
- `cd ddd`
- `pnpm install`
- `cp .env.example .env.local`
- `pnpm setup` (criado em 03-01-PLAN)
- `pnpm dev`

Vars críticas em .env.local (do .env.example):
- `DATABASE_URL` (pooler 6543)
- `SUPABASE_DB_URL` (pooler 5432)
- `BETTER_AUTH_SECRET` (gerado via `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`)
- `PAPERCLIP_INSTANCE_ID=team-shared` (DEVE ser literal)

Fluxo de invite (de doc/spec/invite-flow.md):
- Bootstrap: dev #1 (CEO) executa `tsx packages/db/scripts/create-auth-bootstrap-invite.ts --config <path> --base-url <url>`
- Demais: aceitam URL `/invite/<token>` enviada pelo CEO no canal interno
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar ONBOARDING.md com 7 seções (decisão D-06)</name>
  <files>ONBOARDING.md</files>
  <read_first>
    - `D:/projetos/ddd/.env.example` (vars críticas exatas, valores placeholder, comentários inline para citar — esp. `PAPERCLIP_INSTANCE_ID=team-shared`)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` §"Onboarding Doc (TEAM-02)" D-05..D-08 (estrutura travada — 7 seções, pt-br, tempo-alvo <30 min)
    - `D:/projetos/ddd/CONTRIBUTING.md` (política de fork hard — referenciar mas não duplicar)
    - `D:/projetos/ddd/UPSTREAM_REFERENCE.md` (link para política e SHA upstream)
    - `D:/projetos/ddd/doc/spec/invite-flow.md` lines 1-80 (entender que bootstrap_ceo é one-time, demais devs entram via company_join)
    - `D:/projetos/ddd/packages/db/scripts/create-auth-bootstrap-invite.ts` (assinatura: `--config <path> --base-url <url>`; só para citar comando exato)
  </read_first>
  <action>
Criar arquivo NOVO em `D:/projetos/ddd/ONBOARDING.md`. Idioma: **pt-br** (decisão specifics). Estrutura segue D-06 (7 seções obrigatórias):

```markdown
# Onboarding — DDD (fork da equipe)

> **Idioma:** pt-br. Tempo-alvo: <30 minutos do clone à primeira sessão logada.
>
> Este doc é específico do fork DDD (hard fork de `paperclipai/paperclip` — ver [UPSTREAM_REFERENCE.md](UPSTREAM_REFERENCE.md)). O `README.md` da raiz continua sendo o README público do paperclip; este arquivo é a entrada operacional para a equipe interna.
>
> **Travou em algum passo?** Pule direto para [TROUBLESHOOTING.md](TROUBLESHOOTING.md).

## 1. Pré-requisitos

- **Node.js 20+** ([nodejs.org](https://nodejs.org))
- **pnpm 9.15.4+** — recomendado via corepack:
  ```bash
  corepack enable
  corepack prepare pnpm@9.15.4 --activate
  ```
- **Git** ([git-scm.com](https://git-scm.com))
- **Sistema operacional:** Windows / macOS / Linux (todos suportados; Windows tem warts conhecidos — ver TROUBLESHOOTING).
- **Opcional, recomendado para a Fase 4+:** [Claude Code CLI](https://docs.claude.com/en/docs/claude-code/setup). Sem ele, `pnpm setup` emite warning mas continua.

## 2. Clonar o repositório e instalar dependências

```bash
git clone <url-do-repo-ddd>
cd ddd
pnpm install
```

> Se `pnpm install` reclamar de file locks no Windows ou paths longos, ver [TROUBLESHOOTING.md § Windows NTFS](TROUBLESHOOTING.md#windows-ntfs).

## 3. Configurar `.env.local`

```bash
cp .env.example .env.local
```

Abra `.env.local` e preencha cada `TODO_FILL_ME`. **Os valores reais não vão neste doc nem no `.env.example`** — eles ficam no Vault/1Password compartilhado da equipe. Peça acesso no canal interno (Discord/Slack do time) se ainda não tiver.

Variáveis críticas que **DEVEM** estar preenchidas para o servidor subir:

| Variável | Valor | Origem |
|----------|-------|--------|
| `DATABASE_URL` | pooler 6543 da Supabase (transaction mode) | Vault da equipe |
| `SUPABASE_DB_URL` | pooler 5432 da Supabase (session mode, DDL) | Vault da equipe |
| `BETTER_AUTH_SECRET` | 32 bytes random base64 | Gere localmente (ver abaixo) |
| `PAPERCLIP_INSTANCE_ID` | **exatamente** `team-shared` | Literal — não invente |

Para gerar `BETTER_AUTH_SECRET` próprio (recomendado):

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

> ⚠️ `PAPERCLIP_INSTANCE_ID` precisa ser literal `team-shared` — qualquer outro valor faz seu cookie de sessão divergir do resto do time e você não verá a mesma company. Ver [TROUBLESHOOTING.md § Cookie prefix divergente](TROUBLESHOOTING.md#cookie-prefix-divergente).

> 🔒 `.env.local` está no `.gitignore`. **Nunca** commit valores reais. O pre-commit hook bloqueia JWTs vazados em arquivos client-side, mas não te salva de commitar `.env.local` por engano.

## 4. Validar o ambiente com `pnpm setup`

```bash
pnpm setup
```

Esse script roda em segundos e checa:

1. Versão do Node (>=20)
2. Versão do pnpm (>=9.15.4)
3. Existência do `.env.local`
4. Vars críticas preenchidas e `PAPERCLIP_INSTANCE_ID=team-shared`
5. CLI `claude` no PATH (warning se ausente)
6. Conexão Supabase (`SELECT 1` no pooler 6543)
7. Schema Better Auth aplicado (tabela `user` existe)

Se algo falhar, `pnpm setup` imprime exatamente o que corrigir e sai com código não-zero. Para falhas comuns, ver [TROUBLESHOOTING.md](TROUBLESHOOTING.md).

## 5. Subir o servidor: `pnpm dev`

```bash
pnpm dev
```

Isso sobe o servidor Express + UI (Vite middleware mounted in-process) na porta `3100`. Acesse [http://localhost:3100](http://localhost:3100).

> ⚠️ `pnpm dev` arranca em modo `local_trusted` por padrão — o handler do Better Auth não é montado nesse modo, então o invite flow não funciona com `pnpm dev` puro. Para exercitar o fluxo completo de equipe (signup, invite, login compartilhado), use:
>
> ```bash
> PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
> ```
>
> (Decisão registrada na Fase 2 — ver `.planning/STATE.md` 02-06.)

## 6. Aceitar invite e fazer primeiro login

A equipe inteira compartilha **uma única company** no Supabase remoto.

**Se você é o dev #1 (CEO bootstrap):** rode o script de bootstrap para criar o invite inicial. Ver `packages/db/scripts/create-auth-bootstrap-invite.ts` para argumentos exatos. **Esse passo só acontece uma vez no time inteiro.**

**Se você é o dev #2..N (caso comum):**

1. Peça um invite no canal interno do time. Você vai receber uma URL `/invite/<token>`.
2. Suba o servidor em modo `authenticated` (comando da seção 5).
3. Abra a URL do invite no browser, faça signup com email/senha (Better Auth — sem OAuth no v1).
4. Aceite o invite. Aguarde aprovação se for `pending_approval` (admin do time aprova no Company Settings).
5. Após aprovação, você é redirecionado pra company compartilhada e vê o dashboard.

> Detalhes do fluxo de invite: [doc/spec/invite-flow.md](doc/spec/invite-flow.md). Não precisa entender tudo para onboardar — basta seguir o link.

## 7. Travou em algo?

Veja **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** — cobre falhas comuns:

- Windows NTFS (file locks, paths longos, CRLF)
- Stale registry em `~/.paperclip/instances/default/runtime-services/`
- Supabase no limite de conexões (`too many connections`)
- Cookie prefix divergente
- Schema desatualizado (`pnpm db:migrate` vs CI)
- `claude` CLI ausente
- `prepared statement does not exist` em pooler 6543

Se nada do troubleshooting resolver, peça ajuda no canal interno do time.

---

**Política de fork:** Esse repositório é hard fork de `paperclipai/paperclip`. Ver [CONTRIBUTING.md](CONTRIBUTING.md) e [UPSTREAM_REFERENCE.md](UPSTREAM_REFERENCE.md) — sem PRs upstream, sem merges automáticos, drift é intencional.
```

Notas de implementação:
- Markdown puro, sem MDX (decisão D-discrição da Fase 3)
- Linguagem direta, sem floreios
- Cada comando em bloco shell separado (copy-paste friendly)
- Toda menção a problema linka pra TROUBLESHOOTING.md (anchors em snake-case que vão precisar bater com os headings da Tarefa do Plan 03-03)
- Tempo-alvo (<30 min) declarado no topo (D-07)
- Política de fork referenciada no rodapé sem duplicar conteúdo do CONTRIBUTING.md
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('ONBOARDING.md','utf8'); const checks=[/Pré-requisitos/,/cp \.env\.example \.env\.local/,/pnpm setup/,/pnpm dev/,/PAPERCLIP_INSTANCE_ID/,/team-shared/,/TROUBLESHOOTING\.md/,/invite/i]; for(const r of checks){if(!r.test(c)){console.error('MISSING:',r);process.exit(1)}}; if(c.split('\\n').length<80){console.error('TOO SHORT');process.exit(2)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/ONBOARDING.md` existe
    - Arquivo tem >= 80 linhas (`wc -l ONBOARDING.md` >= 80)
    - `grep -c "^## " D:/projetos/ddd/ONBOARDING.md` retorna >= 7 (sete seções H2 numeradas)
    - `grep -q "Pré-requisitos" D:/projetos/ddd/ONBOARDING.md`
    - `grep -q "cp \.env\.example \.env\.local" D:/projetos/ddd/ONBOARDING.md`
    - `grep -q "pnpm setup" D:/projetos/ddd/ONBOARDING.md`
    - `grep -q "pnpm dev" D:/projetos/ddd/ONBOARDING.md`
    - `grep -q "team-shared" D:/projetos/ddd/ONBOARDING.md`
    - `grep -q "TROUBLESHOOTING.md" D:/projetos/ddd/ONBOARDING.md` (link presente)
    - `grep -qi "invite" D:/projetos/ddd/ONBOARDING.md`
    - Doc é em pt-br: `grep -q "Pré-requisitos\|Clonar\|Configurar\|Validar" D:/projetos/ddd/ONBOARDING.md` (heads pt-br)
  </acceptance_criteria>
  <done>ONBOARDING.md criado na raiz com 7 seções pt-br, comandos copy-paste, e link explícito para TROUBLESHOOTING.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Adicionar nota no topo do README.md apontando para ONBOARDING.md</name>
  <files>README.md</files>
  <read_first>
    - `D:/projetos/ddd/README.md` (linha 1 atual: `> **DDD** — hard fork of [paperclipai/paperclip]...`. A nota nova vai antes ou logo após esta linha — preservar conteúdo paperclip integralmente)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` D-08 (decisão: nota curta no topo apontando para ONBOARDING.md)
  </read_first>
  <action>
Editar `D:/projetos/ddd/README.md`. Atualmente a linha 1 é:

```markdown
> **DDD** — hard fork of [paperclipai/paperclip](https://github.com/paperclipai/paperclip). See `UPSTREAM_REFERENCE.md` for the imported SHA and `CONTRIBUTING.md` for the fork policy. Paperclip's original README content follows.
```

Substituir essa linha 1 pelo bloco abaixo (mantendo a linha existente e adicionando a nota de onboarding ANTES dela):

```markdown
> **🧭 Dev da equipe DDD?** Comece pelo **[ONBOARDING.md](ONBOARDING.md)** (pt-br) e use **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** quando travar. Tempo-alvo: <30 min do clone à primeira sessão logada.
>
> **DDD** — hard fork of [paperclipai/paperclip](https://github.com/paperclipai/paperclip). See `UPSTREAM_REFERENCE.md` for the imported SHA and `CONTRIBUTING.md` for the fork policy. Paperclip's original README content follows.
```

NÃO modificar mais nenhuma linha do README.md. Todo o conteúdo abaixo (do paperclip público — quickstart, features, problems, what's-under-the-hood, FAQ, development, roadmap, etc.) deve permanecer literal byte-por-byte.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('README.md','utf8'); if(!c.includes('ONBOARDING.md')){console.error('MISSING ONBOARDING link');process.exit(1)}; if(!c.includes('hard fork of [paperclipai/paperclip]')){console.error('MISSING paperclip preservation');process.exit(2)}; if(!c.includes('What is Paperclip?')){console.error('MISSING paperclip body');process.exit(3)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `D:/projetos/ddd/README.md` contém literalmente `[ONBOARDING.md](ONBOARDING.md)` (link pro arquivo criado na Tarefa 1)
    - `D:/projetos/ddd/README.md` ainda contém literalmente `hard fork of [paperclipai/paperclip]` (linha original do fork preservada)
    - `D:/projetos/ddd/README.md` ainda contém `## What is Paperclip?` (corpo do README upstream preservado)
    - `D:/projetos/ddd/README.md` ainda contém `## Quickstart` (corpo do README upstream preservado)
    - Linha 1 do `README.md` é a nota DDD (não a linha original do paperclip): `head -n 1 README.md | grep -q "ONBOARDING.md"`
    - `wc -l README.md` retorna >= 430 (preservou o corpo paperclip que tinha ~438 linhas; ganho de 1-2 linhas pela nota)
  </acceptance_criteria>
  <done>README.md tem nota DDD no topo apontando para ONBOARDING.md; corpo paperclip preservado.</done>
</task>

</tasks>

<verification>
- `head -n 1 README.md` mostra nota DDD com link para ONBOARDING.md
- `wc -l ONBOARDING.md` >= 80
- Todos os 7 cabeçalhos H2 presentes em ONBOARDING.md
- Links cruzados consistentes: ONBOARDING.md → TROUBLESHOOTING.md, README.md → ONBOARDING.md
</verification>

<success_criteria>
1. Dev novo abre o repo, vê linha 1 do README, clica no link de ONBOARDING.md, e tem caminho linear sem precisar fazer perguntas externas.
2. ONBOARDING.md tem todos os 7 passos de D-06 com comandos copy-paste prontos.
3. TEAM-02 endereçado: critério #1 do ROADMAP cumprido (path para <30 min).
4. Política de fork preservada: README.md raiz continua sendo doc paperclip público, ONBOARDING.md é doc específico DDD.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-de-equipe-onboarding/03-02-SUMMARY.md` documenting: ONBOARDING.md final structure, any deviations from the 7-section template, exact diff applied to README.md.
</output>
