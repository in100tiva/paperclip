---
phase: 03-workflow-de-equipe-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup.ts
  - package.json
autonomous: true
requirements: [TEAM-03]

must_haves:
  truths:
    - "Dev novo executa `pnpm setup` e recebe diagnóstico claro do estado do ambiente"
    - "`pnpm setup` falha com exit code não-zero quando algo crítico está errado, com mensagem acionável"
    - "`pnpm setup` faz ping real no Supabase (pooler 6543, prepare:false) usando a factory existente"
    - "`pnpm setup` checa schema Better Auth (tabela `user`) e instrui `pnpm db:migrate` se faltar"
    - "`pnpm setup` checa presença do CLI `claude` no PATH (warning, não fatal)"
  artifacts:
    - path: "scripts/setup.ts"
      provides: "Setup script TypeScript invocado por `pnpm setup`"
      contains: "checkNodeVersion|checkPnpm|checkEnvLocal|checkCriticalEnvVars|checkSupabaseConnection|checkBetterAuthSchema|checkClaudeCli"
      min_lines: 200
    - path: "package.json"
      provides: "Script entry `setup`"
      contains: "\"setup\":"
  key_links:
    - from: "scripts/setup.ts"
      to: "packages/db/src/client.ts"
      via: "import { createDb } from \"@paperclipai/db\""
      pattern: "from\\s+\"@paperclipai/db\""
    - from: "package.json"
      to: "scripts/setup.ts"
      via: "tsx invocation"
      pattern: "tsx\\s+scripts/setup\\.ts"
---

<objective>
Construir `scripts/setup.ts` invocável via `pnpm setup` que valida o ambiente local de um dev novo: Node version, pnpm, `.env.local`, env vars críticas, ping no Supabase, schema Better Auth presente e `claude` CLI. Falha rápido com mensagens acionáveis e exit code não-zero quando algo crítico está errado.

Purpose: Satisfazer TEAM-03. Critério de sucesso #2 do ROADMAP: "`pnpm setup` valida env vars críticas, conexão com Supabase, presença do `claude` CLI e login Better Auth — falhando com mensagem acionável quando algo está errado."

Output: Script TypeScript autossuficiente que reusa a factory `createDb` existente (com `prepare:false` automático em pooler 6543) e instrui o dev a corrigir cada falha encontrada.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md
@.env.example
@package.json
@packages/db/src/client.ts

<interfaces>
<!-- Tipos e contratos chave que o executor precisa. Extraídos da base de código. -->

From packages/db/src/client.ts:
```typescript
export function createDb(url: string): Db;  // Drizzle handle; auto-aplica prepare:false em :6543
export function buildPostgresOptions(url: string): Parameters<typeof postgres>[1];
export type Db = ReturnType<typeof createDb>;
```

From .env.example (vars críticas a validar):
- `DATABASE_URL` — pooler 6543 (transaction); template `postgresql://postgres.<ref>:TODO_FILL_ME@aws-1-sa-east-1.pooler.supabase.com:6543/postgres`
- `SUPABASE_DB_URL` — pooler 5432 (session); template idem com `:5432`
- `BETTER_AUTH_SECRET` — placeholder `TODO_FILL_ME` indica não preenchido
- `PAPERCLIP_INSTANCE_ID` — DEVE ser literal `team-shared` (Decisão D-02 Fase 3 + Fase 2 AUTH-02)

From package.json (versões mínimas):
- `engines.node` = `>=20`
- `packageManager` = `pnpm@9.15.4`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar scripts/setup.ts com checks ordenados</name>
  <files>scripts/setup.ts</files>
  <read_first>
    - `D:/projetos/ddd/.env.example` (template canônico — quais env vars existem, quais valores são placeholder `TODO_FILL_ME`, qual é o literal esperado para `PAPERCLIP_INSTANCE_ID`)
    - `D:/projetos/ddd/packages/db/src/client.ts` (factory `createDb` + `buildPostgresOptions` — REUSAR; não reimplementar postgres-js wiring)
    - `D:/projetos/ddd/scripts/check-no-service-role-leak.mjs` (padrão de script de validação existente — estilo de output, exit codes, formato de mensagem)
    - `D:/projetos/ddd/package.json` (raiz; ver `engines.node`, `packageManager`, listar scripts existentes)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` §"Setup Script (`pnpm setup`)" D-01..D-04 (decisões travadas — ordem dos checks, exit code semantics)
  </read_first>
  <behavior>
    - Quando todas as condições passam: stdout lista cada check com `✓` em verde, exit code 0
    - Quando Node < 20: imprime `✗ Node 20+ requerido (atual: vX.Y.Z). Atualize: https://nodejs.org` e exit 1
    - Quando pnpm ausente ou < 9.15.4: imprime `✗ pnpm 9.15.4+ requerido. Instale: corepack enable && corepack prepare pnpm@9.15.4 --activate` e exit 1
    - Quando `.env.local` não existe: imprime `✗ .env.local ausente. Rode: cp .env.example .env.local` e exit 1
    - Quando uma var crítica está ausente OU igual a `TODO_FILL_ME`: imprime `✗ <VAR_NAME> não preenchida em .env.local. Veja .env.example linha N. Peça os valores no canal interno do time.` e exit 1
    - Quando `PAPERCLIP_INSTANCE_ID` ≠ `team-shared`: imprime `✗ PAPERCLIP_INSTANCE_ID deve ser exatamente "team-shared" (atual: "<value>"). Sessões não interoperam entre devs com prefixos diferentes. Veja TROUBLESHOOTING.md "Cookie prefix divergente".` e exit 1
    - Quando ping Supabase (`SELECT 1`) falha: imprime `✗ Conexão Supabase falhou: <erro original>. Veja TROUBLESHOOTING.md "Supabase no limite de conexões".` e exit 1
    - Quando schema Better Auth ausente (tabela `user` não existe): imprime `✗ Schema Better Auth não aplicado. CI normalmente aplica em merge para main; se você acabou de clonar, aguarde ou peça pull do main no time.` e exit 1
    - Quando `claude` CLI ausente: imprime `⚠ claude CLI não encontrado no PATH. Instale: https://docs.claude.com/en/docs/claude-code/setup (aviso, não fatal — você pode continuar sem multi-account na Fase 3)` e CONTINUA (não exit)
    - Quando todos os checks obrigatórios passam mas `claude` ausente: exit 0 com warning visível
  </behavior>
  <action>
Criar arquivo NOVO em `scripts/setup.ts` (TypeScript, ESM, executado via `tsx`). Decisão D-01: usar `tsx` cross-platform; NÃO bash/PowerShell.

Estrutura recomendada (modular por check, decisão D-discrição-livre):

```typescript
#!/usr/bin/env tsx
/**
 * pnpm setup — Validação de ambiente para devs novos da equipe DDD.
 *
 * Satisfaz TEAM-03 (Phase 3). Ver .planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md D-01..D-04.
 *
 * Ordem dos checks (fail-fast, decisão D-02):
 *   1. Node >= 20
 *   2. pnpm >= 9.15.4
 *   3. .env.local existe
 *   4. Vars críticas: DATABASE_URL, SUPABASE_DB_URL, BETTER_AUTH_SECRET, PAPERCLIP_INSTANCE_ID=team-shared
 *   5. claude CLI no PATH (warning, não fatal — D-02 ponto 5)
 *   6. Supabase ping (SELECT 1) via pooler 6543
 *   7. Schema Better Auth (SELECT count(*) FROM "user")
 *
 * Exit codes:
 *   0 — todos os checks obrigatórios passaram (warnings opcionais OK)
 *   1 — qualquer check obrigatório falhou
 */

import { execSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";
import { resolve } from "node:path";
import { createDb } from "@paperclipai/db";
import { sql } from "drizzle-orm";

// ANSI colors (sem dependência externa)
const GREEN = "\x1b[32m";
const RED = "\x1b[31m";
const YELLOW = "\x1b[33m";
const RESET = "\x1b[0m";

const REQUIRED_NODE_MAJOR = 20;
const REQUIRED_PNPM_VERSION = "9.15.4";
const REQUIRED_INSTANCE_ID = "team-shared";
const PLACEHOLDER = "TODO_FILL_ME";
const ENV_LOCAL_PATH = resolve(process.cwd(), ".env.local");
const ENV_EXAMPLE_PATH = resolve(process.cwd(), ".env.example");

const CRITICAL_VARS = ["DATABASE_URL", "SUPABASE_DB_URL", "BETTER_AUTH_SECRET", "PAPERCLIP_INSTANCE_ID"] as const;

type CheckResult = { ok: boolean; fatal: boolean; message: string };

function ok(msg: string): CheckResult { return { ok: true, fatal: false, message: msg }; }
function fail(msg: string): CheckResult { return { ok: false, fatal: true, message: msg }; }
function warn(msg: string): CheckResult { return { ok: false, fatal: false, message: msg }; }

function printCheck(name: string, result: CheckResult): void {
  if (result.ok) {
    process.stdout.write(`${GREEN}✓${RESET} ${name}\n`);
  } else if (result.fatal) {
    process.stdout.write(`${RED}✗${RESET} ${name}\n  ${result.message}\n`);
  } else {
    process.stdout.write(`${YELLOW}⚠${RESET} ${name}\n  ${result.message}\n`);
  }
}

function checkNodeVersion(): CheckResult {
  const major = Number(process.versions.node.split(".")[0]);
  if (major >= REQUIRED_NODE_MAJOR) return ok(`Node ${process.versions.node}`);
  return fail(`Node ${REQUIRED_NODE_MAJOR}+ requerido (atual: v${process.versions.node}). Atualize: https://nodejs.org`);
}

function checkPnpm(): CheckResult {
  try {
    const version = execSync("pnpm --version", { encoding: "utf8", stdio: ["ignore", "pipe", "ignore"] }).trim();
    // Comparação semver simples (X.Y.Z)
    const [maj, min, patch] = version.split(".").map(Number);
    const [rmaj, rmin, rpatch] = REQUIRED_PNPM_VERSION.split(".").map(Number);
    const compatible = maj > rmaj || (maj === rmaj && (min > rmin || (min === rmin && patch >= rpatch)));
    if (compatible) return ok(`pnpm ${version}`);
    return fail(`pnpm ${REQUIRED_PNPM_VERSION}+ requerido (atual: ${version}). Instale: corepack enable && corepack prepare pnpm@${REQUIRED_PNPM_VERSION} --activate`);
  } catch {
    return fail(`pnpm ausente. Instale: corepack enable && corepack prepare pnpm@${REQUIRED_PNPM_VERSION} --activate`);
  }
}

function parseEnvFile(content: string): Record<string, string> {
  const result: Record<string, string> = {};
  for (const line of content.split(/\r?\n/)) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith("#")) continue;
    const eq = trimmed.indexOf("=");
    if (eq === -1) continue;
    const key = trimmed.slice(0, eq).trim();
    let value = trimmed.slice(eq + 1).trim();
    // Strip aspas opcionais
    if ((value.startsWith("\"") && value.endsWith("\"")) || (value.startsWith("'") && value.endsWith("'"))) {
      value = value.slice(1, -1);
    }
    result[key] = value;
  }
  return result;
}

function checkEnvLocal(): { result: CheckResult; env: Record<string, string> } {
  if (!existsSync(ENV_LOCAL_PATH)) {
    return { result: fail(`.env.local ausente. Rode: cp .env.example .env.local`), env: {} };
  }
  const env = parseEnvFile(readFileSync(ENV_LOCAL_PATH, "utf8"));
  return { result: ok(".env.local presente"), env };
}

function checkCriticalEnvVars(env: Record<string, string>): CheckResult[] {
  const results: CheckResult[] = [];
  for (const name of CRITICAL_VARS) {
    const value = env[name];
    if (!value) {
      results.push(fail(`${name} ausente em .env.local. Veja .env.example. Peça os valores no canal interno do time.`));
      continue;
    }
    if (value.includes(PLACEHOLDER)) {
      results.push(fail(`${name} contém "${PLACEHOLDER}" (não preenchida). Peça os valores no canal interno do time.`));
      continue;
    }
    if (name === "PAPERCLIP_INSTANCE_ID" && value !== REQUIRED_INSTANCE_ID) {
      results.push(fail(`PAPERCLIP_INSTANCE_ID deve ser exatamente "${REQUIRED_INSTANCE_ID}" (atual: "${value}"). Sessões não interoperam entre devs com prefixos diferentes. Veja TROUBLESHOOTING.md "Cookie prefix divergente".`));
      continue;
    }
    results.push(ok(`${name} preenchida`));
  }
  return results;
}

function checkClaudeCli(): CheckResult {
  const cmd = process.platform === "win32" ? "where claude" : "which claude";
  try {
    execSync(cmd, { stdio: ["ignore", "ignore", "ignore"] });
    return ok("claude CLI presente no PATH");
  } catch {
    return warn(`claude CLI não encontrado no PATH. Instale: https://docs.claude.com/en/docs/claude-code/setup (aviso, não fatal — você pode continuar sem multi-account na Fase 3)`);
  }
}

async function checkSupabaseConnection(env: Record<string, string>): Promise<CheckResult> {
  // Injetar no process.env para createDb
  process.env.DATABASE_URL = env.DATABASE_URL;
  try {
    const db = createDb(env.DATABASE_URL!);
    const result = await db.execute(sql`SELECT 1 AS ping`);
    // Encerrar pool: createDb retorna drizzle handle; pegar $client
    const closable = db as typeof db & { $client?: { end?: (opts?: { timeout?: number }) => Promise<void> } };
    await closable.$client?.end?.({ timeout: 5 }).catch(() => undefined);
    if (Array.isArray(result) ? result.length > 0 : true) return ok("Supabase ping OK (pooler 6543, prepare:false)");
    return fail("Supabase ping retornou resultado inesperado");
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    return fail(`Conexão Supabase falhou: ${msg}. Veja TROUBLESHOOTING.md "Supabase no limite de conexões".`);
  }
}

async function checkBetterAuthSchema(env: Record<string, string>): Promise<CheckResult> {
  try {
    const db = createDb(env.DATABASE_URL!);
    const result = await db.execute(sql`SELECT count(*)::int AS count FROM "user"`);
    const closable = db as typeof db & { $client?: { end?: (opts?: { timeout?: number }) => Promise<void> } };
    await closable.$client?.end?.({ timeout: 5 }).catch(() => undefined);
    return ok("Schema Better Auth aplicado (tabela `user` presente)");
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    return fail(`Schema Better Auth não aplicado (${msg}). CI normalmente aplica em merge para main; se você acabou de clonar, aguarde ou peça pull do main no time.`);
  }
}

async function main(): Promise<void> {
  process.stdout.write(`\n🔧 DDD setup — validando ambiente local...\n\n`);

  let hasFatalFailure = false;

  const nodeResult = checkNodeVersion();
  printCheck("Node version", nodeResult);
  if (!nodeResult.ok && nodeResult.fatal) hasFatalFailure = true;

  const pnpmResult = checkPnpm();
  printCheck("pnpm version", pnpmResult);
  if (!pnpmResult.ok && pnpmResult.fatal) hasFatalFailure = true;

  if (hasFatalFailure) {
    process.stdout.write(`\n${RED}Setup interrompido. Corrija os erros acima antes de continuar.${RESET}\n`);
    process.exit(1);
  }

  const envCheck = checkEnvLocal();
  printCheck(".env.local", envCheck.result);
  if (!envCheck.result.ok) {
    process.stdout.write(`\n${RED}Setup interrompido. Corrija os erros acima antes de continuar.${RESET}\n`);
    process.exit(1);
  }

  const varResults = checkCriticalEnvVars(envCheck.env);
  for (const r of varResults) {
    printCheck("env var", r);
    if (!r.ok && r.fatal) hasFatalFailure = true;
  }

  if (hasFatalFailure) {
    process.stdout.write(`\n${RED}Setup interrompido. Veja TROUBLESHOOTING.md para detalhes.${RESET}\n`);
    process.exit(1);
  }

  const claudeResult = checkClaudeCli();
  printCheck("claude CLI", claudeResult);
  // Warning não bloqueia

  const supabaseResult = await checkSupabaseConnection(envCheck.env);
  printCheck("Supabase ping", supabaseResult);
  if (!supabaseResult.ok && supabaseResult.fatal) hasFatalFailure = true;

  if (hasFatalFailure) {
    process.stdout.write(`\n${RED}Setup interrompido. Veja TROUBLESHOOTING.md para detalhes.${RESET}\n`);
    process.exit(1);
  }

  const schemaResult = await checkBetterAuthSchema(envCheck.env);
  printCheck("Better Auth schema", schemaResult);
  if (!schemaResult.ok && schemaResult.fatal) hasFatalFailure = true;

  if (hasFatalFailure) {
    process.stdout.write(`\n${RED}Setup interrompido. Veja TROUBLESHOOTING.md para detalhes.${RESET}\n`);
    process.exit(1);
  }

  process.stdout.write(`\n${GREEN}✅ Ambiente OK. Próximo passo: pnpm dev${RESET}\n\n`);
  process.exit(0);
}

main().catch((err) => {
  process.stderr.write(`\n${RED}Erro inesperado em pnpm setup:${RESET}\n${err instanceof Error ? err.stack : String(err)}\n`);
  process.exit(2);
});
```

Notas de implementação:
- NÃO chamar `dotenv.config()` (não há dependência); o script lê `.env.local` manualmente para ter controle total sobre placeholder detection.
- Reusar `createDb` de `@paperclipai/db` (export do workspace) — ele já aplica `prepare:false` automaticamente para porta 6543 (ver `buildPostgresOptions`).
- Encerrar o pool após cada check para não vazar conexões (criar uma nova conexão por check é OK; setup é one-shot).
- Cores ANSI inline (sem dependência externa como `chalk`).
- Decisão D-discrição: NÃO incluir `--full` flag com Better Auth signup smoke (ponto 8 do D-02 marcado como opcional). Manter escopo enxuto; signup smoke fica para Plan 03-04 (cross-machine).
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./scripts/setup.ts').then(()=>console.log('imports ok')).catch(e=>{console.error(e);process.exit(1)})" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/scripts/setup.ts` existe
    - `grep -c "checkNodeVersion\|checkPnpm\|checkEnvLocal\|checkCriticalEnvVars\|checkSupabaseConnection\|checkBetterAuthSchema\|checkClaudeCli" D:/projetos/ddd/scripts/setup.ts` retorna >= 7 (uma definição + uma chamada cada, ou similar; mínimo todas as 7 funções referenciadas)
    - `grep -q "from \"@paperclipai/db\"" D:/projetos/ddd/scripts/setup.ts` retorna sucesso (reusa factory)
    - `grep -q "team-shared" D:/projetos/ddd/scripts/setup.ts` retorna sucesso (literal de PAPERCLIP_INSTANCE_ID validado)
    - `grep -q "TODO_FILL_ME" D:/projetos/ddd/scripts/setup.ts` retorna sucesso (placeholder detection)
    - `grep -q "TROUBLESHOOTING.md" D:/projetos/ddd/scripts/setup.ts` retorna sucesso (mensagens linkam para troubleshooting)
    - `grep -q "process.exit(1)" D:/projetos/ddd/scripts/setup.ts` retorna sucesso (exit code não-zero em falha)
    - Arquivo tem >= 200 linhas
  </acceptance_criteria>
  <done>scripts/setup.ts existe, importa createDb de @paperclipai/db, valida 7 checks (Node, pnpm, .env.local, vars críticas, claude CLI warning, Supabase ping, Better Auth schema), exit 1 em qualquer falha obrigatória.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Registrar `pnpm setup` em package.json</name>
  <files>package.json</files>
  <read_first>
    - `D:/projetos/ddd/package.json` (raiz; ver formato de scripts existentes — convenção `tsx scripts/X.ts` vs `pnpm --filter ...`)
    - `D:/projetos/ddd/scripts/setup.ts` (criado na Tarefa 1; o caminho exato deve coincidir)
  </read_first>
  <action>
Adicionar uma entrada nova ao bloco `scripts` do `D:/projetos/ddd/package.json`:

```json
"setup": "tsx scripts/setup.ts",
```

Posicionamento sugerido: logo após `"prepare": "husky",` e antes de `"preflight:workspace-links"`. Não modificar nenhum outro script existente. Não mexer em `devDependencies`, `engines`, `pnpm`, ou outros campos.

Nota sobre `tsx`: o monorepo já usa `tsx` em outros scripts via `node cli/node_modules/tsx/dist/cli.mjs` (ver `paperclipai`) ou via `pnpm --filter @paperclipai/server exec tsx` (ver `dev`). Para `pnpm setup` rodar do root sem dependência de workspace, usar `tsx` diretamente — funciona se `tsx` estiver no PATH via pnpm hoisting. Se em verify o comando falhar com "tsx: command not found", trocar por `node cli/node_modules/tsx/dist/cli.mjs scripts/setup.ts` (mesmo padrão de `preflight:workspace-links`).

Se a checagem falhar, usar a forma alternativa:
```json
"setup": "node cli/node_modules/tsx/dist/cli.mjs scripts/setup.ts",
```
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.setup){process.exit(1)}; if(!p.scripts.setup.includes('scripts/setup.ts')){process.exit(2)}; console.log('OK:', p.scripts.setup)"</automated>
  </verify>
  <acceptance_criteria>
    - `D:/projetos/ddd/package.json` campo `scripts.setup` existe
    - Valor de `scripts.setup` contém literalmente o substring `scripts/setup.ts`
    - Valor de `scripts.setup` invoca `tsx` (via `tsx scripts/setup.ts` OU `node cli/node_modules/tsx/dist/cli.mjs scripts/setup.ts`)
    - `grep -q "\"setup\":" D:/projetos/ddd/package.json` retorna sucesso
    - Nenhum outro script foi removido: `node -e "const p=require('./package.json'); const expected=['dev','build','typecheck','test','db:migrate']; for(const s of expected){if(!p.scripts[s])throw new Error('missing '+s)}"` passa
  </acceptance_criteria>
  <done>package.json tem entrada `"setup": "tsx scripts/setup.ts"` (ou variante node-tsx), todos os outros scripts preservados.</done>
</task>

</tasks>

<verification>
- `node -e "const p=require('./package.json'); console.log(p.scripts.setup)"` imprime comando contendo `scripts/setup.ts`
- `cat scripts/setup.ts | wc -l` >= 200
- `pnpm setup` (executado com `.env.local` preenchido) sai com 0; (executado sem `.env.local`) sai com 1 e mensagem mencionando `cp .env.example .env.local`
</verification>

<success_criteria>
1. `pnpm setup` é invocável e roda sem erros de import/sintaxe.
2. Falha rápido (exit 1) com mensagem acionável quando qualquer check obrigatório falha.
3. Sucesso (exit 0) quando ambiente está corretamente configurado, mesmo que `claude` CLI esteja ausente (warning).
4. TEAM-03 endereçado: critério #2 do ROADMAP cumprido.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-de-equipe-onboarding/03-01-SUMMARY.md` documenting: actual file paths, function names exposed, decision on `tsx` invocation form chosen, any deviations from the action spec.
</output>
