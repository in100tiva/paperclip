---
phase: 03-workflow-de-equipe-onboarding
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - .planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md
autonomous: true
requirements: [TEAM-01]

must_haves:
  truths:
    - "Procedimento documentado para bootstrap CEO (one-time) + cadastro coletivo de 5+ devs via fluxo company_join"
    - "Procedimento reusa fluxo de invite EXISTENTE (server/src/services/invite-grants.ts + ui CompanyInvites) — sem código novo"
    - "Validação de sucesso é uma SQL definida no Supabase Studio que conta users na company"
    - "Itens que requerem 5+ devs reais cadastrando estão roteados para HUMAN-UAT (já criado em 03-04), não fingidos como auto-passable"
  artifacts:
    - path: ".planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md"
      provides: "Procedimento manual de bootstrap CEO + cadastro coletivo de 5+ devs"
      contains: "bootstrap_ceo|company_join|invite|create-auth-bootstrap-invite|SELECT count"
      min_lines: 80
  key_links:
    - from: "TEAM-SIGNUP-PROCEDURE.md"
      to: "packages/db/scripts/create-auth-bootstrap-invite.ts"
      via: "comando exato com --config e --base-url"
      pattern: "create-auth-bootstrap-invite"
    - from: "TEAM-SIGNUP-PROCEDURE.md"
      to: "ONBOARDING.md"
      via: "referência ao setup que cada novo dev faz antes de aceitar invite"
      pattern: "ONBOARDING\\.md"
    - from: "TEAM-SIGNUP-PROCEDURE.md"
      to: "doc/spec/invite-flow.md"
      via: "referência ao mapa do fluxo de invite (states, transitions)"
      pattern: "invite-flow\\.md"
---

<objective>
Documentar o procedimento de cadastro coletivo da equipe (`TEAM-SIGNUP-PROCEDURE.md`): dev #1 executa bootstrap_ceo via script existente, gera invites, e demais 4+ devs entram via fluxo `company_join` padrão do paperclip. Reusa fluxo existente — zero código novo. Validação é SQL no Supabase contando users.

Purpose: Satisfazer TEAM-01 (no que é automatizável). Critério de sucesso #3 do ROADMAP: "5+ devs estão cadastrados via fluxo de invite/board-claim do paperclip apontando para Supabase compartilhado."

Output: Doc procedural pt-br linkado pelo HUMAN-UAT-03-02 (criado em 03-04). Execução real (5+ devs cadastrando) fica para HUMAN-UAT — esta fase não pode fingir cadastro de devs reais.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md
@doc/spec/invite-flow.md
@packages/db/scripts/create-auth-bootstrap-invite.ts
@ONBOARDING.md

<interfaces>
<!-- Comandos reais e fluxos existentes a citar literalmente -->

Bootstrap script signature (de packages/db/scripts/create-auth-bootstrap-invite.ts):
```
tsx packages/db/scripts/create-auth-bootstrap-invite.ts --config <path-to-config> --base-url <url>
```
Config padrão: `~/.paperclip/instances/default/config.json`
Output: URL `<base-url>/invite/<token>` impressa em stdout.

Invite types (de doc/spec/invite-flow.md):
- `bootstrap_ceo` — one-time, cria primeiro user/company. Não cria join_request.
- `company_join` — fluxo normal pra demais devs. Cria join_request com `pending_approval` que admin aprova.

Schemas relevantes (Supabase / packages/db/src/schema):
- `user` — Better Auth users (id text, email, name)
- `company` — companies
- `company_membership` — join table (company_id, user_id, role)
- `invites` — invite tokens (inviteType, tokenHash, expiresAt, revokedAt, acceptedAt)
- `join_requests` — pending/approved/rejected requests

Decisões D-09..D-12 da Fase 3:
- D-09: REUSAR fluxo existente — não reescrever
- D-10: Bootstrap dev #1 via script
- D-11: Demais via canal interno (URL postada pelo CEO)
- D-12: Sucesso = 5+ rows em `user` na mesma company
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar TEAM-SIGNUP-PROCEDURE.md com bootstrap + cadastro coletivo + SQL de validação</name>
  <files>.planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md</files>
  <read_first>
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` §"Invite/Cadastro de Devs (TEAM-01)" D-09..D-12 (decisões travadas — reusar fluxo, bootstrap one-time, validação por SQL)
    - `D:/projetos/ddd/packages/db/scripts/create-auth-bootstrap-invite.ts` (assinatura exata: `--config <path> --base-url <url>`; output em stdout)
    - `D:/projetos/ddd/doc/spec/invite-flow.md` linhas 1-80 (entender bootstrap_ceo vs company_join lifecycle)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md` §UAT-03-02 (este procedimento é referenciado por aquele UAT)
    - `D:/projetos/ddd/.planning/STATE.md` decisão 02-06 sobre `local_trusted` vs `authenticated` mode (relevante: bootstrap funciona em qualquer mode mas signups via UI requerem `authenticated`)
  </read_first>
  <action>
Criar arquivo NOVO em `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md`. Idioma: pt-br.

Conteúdo:

```markdown
# Cadastro Coletivo da Equipe — Phase 3 / TEAM-01

**Phase:** 03 — Workflow de Equipe + Onboarding
**Requirement satisfied:** TEAM-01
**Padrão de execução:** manual (CEO executa script + 4+ devs aceitam invites via UI)
**Reusa:** Fluxo `bootstrap_ceo` + `company_join` existente do paperclip (decisão D-09 — sem código novo)

## Objetivo

Cumprir o critério de sucesso #3 do ROADMAP da Phase 3:

> 5+ devs estão cadastrados via fluxo de invite/board-claim do paperclip apontando para Supabase compartilhado.

Validação final é SQL contando users em company shared (D-12).

## Visão Geral do Fluxo

```
Dev #1 (CEO bootstrap)
  ↓ executa create-auth-bootstrap-invite.ts
  ↓ recebe URL /invite/<token>
  ↓ abre URL → signup → aceita → company criada
  ↓
  ↓ vai pra Company Settings → Invites → cria invite human
  ↓ recebe URL /invite/<novo-token>  (tipo: company_join)
  ↓ posta URL no canal interno do time
  ↓
Devs #2..N (cadastro normal)
  ↓ leem ONBOARDING.md, fazem pnpm setup
  ↓ pegam URL no canal interno
  ↓ abrem URL → signup → aceita → join_request pending_approval
  ↓
Dev #1 (admin)
  ↓ Company Settings → Access → aprova join_requests
  ↓
Devs #2..N
  ↓ refresh → redirecionados pra dashboard da company shared
```

Ver mapa completo dos states + transitions: [doc/spec/invite-flow.md](../../../doc/spec/invite-flow.md).

## Pré-requisitos

- [ ] Phase 2 fechada (Supabase remoto operacional, Better Auth funcional, schema aplicado via CI)
- [ ] Dev #1 (CEO bootstrap) tem `.env.local` preenchido e `pnpm setup` saiu OK
- [ ] Servidor authenticated subido **uma vez** pra rodar o bootstrap:
  ```bash
  PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
  ```

## Passo 1: Bootstrap CEO (one-time, dev #1)

> ⚠️ **Esse passo só roda UMA VEZ na vida do time.** Se a company shared já existe, pular para o Passo 2.

Para checar se já existe bootstrap_ceo válido:

```sql
-- Supabase Studio → SQL Editor
SELECT id, "inviteType", "expiresAt", "acceptedAt", "revokedAt"
FROM invites
WHERE "inviteType" = 'bootstrap_ceo'
ORDER BY "createdAt" DESC
LIMIT 5;
```

Se retornar linha com `acceptedAt IS NOT NULL`: bootstrap já feito → pular para Passo 2.

Caso contrário, dev #1 executa:

```bash
tsx packages/db/scripts/create-auth-bootstrap-invite.ts \
  --config ~/.paperclip/instances/default/config.json \
  --base-url http://localhost:3100
```

Output esperado (stdout):

```
http://localhost:3100/invite/pcp_bootstrap_<64hex>
```

Dev #1:
1. Copia a URL.
2. Abre no browser **com servidor em authenticated mode** (ver pré-requisitos).
3. Faz signup com email + senha do time. **Anote o BETTER_AUTH_SECRET** — todos os devs precisam usar o mesmo secret no `.env.local` (ver ONBOARDING.md §3).
4. Aceita o invite — bootstrap_ceo cria company padrão automaticamente, sem aprovação.
5. Confirma que está no dashboard da company.

Validação intermediária:

```sql
SELECT count(*)::int AS user_count FROM "user";
SELECT count(*)::int AS company_count FROM company;
```

Esperado: `user_count = 1`, `company_count = 1`.

## Passo 2: Gerar invites company_join (dev #1, repetido N vezes)

Para cada dev novo a cadastrar, dev #1 (logado como admin):

1. Navega para **Company Settings → Invites** na UI.
2. Clica **Create invite** (role: padrão `member` ou `admin` conforme a política do time).
3. Copia URL gerada (formato `http://localhost:3100/invite/<token>`).
4. Posta no **canal interno do time** (Discord/Slack) — **NÃO** no `.env.example` ou em qualquer arquivo do repo (decisão D-11).

Repetir até ter pelo menos 4 invites ativos para os devs #2..#5+.

> Os invites têm expiração default. Se um dev demorar a aceitar, gerar invite novo (revogar o velho via mesma UI se ele ainda não aceitou).

## Passo 3: Devs #2..N aceitam invites (cada dev em sua máquina)

Cada dev novo:

1. Lê [ONBOARDING.md](../../../ONBOARDING.md) e completa setup local até `pnpm setup` sair OK.
2. Sobe servidor authenticated:
   ```bash
   PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
   ```
3. Pega URL no canal interno.
4. Abre URL no browser — vê tela "Inline Auth" (porque está em authenticated mode + sem session).
5. Faz **signup** (toggle "create account") com email/senha.
6. Aceita o invite — sistema cria `join_request` com status `pending_approval`.
7. Vê tela "Aguardando aprovação".

Dev #1 (admin):

1. Volta na UI → **Company Settings → Access**.
2. Vê lista de join_requests pending.
3. Aprova cada um.

Devs #2..N:

1. Refresh da página de invite.
2. Redirecionados pra dashboard da company shared.

## Passo 4: Validação Final (TEAM-01 critério de sucesso)

Quando atingir 5+ devs cadastrados, executar via Supabase Studio SQL Editor:

```sql
-- Total de users
SELECT count(*)::int AS total_users FROM "user";

-- Distribuição por company (esperado: TODOS na company shared)
SELECT
  c.id AS company_id,
  c.name AS company_name,
  count(cm."userId")::int AS members
FROM company c
LEFT JOIN company_membership cm ON cm."companyId" = c.id
GROUP BY c.id, c.name
ORDER BY members DESC;
```

**Critério PASS (D-12):**
- `total_users >= 5`
- A company principal (shared) tem `members >= 5`
- Não existem outras companies com membros — todos os devs estão na mesma company.

> **Nota sobre nomes de colunas:** Drizzle pode gerar tabelas com `camelCase` ou `snake_case` dependendo da versão. Se a query falhar com `column "userId" does not exist`, tentar `user_id` / `company_id`. O schema vivo está em `packages/db/src/schema/`.

## Notas Operacionais

- **NÃO criar users via SQL direto.** O fluxo Better Auth gera password hashes e timestamps que não devem ser falsificados — viola D-09 (reusar fluxo existente).
- **NÃO compartilhar invite tokens em arquivos versionados.** Tokens viram parte do `tokenHash` no schema; expor em git compromete o fluxo. Use sempre canal interno efêmero.
- **Revogar invites não usados** após N dias (UI tem botão Revoke). Tokens vivem no DB indefinidamente até serem revogados ou expirarem.
- **Cookie prefix**: validar que cada dev tem `paperclip-team-shared.session_token` no DevTools após login. Se cookie tem outro prefix, esse dev configurou `PAPERCLIP_INSTANCE_ID` errado — corrigir antes de prosseguir (ver [TROUBLESHOOTING.md § Cookie prefix divergente](../../../TROUBLESHOOTING.md#cookie-prefix-divergente)).

## Status de Execução

Esta validação é HUMAN-UAT (ver [03-HUMAN-UAT.md § UAT-03-02](03-HUMAN-UAT.md#uat-03-02--cadastro-de-5-devs-reais-team-01)).

- [ ] Bootstrap CEO completado por dev #1
- [ ] Invites company_join gerados (4+)
- [ ] Devs #2..#5+ aceitaram e foram aprovados
- [ ] SQL de validação executada com `total_users >= 5` e todos na company shared
- [ ] Resultado registrado em `03-HUMAN-UAT.md` UAT-03-02

Quando todas as caixas marcadas, atualizar status do UAT-03-02 para `approved`.

## Fallback Aceito

Se < 5 devs reais disponíveis na janela atual: marcar status do UAT-03-02 como `pending-team-growth` e re-checar quando atingir 5. **NÃO** criar users falsos — eles poluem a base e quebram cost attribution na Fase 6.
```

Notas:
- Procedimento descreve TODO o fluxo mas NÃO executa nada — execução é HUMAN-UAT.
- Inclui SQL de validação prontas para copy-paste.
- Linka explicitamente para `03-HUMAN-UAT.md` (criado em 03-04) para fechar o loop de tracking.
- Reforça D-09 (reusar fluxo, não reescrever) e D-11 (canal interno, nada no repo).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md'; const c=fs.readFileSync(p,'utf8'); const checks=[/bootstrap_ceo/,/company_join/,/create-auth-bootstrap-invite/,/--config/,/--base-url/,/PAPERCLIP_DEPLOYMENT_MODE=authenticated/,/SELECT count/,/total_users/,/ONBOARDING\.md/,/invite-flow\.md/,/03-HUMAN-UAT\.md/,/canal interno/i,/Fallback/i]; for(const r of checks){if(!r.test(c)){console.error('MISSING:',r);process.exit(1)}}; if(c.split('\\n').length<80){console.error('TOO SHORT');process.exit(2)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md` existe
    - Arquivo tem >= 80 linhas
    - Cita comando bootstrap script: `grep -q "create-auth-bootstrap-invite" TEAM-SIGNUP-PROCEDURE.md && grep -q "\-\-config" TEAM-SIGNUP-PROCEDURE.md && grep -q "\-\-base-url" TEAM-SIGNUP-PROCEDURE.md`
    - Cita modo authenticated: `grep -q "PAPERCLIP_DEPLOYMENT_MODE=authenticated" TEAM-SIGNUP-PROCEDURE.md`
    - Distingue bootstrap_ceo de company_join: `grep -q "bootstrap_ceo" TEAM-SIGNUP-PROCEDURE.md && grep -q "company_join" TEAM-SIGNUP-PROCEDURE.md`
    - SQL de validação presente: `grep -q "SELECT count" TEAM-SIGNUP-PROCEDURE.md && grep -q "total_users\|user_count" TEAM-SIGNUP-PROCEDURE.md`
    - Linka invite-flow spec: `grep -q "invite-flow.md" TEAM-SIGNUP-PROCEDURE.md`
    - Linka ONBOARDING: `grep -q "ONBOARDING.md" TEAM-SIGNUP-PROCEDURE.md`
    - Linka HUMAN-UAT: `grep -q "03-HUMAN-UAT.md" TEAM-SIGNUP-PROCEDURE.md`
    - Documenta canal interno (não em repo): `grep -qi "canal interno" TEAM-SIGNUP-PROCEDURE.md`
    - Fallback documentado: `grep -qi "fallback\|pending-team-growth" TEAM-SIGNUP-PROCEDURE.md`
    - Reforça reuso de fluxo existente: `grep -q "D-09\|reusar fluxo\|sem código novo" TEAM-SIGNUP-PROCEDURE.md`
    - Avisa contra falsificação: `grep -qi "NÃO criar users via SQL\|users falsos" TEAM-SIGNUP-PROCEDURE.md`
  </acceptance_criteria>
  <done>TEAM-SIGNUP-PROCEDURE.md criado documentando bootstrap CEO + cadastro coletivo via fluxo existente, com SQL de validação pronta e fallback aceito; execução real é HUMAN-UAT.</done>
</task>

</tasks>

<verification>
- `wc -l .planning/phases/03-workflow-de-equipe-onboarding/TEAM-SIGNUP-PROCEDURE.md` >= 80
- Procedimento auto-suficiente para humanos executarem (CEO + 4+ devs)
- Cross-references válidos: TEAM-SIGNUP-PROCEDURE → ONBOARDING, TROUBLESHOOTING, invite-flow.md, 03-HUMAN-UAT
- Nenhum código fonte novo escrito (decisão D-09 respeitada)
</verification>

<success_criteria>
1. Procedimento documentado para chegar em 5+ devs cadastrados via fluxo existente do paperclip.
2. SQL de validação pronta para o admin executar quando atingir o threshold.
3. Fallback `pending-team-growth` aceito (sem fingir cadastros).
4. Loop de tracking fechado: doc linka HUMAN-UAT que linka de volta.
5. TEAM-01 endereçado: critério #3 do ROADMAP coberto pelo procedimento; execução real é HUMAN-UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-de-equipe-onboarding/03-05-SUMMARY.md` documenting: estrutura final do procedimento, SQL queries efetivamente incluídas, justificativa para roteamento HUMAN-UAT em vez de auto-passable, e qualquer ajuste de query SQL feito após verificar nomes reais de colunas no schema (snake_case vs camelCase).
</output>
