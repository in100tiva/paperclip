---
state_version: 1.0
milestone: v1.1
milestone_name: Internacionalização pt-BR
status: "Plano 11-02 concluído em ~10min (3 commits atomic — `1955666` feat buildLanguageDirectiveBlock pure function (server/src/services/agent-instructions-locale-directive.ts, 28 linhas) retornando bloco markdown canônico para pt-BR + string vazia para en-US (model default + token economy) + 4 unit cases GREEN; `6bf9896` feat exportFiles wiring com options?: { locale?: RuntimeLocale } opcional (backward-compat company-portability.ts:3244) anexando directive APENAS ao entry file content em ambos branches managed/external e legacy fallback + 3 novos cases agent-instructions-service.test.ts (filter agent-instructions 20/20 GREEN); `e939fa0` feat bundleKey + resume fallback — buildClaudePromptBundleKey/prepareClaudePromptBundle agora REQUEREM locale, hash version v1→v2 (one-shot natural cache invalidation Pitfall 1); execute.ts lê context.runtimeLocale e propaga; resume-session fallback (Pitfall 2) — buildLanguageDirectiveBlockForResume duplicado inline com sync-comment porque adapter-claude-local não depende de @paperclipai/server (one-way deps preservation) + appended ao último joinPromptSections entry quando sessionId existe (recency bias maximiza adesão pt-BR em sessões resumidas) + __testing__ export para testes alcançarem helper interno + 3 novos cases claude-local-prompt-cache-locale.test.ts GREEN. Padrões novos: canonical-text-duplication-with-sync-comment (cross-package one-way deps); resume-session prompt-body fallback for system-prompt directives (claude-local --append-system-prompt-file é one-shot, skipped on resume); bundle hash version-bump pattern (cache invalidation natural one-shot). Decisões: (1) inline-duplicate buildLanguageDirectiveBlock; (2) empty-string en-US directive (revisitar via UAT); (3) directive ao fim do prompt body em resume; (4) hash bump v1→v2; (5) exportFiles options opcional. Sem desvios — plano executou exatamente como escrito. AGENT-SKILL-01 satisfeito at code-level. Self-check PASSED. agent-instructions-locale-directive 4/4 GREEN; agent-instructions filter 20/20 GREEN; claude-local-prompt-cache-locale 3/3 GREEN; claude-local-execute 3/14 GREEN com 11/14 fail pre-existing Windows shebang (verificado em master via stash); typecheck claude-local exit 0; typecheck server preserva apenas pre-existing recovery/service.ts:459 baseline. Plano 11-03 destravado — file sets disjuntos confirmados (11-02 modificou prompt-cache.ts top-half hashing/key-shape; 11-03 vai modificar bottom-half symlink loop em ensurePaperclipSkillSymlink → materializeSkillForLocale)."
last_updated: "2026-04-27T02:50:00.000Z"
progress:
  total_phases: 5
  completed_phases: 4
  total_plans: 22
  completed_plans: 21
---

# Estado do Projeto

## Referência do Projeto

Ver: .planning/PROJECT.md (atualizado em 2026-04-26)

**Valor central:** Equipe inteira opera sobre estado compartilhado (Supabase remoto) e agentes nunca param por exhaustão de token — basta trocar conta e continuar.
**Foco atual:** Milestone v1.1 (Internacionalização pt-BR) iniciado em 2026-04-26. Definindo requisitos. Continuação de numeração de fases a partir da fase 7.

## Posição Atual

Fase: 11 de 11 (Skills + System Prompts dos Agentes) — **IN PROGRESS** (2/3 plans complete)
Plano: 11-02 — concluído. Próxima ação: /executar-fase 11 → 11-03 (Skill pt-BR variants + loader + HUMAN-UAT — AGENT-SKILL-02 + AGENT-SKILL-04). 11-03 consome buildClaudePromptBundleKey({locale}) já requerido + context.runtimeLocale já lido em execute.ts; só precisa adicionar materializeSkillForLocale para swap symlink → copy+rename quando SKILL.{locale}.md existe (Pattern 3 do RESEARCH).
Status: Plano 11-02 concluído em ~10min (3 commits atomic — `1955666` feat buildLanguageDirectiveBlock pure function (server/src/services/agent-instructions-locale-directive.ts, 28 linhas) retornando bloco markdown canônico "## Idioma de Resposta\n\nResponda ao usuário em português brasileiro (pt-BR). Use linguagem natural, técnica quando apropriado. Mantenha termos técnicos em inglês quando idiomáticos (ex: \"commit\", \"merge\", \"pull request\")." para pt-BR + string vazia para en-US (token economy — model default; revisitar se UAT mostrar drift) + 4 unit cases GREEN cobrindo conteúdo, en-US empty, idempotência e leading/trailing newlines safe-concat; `6bf9896` feat exportFiles wiring com options?: { locale?: RuntimeLocale } opcional (backward-compat — company-portability.ts:3244 callsite sem runtime context preserva comportamento legacy) anexando directive APENAS ao entry file content (siblings TOOLS.md/etc untouched) em ambos branches managed/external e legacy fallback; 3 novos cases em agent-instructions-service.test.ts cobrindo pt-BR appends + en-US no-op + sem-locale legacy (full filter agent-instructions: 20/20 GREEN incluindo 4 directive + 5 routes + 11 service); `e939fa0` feat bundleKey + resume fallback — buildClaudePromptBundleKey/prepareClaudePromptBundle agora REQUEREM locale, hash version bumpado v1→v2 (paperclip-claude-prompt-bundle:v2\\n) garantindo invalidação one-shot natural no primeiro spawn pós-deploy (Pitfall 1 fechado sem migration script/TTL); execute.ts lê context.runtimeLocale (default pt-BR via narrowing === 'en-US' ? 'en-US' : 'pt-BR') e propaga para prepareClaudePromptBundle; resumo-session fallback (Pitfall 2) — buildLanguageDirectiveBlockForResume duplicado inline em execute.ts com sync-comment apontando canonical source porque @paperclipai/adapter-claude-local não depende de @paperclipai/server (one-way: server -> adapters; importar inverteria a direção e quebraria publishing) + appended ao último joinPromptSections entry quando sessionId existe (model attention bias para recência maximiza adesão pt-BR em sessões resumidas onde system prompt cached pode estar em en-US); __testing__ export adicionado em packages/adapters/claude-local/src/server/index.ts re-exportando buildClaudePromptBundleKey para tests em server/src/__tests__/ alcançarem helper interno sem expor formal public API; 3 novos cases em claude-local-prompt-cache-locale.test.ts (server-side) GREEN cobrindo locale divergence + stability + sha256 shape. Padrões novos: canonical-text-duplication-with-sync-comment (cross-package one-way deps onde duplicação literal ~28 LOC é mais barato que inverter dependency graph); resume-session prompt-body fallback for system-prompt directives (claude-local --append-system-prompt-file é one-shot, skipped on resume; directives que devem chegar a CADA heartbeat são appended ao prompt body via joinPromptSections com empty-string para en-US mantendo path universal sem branching no callsite); bundle hash version-bump pattern (quando bundleKey hash input shape muda, bumpar versão literal v1→v2 → caches invalidam one-shot no primeiro spawn pós-deploy, content-addressed natural rotation, sem TTL/migration). Decisões: (1) inline-duplicate buildLanguageDirectiveBlock em claude-local execute.ts (deps direction preserved); (2) empty-string en-US directive (model default + token economy; revisita por UAT); (3) directive ao FIM do prompt body em resume (recency bias maximiza adesão); (4) hash bump v1→v2 (one-shot natural invalidation); (5) exportFiles options opcional (company-portability.ts:3244 backward-compat). Sem desvios — plano executou exatamente como escrito; ajustes operacionais menores (test file location server/src/__tests__/ vs claude-local subpath porque pacote não tem vitest devDep; __testing__ namespace export para alcançar helper sem public API leak; test naming convention claude-local-* prefix matching siblings) documentados no SUMMARY mas não constituem desvio. AGENT-SKILL-01 satisfeito at code-level. Self-check PASSED. agent-instructions-locale-directive 4/4 GREEN; agent-instructions filter 20/20 GREEN; claude-local-prompt-cache-locale 3/3 GREEN; claude-local-execute 3/14 GREEN, 11/14 fail com pre-existing Windows shebang adapter spawn (verificado em master via stash, 06-03 deferred-items.md, fora-de-escopo); typecheck claude-local exit 0; typecheck server preserva apenas pre-existing recovery/service.ts:459 baseline (TS2339 'title', confirmed em master baseline desde Phase 7-04 + Phase 8-05 deferred-items + 11-01-SUMMARY.md, fora-de-escopo per <deviation_rules> boundary). Plano 11-03 destravado — file sets disjuntos confirmados (11-02 modificou prompt-cache.ts top-half hashing/key-shape; 11-03 vai modificar bottom-half symlink loop em ensurePaperclipSkillSymlink → materializeSkillForLocale).

Posição anterior: Fase: 11 de 11 (Skills + System Prompts dos Agentes) — **IN PROGRESS** (1/3 plans complete)
Plano anterior: 11-01 — concluído. Próxima ação anterior: /executar-fase 11 → 11-02 (Language directive content — AGENT-SKILL-01) e/ou 11-03 (Skill pt-BR variants + loader + HUMAN-UAT — AGENT-SKILL-02 + AGENT-SKILL-04). Wave 2 paralela: file sets disjuntos (11-02 toca server/src/services/agent-instructions.ts + novo agent-instructions-locale-directive.ts; 11-03 toca packages/adapters/claude-local/src/server/prompt-cache.ts + novos skills/*/SKILL.pt-BR.md). Ambos consomem RuntimeLocale + context.runtimeLocale agora disponíveis em server/src/services/heartbeat-locale.ts.
Status anterior: Plano 11-01 concluído em ~12min (3 commits atomic — `4cae59e` Wave 0 RED suite heartbeat-locale.test.ts com 5 cases (null short-circuit asserting db.select never called + system actor → pt-BR + user pt-BR/en-US + user actor null locale race fallback → pt-BR); `b0e78ad` GREEN heartbeat-locale.ts (56 LOC) export RuntimeLocale type + resolveRunOwnerLocale single-query leftJoin agent_wakeup_requests ⟕ authUsers on requestedByActorId=id + narrowLocale defensive helper collapsing any non-'en-US' to 'pt-BR' absorbing schema drift/legacy/race; `2fe6750` heartbeat.ts integration adding 1 import + resolution call linha ~5360 antes de adapter.execute mutando context.runtimeLocale via existing Record<string, unknown> channel — zero breaking change ao ServerAdapterModule contract em @paperclipai/adapter-utils). Padrões novos: locale resolution chain server-side (wakeupRequest → user com leftJoin único, narrowing literal RuntimeLocale na boundary); context-channel propagation via context.runtimeLocale (não augmentedConfig.runtimeLocale — context é flexible Record já consumido por todos 7 adapters; augmentedConfig reserved p/ adapter-config tipo claude credential dir); naming runtimeLocale (não paperclipRuntimeLocale prefix) — locale é first-class runtime input que Plans 11-02/11-03 leem explicitamente, reservar paperclip* prefix para framework-internal scratch state (paperclipSessionHandoffMarkdown etc); narrowLocale absorvendo schema drift/legacy/JOIN-miss em fallback pt-BR (helper nunca throw na resolução de locale — best-effort metadata, não precondition). Decisões: (1) field channel context.runtimeLocale; (2) field name runtimeLocale; (3) single leftJoin (não 2-stage select como middleware/auth.ts) — heartbeat hot path 1 round-trip vs 2; (4) defensive narrowing absorvendo todos cenários de drift. Sem desvios — plano executou exatamente como escrito; tipos confirmados (Db de @paperclipai/db, agentWakeupRequests + authUsers re-exported via barrel schema/index.ts); pattern de mock chained-builder reaproveitado de middleware-locale.test.ts (Phase 7-04 precedent). heartbeat-locale 5/5 GREEN; sibling regression suite 22/22 GREEN (heartbeat-locale + claude-accounts-swap + claude-account-costs); typecheck preserva apenas pre-existing services/recovery/service.ts:459 baseline (out-of-scope per STATE Phase 7-04 record). 5 test files / 2 tests failed em vitest -t heartbeat full sweep — todos pre-existing Windows shebang adapter spawn (gemini/codex/etc.) confirmed em master, documentado em 06-03 deferred-items.md. AGENT-SKILL-03 satisfeito at code-level (RuntimeLocale type exportado + resolveRunOwnerLocale + context.runtimeLocale propagation funcionando). Self-check PASSED. Wave 2 destravada — 11-02 e 11-03 podem rodar paralelos (file sets disjuntos confirmed).

Posição anterior: Fase: 10 de 11 (Mensagens dos Agentes ao Usuário) — **COMPLETE-WITH-PENDING-UAT** (3/3 plans complete)
Plano anterior: 10-03 — concluído. Próxima ação anterior: /verificar-fase 10 (PHASE-CHECKER validação) OU /planejar-fase 11 (Skills + System Prompts — AGENT-SKILL-01..04; última fase do milestone v1.1; agentes respondem em pt-BR quando idioma ativo via system prompt + agent context propagation).
Status anterior: Plano 10-03 concluído em ~14min (3 commits atomic — `8b1e9cc` extend common.json com 4 toast sub-trees + 1 leaf (20 new keys × 2 locales — toast.agent 3, toast.run 6, toast.join-request 4, toast.activity 6, toast.watchdog-decision-not-recorded 1) preservando Phase 7-9 keys byte-for-byte + LiveUpdatesProvider.toast.i18n.test.ts skeleton 5 it.todo; `79faa50` LiveUpdatesProvider refactor com tRef pattern (Pitfall 2 — useEffect socket-mounting deps UNCHANGED, language change não dispara closeSocketQuietly) + 4 toast builders (buildActivityToast/buildJoinRequestToast/buildAgentStatusToast/buildRunStatusToast) aceitando TFunction last param + handleLiveEvent forwarding t + socket.onmessage callback passing tRef.current at invocation time + __liveUpdatesTestUtils estendido com 4 builders + handleLiveEvent + LiveUpdatesProvider.test.ts tFake helper preservando 14 cases byte-for-byte (Rule 3 — 4 callsite signature mismatches) + LiveUpdatesProvider.toast.i18n.test.ts 5 cases promoted to GREEN (4 builders pt-BR via real i18n + 1 mocked-WebSocket no-reconnect via vi.mock CompanyContext/authApi/router + FakeWebSocket constructor count + close.mock.calls.length unchanged across i18n.changeLanguage); `a51e331` AgentDetail.tsx ConfigurationTab "Save failed" toast + IssueRunLedger.tsx watchdog "decision not recorded" toast migrados via t() — closes Plano 10-02 line 391/393 handoff explícito. Padrões novos: tRef pattern (Pitfall 2 mitigation — capture t via useRef + dedicated useEffect, socket useEffect deps UNCHANGED — reusable para qualquer provider hosting long-lived imperative resources); TFunction last-param convention on pure helper functions (4 builders); English-equivalent fake TFunction (tFake) helper para legacy unit tests (preserve byte-for-byte assertions sem migrar para RTL com provider); 5ª aplicação do `as never` typed-t() bypass para cross-namespace template-literal keys. Desvios: 2 auto-fixes (Rule 3 — 4 callsite signature mismatches in LiveUpdatesProvider.test.ts → tFake helper; Rule 1 — initial probe assertion 'Someone' vs actual 'Board' fallback semantics → updated assertion). Full UI suite **706/706 GREEN** (was 701, +5 net new probe cases; 0 regressions); CI=true missing-keys exit 0; UI typecheck preserva apenas pre-existing ActivityRow:42 baseline. AGENT-MSG-04 satisfeito at code-level. **Phase 10 completa** (10-01+10-02+10-03 cobrem AGENT-MSG-01..04 cumulativamente — agents.json 197 keys + common.json 164 keys × 2 locales). HUMAN-UAT-10-01..03 routed para operator validation post-merge (10-HUMAN-UAT.md persistido com status pending). Phase 10 fecha **complete-with-pending-UAT** (precedente Phases 3-9). Self-check PASSED.

Posição anterior: Fase: 10 de 11 (Mensagens dos Agentes ao Usuário) — **IN PROGRESS** (2/3 plans complete)
Plano anterior: 10-02 — concluído. Próxima ação anterior: /executar-fase 10 → 10-03 (Toasts/Notificações + LiveUpdatesProvider builders + activity log gaps — AGENT-MSG-04) Wave 2 sequencial; depende de namespace agents (Wave 1 concluída).
Status anterior: Plano 10-02 concluído em ~17min (3 commits atomic — `df39cf8` bootstrap agents.json estendido 147→197 leaf keys × 2 locales com 3 sub-trees DISJUNTOS de 10-01: transcript 33+aria 6, run-ledger 10 com plurals, live-runs 7 + RunTranscriptView.i18n.test.tsx 8-case probe pt-BR/en-US com interpolação `{{code}}`+plurals; `e29d9f4` RunTranscriptView 1526 LOC com optional TFunction param em pure helpers (displayToolName/summarizeToolResult/normalizeTranscript) preservando RunTranscriptView.test.tsx 5 cases sem provider + useTranslation em 6 sub-components + main + 30 strings + 6 aria-labels + interpolação failed-with-exit + plurals log-lines/system-messages/executed-commands/used-tool-multiple/tools-multiple via t(); RunChatSurface emptyMessage default via t(); LiveRunWidget 5 strings via t(); `c2eea53` IssueRunLedger linhas 416-708 com `{{active}}`/`{{done}}`/`{{cancelled}}` interpolação + more-children plural _one/_other PRESERVANDO linha 391 watchdog toast em inglês como handoff explícito para 10-03 + IssueRunLedger.test.tsx selector switch para raw key strings i18n-resilient). Padrões novos: optional TFunction parameter pattern (pure helpers aceitam `t?` com English fallback — preserva backward compat com tests sem provider); TFunction widening cast `t as unknown as TFunction` bridging useTranslation(["agents"]) narrow signature + helper default common signature; bare-key + _one + _other plural triplet (4ª aplicação após 8-03/9-04/10-01); subcomponent useTranslation pattern (cada bloco chama hook ao invés de receber t via prop — react-i18next idiom). Desvios: 3 auto-fixes Rule 3 (pure helpers fora do React scope → optional TFunction param; TFunction signature mismatch → as unknown as TFunction cast; IssueRunLedger.test.tsx assertions de inglês quebraram sem provider → switch para raw key strings, 3ª aplicação do pattern após 10-01 deviation #2). Full UI suite **701/701 GREEN** (was 693, +8 net new probes; 0 regressions); CI=true missing-keys exit 0; UI typecheck preserva apenas pre-existing ActivityRow:42 baseline. AGENT-MSG-02 satisfeito at code-level. **Wave 1 completa** (10-01+10-02 file sets disjuntos zero merge friction); Wave 2 destravada para 10-03. Self-check PASSED.

Posição anterior: Fase: 10 de 11 (Mensagens dos Agentes ao Usuário) — **IN PROGRESS** (1/3 plans complete)
Plano anterior: 10-01 — concluído. Próxima ação anterior: /executar-fase 10 → 10-02 (Summaries / RunTranscriptView / IssueRunLedger / LiveRunWidget — AGENT-MSG-02; file set disjunto de 10-01, pode rodar paralelo). 10-03 (Toasts/Notificações + LiveUpdatesProvider builders + activity log gaps — AGENT-MSG-04) é Wave 2 sequencial dependendo de namespace agents.
Status anterior: Plano 10-01 concluído em ~30min (3 commits atomic — `b73c9fa` bootstrap agents.json populado de {} para 147 leaf keys × 2 locales (100% paridade) + ui/src/lib/agent-keys.ts módulo (AGENT_STATUS_KEY 7 entries + AGENT_ROLE_KEY 11 entries — Pattern 1 + Pattern 2 do RESEARCH) + StatusBadge.label.test.tsx 2 cases (case (a) RED até Tarefa 1) + AgentDetail.i18n.test.tsx skeleton 6 cases probe-component pt-BR ↔ en-US; `602a2f1` StatusBadge refactor com optional `label` prop preservando legacy fallback + AgentActionButtons label/pauseLabel/resumeLabel REQUIRED (type-level i18n enforcement) + AgentDetail.tsx (header/tabs/breadcrumbs/actions/Live badge/more menu via t()) + AgentProperties.tsx + ActiveAgentsPanel.tsx (more-runs i18next plural _one/_other) + RoutineDetail.tsx Rule 3 fix label="Run now" + storybook fix + ActiveAgentsPanel.test.tsx selector switch from textContent regex to href; `068e48f` AgentConfigForm.tsx 41 strings (23 help + 18 labels) + agent-config-primitives.tsx help const REMOVED 32 entries + 6 adapter config-fields + runtime-json-fields migrados + NewAgentDialog 9 strings + Agents.tsx (filter tabs via AGENT_STATUS_KEY + breadcrumb + count plural + empty states) + NewAgent.tsx 14 strings + storybook STORY_HINTS const). Padrões novos: optional vs required label prop strategy (StatusBadge optional preserves 4+ legacy consumers; AgentActionButtons required enforces i18n at type level — opposite strategies justified by semantics); ENUM_KEY proliferation (Pattern 1+2 from Phase 8-03 STATUS_KEY now canonical across milestone v1.1: AGENT_STATUS_KEY + AGENT_ROLE_KEY); defensive lookup `?? "agents:status.idle"` for runtime-server divergence; agent-config-primitives `help` const removal cascaded to 9 consumers (only 1 in plan file list — Rule 3 scope expansion). Desvios: 4 auto-fixes Rule 3 (RoutineDetail+storybook AgentActionButtons callsites broke após required props; ActiveAgentsPanel.test.tsx textContent selector matched English no longer present; help const removal cascaded to 6 adapter files + storybook; missing-keys detector flagged bare more-runs key alongside _one/_other variants). Full UI suite **693/693 GREEN** (was 685, +8 net = 2 StatusBadge.label + 6 AgentDetail.i18n; 0 regressions); CI=true missing-keys GREEN; UI typecheck preserva apenas pre-existing ActivityRow:42. AGENT-MSG-01 + AGENT-MSG-03 satisfeitos at code-level. Self-check PASSED.

Posição anterior: Fase: 9 de 11 (Tradução UI Admin + Auth + Sistêmicas) — **COMPLETE-WITH-PENDING-UAT** (5/5 plans complete). Plano: 09-04 — concluído. Próxima ação: /verificar-fase 9 (PHASE-CHECKER validação) OU /planejar-fase 10 (Mensagens dos Agentes ao Usuário — AGENT-MSG-01..04 cobrindo activity log templates, agent status messages, summary copy, prompts UI shown to user). UI-04/06/07/08 satisfeitos at code-level; HUMAN-UAT artifact com 4 procedimentos (UAT-09-01..04) routed para operator validation post-merge.
Status anterior: Plano 09-04 concluído em ~15min (4 commits atomic — `b9a6e4a` common.json estendido 114→144 leaf keys com 3 sub-trees toast/confirm/empty-state preservando Phase 7-8 byte-by-byte; `e2b8f1f` 2 residual window.confirm() migrados em AgentDetail.RunDetail + ApprovalDetail (Rule 3 blocker auto-fix); `65c7e0c` 2 anti-regression tests (confirm-strings.lint via paren-balanced parser + Toast.translation RTL com plural across locale toggle, 6 cases GREEN); `08e24fa` Toast test typecheck cast fix). HUMAN-UAT artifact com 4 procedimentos persistido (status pending). Padrões novos: paren-balanced parser char-walking para structural lint tests (reusable para useTranslation/t() futuras); 2nd application bare-key + suffix plural duplication (confirm.clear-sessions-for-touched-issues.body bare + body_one + body_other). Pre-existing migration coverage validated: 100% in-scope files já t()-driven via namespace-specific keys (Phase 9-01/02). Out-of-scope residuals (~12 pages tooltips/IssueDetail toasts) documentados como Phase 10/v2. Full UI suite 685/685 GREEN (was 680, +5 net); CI=true missing-keys GREEN; UI typecheck preserva apenas pre-existing ActivityRow:42. UI-08 satisfied; Phase 9 closes complete-with-pending-UAT (precedente Phases 3-7). Self-check PASSED.

Posição anterior: Plano 09-03a concluído em ~13min (3 commits atomic — `e1919dc` HttpError ganha `code?: string` field + 6 helpers `*WithCode()` paralelos preservando 130+ legacy callsites byte-identical + 16 unit tests; `571fdac` errorHandler middleware emite `{error, code?, details?}` quando code presente + ZodError emite `code: "validation.error"` + per-issue `code: "validation.${issue.code}"` + 5 supertest integration tests; `fef1d67` 88 priority callsites migrados em 7 routes — companies/access/issues/agents/projects/routines/plugins). 32 códigos canônicos kebab-case + dot-segmented populados (e.g. company.not-found, invite.expired, invite.already-used, member.last-owner, board-claim.code-required, claim-secret.expired, agent.shortname-ambiguous, validation.* per-issue). Padrão novo: parallel helpers — *WithCode helper signature `(message, code, details?) => HttpError`, code mandatório second positional; legacy 6 helpers byte-identical (zero regressão em 130+ callsites legados). Padrão de namespace `domain.subject[.constraint]` espelha layout JSON do client (`errors:invite.expired` → `errors.json.invite.expired`). Auth-gate forbidden() throws (Agent key cannot access another company, Permission denied, Missing permission: X) deixados como legacy — surgem só em cross-tenant attack/role bug, cliente cai em fallback `errors:auth.forbidden` (decisão CONTEXT). ZodError per-issue path dot-joined (`address.city`); code prefixado com `validation.` (e.g. validation.invalid_string, validation.too_small). Sem desvios — plano executou exatamente como escrito; Open Question #1 resolvida como (b) novos helpers paralelos (Pitfall 1 evitado). Pre-existing TS error em services/recovery/service.ts:459 confirmado em master (out-of-scope per Boundary). Cold-start vitest 5000ms timeout em 2 testes (cli-auth-routes, agent-cross-tenant-authz-routes) na primeira run — re-execução com --testTimeout=60000 GREEN, padrão Phase 8 conhecido. 21/21 unit+integration tests GREEN; route-touching tests (companies-route-path-guard, access-service, agent-cross-tenant-authz-routes, cli-auth-routes) GREEN. UI-07 (parte server) satisfeito; cliente Plan 09-03b destravado para mapping `error.code → t("errors:${code}")`. Self-check PASSED.

Posição anterior: Fase: 8 de 11 (Tradução UI Core) — **COMPLETE** (5/5 plans complete)
Plano: 08-05 — concluído. Próxima ação: /planejar-fase 9 (UI Admin + Auth + Sistêmicas — UI-04, UI-06, UI-07, UI-08).
Status: Plano 08-05 concluído em ~30min (3 commits atomic — `31365d3` schema migration 0074 + ActivityEvent type + LogActivityInput extension + `e283f75` 49 callsite backfill + 2 dictionaries em kebab-case + ActivityRow t() refactor com fallback (legado) + `cd935fc` 2 testes (server integration 3/3 + UI RTL 4/4)). Migration 0074_add_activity_action_key.sql gerada via drizzle-kit (clean: 2 ALTER ADD COLUMN, zero RENAME); NÃO aplicada localmente (DB-03 — apply é CI-only via `.github/workflows/db-migrate.yml`). Schema Drizzle estendido com `actionKey: text("action_key")` + `paramsJson: jsonb("params_json")` nullable; `ActivityEvent` em `@paperclipai/shared` recebe campos opcionais; `LogActivityInput` em `server/src/services/activity-log.ts` aceita ambos; `db.insert(activityLog).values()` inclui ambos com null fallback; `publishLiveEvent` payload broadcast inclui ambos. 49 callsites server emitem actionKey kebab-case + paramsJson distribuídos: 19 em `routes/issues.ts`, 11 em `routes/agents.ts`, 3 em `routes/projects.ts`, 3 em `routes/goals.ts`, 3 em `routes/companies.ts`, 3 em `routes/approvals.ts`, 3 em `routes/costs.ts`, 1 em `services/claude-accounts.ts` (claude-account-rotated kebab top-level com `paramsJson: { reason }`), 2 em `services/recovery/service.ts`, 1 em `services/heartbeat.ts`. Acima dos 36 priority actions mínimos. ActivityRow.tsx refatorado: `useTranslation(["activity"])` hook + `event.actionKey ? t(\`activity:${event.actionKey}\`, paramsJson) : formatActivityVerb(...)` com fallback ALSO triggered se translation retorna string vazia (graceful para keys ainda não dicionarizadas); render `<span className="ml-1 text-xs italic text-muted-foreground">(legado)</span>` quando isLegacy. Padrão novo: dynamic actionKey i18n (server emits kebab-case key + paramsJson, client renders via `t()`); zero data migration backwards-compat (runtime resolution em ActivityRow com (legado) marker explícito); underscore→kebab transform mapping (`issue.checked_out` → `issue.checked-out`, `claude_account_rotated` → `claude-account-rotated` actionKey enquanto mantém legacy action constant intocada para route filter `routes/claude-accounts.ts:111` + test fixtures). Decisão: paramsJson armazena APENAS subset de details usado pelo template i18n (ex: `{ title: issue.title }`, `{ reason }`); details retém payload completo. ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED constante (underscore) preservada byte-per-byte para retrocompat com `services/claude-accounts.test.ts:7,422,450` e `services/claude-accounts-swap.md`. Desvios: **Regra 3** (vitest cold-start setup excedia 5000ms default — passou `--testTimeout=60000` na invocação; teste real 153ms); **Regra 1** (RTL DOM leak entre tests com I18nextProvider language change — adicionou `afterEach(() => cleanup())` ao describe block); **Regra 1** (regex `/created/i` overmatch — mudou para `getAllByText().length > 0`). Pre-step coverage check (Sub-passo 2.0) confirmou que todos os 36 priority callsites vivem nos 11 arquivos declarados em files_modified — zero surprise files. Server integration test 3/3 GREEN (~13ms); UI RTL test 4/4 GREEN (102ms); CI=true missing-keys vitest GREEN (1/1, 717ms). pnpm -r typecheck exit 0 (verified twice). UI-09 satisfied. Self-check PASSED. Phase 8 fecha COMPLETE — todas as superfícies UI core (Inbox/Projects/Settings/Navigation/Activity) totalmente traduzidas pt-BR/en-US.

Posição anterior: Fase: 8 de 11 (Tradução UI Core) — **IN PROGRESS** (4/5 plans complete)
Plano: 08-04 — concluído. Próxima ação: /executar-fase 08 → 08-05 (Activity log — última do Phase 8; envolve migration 0074 + actionKey/paramsJson schema + ActivityRow refactor com fallback para entries pré-Fase-8).
Status: Plano 08-04 concluído em ~10min (3 commits atomic — `36c8062` common.json estendido com nav.* sub-tree (14 sub-trees, ~80 leaf keys; preservou Phase 7 + 08-01 actions.*) + `a2b7033` 11-file navigation surface migration to t() (Sidebar/InstanceSidebar/SidebarAccountMenu/SidebarCompanyMenu/SidebarAgents/SidebarProjects/BreadcrumbBar/MobileBottomNav/Layout/CompanyRail/CompanySwitcher) + `c4cf623` Sidebar.translation.test.tsx RTL probe + 3 Rule 1 fixture fixes atomic). Navigation surface (1565+ LOC across 11 files) totalmente traduzido pt-BR/en-US. Padrão novo: brand-preserving interpolation 'Paperclip v{{version}}' — brand stays literal inside i18next template, só {{version}} interpola; RTL probe asserta identical output 'Paperclip v1.2.3' across both locales. Pitfall 2 enforced architecturally — BreadcrumbBar.tsx tem 1 t() call (aria-label apenas); crumb labels owned by pages via setBreadcrumbs (08-01/08-02/08-03 já honram; pages fora do escopo Phase 8 mantêm hardcoded até Phase 9). Inner-component useTranslation pattern aplicado em SortableProjectItem (3rd application after 8-02 OverviewContent/ColorPicker e 8-03 StatusBadge/ClaudeAccountRow). MobileBottomNav items array via t() inside useMemo com [t] dep — canonical react-i18next pattern. Desvios: 3 auto-fixes Rule 1 (Sidebar.test.tsx + SidebarAccountMenu.test.tsx + SidebarCompanyMenu.test.tsx wrapped em I18nextProvider + beforeAll/afterAll en-US locale pin — 4ª aplicação do fixture-fix protocol; landed atomic com Task 3 ao invés de Task 2 porque fixture breaks só foram descobertos quando Task 3 rodou full suite). Full UI suite **645/645 GREEN** (was 643, +2 novos probe tests); CI=true missing-keys vitest GREEN; pnpm --filter @paperclipai/ui typecheck exit 0. UI-05 satisfied. Self-check PASSED.
Última atividade: 2026-04-26 — Plano 08-04 concluído em ~10min. Próximas ações desbloqueadas: 08-05 (Activity log — última do Phase 8). Brand-preserving interpolation pattern documentado em SUMMARY 08-04 para reuso em Phase 9/10/11 (qualquer mensagem agent/system prompt referenciando 'Paperclip' como product noun). Pitfall 2 architectural seam reforçado — Phase 9 plans são responsáveis por traduzir suas próprias setBreadcrumbs callsites. Layout/BreadcrumbBar/MobileBottomNav/CompanyRail já migrados — Phase 9 admin/auth pages plug into translated chrome com zero extra work.

Posição anterior: Fase 8 de 11 (Tradução UI Core) — **IN PROGRESS** (3/5 plans complete)
Plano anterior: 08-03 — concluído. Próxima ação: /executar-fase 08 → 08-04 (Navegação) ou /executar-fase 08 → 08-05 (Activity log). Plans 04 + 05 podem rodar em paralelo (arquivos disjuntos: nav components em ui/src/components/Sidebar* + BreadcrumbBar + Layout + MobileBottomNav vs activity em packages/db schema + server services + ui/src/components/Activity*).
Status anterior: Plano 08-03 concluído em ~19min (3 commits atomic — `6bb9b52` settings.json estendido 5→192 leaf keys preservando language.* da Phase 7 + `3eb597c` 5-file Settings surface migration to t() with Rule 1 ClaudeAccounts.test.tsx fixture-fix + `b16e5bc` Settings.translation.test.tsx RTL probe). Settings surface (InstanceSettings 283 LOC + InstanceGeneralSettings 382 LOC + InstanceExperimentalSettings 157 LOC + ProfileSettings extension + ClaudeAccounts 480 LOC) totalmente traduzido pt-BR/en-US. Padrões novos estabelecidos: bare-key + _one/_other plural duplication (i18next built-in plurals usam underscore suffix, mas missing-keys regex aceita só kebab-case + dot-notation; bare key satisfaz detector, suffixed variants drive runtime plural resolution — pattern aplicado em heartbeats.company, .actions.confirm-disable-all, .actions.disable-failed, general.backup-retention.days/weeks/months). STATUS_KEY static lookup map (Record<ClaudeAccountStatus, LiteralKeyUnion>) herdou PROJECT_STATUS_KEY pattern de 8-02 — agora canônico across milestone v1.1 para qualquer enum → kebab-case dynamic key com typed-t() augmentation. Window.confirm() traduzido via t('settings:heartbeats.actions.confirm-disable-all', { count: enabledCount }) com runtime plural resolution. ai-feedback retest-hint split em 5 segmentos prose (prefix/from/or-set/suffix/end) ao redor de 3 <code> tags técnicos para preservar literais técnicos enquanto traduz prose envolvente. Phase 7 ProfileSettings language section preservada byte-per-byte (linhas 305-332 untouched — UI-03 truth #2 regression-free; verified ProfileSettings.locale-toggle.test.tsx 2/2 GREEN post-migration). Inner components consomem useTranslation diretamente (StatusBadge, ClaudeAccountRow) em vez de receber t() via prop. Desvios: 2 auto-fixes (Rule 3 — detector regex blocker resolved com bare-key duplication para 6 chaves pluralizadas; Rule 1 — ClaudeAccounts.test.tsx 8 renders wrapped em I18nextProvider + beforeAll en-US, mirrora Phase 8-01/8-02 fixture-fix pattern, atomic com migration commit). Full UI suite **643/643 GREEN** (was 641, +2 novos probe tests); CI=true missing-keys vitest GREEN; pnpm --filter @paperclipai/ui typecheck exit 0. Plans 08-01/08-02 integrations intact (suite-wide GREEN). UI-03 satisfied. Self-check PASSED.
Última atividade: 2026-04-26 — Plano 08-03 concluído em ~19min. Próximas ações desbloqueadas: 08-04 (Navegação — common.nav.* sub-tree + Sidebar/SidebarAccountMenu/BreadcrumbBar/MobileBottomNav/Layout, 11 arquivos TSX); 08-05 (Activity log — migration 0074 + actionKey/paramsJson schema + ActivityRow refactor com fallback para entries pré-Fase-8). Plans 04 + 05 podem rodar em paralelo (arquivos disjuntos: nav components em ui/src/components/* vs activity em packages/db schema + server services + ui/src/components/Activity*). Bare-key + _one/_other plural duplication documentado em SUMMARY 08-03 para reuso em Plans 04/05 quando hit count-driven labels (sidebar badges, activity templates). STATUS_KEY pattern (canônico agora em milestone v1.1) reusable se Plan 04 nav-items ou Plan 05 activity actions tiverem typed-enum → kebab-case key bridge.

Posição anterior: Fase 7 de 11 (Foundation i18n + Toggle de Settings) — **COMPLETE-WITH-PENDING-UAT** (5/5 plans complete)
Plano: 07-05 — concluído.
Status anterior: Phase 7 fecha como `complete-with-pending-UAT` (precedente Phases 3-6). UI toggle end-to-end pronto: I18nextProvider em main.tsx (Pitfall 5 — entre QueryClientProvider e ThemeProvider), ProfileSettings com Language section + 2 radios (pt-BR/en-US) + optimistic locale mutation com Pitfall 3 ordering (`await i18n.changeLanguage(newLocale)` ANTES de `queryClient.setQueryData`) + session-driven hydration (useEffect que chama changeLanguage quando `i18n.language !== userLocale`). Wave 0 UI test `ProfileSettings.locale-toggle.test.tsx` 2/2 GREEN (SETTINGS-01 + SETTINGS-04 fechados em código); full UI suite **637/637 GREEN**; `pnpm -r typecheck` exit 0; `pnpm --filter @paperclipai/ui build` exit 0. UAT-07-01 (hot-swap + persistência cross-reload + cross-logout/login) + UAT-07-02 (default pt-BR para new user) deferidos como pending HUMAN-UAT — artifact `.planning/phases/07-foundation-i18n-toggle-de-settings/07-HUMAN-UAT.md` persistido com frontmatter `status: pending` e procedimentos completos. SETTINGS-01 + SETTINGS-04 marcados Complete em REQUIREMENTS.md (UAT pending non-blocking).
Última atividade: 2026-04-26 — Plano 07-05 concluído em ~18min (commits `151d5ba` Task 1 wire I18nextProvider + testing-library devDeps, `84d909c` Task 2 ProfileSettings Language section + optimistic toggle + hydration, `6a8a16b` Task 3 07-HUMAN-UAT.md artifact). 3 desvios Regra 3 (bloqueadores): (1) `@testing-library/react`+`@testing-library/jest-dom`+`@testing-library/user-event` ausentes em `ui/package.json` — adicionados como devDependencies (Plan 01 escreveu o teste, Plan 03 instalou só i18next/react-i18next; testing-library nunca foi instalado); (2) `Locale` type não re-exportado de `@paperclipai/shared` barrel — adicionado `export type { Locale } from "./validators/access"` ao barrel para que `import type { Locale } from "@paperclipai/shared"` resolvesse; (3) Wave 0 test do Plan 01 usava `render(<ProfileSettings/>)` bare sem QueryClientProvider/I18nextProvider wrappers — helper `renderProfileSettings()` adicionado wrappando em ambos providers (mesmo contrato de assertion). Todos os 3 desvios são pré-condições ausentes para o plano executar como escrito; nenhuma mudança de comportamento ou escopo. Decisão de fechamento: HUMAN-UAT routing seguindo precedente Phases 3-6 (Phase 3 TEAM-04, Phase 4 SPIKE-04/05, Phase 5 UAT-05-01, Phase 6 UAT-06-01) — quando validação requer browser real + julgamento perceptual humano (hot-swap visível ao olho, sem flicker de chaves cruas, persistência sentida cross-session), executor persiste o artifact com status pending e roteia phase para `complete-with-pending-UAT`. Self-check PASSED. Phase 8 destravada — todas as preconditions atendidas (I18nextProvider mounted globally, 8 namespaces ready, missing-keys detector wired CI mode, session.user.locale round-trip end-to-end).

Posição anterior: Fase 7 de 11 (Foundation i18n + Toggle de Settings) — **IN PROGRESS** (4/5 plans complete)
Plano: 07-04 → 07-05 (próximo — UI ProfileSettings toggle)
Status anterior: Server-side locale plumbing fully wired: DB → middleware → route → response. Wave 0 server tests (`auth-routes-locale.test.ts` SETTINGS-02+03, `middleware-locale.test.ts` I18N-05) GREEN. Authenticated requests carry `req.locale` from `authUsers.locale`; anonymous requests fall back through `parseAcceptLanguage` to `pt-BR` default. Plan 05 (UI toggle in ProfileSettings) is the only remaining work to close Phase 7. All preconditions for 07-05 met: `UpdateCurrentUserProfile` includes `locale?:`, PATCH endpoint round-trips locale, `currentUserProfileSchema.locale` required, i18next singleton (Plan 03) ready for `i18n.changeLanguage()`.
Última atividade anterior: 2026-04-26 — Plano 07-04 concluído em ~36min (commits `0ae90f8` Task 1 helper+type-augmentation, `fdf6324` Task 2 actorMiddleware 3-tier fallback). Task 3 pre-completed by 07-02 as Rule 3 blocker fix (`7706274`); verified intact, no new commit needed. Tarefas: (1) `server/src/lib/parse-accept-language.ts` (21 lines, RFC-light pt/en startsWith parser, RequestLike interface for testability, defers full RFC 7231 to v2 L10N-01); `server/src/types/express.d.ts` augments Express.Request.locale: 'pt-BR'|'en-US' non-optional → TS enforces middleware coverage. (2) `server/src/middleware/auth.ts` extends Promise.all in authenticated branch with 3rd select reading authUsers.locale; resolution `userLocale ?? parseAcceptLanguage(req) ?? "pt-BR"`; all 8 non-authenticated code paths (no header, board_key, agent_jwt × 3 variants, agent_key × 2 variants) set `req.locale = parseAcceptLanguage(req) ?? "pt-BR"`. Desvios: **Regra 1** `auth-routes.test.ts` persistedProfileFixture missing locale → fix add `locale: "pt-BR"` (07-02 made it required in `currentUserProfileSchema`). **Regra 1** `auth-session-route.test.ts` db mock chained 2× mockImplementationOnce but middleware now does 3 selects → fix add 3rd `.mockImplementationOnce(createSelectChain([{ locale: "pt-BR" }]))` for userLocaleRow. Both fixture fixes folded into Task 2 commit (atomic with middleware change). Server typecheck exit 0; Wave 0 middleware-locale 3/3 GREEN (verified RED→GREEN via stash + re-run); auth-routes-locale 3/3 GREEN; focused regression suite (auth-routes 5/5, auth-session-route 1/1, authz-company-access 9/9) all GREEN. Pre-existing 70 server failures (18 files: adapter shebang Windows + workspace-runtime taskkill ENOENT) confirmed pre-existing per State/06-03 deferred-items.md, out-of-scope. Self-check PASSED. Próxima ação: /executar-fase 07 → 07-05 ou /planejar-fase 8.

Posição anterior: Fase 7 de 11 (Foundation i18n + Toggle de Settings) — **IN PROGRESS** (3/5 plans complete)
Plano: 07-03 → 07-04 (próximo)
Status: Wave 1 paralelo encerrado — 07-02 (schema/types/validators) + 07-03 (i18next bootstrap) ambos commitados em paths disjuntos. Plan 04 destravado.
Última atividade anterior: 2026-04-26 — Plano 07-01 concluído em ~4min (commits `6215174` 16 JSON dicts + `dada008` 3 UI failing tests + `0f5179a` 2 server failing tests). 3 tarefas atômicas: (1) 16 JSON dictionary scaffolds em ui/src/i18n/locales/{pt-BR,en-US}/{common,inbox,projects,settings,auth,agents,errors,activity}.json — common.json populado com app-name/loading/save/cancel; settings.json populado com language.{title,description,pt-br,en-us}; outros 6 namespaces como {} placeholders para Phases 8-11; verificação inline `node -e ...JSON.parse...` passou "OK 16 valid JSON"; (2) 3 UI failing tests — init.test.ts cobre I18N-01 (8 namespaces) + I18N-02 (16 dict files exist) + I18N-03 (en-US fallback); missing-keys.test.ts cobre I18N-04 (regex extrai t() calls; CI=true falha vs warn em dev per CONTEXT); ProfileSettings.locale-toggle.test.tsx cobre SETTINGS-01 (radio render) + SETTINGS-04 (i18n.changeLanguage + PATCH locale) com `// @vitest-environment jsdom` annotation para evitar mudar config global; (3) 2 server failing tests — auth-routes-locale.test.ts cobre SETTINGS-02 (PATCH persistence) + SETTINGS-03 (default pt-BR) + enum reject 400 espelhando fixture stubbed-drizzle de auth-routes.test.ts; middleware-locale.test.ts cobre I18N-05 (req.locale de authUsers.locale + Accept-Language fallback + final fallback pt-BR) usando assinatura real `actorMiddleware(db, opts)`. Todos os 5 arquivos confirmados RED via `npx vitest run` — módulos `../index`/`../resources`/`@/i18n` ainda não existem (Plan 03), schema/route não aceitam locale (Plans 02+04), middleware não popula req.locale (Plan 04). Sem desvios — plano executou exatamente como escrito, com adaptações documentadas em SUMMARY (signature-mirror para actorMiddleware, supertest fixture-mirror para auth-routes — ambas explicitamente instruídas pelo plano). Self-check PASSED. Próxima ação: /executar-fase ou wave-parallel-launch para Plans 02+03.

Posição anterior: Fase 7 de 11 (Foundation i18n + Toggle de Settings) — **PENDING**
Plano: — (aguardando /planejar-fase 7)
Status: Roadmap v1.1 criado (5 fases 7-11, 26/26 requisitos mapeados, zero órfãos). Fase 7 destravada como entry point — schema de preferência de idioma, toggle UI em instance/settings, biblioteca i18n com namespaces, dicionários pt-BR/en-US, fallback en-US e detector de chaves ausentes. Sem essa fundação as fases seguintes (UI tradução, mensagens de agente, system prompts) não têm onde renderizar nem por onde propagar locale.
Última atividade anterior: 2026-04-26 — Roadmap v1.1 escrito por /roadmap. Continuação de numeração (v1.0 fechou em Fase 6 → v1.1 começa em Fase 7). 5 fases derivadas dos 26 requisitos das 5 categorias SETTINGS/I18N/UI/AGENT-MSG/AGENT-SKILL: Fase 7 Foundation (9 reqs SETTINGS-01..04 + I18N-01..05), Fase 8 UI Core (5 reqs UI-01/02/03/05/09), Fase 9 UI Admin+Auth+Sistêmicas (4 reqs UI-04/06/07/08), Fase 10 Mensagens dos Agentes (4 reqs AGENT-MSG-01..04), Fase 11 Skills+System Prompts (4 reqs AGENT-SKILL-01..04). Ordem ditada por dependência técnica: infra i18n + persistence antes de tradução UI; UI antes de mensagens de agente; system prompts/skills por último (precisam da preference já propagada user → agent context → spawn). Próxima ação: /planejar-fase 7.

Fase anterior: 6 de 6 (Multi-Projeto + Polish — milestone v1.0 final) — **COMPLETE**
Plano: 6/6 — todas waves Phase 6 concluídas. Plano 06-06 (V1-READINESS.md + REQUIREMENTS.md closure) entregue.
Status: Milestone v1.0 declared ready com 45/45 requisitos Complete + 7 UATs registradas em `*-HUMAN-UAT.md` arquivos como itens de validação humana contínua não-bloqueantes (precedente Phase 3/4/5). Convergência via uso real conduzirá próximas iterações; v2 backlog priorizado por sinal real (5 categorias × 13 itens em REQUIREMENTS.md §"Requisitos v2").
Última atividade: 2026-04-26 — Plano 06-06 concluído em ~8min (commits `0db9c87` V1-READINESS.md + `179b45c` REQUIREMENTS.md closure). 2 artefatos entregues: (1) `.planning/phases/06-multi-projeto-polish/V1-READINESS.md` (275 linhas pt-br, 8 sections H2 numeradas seguindo D-16): §1 Declaração formal v1.0 PRONTO citando valor central PROJECT.md; §2 Inventário de Requisitos v1 (45 total — não 47 conforme CONTEXT/PLAN, recontagem real verificada D-COUNT) em 8 sub-tabelas (FORK 5 + INFRA 6 + DB 5 + AUTH 5 + TEAM 5 + SPIKE 5 + MULTI 11 + PROJ 3) com colunas ID/Phase/Status/Validation/Notes; §3 UATs Pendentes (7 ao total: UAT-03-01/02 + UAT-04-01/02/03 + UAT-05-01 + UAT-06-01) com tabela inventário + justificativa não-bloqueante (recursos físicos fora controle executor, validação SQL/CI cobre invariantes, convergência empírica via uso real, precedente Phase 3/4/5/6) + mecânica de fechamento; §4 Fluxos Críticos Validados (4 procedimentos: multi-dev shared state via 02-06+03-04, schema migrations via 02-04+CI, multi-account swap via 05-08 SMOKE-E2E.md Modo A SQL force, multi-company isolation via 06-05 MULTI-COMPANY-SMOKE.md Modo A); §5 Documentação Operacional (8 docs canônicos verificados); §6 Próximos Passos v2 Backlog (5 categorias AUTH2/OBS/POOL/STOR/RLS, 13 itens, decisão D-V2-NO-ROADMAP); §7 Métricas Finais v1.0 + 5 lições aprendidas (audit antes de implementar destrava waves, spike isolado evita retrabalho, HUMAN-UAT routing > falsificar validação, schema multi-tenant desde dia 1 simplifica fases, defaults legacy permitem opt-in seguro); §8 Assinatura. (2) `.planning/REQUIREMENTS.md` 5 mudanças fechando rastreabilidade: linha 69 checklist MULTI-09 [x], linha 169 tabela MULTI-09 Phase 5 Complete (entregue Phase 5 plan 05-07; STATE.md confirma), linha 177 cobertura "Requisitos v1: 44 total" → "45 total" (recontagem real, era 44 pré-Phase 6, +1 do PROJ; valor 47 do CONTEXT estava errado), linha 178 "Mapeados para fases: 44" → "45", linha 183 footer "2026-04-25 após criação do roadmap" → "2026-04-26 após v1.0 readiness (Phase 6 closure)". PROJ-01..03 já estavam [x] e Complete em planos anteriores (06-01..06-05) via tool `requirements mark-complete` — Tarefa 2 verificou estado e atualizou apenas residuos. Decisões: D-COUNT (recontagem 47 → 45), D-D17 (multi-company refs em README/ONBOARDING/TROUBLESHOOTING NÃO bloqueia readiness — config avançada, não entry-level), D-V2-NO-ROADMAP (backlog v2 priorizado por sinal real, criar Phase 7 quando 2-3 itens acumularem demanda). Self-check PASSED: V1-READINESS.md existe (275 linhas), commits ambos no log, REQUIREMENTS.md grep "Pending" = 0, "Requisitos v1: 45 total" = 1. Sem desvios estruturais — plano executou exatamente como escrito (D-COUNT é correção de discrepância CONTEXT/realidade flagada pelo orquestrador antes da execução, não desvio em runtime). Phase 6 fecha como `complete-with-pending-UAT`; milestone v1.0 declared ready.

Última atividade anterior: 2026-04-26 — Plano 06-05 concluído em ~4min (commits `10f99b5` MULTI-COMPANY-SMOKE.md + `9a55340` 06-HUMAN-UAT.md). 2 documentos pt-br entregues: (1) `MULTI-COMPANY-SMOKE.md` (284 linhas, 7 H2 sections) com Modo A canonical SQL procedure — pré-requisitos (8 itens), Setup com placeholders `{{COMPANY_A_ID}}`/`{{OWNER_A_ID}}`/`{{AGENT_A_ID}}`, queries Q1-Q4 (3-table JOINs validando cross-tenant attribution + activity_log isolation + cost-summary endpoint via curl autenticado), bonus PROJ-02 cross-validation (UPDATE poolMode='shared' + INSERT scope='shared' + Q1-bonus visibility + Q2-bonus invisibility de B em per_company), pass/fail table (7 critérios), cleanup com FK reverse-order DELETE, limitations section roteando 5 itens UI para HUMAN-UAT. 13 SELECTs + 3 INSERTs + 2 UPDATEs + 3 DELETEs + 1 curl. (2) `06-HUMAN-UAT.md` (145 linhas) frontmatter `type: human-uat status: pending requirements:[PROJ-01,PROJ-02,PROJ-03]` consistente com 05-HUMAN-UAT.md/04-HUMAN-UAT.md; UAT-06-01 detalhado: pré-condições table (8 itens), 13 steps numerados (login A/B browsers → register A1/B1 → toggle A para shared → register C-shared → spawn agentes paralelos → verify cost summary isolation → verify rotation history isolation → optional revert), 7 dimensões pass/fail tabuladas (cross-tab A→B/B→A, pool persistence, scope radio works, shared visibility positive/negative, cost summary isolation), failure paths com 5 cenários e troubleshooting concreto (multi-tenant-leak issue label, cookie prefix divergente → TROUBLESHOOTING.md, cost summary zero → 05-06 wiring, pool toggle não persiste → 06-04 D-04 mapping, scope default → 06-04 D-05). Decisão de routing D-15: PROJ-01 dual mode — Modo A executável agora pelo executor Claude/CI (substitui rodada UI), Modo B HUMAN-UAT pelo operador com 2 browsers/profiles. Bonus pool=shared embutido em Modo A para evitar terceiro doc cobrindo PROJ-02. Phase 6 pronta para fechar como `complete-with-pending-UAT` (precedente Phase 3/4/5). Sem desvios materiais — plano executou exatamente como escrito.

Última atividade anterior: 2026-04-26 — Plano 06-03 concluído em ~12min (commits `ffe2183` test RED + `3b1e51e` feat GREEN). TDD ciclo: PM-1 inicialmente falha porque impl Phase 5 não consultava companies; após adicionar query de poolMode + scope filter condicional, 27/27 tests passam. server/src/services/claude-accounts.ts ganha import de `companies` do barrel + Step 2a `select({ poolMode: companies.claudeAccountPoolMode }).from(companies).where(eq(companies.id, input.companyId)).limit(1)` dentro do withAgentLock + narrowing defensivo `rawMode === "shared" ? "shared" : "per_company"` (fail-closed para values desconhecidos) + Step 3 scopeFilter condicional ao poolMode (per_company: `and(companyId=?, scope='company')`; shared: `or(and(companyId=?, scope='company'), scope='shared')`). ClaudeAccount interface ganha `scope: "company" | "shared"` required; rowToAccount mapeia `row.scope ?? "company"` para D-08 backwards-compat. server/src/services/claude-accounts.test.ts ganha 6 vitest cases PM-1..PM-6 (pool-mode-aware describe block) cobrindo: PM-1 per_company ignora foreign-shared (asserta `tableSelects` contém "companies"), PM-2 shared inclui foreign-shared via round-robin, PM-3 shared pega shared único quando company sem own, PM-4 per_company throws com 0 own, PM-5 cross-tenant isolation Company B nunca vê Company A, PM-6 unknown poolMode → fail-closed per_company. Mock Db extended com selectCompaniesQueue + tableSelects assertion surface. 21 tests Phase 5 zero regressão. Desvios: **Regra 1** claude-accounts-swap.test.ts fixture accountB faltando scope após interface change → fix `scope: "company" as const,` inline. Pre-existing 63 test failures (14 arquivos) em adapter `*-execute.test.ts` por shebang scripts não resolvendo no Windows spawn — reproduzível em master, fora de escopo, documentado em `.planning/phases/06-multi-projeto-polish/deferred-items.md` para `/limpeza` ou v2. server pnpm tsc --noEmit exit 0. Sibling 06-04 rodou em paralelo sem conflito (arquivos disjuntos: services/claude-accounts.ts vs routes/claude-accounts.ts + ui/*). PROJ-02 D-06 algoritmo wired ao runtime; defaults preservam Phase 5 byte-por-byte.

Última atividade anterior: 2026-04-26 — Plano 06-04 concluído em ~12min (commits `860be18` API contracts + `fcba897` UI). 2 tarefas atômicas: (1) shared validator + Company type + server route ganham campos novos — `updateCompanySchema.claudeAccountPoolMode` opcional enum; `Company.claudeAccountPoolMode` agora obrigatório; `createAccountSchema.scope` opcional com `.default("company")` preservando back-compat; `patchAccountSchema.scope` opcional para promoção mid-life (D-05); `companies` service selection inclui `claudeAccountPoolMode` para round-trip GET. (2) UI completa: `CompanySettings.tsx` ganha section "Claude Account Pool" com 2 radios per_company/shared (testids `pool-mode-per-company`/`pool-mode-shared`) + state hidratado de selectedCompany + dirty check + payload save; `ClaudeAccounts.tsx` register form ganha radio scope (testids `scope-company`/`scope-shared`) + Accounts table ganha coluna Scope com badge `shared` ou texto muted `company` (data-scope attrs); nova section Cost summary entre Accounts e Rotation history consumindo claudeAccountsApi.costSummary com 3 estados (loading / `costs-empty` / `costs-table`); 5 colunas (Account, Cost USD 4 decimals, Input/Output tokens via toLocaleString, Steps); `claudeAccountsApi.costSummary(companyId, range?)` helper com query string serialization opcional. 4 testes vitest novos (9 total passing): cost table render, cost empty state, register com scope=shared, shared badge na listagem. Desvios: **Regra 3** storybook fixtures (3 companies em paperclipData.ts) faltavam claudeAccountPoolMode após o tipo virar obrigatório — fix replace_all com `claudeAccountPoolMode: "per_company"`. **Regra 1** assertion `expect(textContent).toContain("12,345")` falhou em jsdom (ICU default rendeu "12.345" estilo alemão) — fix usar `(12345).toLocaleString()` para staying locale-agnostic. **Regra 2** PATCH scope adicionado por simetria com create (D-05 mid-life mudança). UI tsc --noEmit exit 0; 9/9 vitest tests pass; pre-existing TS error em claude-accounts.test.ts é do sibling 06-03 paralelo (out of scope). Plano executou em paralelo com 06-03 sem conflitos (arquivos disjuntos: shared/types vs services; routes vs services; ui/* vs services). PROJ-02 satisfied (já marcado), PROJ-03 satisfied (cost summary UI fechado).

Última atividade anterior: 2026-04-26 — Plano 06-01 concluído em ~7min (commits `7e38f8d` companies.claudeAccountPoolMode, `6da289a` claude_accounts.scope, `5969f67` migration 0072_clumsy_leader): 2 colunas de schema adicionadas com defaults que preservam Phase 5 byte-for-byte (per_company / company); migration drizzle-kit gerou clean (zero phantom DDL — reversão da debt do 0071), 2 ALTER TABLE statements limpos; NOT aplicada localmente (DB-03 CI-only). PROJ-02 schema foundation pronta; Wave 2 (06-03 selectActiveAccount + 06-04 UI) destravada. Sibling 06-02 rodando em paralelo (arquivos disjuntos: schemas vs services/cost). Desvio único: Regra 3 env loading inline para drizzle-kit (`export $(grep ... .env.local | xargs) && pnpm db:generate`).

Última atividade anterior: 2026-04-26 — Plano 05-04 concluído em ~25min (commits `2f7de6f` RED test errors, `27f3ddc` GREEN errors, `6255a21` service+tests) (commits `3d9527f` HUMAN-UAT, `f4fe4c1` FINDINGS): server/src/errors.ts ganha 2 classes (NoAccountsAvailableError com props companyId/agentId, CredentialDirMissingError com props configDirSlug/expectedPath). server/src/services/claude-accounts.ts (515 linhas) implementa factory com 7 métodos: listAccounts, selectActiveAccount (advisory lock pg_advisory_xact_lock(hashtextextended(agentId,0)) dentro de db.transaction; respeita sticky/manual quando live; lazy cooldown sweep; round-robin lastUsedAt ASC; throw NoAccountsAvailableError em pool vazio), rotateOnQuotaExhausted (W1 fix: NÃO emite activity log; retorna RotationOutcome={rotationId, newAccount}; registra PendingRotation in-memory com TTL 10min), recordSwapOutcome (consome rotationId, emite logActivity claude_account_rotated com swapStrategy efetivo + swapStatus, deleta pending entry — idempotência-by-id), resolveCredentialDir (path.join $HOME/.paperclip/claude-accounts/<slug>; throw CredentialDirMissingError em ENOENT/non-dir), recordStepExecution (insert append-only), markCooldownPassed (narrow update). Helpers: computeMaxExhaustion = MAX(windows), defaultExhaustionFallback (rpm/tpm 1min, daily 24h, weekly 7d, 5h, org_tier 5min). server/src/services/claude-accounts.test.ts (554 linhas) com 21 vitest cases cobrindo lock, ordering, sticky-respect, sticky-fallback, cooldown filter, no-accounts error, binding-create, W1 invariants (returns rotationOutcome + does NOT emit logActivity), windows update, MAX recomputation, recordSwapOutcome (emit + unknown id throw + idempotency), resolveCredentialDir (3 paths), recordStepExecution append-only, markCooldownPassed. server/src/errors.test.ts (40 linhas) com 4 cases para os 2 errors. 25/25 tests pass; pnpm tsc --noEmit exit 0. Desvios: Regra 3 path errado no plan frontmatter (server/src/services/errors.ts inexistente → fix: usar server/src/errors.ts existente); Regra 2 idempotency-by-id em recordSwapOutcome + TTL safety valve. MULTI-04 satisfeito.

Progresso: [█████████░] 95% (21/22 plans — milestone v1.1 in progress; Phase 11 2/3)

## Métricas de Performance

**Velocidade:**

- Total de planos concluídos: 20
- Duração média: ~14 min
- Tempo total de execução: ~4h+

**Por Fase:**

| Fase | Planos | Total | Média/Plano |
|------|--------|-------|-------------|
| 01-fork-hard | 2 | ~18min | ~9min |
| 02-supabase | 6 | ~71min+ | ~12min |
| 03-team-onboarding | 5 | ~50min | ~10min |
| 04-spike-detection | 5 | ~20min+ | ~4min (artefatos documentais + harness shell + prototype TS) |
| 08-traducao-ui-core | 2 (até agora) | ~95min | ~47min (08-01 Inbox 2563+1311 LOC ~75min; 08-02 Projects surface 6 files ~20min — IssuesList já feito por 08-01 reduziu escopo) |

**Tendência Recente:**

- Últimos 6 planos (Phase 2): 02-01 (~6min), 02-02 (~5min), 02-03 (~40min), 02-04 (~4min), 02-05 (paralelo), 02-06 (~20min + checkpoint humano)
- Tendência: planos com checkpoints humanos (02-06) consomem wall-clock proporcional ao tempo de validação manual; planos auto-only fluem rápido (02-04 ~4min). Investimento alto em audit (02-01/02-03) destrava waves seguintes em minutos.

*Atualizado após cada conclusão de plano*

## Contexto Acumulado

### Decisões

Decisões estão registradas na tabela de Decisões Chave do PROJECT.md.
Decisões recentes que afetam o trabalho atual:

- Inicial: Fork hard sem upstream — liberdade para customizar sem custo de merge contínuo.
- Inicial: Supabase remoto compartilhado (`bxlczioxgizgvtznukwt`) — estado único da equipe.
- Inicial: Manter Better Auth, usar Supabase só como Postgres — schemas text-id incompatíveis com `auth.users` uuid; migração HIGH effort sem ganho v1.
- Inicial: RLS opcional no v1 — sem `auth.uid()` resolúvel; autorização aplicacional via membership.
- Roadmap: Fase 4 é spike investigativo puro — sem código de produção; entregáveis são taxonomia 429, classifier prototype e validação de mecânica de retomada.
- 01-01: Import strategy = `git read-tree -u --reset` from temporary remote (não clone-to-temp). Mantém SHA traceable em git history.
- 01-01: Substituir paperclip's CONTRIBUTING.md inteiramente (hard-fork policy supersede upstream contribution guide).
- 01-01: Renomear paperclip's root ROADMAP.md → paperclip-ROADMAP.md (preservar como referência; nosso roadmap em .planning/).
- 01-02: Default `mode: embedded-postgres` validado — paperclip's runtime-config faz fallback para embedded sem precisar de env override.
- 01-02: Server + Vite compartilham porta 3100 via Vite dev middleware mounted in-process (paperclip pattern; não portas separadas).
- 01-02: Stale runtime-services registry on Windows é wart conhecido — taskkill não dispara cleanup hooks. Diferido para Phase 3 (workflow + onboarding).
- 01-02: Captured concrete baseline values (ports 3100/54329, 71 migrations, /api/health 200, UI dark-theme) — Phase 2 deve preservar fallback path contra essa referência literal.
- 02-01: 71 migrations apply try-as-is contra Supabase (apenas 1 CREATE EXTENSION pg_trgm, zero CREATE SCHEMA custom — alta confiança ex ante).
- 02-01: pg_advisory_xact_lock confirmado compatível com Supavisor transaction mode (xact, lock liberado no commit/rollback).
- 02-01: auto-migrations no startup serão desabilitadas no plano 02-03 (HIGH risk em DB compartilhado por 5 devs).
- 02-01: pool config travada — max=5, idle_timeout=20, prepare:false condicional para porta 6543.
- 02-01: `aws-1-sa-east-1.pooler.supabase.com` confirmado como hostname Supavisor empírico (não documentado em Supabase docs públicas).
- 02-02: Pre-commit guard escolheu husky@9 (idiomatic single-line hook) em vez de simple-git-hooks ou lint-staged-only. Husky se auto-instala via `prepare` script — zero fricção para devs novos.
- 02-02: Path classification rule: `ui/src/**` + `*.tsx`/`*.jsx` são client-side (forbidden for JWTs); `server/**`, `packages/**`, `scripts/**` são livres. VITE_*SERVICE_ROLE/SECRET* names são bloqueados em qualquer arquivo (env files inclusive).
- 02-02: Standalone vitest config (`scripts/check-no-service-role-leak.vitest.config.mjs`) necessária — root vitest.config.ts declara 12 workspace projects, nenhum inclui `scripts/`. Adicionado como artefato versionado para verifiability.
- 02-03: `buildPostgresOptions` retorna `undefined` para portas ≠ 6543/5432 — preserva default postgres-js do embedded sem fricção em vez de expor options vazio.
- 02-03: Embedded fallback agora opt-in via `PAPERCLIP_DB_MODE=embedded-postgres`; sem flag E sem DATABASE_URL → throw acionável (fail-fast detecta misconfig imediatamente, em vez de cair silenciosamente em embedded).
- 02-03: `drizzle.config.ts` lança erro quando nenhum env var presente (drizzle-kit ser invocado sem env apontando para Supabase é misconfig grave que pode aplicar DDL no lugar errado).
- 02-03: Two-URL convention estabelecido: `DATABASE_URL` = pooler 6543 (transaction, app runtime), `SUPABASE_DB_URL` = pooler 5432 (session, DDL/migrations). Ambos no `.env.example` com TODO_FILL_ME.
- 02-03: `promptApplyMigrations` em non-TTY retorna `false` (era `true`) — fechamento de Audit F.1 HIGH risk. Watchers/dev-runner/daemon não aplicam migrations sem opt-in (`PAPERCLIP_MIGRATION_AUTO_APPLY=true`) ou TTY interativo.
- 02-05: Verification-only execution — better-auth.ts módulo preservado intacto (zero edits estruturais), apenas comment block (33 linhas) mapeando AUTH-01..04 a linhas específicas. Readiness suite (7 tests) valida AUTH-01 (integration contra Supabase real, gated por SUPABASE_DB_URL) + AUTH-02 (5 unit tests, cookie prefix paperclip-team-shared empiricamente confirmado).
- 02-05: Integration test gated por `it.skipIf(skipReason !== null)` baseado em SUPABASE_DB_URL/DATABASE_URL presence — graceful skip sem env, valida wiring real com .env.local. Padrão reusable para futuras fases de testes que requerem secrets.
- 02-04: 71 migrations aplicaram cleanly contra Supabase pooler 5432 (Outcome A) — 80 tabelas em `public.*`, 71 rows em `drizzle.__drizzle_migrations`; sem necessidade de regenerate-baseline. Confiança ex ante do MIGRATION_AUDIT.md (J.3 ALTA) confirmada empiricamente em ~6s de apply.
- 02-04: GitHub Actions workflow `db-migrate.yml` é único caminho automatizado de `pnpm db:migrate` em merge para `main` (DB-03). Concurrency group `db-migrate-supabase` impede runs paralelas; `workflow_dispatch` habilitado para emergências manuais. Path filter restrito a `packages/db/src/{migrations,schema}/**` + `drizzle.config.ts`.
- 02-04: Schema governance dual gate — humano (PR template `## Schema/Migration Changes (DB-04)` com 4 checkboxes) + automatizado (workflow path filter). CONTRIBUTING.md `## Database Migration Policy` declara drizzle-kit como única fonte (DB-05), proíbe explicitamente `supabase migration new` e `pnpm db:migrate` local contra Supabase compartilhado.
- 02-04: Workflow YAML usa `working-directory: packages/db` nos steps node de verification — postgres package resolve via `packages/db/node_modules` (workspace install pattern). Lição aprendida durante pré-flight local: `node -e "require('postgres')"` rodando do root falha com MODULE_NOT_FOUND.
- 02-06: Smoke E2E não orquestra server — script roda contra server externo (humano/Claude inicia `pnpm --filter @paperclipai/server dev`). Child-process orchestration em Windows com Vite middleware é brittle; manter responsabilidades separadas mantém o smoke focado em validar wiring real, não startup.
- 02-06: `dev-runner.ts` em `pnpm dev`/`pnpm dev:once` arranca em `local_trusted` mode (Better Auth handler não montado). Para exercitar `authenticated` mode em local com loopback bind, invocar `PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev`. Captured como finding para TEAM-02 (onboarding doc Phase 3).
- 02-06: Better Auth rejeita signup sem header `Origin` com 403 `MISSING_OR_NULL_ORIGIN`. Clientes (testes, scripts) precisam enviar `Origin: <server-url>` ou hostname declarado em `BETTER_AUTH_TRUSTED_ORIGINS`. Captured para TEAM-05 (troubleshooting Phase 3).
- 02-06: Default-company auto-create é gated a `local_trusted` mode (`ensureLocalTrustedBoardPrincipal`); em `authenticated`, signups novos não recebem company default e `GET /api/companies` retorna `[]`. Comportamento correto — onboarding team-shared (invite/board-claim) é responsabilidade da Phase 3 TEAM-01.
- 02-06: Phase 2 declared complete via human-verify approval; 16/16 requirements satisfeitos (INFRA-01..06, DB-01..05, AUTH-01..05). Multi-dev cross-machine validation deferida para Phase 3 TEAM-04 — infra está pronta, falta só o human flow.
- 03-02: ONBOARDING.md pt-br criado na raiz (124 linhas, 7 seções H2 numeradas seguindo D-06) com tempo-alvo <30min declarado no topo (D-07). Vars críticas em tabela markdown; PAPERCLIP_INSTANCE_ID=team-shared destacado como literal obrigatório com aviso de divergência → cookie isolado. Modo `authenticated` documentado como override explícito para invite flow (finding 02-06: pnpm dev arranca em local_trusted, Better Auth handler não montado). Bootstrap CEO vs dev #2..N split na seção 6. README.md ganhou nota DDD curta no topo apontando para ONBOARDING.md (D-08); paperclip body intocado byte-por-byte (439 linhas remanescentes). Anchor convention estabelecida para TROUBLESHOOTING.md em snake-case (#windows-ntfs, #cookie-prefix-divergente). TEAM-02 satisfeito.
- 03-03: TROUBLESHOOTING.md pt-br criado (193 linhas, 7 seções) cobrindo Windows NTFS, stale registry, too-many-connections, cookie prefix, schema desatualizado, claude CLI, prepared statement 6543. Anchors GFM alinhados com links do ONBOARDING.md; cada causa em código cita path:linha (D-18). Satisfies TEAM-05.
- 03-05: TEAM-SIGNUP-PROCEDURE.md (185 linhas, pt-br) documenta bootstrap CEO via `tsx packages/db/scripts/create-auth-bootstrap-invite.ts --config <path> --base-url <url>` + cadastro coletivo de 4+ devs via fluxo `company_join` existente (D-09: zero código novo). 3 SQL queries de validação literal (pre-check bootstrap_ceo, count intermediário 1user/1company, count final 5+ users na shared company). Modo `authenticated` documentado como override obrigatório (finding 02-06). Fallback `pending-team-growth` aceito explicitamente — proibido falsificar users via SQL (viola D-09 e quebra cost attribution Fase 6). Forward link para 03-HUMAN-UAT.md#uat-03-02 (será resolvido pelo plano paralelo 03-04 da Wave 2). Satisfies TEAM-01 (no que é automatizável; execução real é HUMAN-UAT).
- 03-04: CROSS-MACHINE-SMOKE.md (142 linhas, pt-br) documenta procedimento canônico D-13 (Dev A em máquina X cria company → Dev B em máquina Y vê via Supabase remoto + cookie `paperclip-team-shared` interoperável) + fallback single-host D-14 (two-browser-profiles, com limitação explícita de não provar literalmente cross-machine) + bloco de log timestamped D-15. 03-HUMAN-UAT.md (82 linhas) com frontmatter YAML (`type: human-uat`, `status: pending`) lista 2 UATs: UAT-03-01 (TEAM-04 cross-machine smoke) e UAT-03-02 (TEAM-01 5+ devs com SQL inline pra Supabase Studio). Decisão de roteamento: TEAM-04 satisfeito como **artefato** (procedimento auto-suficiente existe), validação **empírica** explicitamente HUMAN-UAT — executor Claude não tem duas máquinas físicas distintas, fingir validação seria desonesto. Phase 3 fecha como complete-with-pending-UAT (artefatos entregues, UATs ficam como trabalho contínuo da equipe). Satisfies TEAM-04.
- 04-02: DECISION-DETECTION-STRATEGY.md (102 linhas, pt-br) registra decisão arquitetural sobre detecção 429 Claude Code: (1) **reativa como primary** (parse stream-JSON/stderr via `CLAUDE_TRANSIENT_UPSTREAM_RE` parse.ts:13 — battle-tested, transport-agnostic, não depende de headers HTTP); (2) **pré-emptiva como enhancement opt-in** (anthropic-ratelimit-tokens-remaining, condicional a investigação empírica confirmar que CLI propaga esses headers); (3) **cooldown 30s configurável** via `CLAUDE_ACCOUNT_COOLDOWN_SECONDS` aplicando-se APÓS swap real (não bloqueia primeira detecção; `agent_account_bindings.lastRotatedAt` é o gate); (4) **retry-after honrado** — `extractClaudeRetryNotBefore` (execute.ts:640) já produz ISO timestamp utilizável diretamente como `claude_accounts.exhaustedUntil`. Tabela de trade-offs cobre 5 cenários (token-bucket reset prematuro → threshold conservador; retry-after ausente em org_tier → fallback 5min; cooldown saturando pool em rajadas → só aplica após swap real; regex change entre versões CLI → logging para auditoria; headers não expostos → fallback transparente). Mapeamento explícito para Phase 5: MULTI-06 reusa regex existente em vez de duplicar; MULTI-04 filtra por `exhaustedUntil > now AND lastRotatedAt > now - cooldown`; MULTI-01 modela `lastQuotaWindowsJson` por tipos da taxonomia (rpm/tpm/daily/weekly/5h/org). Spike NÃO modifica produção (`git diff master~1..master -- packages/adapters/claude-local/src/server/` = 0 files). Satisfies SPIKE-03.
- 04-03: Prototype descartável `detectClaudeQuotaExhausted` em `.planning/phases/04-*/prototype/` (D-09: standalone, não em packages/). Classifier TS (90 linhas) reusa `CLAUDE_TRANSIENT_UPSTREAM_RE` (parse.ts:13) como gate top-level + chain de discriminators most-specific-first (session_5h strict via "5-hour limit reached" literal; weekly; daily; tpm; org_tier; rpm). Output shape `{ type: QuotaType, confidence: 0..1, retryAt: Date | null, source: "regex" | "header" | "stream_event" }` valida design D-10. **Decisão crítica:** "claude usage limit reached" é token ambíguo (aparece em daily e session_5h); discriminator strict para session_5h exige `5-hour limit reached` literal — sem ele, default conservador roteia para daily_quota (daily reset menos disruptivo). 8 vitest cases (6 fixtures + 2 negative) passando. Standalone vitest config (`prototype/vitest.config.ts`) com `esbuild.tsconfigRaw` inline necessário porque (a) `vitest.config.ts` raiz declara workspace projects fechados em packages/server/ui/cli, (b) tsconfck walk-up falha em referência stale a `packages/adapters/droid-local` (pasta ausente — pre-existing repo issue, não corrigido por D-01: spike read-only). 6 fixtures stub baseados em parse.ts:13 tokens + docs Anthropic — HUMAN-UAT-04-01 substituirá com captura real. README pt-br (D-08) documenta API + comando `npx vitest run --config <path>`. Spike NÃO modifica produção (`git diff master~3..HEAD -- packages/` = 0). SPIKE-02 satisfeito.
- 04-04: Harness multi-account criado em `.planning/phases/04-*/harness/` (D-16/D-18 — descartável, não em scripts/ raiz). 3 artefatos: `capture-fixture.sh` (74 linhas — spawn de `claude` CLI com `CLAUDE_CONFIG_DIR=$HOME/.paperclip/claude-accounts/<slug>` isolado, captura stream-JSON via `--output-format stream-json --verbose`, extrai session_id; pre-flight rejeita ausência de account dir/CLI), `test-multi-account-resume.sh` (141 linhas — smoke 3-step orquestrado: baseline A → baseline B → tentativa de cross-account resume com `--resume $SESSION_A` mas `CLAUDE_CONFIG_DIR=B`; gera `multi-account-empirical.md` com tabela session_A/session_B + exit code do resume + checklist manual), `README.md` (95 linhas pt-br conforme D-08 — pré-requisitos `claude login` para 2 dirs, fluxo HUMAN-UAT recomendado mapeando UAT-04-01 (fixture 429), UAT-04-02 (session_id per-account), UAT-04-03 (swap manual), nota disposable mencionando Phase 5 MULTI-04 como sucessor de produção). Test script reusa capture-fixture.sh para evitar duplicar lógica de spawn (single source of truth). Constraint SPIKE respeitada: zero linhas de produção em `packages/adapters/claude-local/src/server/` modificadas. SPIKE-04 + SPIKE-05 satisfeitos como artefato — validação empírica destravada quando operador tiver 2 contas Claude reais (fora do controle do executor).
- 04-01: CLAUDE_429_TAXONOMY.md (108 linhas, pt-br) audita 6 tipos de 429 (rpm_transient, tpm_transient, daily_quota, weekly_quota, org_tier, session_5h) contra `CLAUDE_TRANSIENT_UPSTREAM_RE` (parse.ts:13) e `CLAUDE_EXTRA_USAGE_RESET_RE` (parse.ts:14). Cobertura: 4 yes (rpm/daily/weekly/5h cobertos diretamente pelo regex existente); 2 partial — **tpm_transient** sem discriminator de dimensão via mensagem (RPM e TPM textualmente indistinguíveis; discriminator natural vem de header `*-reset` zerado, não da string), **org_tier** sem token canônico empiricamente confirmado (cai em fallback transient_upstream genérico via `overloaded_error`/`service_unavailable`/`503`/`529`). Implicação para Phase 5 MULTI-06: classifier deve REUSAR regex existente, não duplicar; gaps `partial` são candidatos a extensão pontual quando HUMAN-UAT-04-01 capturar fixtures reais. Sub-tipo discriminator no output do classifier (`type: "rpm_transient" | "tpm_transient" | "daily_quota" | "weekly_quota" | "session_5h" | "org_tier" | "unknown"`) é design pattern recomendado mesmo quando regex de detecção compartilha match — UI/schema diferenciam políticas. Tabela de 7 headers Anthropic incluída como referência (`anthropic-ratelimit-{requests,tokens}-{limit,remaining,reset}` + `retry-after`); investigação empírica em UAT-04-01 confirma se Claude Code CLI propaga estes headers no stream-JSON (destrava decisão pré-emptiva do 04-02). Spike NÃO modifica produção. Satisfies SPIKE-01.
- 05-03: Foundation artifacts (env var + type + design doc) entregues em ~3min 20s. `.env.example` documenta `CLAUDE_ACCOUNT_COOLDOWN_SECONDS=30` com comentário pt-br explicando semântica de cooldown (aplica APÓS swap real, evita ping-pong, configurável por tier; default justificado em DECISION-DETECTION-STRATEGY.md). `server/src/services/activity-log.ts` ganha `export const ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED` + `export interface ClaudeAccountRotatedDetails` com schema D-30 completo (7 campos: agentId, fromAccountId, toAccountId, reason 'exhausted'|'manual'|'cooldown_expired', errorFamily string|null, retryNotBefore ISO|null, swapStrategy 'resume'|'fallback_full_context'|null). JSDoc do bloco refere `claudeAccountsService.rotateOnQuotaExhausted` como emissor (consumido por 05-04). `logActivity()` signature mantida intacta (action: string aberto) — consumers importam constante e passam string; `ACTIVITY_ACTION_TO_PLUGIN_EVENT` map intencionalmente NÃO atualizado (evento interno operator-only v1, exposição via plugin SDK pode vir depois sem breaking change). `server/src/services/claude-accounts-swap.md` (87 linhas, inglês — convenção paperclip de doc adjacente ao código TS) documenta Strategy A (cross-account `--resume <session_id>` optimistic, ~5s timeout, abandon on session-not-found / exit-before-system-init) + Strategy B (Plan B fallback: spawn nova sessão com `issue_continuation_summary` embebido como prompt inicial; trade-off tokens vs continuidade), guard D-19 (máximo 1 rotação automática por step — anti-thrash), mapping de swapStrategy field para auditoria operator (resume / fallback_full_context / null), e refinement path condicional a UAT-04-03 (se Strategy A funcionar consistentemente → keep code; se falhar sempre → remover tentativa para economizar ~5s). TypeScript compila clean (`pnpm tsc --noEmit` exit 0). 3 commits: 273290d (env), 58a6178 (types), 13cc9fb (design doc). MULTI-10 type registration **parcial** — emissão real (`logActivity` call) virá em 05-04. Plano executou em paralelo com 05-01/05-02/05-05 sem conflitos (arquivos disjuntos).
- 05-01: 3 schemas Drizzle multi-account criados em ~4min (commits ef3120c feat schemas + 320d47b chore migration). `claude_accounts` (74 linhas, FKs companyId uuid → companies.id + ownerUserId text → user.id Better Auth; 3 indexes incluindo UNIQUE config_dir_slug; lastQuotaWindowsJson jsonb tipado com TypeScript Record para 6 sub-tipos taxonomia 429; exhaustedUntil top-level cached = MAX(windows) para query rápida sem JSONB scan). `agent_account_bindings` (30 linhas, agentId uuid PK → agents.id, activeAccountId nullable, rotationPolicy 'auto'|'sticky'|'manual', lastRotatedAt para cooldown gate). `agent_step_executions` (53 linhas, append-only sem campo de mutação, FKs runId → heartbeat_runs.id + accountId → claude_accounts.id, 2 indexes (run_id,step_id) e (account_id,started_at)). Decisão crítica: id=uuid (não text como CONTEXT D-02 sugeria) — alinha com convenção codebase (companies, agents, cost_events, heartbeat_runs); ownerUserId=text para casar Better Auth user.id; documentado inline. Migration 0071_lively_azazel.sql gerada via `pnpm db:generate` mas manualmente curada (242→62 linhas) para remover DDL fantasma de tabelas pré-existentes — drift entre drizzle-kit `meta/` snapshots (gaps 0042/0043/0054/0059/0062-0070) e SQL hand-written; rebaseline geral é debt repo hygiene fora de escopo. NÃO aplicada contra Supabase (DB-03 CI-only). Acceptance criterion `! grep -q "updatedAt"` exigiu rewording de JSDoc comment ("sem campo de mutação pós-INSERT" em vez de literal "sem updatedAt") — Regra 1 fix. Barrel `packages/db/src/schema/index.ts` exporta os 3 após `costEvents`. TypeScript `pnpm tsc --noEmit` exit 0. Plano executou em paralelo com 05-02/05-03/05-05 sem conflitos (arquivos disjuntos: schema/ vs services/ vs ui/). MULTI-01/02/03 satisfeitos.
- 05-05: MULTI-05 wiring entregue em ~7min (commits 74f5f40 feat + 9a5c2b3 test). `buildClaudeRuntimeConfig` em `packages/adapters/claude-local/src/server/execute.ts` aceita campo opcional `config.claudeConfigDir`; quando provided, seta `env.CLAUDE_CONFIG_DIR` APÓS loop envConfig (linhas 240-242) — precedência: `config.claudeConfigDir > config.env.CLAUDE_CONFIG_DIR > process.env.CLAUDE_CONFIG_DIR (via includeRuntimeKeys logging)`. 11 linhas adicionadas (declaração via `asString(config.claudeConfigDir, "")` + assign + comentário MULTI-05 documentando precedência e referenciando FINDINGS-FOR-PHASE-5.md Finding 2). `includeRuntimeKeys: ["HOME", "CLAUDE_CONFIG_DIR"]` na linha 253 PERMANECE intacta (logging side ortogonal). 3 test cases em `server/src/__tests__/claude-local-execute.test.ts` (`describe "MULTI-05 — claudeConfigDir wiring"`) validam: (1) campo dedicado vira env, (2) precedência sobre env genérico, (3) sem campo → host process.env continua propagando. Decisão técnica: tests usam `onMeta` callback para capturar `loggedEnv` ANTES do spawn — cross-platform (bypassa shebang resolution no Windows que faz fake CLI scripts existentes falharem). Comando aponta para `process.execPath` (Node binary); exit code não asserted porque wiring está totalmente capturado em `buildClaudeRuntimeConfig` antes do child process iniciar. Existing claude-local-* tests (15 cases em 3 arquivos) continuam passando — zero regressão. `runClaudeLogin` (linha 280+) NÃO modificado — mas como usa mesmo `buildClaudeRuntimeConfig`, ganha o wiring automaticamente se config field for fornecido (não testado aqui, mas suportado). Plano executou em paralelo com 05-01/05-02/05-03 sem conflitos (arquivos disjuntos). MULTI-05 satisfeito; pronto para 05-06 (heartbeat) consumir via `executeAdapter({ ..., config: { ..., claudeConfigDir: resolveCredentialDir(account) } })`.
- 05-06: heartbeat wiring + orchestrateClaudeSwap module entregues em ~25min (commits `a222e0b` swap module + `7cc1026` heartbeat). server/src/services/claude-accounts-swap.ts (~220 linhas) expõe 3 helpers: `orchestrateClaudeSwap` (detecta exhaustão via detectClaudeQuotaExhausted → rotateOnQuotaExhausted DB-only sem activity log emit → resolveCredentialDir → retorna retry plan Strategy A com rotationId para caller finalizar via recordSwapOutcome), `orchestrateFallbackFullContext` (constrói prompt Strategy B com continuation summary embedded; W1 invariant: NUNCA emite activity log), `detectResumeFailed` (D-22 patterns: session not found / invalid session id / unknown session / no conversation found + exit-without-system-init). Reasons taxonomy: swapped | no_detection | pool_empty | rotation_cap_reached | missing_credentials. server/src/services/__tests__/claude-accounts-swap.test.ts (~280 linhas) com 11 vitest cases incluindo Test 10 W1 critical guard (mockRecordSwapOutcome NEVER invoked by orchestrate). server/src/services/heartbeat.ts ganha 4 pontos cirúrgicos: (A) imports claudeAccountsService + helpers; (B) ~linha 5302 antes adapter.execute, gated por adapterType==='claude_local', selectActiveAccount + resolveCredentialDir injeta claudeConfigDir em augmentedConfig (errors fallback para process.env preservando legacy); (C) ~linha 5340 pós-adapterResult, bloco MULTI-08 com Strategy A/B orchestration: orchestrateClaudeSwap → re-spawn com retrySessionId → detectResumeFailed → se falhou orchestrateFallbackFullContext + re-spawn com initialPrompt; effectiveSwapStrategy ('resume' ou 'fallback_full_context') determinado pela rota que produziu adapterResult final; recordSwapOutcome chamado APÓS retry settle com effective strategy + swapStatus succeeded/failed (W1 D-32 accuracy); alreadyRotatedThisStep flag local enforça D-19 (max 1 rotação por step); missing_credentials path também emite recordSwapOutcome com swapStatus='failed' (rotação persistida mas spawn impossível); (D) ~linha 5610 após normalizedUsage, recordStepExecution com executingAccountId (W2 PROJ-03: variável tracking que reflete conta executora real — selectedAccount.id default, swapResult.newAccount.id pós-rotação). Decisão crítica: orchestrateClaudeSwap retorna apenas Strategy A retry plan; caller (heartbeat) invoca orchestrateFallbackFullContext separadamente — alternativa de orchestrate fazer 2 spawns rejeitada porque spawn precisa de executionTarget/remoteExecution/persistRunProcessMetadata/onSpawn/authToken (todos no scope heartbeat); manter no caller é mais simples e mantém o módulo puro/testável. Strategy B re-spawn dentro da mesma rotação NÃO conta como segunda rotação (D-19 cap mantém "1 rotação por step"). Single-row attribution v1 em agent_step_executions; two-row split (A failed + B retry) deferred v2 se PROJ-03 demandar granular failure attribution. Desvios: **Regra 3** swapStatus enum mismatch — plan especificava 'success'/'failed' mas service 05-04 implementou 'succeeded'/'failed'; heartbeat alinhado com service real (TS compile destrava). **Regra 2** mkdir -p server/src/services/__tests__/ pré-Write (subdir não existia). **Decisão semântica** missing_credentials path tem ocorrência literal `swapStrategy: 'resume'` em recordSwapOutcome — Strategy A foi a INTENÇÃO que nunca executou; reportar intenção falhada é a única opção honesta neste edge case (W1 invariant preservado em orchestrate.ts: zero ocorrências literais lá). Known stubs documentados no SUMMARY: `config.initialPrompt` e `config.resumeSessionId` setados pelo heartbeat mas adapter buildClaudeRuntimeConfig não os consome — Strategy A vai usar runtime.sessionParams.sessionId (sessão velha, vai falhar em cross-account → trigger Strategy B caminho esperado per Finding 7); Strategy B vai re-spawn com prompt template default sem continuation summary. Estes gaps são aceitáveis em v1 (Plano B do CONTEXT D-21 explicitamente aceita "tokens duplicados na primeira chamada"); fechamento end-to-end fica para 05-08 smoke ou refinement pós-UAT-04-03. server `pnpm tsc --noEmit` exit 0; 36/36 tests pass (claude-accounts-swap 11 + claude-accounts 21 + heartbeat-context-summary 4); zero regressão. Plano executou em paralelo com 05-07 sem conflitos (arquivos disjuntos: services/heartbeat.ts + claude-accounts-swap.ts vs routes/claude-accounts.ts + ui/*). MULTI-07 + MULTI-08 satisfeitos.
- 05-04: claudeAccountsService(db) factory entregue com 7 métodos em ~25min (commits `2f7de6f` RED test errors, `27f3ddc` GREEN errors, `6255a21` service+tests). server/src/errors.ts ganha NoAccountsAvailableError(companyId, agentId) + CredentialDirMissingError(configDirSlug, expectedPath) — appended preservando exports existentes. server/src/services/claude-accounts.ts (515 linhas) implementa 7 métodos do D-09 com **W1 fix arquitetural**: rotateOnQuotaExhausted retorna `{ rotationId, newAccount }` (em vez de só ClaudeAccount) e **NÃO emite activity log** — apenas atualiza DB (mark from exhausted com lastQuotaWindowsJson[errorFamily] update + recompute MAX(windows), select next eligible excluindo from, update binding lastRotatedAt + activeAccountId). Pending rotation registrada em Map<rotationId, PendingRotation> in-memory com TTL 10min e GC lazy. recordSwapOutcome({ rotationId, swapStrategy, swapStatus }) consome a entrada, emite logActivity claude_account_rotated com swapStrategy efetivo ('resume' | 'fallback_full_context' | null) + swapStatus, deleta a pending entry (idempotência-by-id — segunda chamada throws). Isto destrava heartbeat (05-06) emitir activity log APÓS Strategy A (--resume cross-account) ou Strategy B (full-context fallback) resolver, mantendo D-32 observability acurado. Métodos restantes: listAccounts (ordenado por createdAt), selectActiveAccount (advisory lock pg_advisory_xact_lock(hashtextextended(agentId,0)) dentro de db.transaction; respeita sticky/manual quando live; lazy cooldown sweep flippa exhausted→live se exhaustedUntil < now; round-robin lastUsedAt ASC; throw NoAccountsAvailableError em pool vazio; cria binding lazy se ausente), resolveCredentialDir (path.join(homedir(), '.paperclip', 'claude-accounts', slug); throw CredentialDirMissingError em ENOENT/non-dir), recordStepExecution (insert append-only em agent_step_executions), markCooldownPassed (narrow update id+status='exhausted'+exhaustedUntil<now → 'live'). Helpers: computeMaxExhaustion (MAX dos windows.exhaustedUntil), defaultExhaustionFallback (rpm/tpm 1min, daily 24h, weekly 7d, session_5h 5h, org_tier 5min). Tests: 21 vitest cases em claude-accounts.test.ts (554 linhas) com manual chainable mock Db + per-table queue de result arrays + vi.mock para activity-log e node:fs/promises; cobrem advisory lock, ordering, sticky-respect/fallback, cooldown filter, no-accounts error, binding-create, **W1 invariants críticos** (returns RotationOutcome shape + does NOT emit logActivity em rotate; recordSwapOutcome emite com swapStrategy + idempotência), windows update, MAX recomputation, resolveCredentialDir 3 paths, recordStepExecution append-only, markCooldownPassed. errors.test.ts (40 linhas) com 4 cases (instance + props + default+custom message). 25/25 tests pass; pnpm tsc --noEmit exit 0. Desvios: **Regra 3 blocker fix** — plan frontmatter listava `server/src/services/errors.ts` (path inexistente); fix usar `server/src/errors.ts` existente preservando single-source-of-truth para errors do server. **Regra 2 critical functionality** — adicionado idempotency-by-id em recordSwapOutcome (segunda chamada throws "Unknown or expired rotationId" em vez de double-emit) + TTL 10min como safety valve para process restart entre rotate e outcome. Plano executou sequencialmente (Wave 2 depende de 05-01 schemas + 05-03 activity log type). MULTI-04 satisfeito; pronto para 05-06 heartbeat consumir via `const { rotationId, newAccount } = await svc.rotateOnQuotaExhausted(...)` → tentar Strategy A → se falhar, Strategy B → `await svc.recordSwapOutcome({ rotationId, swapStrategy: ..., swapStatus: 'succeeded' })`.
- 04-05: Phase 4 fechada via HUMAN-UAT routing. 04-HUMAN-UAT.md (240 linhas pt-br, frontmatter `type: human-uat status: pending`) lista UAT-04-01 (capturar fixtures reais via harness/capture-fixture.sh — substitui stubs em prototype/fixtures/), UAT-04-02 (confirmar session_id per-account via test-multi-account-resume.sh), UAT-04-03 (smoke 2-account swap com continuation summary — destrava MULTI-08). FINDINGS-FOR-PHASE-5.md (190 linhas pt-br) lista 8 findings explícitos com Evidência (path:linha literal) + Implicação + Ação Phase 5: F1 (MULTI-06 reusa CLAUDE_TRANSIENT_UPSTREAM_RE em parse.ts:13 — estende, não reescreve), F2 (CLAUDE_CONFIG_DIR passa pelo spawn em execute.ts:253 — MULTI-05 vira wiring), F3 (lastQuotaWindowsJson schema per-tipo com 6 chaves rpm/tpm/daily/weekly/5h/org cada uma `{exhaustedUntil, lastTriggeredAt, count}` — MULTI-01; `claude_accounts.exhaustedUntil` top-level = MAX dos 6, cached para query rápida), F4 (partial coverage gaps em org_tier e tpm_transient — classifier best-effort + log), F5 (extractClaudeRetryNotBefore execute.ts:640 produz ISO timestamp — MULTI-04 reusa), F6 (MULTI-08 swap automático bloqueado em UAT-04-03 — planejador Phase 5 deve verificar antes; plano B documentado: re-prompt full context na conta nova), F7 (session_id per-account; --resume cross-account não funciona; MULTI-08 strategy: drena step → captura summary → spawn new session em conta B; UI mostra "session swapped at <timestamp>; continuation summary: <preview>"), F8 (vitest standalone funciona — tests classifier MULTI-06 ficam em server/src/__tests__/claude-local-adapter-quota-detection.test.ts). Tabela final mapeia 8 findings → 5 MULTI-* requirements (MULTI-01/04/05/06/08); tabela de riscos mapeia 4 cenários incertos → planos B. Decisão de routing: SPIKE-04/05 via HUMAN-UAT (executor Claude não tem 2 contas reais; precedente Phase 3 03-04). Phase 5 redirecionada de "construir classifier" para "reusar regex existente + adicionar discriminator + integrar swap mechanic". Constraint SPIKE preservada (zero linhas modificadas em packages/adapters/claude-local/src/server/). Tooling friction: heurística do framework bloqueou Write tool em filename `FINDINGS-*.md` (interpretou como artefato de subagente); workaround: write como `.txt` staging + rename via mv. Padrão a documentar para futuros artefatos com nomes "FINDINGS"/"REPORT". SPIKE-04 + SPIKE-05 satisfeitos como artefato; ROADMAP success criteria #1-#5 todos endereçados.

### Todos Pendentes

Nenhum ainda.

### Bloqueios/Preocupações

- **Wart conhecido (Windows, deferido para Phase 3):** stale `~/.paperclip/instances/default/runtime-services/` survives `taskkill //F //T //PID`. Recomendar `scripts/kill-dev.sh` para shutdown gracioso. Candidatos de fix: PowerShell wrapper, `SIGBREAK` handler em `dev-runner.ts`, ou defensive PID-alive check no startup. Documentado em `.planning/phases/01-fork-hard-cerim-nia-de-corte/SMOKE-TEST-LOG.md` Outcome.
- **Configuração manual pendente (02-04, owner do repo):** GitHub Actions secret `SUPABASE_DB_URL` precisa ser adicionado via Settings → Secrets and variables → Actions antes do primeiro merge para `main` que toque `packages/db/`. Sem o secret, workflow `.github/workflows/db-migrate.yml` falha cedo com erro claro. Documentado em `MIGRATION_APPLY_LOG.md` e `02-04-SUMMARY.md`.

## Continuidade de Sessão

Última sessão: 2026-04-27
Parou em: **Plano 11-02 (language directive content + bundleKey + resume fallback) concluído.** AGENT-SKILL-01 satisfeito at code-level. buildLanguageDirectiveBlock pure function emite o bloco "## Idioma de Resposta" canônico para pt-BR (string vazia para en-US — token economy + model default); agent-instructions.exportFiles aceita options.locale opcional e anexa o directive ao entry file APENAS (siblings TOOLS.md/etc untouched); buildClaudePromptBundleKey requer locale + hash version v1→v2 garantindo invalidação one-shot natural no primeiro spawn pós-deploy; execute.ts propaga context.runtimeLocale ao bundle key e re-injeta o directive como último joinPromptSections entry quando sessionId existe (Pitfall 2 fallback semantic — UAT 11-03 valida empiricamente; recency bias maximiza adesão em resume sessions onde system prompt cached pode estar em en-US). 3 commits atômicos `1955666`/`6bf9896`/`e939fa0`; 4+3+3 novos test cases GREEN; typecheck claude-local exit 0; typecheck server preserva pre-existing recovery/service.ts:459 baseline. Plano 11-03 destravado — file sets disjuntos confirmados (top-half de prompt-cache.ts hashing/key-shape modificado por 11-02; bottom-half symlink loop ensurePaperclipSkillSymlink → materializeSkillForLocale a fazer por 11-03 + 4 novos SKILL.pt-BR.md skills/* + HUMAN-UAT artifact AGENT-SKILL-04). Próxima ação: /executar-fase 11 → 11-03.

Parou anteriormente em: **Plano 10-02 (run summaries) concluído.** Wave 1 da Fase 10 completa — 10-01 (panels/status/config) + 10-02 (summaries: transcript/run-ledger/live-runs/chat-surface) ambos at code-level com file sets disjuntos zero merge friction. agents.json em 197 leaf keys × 2 locales (100% paridade). AGENT-MSG-01, AGENT-MSG-02, AGENT-MSG-03 satisfeitos at code-level; AGENT-MSG-04 pendente (Wave 2 — toasts/notifications + LiveUpdatesProvider builders, depende de namespace agents). Próxima ação: /executar-fase 10 → 10-03 (sequential Wave 2 — toca LiveUpdatesProvider.tsx + AgentDetail.tsx:1606 + IssueRunLedger.tsx:391 watchdog toast preservado por 10-02 + activity.json/common.json extensions).

Parou anteriormente em: **Milestone v1.1 (Internacionalização pt-BR) iniciado e roadmap criado.** ROADMAP.md escrito com 5 fases (7-11) cobrindo 26/26 requisitos sem órfãos. REQUIREMENTS.md atualizado com tabela de Rastreabilidade preenchida (SETTINGS/I18N → Fase 7; UI core → Fase 8; UI admin/auth/sistêmicas → Fase 9; AGENT-MSG → Fase 10; AGENT-SKILL → Fase 11; status Pending). Próxima ação: /planejar-fase 7 para decompor Foundation i18n + Toggle de Settings em planos executáveis. Ordem das fases: 7 (foundation) → 8 (UI core) → 9 (UI admin+auth+sistêmicas) → 10 (agent messages) → 11 (agent skills + system prompts).

Parou anteriormente em: **Milestone v1.0 (Fork + Multi-Account) DECLARED READY.** Plano 06-06 completo (V1-READINESS.md 275 linhas + REQUIREMENTS.md closure 45/45 zero Pending). Phase 6 fechada como `complete-with-pending-UAT` (precedente Phase 3/4/5). 7 UATs registradas como itens de validação humana contínua não-bloqueantes. Próxima ação: `/auditar-marco` (autonomous lifecycle) ou execução pontual das UATs pendentes pelo operador humano conforme acesso a recursos físicos (2 máquinas para UAT-03-01, 5+ devs reais para UAT-03-02, 2 contas Claude reais para UAT-04-02/03 + UAT-05-01, 2 browsers/profiles para UAT-06-01). v2 backlog priorizado por sinal real de uso (5 categorias × 13 itens em REQUIREMENTS.md §"Requisitos v2"); criar Phase 7 quando 2-3 itens acumularem demanda concreta.

Parou anteriormente em: 06-05 completo (MULTI-COMPANY-SMOKE.md + 06-HUMAN-UAT.md); PROJ-01 satisfeita como artefato testável (Modo A SQL) + UAT-06-01 pending (Modo B visual cross-browser).

Parou anteriormente em: Phase 5 fechada como complete-with-pending-UAT — 05-08 entregou SMOKE-E2E.md (Modo A canonical procedure 218 linhas, 6 Passos D-33, 7 critérios pass/fail) + 05-HUMAN-UAT.md (UAT-05-01 Modo B 184 linhas, requer 2 contas Claude reais para validação empírica final). 8/8 planos Phase 5 completos. MULTI-11 satisfeito como artefato. Próximo: Phase 6 (Multi-Project — PROJ-01..03) ou execução empírica de UATs Phase 4/5 (UAT-04-01..03 + UAT-05-01) pelo operador humano. Phase 5 promovida de complete-with-pending-UAT para complete formal quando UAT-05-01 PASS.

- 05-08: Phase 5 closure entregue em ~3min (commits `2ed7984` SMOKE-E2E.md + `85d645e` 05-HUMAN-UAT.md). 2 documentos pt-br consolidam validação empírica Phase 5: (1) SMOKE-E2E.md (218 linhas) procedimento canônico Modo A em 6 Passos D-33 — registrar 2 contas via UI → spawnar agente → forçar exhaustão de A via SQL `UPDATE claude_accounts SET status='exhausted', exhausted_until=now()+1h, last_quota_windows_json=jsonb_set(...,'{daily_quota}',...)` → spawnar próximo step → observar swap automático → validar attribution em agent_step_executions (rows com ambos accountIds) + activity_log (entry claude_account_rotated com 8 campos: agentId/fromAccountId/toAccountId/reason/errorFamily/retryNotBefore/swapStrategy/swapStatus) + UI rotation history; pré-requisitos table (8 itens), SQL queries literais para 4 tabelas (claude_accounts, agent_account_bindings, agent_step_executions, activity_log), pass/fail table (7 critérios), cleanup com SQL reset, limitations (5 itens roteados para UAT-05-01). (2) 05-HUMAN-UAT.md (184 linhas) frontmatter YAML `type: human-uat status: pending` consistente com 04-HUMAN-UAT.md/03-HUMAN-UAT.md; UAT-05-01 detalhado cobrindo MULTI-11 success criterion #1 do ROADMAP — validação empírica real cross-account com exhaustão natural (2 contas Claude reais Pro/Team/Max, ~30min-2h até esgotar A naturalmente, observar Strategy A optimistic com `--resume <session_id>` ou Strategy B fallback full-context com continuation summary embedded); pass/fail em 7 dimensões (detection real, swap automático, continuity, attribution, exhaustedUntil real, swapStrategy populado, D-19 cap 1 rotação por step); 4 cenários failure-path explícitos com ação corretiva — Continuity fail aponta para known stub do 05-06 (config.initialPrompt não consumido por buildClaudeRuntimeConfig), Detection fail aponta para CLAUDE_TRANSIENT_UPSTREAM_RE em parse.ts:13 + 04-CONTEXT D-15 ordering rules, Strategy A always-fail destrava decisão de remover tentativa optimistic (~5s economy); instruções de report (status pending → passed/failed/partial). Decisão de routing D-34 reaffirmed: MULTI-11 dividido em Modo A (executável agora pelo executor Claude/CI via SQL force) + Modo B (UAT-05-01 pending para operador humano). Phase 5 fecha como `complete-with-pending-UAT` — precedente Phase 3 plan 03-04 (TEAM-04 cross-machine) e Phase 4 plan 04-05 (SPIKE-04/05). 9/9 requirements MULTI-01..11 satisfeitos como artefato testável; ROADMAP success criterion #1 endereçado. Forward dependencies pós-Phase 5: UAT-05-01+UAT-04-03 destravam refinamento de swapStrategy; UAT-04-01 destrava substituição de fixture stubs por mensagens reais Claude CLI; Phase 6 PROJ-03 reusa agent_step_executions validado por UAT-05-01. Sem desvios materiais — plano executou exatamente como escrito (frontmatter ajustado para incluir `phase` field alinhado com Phase 4 pattern, decisão menor de consistência). Self-check passed: ambos arquivos verificados via filesystem + git log; todos acceptance criteria validados.

- 05-07: Backing API REST + UI ClaudeAccounts.tsx entregues em ~22min (commits `496f26a` server route, `af34235` UI). server/src/routes/claude-accounts.ts (~175 linhas) expõe 4 endpoints sob `/api`: GET list (assertCompanyAccess), POST create (owner/admin via assertCompanyOwnerOrAdmin inline; 23505 unique-violation → HTTP 409), PATCH toggle status/rename, GET rotation-history (filtro activity_log por ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED, default limit 50 max 200). Validação Zod (slug regex `^[a-z0-9][a-z0-9-]{0,62}$`, label 1-100 chars). Mount em server/src/app.ts via `api.use(claudeAccountsRoutes(db))`. ui/src/api/claude-accounts.ts (~80 linhas) expõe `claudeAccountsApi` com helpers list/create/patch/rotationHistory tipados (ClaudeAccount + RotationHistoryEntry mirroring contrato server). ui/src/pages/ClaudeAccounts.tsx (~280 linhas) reusa shadcn Badge (variants default/destructive/secondary/outline mapeando live/exhausted/cooldown/disabled), Button, lucide-react icons (KeyRound/Power); três seções (register form, accounts table, rotation history list); permissions errors via toast e 403-aware loading state; pattern de useQuery+useMutation copiado de CompanyInvites.tsx; setBreadcrumbs com hierarquia Company→Settings→Claude Accounts. ui/src/pages/ClaudeAccounts.test.tsx (~270 linhas) 5 vitest cases passando: badges status (4 variantes com data-status), empty state, form submission (com setControlledInputValue helper para React-controlled inputs via native value setter — fix Regra 1), toggle status, history render. Rota `/:companyPrefix/company/settings/claude-accounts` em ui/src/App.tsx (D-28 ajustado para alinhar com paperclip company-prefix routing — Regra 1 deviation documentada). Desvios: **Regra 3** Express `req.params.companyId` typed `string | string[]` rejeitada por drizzle → wrap com `String(...)`. **Regra 1** input.value= em React-controlled rejeita state update → helper Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value')?.set. **Regra 1** path D-28 `/companies/:companyId/claude-accounts` literal não fits paperclip routing scheme → reroute para padrão company-prefix. server `pnpm tsc --noEmit` exit 0; ui `pnpm tsc --noEmit` exit 0; 5/5 vitest cases pass. Plano executou em paralelo com 05-06 sem conflitos (arquivos disjuntos: routes/claude-accounts.ts + ui/* vs heartbeat consumer). MULTI-09 satisfeito.

Arquivo de retomada: Nenhum
