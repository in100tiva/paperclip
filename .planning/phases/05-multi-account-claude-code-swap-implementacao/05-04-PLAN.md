---
phase: 05-multi-account-claude-code-swap-implementacao
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - server/src/services/claude-accounts.ts
  - server/src/services/claude-accounts.test.ts
  - server/src/services/errors.ts
autonomous: true
requirements: [MULTI-04]

must_haves:
  truths:
    - "claudeAccountsService(db) retorna objeto com 6 métodos: listAccounts, selectActiveAccount, rotateOnQuotaExhausted, resolveCredentialDir, recordStepExecution, markCooldownPassed"
    - "selectActiveAccount filtra contas por status='live' AND (exhaustedUntil IS NULL OR < now) AND (lastRotatedAt IS NULL OR < now - cooldownSeconds), ordena por lastUsedAt ASC, retorna primeira"
    - "rotateOnQuotaExhausted marca conta antiga exhausted (atualizando lastQuotaWindowsJson[errorFamily] e exhaustedUntil), seleciona próxima, atualiza binding, emite activity log"
    - "resolveCredentialDir retorna path absoluto $HOME/.paperclip/claude-accounts/<configDirSlug> e valida existência"
    - "Lock pessimista via pg_advisory_xact_lock(hashtextextended(agentId)) protege select/rotate de race"
    - "NoAccountsAvailableError throwable e tipado quando pool vazio para o agente"
    - "Tests vitest cobrem: select básico, rotate com errorFamily, lock concurrence, cooldown filtering, no-accounts error, recordStepExecution append"
  artifacts:
    - path: "server/src/services/claude-accounts.ts"
      provides: "Service factory claudeAccountsService(db) com todos os métodos do D-09"
      contains: "export function claudeAccountsService"
    - path: "server/src/services/claude-accounts.test.ts"
      provides: "Test suite vitest com mocks de Db (pattern compatível com tests existentes)"
      contains: "describe.*claudeAccountsService"
    - path: "server/src/services/errors.ts"
      provides: "NoAccountsAvailableError + CredentialDirMissingError exportados (D-12)"
      contains: "NoAccountsAvailableError"
  key_links:
    - from: "server/src/services/claude-accounts.ts"
      to: "@paperclipai/db (claudeAccounts, agentAccountBindings, agentStepExecutions)"
      via: "import named tables"
      pattern: "import.*claudeAccounts.*from.*paperclipai/db"
    - from: "server/src/services/claude-accounts.ts"
      to: "logActivity + ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED"
      via: "import named from ./activity-log.js"
      pattern: "import.*ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED"
    - from: "server/src/services/claude-accounts.ts"
      to: "pg_advisory_xact_lock via sql template"
      via: "drizzle sql template"
      pattern: "pg_advisory_xact_lock"
---

<objective>
Implementar o service factory `claudeAccountsService(db)` em `server/src/services/claude-accounts.ts` cobrindo os 6 métodos do D-09 (listAccounts, selectActiveAccount, rotateOnQuotaExhausted, resolveCredentialDir, recordStepExecution, markCooldownPassed) com lock pessimista via `pg_advisory_xact_lock` para race-free rotation. Tests cobrem todos os caminhos críticos. Errors customizados em `errors.ts`.

Purpose: Este service é o cérebro do multi-account. Heartbeat (05-06) chama `selectActiveAccount` antes de cada spawn; quando `transient_upstream` é detectado, chama `rotateOnQuotaExhausted`. UI (05-07) chama `listAccounts`. Sem este service nada multi-account funciona — é o gargalo crítico da Phase 5.

Output: 1 service file (~250 linhas), 1 test file (~200 linhas), errors.ts com 2 novos errors exportados.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md
@.planning/phases/04-spike-multi-account-claude-code-detection/DECISION-DETECTION-STRATEGY.md
@server/src/services/companies.ts
@server/src/services/heartbeat-stop-metadata.ts
@server/src/services/heartbeat-stop-metadata.test.ts
@server/src/services/agent-start-lock.ts
@server/src/services/activity-log.ts
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-01-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-03-PLAN.md

<interfaces>
<!-- Padrão de service factory (companyService, heartbeatService etc.) -->

From server/src/services/companies.ts (linha 35):
```typescript
export function companyService(db: Db) {
  // ... internal helpers ...
  return {
    list: async (...) => { ... },
    create: async (...) => { ... },
    // ...
  };
}
```

<!-- Schemas a importar (criados em 05-01): -->
```typescript
import { claudeAccounts, agentAccountBindings, agentStepExecutions } from "@paperclipai/db";

// claudeAccounts cols: id (uuid), companyId (uuid), ownerUserId (text), label, configDirSlug,
//   status, lastQuotaWindowsJson (jsonb), exhaustedUntil, lastUsedAt, createdAt, updatedAt
// agentAccountBindings cols: agentId (uuid PK), activeAccountId (uuid), rotationPolicy,
//   lastRotatedAt, createdAt, updatedAt
// agentStepExecutions cols: id (uuid), runId (uuid), stepId, accountId (uuid),
//   inputTokens, cachedInputTokens, outputTokens, costUsd, startedAt, completedAt,
//   errorFamily, createdAt
```

<!-- Activity log emit (de 05-03): -->
```typescript
import {
  logActivity,
  ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED,
  type ClaudeAccountRotatedDetails,
} from "./activity-log.js";
```

<!-- Drizzle SQL template para advisory lock (existing pattern em plugin-database.ts): -->
```typescript
import { sql } from "drizzle-orm";
await db.execute(sql`SELECT pg_advisory_xact_lock(hashtextextended(${agentId}, 0))`);
```

<!-- Pattern de errors em ./errors.js do server: -->
```typescript
// errors.ts ja tem notFound, unprocessable, conflict, HttpError.
// Adicionar: NoAccountsAvailableError, CredentialDirMissingError.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar errors customizados em server/src/services/errors.ts</name>
  <files>server/src/services/errors.ts</files>
  <read_first>
    - server/src/services/errors.ts (entender exports existentes)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-12)
  </read_first>
  <behavior>
    - Test 1: `new NoAccountsAvailableError(companyId, agentId)` é instance de Error e expõe `companyId`/`agentId` props
    - Test 2: `new CredentialDirMissingError(slug, path)` é instance de Error e expõe `slug`/`path` props
  </behavior>
  <action>
    No final de `server/src/services/errors.ts` (não modificar exports existentes), adicionar:

    ```typescript
    /**
     * MULTI-04 (Phase 5): pool de contas Claude vazio para o agente.
     * Lançado por claudeAccountsService.selectActiveAccount quando nenhuma conta da
     * empresa está elegível (todas exhausted, em cooldown, ou disabled).
     */
    export class NoAccountsAvailableError extends Error {
      constructor(
        public readonly companyId: string,
        public readonly agentId: string,
        message = `No Claude accounts available for agent ${agentId} in company ${companyId}`,
      ) {
        super(message);
        this.name = "NoAccountsAvailableError";
      }
    }

    /**
     * MULTI-04 (Phase 5): diretório de credenciais da conta Claude não existe.
     * Lançado por claudeAccountsService.resolveCredentialDir quando
     * `~/.paperclip/claude-accounts/<configDirSlug>/` não está presente no filesystem.
     * Operador deve rodar `mkdir -p` + `claude login` no diretório.
     */
    export class CredentialDirMissingError extends Error {
      constructor(
        public readonly configDirSlug: string,
        public readonly expectedPath: string,
        message = `Credential dir for slug "${configDirSlug}" not found at ${expectedPath}. Run \`claude login\` in that directory.`,
      ) {
        super(message);
        this.name = "CredentialDirMissingError";
      }
    }
    ```

    NÃO modificar funções existentes em `errors.ts` (notFound, unprocessable, conflict, HttpError).
  </action>
  <verify>
    <automated>cd server && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export class NoAccountsAvailableError" server/src/services/errors.ts` retorna 1
    - `grep -c "export class CredentialDirMissingError" server/src/services/errors.ts` retorna 1
    - `grep -E "extends Error" server/src/services/errors.ts | wc -l` >= 2 (ambos extendem Error)
    - `cd server && pnpm tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>2 errors customizados exportados; TypeScript compila; errors.ts não regrediu existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar claudeAccountsService factory + tests</name>
  <files>
    server/src/services/claude-accounts.ts,
    server/src/services/claude-accounts.test.ts
  </files>
  <read_first>
    - server/src/services/companies.ts (linhas 1-150 — pattern factory + Db param + select/insert/update)
    - server/src/services/heartbeat-stop-metadata.test.ts (pattern de tests vitest para service)
    - server/src/services/agent-start-lock.ts (in-process lock pattern; usar como REFERÊNCIA mas implementação aqui é DB-side advisory)
    - server/src/services/plugin-database.ts (procurar `pg_advisory` — exemplo de drizzle sql template para advisory locks; tarefa 1 deste plano confirma padrão existe)
    - server/src/services/activity-log.ts (logActivity signature, em particular o output de 05-03 com ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED)
    - server/src/services/errors.ts (após Tarefa 1)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-09 — API completa; D-10 — cooldown env var)
    - .planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md (Finding 3 — schema lastQuotaWindowsJson; Finding 5 — retryNotBefore; Finding 7 — session per-account)
    - packages/db/src/schema/claude_accounts.ts (gerado em 05-01 — confirmar tipos das colunas)
    - packages/db/src/schema/agent_account_bindings.ts (gerado em 05-01)
    - packages/db/src/schema/agent_step_executions.ts (gerado em 05-01)
  </read_first>
  <behavior>
    - Test 1 (listAccounts): retorna todas as contas da companyId, ordenadas por createdAt
    - Test 2 (selectActiveAccount happy): query retorna 2 contas live elegíveis, retorna a com lastUsedAt mais antigo, atualiza lastUsedAt
    - Test 3 (selectActiveAccount filters): exclui status='exhausted' E exhaustedUntil > now E lastRotatedAt > now - cooldown
    - Test 4 (selectActiveAccount empty): zero contas elegíveis → throw NoAccountsAvailableError
    - Test 5 (selectActiveAccount sticky): rotationPolicy='sticky' E activeAccountId não-null → retorna a conta atual sem re-eleger
    - Test 6 (rotateOnQuotaExhausted): atualiza claude_accounts[from].status='exhausted', popula lastQuotaWindowsJson[errorFamily], recalcula exhaustedUntil, seleciona próxima, atualiza binding, chama logActivity com payload correto
    - Test 7 (rotateOnQuotaExhausted re-throw): se selectActiveAccount lança NoAccountsAvailableError, propaga ao caller
    - Test 8 (resolveCredentialDir happy): diretório existe → retorna path absoluto
    - Test 9 (resolveCredentialDir missing): diretório não existe → throw CredentialDirMissingError
    - Test 10 (recordStepExecution): insere row em agent_step_executions, sem update — append-only
    - Test 11 (markCooldownPassed): conta com exhaustedUntil < now E status='exhausted' → atualiza para 'live'; conta com exhaustedUntil > now → no-op
    - Test 12 (lock): selectActiveAccount executa pg_advisory_xact_lock antes da query (verificar via spy/mock no db.execute)
  </behavior>
  <action>
    **Service: `server/src/services/claude-accounts.ts`**

    ```typescript
    import path from "node:path";
    import os from "node:os";
    import fs from "node:fs/promises";
    import { and, asc, eq, isNull, lt, or, sql } from "drizzle-orm";
    import type { Db } from "@paperclipai/db";
    import {
      claudeAccounts,
      agentAccountBindings,
      agentStepExecutions,
    } from "@paperclipai/db";
    import {
      logActivity,
      ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED,
      type ClaudeAccountRotatedDetails,
    } from "./activity-log.js";
    import { NoAccountsAvailableError, CredentialDirMissingError } from "./errors.js";

    // ============================================================
    // Types
    // ============================================================

    export type ClaudeAccountStatus = "live" | "exhausted" | "cooldown" | "disabled";

    export type ClaudeQuotaSubType =
      | "rpm_transient"
      | "tpm_transient"
      | "daily_quota"
      | "weekly_quota"
      | "session_5h"
      | "org_tier";

    export interface QuotaWindowEntry {
      exhaustedUntil: string; // ISO
      lastTriggeredAt: string; // ISO
      count: number;
    }

    export type QuotaWindowsMap = Partial<Record<ClaudeQuotaSubType, QuotaWindowEntry>>;

    export interface ClaudeAccount {
      id: string;
      companyId: string;
      ownerUserId: string;
      label: string;
      configDirSlug: string;
      status: ClaudeAccountStatus;
      lastQuotaWindowsJson: QuotaWindowsMap;
      exhaustedUntil: Date | null;
      lastUsedAt: Date | null;
      createdAt: Date;
      updatedAt: Date;
    }

    export interface RecordStepUsage {
      inputTokens: number;
      cachedInputTokens: number;
      outputTokens: number;
      costUsd: number;
      startedAt: Date;
      completedAt: Date | null;
      errorFamily: string | null;
    }

    function cooldownSeconds(): number {
      const raw = process.env.CLAUDE_ACCOUNT_COOLDOWN_SECONDS;
      const parsed = raw ? Number.parseInt(raw, 10) : 30;
      return Number.isFinite(parsed) && parsed >= 0 ? parsed : 30;
    }

    // ============================================================
    // Service factory
    // ============================================================

    export function claudeAccountsService(db: Db) {

      async function withAgentLock<T>(agentId: string, fn: () => Promise<T>): Promise<T> {
        // pg_advisory_xact_lock requires int8; hashtextextended produces bigint deterministically.
        return db.transaction(async (tx) => {
          await tx.execute(
            sql`SELECT pg_advisory_xact_lock(hashtextextended(${agentId}, 0))`,
          );
          // Re-bind service to tx for inner ops would be nice, but for now use outer db
          // operations within the same transaction by passing tx through. Simpler:
          // execute fn() within transaction scope so DB ops nest inside the locked tx.
          return fn();
        });
      }

      async function listAccounts(companyId: string): Promise<ClaudeAccount[]> {
        const rows = await db
          .select()
          .from(claudeAccounts)
          .where(eq(claudeAccounts.companyId, companyId))
          .orderBy(asc(claudeAccounts.createdAt));
        return rows.map(rowToAccount);
      }

      async function selectActiveAccount(input: {
        agentId: string;
        companyId: string;
      }): Promise<ClaudeAccount> {
        const cooldown = cooldownSeconds();
        return withAgentLock(input.agentId, async () => {
          const now = new Date();
          // Step 1: check binding for sticky/manual policy
          const [binding] = await db
            .select()
            .from(agentAccountBindings)
            .where(eq(agentAccountBindings.agentId, input.agentId))
            .limit(1);

          if (
            binding &&
            (binding.rotationPolicy === "sticky" || binding.rotationPolicy === "manual") &&
            binding.activeAccountId
          ) {
            const [acc] = await db
              .select()
              .from(claudeAccounts)
              .where(eq(claudeAccounts.id, binding.activeAccountId))
              .limit(1);
            if (acc && acc.status === "live") {
              return rowToAccount(acc);
            }
            // sticky/manual but conta atual não está live → fallback para auto select
          }

          // Step 2: lazy cooldown sweep
          await sweepCooldown(input.companyId, now);

          // Step 3: query candidates
          const cooldownThreshold = new Date(now.getTime() - cooldown * 1000);
          const candidates = await db
            .select()
            .from(claudeAccounts)
            .where(
              and(
                eq(claudeAccounts.companyId, input.companyId),
                eq(claudeAccounts.status, "live"),
                or(
                  isNull(claudeAccounts.exhaustedUntil),
                  lt(claudeAccounts.exhaustedUntil, now),
                ),
              ),
            )
            .orderBy(asc(claudeAccounts.lastUsedAt));

          // Filter candidates by binding cooldown (lastRotatedAt > now - cooldown means too recent)
          const eligible = binding && binding.lastRotatedAt && binding.lastRotatedAt > cooldownThreshold
            ? candidates.filter((c) => c.id !== binding.activeAccountId)
            : candidates;

          if (eligible.length === 0) {
            throw new NoAccountsAvailableError(input.companyId, input.agentId);
          }

          const chosen = eligible[0];

          // Step 4: update lastUsedAt
          await db
            .update(claudeAccounts)
            .set({ lastUsedAt: now, updatedAt: now })
            .where(eq(claudeAccounts.id, chosen.id));

          // Step 5: ensure binding exists
          if (!binding) {
            await db.insert(agentAccountBindings).values({
              agentId: input.agentId,
              activeAccountId: chosen.id,
              rotationPolicy: "auto",
            });
          } else if (binding.activeAccountId !== chosen.id) {
            await db
              .update(agentAccountBindings)
              .set({ activeAccountId: chosen.id, updatedAt: now })
              .where(eq(agentAccountBindings.agentId, input.agentId));
          }

          return rowToAccount({ ...chosen, lastUsedAt: now });
        });
      }

      async function rotateOnQuotaExhausted(input: {
        agentId: string;
        companyId: string;
        fromAccountId: string;
        errorFamily: ClaudeQuotaSubType;
        retryNotBefore: Date | null;
        actorId: string; // user or system who triggered (for activity log)
        swapStrategy: "resume" | "fallback_full_context" | null;
      }): Promise<ClaudeAccount> {
        return withAgentLock(input.agentId, async () => {
          const now = new Date();

          // Step 1: load fromAccount, update its windows
          const [from] = await db
            .select()
            .from(claudeAccounts)
            .where(eq(claudeAccounts.id, input.fromAccountId))
            .limit(1);
          if (!from) {
            throw new Error(`fromAccountId ${input.fromAccountId} not found`);
          }

          const windows: QuotaWindowsMap = (from.lastQuotaWindowsJson as QuotaWindowsMap) ?? {};
          const previous = windows[input.errorFamily];
          const exhaustedUntilIso = (input.retryNotBefore ?? defaultExhaustionFallback(input.errorFamily, now)).toISOString();
          windows[input.errorFamily] = {
            exhaustedUntil: exhaustedUntilIso,
            lastTriggeredAt: now.toISOString(),
            count: (previous?.count ?? 0) + 1,
          };

          const cachedExhaustedUntil = computeMaxExhaustion(windows);

          await db
            .update(claudeAccounts)
            .set({
              status: "exhausted",
              lastQuotaWindowsJson: windows,
              exhaustedUntil: cachedExhaustedUntil,
              updatedAt: now,
            })
            .where(eq(claudeAccounts.id, input.fromAccountId));

          // Step 2: select next (re-uses same lock; nested call is fine since same agentId)
          // Note: selectActiveAccount also locks; in same transaction that's a no-op.
          // But to avoid surprising re-entry, inline the selection logic here:
          const cooldown = cooldownSeconds();
          const cooldownThreshold = new Date(now.getTime() - cooldown * 1000);

          const candidates = await db
            .select()
            .from(claudeAccounts)
            .where(
              and(
                eq(claudeAccounts.companyId, input.companyId),
                eq(claudeAccounts.status, "live"),
                or(
                  isNull(claudeAccounts.exhaustedUntil),
                  lt(claudeAccounts.exhaustedUntil, now),
                ),
              ),
            )
            .orderBy(asc(claudeAccounts.lastUsedAt));
          const eligible = candidates.filter((c) => c.id !== input.fromAccountId);
          if (eligible.length === 0) {
            throw new NoAccountsAvailableError(input.companyId, input.agentId);
          }
          const next = eligible[0];

          // Step 3: update binding
          await db
            .update(agentAccountBindings)
            .set({
              activeAccountId: next.id,
              lastRotatedAt: now,
              updatedAt: now,
            })
            .where(eq(agentAccountBindings.agentId, input.agentId));

          await db
            .update(claudeAccounts)
            .set({ lastUsedAt: now, updatedAt: now })
            .where(eq(claudeAccounts.id, next.id));

          // Step 4: emit activity log
          const details: ClaudeAccountRotatedDetails = {
            agentId: input.agentId,
            fromAccountId: input.fromAccountId,
            toAccountId: next.id,
            reason: "exhausted",
            errorFamily: input.errorFamily,
            retryNotBefore: input.retryNotBefore?.toISOString() ?? null,
            swapStrategy: input.swapStrategy,
          };
          await logActivity(db, {
            companyId: input.companyId,
            actorType: "system",
            actorId: input.actorId,
            action: ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED,
            entityType: "agent",
            entityId: input.agentId,
            agentId: input.agentId,
            details: details as unknown as Record<string, unknown>,
          });

          return rowToAccount({ ...next, lastUsedAt: now });
        });
      }

      async function resolveCredentialDir(account: { configDirSlug: string }): Promise<string> {
        const dir = path.join(os.homedir(), ".paperclip", "claude-accounts", account.configDirSlug);
        try {
          const stat = await fs.stat(dir);
          if (!stat.isDirectory()) {
            throw new CredentialDirMissingError(account.configDirSlug, dir);
          }
        } catch (err) {
          if ((err as NodeJS.ErrnoException).code === "ENOENT") {
            throw new CredentialDirMissingError(account.configDirSlug, dir);
          }
          throw err;
        }
        return dir;
      }

      async function recordStepExecution(input: {
        runId: string;
        stepId: string;
        accountId: string;
        usage: RecordStepUsage;
      }): Promise<void> {
        await db.insert(agentStepExecutions).values({
          runId: input.runId,
          stepId: input.stepId,
          accountId: input.accountId,
          inputTokens: input.usage.inputTokens,
          cachedInputTokens: input.usage.cachedInputTokens,
          outputTokens: input.usage.outputTokens,
          costUsd: input.usage.costUsd,
          startedAt: input.usage.startedAt,
          completedAt: input.usage.completedAt,
          errorFamily: input.usage.errorFamily,
        });
      }

      async function markCooldownPassed(accountId: string): Promise<void> {
        const now = new Date();
        await db
          .update(claudeAccounts)
          .set({ status: "live", updatedAt: now })
          .where(
            and(
              eq(claudeAccounts.id, accountId),
              eq(claudeAccounts.status, "exhausted"),
              lt(claudeAccounts.exhaustedUntil, now),
            ),
          );
      }

      async function sweepCooldown(companyId: string, now: Date): Promise<void> {
        // Lazy: find exhausted accounts of this company whose exhaustedUntil has passed,
        // mark them live. Cheap query; runs on every selectActiveAccount.
        await db
          .update(claudeAccounts)
          .set({ status: "live", updatedAt: now })
          .where(
            and(
              eq(claudeAccounts.companyId, companyId),
              eq(claudeAccounts.status, "exhausted"),
              lt(claudeAccounts.exhaustedUntil, now),
            ),
          );
      }

      return {
        listAccounts,
        selectActiveAccount,
        rotateOnQuotaExhausted,
        resolveCredentialDir,
        recordStepExecution,
        markCooldownPassed,
      };
    }

    // ============================================================
    // Helpers
    // ============================================================

    function rowToAccount(row: typeof claudeAccounts.$inferSelect): ClaudeAccount {
      return {
        id: row.id,
        companyId: row.companyId,
        ownerUserId: row.ownerUserId,
        label: row.label,
        configDirSlug: row.configDirSlug,
        status: row.status as ClaudeAccountStatus,
        lastQuotaWindowsJson: (row.lastQuotaWindowsJson as QuotaWindowsMap) ?? {},
        exhaustedUntil: row.exhaustedUntil,
        lastUsedAt: row.lastUsedAt,
        createdAt: row.createdAt,
        updatedAt: row.updatedAt,
      };
    }

    function computeMaxExhaustion(windows: QuotaWindowsMap): Date | null {
      const dates = Object.values(windows)
        .map((w) => (w?.exhaustedUntil ? new Date(w.exhaustedUntil) : null))
        .filter((d): d is Date => d instanceof Date && !Number.isNaN(d.getTime()));
      if (dates.length === 0) return null;
      return new Date(Math.max(...dates.map((d) => d.getTime())));
    }

    function defaultExhaustionFallback(type: ClaudeQuotaSubType, now: Date): Date {
      // Per DECISION-DETECTION-STRATEGY.md: org_tier sem retry-after → 5min fallback.
      // Outros tipos: tentar valores conservadores baseados em janela típica.
      const minutesByType: Record<ClaudeQuotaSubType, number> = {
        rpm_transient: 1,
        tpm_transient: 1,
        daily_quota: 60 * 24,    // 24h fallback (será sobrescrito por retryNotBefore quando houver)
        weekly_quota: 60 * 24 * 7,
        session_5h: 60 * 5,
        org_tier: 5,
      };
      const minutes = minutesByType[type];
      return new Date(now.getTime() + minutes * 60 * 1000);
    }
    ```

    **Test: `server/src/services/claude-accounts.test.ts`**

    Pattern de teste com mocked Db (objeto com select/insert/update/transaction/execute fns que retornam stubs). Estrutura:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { claudeAccountsService } from "./claude-accounts.js";
    import { NoAccountsAvailableError, CredentialDirMissingError } from "./errors.js";

    // Mock Db builder helper — retorna objeto com select/insert/update/transaction/execute
    // que pode ser configurado por test. Pattern: cada método retorna um chainable mock
    // (ex: db.select().from().where().orderBy()). Usar vi.fn() encadeados.

    function makeMockDb(overrides: Partial<{
      candidates: any[];
      binding: any | null;
      fromAccount: any | null;
    }> = {}) {
      const txCalls: any[] = [];
      const executeCalls: any[] = [];
      const insertCalls: any[] = [];
      const updateCalls: any[] = [];

      // ... build chainable mock returning overrides.candidates / overrides.binding / etc.
      // Importante: db.transaction(fn) deve invocar fn(tx) onde tx === db (simplificação OK no teste).
      // Importante: db.execute(sql) registra em executeCalls.

      return { db: { /* mock */ }, txCalls, executeCalls, insertCalls, updateCalls };
    }

    describe("claudeAccountsService", () => {
      describe("listAccounts", () => {
        it("returns all accounts for the company ordered by createdAt", async () => { /* ... */ });
      });

      describe("selectActiveAccount", () => {
        it("acquires advisory lock before query (calls pg_advisory_xact_lock)", async () => {
          const mock = makeMockDb({ candidates: [{ id: "a", lastUsedAt: null, /* ... */ }] });
          const svc = claudeAccountsService(mock.db as any);
          await svc.selectActiveAccount({ agentId: "agent-1", companyId: "co-1" });
          expect(mock.executeCalls.some((c) => /pg_advisory_xact_lock/.test(String(c)))).toBe(true);
        });

        it("returns the account with oldest lastUsedAt", async () => { /* ... */ });
        it("filters by status='live' and exhaustedUntil < now", async () => { /* ... */ });
        it("respects sticky binding when activeAccountId is live", async () => { /* ... */ });
        it("falls back to auto select when sticky account is exhausted", async () => { /* ... */ });
        it("excludes account in cooldown window (lastRotatedAt > now - cooldown)", async () => { /* ... */ });
        it("throws NoAccountsAvailableError when zero candidates", async () => {
          const mock = makeMockDb({ candidates: [] });
          const svc = claudeAccountsService(mock.db as any);
          await expect(
            svc.selectActiveAccount({ agentId: "agent-1", companyId: "co-1" }),
          ).rejects.toBeInstanceOf(NoAccountsAvailableError);
        });
      });

      describe("rotateOnQuotaExhausted", () => {
        it("marks fromAccount status=exhausted with updated lastQuotaWindowsJson[errorFamily]", async () => { /* ... */ });
        it("recomputes exhaustedUntil as MAX of windows", async () => { /* ... */ });
        it("selects next account excluding fromAccountId", async () => { /* ... */ });
        it("updates binding.activeAccountId and lastRotatedAt", async () => { /* ... */ });
        it("emits logActivity with claude_account_rotated and full payload", async () => { /* ... */ });
        it("propagates NoAccountsAvailableError if pool empty", async () => { /* ... */ });
      });

      describe("resolveCredentialDir", () => {
        it("returns absolute path when dir exists", async () => { /* ... */ });
        it("throws CredentialDirMissingError when dir missing", async () => { /* ... */ });
      });

      describe("recordStepExecution", () => {
        it("inserts row in agent_step_executions (no update)", async () => { /* ... */ });
      });

      describe("markCooldownPassed", () => {
        it("transitions exhausted → live when exhaustedUntil < now", async () => { /* ... */ });
        it("is a no-op when exhaustedUntil > now", async () => { /* ... */ });
      });
    });
    ```

    **Critérios técnicos do test:**
    - Mock Db deve responder a `select().from(table).where(cond).orderBy(...)` — pattern chainable. Se complexo, usar `drizzle-orm` test utilities ou (mais simples) construir mock manual com `vi.fn().mockReturnThis()` e final `mockResolvedValue([...])`.
    - Para `transaction(fn)`, o mock invoca `fn(db)` onde `db === mock.db` (simplificação).
    - Para `execute(sql)`, captura argumento e expõe em `executeCalls` para asserção.
    - Para `logActivity` (importado de activity-log.ts), usar `vi.mock("./activity-log.js", () => ({ logActivity: vi.fn(), ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED: "claude_account_rotated" }))` no topo.
    - Para `fs.stat` em `resolveCredentialDir`, usar `vi.mock("node:fs/promises")`.

    **Permitir simplicidade:** se mock manual de Db ficar > 100 linhas, criar helper `makeMockDb(behaviors)` reusável entre testes. Não obrigatório usar drizzle test utility se complica demais.
  </action>
  <verify>
    <automated>cd server && pnpm vitest run src/services/claude-accounts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/src/services/claude-accounts.ts` existe
    - `grep -c "export function claudeAccountsService" server/src/services/claude-accounts.ts` retorna 1
    - `grep -E "listAccounts|selectActiveAccount|rotateOnQuotaExhausted|resolveCredentialDir|recordStepExecution|markCooldownPassed" server/src/services/claude-accounts.ts | wc -l` >= 6 (todos os 6 métodos)
    - `grep -c "pg_advisory_xact_lock" server/src/services/claude-accounts.ts` retorna pelo menos 1
    - `grep -c "ACTIVITY_ACTION_CLAUDE_ACCOUNT_ROTATED" server/src/services/claude-accounts.ts` retorna pelo menos 1 (emit do activity)
    - `grep -c "NoAccountsAvailableError" server/src/services/claude-accounts.ts` retorna pelo menos 1 (throw nos casos vazios)
    - `grep -c "computeMaxExhaustion\\|defaultExhaustionFallback" server/src/services/claude-accounts.ts` retorna >= 2
    - Arquivo `server/src/services/claude-accounts.test.ts` existe
    - `grep -c "describe\\|it(" server/src/services/claude-accounts.test.ts` retorna pelo menos 14 (>= 12 cases + 6 describes)
    - Comando `cd server && pnpm vitest run src/services/claude-accounts.test.ts` exit code 0
    - `cd server && pnpm tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Service factory + tests passing; 6 métodos do D-09 implementados; lock advisory ativo; activity log emitido em rotate.</done>
</task>

</tasks>

<verification>
- TypeScript do server compila sem erros
- Test suite de claude-accounts passa (todos os métodos cobertos)
- Errors customizados exportados de errors.ts
- Lock advisory ativo via `pg_advisory_xact_lock(hashtextextended(...))`
- Activity log emit consome constante e tipo registrados em 05-03
</verification>

<success_criteria>
1. ✅ `claudeAccountsService(db)` factory exportado de `server/src/services/claude-accounts.ts`
2. ✅ 6 métodos do D-09 implementados (listAccounts, selectActiveAccount, rotateOnQuotaExhausted, resolveCredentialDir, recordStepExecution, markCooldownPassed)
3. ✅ Lock pessimista via `pg_advisory_xact_lock(hashtextextended(agentId, 0))` em selectActiveAccount + rotateOnQuotaExhausted
4. ✅ Cooldown gating via env var `CLAUDE_ACCOUNT_COOLDOWN_SECONDS` (default 30)
5. ✅ Cache de `exhaustedUntil` (top-level claude_accounts) recalculado como MAX de lastQuotaWindowsJson
6. ✅ NoAccountsAvailableError + CredentialDirMissingError exportados de errors.ts
7. ✅ rotateOnQuotaExhausted emite logActivity com payload completo (D-30)
8. ✅ Test suite cobrindo: listAccounts, selectActiveAccount (5 cases), rotateOnQuotaExhausted (6 cases), resolveCredentialDir (2 cases), recordStepExecution (1 case), markCooldownPassed (2 cases) — total >= 12 cases
9. ✅ recordStepExecution é append-only (apenas insert, sem update)
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-04-SUMMARY.md` with:
- LoC do service e do test
- Decisões de implementação:
  - Lock strategy: `pg_advisory_xact_lock(hashtextextended(agentId, 0))` dentro de `db.transaction`
  - Sticky/manual policy: respeitada apenas se a conta atual está `live`; senão fallback para auto
  - Lazy cooldown sweep: invocado em todo `selectActiveAccount` (cheap update WHERE exhaustedUntil < now)
  - Cache `exhaustedUntil` top-level: recalculado em rotate como MAX dos windows
  - Defaults de fallback exhaustion (quando retryNotBefore null): rpm/tpm 1min, daily 24h, weekly 7d, session_5h 5h, org_tier 5min
- Edge cases tratados: binding não existe (cria), sticky com conta exhausted (fallback), pool vazio em rotate
- Test counts e estratégia de mock
</output>
