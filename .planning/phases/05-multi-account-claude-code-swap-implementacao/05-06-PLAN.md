---
phase: 05-multi-account-claude-code-swap-implementacao
plan: 06
type: execute
wave: 3
depends_on: [05-04, 05-05]
files_modified:
  - server/src/services/heartbeat.ts
  - server/src/services/claude-accounts-swap.ts
  - server/src/services/__tests__/claude-accounts-swap.test.ts
autonomous: true
requirements: [MULTI-07, MULTI-08]

must_haves:
  truths:
    - "Antes de cada spawn de adapter Claude (adapterType === 'claude_local'), heartbeat chama claudeAccountsService.selectActiveAccount → resolveCredentialDir e injeta resultado em runtimeConfig.claudeConfigDir"
    - "Quando adapterResult retorna errorFamily='transient_upstream' E adapter é claude_local, heartbeat chama orchestrateClaudeSwap que detecta sub-tipo via detectClaudeQuotaExhausted, chama rotateOnQuotaExhausted, captura continuation summary, e re-tenta o step com a nova conta"
    - "Limite de 1 rotação por step enforced (D-19): se a re-tentativa também falha com transient_upstream, step marca permanent failure"
    - "swapStrategy registrada na activity log: tenta 'resume' primeiro, fallback 'fallback_full_context' (D-20, D-21)"
    - "Falha de cross-account resume detectada via stderr/stdout pattern (D-22): 'session not found' / 'invalid session id' / exit != 0 antes de system:init"
    - "Tests vitest cobrem: selectActiveAccount injection, swap on transient_upstream, swap quota cap, resume detect-fail → fallback, MULTI-08 max-1-rotation guard"
  artifacts:
    - path: "server/src/services/claude-accounts-swap.ts"
      provides: "Orchestration logic isolada (drainStep, captureSummary, attemptResume, fallbackFullContext)"
      contains: "export async function orchestrateClaudeSwap"
    - path: "server/src/services/heartbeat.ts"
      provides: "Wiring de selectActiveAccount + orchestrateClaudeSwap em torno do adapter.execute"
      contains: "claudeAccountsService"
    - path: "server/src/services/__tests__/claude-accounts-swap.test.ts"
      provides: "Tests da orquestração isolada (sem dependência do heartbeat completo)"
      contains: "describe.*orchestrateClaudeSwap"
  key_links:
    - from: "server/src/services/heartbeat.ts"
      to: "claudeAccountsService (de 05-04)"
      via: "import { claudeAccountsService }"
      pattern: "import.*claudeAccountsService.*from.*claude-accounts"
    - from: "server/src/services/heartbeat.ts (linha ~5302 antes adapter.execute)"
      to: "selectActiveAccount + resolveCredentialDir"
      via: "call gated by agent.adapterType === 'claude_local'"
      pattern: "adapterType.*claude_local"
    - from: "server/src/services/claude-accounts-swap.ts"
      to: "detectClaudeQuotaExhausted (de 05-02)"
      via: "import from @paperclipai/adapter-claude-local/server"
      pattern: "detectClaudeQuotaExhausted"
    - from: "server/src/services/claude-accounts-swap.ts"
      to: "issue-continuation-summary.ts (existing primitive)"
      via: "fetchIssueContinuationSummaryDocument or equivalent helper"
      pattern: "issue.*continuation.*summary|ISSUE_CONTINUATION_SUMMARY"
---

<objective>
Integrar o serviço de contas (05-04) ao loop principal do heartbeat: antes de cada spawn de adapter `claude_local`, selecionar conta ativa e injetar `claudeConfigDir` no runtime config (MULTI-07). Quando o adapter retorna `errorFamily: 'transient_upstream'`, orquestrar o swap automático com Plano B (MULTI-08): detectar sub-tipo, rotacionar conta, capturar continuation summary, tentar resume na conta nova, fallback full-context se resume falhar. Guardar 1 rotação por step (D-19).

Purpose: Esta é a feature núcleo que justifica a Phase 5. Sem isso, todas as outras peças (schemas, service, classifier, adapter wiring, UI) ficam desconectadas. Plan B é confirmado pelo usuário como estratégia padrão até UAT-04-03 destravar refinamento empírico.

Output: Module isolado `claude-accounts-swap.ts` (~200 linhas), pequenos diffs cirúrgicos em `heartbeat.ts` (~30 linhas em 2 pontos), test suite focada na orquestração (~150 linhas).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md
@server/src/services/claude-accounts-swap.md
@server/src/services/heartbeat.ts
@server/src/services/issue-continuation-summary.ts
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-04-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-05-PLAN.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-02-PLAN.md

<interfaces>
<!-- Service do 05-04: -->
```typescript
import { claudeAccountsService, type ClaudeAccount, type ClaudeQuotaSubType, type RecordStepUsage } from "./claude-accounts.js";

const svc = claudeAccountsService(db);
const account = await svc.selectActiveAccount({ agentId, companyId });
const dir = await svc.resolveCredentialDir(account); // throws CredentialDirMissingError
const next = await svc.rotateOnQuotaExhausted({ agentId, companyId, fromAccountId, errorFamily, retryNotBefore, actorId, swapStrategy });
await svc.recordStepExecution({ runId, stepId, accountId, usage });
```

<!-- Detector do 05-02: -->
```typescript
import { detectClaudeQuotaExhausted, type ClaudeQuotaType, type ClaudeQuotaDetection } from "@paperclipai/adapter-claude-local/server";

const detection = detectClaudeQuotaExhausted({ stdout, stderr });
// detection.type: 'rpm_transient' | ... | null
```

<!-- Heartbeat existing relevant lines: -->
<!-- Line 5302: const adapter = getServerAdapter(agent.adapterType); -->
<!-- Line 5317-5340: const adapterResult = await adapter.execute({ ... runtime: runtimeForAdapter, config: runtimeConfig, ... }); -->
<!-- Line 188-225: errorFamily/transient_upstream handling already exists; transientRecovery flow uses errorFamily and retryNotBefore -->
<!-- Line 240: adapterType === "claude_local" check pattern existe -->

<!-- Continuation summary existing primitive: -->
<!-- ISSUE_CONTINUATION_SUMMARY_DOCUMENT_KEY (imported em heartbeat.ts:10) -->
<!-- issue-continuation-summary.ts existe — lê da issueDocuments table; campo é texto. -->

<!-- D-22 detection pattern para resume failure: -->
<!-- - stderr regex: /session not found|invalid session id/i -->
<!-- - OU exit code !== 0 antes de system:init event no stdout JSON stream -->

<!-- Plano B (D-21): spawn nova sessão com prompt: -->
<!-- "Continuation of prior work (account swap due to quota exhaustion). -->
<!--  Previous progress summary: <continuation_summary>. -->
<!--  Resume from: <last instruction>." -->

<!-- Heartbeat já tem retry logic (linhas 3427+, 3447+, 3513+): pattern transientRecovery -->
<!-- O swap é tratado como variante de transient recovery: em vez de só retry mesmo agente, -->
<!-- swap conta E retry. Lógica adicional não substitui retry; complementa. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar orchestrateClaudeSwap module isolado</name>
  <files>
    server/src/services/claude-accounts-swap.ts,
    server/src/services/__tests__/claude-accounts-swap.test.ts
  </files>
  <read_first>
    - server/src/services/claude-accounts-swap.md (de 05-03 — estratégia documentada)
    - server/src/services/issue-continuation-summary.ts (linhas 1-50 — entender API: getIssueContinuationSummary ou equivalente)
    - server/src/services/claude-accounts.ts (de 05-04 — assinatura completa do service)
    - packages/adapters/claude-local/src/server/parse.ts (de 05-02 — assinatura do detector)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-19, D-20, D-21, D-22, D-24)
    - server/src/services/heartbeat-stop-metadata.test.ts (pattern de test setup)
  </read_first>
  <behavior>
    - Test 1: orchestrateClaudeSwap retorna `{ swapped: false, reason: 'no_detection' }` quando detectClaudeQuotaExhausted.detected === false
    - Test 2: orchestrateClaudeSwap chama rotateOnQuotaExhausted com sub-tipo correto e retryAt do detector
    - Test 3: orchestrateClaudeSwap recolhe continuation summary via primitive existente
    - Test 4: orchestrateClaudeSwap tenta Strategy A (resume) primeiro; passa session_id e new credential dir ao retry callback
    - Test 5: orchestrateClaudeSwap detecta failure de resume via output pattern (`session not found`) e roteia para Strategy B
    - Test 6: Strategy B constrói prompt full-context com summary embebido e chama retry callback com prompt customizado
    - Test 7: Quando rotateOnQuotaExhausted lança NoAccountsAvailableError, orchestrate retorna `{ swapped: false, reason: 'pool_empty' }` (caller decide o que fazer)
    - Test 8: max-1-rotation guard: se called com flag `alreadyRotated: true`, retorna `{ swapped: false, reason: 'rotation_cap_reached' }` sem invocar service
  </behavior>
  <action>
    **Module: `server/src/services/claude-accounts-swap.ts`**

    Function signature (designed para ser testável isoladamente, sem heartbeat completo):

    ```typescript
    import type { Db } from "@paperclipai/db";
    import {
      claudeAccountsService,
      type ClaudeAccount,
      type ClaudeQuotaSubType,
    } from "./claude-accounts.js";
    import { NoAccountsAvailableError } from "./errors.js";
    import { detectClaudeQuotaExhausted } from "@paperclipai/adapter-claude-local/server";

    /**
     * MULTI-08 (Phase 5): Claude account swap orchestration.
     * See: server/src/services/claude-accounts-swap.md for design rationale.
     *
     * Flow:
     *   1. Classify exhaustion via detectClaudeQuotaExhausted
     *   2. Rotate via claudeAccountsService.rotateOnQuotaExhausted
     *   3. Capture continuation summary (issue-continuation-summary primitive)
     *   4. Try Strategy A: resume with --resume <session_id> on new account
     *   5. Detect resume failure (D-22), fall back to Strategy B (full-context re-prompt)
     *   6. Return result for caller (heartbeat) to invoke retry with new account + prompt
     */

    export type SwapReason =
      | "swapped"
      | "no_detection"
      | "pool_empty"
      | "rotation_cap_reached"
      | "missing_credentials";

    export type SwapStrategy = "resume" | "fallback_full_context";

    export interface OrchestrateClaudeSwapInput {
      db: Db;
      agentId: string;
      companyId: string;
      runId: string;
      stepId: string;
      fromAccountId: string;
      previousSessionId: string | null; // session_id do step que falhou
      adapterStdout: string;
      adapterStderr: string;
      continuationSummary: string | null;     // null se issue não tem summary ainda
      alreadyRotatedThisStep: boolean;        // D-19 guard
      actorId: string;
    }

    export interface OrchestrateClaudeSwapResult {
      swapped: boolean;
      reason: SwapReason;
      newAccount: ClaudeAccount | null;
      newCredentialDir: string | null;
      strategy: SwapStrategy | null;
      retryPrompt: string | null;             // null para Strategy A; string para Strategy B
      retrySessionId: string | null;          // previousSessionId para Strategy A; null para Strategy B
      detectedType: ClaudeQuotaSubType | null;
    }

    export async function orchestrateClaudeSwap(
      input: OrchestrateClaudeSwapInput,
    ): Promise<OrchestrateClaudeSwapResult> {
      // D-19: Guard — no more than 1 rotation per step.
      if (input.alreadyRotatedThisStep) {
        return emptyResult("rotation_cap_reached");
      }

      // Step 1: Classify
      const detection = detectClaudeQuotaExhausted({
        stdout: input.adapterStdout,
        stderr: input.adapterStderr,
      });
      if (!detection.detected || detection.type === null) {
        return emptyResult("no_detection");
      }

      const svc = claudeAccountsService(input.db);

      // Step 2: Rotate (with planned strategy='resume' first; may be rewritten to 'fallback_full_context' downstream)
      let nextAccount: ClaudeAccount;
      try {
        nextAccount = await svc.rotateOnQuotaExhausted({
          agentId: input.agentId,
          companyId: input.companyId,
          fromAccountId: input.fromAccountId,
          errorFamily: detection.type,
          retryNotBefore: detection.retryAt,
          actorId: input.actorId,
          swapStrategy: "resume", // optimistic; fallback writes another activity entry if used
        });
      } catch (err) {
        if (err instanceof NoAccountsAvailableError) {
          return emptyResult("pool_empty");
        }
        throw err;
      }

      // Step 3: Resolve credential dir for new account
      let newCredentialDir: string;
      try {
        newCredentialDir = await svc.resolveCredentialDir(nextAccount);
      } catch (err) {
        // Conta foi marcada ativa mas operador não criou o dir → operacional, não código.
        return {
          swapped: true, // rotation persisted, but spawn impossível agora
          reason: "missing_credentials",
          newAccount: nextAccount,
          newCredentialDir: null,
          strategy: null,
          retryPrompt: null,
          retrySessionId: null,
          detectedType: detection.type,
        };
      }

      // Step 4 & 5: Build retry plan
      // Default to Strategy B (Plano B confirmed by user; Strategy A will be tested by caller)
      // The caller invokes adapter.execute with retrySessionId; if that spawn detects
      // "session not found" pattern, caller calls finalizeFallbackToFullContext to switch.
      // OR — simpler design: orchestrateClaudeSwap returns Strategy A by default,
      // and the caller's retry-detector decides to call again with strategy='fallback_full_context'.

      // Por simplicidade no v1, retornar Strategy A (resume com previousSessionId);
      // caller (heartbeat) detecta failure no retry e invoca orchestrateFallback() separadamente.
      return {
        swapped: true,
        reason: "swapped",
        newAccount: nextAccount,
        newCredentialDir,
        strategy: "resume",
        retryPrompt: null,
        retrySessionId: input.previousSessionId,
        detectedType: detection.type,
      };
    }

    /**
     * Called by heartbeat when Strategy A failed (detected post-spawn).
     * Constructs Strategy B prompt and emits supplemental activity log entry.
     */
    export async function orchestrateFallbackFullContext(input: {
      db: Db;
      agentId: string;
      companyId: string;
      newAccount: ClaudeAccount;
      newCredentialDir: string;
      continuationSummary: string | null;
      lastInstruction: string | null;
      actorId: string;
    }): Promise<{ retryPrompt: string; strategy: SwapStrategy }> {
      const summary = input.continuationSummary?.trim() ?? "(no continuation summary captured before swap)";
      const lastInstr = input.lastInstruction?.trim() ?? "(no recorded last instruction; agent should re-read the issue)";

      const retryPrompt = [
        "Continuation of prior work (account swap due to Claude quota exhaustion).",
        "",
        "Previous progress summary:",
        summary,
        "",
        "Resume from:",
        lastInstr,
      ].join("\n");

      // Note: rotateOnQuotaExhausted emits the primary activity entry with swapStrategy='resume'.
      // For visibility of the actual strategy used, emit a follow-up entry. (Or, simpler v1:
      // just log to heartbeat run events; activity log entry can stay 'resume' as initial intent.)
      // TODO: refine post-UAT-04-03 if user wants strict accuracy on swapStrategy field.

      return { retryPrompt, strategy: "fallback_full_context" };
    }

    /**
     * Pattern detector for Strategy A failure (D-22).
     * Returns true if stdout/stderr indicates the resume failed.
     */
    export function detectResumeFailed(input: {
      stdout: string;
      stderr: string;
      exitCode: number | null;
    }): boolean {
      const blob = `${input.stdout}\n${input.stderr}`;
      if (/session\s+not\s+found|invalid\s+session\s+id|unknown\s+session/i.test(blob)) {
        return true;
      }
      // Exit code != 0 BEFORE any "system":"init" event in stdout JSON stream
      if (input.exitCode !== null && input.exitCode !== 0) {
        const hasInit = /"type":\s*"system"[\s\S]*?"subtype":\s*"init"/.test(input.stdout);
        if (!hasInit) return true;
      }
      return false;
    }

    function emptyResult(reason: SwapReason): OrchestrateClaudeSwapResult {
      return {
        swapped: false,
        reason,
        newAccount: null,
        newCredentialDir: null,
        strategy: null,
        retryPrompt: null,
        retrySessionId: null,
        detectedType: null,
      };
    }
    ```

    **Test: `server/src/services/__tests__/claude-accounts-swap.test.ts`**

    Mock the underlying svc and detector. Pattern:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { orchestrateClaudeSwap, orchestrateFallbackFullContext, detectResumeFailed } from "../claude-accounts-swap.js";
    import { NoAccountsAvailableError } from "../errors.js";

    vi.mock("../claude-accounts.js", () => {
      const mockRotate = vi.fn();
      const mockResolveDir = vi.fn();
      return {
        claudeAccountsService: () => ({
          rotateOnQuotaExhausted: mockRotate,
          resolveCredentialDir: mockResolveDir,
          // outros métodos mock conforme necessário
        }),
        // re-export types as themselves for test type compatibility
      };
    });

    vi.mock("@paperclipai/adapter-claude-local/server", () => ({
      detectClaudeQuotaExhausted: vi.fn(),
    }));

    // ... tests baseados nos behaviors enumerados
    ```

    **8 cases mínimos** conforme `<behavior>` block acima.

    **Notas:**
    - Test não precisa de Db real — mock passes through.
    - Strategy A path é o "default" do orchestrateClaudeSwap; Strategy B é triggered separadamente pelo caller via orchestrateFallbackFullContext.
    - detectResumeFailed é função pura; teste-a com 4-5 cases (vários patterns de stdout/stderr/exitCode).
  </action>
  <verify>
    <automated>cd server && pnpm vitest run src/services/__tests__/claude-accounts-swap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/src/services/claude-accounts-swap.ts` existe
    - `grep -c "export async function orchestrateClaudeSwap" server/src/services/claude-accounts-swap.ts` retorna 1
    - `grep -c "export async function orchestrateFallbackFullContext" server/src/services/claude-accounts-swap.ts` retorna 1
    - `grep -c "export function detectResumeFailed" server/src/services/claude-accounts-swap.ts` retorna 1
    - `grep -c "alreadyRotatedThisStep" server/src/services/claude-accounts-swap.ts` retorna pelo menos 1 (D-19 guard)
    - `grep -c "rotation_cap_reached\\|pool_empty\\|missing_credentials\\|no_detection" server/src/services/claude-accounts-swap.ts` retorna >= 4 (todos os reasons)
    - `grep -c "session\\s\\+not\\s\\+found\\|invalid\\s\\+session" server/src/services/claude-accounts-swap.ts` retorna >= 1 (D-22 patterns)
    - `grep -c "fallback_full_context" server/src/services/claude-accounts-swap.ts` retorna >= 2
    - Arquivo `server/src/services/__tests__/claude-accounts-swap.test.ts` existe
    - `grep -c "it\\(" server/src/services/__tests__/claude-accounts-swap.test.ts` retorna pelo menos 8
    - `cd server && pnpm vitest run src/services/__tests__/claude-accounts-swap.test.ts` exit code 0
    - `cd server && pnpm tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Module orchestrate isolado + tests passando; lógica de Strategy A/B clara; D-19 guard explícito.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Wire heartbeat.ts — selectActiveAccount + orchestrateClaudeSwap em torno de adapter.execute</name>
  <files>server/src/services/heartbeat.ts</files>
  <read_first>
    - server/src/services/heartbeat.ts (linhas 1-100 — imports atuais; linhas 5280-5500 — adapter.execute call e error handling; linhas 188-225 — errorFamily/transient_upstream patterns; linhas 3427-3520 — transientRecovery flow existing; linhas 240 — claude_local check pattern)
    - server/src/services/claude-accounts.ts (de 05-04 — API)
    - server/src/services/claude-accounts-swap.ts (de Tarefa 1 — orchestrateClaudeSwap signature)
    - server/src/services/issue-continuation-summary.ts (existing — verificar como ler summary de uma issue)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-18, D-19)
  </read_first>
  <behavior>
    - Test 1 (selectActiveAccount injection): quando agent.adapterType === 'claude_local' e companyId/agentId conhecido, runtimeConfig é augmented com claudeConfigDir antes de adapter.execute
    - Test 2 (non-claude adapters): quando adapterType !== 'claude_local', selectActiveAccount NÃO é chamado; runtimeConfig passes através inalterado
    - Test 3 (transient_upstream → swap): quando adapterResult.errorFamily === 'transient_upstream' e adapter é claude_local, orchestrateClaudeSwap é invocado com inputs corretos; se swapped, heartbeat invoca retry com new credential dir
    - Test 4 (max 1 rotation per step): segunda invocação de orchestrateClaudeSwap dentro do mesmo step recebe alreadyRotatedThisStep=true
    - Test 5 (recordStepExecution): após cada adapter.execute (sucesso ou falha) com claude_local, heartbeat chama recordStepExecution com tokens/cost extraídos do adapterResult
  </behavior>
  <action>
    **NÃO** reescrever heartbeat.ts. Mudanças cirúrgicas em 3 pontos:

    **Ponto A: imports (próximo aos imports existentes em linhas 1-65)**

    ```typescript
    import {
      claudeAccountsService,
      type ClaudeAccount,
    } from "./claude-accounts.js";
    import {
      orchestrateClaudeSwap,
      orchestrateFallbackFullContext,
      detectResumeFailed,
    } from "./claude-accounts-swap.js";
    ```

    **Ponto B: antes de `adapter.execute()` (próximo a linha 5302-5317)**

    Inserir antes de `const adapterResult = await adapter.execute({...})`:

    ```typescript
    // MULTI-07 (Phase 5): inject claudeConfigDir for claude_local adapter.
    // selectActiveAccount picks the eligible account; resolveCredentialDir validates dir exists.
    let selectedAccount: ClaudeAccount | null = null;
    let augmentedConfig = runtimeConfig;
    if (agent.adapterType === "claude_local") {
      try {
        const acctSvc = claudeAccountsService(db);
        selectedAccount = await acctSvc.selectActiveAccount({
          agentId: agent.id,
          companyId: agent.companyId,
        });
        const credDir = await acctSvc.resolveCredentialDir(selectedAccount);
        augmentedConfig = { ...runtimeConfig, claudeConfigDir: credDir };
      } catch (err) {
        // NoAccountsAvailableError or CredentialDirMissingError — log and continue
        // with original runtimeConfig (relies on process.env CLAUDE_CONFIG_DIR fallback).
        // Operator visibility via stdout log.
        await onLog(
          "stderr",
          `[paperclip] Multi-account: ${err instanceof Error ? err.message : String(err)} — falling back to process env\n`,
        );
      }
    }
    ```

    Then change `await adapter.execute({...})` to use `config: augmentedConfig` (in place of `config: runtimeConfig`).

    **Ponto C: post adapter.execute, error handling for transient_upstream (próximo a linha 5483 onde errorFamily é referenciado)**

    Após o assignment de `adapterResult` e antes do retry loop existente (transientRecovery), adicionar bloco gated por `adapterType === "claude_local"`:

    ```typescript
    // MULTI-08 (Phase 5): orchestrate swap on Claude quota exhaustion.
    // Runs ONCE per step (D-19); flag tracked via local boolean.
    let alreadyRotatedThisStep = false;
    let workingAdapterResult = adapterResult;
    let workingCredentialDir: string | null = augmentedConfig?.claudeConfigDir ?? null;

    if (
      agent.adapterType === "claude_local" &&
      workingAdapterResult.errorFamily === "transient_upstream" &&
      selectedAccount &&
      !alreadyRotatedThisStep
    ) {
      const continuationSummary = await readIssueContinuationSummary(db, issueRef?.id ?? null); // helper inline ou de issue-continuation-summary.ts
      const swapResult = await orchestrateClaudeSwap({
        db,
        agentId: agent.id,
        companyId: agent.companyId,
        runId: run.id,
        stepId: String(seq), // ou identificador equivalente do step
        fromAccountId: selectedAccount.id,
        previousSessionId: workingAdapterResult.sessionId ?? null,
        adapterStdout: workingAdapterResult.stdoutExcerpt ?? "",
        adapterStderr: workingAdapterResult.stderrExcerpt ?? "",
        continuationSummary,
        alreadyRotatedThisStep: false,
        actorId: "system",
      });

      if (swapResult.swapped && swapResult.newCredentialDir) {
        alreadyRotatedThisStep = true;
        workingCredentialDir = swapResult.newCredentialDir;
        // Retry adapter.execute with new credential dir + Strategy A (resume previous session)
        const retryConfig = {
          ...augmentedConfig,
          claudeConfigDir: swapResult.newCredentialDir,
          // se Strategy A: passar resumeSessionId; adapter pode aceitar ou ignorar
          ...(swapResult.retrySessionId ? { resumeSessionId: swapResult.retrySessionId } : {}),
          ...(swapResult.retryPrompt ? { initialPrompt: swapResult.retryPrompt } : {}),
        };
        const retryResult = await adapter.execute({
          // ... mesmos params, mas config: retryConfig
          runId: run.id,
          agent,
          runtime: runtimeForAdapter,
          config: retryConfig,
          context,
          executionTarget,
          executionTransport: remoteExecution
            ? { remoteExecution: remoteExecution as unknown as Record<string, unknown> }
            : undefined,
          onLog,
          onMeta: onAdapterMeta,
          onSpawn: async (meta) => {
            await persistRunProcessMetadata(run.id, {
              pid: meta.pid,
              processGroupId: "processGroupId" in meta && typeof meta.processGroupId === "number" ? meta.processGroupId : null,
              startedAt: meta.startedAt,
            });
          },
          authToken: authToken ?? undefined,
        });

        // Detect Strategy A failure (D-22) → fall back to Strategy B
        if (
          swapResult.strategy === "resume" &&
          detectResumeFailed({
            stdout: retryResult.stdoutExcerpt ?? "",
            stderr: retryResult.stderrExcerpt ?? "",
            exitCode: retryResult.exitCode ?? null,
          })
        ) {
          const fallbackPlan = await orchestrateFallbackFullContext({
            db,
            agentId: agent.id,
            companyId: agent.companyId,
            newAccount: swapResult.newAccount!,
            newCredentialDir: swapResult.newCredentialDir,
            continuationSummary,
            lastInstruction: null, // pode-se enriquecer com último user message do issue se desejado
            actorId: "system",
          });
          // Retry novamente com Strategy B (NÃO conta como rotação adicional — D-19 atende ao 1 rotation max; este é re-spawn da mesma rotação)
          const fallbackConfig = {
            ...retryConfig,
            initialPrompt: fallbackPlan.retryPrompt,
            resumeSessionId: undefined, // limpar Strategy A flag
          };
          workingAdapterResult = await adapter.execute({
            // ... mesmos params, mas config: fallbackConfig
            runId: run.id,
            agent,
            runtime: runtimeForAdapter,
            config: fallbackConfig,
            context,
            executionTarget,
            executionTransport: remoteExecution
              ? { remoteExecution: remoteExecution as unknown as Record<string, unknown> }
              : undefined,
            onLog,
            onMeta: onAdapterMeta,
            onSpawn: async (meta) => {
              await persistRunProcessMetadata(run.id, {
                pid: meta.pid,
                processGroupId: "processGroupId" in meta && typeof meta.processGroupId === "number" ? meta.processGroupId : null,
                startedAt: meta.startedAt,
              });
            },
            authToken: authToken ?? undefined,
          });
        } else {
          workingAdapterResult = retryResult;
        }
      }
    }

    // Replace `adapterResult` references downstream com `workingAdapterResult`
    // OU simplesmente: const adapterResult = workingAdapterResult; (após o bloco)
    ```

    **Ponto D: recordStepExecution (após adapterResult final, antes de costService consume usage)**

    ```typescript
    if (agent.adapterType === "claude_local" && selectedAccount) {
      const acctSvc = claudeAccountsService(db);
      await acctSvc.recordStepExecution({
        runId: run.id,
        stepId: String(seq), // mesmo step identifier
        accountId: workingCredentialDir
          ? selectedAccount.id // ou newAccount.id se rotated; tracking via swapResult
          : selectedAccount.id,
        usage: {
          inputTokens: workingAdapterResult.usage?.inputTokens ?? 0,
          cachedInputTokens: workingAdapterResult.usage?.cachedInputTokens ?? 0,
          outputTokens: workingAdapterResult.usage?.outputTokens ?? 0,
          costUsd: 0, // ou derivar do costService — manter 0 v1 se cost calc não disponível aqui
          startedAt: new Date(/* spawn time */),
          completedAt: new Date(),
          errorFamily: workingAdapterResult.errorFamily ?? null,
        },
      });
    }
    ```

    **NÃO modificar:**
    - O retry/transientRecovery existing flow (linhas 3427+) — opera independentemente; nosso swap é DENTRO de uma única invocação de adapter.execute (rotation cap = 1 por step).
    - Outras chamadas de adapter.execute em outros pontos do heartbeat (apenas a principal do agent run; busca `adapter.execute` para confirmar).

    **Critério crítico:** o bloco entre Ponto B e Ponto D deve ser **gated** por `agent.adapterType === "claude_local"`. Outros adapters (codex_local, gemini, etc.) NÃO são afetados — comportamento atual preservado.

    **Erro acceptance:** se `selectedAccount` é null (errors no Ponto B), bloco de swap (Ponto C) é skipped; bloco de recordStepExecution (Ponto D) também skipped. Heartbeat continua com comportamento atual (pré-multi-account).
  </action>
  <verify>
    <automated>cd server && pnpm tsc --noEmit && pnpm vitest run src/__tests__/heartbeat 2>/dev/null || cd server && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccountsService" server/src/services/heartbeat.ts` retorna pelo menos 2 (import + uso)
    - `grep -c "orchestrateClaudeSwap" server/src/services/heartbeat.ts` retorna pelo menos 2 (import + chamada)
    - `grep -c "detectResumeFailed" server/src/services/heartbeat.ts` retorna pelo menos 1
    - `grep -c "alreadyRotatedThisStep\\|rotation_cap_reached" server/src/services/heartbeat.ts` retorna pelo menos 1 (D-19 guard explícito)
    - `grep -B2 "selectActiveAccount" server/src/services/heartbeat.ts | grep -q "claude_local"` casa (gating ativo)
    - `grep -c "recordStepExecution" server/src/services/heartbeat.ts` retorna pelo menos 1
    - `grep -c "MULTI-07\\|MULTI-08" server/src/services/heartbeat.ts` retorna pelo menos 2 (rastreabilidade nos comentários)
    - `cd server && pnpm tsc --noEmit` exit code 0
    - Tests existentes do server (se vitest workspace inclui heartbeat) continuam passando: `cd server && pnpm vitest run src/services/heartbeat-stop-metadata.test.ts` exit code 0 (sanity check de não-regressão)
  </acceptance_criteria>
  <done>Heartbeat injeta claudeConfigDir para claude_local; orquestra swap em transient_upstream; max 1 rotation por step; outros adapters não afetados; TypeScript compila.</done>
</task>

</tasks>

<verification>
- TypeScript do server compila
- Tests do orchestrate isolado (Tarefa 1) passam
- Tests existentes do heartbeat continuam passando (não regrediu)
- Wiring é gated por `adapterType === "claude_local"` (não afeta outros adapters)
- D-19 (max 1 rotation per step) explícito via flag local
- Strategy A → fallback Strategy B path implementado (D-21, D-22)
</verification>

<success_criteria>
1. ✅ MULTI-07: heartbeat chama selectActiveAccount + resolveCredentialDir antes de cada spawn de claude_local
2. ✅ MULTI-08: orchestrateClaudeSwap invocado em transient_upstream com inputs corretos
3. ✅ Strategy A (resume previous session_id) tentada primeiro
4. ✅ Strategy B (fallback full-context com continuation summary embebido) ativada quando detectResumeFailed=true
5. ✅ D-19: 1 rotação por step (flag alreadyRotatedThisStep)
6. ✅ NoAccountsAvailableError → operacional log (não crash); heartbeat continua com fallback process.env
7. ✅ recordStepExecution chamado após cada spawn (append-only attribution)
8. ✅ Test suite de orchestrate isolado: 8+ cases passando
9. ✅ Não regrediu tests existentes do heartbeat (heartbeat-stop-metadata.test.ts ainda passa)
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-06-SUMMARY.md` with:
- 4 pontos de modificação em heartbeat.ts (line ranges aproximados)
- Novo module claude-accounts-swap.ts (LoC + decisões: por que orchestrateClaudeSwap retorna Strategy A default e caller invoca orchestrateFallbackFullContext separadamente em vez de orquestrar tudo internamente)
- Test counts e estratégia de mock
- D-19 explícito: flag local `alreadyRotatedThisStep` (não persistida em DB; reset a cada novo step)
- Edge cases: errors no Ponto B (NoAccountsAvailable, CredentialDirMissing) → falha graceful, log para operador, fallback para process.env
- Open question pós-Phase 5: rotation_cap_reached em fallback retry — atualmente fallback re-spawn dentro da mesma "rotation" (sem incrementar guard). Documentar para refinamento pós-UAT-04-03.
</output>
