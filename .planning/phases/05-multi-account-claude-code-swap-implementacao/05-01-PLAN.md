---
phase: 05-multi-account-claude-code-swap-implementacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/claude_accounts.ts
  - packages/db/src/schema/agent_account_bindings.ts
  - packages/db/src/schema/agent_step_executions.ts
  - packages/db/src/schema/index.ts
  - packages/db/migrations/
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03]

must_haves:
  truths:
    - "Drizzle compila sem erros de tipo após adição das 3 schemas"
    - "Generated SQL migration cria 3 tables com FKs corretas para companies/agents/user"
    - "claude_accounts.exhaustedUntil é queryable via index para selectActiveAccount filter"
    - "agent_step_executions é append-only (sem campo updatedAt) e indexado em (runId, stepId)"
  artifacts:
    - path: "packages/db/src/schema/claude_accounts.ts"
      provides: "Schema para pool de contas Claude Code (D-02)"
      contains: "export const claudeAccounts"
    - path: "packages/db/src/schema/agent_account_bindings.ts"
      provides: "Schema para binding agentId → activeAccountId + cooldown gating (D-04)"
      contains: "export const agentAccountBindings"
    - path: "packages/db/src/schema/agent_step_executions.ts"
      provides: "Schema append-only de attribution por step (D-05)"
      contains: "export const agentStepExecutions"
    - path: "packages/db/src/schema/index.ts"
      provides: "Exports das 3 novas tabelas"
      contains: "claudeAccounts"
    - path: "packages/db/migrations/"
      provides: "SQL migration gerada via drizzle-kit"
      contains: "CREATE TABLE"
  key_links:
    - from: "packages/db/src/schema/claude_accounts.ts"
      to: "companies (uuid FK), authUsers (text FK)"
      via: "references()"
      pattern: "references\\(\\) => companies\\.id"
    - from: "packages/db/src/schema/agent_account_bindings.ts"
      to: "agents (uuid FK), claudeAccounts (uuid FK)"
      via: "references()"
      pattern: "references\\(\\) => agents\\.id"
    - from: "packages/db/src/schema/agent_step_executions.ts"
      to: "claudeAccounts (uuid FK)"
      via: "references()"
      pattern: "references\\(\\) => claudeAccounts\\.id"
---

<objective>
Criar os 3 schemas Drizzle para o pool multi-conta (claude_accounts, agent_account_bindings, agent_step_executions), exportá-los do barrel index.ts e gerar a migration SQL via drizzle-kit. Esta é a fundação de toda a Phase 5 — todos os outros planos dependem destes schemas existirem antes de poder ser implementados/testados.

Purpose: Persistir o pool de contas com status/exhaustion windows, o binding atual por agente com cooldown gating, e o append-only attribution log de cada step com tokens/custo/conta — habilitando rotação atômica em exhaustão (MULTI-08) e cost attribution por conta (Phase 6 PROJ-03).

Output: 3 arquivos schema TypeScript, index.ts atualizado, e SQL migration gerada committada em packages/db/migrations/.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md
@.planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md
@packages/db/src/schema/heartbeat_runs.ts
@packages/db/src/schema/cost_events.ts
@packages/db/src/schema/index.ts

<interfaces>
<!-- Padrões reais da codebase (verificar antes de implementar): -->
<!-- companies.id e agents.id são UUID; auth.users (Better Auth) usa text. -->
<!-- IMPORTANTE: CONTEXT.md D-02/D-04/D-05 diz "text PK", mas alinhar com convenção real do código: -->
<!--   - claude_accounts.id → uuid (consistente com companies, agents, cost_events) -->
<!--   - claude_accounts.companyId → uuid → companies.id -->
<!--   - claude_accounts.ownerUserId → text → authUsers.id (Better Auth é text) -->
<!--   - agent_account_bindings.agentId → uuid → agents.id -->
<!--   - agent_step_executions.runId → uuid → heartbeatRuns.id -->

From packages/db/src/schema/cost_events.ts (append-only attribution pattern):
```typescript
export const costEvents = pgTable("cost_events", {
  id: uuid("id").primaryKey().defaultRandom(),
  companyId: uuid("company_id").notNull().references(() => companies.id),
  agentId: uuid("agent_id").notNull().references(() => agents.id),
  // ... (no updatedAt — append-only)
  occurredAt: timestamp("occurred_at", { withTimezone: true }).notNull(),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
}, (table) => ({
  companyAgentOccurredIdx: index("cost_events_company_agent_occurred_idx").on(
    table.companyId, table.agentId, table.occurredAt,
  ),
}));
```

From packages/db/src/schema/heartbeat_runs.ts (timestamps + indexes pattern):
```typescript
export const heartbeatRuns = pgTable("heartbeat_runs", {
  id: uuid("id").primaryKey().defaultRandom(),
  // ...
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
}, (table) => ({
  companyAgentStartedIdx: index("heartbeat_runs_company_agent_started_idx").on(...),
}));
```

From packages/db/src/schema/auth.ts (Better Auth user FK):
```typescript
export const authUsers = pgTable("user", {
  id: text("id").primaryKey(),
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar 3 schemas Drizzle + atualizar barrel</name>
  <files>
    packages/db/src/schema/claude_accounts.ts,
    packages/db/src/schema/agent_account_bindings.ts,
    packages/db/src/schema/agent_step_executions.ts,
    packages/db/src/schema/index.ts
  </files>
  <read_first>
    - packages/db/src/schema/cost_events.ts (pattern append-only com FKs uuid)
    - packages/db/src/schema/heartbeat_runs.ts (pattern timestamps + indexes + jsonb)
    - packages/db/src/schema/companies.ts (linhas 1-15: pgTable + uuid + defaultRandom pattern)
    - packages/db/src/schema/agents.ts (linhas 1-20: confirmar agentId é uuid)
    - packages/db/src/schema/auth.ts (linhas 1-20: confirmar user.id é text)
    - packages/db/src/schema/index.ts (barrel atual; adicionar 3 novos exports após linha 57 onde costEvents é exportado)
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-CONTEXT.md (D-02, D-04, D-05 — schemas exatos)
    - .planning/phases/04-spike-multi-account-claude-code-detection/FINDINGS-FOR-PHASE-5.md (Finding 3 — estrutura lastQuotaWindowsJson)
  </read_first>
  <behavior>
    - Test 1: import claudeAccounts from @paperclipai/db retorna pgTable instance com colunas (id, companyId, ownerUserId, label, configDirSlug, status, lastQuotaWindowsJson, exhaustedUntil, lastUsedAt, createdAt, updatedAt)
    - Test 2: import agentAccountBindings retorna pgTable com (agentId PK, activeAccountId, rotationPolicy, lastRotatedAt, createdAt, updatedAt)
    - Test 3: import agentStepExecutions retorna pgTable com (id, runId, stepId, accountId, inputTokens, cachedInputTokens, outputTokens, costUsd, startedAt, completedAt, errorFamily, createdAt) — sem updatedAt (append-only)
    - Test 4: TypeScript compila pacote @paperclipai/db sem erros
  </behavior>
  <action>
    **Schema 1: `packages/db/src/schema/claude_accounts.ts`** (D-02 + Finding 3)

    ```typescript
    import { pgTable, uuid, text, timestamp, jsonb, index, uniqueIndex } from "drizzle-orm/pg-core";
    import { companies } from "./companies.js";
    import { authUsers } from "./auth.js";

    export const claudeAccounts = pgTable(
      "claude_accounts",
      {
        id: uuid("id").primaryKey().defaultRandom(),
        companyId: uuid("company_id").notNull().references(() => companies.id),
        ownerUserId: text("owner_user_id").notNull().references(() => authUsers.id),
        label: text("label").notNull(),
        configDirSlug: text("config_dir_slug").notNull(),
        status: text("status").notNull().default("live"), // 'live' | 'exhausted' | 'cooldown' | 'disabled'
        lastQuotaWindowsJson: jsonb("last_quota_windows_json")
          .$type<Record<string, { exhaustedUntil: string; lastTriggeredAt: string; count: number }>>()
          .notNull()
          .default({}),
        exhaustedUntil: timestamp("exhausted_until", { withTimezone: true }),
        lastUsedAt: timestamp("last_used_at", { withTimezone: true }),
        createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
        updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
      },
      (table) => ({
        companyStatusIdx: index("claude_accounts_company_status_idx").on(table.companyId, table.status),
        companyExhaustedIdx: index("claude_accounts_company_exhausted_idx").on(table.companyId, table.exhaustedUntil),
        configDirSlugUnq: uniqueIndex("claude_accounts_config_dir_slug_unq").on(table.configDirSlug),
      }),
    );
    ```

    Notas inline:
    - `status` default `'live'` consistente com D-02 (registro novo é elegível por padrão).
    - `lastQuotaWindowsJson` tipo TypeScript reflete os 6 sub-tipos da taxonomia (D-03/Finding 3): chaves `rpm_transient`, `tpm_transient`, `daily_quota`, `weekly_quota`, `session_5h`, `org_tier` — não enforçar via schema (jsonb), enforçar via TypeScript do service.
    - `exhaustedUntil` é cached (MAX dos 6 windows), populado pelo service no rotateOnQuotaExhausted.
    - `configDirSlug` UNIQUE global (operador escolhe slug único; harness cria `~/.paperclip/claude-accounts/<slug>/`).

    **Schema 2: `packages/db/src/schema/agent_account_bindings.ts`** (D-04)

    ```typescript
    import { pgTable, uuid, text, timestamp } from "drizzle-orm/pg-core";
    import { agents } from "./agents.js";
    import { claudeAccounts } from "./claude_accounts.js";

    export const agentAccountBindings = pgTable("agent_account_bindings", {
      agentId: uuid("agent_id").primaryKey().references(() => agents.id),
      activeAccountId: uuid("active_account_id").references(() => claudeAccounts.id),
      rotationPolicy: text("rotation_policy").notNull().default("auto"), // 'auto' | 'sticky' | 'manual'
      lastRotatedAt: timestamp("last_rotated_at", { withTimezone: true }),
      createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
      updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
    });
    ```

    Notas: `activeAccountId` é nullable (binding pode existir sem conta ativa atribuída ainda). `lastRotatedAt` nullable (primeira atribuição não é rotação).

    **Schema 3: `packages/db/src/schema/agent_step_executions.ts`** (D-05)

    ```typescript
    import { pgTable, uuid, text, timestamp, integer, real, index } from "drizzle-orm/pg-core";
    import { claudeAccounts } from "./claude_accounts.js";
    import { heartbeatRuns } from "./heartbeat_runs.js";

    export const agentStepExecutions = pgTable(
      "agent_step_executions",
      {
        id: uuid("id").primaryKey().defaultRandom(),
        runId: uuid("run_id").notNull().references(() => heartbeatRuns.id),
        stepId: text("step_id").notNull(),
        accountId: uuid("account_id").notNull().references(() => claudeAccounts.id),
        inputTokens: integer("input_tokens").notNull().default(0),
        cachedInputTokens: integer("cached_input_tokens").notNull().default(0),
        outputTokens: integer("output_tokens").notNull().default(0),
        costUsd: real("cost_usd").notNull().default(0),
        startedAt: timestamp("started_at", { withTimezone: true }).notNull(),
        completedAt: timestamp("completed_at", { withTimezone: true }),
        errorFamily: text("error_family"), // 'transient_upstream' | 'auth' | etc — null em sucesso
        createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
      },
      (table) => ({
        runStepIdx: index("agent_step_executions_run_step_idx").on(table.runId, table.stepId),
        accountStartedIdx: index("agent_step_executions_account_started_idx").on(table.accountId, table.startedAt),
      }),
    );
    ```

    Notas: **sem `updatedAt`** — append-only conforme D-05. `runId` referencia `heartbeatRuns.id` (uuid) em vez de string opaca para integridade referencial. `stepId` é `text` (definido pelo orchestrator do heartbeat; pode ser timestamp/sequencial). `errorFamily` nullable (sucesso = null; transient_upstream = candidato a swap).

    **Atualizar `packages/db/src/schema/index.ts`** (após linha 57 `export { costEvents }`):

    ```typescript
    export { claudeAccounts } from "./claude_accounts.js";
    export { agentAccountBindings } from "./agent_account_bindings.js";
    export { agentStepExecutions } from "./agent_step_executions.js";
    ```

    Não usar transações no schema — apenas declarações puras.
  </action>
  <verify>
    <automated>cd packages/db && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccounts" packages/db/src/schema/index.ts` retorna >= 1
    - `grep -c "agentAccountBindings" packages/db/src/schema/index.ts` retorna >= 1
    - `grep -c "agentStepExecutions" packages/db/src/schema/index.ts` retorna >= 1
    - `grep -E "id:.*uuid|companyId:.*uuid|ownerUserId:.*text" packages/db/src/schema/claude_accounts.ts` casa todas as 3 (FKs corretos)
    - `grep -c "uniqueIndex.*config_dir_slug" packages/db/src/schema/claude_accounts.ts` retorna 1
    - `! grep -q "updatedAt" packages/db/src/schema/agent_step_executions.ts` (append-only — confirma ausência)
    - `cd packages/db && pnpm tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>3 schema files existem em `packages/db/src/schema/`, exportados em `index.ts`, TypeScript compila sem erros.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Gerar SQL migration via drizzle-kit</name>
  <files>packages/db/migrations/</files>
  <read_first>
    - drizzle.config.ts (raiz — confirmar paths e env-gating)
    - packages/db/migrations/ (ls atual; identificar último número de prefix sequencial)
    - .planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md (DB-03 — apenas CI aplica `pnpm db:migrate`; gerar local é OK)
  </read_first>
  <action>
    Executar `pnpm --filter @paperclipai/db db:generate` (ou comando equivalente em scripts) para gerar SQL diff entre schema atual e schemas adicionados na Tarefa 1.

    **Validações antes de aceitar a migration gerada:**
    1. Arquivo `.sql` novo aparece em `packages/db/migrations/` com prefix sequencial após o último existente.
    2. SQL contém `CREATE TABLE "claude_accounts"` com colunas matching o schema (uuid PK, companyId FK, ownerUserId FK to "user").
    3. SQL contém `CREATE TABLE "agent_account_bindings"` com agentId PK referenciando agents.
    4. SQL contém `CREATE TABLE "agent_step_executions"` com FKs para claude_accounts e heartbeat_runs.
    5. SQL contém `CREATE UNIQUE INDEX "claude_accounts_config_dir_slug_unq"`.
    6. SQL contém os 4 índices declarados (claude_accounts_company_status_idx, claude_accounts_company_exhausted_idx, agent_step_executions_run_step_idx, agent_step_executions_account_started_idx).
    7. **NÃO aplicar** a migration localmente contra Supabase compartilhado — DB-03 (Phase 2) restringe apply para CI. Migration fica committada e será aplicada via GitHub Actions no merge para main.
    8. Se `pnpm db:generate` requer env (DATABASE_URL/SUPABASE_DB_URL), usar local fixture .env conforme padrão do repo; comando deve ser idempotente.

    Se drizzle-kit detectar inconsistência (ex: schema referencia tabela inexistente), **parar e investigar** — pode indicar erro na Tarefa 1.

    **Convenção de naming:** drizzle-kit gera nomes hashed (`0072_<random_words>.sql`); aceitar como gerado, não renomear.
  </action>
  <verify>
    <automated>ls packages/db/migrations/*.sql | tail -5 && grep -l "claude_accounts" packages/db/migrations/*.sql | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Pelo menos 1 arquivo `.sql` novo existe em `packages/db/migrations/` com data/prefix posterior ao último existente
    - `grep -l 'CREATE TABLE.*claude_accounts' packages/db/migrations/*.sql` retorna pelo menos 1 path
    - `grep -l 'CREATE TABLE.*agent_account_bindings' packages/db/migrations/*.sql` retorna pelo menos 1 path
    - `grep -l 'CREATE TABLE.*agent_step_executions' packages/db/migrations/*.sql` retorna pelo menos 1 path
    - `grep -c 'CREATE UNIQUE INDEX.*config_dir_slug' packages/db/migrations/*.sql` retorna pelo menos 1
    - Migration NÃO foi aplicada contra Supabase compartilhado (verificar: `grep -L "apply" .` — n/a; em vez disso, confirmar via histórico de commits que `pnpm db:migrate` não foi rodado contra DATABASE_URL=Supabase pelo executor)
  </acceptance_criteria>
  <done>SQL migration gerada committada em `packages/db/migrations/`; aplicação fica para CI conforme DB-03.</done>
</task>

</tasks>

<verification>
- Executar `cd packages/db && pnpm tsc --noEmit` — exit code 0
- Verificar 3 exports adicionados em `packages/db/src/schema/index.ts`
- Verificar SQL migration gerada existe e contém as 3 tabelas
- Confirmar que NÃO foi aplicada contra Supabase compartilhado (DB-03 = CI-only)
</verification>

<success_criteria>
1. ✅ `packages/db/src/schema/claude_accounts.ts` existe e compila
2. ✅ `packages/db/src/schema/agent_account_bindings.ts` existe e compila
3. ✅ `packages/db/src/schema/agent_step_executions.ts` existe e compila
4. ✅ `packages/db/src/schema/index.ts` exporta as 3 novas tabelas
5. ✅ SQL migration gerada via drizzle-kit em `packages/db/migrations/`
6. ✅ FKs corretos: companyId → companies (uuid), ownerUserId → user (text), agentId → agents (uuid), accountId → claude_accounts (uuid), runId → heartbeat_runs (uuid)
7. ✅ Index unique em `config_dir_slug`; index em `(companyId, status)` e `(companyId, exhaustedUntil)` para selectActiveAccount
8. ✅ Append-only confirmado: `agent_step_executions` sem `updatedAt`
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-account-claude-code-swap-implementacao/05-01-SUMMARY.md` with:
- 3 schema files created (paths + line counts)
- index.ts diff (3 new exports)
- Migration filename gerada
- Decisão sobre tipo de id (uuid em vez de text conforme convenção codebase, ajuste vs CONTEXT D-02)
- FK strategy (ownerUserId é text porque Better Auth; resto é uuid)
- Constraint: migration NÃO aplicada contra Supabase (CI-only DB-03)
</output>
