---
phase: 06-multi-projeto-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md
  - .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md
autonomous: true
requirements:
  - PROJ-01
must_haves:
  truths:
    - "MULTI-COMPANY-SMOKE.md documenta procedure canônica em pt-br para validar isolamento"
    - "Procedure tem Modo A automatizado via SQL queries diretas contra Supabase"
    - "Procedure tem Modo B manual cross-tab/cross-browser para validação UI"
    - "Bonus scenario inclui pool_mode=shared validation"
    - "06-HUMAN-UAT.md registra UAT-06-01 com frontmatter status: pending"
    - "Pass/fail criteria explicitos (>= 5 dimensões)"
  artifacts:
    - path: ".planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md"
      provides: "Canonical procedure for PROJ-01 multi-company smoke"
      min_lines: 100
    - path: ".planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md"
      provides: "UAT-06-01 placeholder pending operator execution"
      min_lines: 50
  key_links:
    - from: "MULTI-COMPANY-SMOKE.md"
      to: "Supabase via SQL"
      via: "psql / Supabase Studio queries"
      pattern: "SELECT.*FROM agent_step_executions|SELECT.*FROM activity_log"
    - from: "06-HUMAN-UAT.md"
      to: "MULTI-COMPANY-SMOKE.md"
      via: "Modo B references procedure"
      pattern: "MULTI-COMPANY-SMOKE.md"
---

<objective>
Documentar procedimento canônico de smoke E2E multi-company satisfazendo PROJ-01 + criar UAT pendente para validação humana cross-browser.

Purpose: PROJ-01 success criterion #1 do ROADMAP — "Duas companies rodam agentes em paralelo simultaneamente no mesmo Supabase e nenhum agente vê dados/contas/runs da outra company." D-01 estabelece que isolamento já é core no paperclip — esta fase VALIDA, não implementa. Validação tem dois caminhos:

- Modo A (automatizado): SQL queries diretas contra Supabase exercitam invariante (agent_step_executions + activity_log filtrados por companyId não vazam entre companies). Executável agora pelo executor Claude/CI.
- Modo B (HUMAN-UAT): Operador humano abre 2 browsers/profiles em 2 companies distintas e valida visualmente que UI ClaudeAccounts/CompanySettings/cost-summary não vazam dados.

Output: 2 documentos pt-br no diretório da fase. Sem código de produção. Precedente: Phase 5 plan 05-08 (SMOKE-E2E.md + 05-HUMAN-UAT.md).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-projeto-polish/06-CONTEXT.md
@.planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md
@.planning/phases/06-multi-projeto-polish/06-02-SUMMARY.md
@.planning/phases/06-multi-projeto-polish/06-03-SUMMARY.md
@.planning/phases/06-multi-projeto-polish/06-04-SUMMARY.md

@.planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md
@.planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md

<interfaces>
Schemas chaves para SQL queries:
- companies (id uuid, claudeAccountPoolMode text DEFAULT 'per_company')
- claude_accounts (id uuid, companyId uuid → companies.id, scope text DEFAULT 'company', label, status, configDirSlug)
- agent_account_bindings (agentId uuid, activeAccountId uuid → claude_accounts.id, rotationPolicy)
- agent_step_executions (runId uuid → heartbeat_runs.id, accountId uuid → claude_accounts.id, costUsd, ...)
- heartbeat_runs (id uuid, companyId uuid → companies.id, agentId uuid)
- activity_log (companyId uuid, action text, agentId uuid, details jsonb)
- agents (id uuid, companyId uuid)

Patterns de Phase 5 SMOKE-E2E.md a replicar:
- Frontmatter YAML
- Pre-requisitos table
- 6 Passos numerados (Setup → Spawn → Validate → Cleanup)
- SQL queries literais (com placeholders {{COMPANY_A_ID}})
- Pass/fail table (7 critérios)
- Limitations seção (rotear para HUMAN-UAT)

Patterns de 05-HUMAN-UAT.md a replicar:
- Frontmatter YAML: type: human-uat, status: pending
- 1 UAT detalhado com pre-conditions, steps, pass/fail dimensions, report instructions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar MULTI-COMPANY-SMOKE.md (Modo A automatizado + bonus shared scenario)</name>
  <files>.planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md</files>
  <read_first>
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/SMOKE-E2E.md (template + tone — pt-br, estrutura)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-14, D-15)
    - .planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md (confirma migration 0072)
    - .planning/phases/06-multi-projeto-polish/06-03-SUMMARY.md (confirma selectActiveAccount aware)
    - packages/db/src/schema/companies.ts (estado pós-06-01)
    - packages/db/src/schema/claude_accounts.ts (estado pós-06-01)
    - packages/db/src/schema/agent_step_executions.ts (estado Phase 5)
  </read_first>
  <action>
    Criar arquivo `.planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md` em pt-br (~150-200 linhas) com a seguinte estrutura:

    Frontmatter YAML:
    ```yaml
    ---
    phase: 06-multi-projeto-polish
    type: smoke-procedure
    requirement: PROJ-01
    status: ready
    ---
    ```

    Sections (H2 numeradas):

    ## 1. Objetivo
    Validar empiricamente o success criterion #1 do ROADMAP Phase 6: Duas companies rodam agentes em paralelo no mesmo Supabase sem cross-contamination. D-01 (06-CONTEXT) reforça que isolamento por companyId já é core no paperclip — este procedimento VALIDA o invariante, não implementa.

    ## 2. Pré-requisitos
    Tabela com 8 itens:
    - Migration 0072 aplicada via CI em main (verificar em GitHub Actions)
    - `claudeAccountPoolMode` column existe em companies
    - `scope` column existe em claude_accounts
    - Pelo menos 2 companies existentes em Supabase (Company A, Company B) — pode reusar dev signups Phase 3 ou seed via SQL
    - Pelo menos 1 agente por company
    - Acesso a Supabase Studio (SQL editor) ou psql contra `SUPABASE_DB_URL`
    - Credenciais Better Auth para ambas companies (via Phase 3 invite flow ou direct DB)
    - 2 contas Claude reais OU contas registradas via UI com `claude login` feito (mesmo que não exercitemos exhaustão real aqui)

    ## 3. Modo A — Validação automatizada via SQL (D-14 / executável agora)

    ### 3.1 Setup via SQL
    Bloco SQL com placeholders `{{COMPANY_A_ID}}` e `{{COMPANY_B_ID}}`:
    ```sql
    -- Confirmar 2 companies existem
    SELECT id, name, claude_account_pool_mode FROM companies
      WHERE id IN ('{{COMPANY_A_ID}}', '{{COMPANY_B_ID}}');
    -- Esperado: 2 rows, ambas com claude_account_pool_mode='per_company'

    -- Registrar 1 conta para A (via UI POST ou direct INSERT para teste)
    INSERT INTO claude_accounts (company_id, owner_user_id, label, config_dir_slug, status, scope)
      VALUES ('{{COMPANY_A_ID}}', '{{OWNER_A_ID}}', 'A1', 'a1', 'live', 'company');

    -- Registrar 1 conta para B
    INSERT INTO claude_accounts (company_id, owner_user_id, label, config_dir_slug, status, scope)
      VALUES ('{{COMPANY_B_ID}}', '{{OWNER_B_ID}}', 'B1', 'b1', 'live', 'company');
    ```

    ### 3.2 Spawn agentes em paralelo (manual via UI ou script)
    Pode usar `pnpm dev` com 2 sessões Better Auth distintas, ou trigger via script — registrar passo manual.

    ### 3.3 Validações SQL (after run)

    Q1 — agent_step_executions de A só usam contas de A:
    ```sql
    SELECT
      hr.company_id,
      ase.account_id,
      ca.company_id AS account_owner_company_id,
      ca.label
    FROM agent_step_executions ase
      JOIN heartbeat_runs hr ON hr.id = ase.run_id
      JOIN claude_accounts ca ON ca.id = ase.account_id
    WHERE hr.company_id = '{{COMPANY_A_ID}}';
    -- Esperado: TODAS as rows com account_owner_company_id = COMPANY_A_ID
    -- FALHA: qualquer row com account_owner_company_id != COMPANY_A_ID indica vazamento
    ```

    Q2 — Idem invertido para B (paranoia):
    ```sql
    -- (mesma query, swap A<->B)
    ```

    Q3 — activity_log de A não tem eventos com agentId de B:
    ```sql
    SELECT al.id, al.action, al.agent_id, a.company_id AS agent_company_id
    FROM activity_log al
      LEFT JOIN agents a ON a.id = al.agent_id
    WHERE al.company_id = '{{COMPANY_A_ID}}'
      AND a.company_id IS NOT NULL
      AND a.company_id != '{{COMPANY_A_ID}}';
    -- Esperado: 0 rows
    ```

    Q4 — Cost summary endpoint validation (chamar via curl autenticado):
    ```bash
    curl -H "Cookie: paperclip-team-shared.session_token=..." \
         http://localhost:3100/api/companies/{{COMPANY_A_ID}}/claude-accounts/cost-summary
    # Esperado: rows todas com accountId pertencente a contas de A; nenhum accountId de B
    ```

    ### 3.4 Bonus — Pool shared scenario (D-15 / específicas)

    Adicionar 3ª etapa com:
    ```sql
    -- Set Company A em modo shared
    UPDATE companies SET claude_account_pool_mode = 'shared' WHERE id = '{{COMPANY_A_ID}}';

    -- Criar conta C com scope=shared (owner: alguém da company A ou B, irrelevante)
    INSERT INTO claude_accounts (company_id, owner_user_id, label, config_dir_slug, status, scope)
      VALUES ('{{COMPANY_A_ID}}', '{{OWNER_A_ID}}', 'C-shared', 'c-shared', 'live', 'shared');
    ```

    Spawnar agente em A, depois em B. Validar:
    - Agente A pode rodar com conta C (modo shared) → query Q1 mostra account_id em {A1, C}
    - Agente B (ainda em per_company) NÃO pode usar C → continua só usando B1; Q1 invertido para B mostra apenas B1

    ## 4. Pass/Fail Criteria
    Tabela com 7 critérios:
    1. Q1/Q2 retornam 0 rows com cross-contamination
    2. Q3 retorna 0 rows
    3. Q4 endpoint não vaza
    4. UI Company A não mostra contas de B (Modo B / 06-HUMAN-UAT)
    5. Bonus shared: A usa C; B não usa C
    6. agent_account_bindings.activeAccountId de agente em A só aponta para conta legítima
    7. Sem erros server side (verificar logs do server durante run)

    ## 5. Cleanup
    SQL para remover dados de teste (DELETE em ordem reversa por FK).

    ## 6. Limitações (roteadas para HUMAN-UAT)
    Lista de 4-5 itens que SQL não cobre e que ficam para 06-HUMAN-UAT.md UAT-06-01:
    - Validação UI cross-tab (CompanySettings em A vs B simultâneo)
    - Cost summary visual rendering
    - Toggle pool mode persistence + immediate effect on next agent spawn
    - Cross-browser cookie isolation (paperclip-team-shared cookie shared corretamente entre profiles)
    - Sentir lentidão / UX em modo shared com pool maior
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && test -f .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md && wc -l .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md && grep -c "^## " .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md && grep -c "SELECT" .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md && grep -c "claude_account_pool_mode" .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e tem >= 100 linhas
    - >= 6 sections H2 numeradas
    - >= 4 SELECT statements (Q1-Q4)
    - >= 1 INSERT statement (registro de conta)
    - >= 1 UPDATE statement (bonus shared)
    - Frontmatter YAML válido com `requirement: PROJ-01`
    - Bonus scenario shared documentado
    - Pass/fail table com >= 5 critérios
    - Limitações seção aponta para 06-HUMAN-UAT.md
    - pt-br consistente com outros docs em .planning/
  </acceptance_criteria>
  <done>Procedimento canônico documentado; executável agora via SQL contra Supabase; bonus shared cobre PROJ-02 cross-validation; limitações UI roteadas para HUMAN-UAT.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Criar 06-HUMAN-UAT.md com UAT-06-01 pending</name>
  <files>.planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-multi-account-claude-code-swap-implementacao/05-HUMAN-UAT.md (template — frontmatter, 1 UAT estrutura, pass/fail dimensions)
    - .planning/phases/04-spike-multi-account-claude-code-detection/04-HUMAN-UAT.md (template alternativo — frontmatter pattern)
    - .planning/phases/06-multi-projeto-polish/MULTI-COMPANY-SMOKE.md (criado na Tarefa 1 — UAT referencia o Modo B / Limitações)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-15)
  </read_first>
  <action>
    Criar arquivo `.planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md` em pt-br (~80-120 linhas):

    Frontmatter YAML:
    ```yaml
    ---
    phase: 06-multi-projeto-polish
    type: human-uat
    status: pending
    requirements:
      - PROJ-01
      - PROJ-02
      - PROJ-03
    ---
    ```

    Sections:

    ## Visão geral
    Este arquivo registra UATs (User Acceptance Tests) que requerem operador humano. Modo A (validação SQL) está em `MULTI-COMPANY-SMOKE.md`. Aqui ficam validações UI cross-tab/cross-browser e percepção operacional que SQL não cobre.

    Status `pending` significa que a Phase 6 fecha como `complete-with-pending-UAT` — artefatos entregues, validação humana pendente. Precedente: Phase 3 plan 03-04 (TEAM-04), Phase 4 plan 04-05 (SPIKE-04/05), Phase 5 plan 05-08 (UAT-05-01).

    ## UAT-06-01 — Multi-company isolation cross-browser

    Requisitos cobertos: PROJ-01 (success criterion #1), PROJ-02 UI (toggle + scope visual), PROJ-03 UI (cost summary visual)

    Pré-condições:
    - Phase 6 plans 01-04 deployed e migration 0072 aplicada via CI
    - 2 companies existem em Supabase (Company A, Company B)
    - 2 browsers/profiles distintos disponíveis (Chrome + Firefox OU 2 Chrome profiles)
    - Operador tem credenciais Better Auth para ambas companies
    - Server rodando em modo `authenticated` (`PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev`)

    Steps (numerados, exatos):
    1. Browser 1: login em Company A; navegar para Settings -> Claude Accounts. Registrar 1 conta (label=A1, slug=a1, scope=company).
    2. Browser 2: login em Company B; navegar para Settings -> Claude Accounts. Registrar 1 conta (label=B1, slug=b1, scope=company).
    3. Browser 1 (A): confirmar que listagem mostra APENAS A1; nenhuma conta B aparece.
    4. Browser 2 (B): confirmar que listagem mostra APENAS B1.
    5. Browser 1 (A): navegar para CompanySettings; trocar Pool Mode toggle para `shared`; salvar; refresh; confirmar persistência.
    6. Browser 1 (A): voltar para ClaudeAccounts; registrar nova conta (label=C-shared, slug=c-shared, scope=shared).
    7. Browser 2 (B): refresh ClaudeAccounts; confirmar que conta C-shared NÃO aparece (B em per_company não vê shared).
    8. Spawnar 1 agente em A com prompt simples (qualquer task). Aguardar pelo menos 1 step concluído.
    9. Spawnar 1 agente em B em paralelo. Aguardar pelo menos 1 step.
    10. Browser 1 (A): refresh ClaudeAccounts -> Cost summary section. Confirmar que mostra linhas para A1 e/ou C-shared, mas NUNCA B1.
    11. Browser 2 (B): refresh ClaudeAccounts -> Cost summary. Confirmar que mostra apenas B1, nunca A1 nem C-shared.
    12. Browser 1 (A): Rotation history section — confirmar que mostra apenas eventos de agentes de A (se houve rotação).
    13. (Opcional) Trocar Pool Mode de A de volta para per_company; refresh; confirmar que C-shared não é mais exibida em "Accounts" table de A (passa a ser invisível em modo per_company); ou — decisão de implementação — continua exibida pois é owner.

    Pass/Fail dimensions (>= 7):
    1. Cross-tab isolation: A nunca vê dados de B em qualquer section
    2. Cross-tab isolation reverso: B nunca vê dados de A
    3. Pool mode persistence: toggle salvo + rehidratado após refresh
    4. Scope radio works: seleção persiste no payload do create
    5. Shared visibility (positive): A em modo shared vê C-shared
    6. Shared visibility (negative): B em modo per_company NÃO vê C-shared
    7. Cost summary isolation: linhas só refletem agent_step_executions da company logada

    Failure paths (cenários conhecidos):
    - Se algum item falhar: capturar screenshot + SQL state via queries do MULTI-COMPANY-SMOKE.md -> criar issue no repo com label `multi-tenant-leak` (BLOQUEANTE para v1) -> Phase 6 reabre para fechamento de lacuna
    - Se cookie cross-browser não funciona: verificar `PAPERCLIP_INSTANCE_ID=team-shared` e cookie prefix (TROUBLESHOOTING.md#cookie-prefix-divergente)
    - Se cost summary mostra 0 rows mesmo após agent spawn: verificar que heartbeat está populando agent_step_executions (Phase 5 wiring 05-06); SQL `SELECT count(*) FROM agent_step_executions WHERE created_at > now() - interval '1 hour'`

    Como reportar:
    Atualizar este arquivo: trocar `status: pending` para `status: passed` (ou `failed` / `partial`); adicionar seção `## Resultado UAT-06-01` com data, executor, observações; commitar.

    ## Conexão com Phase 6 closure
    Phase 6 fecha como `complete-with-pending-UAT` quando:
    - 06-01..06-04 implementados e tests passing
    - 06-05 documentos entregues
    - 06-06 V1-READINESS.md publicado

    UAT-06-01 não bloqueia closure do milestone v1.0 — registrada como item operacional contínuo. v1.0 declared ready com este UAT pendente; convergência via uso real (precedente Phase 3, 4, 5).
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && test -f .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md && wc -l .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md && grep -c "status: pending" .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md && grep -c "UAT-06-01" .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md && grep -c "MULTI-COMPANY-SMOKE.md" .planning/phases/06-multi-projeto-polish/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e tem >= 50 linhas
    - Frontmatter contém `status: pending` (1+ match)
    - `UAT-06-01` aparece pelo menos 1x
    - Referência a `MULTI-COMPANY-SMOKE.md` presente (>= 1 match)
    - Pass/fail dimensions >= 7 enumerados
    - Steps numerados >= 12
    - Failure paths section presente
    - pt-br consistente
  </acceptance_criteria>
  <done>UAT-06-01 documentado em status pending; pronto para operador humano executar pós-merge; rota explícita de feedback (passed/failed/partial).</done>
</task>

</tasks>

<verification>
- 2 arquivos pt-br criados em .planning/phases/06-multi-projeto-polish/
- Modo A executável agora; Modo B documentado para humano
- Sem código de produção tocado neste plano
- UAT segue padrão Phase 3/4/5 (status: pending no frontmatter)
</verification>

<success_criteria>
- PROJ-01 satisfeita como artefato testável (Modo A) + UAT explícito (Modo B)
- Bonus shared scenario cobre PROJ-02 cross-validation
- v1 pode ser declarado ready (Plano 06-06) com UAT-06-01 pending — precedente estabelecido
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-projeto-polish/06-05-SUMMARY.md` documentando:
- Linhas finais de cada arquivo criado
- Decisão de routing: PROJ-01 satisfeita como artefato + UAT-06-01 (Modo B humano)
- Confirmação que MULTI-COMPANY-SMOKE.md tem todos os SQL queries esperados
- Lista das 7 dimensões de pass/fail UAT-06-01
- Quaisquer ajustes de placeholders SQL ({{COMPANY_A_ID}} etc) ou estrutura
</output>
