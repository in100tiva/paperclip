---
phase: 06-multi-projeto-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/companies.ts
  - packages/db/src/schema/claude_accounts.ts
  - packages/db/src/migrations/0072_*.sql
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "companies table has claudeAccountPoolMode column with default 'per_company'"
    - "claude_accounts table has scope column with default 'company'"
    - "Drizzle migration is generated containing both ALTER TABLE statements"
    - "TypeScript compiles clean across packages/db"
    - "Migration is NOT applied locally (DB-03 — CI-only)"
  artifacts:
    - path: "packages/db/src/schema/companies.ts"
      provides: "claudeAccountPoolMode column declaration"
      contains: "claudeAccountPoolMode"
    - path: "packages/db/src/schema/claude_accounts.ts"
      provides: "scope column declaration"
      contains: "scope"
    - path: "packages/db/src/migrations/0072_*.sql"
      provides: "ALTER TABLE companies ADD COLUMN claude_account_pool_mode + ALTER TABLE claude_accounts ADD COLUMN scope"
      contains: "claude_account_pool_mode"
  key_links:
    - from: "packages/db/src/schema/companies.ts"
      to: "drizzle-kit generated SQL"
      via: "pnpm db:generate"
      pattern: "ALTER TABLE.*claude_account_pool_mode"
    - from: "packages/db/src/schema/claude_accounts.ts"
      to: "drizzle-kit generated SQL"
      via: "pnpm db:generate"
      pattern: "ALTER TABLE.*scope"
---

<objective>
Adicionar duas colunas de schema que destravam PROJ-02 (pool config configurável):
- `companies.claudeAccountPoolMode` (text, default 'per_company') — toggle por empresa entre isolamento total ou pool compartilhado
- `claude_accounts.scope` (text, default 'company') — marca conta como exclusiva da company OU disponível no shared pool

Purpose: Defaults preservam comportamento Fase 5 byte-por-byte (per_company isolation + scope=company exclusivo). Opt-in para shared mode é decisão explícita do operador via UI (Plano 06-04). Sem aplicação local da migration — DB-03 mantém CI-only.

Output: 2 schemas modificados + 1 migration Drizzle gerada via `pnpm db:generate`. Nenhum service ou rota tocado neste plano (Wave 2 consome).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-projeto-polish/06-CONTEXT.md

@packages/db/src/schema/companies.ts
@packages/db/src/schema/claude_accounts.ts

<interfaces>
<!-- Estado atual de companies.ts (33 linhas) — preservar todo o conteúdo, ADICIONAR coluna após brandColor -->
```typescript
// packages/db/src/schema/companies.ts (estado atual relevante)
export const companies = pgTable(
  "companies",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    name: text("name").notNull(),
    // ... outros campos ...
    brandColor: text("brand_color"),
    // ⬇ ADICIONAR AQUI (antes de createdAt/updatedAt)
    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
  },
  // ...
);
```

<!-- Estado atual de claude_accounts.ts (74 linhas) — preservar todo o conteúdo, ADICIONAR coluna após status -->
```typescript
// packages/db/src/schema/claude_accounts.ts (estado atual relevante)
export const claudeAccounts = pgTable(
  "claude_accounts",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    companyId: uuid("company_id").notNull().references(() => companies.id),
    ownerUserId: text("owner_user_id").notNull().references(() => authUsers.id),
    label: text("label").notNull(),
    configDirSlug: text("config_dir_slug").notNull(),
    status: text("status").notNull().default("live"),
    // ⬇ ADICIONAR AQUI (antes de lastQuotaWindowsJson)
    lastQuotaWindowsJson: jsonb("last_quota_windows_json").$type<...>().notNull().default({}),
    // ...
  },
  // ...
);
```

<!-- Drizzle generation flow -->
- Comando: `pnpm db:generate` (alias para `drizzle-kit generate`)
- Output esperado: `packages/db/src/migrations/0072_<adjective>_<noun>.sql` (numero sequencial após 0071_lively_azazel.sql que é o último presente)
- Snapshot meta: drizzle-kit gera tambem `packages/db/src/migrations/meta/0072_snapshot.json` automaticamente
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Adicionar coluna claudeAccountPoolMode em companies schema</name>
  <files>packages/db/src/schema/companies.ts</files>
  <read_first>
    - packages/db/src/schema/companies.ts (todo o arquivo — 33 linhas)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-04 para semântica e default)
  </read_first>
  <action>
    Conforme D-04: Adicionar coluna `claudeAccountPoolMode` ao objeto de definição de columns em `companies` (D-04).

    Posicionamento: imediatamente APÓS a linha `brandColor: text("brand_color"),` e ANTES de `createdAt:`.

    Linha exata a adicionar:
    ```typescript
    claudeAccountPoolMode: text("claude_account_pool_mode").notNull().default("per_company"),
    ```

    Adicionar JSDoc inline acima da declaração explicando: 'Phase 6 / D-04 / PROJ-02. Valores aceitos: per_company (default — isolamento total, contas filtradas por companyId + scope=company) | shared (permite usar contas com scope=shared além das próprias). Validação aplicacional, sem CHECK constraint hard.'

    NÃO modificar:
    - Outros campos existentes (todos preservados)
    - Index `companies_issue_prefix_idx` (não alterado)
    - Imports (text já importado de drizzle-orm/pg-core)
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "claude_account_pool_mode" packages/db/src/schema/companies.ts && grep -n "claudeAccountPoolMode" packages/db/src/schema/companies.ts && pnpm --filter @paperclipai/db tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claudeAccountPoolMode" packages/db/src/schema/companies.ts` retorna 1
    - `grep -c "claude_account_pool_mode" packages/db/src/schema/companies.ts` retorna 1
    - `grep -c "per_company" packages/db/src/schema/companies.ts` retorna >=1
    - `pnpm --filter @paperclipai/db tsc --noEmit` exit code 0
    - Linha existente `brandColor: text("brand_color"),` continua presente intacta (regressão check)
  </acceptance_criteria>
  <done>Coluna claudeAccountPoolMode declarada em companies schema com default 'per_company'; TS compila clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Adicionar coluna scope em claude_accounts schema</name>
  <files>packages/db/src/schema/claude_accounts.ts</files>
  <read_first>
    - packages/db/src/schema/claude_accounts.ts (todo o arquivo — 74 linhas)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-05 e D-08 para semântica e default)
  </read_first>
  <action>
    Conforme D-05: Adicionar coluna `scope` ao objeto de definição de columns em `claudeAccounts` (D-05).

    Posicionamento: imediatamente APÓS a linha `status: text("status").notNull().default("live"),` e ANTES de `lastQuotaWindowsJson:`.

    Linha exata a adicionar:
    ```typescript
    scope: text("scope").notNull().default("company"),
    ```

    Adicionar JSDoc inline acima da declaração explicando: 'Phase 6 / D-05 / D-08 / PROJ-02. Valores aceitos: company (default — exclusiva da companyId declarada, semântica Fase 5) | shared (qualquer company com claudeAccountPoolMode=shared pode usá-la; companyId continua marcando o owner). Migração Fase 5→6: registros existentes adquirem default company sem precisar de UPDATE explícito (D-08).'

    Atualizar TAMBÉM o JSDoc do bloco principal da tabela (linhas 5-29 atualmente): adicionar bullet `- scope: 'company' | 'shared' (D-05). Default 'company' preserva semântica Fase 5.` na lista de campos descritos.

    NÃO modificar:
    - Outros campos existentes
    - 3 indexes existentes (companyStatusIdx, companyExhaustedIdx, configDirSlugUnq)
    - Imports
    - FKs

    Considerar adicionar index opcional para query do `selectActiveAccount` (D-06) que filtra por `scope`. Decisão: NÃO criar index novo neste plano — `claude_accounts_company_status_idx` (companyId, status) já existe e basta para filter; scope adicional é selectividade baixa (2 valores) e plan 06-03 valida via tests se query plan precisa de tuning. Se Wave 2 demonstrar slow query, adicionar index em plano de fechamento de lacuna.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "scope:" packages/db/src/schema/claude_accounts.ts && grep -n "\"scope\"" packages/db/src/schema/claude_accounts.ts && pnpm --filter @paperclipai/db tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scope: text" packages/db/src/schema/claude_accounts.ts` retorna 1
    - `grep -c "default(\"company\")" packages/db/src/schema/claude_accounts.ts` retorna >=1
    - `pnpm --filter @paperclipai/db tsc --noEmit` exit code 0
    - 3 indexes existentes (companyStatusIdx, companyExhaustedIdx, configDirSlugUnq) continuam presentes (regressão check via grep)
    - FK `references(() => companies.id)` em companyId continua presente
  </acceptance_criteria>
  <done>Coluna scope declarada em claude_accounts schema com default 'company'; TS compila clean; nenhuma regressão em indexes/FKs.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Gerar Drizzle migration e validar conteúdo</name>
  <files>packages/db/src/migrations/0072_*.sql</files>
  <read_first>
    - packages/db/src/migrations/0071_lively_azazel.sql (referência da última migration — pattern e numbering)
    - packages/db/drizzle.config.ts (confirmar configuração ativa)
    - .planning/STATE.md (D-03 / DB-03 — migration NÃO aplicada localmente)
  </read_first>
  <action>
    Executar `pnpm db:generate` da raiz do repo (ou `cd packages/db && pnpm drizzle-kit generate` se necessário). Drizzle-kit detecta os 2 ALTER TABLE necessários a partir do diff entre snapshot prévio e schemas atualizados.

    Output esperado:
    - `packages/db/src/migrations/0072_<random_adjective>_<random_noun>.sql` (drizzle gera nome aleatório no padrão)
    - `packages/db/src/migrations/meta/0072_snapshot.json` (atualizado automaticamente)
    - `packages/db/src/migrations/meta/_journal.json` (entrada nova com idx 72)

    Conteúdo SQL esperado (verificar):
    ```sql
    ALTER TABLE "companies" ADD COLUMN "claude_account_pool_mode" text DEFAULT 'per_company' NOT NULL;
    --> statement-breakpoint
    ALTER TABLE "claude_accounts" ADD COLUMN "scope" text DEFAULT 'company' NOT NULL;
    ```

    Se drizzle-kit gerar DDL fantasma para tabelas pré-existentes (drift entre snapshots conhecido — ver 05-01 STATE), fazer curagem MANUAL do arquivo SQL: manter APENAS os 2 ALTER TABLE acima, remover qualquer CREATE TABLE / outro ALTER que apareça. Isso já aconteceu em 05-01 (242→62 linhas) e é precedente aceito; documentar deviação no SUMMARY.

    **CRÍTICO (D-03 / DB-03):** NÃO executar `pnpm db:migrate` localmente. Aplicação ocorre via GitHub Actions workflow `.github/workflows/db-migrate.yml` em merge para main. Plan 06-05 valida via SQL contra Supabase APÓS o merge.

    Adicionar entrada no journal se drizzle-kit não gerou (raro — geralmente automático).
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && ls packages/db/src/migrations/0072_*.sql && grep -E "ALTER TABLE.*(claude_account_pool_mode|claude_accounts.*scope)" packages/db/src/migrations/0072_*.sql && grep -c "ALTER TABLE" packages/db/src/migrations/0072_*.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `packages/db/src/migrations/0072_*.sql` existe (1 match)
    - SQL contém literal `ADD COLUMN "claude_account_pool_mode" text DEFAULT 'per_company' NOT NULL`
    - SQL contém literal `ADD COLUMN "scope" text DEFAULT 'company' NOT NULL`
    - `grep -c "ALTER TABLE" packages/db/src/migrations/0072_*.sql` retorna exatamente 2 (após curagem se necessário)
    - `packages/db/src/migrations/meta/_journal.json` contém entry com `idx: 72`
    - Migration NÃO foi aplicada localmente (sem mudança em Supabase neste plano)
  </acceptance_criteria>
  <done>Migration 0072 gerada com 2 ALTER TABLE limpos; journal atualizado; sem aplicação local; pronta para CI consumir em merge para main.</done>
</task>

</tasks>

<verification>
- TypeScript compile clean: `pnpm --filter @paperclipai/db tsc --noEmit` exit 0
- Schema files contêm novas colunas com defaults corretos
- Migration SQL contém exatamente 2 ALTER TABLE statements
- Nenhuma mudança em runtime (server/ui não tocados)
</verification>

<success_criteria>
- `companies.claudeAccountPoolMode` declarado com default 'per_company'
- `claude_accounts.scope` declarado com default 'company'
- Migration 0072 gerada e curada
- Sem regressão em campos/indexes/FKs existentes
- Wave 2 (06-03 selectActiveAccount, 06-04 UI) pode consumir os tipos
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-projeto-polish/06-01-SUMMARY.md` documentando:
- Conteúdo final da migration 0072 (path + 2 statements literais)
- Linhas adicionadas em companies.ts e claude_accounts.ts
- Confirmação de tsc --noEmit clean
- Decisão sobre index para scope (NÃO criado — justificativa via 06-03 tests)
- Quaisquer desvios (especialmente curagem manual do SQL se drizzle-kit gerou DDL fantasma)
</output>
