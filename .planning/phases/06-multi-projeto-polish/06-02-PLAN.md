---
phase: 06-multi-projeto-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/claude-account-costs.ts
  - server/src/services/__tests__/claude-account-costs.test.ts
  - server/src/services/index.ts
  - server/src/routes/claude-accounts.ts
  - server/src/__tests__/claude-account-costs-route.test.ts
autonomous: true
requirements:
  - PROJ-03
must_haves:
  truths:
    - "claudeAccountCostsService factory exists with aggregateByCompany method"
    - "GET /api/companies/:companyId/claude-accounts/cost-summary endpoint returns aggregated rows"
    - "Aggregation joins agent_step_executions + claude_accounts (label) + heartbeat_runs (companyId scope)"
    - "Response shape matches { accountId, accountLabel, totalCostUsd, totalInputTokens, totalOutputTokens, stepCount }"
    - "Tests cover: empty pool, single account multi-steps, multi-account isolation, date range filter, cross-company isolation"
  artifacts:
    - path: "server/src/services/claude-account-costs.ts"
      provides: "claudeAccountCostsService factory with aggregateByCompany"
      contains: "aggregateByCompany"
    - path: "server/src/services/__tests__/claude-account-costs.test.ts"
      provides: "vitest cases covering aggregation correctness"
      min_lines: 100
    - path: "server/src/routes/claude-accounts.ts"
      provides: "GET /companies/:companyId/claude-accounts/cost-summary route"
      contains: "cost-summary"
  key_links:
    - from: "server/src/services/claude-account-costs.ts"
      to: "agent_step_executions + claude_accounts + heartbeat_runs"
      via: "Drizzle SQL with JOIN + groupBy"
      pattern: "innerJoin|leftJoin"
    - from: "server/src/routes/claude-accounts.ts"
      to: "claudeAccountCostsService"
      via: "router.get cost-summary handler"
      pattern: "cost-summary"
---

<objective>
Implementar serviço + endpoint REST de cost attribution agregado por (companyId, accountId), satisfazendo PROJ-03. Agregação cobre todos `agent_step_executions` da company filtrados por date range opcional.

Purpose: PROJ-03 success criterion #3 do ROADMAP — "Dashboard de cost attribution mostra gasto agregado por (companyId, accountId)". Service + endpoint são o backend; UI consumer fica no Plano 06-04.

Output: Service factory tipado + endpoint REST + tests. Sem mudanças em schemas (Wave 1 paralela 06-01 cuida) e sem mudanças em selectActiveAccount (Wave 2 cuida).

D-01 invariante: aggregation respeita multi-tenant isolation já core no paperclip (heartbeat_runs.companyId é fonte de verdade — agent_step_executions JOIN via runId garante que custos de outra company NÃO vazam).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-projeto-polish/06-CONTEXT.md

@packages/db/src/schema/agent_step_executions.ts
@packages/db/src/schema/claude_accounts.ts
@packages/db/src/schema/heartbeat_runs.ts
@server/src/routes/claude-accounts.ts
@server/src/services/claude-accounts.ts

<interfaces>
<!-- agent_step_executions schema (Phase 5 / MULTI-03) -->
```typescript
// packages/db/src/schema/agent_step_executions.ts
export const agentStepExecutions = pgTable("agent_step_executions", {
  id: uuid("id").primaryKey().defaultRandom(),
  runId: uuid("run_id").notNull().references(() => heartbeatRuns.id),
  stepId: text("step_id").notNull(),
  accountId: uuid("account_id").notNull().references(() => claudeAccounts.id),
  inputTokens: integer("input_tokens").notNull().default(0),
  cachedInputTokens: integer("cached_input_tokens").notNull().default(0),
  outputTokens: integer("output_tokens").notNull().default(0),
  costUsd: real("cost_usd").notNull().default(0),
  startedAt: timestamp("started_at", { withTimezone: true }).notNull(),
  completedAt: timestamp("completed_at", { withTimezone: true }),
  errorFamily: text("error_family"),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
});
```

<!-- heartbeat_runs.companyId — JOIN para attribution per-company -->
```typescript
// packages/db/src/schema/heartbeat_runs.ts (linha 10)
companyId: uuid("company_id").notNull().references(() => companies.id),
```

<!-- claude_accounts (label para display) -->
```typescript
// packages/db/src/schema/claude_accounts.ts
label: text("label").notNull(),
```

<!-- Service factory pattern existente — replicar -->
```typescript
// server/src/services/claude-accounts.ts (estrutura exemplo)
export function claudeAccountsService(db: Db) {
  async function listAccounts(companyId: string): Promise<ClaudeAccount[]> { ... }
  async function selectActiveAccount(input: { ... }): Promise<ClaudeAccount> { ... }
  return { listAccounts, selectActiveAccount, ... };
}
```

<!-- Routes pattern existente em claude-accounts.ts -->
```typescript
// server/src/routes/claude-accounts.ts (linha 86)
router.get("/companies/:companyId/claude-accounts/rotation-history", async (req, res) => {
  assertCompanyAccess(req, String(req.params.companyId));
  // ... query ...
  res.json({ entries: rows });
});
```

<!-- Drizzle SQL aggregation reference (sql template + sum/count) -->
```typescript
import { sql, sum, count, eq, and, gte, lte } from "drizzle-orm";
// sum(table.col).mapWith(Number) ; count() ; sql<number>`COALESCE(SUM(...), 0)`
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar claudeAccountCostsService factory + tests RED→GREEN</name>
  <files>
    server/src/services/claude-account-costs.ts,
    server/src/services/__tests__/claude-account-costs.test.ts,
    server/src/services/index.ts
  </files>
  <read_first>
    - server/src/services/claude-accounts.ts (factory pattern + Db typing — linhas 462-470 do export)
    - server/src/services/__tests__/claude-accounts.test.ts (vitest pattern com manual chainable mock Db, se executor preferir mock; OU integration test se infra suportar)
    - packages/db/src/schema/agent_step_executions.ts (full)
    - packages/db/src/schema/heartbeat_runs.ts (campo companyId — linha 10)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-09, D-10, D-13)
  </read_first>
  <behavior>
    Casos a cobrir nos tests (RED primeiro, depois GREEN):
    - Test 1: Pool vazio (sem agent_step_executions para a company) → retorna `[]`
    - Test 2: 1 conta, 3 steps com costUsd=0.01 cada e tokens conhecidos → retorna 1 row com totalCostUsd=0.03, stepCount=3, tokens somados
    - Test 3: 2 contas (A com 2 steps, B com 1 step) → retorna 2 rows uma por conta com totais corretos, ordenadas (decisão: ordenar por totalCostUsd DESC)
    - Test 4: Date range filter (from/to fornecidos) → exclui steps fora do intervalo (filtro em `agent_step_executions.startedAt`)
    - Test 5: Cross-company isolation (D-01) → step de Company B NÃO aparece no aggregate de Company A (garantido pelo JOIN com heartbeat_runs.companyId)
    - Test 6: account_label preservado mesmo se conta foi disabled depois (LEFT JOIN com claude_accounts ou usar label do snapshot — decisão: INNER JOIN, label vem do estado atual da conta — se conta deletada, step ainda aparece via FK mas sem label; v1 aceita sem label se isso ocorrer)
  </behavior>
  <action>
    Conforme D-09, D-10, D-13:

    **Arquivo 1 — server/src/services/claude-account-costs.ts** (~80-120 linhas):

    Criar service factory:
    ```typescript
    import { and, desc, eq, gte, lte, sql } from "drizzle-orm";
    import type { Db } from "@paperclipai/db";
    import { agentStepExecutions, claudeAccounts, heartbeatRuns } from "@paperclipai/db";

    export interface CostSummaryRow {
      accountId: string;
      accountLabel: string;
      totalCostUsd: number;
      totalInputTokens: number;
      totalOutputTokens: number;
      stepCount: number;
    }

    export interface CostSummaryRange {
      from?: Date;
      to?: Date;
    }

    export function claudeAccountCostsService(db: Db) {
      async function aggregateByCompany(
        companyId: string,
        range?: CostSummaryRange,
      ): Promise<CostSummaryRow[]> {
        const conditions = [eq(heartbeatRuns.companyId, companyId)];
        if (range?.from) conditions.push(gte(agentStepExecutions.startedAt, range.from));
        if (range?.to) conditions.push(lte(agentStepExecutions.startedAt, range.to));

        const rows = await db
          .select({
            accountId: agentStepExecutions.accountId,
            accountLabel: claudeAccounts.label,
            totalCostUsd: sql<number>`COALESCE(SUM(${agentStepExecutions.costUsd}), 0)::float8`,
            totalInputTokens: sql<number>`COALESCE(SUM(${agentStepExecutions.inputTokens}), 0)::int`,
            totalOutputTokens: sql<number>`COALESCE(SUM(${agentStepExecutions.outputTokens}), 0)::int`,
            stepCount: sql<number>`COUNT(*)::int`,
          })
          .from(agentStepExecutions)
          .innerJoin(heartbeatRuns, eq(agentStepExecutions.runId, heartbeatRuns.id))
          .innerJoin(claudeAccounts, eq(agentStepExecutions.accountId, claudeAccounts.id))
          .where(and(...conditions))
          .groupBy(agentStepExecutions.accountId, claudeAccounts.label)
          .orderBy(desc(sql`COALESCE(SUM(${agentStepExecutions.costUsd}), 0)`));

        return rows.map((r) => ({
          accountId: String(r.accountId),
          accountLabel: String(r.accountLabel),
          totalCostUsd: Number(r.totalCostUsd),
          totalInputTokens: Number(r.totalInputTokens),
          totalOutputTokens: Number(r.totalOutputTokens),
          stepCount: Number(r.stepCount),
        }));
      }

      return { aggregateByCompany };
    }

    export type ClaudeAccountCostsService = ReturnType<typeof claudeAccountCostsService>;
    ```

    JSDoc no topo: 'Phase 6 / D-09, D-10 / PROJ-03. Aggregate cost por (companyId, accountId) via JOIN agent_step_executions ⨝ heartbeat_runs.companyId ⨝ claude_accounts.label. D-01 invariante: cross-company isolation garantida pelo predicate em heartbeat_runs.companyId — runs de Company B não aparecem no resultado de Company A.'

    **Arquivo 2 — server/src/services/__tests__/claude-account-costs.test.ts** (~150-200 linhas):

    Seguir RED-GREEN-REFACTOR:
    - Setup: usar pattern de claude-accounts.test.ts (manual chainable Db mock OU se executor preferir, integration test gated por SUPABASE_DB_URL como AUTH-01 readiness suite). Recomendado: manual mock para velocidade e independência de Supabase.
    - 6 test cases cobrindo behavior listado acima.
    - Mock retorna estrutura `{ accountId, accountLabel, totalCostUsd, totalInputTokens, totalOutputTokens, stepCount }` com valores plantados; service apenas mapeia tipos (`Number()`).
    - Para Test 5 (cross-company), mock retorna apenas rows da company A; o test valida que a CALL ao mock incluiu `where ... eq(heartbeatRuns.companyId, 'companyA-uuid')` (verificar via spy do método `.where()` ou snapshot do query builder).

    Convenção: nome do file `__tests__/claude-account-costs.test.ts` (alinhado com claude-accounts-swap.test.ts).

    **Arquivo 3 — server/src/services/index.ts**:

    Adicionar export:
    ```typescript
    export { claudeAccountCostsService } from "./claude-account-costs.js";
    export type { CostSummaryRow, CostSummaryRange, ClaudeAccountCostsService } from "./claude-account-costs.js";
    ```

    Ordem de implementação:
    1. RED: criar test file com casos vazios (todos failing); commit `test(06-02): RED claude account costs aggregation`
    2. GREEN: implementar service; commit `feat(06-02): claude account costs aggregation service`
    3. REFACTOR (se necessário): limpar; commit `refactor(06-02): clean up`
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && pnpm --filter @paperclipai/server test -- claude-account-costs && pnpm --filter @paperclipai/server tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/src/services/claude-account-costs.ts` existe e exporta `claudeAccountCostsService`
    - `grep -c "aggregateByCompany" server/src/services/claude-account-costs.ts` >= 1
    - `grep -c "innerJoin" server/src/services/claude-account-costs.ts` >= 2 (heartbeatRuns + claudeAccounts)
    - `grep -c "heartbeatRuns.companyId" server/src/services/claude-account-costs.ts` >= 1 (cross-company isolation invariante)
    - Arquivo `server/src/services/__tests__/claude-account-costs.test.ts` existe com >= 6 `it(` ou `test(` cases
    - `pnpm --filter @paperclipai/server test -- claude-account-costs` passa todos os cases (>= 6 passes, 0 fails)
    - `pnpm --filter @paperclipai/server tsc --noEmit` exit code 0
    - `server/src/services/index.ts` exporta `claudeAccountCostsService`
  </acceptance_criteria>
  <done>Service tipado + 6 tests passing + export consumido pelo Plano 06-04 (UI) e Tarefa 2 deste plano (route).</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Endpoint REST GET /companies/:companyId/claude-accounts/cost-summary + tests</name>
  <files>
    server/src/routes/claude-accounts.ts,
    server/src/__tests__/claude-account-costs-route.test.ts
  </files>
  <read_first>
    - server/src/routes/claude-accounts.ts (todo o arquivo — 172 linhas; entender mount, permissions, ordem de routes)
    - .planning/phases/06-multi-projeto-polish/06-CONTEXT.md (D-11)
    - server/src/services/claude-account-costs.ts (criado na Tarefa 1)
  </read_first>
  <behavior>
    - Test A: Authenticated company member → 200 com `{ rows: CostSummaryRow[] }`
    - Test B: Unauthenticated / non-member → 401/403 (assertCompanyAccess rejeita)
    - Test C: Query params from/to ISO válidos → Date convertidos e passados para service
    - Test D: Query params inválidos (from não-ISO) → 400 com mensagem clara
    - Test E: from sem to (ou vice-versa) → aceito; service trata como bound único
  </behavior>
  <action>
    Conforme D-11:

    **Modificações em server/src/routes/claude-accounts.ts:**

    1. Imports adicionais no topo:
    ```typescript
    import { claudeAccountCostsService } from "../services/claude-account-costs.js";
    ```

    2. Adicionar zod schema próximo aos schemas existentes (linha ~30):
    ```typescript
    const costSummaryQuerySchema = z.object({
      from: z.string().datetime({ offset: true }).optional(),
      to: z.string().datetime({ offset: true }).optional(),
    });
    ```

    3. Dentro de `claudeAccountsRoutes(db)`, instanciar o service novo (próximo ao `const svc = claudeAccountsService(db);` linha 74):
    ```typescript
    const costsSvc = claudeAccountCostsService(db);
    ```

    4. Adicionar route handler IMEDIATAMENTE APÓS o handler de `/rotation-history` (linha ~102) — ordem importa: paths mais específicos antes do `/:accountId` PATCH genérico:
    ```typescript
    // GET cost summary — D-11 / PROJ-03 (Phase 6)
    router.get("/companies/:companyId/claude-accounts/cost-summary", async (req, res) => {
      assertCompanyAccess(req, String(req.params.companyId));
      const parsed = costSummaryQuerySchema.safeParse(req.query);
      if (!parsed.success) {
        res.status(400).json({ error: "Invalid query parameters", issues: parsed.error.issues });
        return;
      }
      const range = {
        from: parsed.data.from ? new Date(parsed.data.from) : undefined,
        to: parsed.data.to ? new Date(parsed.data.to) : undefined,
      };
      const rows = await costsSvc.aggregateByCompany(String(req.params.companyId), range);
      res.json({ rows });
    });
    ```

    Permissão: read-only para qualquer company member (D-11 — não restrito a owner/admin pois é informação operacional não-mutativa).

    NÃO modificar:
    - Handlers existentes (GET list, POST, PATCH, GET rotation-history)
    - Função `assertCompanyOwnerOrAdmin` (não aplicável — read-only)
    - Mount em app.ts (já feito em Phase 5)

    **Criar server/src/__tests__/claude-account-costs-route.test.ts:**

    Pattern: replicar `server/src/__tests__/claude-accounts-route.test.ts` (se existir; se não, replicar pattern de outro route test em `server/src/__tests__/`). Usar supertest contra Express app montada com Db mock injetado.

    5 cases cobrindo behavior acima. Mock `claudeAccountCostsService.aggregateByCompany` retornando array fixo.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && grep -n "cost-summary" server/src/routes/claude-accounts.ts && pnpm --filter @paperclipai/server test -- claude-account-costs-route && pnpm --filter @paperclipai/server tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cost-summary" server/src/routes/claude-accounts.ts` retorna 2 (route literal + comentário)
    - `grep -c "claudeAccountCostsService" server/src/routes/claude-accounts.ts` >= 1
    - `grep -c "costSummaryQuerySchema" server/src/routes/claude-accounts.ts` >= 1
    - Route declarada ANTES do `router.patch("/:accountId" ...)` (verificar ordem via line numbers — cost-summary antes de :accountId)
    - Arquivo `server/src/__tests__/claude-account-costs-route.test.ts` existe com >= 5 cases
    - Test suite passa (>= 5 passes, 0 fails)
    - `pnpm --filter @paperclipai/server tsc --noEmit` exit code 0
    - Handlers existentes (GET list, POST, PATCH, GET rotation-history) continuam declarados (regressão check)
  </acceptance_criteria>
  <done>Endpoint disponível em GET /api/companies/:companyId/claude-accounts/cost-summary?from=ISO&to=ISO; permissão via assertCompanyAccess; 5 tests passing; pronto para UI (06-04) consumir.</done>
</task>

</tasks>

<verification>
- 11+ tests novos passing (6 service + 5 route mínimo)
- TS clean em server
- Cross-company isolation invariante verificada via test e via predicate `eq(heartbeatRuns.companyId, ...)`
- Sem regressão em testes existentes da Phase 5
</verification>

<success_criteria>
- claudeAccountCostsService.aggregateByCompany funciona, validado por tests
- Endpoint REST live, permissão correta, response shape definido
- D-01 invariante (multi-tenant isolation) preservada e testada
- Plano 06-04 pode consumir endpoint via novo helper `claudeAccountsApi.costSummary`
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-projeto-polish/06-02-SUMMARY.md` documentando:
- Service signature final + decisões (innerJoin vs leftJoin para claude_accounts label, mapeamento de tipos sql<number>)
- Route path + permission model (D-11 read-only)
- Conteúdo dos test cases (lista enumerada)
- Confirmação que cross-company isolation foi testada (Test 5)
- Quaisquer desvios da especificação SQL (ex: COALESCE casts, ordering)
</output>
