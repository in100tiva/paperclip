---
phase: 03-workflow-de-equipe-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - TROUBLESHOOTING.md
autonomous: true
requirements: [TEAM-05]

must_haves:
  truths:
    - "Dev encontra solução para qualquer dos 7 problemas conhecidos sem ajuda externa"
    - "Cada entrada cita arquivo/linha relevante quando aplicável (ex: `packages/db/src/client.ts`)"
    - "Anchors em snake-case batem com os links que ONBOARDING.md já espera"
    - "Documento em pt-br conforme decisão specifics da Fase 3"
  artifacts:
    - path: "TROUBLESHOOTING.md"
      provides: "Guia de troubleshooting pt-br para devs do fork DDD"
      contains: "Windows NTFS|stale|too many connections|Cookie prefix|Schema desatualizado|claude CLI|prepared statement"
      min_lines: 150
  key_links:
    - from: "TROUBLESHOOTING.md"
      to: "scripts/kill-dev.sh"
      via: "menção em seção stale registry"
      pattern: "scripts/kill-dev\\.sh"
    - from: "TROUBLESHOOTING.md"
      to: "packages/db/src/client.ts"
      via: "citação de linha em seção prepared statements"
      pattern: "packages/db/src/client\\.ts"
    - from: "TROUBLESHOOTING.md"
      to: ".env.example"
      via: "referência em seções env vars"
      pattern: "\\.env\\.example"
---

<objective>
Criar `TROUBLESHOOTING.md` na raiz do repositório com 7 seções (decisão D-17), cada uma cobrindo um problema conhecido com formato Sintoma → Causa → Solução. Linguagem pt-br. Cada entrada cita arquivo/linha relevante (decisão D-18).

Purpose: Satisfazer TEAM-05. Critério de sucesso #5 do ROADMAP: "`TROUBLESHOOTING.md` cobre falhas comuns: Windows NTFS, Supabase no limite de conexões, Better Auth cookie prefix, schemas desatualizados."

Output: Documento descobrível via link no `ONBOARDING.md` (Plan 03-02) e em mensagens de erro do `pnpm setup` (Plan 03-01). Anchors em snake-case (`#windows-ntfs`, `#cookie-prefix-divergente`, etc.) que correspondem aos links já criados pelo ONBOARDING.md.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.env.example
@scripts/kill-dev.sh

<interfaces>
<!-- Achados específicos da Fase 1/2 que precisam aparecer no TROUBLESHOOTING -->

Wart documentado da Fase 1 (STATE.md "Bloqueios/Preocupações"):
> stale `~/.paperclip/instances/default/runtime-services/` survives `taskkill //F //T //PID`. Recomendar `scripts/kill-dev.sh`.

Achados Fase 2 (de STATE.md):
- 02-06: Better Auth rejeita signup sem header `Origin` com 403 `MISSING_OR_NULL_ORIGIN`. Clientes precisam enviar `Origin: <server-url>` ou hostname declarado em `BETTER_AUTH_TRUSTED_ORIGINS`. Capturado para TEAM-05.
- 02-06: `dev-runner.ts` em `pnpm dev` arranca em `local_trusted` mode (Better Auth handler não montado). Para exercitar `authenticated` mode: `PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev`.
- 02-03: `prepare:false` aplicado automaticamente quando porta=6543 em `packages/db/src/client.ts:83-86` (`buildPostgresOptions`).
- 02-04: GitHub Actions workflow `db-migrate.yml` é único caminho automatizado; auto-migrations no startup desabilitadas (DB-02).

Free tier Supabase: ~60 conexões shared. 5 devs × max=5 = 25 (de PROJECT.md).

Cookie prefix: `paperclip-team-shared` literal (validado em 02-05/02-06; ver `server/src/auth/better-auth.ts:65-71` deriveAuthCookiePrefix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar TROUBLESHOOTING.md com 7 seções de problemas conhecidos</name>
  <files>TROUBLESHOOTING.md</files>
  <read_first>
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` §"TROUBLESHOOTING.md (TEAM-05)" D-16, D-17, D-18 (cobertura mínima travada — 7 problemas)
    - `D:/projetos/ddd/.planning/STATE.md` "Bloqueios/Preocupações" + decisões 02-06 (achados Fase 2 a documentar)
    - `D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md` §A.1, F.1, G.1 (referências exatas a arquivos/linhas para citar)
    - `D:/projetos/ddd/scripts/kill-dev.sh` (verificar que o script existe — vai ser referenciado na seção stale registry)
    - `D:/projetos/ddd/packages/db/src/client.ts` linhas 74-94 (função `buildPostgresOptions` — citação exata para seção prepared statements)
    - `D:/projetos/ddd/server/src/auth/better-auth.ts` linhas 65-71 (`deriveAuthCookiePrefix` — citação exata para seção cookie prefix)
    - `D:/projetos/ddd/.env.example` (instruções inline e templates de vars)
  </read_first>
  <action>
Criar arquivo NOVO em `D:/projetos/ddd/TROUBLESHOOTING.md`. Idioma: **pt-br**. Estrutura: 7 seções H2 com slug em snake-case kebab-case (compatível com anchors `#windows-ntfs`, `#cookie-prefix-divergente` que ONBOARDING.md já referencia).

Conteúdo literal (copiar com adaptações apenas se algum arquivo/linha citado divergir do que vc encontrou em `read_first`):

```markdown
# Troubleshooting — DDD

> **Idioma:** pt-br. Onboarding em [ONBOARDING.md](ONBOARDING.md). Este doc cobre falhas comuns que travam o setup local.
>
> **Formato de cada entrada:** Sintoma → Causa → Solução. Citações apontam para arquivos do repo quando aplicável.

## Windows NTFS

**Sintoma:** `pnpm install` falha com `EBUSY: resource busy or locked` ou paths cortados (`ENAMETOOLONG`). Linhas com `\r\n` no lugar de `\n` em scripts shell.

**Causa:**
- File locks em `node_modules` quando o servidor de dev ainda está rodando ou um editor segura arquivos.
- Limit de path do Windows (260 chars) com pastas profundas em `node_modules`.
- Git auto-converte LF→CRLF em arquivos de texto se `core.autocrlf=true`.

**Solução:**

1. Garanta que nenhum processo Node está rodando:
   ```powershell
   # Bash (Git Bash):
   ./scripts/kill-dev.sh
   # PowerShell:
   Get-Process node | Stop-Process -Force
   ```
2. Habilite long paths no Git e no Windows:
   ```powershell
   git config --global core.longpaths true
   # PowerShell admin:
   New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
   ```
3. Force LF para shell scripts:
   ```bash
   git config --global core.autocrlf input
   git rm --cached -r .
   git reset --hard HEAD
   ```
4. Se o problema persistir em `node_modules`, delete e reinstale:
   ```powershell
   Remove-Item -Recurse -Force node_modules
   pnpm install
   ```

***

## Stale runtime-services registry

**Sintoma:** Após `taskkill /F /T /PID <pid>` (ou kill manual no Windows), arquivos em `~/.paperclip/instances/default/runtime-services/` permanecem. Próximo `pnpm dev` reclama que serviço já está rodando ou falha por conflito de porta 3100/54329.

**Causa:** `taskkill /F` no Windows não dispara cleanup hooks do Node (sem `SIGTERM` real), então PID files e symlinks ficam órfãos. Wart conhecido — documentado em `.planning/phases/01-fork-hard-cerim-nia-de-corte/SMOKE-TEST-LOG.md`.

**Solução:**

```bash
./scripts/kill-dev.sh
```

Esse script encontra processos do paperclip dev (worktrees inclusos), mata graceful, e limpa o registry stale. Ver `scripts/kill-dev.sh` para detalhes. Se mesmo após `kill-dev.sh` o diretório persistir:

```bash
# Last resort (Windows PowerShell ou Git Bash):
rm -rf ~/.paperclip/instances/default/runtime-services/
```

***

## Supabase no limite de conexões (`too many connections`)

**Sintoma:** Connection error `FATAL: too many connections for role "postgres"` durante `pnpm dev`, `pnpm setup` ou queries do app.

**Causa:** Free tier do Supabase compartilha ~60 conexões. Cada `pnpm dev` consome até 5 conexões (pool `max: 5` configurado em `packages/db/src/client.ts:85`). Com 5 devs × 5 = 25, está OK; mas se múltiplas instâncias do servidor rodam simultaneamente no mesmo dev (ex: dois `pnpm dev` em duas pastas), o limite estoura.

**Solução:**

1. Cheque quantos `pnpm dev` você tem rodando localmente:
   ```bash
   ./scripts/kill-dev.sh --dry
   ```
2. Pergunte no canal interno do time quem mais está rodando — provavelmente alguém esqueceu uma instância subida.
3. Mate instâncias órfãs com `./scripts/kill-dev.sh`.
4. Se precisar reduzir ainda mais o footprint local, edite `packages/db/src/client.ts` (linha 85) reduzindo `max: 5` para `max: 3` — mas isso afeta concorrência do app, prefira matar instâncias.

> Ver `MIGRATION_AUDIT.md §G.1` para a justificativa do `max: 5`.

***

## Cookie prefix divergente

**Sintoma:** Você fez signup com sucesso no servidor `authenticated` mode, mas o front mostra "não logado" / não vê a company que outro dev criou. Inspecionando cookies, vê algo como `paperclip-meunome.session_token` em vez de `paperclip-team-shared.session_token`.

**Causa:** `PAPERCLIP_INSTANCE_ID` no seu `.env.local` está diferente de `team-shared`. O cookie prefix é derivado dessa var em `server/src/auth/better-auth.ts:65-71` (`deriveAuthCookiePrefix`). Se cada dev tiver um valor diferente, sessões não interoperam — cada dev fica numa "ilha" lógica.

**Solução:**

1. Edite `.env.local`:
   ```bash
   PAPERCLIP_INSTANCE_ID=team-shared
   ```
2. Rode `pnpm setup` — o check `env var` vai validar isso explicitamente.
3. Limpe os cookies antigos no browser (DevTools → Application → Cookies → delete tudo de `localhost:3100`).
4. Reinicie o servidor e faça login de novo.

> O literal `team-shared` foi validado empiricamente na Fase 2 (cookie `paperclip-team-shared.session_token`). Não invente outros valores.

***

## Schema desatualizado

**Sintoma:** Erro Drizzle ao logar ou subir o servidor: `relation "user" does not exist`, `column "X" does not exist`, ou `pnpm setup` falha em "Better Auth schema".

**Causa:** Migrations no `packages/db/src/migrations/` foram adicionadas em PRs recentes, mas ainda não foram aplicadas ao Supabase compartilhado pelo workflow `.github/workflows/db-migrate.yml`. Auto-migrations no startup estão desabilitadas (DB-02 — fail-fast com 5+ devs).

**Solução:**

1. Atualize seu repo:
   ```bash
   git pull origin main
   pnpm install
   ```
2. Verifique se o workflow já rodou:
   - GitHub → Actions → "DB Migrate (Supabase)"
   - Se ainda está running, espere terminar.
   - Se não rodou ainda (último merge sem schema change), confirme com quem fez a alteração.
3. Se o workflow falhou (improvável, mas possível): peça pra um maintainer rodar manualmente via `workflow_dispatch` (Actions → workflow → "Run workflow").
4. **NÃO** rode `pnpm db:migrate` localmente apontando pro Supabase compartilhado — viola DB-03 (apenas CI aplica). Ver `CONTRIBUTING.md §"Database Migration Policy"`.

> Para desenvolvimento offline (sem Supabase), use embedded postgres:
> ```bash
> # .env.local
> PAPERCLIP_DB_MODE=embedded-postgres
> ```

***

## `claude` CLI ausente

**Sintoma:** `pnpm setup` imprime `⚠ claude CLI não encontrado no PATH`. Ou comandos da Fase 4+ (multi-account) falham com `command not found: claude`.

**Causa:** O CLI oficial do Claude Code não está instalado, ou está em um diretório fora do PATH.

**Solução:**

Siga a instalação oficial: [https://docs.claude.com/en/docs/claude-code/setup](https://docs.claude.com/en/docs/claude-code/setup).

Após instalar, valide:

```bash
which claude     # macOS/Linux
where claude     # Windows
claude --version
```

Se o `which`/`where` retornar caminho mas `pnpm setup` ainda reclamar, o terminal pode não estar enxergando o PATH atualizado — feche e reabra o terminal.

> Aviso, não fatal: você pode operar nas Fases 1-3 sem o CLI. Ele só vira obrigatório a partir da Fase 4 (spike multi-account).

***

## `prepared statement does not exist` (pooler 6543)

**Sintoma:** Queries normalmente OK, mas intermitentemente falham com `prepared statement "sN" does not exist`. Ocorre principalmente sob concorrência ou em ambientes recém-deployados.

**Causa:** Supavisor transaction pooler (porta 6543) reusa conexões físicas entre transactions de clientes diferentes. postgres-js cacheia prepared statements por conexão lógica — quando a conexão física é trocada, o statement não existe mais. Ver `MIGRATION_AUDIT.md §A.1` (HIGH risk documentado).

**Solução já aplicada no codebase:**

`packages/db/src/client.ts` (função `buildPostgresOptions`, linhas 74-94) detecta automaticamente porta 6543 e aplica `prepare: false`:

```ts
if (port === "6543") {
  return { prepare: false, max: 5, idle_timeout: 20, connect_timeout: 10 };
}
```

**Se você está vendo esse erro mesmo assim, possíveis causas:**

1. **Você criou um novo arquivo que faz `postgres(url, ...)` direto sem usar `createDb` / `createUtilitySql`.** Solução: importe a factory de `@paperclipai/db` em vez de instanciar `postgres-js` manualmente.
2. **Você passou `SUPABASE_DB_URL` (porta 5432) onde o código espera `DATABASE_URL` (porta 6543), ou vice-versa.** Cheque `.env.local` — porta 6543 é runtime, 5432 é DDL/migrations.
3. **Você adicionou `prepare: true` explicitamente em algum lugar.** Remova.

Para validar quais ports você está usando agora:

```bash
node -e "console.log(new URL(process.env.DATABASE_URL).port, new URL(process.env.SUPABASE_DB_URL).port)"
```

Esperado: `6543 5432`.

***

## Não achou seu problema?

Pergunte no canal interno do time. Se descobrir um novo problema comum, abra um PR adicionando uma nova seção aqui — todo mundo agradece.
```

Notas:
- **NÃO** mencionar problemas de Phase 4+ (multi-account) que ainda não existem.
- Cada anchor da seção (slug do H2 em lowercase com hyphens) deve corresponder ao link que ONBOARDING.md já cria. Validar batendo:
  - `#windows-ntfs`
  - `#stale-runtime-services-registry`
  - `#supabase-no-limite-de-conex-es-too-many-connections` (GitHub gera assim — ou usar id explícito; se quisermos garantir, escrever o título como `Supabase no limite de conexões` sem caracteres especiais)
  - `#cookie-prefix-divergente`
  - `#schema-desatualizado`
  - `#claude-cli-ausente`
  - `#prepared-statement-does-not-exist-pooler-6543`
- Se descobrir que `scripts/kill-dev.sh` NÃO existe ou não funciona como descrito ao ler o read_first, ajuste a seção "Stale runtime-services registry" para apontar para a alternativa real (mas o read_first já confirmou que existe).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('TROUBLESHOOTING.md','utf8'); const sections=['Windows NTFS','Stale','too many connections','Cookie prefix','Schema desatualizado','claude.{1,5}CLI','prepared statement']; for(const s of sections){const r=new RegExp(s,'i'); if(!r.test(c)){console.error('MISSING SECTION:',s);process.exit(1)}}; const refs=['scripts/kill-dev.sh','packages/db/src/client.ts','better-auth.ts','.env.local']; for(const r of refs){if(!c.includes(r)){console.error('MISSING REF:',r);process.exit(2)}}; if(c.split('\\n').length<150){console.error('TOO SHORT');process.exit(3)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/TROUBLESHOOTING.md` existe
    - Arquivo tem >= 150 linhas (`wc -l TROUBLESHOOTING.md` >= 150)
    - `grep -c "^## " D:/projetos/ddd/TROUBLESHOOTING.md` retorna >= 7 (sete problemas + opcional "Não achou seu problema")
    - Seção "Windows NTFS" presente: `grep -q "## Windows NTFS" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção stale registry: `grep -qi "stale" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "scripts/kill-dev.sh" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção too-many-connections: `grep -q "too many connections" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção cookie prefix: `grep -q "Cookie prefix" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "team-shared" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "better-auth.ts" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção schema: `grep -q "Schema desatualizado" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "db-migrate.yml" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção claude CLI: `grep -qi "claude.\{1,5\}CLI" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Seção prepared statement: `grep -q "prepared statement" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "packages/db/src/client.ts" D:/projetos/ddd/TROUBLESHOOTING.md` E `grep -q "6543" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Doc é em pt-br (citações de palavras pt-br): `grep -q "Sintoma\|Causa\|Solução" D:/projetos/ddd/TROUBLESHOOTING.md`
    - Cita pelo menos 3 caminhos de arquivo do repo (regra D-18): `grep -cE "(packages/db|server/src|scripts/|\.env\.local)" D:/projetos/ddd/TROUBLESHOOTING.md` retorna >= 5
  </acceptance_criteria>
  <done>TROUBLESHOOTING.md criado com 7 seções pt-br cobrindo D-17; cada seção cita arquivo/linha quando aplicável (D-18); links cruzados consistentes com ONBOARDING.md.</done>
</task>

</tasks>

<verification>
- `wc -l TROUBLESHOOTING.md` >= 150
- 7 seções H2 presentes (Windows NTFS, Stale, Supabase, Cookie prefix, Schema, claude CLI, Prepared statement)
- Links cruzados batem com ONBOARDING.md (anchors `#windows-ntfs` etc.)
- Citações de arquivos do repo (`packages/db/src/client.ts`, `server/src/auth/better-auth.ts`, `scripts/kill-dev.sh`) presentes
</verification>

<success_criteria>
1. TROUBLESHOOTING.md cobre todos os 7 problemas listados em D-17.
2. Cada seção tem formato Sintoma → Causa → Solução com comando(s) acionável(is).
3. Cada problema com causa em código cita arquivo + linha (D-18).
4. TEAM-05 endereçado: critério #5 do ROADMAP cumprido.
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-de-equipe-onboarding/03-03-SUMMARY.md` documenting: lista exata das 7 seções escritas, anchors GitHub-flavored gerados, qualquer divergência da estrutura proposta no action.
</output>
