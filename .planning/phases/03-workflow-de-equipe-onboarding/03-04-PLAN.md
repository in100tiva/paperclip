---
phase: 03-workflow-de-equipe-onboarding
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - .planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md
  - .planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md
autonomous: true
requirements: [TEAM-04]

must_haves:
  truths:
    - "Procedimento documentado de smoke test cross-machine existe e referencia ferramentas reais (kill-dev.sh, pnpm setup, pnpm dev modo authenticated)"
    - "Fallback single-host (D-14) está documentado com limitação explícita"
    - "Itens que requerem dois devs reais em máquinas distintas estão roteados para HUMAN-UAT, não fingidos como auto-passable"
    - "Procedimento valida o critério #4 do ROADMAP (dev A cria company, dev B em outra máquina vê a mesma company sem sync manual)"
  artifacts:
    - path: ".planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md"
      provides: "Procedimento manual de smoke E2E cross-machine + fallback single-host"
      contains: "Dev A|Dev B|máquina|invite|company|fallback|two-browser"
      min_lines: 80
    - path: ".planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md"
      provides: "Lista de itens que dependem de validação humana real (executor não pode fechar sozinho)"
      contains: "TEAM-04|cross-machine|status:"
      min_lines: 30
  key_links:
    - from: "CROSS-MACHINE-SMOKE.md"
      to: "ONBOARDING.md"
      via: "referência ao setup que cada dev deve seguir antes do smoke"
      pattern: "ONBOARDING\\.md"
    - from: "CROSS-MACHINE-SMOKE.md"
      to: "TROUBLESHOOTING.md"
      via: "referência para falhas durante execução"
      pattern: "TROUBLESHOOTING\\.md"
    - from: "03-HUMAN-UAT.md"
      to: "CROSS-MACHINE-SMOKE.md"
      via: "linka procedimento que humano deve executar"
      pattern: "CROSS-MACHINE-SMOKE\\.md"
---

<objective>
Documentar o procedimento manual de smoke test E2E cross-machine (`CROSS-MACHINE-SMOKE.md`) que valida estado compartilhado entre dois devs em máquinas distintas, e registrar em `03-HUMAN-UAT.md` os itens que requerem execução humana real (não automatizável pelo executor).

Purpose: Satisfazer TEAM-04. Critério de sucesso #4 do ROADMAP: "Smoke test E2E executado: dev A faz login + cria company; dev B em outra máquina vê a mesma company sem sincronização manual."

Output: Procedimento step-by-step + fallback single-host (D-14) documentado. Item HUMAN-UAT explícito porque o executor (Claude rodando em ambiente único) não consegue genuinamente provar "duas máquinas distintas" sem coordenação humana real.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-HUMAN-UAT.md
@ONBOARDING.md
@TROUBLESHOOTING.md
@doc/spec/invite-flow.md

<interfaces>
<!-- Padrões da Fase 2 a seguir + comandos específicos a citar -->

Comando para subir servidor em modo `authenticated` (de 02-CONTEXT.md decisões 02-06):
```bash
PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
```

Comando para gerar bootstrap invite (de packages/db/scripts/create-auth-bootstrap-invite.ts):
```bash
tsx packages/db/scripts/create-auth-bootstrap-invite.ts --config <path-to-config> --base-url http://localhost:3100
```

Cookie prefix esperado: `paperclip-team-shared` (validado em 02-05).

Decisão D-13 estrutura do procedimento:
1. Dev A em máquina X: pnpm dev → signup → criar company "Test Co"
2. Dev B em máquina Y (mesmo Supabase, mesmo PAPERCLIP_INSTANCE_ID): aceitar invite → login → ver "Test Co"
3. Dev B cria task em "Test Co"; Dev A faz refresh → vê task

Decisão D-14: fallback single-host com two-browser-profiles aceito, com limitação documentada.
Decisão D-15: resultado registrado em log com timestamps + PASS/FAIL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar CROSS-MACHINE-SMOKE.md com procedimento + fallback</name>
  <files>.planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md</files>
  <read_first>
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` §"Smoke Test E2E Cross-Machine (TEAM-04)" D-13, D-14, D-15 (procedimento travado)
    - `D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md` (padrão da Fase 2 — formato de log, timestamps, PASS/FAIL marker)
    - `D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/02-HUMAN-UAT.md` (formato de HUMAN-UAT existente; estrutura de status `pending`/`approved`)
    - `D:/projetos/ddd/doc/spec/invite-flow.md` linhas 1-120 (entender que `bootstrap_ceo` é one-time + `company_join` flow para demais devs)
    - `D:/projetos/ddd/ONBOARDING.md` (deve estar criado pelo Plan 03-02 — referenciar passos sem duplicar)
  </read_first>
  <action>
Criar arquivo NOVO em `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md`. Idioma: pt-br (consistência com docs da equipe).

Conteúdo:

```markdown
# Smoke Test E2E Cross-Machine — Phase 3

**Phase:** 03 — Workflow de Equipe + Onboarding
**Requirement satisfied:** TEAM-04
**Padrão de execução:** manual (dois devs reais ou fallback single-host)
**Reference padrão:** `.planning/phases/02-migra-o-de-storage-para-supabase/SMOKE-TEST-LOG.md`

## Objetivo

Provar empiricamente que o critério de sucesso #4 do ROADMAP da Phase 3 está satisfeito:

> Smoke test E2E executado: dev A faz login + cria company; dev B em outra máquina vê a mesma company sem sincronização manual.

Cobertura: estado compartilhado via Supabase remoto + cookie prefix `paperclip-team-shared` interoperável entre máquinas.

## Pré-requisitos (ambos os devs)

Antes de começar, cada dev DEVE ter completado o [ONBOARDING.md](../../../ONBOARDING.md):

- [ ] `pnpm install` rodou sem erros
- [ ] `.env.local` preenchido com valores reais (não `TODO_FILL_ME`)
- [ ] `PAPERCLIP_INSTANCE_ID=team-shared` literal em ambos os `.env.local`
- [ ] `pnpm setup` saiu com `✅ Ambiente OK` em ambas as máquinas
- [ ] Mesma `DATABASE_URL` (mesmo project Supabase `bxlczioxgizgvtznukwt`)

Se `pnpm setup` reclamar em qualquer máquina, resolver via [TROUBLESHOOTING.md](../../../TROUBLESHOOTING.md) antes de prosseguir.

## Procedimento Cross-Machine (canônico — D-13)

### Setup inicial: Dev #1 (CEO bootstrap, one-time)

> **Pular esta seção se a equipe já tem company criada.** O bootstrap_ceo só roda uma vez no time inteiro (decisão D-10 da Fase 3).

1. Dev #1 sobe o servidor em modo authenticated:
   ```bash
   PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
   ```
2. Dev #1 gera o bootstrap invite:
   ```bash
   tsx packages/db/scripts/create-auth-bootstrap-invite.ts \
     --config ~/.paperclip/instances/default/config.json \
     --base-url http://localhost:3100
   ```
   Output: URL `http://localhost:3100/invite/<token>`.
3. Dev #1 abre essa URL no browser, faz signup com email/senha, aceita o invite. A company padrão (`Test Co`, `Acme`, ou nome real escolhido) é criada.

### Smoke cross-machine

Pré-condição: company existe na Supabase (criada pelo Dev #1 acima OU já existente do time).

**Dev A — máquina X**

1. Subir servidor authenticated (comando da seção anterior).
2. Login com sua conta Better Auth (email/senha do time).
3. Confirmar que vê a company compartilhada no dashboard.
4. Criar uma nova task com título reconhecível: `cross-machine-smoke-<YYYYMMDD-HHMM>`.
5. Anotar:
   - Hora exata da criação (`HH:MM`)
   - URL da task (ex: `http://localhost:3100/tasks/<id>`)
   - Cookie de sessão presente: DevTools → Application → Cookies → procurar `paperclip-team-shared.session_token` (deve existir).

**Dev B — máquina Y (diferente da X)**

1. Subir servidor authenticated.
2. Login com sua conta Better Auth (DIFERENTE da do Dev A).
3. **Sem refresh manual da página**, navegar para o dashboard:
   - Esperado: ver a mesma company que Dev A está vendo.
   - Esperado: ver a task `cross-machine-smoke-<YYYYMMDD-HHMM>` recém-criada.
4. Criar uma segunda task: `cross-machine-smoke-reply-<YYYYMMDD-HHMM>`.
5. Anotar hora.

**Dev A — verificação reversa**

1. Refresh do browser (F5).
2. Esperado: ver `cross-machine-smoke-reply-<YYYYMMDD-HHMM>` criada pelo Dev B.

**Critério PASS:**
- Dev B vê a company de Dev A sem ter feito setup adicional.
- Dev A vê a task criada por Dev B após refresh simples (sem `pnpm db:migrate`, sem coordenação fora-de-banda).
- Cookies de ambos têm prefix `paperclip-team-shared.`.

**Critério FAIL:**
- Dev B vê dashboard vazio ou company diferente.
- Cookie prefix divergente (significa `PAPERCLIP_INSTANCE_ID` diferente em algum lado).
- Erro `relation "user" does not exist` (schema desatualizado em uma das máquinas).

## Fallback: Single-Host (D-14)

**Quando usar:** Se a equipe não conseguir coordenar dois devs em máquinas físicas distintas para a janela do smoke (ex: time pequeno, time-zones, indisponibilidade momentânea).

**Limitação:** Single-host **não prova** que o cookie prefix funciona literalmente entre máquinas distintas — apenas prova interoperabilidade entre sessions diferentes na mesma máquina. É uma aproximação aceitável para fechar TEAM-04 com sinal "infra OK", mas a prova final cross-machine deve ser feita assim que possível e logada como amendment.

### Procedimento single-host (two-browser-profiles)

1. Subir servidor authenticated (uma vez, na máquina única):
   ```bash
   PAPERCLIP_DEPLOYMENT_MODE=authenticated pnpm --filter @paperclipai/server dev
   ```
2. Abrir **dois browsers DIFERENTES** ou **dois perfis isolados do mesmo browser** (ex: Chrome perfil "Dev A" + Firefox, ou Chrome perfil padrão + perfil "Convidado"). **NÃO** usar duas abas do mesmo perfil — elas compartilham cookies.
3. Em "Dev A" (browser 1): login com conta A → criar task `single-host-smoke-A-<HHMM>`.
4. Em "Dev B" (browser 2): login com conta B → confirmar que vê a task de A.
5. Em "Dev B": criar task `single-host-smoke-B-<HHMM>`.
6. Em "Dev A": refresh → confirmar que vê a task de B.

**Critério PASS (single-host):** Mesmo comportamento do cross-machine, com limitação documentada de que cookies vivem na mesma máquina.

## Registro de Resultado (D-15)

Cada execução do smoke (cross-machine ou fallback) é logada num bloco abaixo. Padrão segue `02-SMOKE-TEST-LOG.md`.

### Execução 1

- **Data/hora:** `<YYYY-MM-DD HH:MM>` (timezone)
- **Modo:** `cross-machine` ou `single-host-fallback`
- **Dev A:** `<email ou handle>` na máquina `<descrição>`
- **Dev B:** `<email ou handle>` na máquina `<descrição>`
- **Resultado:** `PASS` / `FAIL`
- **Observações:**
  - Cookie prefix observado: `paperclip-team-shared.session_token` ✓ / ✗
  - Tasks visíveis cross-account: ✓ / ✗
  - Erros encontrados: `<nenhum>` ou `<descrição>`
- **Screenshots opcionais:** anexar paths se houver.

(Adicionar blocos `### Execução 2`, etc., conforme novas execuções.)

## Rollback / Recovery

Se o smoke FAIL:

1. Confirmar `PAPERCLIP_INSTANCE_ID=team-shared` em **ambos** os `.env.local`.
2. Confirmar que ambos apontam pro mesmo Supabase: `node -e "console.log(new URL(process.env.DATABASE_URL).hostname, new URL(process.env.DATABASE_URL).username)"` deve dar mesma saída em ambas as máquinas.
3. Limpar cookies do browser (`paperclip-*`) e re-login.
4. Verificar [TROUBLESHOOTING.md](../../../TROUBLESHOOTING.md):
   - [Cookie prefix divergente](../../../TROUBLESHOOTING.md#cookie-prefix-divergente)
   - [Schema desatualizado](../../../TROUBLESHOOTING.md#schema-desatualizado)
5. Se ainda falhar, abrir issue interna no canal do time.

## Status

- [ ] Procedimento cross-machine documentado (este arquivo)
- [ ] Fallback single-host documentado (este arquivo)
- [ ] Pelo menos uma execução registrada (será preenchida ao executar; ver `03-HUMAN-UAT.md` para tracking)
```

Notas:
- Não tentar "executar" o smoke aqui — o executor não tem duas máquinas distintas. A execução real é HUMAN-UAT (ver Tarefa 2).
- O procedimento deve ser auto-suficiente: outro dev humano consegue executar lendo só este doc.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md','utf8'); const checks=[/Dev A/,/Dev B/,/máquina/i,/invite/i,/PAPERCLIP_INSTANCE_ID/,/team-shared/,/fallback/i,/single-host/i,/two-browser/i,/PASS/,/FAIL/,/ONBOARDING\.md/,/TROUBLESHOOTING\.md/]; for(const r of checks){if(!r.test(c)){console.error('MISSING:',r);process.exit(1)}}; if(c.split('\\n').length<80){console.error('TOO SHORT');process.exit(2)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md` existe
    - Arquivo tem >= 80 linhas
    - Procedimento cross-machine canônico documentado: `grep -q "Dev A" CROSS-MACHINE-SMOKE.md && grep -q "Dev B" CROSS-MACHINE-SMOKE.md && grep -q "máquina" CROSS-MACHINE-SMOKE.md`
    - Fallback single-host documentado com limitação: `grep -qi "fallback" CROSS-MACHINE-SMOKE.md && grep -qi "single-host" CROSS-MACHINE-SMOKE.md && grep -qi "two-browser\|browsers diferentes\|perfis" CROSS-MACHINE-SMOKE.md`
    - Cita comando authenticated mode: `grep -q "PAPERCLIP_DEPLOYMENT_MODE=authenticated" CROSS-MACHINE-SMOKE.md`
    - Cita bootstrap invite script: `grep -q "create-auth-bootstrap-invite" CROSS-MACHINE-SMOKE.md`
    - Critério PASS/FAIL explícito: `grep -q "PASS" CROSS-MACHINE-SMOKE.md && grep -q "FAIL" CROSS-MACHINE-SMOKE.md`
    - Cookie prefix `paperclip-team-shared` mencionado: `grep -q "paperclip-team-shared" CROSS-MACHINE-SMOKE.md`
    - Linka ONBOARDING e TROUBLESHOOTING: `grep -q "ONBOARDING.md" CROSS-MACHINE-SMOKE.md && grep -q "TROUBLESHOOTING.md" CROSS-MACHINE-SMOKE.md`
    - Bloco de "Registro de Resultado" presente: `grep -q "Execução 1\|Data/hora" CROSS-MACHINE-SMOKE.md`
  </acceptance_criteria>
  <done>CROSS-MACHINE-SMOKE.md documentado com procedimento canônico + fallback single-host + bloco de registro de resultado.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Criar 03-HUMAN-UAT.md roteando execução real para humanos</name>
  <files>.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md</files>
  <read_first>
    - `D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/02-HUMAN-UAT.md` (formato existente — frontmatter, estrutura de items, status)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md` (criado na Tarefa 1; o UAT linka para o procedimento)
    - `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-CONTEXT.md` D-13, D-14, D-15
  </read_first>
  <action>
Criar arquivo NOVO em `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md`. Frontmatter e estrutura seguem o padrão da Fase 2 (`02-HUMAN-UAT.md`).

Conteúdo:

```markdown
***
phase: 03-workflow-de-equipe-onboarding
type: human-uat
created: 2026-04-26
status: pending
***

# Phase 3 Human UAT — Workflow de Equipe + Onboarding

Itens que requerem execução humana real para fechar Phase 3. O executor automatizado (Claude) **não pode** validar sozinho:

- Comportamento entre duas máquinas físicas distintas com network real do Supabase
- Coordenação temporal entre dois devs reais ao mesmo tempo
- Verificação visual de UI (dashboard renderizando company corretamente em browsers reais)

Estes itens NÃO bloqueiam a entrega de artefatos da Phase 3 (docs + script existem), mas bloqueiam a marcação de **TEAM-04** como complete.

## UAT-03-01 — Smoke Cross-Machine (TEAM-04)

**Status:** `pending`

**Procedure:** Ver [CROSS-MACHINE-SMOKE.md](CROSS-MACHINE-SMOKE.md). Executar o "Procedimento Cross-Machine (canônico — D-13)".

**Acceptance:**
- Dois devs distintos em máquinas físicas distintas executam o procedimento.
- Resultado registrado no bloco "Execução 1" (ou seguinte) de CROSS-MACHINE-SMOKE.md com `PASS`.
- Cookie prefix `paperclip-team-shared` confirmado em ambos os browsers.
- Estado compartilhado: Dev B vê company/tasks criadas por Dev A sem refresh manual fora do browser.

**Fallback aceito:** Single-host (Procedimento "Fallback Single-Host" do CROSS-MACHINE-SMOKE.md), com a limitação documentada de que não prova literalmente cross-machine. Se usado fallback, marcar status como `pending-cross-machine` e re-rodar cross-machine real assim que possível.

**Quando aprovado:**
1. Atualizar `status: pending` → `status: approved`
2. Adicionar campo `approved_at: <data>` e `approved_by: <handle>`
3. Anotar ID/timestamp da execução em CROSS-MACHINE-SMOKE.md.

***

## UAT-03-02 — Cadastro de 5+ Devs Reais (TEAM-01)

**Status:** `pending`

**Procedure:** Ver [03-05-PLAN.md](03-05-PLAN.md) (plano que documenta o procedimento de bootstrap + cadastro coletivo).

**Acceptance:**
- Tabela `user` no Supabase compartilhado tem >= 5 rows.
- Todos os 5+ users são membros da mesma `company` (validável via SQL).
- Pelo menos um deles é o `bootstrap_ceo` original.
- Os demais entraram via fluxo `company_join` (não foram criados manualmente via SQL).

**SQL de validação** (executar via Supabase Studio SQL Editor):

```sql
-- Conta total de users
SELECT count(*)::int AS total_users FROM "user";

-- Membership por company
SELECT c.id AS company_id, c.name, count(cm.user_id)::int AS members
FROM company c
LEFT JOIN company_membership cm ON cm.company_id = c.id
GROUP BY c.id, c.name
ORDER BY members DESC;
```

Esperado: `total_users >= 5` e a company principal com `members >= 5`.

**Fallback parcial aceito:** Se < 5 devs reais disponíveis na janela atual, anotar contagem real (ex: "3/5 cadastrados em 2026-04-XX"), marcar status `pending-team-growth`, e re-checar quando atingir 5.

**Quando aprovado:**
1. Atualizar `status` → `approved`
2. Adicionar `approved_at: <data>` e contagem final de users
3. Capturar output da SQL acima inline aqui para histórico.

***

## Sumário

- **Total UAT items:** 2
- **Approved:** 0
- **Pending:** 2

A Phase 3 fecha como **complete-with-pending-UAT** se ambos os UATs estiverem `pending` mas todos os artefatos de Wave 1 (scripts/setup.ts, ONBOARDING.md, TROUBLESHOOTING.md) e Wave 2 (CROSS-MACHINE-SMOKE.md, este arquivo, 03-05-PLAN.md docs) estiverem entregues. Os UATs viram trabalho manual contínuo da equipe e podem ser fechados em sessões posteriores sem replanejar a fase.
```

Notas:
- Status inicial é `pending` em ambos — o executor não pode resolver sozinho.
- Linka para `03-05-PLAN.md` que será o próximo plano (Wave 2 paralelo).
- SQL de validação dado pronto pra copy-paste no Supabase Studio.
- Não inventar timestamps de aprovação.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md'; const c=fs.readFileSync(p,'utf8'); const checks=[/^---/m,/phase: 03-workflow-de-equipe-onboarding/,/status: pending/,/UAT-03-01/,/UAT-03-02/,/TEAM-04/,/TEAM-01/,/CROSS-MACHINE-SMOKE\.md/,/cross-machine/i,/SELECT count\(\*\)/,/Fallback/i]; for(const r of checks){if(!r.test(c)){console.error('MISSING:',r);process.exit(1)}}; if(c.split('\\n').length<30){console.error('TOO SHORT');process.exit(2)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `D:/projetos/ddd/.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md` existe
    - Arquivo tem >= 30 linhas
    - Frontmatter YAML válido com `phase: 03-workflow-de-equipe-onboarding`, `type: human-uat`, `status: pending`
    - Item UAT-03-01 (TEAM-04 cross-machine) presente: `grep -q "UAT-03-01" 03-HUMAN-UAT.md && grep -q "TEAM-04" 03-HUMAN-UAT.md`
    - Item UAT-03-02 (TEAM-01 5+ devs) presente: `grep -q "UAT-03-02" 03-HUMAN-UAT.md && grep -q "TEAM-01" 03-HUMAN-UAT.md`
    - Linka CROSS-MACHINE-SMOKE: `grep -q "CROSS-MACHINE-SMOKE.md" 03-HUMAN-UAT.md`
    - Inclui SQL de validação para TEAM-01: `grep -q "SELECT count" 03-HUMAN-UAT.md`
    - Status inicial `pending` em ambos os UATs: `grep -c "Status:.*pending\|status: pending" 03-HUMAN-UAT.md` retorna >= 2
    - Fallback documentado para ambos os UATs: `grep -ci "fallback" 03-HUMAN-UAT.md` retorna >= 2
  </acceptance_criteria>
  <done>03-HUMAN-UAT.md criado com 2 itens (TEAM-04 cross-machine, TEAM-01 5+ devs), status pending, fallbacks aceitos documentados, SQL de validação para TEAM-01.</done>
</task>

</tasks>

<verification>
- `wc -l .planning/phases/03-workflow-de-equipe-onboarding/CROSS-MACHINE-SMOKE.md` >= 80
- `wc -l .planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md` >= 30
- Frontmatter `03-HUMAN-UAT.md` parseable: `node -e "const yaml=require('js-yaml'); const fm=require('fs').readFileSync('.planning/phases/03-workflow-de-equipe-onboarding/03-HUMAN-UAT.md','utf8').split('---')[1]; yaml.load(fm)"` (se js-yaml indisponível, basta o regex grep do acceptance)
- Cross-references válidos: `CROSS-MACHINE-SMOKE.md` ↔ `03-HUMAN-UAT.md` ↔ `ONBOARDING.md` ↔ `TROUBLESHOOTING.md`
</verification>

<success_criteria>
1. Procedimento de smoke cross-machine documentado e auto-suficiente para humanos executarem.
2. Fallback single-host aceito com limitação explícita (D-14).
3. Item HUMAN-UAT explicita que TEAM-04 e TEAM-01 dependem de execução humana real.
4. Phase 3 pode fechar como "complete-with-pending-UAT" — artefatos entregues, UATs ficam para a equipe rodar.
5. TEAM-04 endereçado: critério #4 do ROADMAP coberto pelo procedimento documentado (execução real é UAT).
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-de-equipe-onboarding/03-04-SUMMARY.md` documenting: estrutura final de CROSS-MACHINE-SMOKE.md, lista de UATs em 03-HUMAN-UAT.md, justificativa de por que execução real foi roteada para UAT em vez de fingida.
</output>
