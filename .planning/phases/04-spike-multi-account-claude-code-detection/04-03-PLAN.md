---
phase: 04-spike-multi-account-claude-code-detection
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/rpm_transient.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/tpm_transient.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/daily_quota.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/weekly_quota.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/org_tier.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/session_5h.txt
  - .planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md
autonomous: true
requirements:
  - SPIKE-02

must_haves:
  truths:
    - "Função `detectClaudeQuotaExhausted(input: string): { type, confidence, retryAt, source }` está exportada e operacional."
    - "Tests passam para >= 6 fixtures (1 por tipo da taxonomia)."
    - "Protótipo é descartável — vive em .planning/phases/04-*/prototype/, NÃO em packages/ ou scripts/."
    - "Fixtures stub iniciais baseados em mensagens conhecidas — HUMAN-UAT substitui com captura real."
  artifacts:
    - path: ".planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts"
      provides: "Função classifier exportada"
      exports: ["detectClaudeQuotaExhausted", "QuotaDetectionResult"]
    - path: ".planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts"
      provides: "Suite vitest contra fixtures"
      contains: "describe|it|expect"
    - path: ".planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/"
      provides: "6 fixtures stub (1 por tipo)"
      min_lines: 6
  key_links:
    - from: "prototype/detect-quota-exhausted.ts"
      to: "packages/adapters/claude-local/src/server/parse.ts"
      via: "regex equivalente a CLAUDE_TRANSIENT_UPSTREAM_RE adaptada (standalone, disposable)"
      pattern: "rate[-\\\\s]?limit|429|usage\\\\s+limit\\\\s+reached|5[-\\\\s]?hour"
    - from: "prototype/detect-quota-exhausted.test.ts"
      to: "prototype/fixtures/"
      via: "fs.readFileSync para cada fixture"
      pattern: "readFileSync|fixtures/"
---

<objective>
Implementar protótipo descartável `detectClaudeQuotaExhausted` em TypeScript com testes vitest standalone, validado contra 6 fixtures stub (uma por tipo da taxonomia). Protótipo serve como referência arquitetural para Phase 5 MULTI-06; HUMAN-UAT substitui fixtures stub com capturas reais conforme disponíveis.

Purpose: SPIKE-02 exige protótipo testado contra fixtures. Conforme D-09: protótipo vive em `.planning/phases/04-*/prototype/`, NÃO em `packages/` (descartável). Conforme D-10: classifier retorna `{ type, confidence, retryAt, source }`.

Output: protótipo TS + vitest test + 6 fixtures + README explicando uso.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-spike-multi-account-claude-code-detection/04-CONTEXT.md
@.planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md

# Audit target — READ-ONLY (regex de referência)
@packages/adapters/claude-local/src/server/parse.ts

<interfaces>
<!-- Regex de referência (parse.ts:12-15). Protótipo reimplementa standalone. -->

```typescript
// De parse.ts — copy-pastear adaptado para prototype standalone:
const CLAUDE_TRANSIENT_UPSTREAM_RE =
  /(?:rate[-\s]?limit(?:ed)?|rate_limit_error|too\s+many\s+requests|\b429\b|overloaded(?:_error)?|server\s+overloaded|service\s+unavailable|\b503\b|\b529\b|high\s+demand|try\s+again\s+later|temporarily\s+unavailable|throttl(?:ed|ing)|throttlingexception|servicequotaexceededexception|out\s+of\s+extra\s+usage|extra\s+usage\b|claude\s+usage\s+limit\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|usage\s+limit\s+reached|usage\s+cap\s+reached)/i;
const CLAUDE_EXTRA_USAGE_RESET_RE =
  /(?:out\s+of\s+extra\s+usage|extra\s+usage|usage\s+limit\s+reached|usage\s+cap\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|claude\s+usage\s+limit\s+reached)[\s\S]{0,80}?\bresets?\s+(?:at\s+)?([^\n()]+?)(?:\s*\(([^)]+)\))?(?:[.!]|\n|$)/i;
```

Output type para o classifier (D-10):
```typescript
type QuotaType =
  | "rpm_transient"
  | "tpm_transient"
  | "daily_quota"
  | "weekly_quota"
  | "session_5h"
  | "org_tier"
  | "unknown";

type DetectionSource = "regex" | "header" | "stream_event";

interface QuotaDetectionResult {
  type: QuotaType;
  confidence: number; // 0..1
  retryAt: Date | null;
  source: DetectionSource;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar fixtures stub (6 arquivos, 1 por tipo)</name>
  <files>
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/rpm_transient.txt,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/tpm_transient.txt,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/daily_quota.txt,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/weekly_quota.txt,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/org_tier.txt,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/session_5h.txt
  </files>

  <read_first>
    - .planning/phases/04-spike-multi-account-claude-code-detection/04-CONTEXT.md (D-11 — fixtures stub iniciais baseados em docs Anthropic + mensagens conhecidas no regex)
    - .planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md (mensagens canônicas por tipo)
    - packages/adapters/claude-local/src/server/parse.ts (linhas 12-15 — tokens canônicos)
  </read_first>

  <behavior>
    - rpm_transient.txt: contém token `rate_limit_error` ou `too many requests` ou `429` literal. Sem timestamp de reset (transient).
    - tpm_transient.txt: contém token `rate_limit_error` com mensagem evocando tokens-per-minute. Sem timestamp de reset.
    - daily_quota.txt: contém token `usage limit reached` ou `out of extra usage` + `resets at <ISO>` parseable.
    - weekly_quota.txt: contém token `weekly limit reached` + `resets at <ISO>` parseable.
    - org_tier.txt: contém token `overloaded_error` ou `service unavailable` ou `503` (genérico — partial coverage no taxonomy).
    - session_5h.txt: contém token `5-hour limit reached` ou `claude usage limit reached` + `resets at <ISO>` parseable.
  </behavior>

  <action>
Criar 6 fixtures stub em `.planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/`. Cada um contém uma linha de stream-JSON ou stderr blob simulando o erro canônico do tipo.

Conforme D-11: fixtures iniciais são baseados em docs Anthropic + mensagens conhecidas (presentes nos regexes existentes). HUMAN-UAT-04-01 substitui com captura real.

**fixtures/rpm_transient.txt** (1-2 linhas):
```
{"type":"system","subtype":"error","message":"rate_limit_error: Too many requests. Please try again later.","status":429}
{"error":{"type":"rate_limit_error","message":"Number of request tokens has exceeded your per-minute rate limit (requests). Please reduce request rate."}}
```

**fixtures/tpm_transient.txt** (1-2 linhas):
```
{"type":"system","subtype":"error","message":"rate_limit_error: tokens-per-minute exceeded","status":429}
{"error":{"type":"rate_limit_error","message":"This request would exceed your organization's rate limit of 80000 input tokens per minute. Please try again in 17 seconds."}}
```

**fixtures/daily_quota.txt** (1-2 linhas):
```
{"type":"result","is_error":true,"result":"Out of extra usage. Resets at 2026-04-27T00:00:00Z (UTC)."}
Error: Claude usage limit reached. Resets at 2026-04-27 00:00 UTC.
```

**fixtures/weekly_quota.txt** (1-2 linhas):
```
{"type":"result","is_error":true,"result":"Weekly limit reached. Resets at 2026-05-03T00:00:00Z (next Sunday UTC)."}
Error: weekly limit reached. resets at 2026-05-03 00:00 UTC.
```

**fixtures/org_tier.txt** (1-2 linhas):
```
{"type":"system","subtype":"error","message":"overloaded_error: Server overloaded. Please retry.","status":529}
{"error":{"type":"overloaded_error","message":"Service unavailable. The Anthropic API is experiencing high demand."}}
```

**fixtures/session_5h.txt** (1-2 linhas):
```
{"type":"result","is_error":true,"result":"5-hour limit reached. Resets at 2026-04-26T18:30:00Z."}
Error: claude usage limit reached. resets at 2026-04-26 18:30 UTC.
```

Cada arquivo contém 1-2 variantes da mensagem para o tipo (texto simples ou linha de stream-JSON). Encoding UTF-8, line-ending LF.

Adicionar nota inline (comentário não-JSON) na linha 1 de cada fixture seria inválido em parse — em vez disso, ALL fixtures são parseable como puro stream/stderr blob; documentação do propósito vai no `prototype/README.md` (Tarefa 3).
  </action>

  <verify>
    <automated>test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/rpm_transient.txt &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/tpm_transient.txt &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/daily_quota.txt &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/weekly_quota.txt &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/org_tier.txt &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/fixtures/session_5h.txt</automated>
  </verify>

  <acceptance_criteria>
    - 6 arquivos `.txt` existem em `prototype/fixtures/`
    - rpm_transient.txt contém `rate_limit_error` ou `429`
    - daily_quota.txt contém `usage limit reached` ou `out of extra usage` + timestamp ISO
    - weekly_quota.txt contém `weekly limit` + timestamp
    - session_5h.txt contém `5-hour limit` ou `claude usage limit` + timestamp
    - org_tier.txt contém `overloaded` ou `service unavailable` ou `503`/`529`
  </acceptance_criteria>

  <done>
    Fixtures stub prontos para servir como input do classifier. Conforme D-11.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar prototype/detect-quota-exhausted.ts e .test.ts</name>
  <files>
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts,
    .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts
  </files>

  <read_first>
    - .planning/phases/04-spike-multi-account-claude-code-detection/04-CONTEXT.md (D-09, D-10, D-12)
    - .planning/phases/04-spike-multi-account-claude-code-detection/CLAUDE_429_TAXONOMY.md (tabela de tipos)
    - packages/adapters/claude-local/src/server/parse.ts (regex literal de referência, linhas 12-15)
    - prototype/fixtures/*.txt (criados na Tarefa 1 — input dos testes)
    - vitest.config.ts (root) — confirmar que vitest está disponível no monorepo
  </read_first>

  <behavior>
    - `detectClaudeQuotaExhausted("rate_limit_error: Too many requests. 429")` retorna `{ type: "rpm_transient", confidence: >= 0.5, retryAt: null, source: "regex" }`.
    - `detectClaudeQuotaExhausted("tokens-per-minute exceeded")` retorna `{ type: "tpm_transient" or "rpm_transient", source: "regex" }` (partial coverage acceptable conforme taxonomy).
    - `detectClaudeQuotaExhausted("usage limit reached. resets at 2026-04-27T00:00:00Z")` retorna `{ type: "daily_quota", retryAt: Date instance, source: "regex" }`.
    - `detectClaudeQuotaExhausted("weekly limit reached. resets at 2026-05-03 00:00 UTC")` retorna `{ type: "weekly_quota", retryAt: Date, source: "regex" }`.
    - `detectClaudeQuotaExhausted("overloaded_error: Server overloaded")` retorna `{ type: "org_tier" or "rpm_transient", source: "regex" }` (partial cover acceptable).
    - `detectClaudeQuotaExhausted("5-hour limit reached. Resets at 2026-04-26T18:30:00Z")` retorna `{ type: "session_5h", retryAt: Date, source: "regex" }`.
    - `detectClaudeQuotaExhausted("hello world")` retorna `{ type: "unknown", confidence: 0, retryAt: null, source: "regex" }`.
  </behavior>

  <action>
**Arquivo 1:** `.planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts`

```typescript
/**
 * Spike Phase 4 — protótipo descartável de detecção de exhaustão Claude Code.
 *
 * NÃO IMPORTAR DESTA LOCALIZAÇÃO em código de produção.
 * Phase 5 MULTI-06 implementa a versão de produção em packages/adapters/claude-local/src/server/.
 *
 * Conforme D-09 (04-CONTEXT.md): protótipo standalone, sem dependências do monorepo.
 * Reusa as regex de parse.ts:12-15 como ponto de partida; estende apenas onde a taxonomia
 * (CLAUDE_429_TAXONOMY.md) identifica gap específico.
 */

export type QuotaType =
  | "rpm_transient"
  | "tpm_transient"
  | "daily_quota"
  | "weekly_quota"
  | "session_5h"
  | "org_tier"
  | "unknown";

export type DetectionSource = "regex" | "header" | "stream_event";

export interface QuotaDetectionResult {
  type: QuotaType;
  confidence: number; // 0..1
  retryAt: Date | null;
  source: DetectionSource;
}

// Regex top-level (cobertura ampla — espelho de CLAUDE_TRANSIENT_UPSTREAM_RE em parse.ts:13)
const TRANSIENT_UPSTREAM_RE =
  /(?:rate[-\s]?limit(?:ed)?|rate_limit_error|too\s+many\s+requests|\b429\b|overloaded(?:_error)?|server\s+overloaded|service\s+unavailable|\b503\b|\b529\b|high\s+demand|try\s+again\s+later|temporarily\s+unavailable|throttl(?:ed|ing)|throttlingexception|servicequotaexceededexception|out\s+of\s+extra\s+usage|extra\s+usage\b|claude\s+usage\s+limit\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|usage\s+limit\s+reached|usage\s+cap\s+reached)/i;

const RESET_RE =
  /(?:out\s+of\s+extra\s+usage|extra\s+usage|usage\s+limit\s+reached|usage\s+cap\s+reached|5[-\s]?hour\s+limit\s+reached|weekly\s+limit\s+reached|claude\s+usage\s+limit\s+reached)[\s\S]{0,80}?\bresets?\s+(?:at\s+)?([^\n()]+?)(?:\s*\(([^)]+)\))?(?:[.!]|\n|$)/i;

// Sub-tipo discriminators (refinamento sobre o regex top-level — cobertura partial flagada na taxonomy)
const SESSION_5H_RE = /5[-\s]?hour\s+limit\s+reached|claude\s+usage\s+limit\s+reached/i;
const WEEKLY_RE = /weekly\s+limit\s+reached/i;
const DAILY_RE = /(?:out\s+of\s+extra\s+usage|extra\s+usage\b|usage\s+limit\s+reached|usage\s+cap\s+reached)/i;
const TPM_RE = /token(?:s)?[-\s]?(?:per[-\s]?minute|\/minute|\/min)|tokens?\s+per\s+minute|input\s+tokens\s+per\s+minute/i;
const RPM_RE = /rate_limit_error|too\s+many\s+requests|\b429\b|request[-\s]?per[-\s]?minute|requests?\s+per\s+minute/i;
const ORG_TIER_RE = /overloaded(?:_error)?|server\s+overloaded|service\s+unavailable|\b503\b|\b529\b|high\s+demand/i;

function parseRetryAt(input: string): Date | null {
  const match = input.match(RESET_RE);
  if (!match) return null;
  const candidate = (match[1] || "").trim();
  if (!candidate) return null;
  const parsed = new Date(candidate);
  if (Number.isNaN(parsed.getTime())) return null;
  return parsed;
}

export function detectClaudeQuotaExhausted(input: string): QuotaDetectionResult {
  if (!input || !TRANSIENT_UPSTREAM_RE.test(input)) {
    return { type: "unknown", confidence: 0, retryAt: null, source: "regex" };
  }

  const retryAt = parseRetryAt(input);

  // Ordem de discriminação: mais específico → mais genérico
  if (SESSION_5H_RE.test(input)) {
    return { type: "session_5h", confidence: 0.9, retryAt, source: "regex" };
  }
  if (WEEKLY_RE.test(input)) {
    return { type: "weekly_quota", confidence: 0.9, retryAt, source: "regex" };
  }
  if (DAILY_RE.test(input) && retryAt && !SESSION_5H_RE.test(input) && !WEEKLY_RE.test(input)) {
    return { type: "daily_quota", confidence: 0.8, retryAt, source: "regex" };
  }
  if (TPM_RE.test(input)) {
    return { type: "tpm_transient", confidence: 0.7, retryAt, source: "regex" };
  }
  if (ORG_TIER_RE.test(input) && !RPM_RE.test(input)) {
    return { type: "org_tier", confidence: 0.6, retryAt, source: "regex" };
  }
  if (RPM_RE.test(input)) {
    return { type: "rpm_transient", confidence: 0.7, retryAt, source: "regex" };
  }

  // Match no top-level mas nenhum discriminator — flag como unknown low-confidence
  return { type: "unknown", confidence: 0.3, retryAt, source: "regex" };
}
```

**Arquivo 2:** `.planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts`

```typescript
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve, dirname } from "node:path";
import { fileURLToPath } from "node:url";
import { detectClaudeQuotaExhausted } from "./detect-quota-exhausted";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

function loadFixture(name: string): string {
  return readFileSync(resolve(__dirname, "fixtures", name), "utf-8");
}

describe("detectClaudeQuotaExhausted (Spike Phase 4 prototype)", () => {
  it("classifies rpm_transient fixture", () => {
    const input = loadFixture("rpm_transient.txt");
    const result = detectClaudeQuotaExhausted(input);
    // Pode classificar como rpm_transient (preferido) OU tpm_transient OU org_tier
    // (partial coverage flagada na taxonomy). Mínimo: NÃO unknown.
    expect(result.type).not.toBe("unknown");
    expect(["rpm_transient", "tpm_transient", "org_tier"]).toContain(result.type);
    expect(result.confidence).toBeGreaterThanOrEqual(0.5);
    expect(result.source).toBe("regex");
  });

  it("classifies tpm_transient fixture", () => {
    const input = loadFixture("tpm_transient.txt");
    const result = detectClaudeQuotaExhausted(input);
    expect(result.type).not.toBe("unknown");
    expect(["tpm_transient", "rpm_transient"]).toContain(result.type);
    expect(result.source).toBe("regex");
  });

  it("classifies daily_quota fixture with retryAt", () => {
    const input = loadFixture("daily_quota.txt");
    const result = detectClaudeQuotaExhausted(input);
    expect(result.type).toBe("daily_quota");
    expect(result.retryAt).toBeInstanceOf(Date);
    expect(result.confidence).toBeGreaterThanOrEqual(0.5);
  });

  it("classifies weekly_quota fixture with retryAt", () => {
    const input = loadFixture("weekly_quota.txt");
    const result = detectClaudeQuotaExhausted(input);
    expect(result.type).toBe("weekly_quota");
    expect(result.retryAt).toBeInstanceOf(Date);
  });

  it("classifies org_tier fixture (partial coverage acceptable)", () => {
    const input = loadFixture("org_tier.txt");
    const result = detectClaudeQuotaExhausted(input);
    expect(result.type).not.toBe("unknown");
    // Partial coverage: pode classificar como org_tier OU rpm_transient
    expect(["org_tier", "rpm_transient"]).toContain(result.type);
  });

  it("classifies session_5h fixture with retryAt", () => {
    const input = loadFixture("session_5h.txt");
    const result = detectClaudeQuotaExhausted(input);
    expect(result.type).toBe("session_5h");
    expect(result.retryAt).toBeInstanceOf(Date);
    expect(result.confidence).toBeGreaterThanOrEqual(0.7);
  });

  it("returns unknown for non-quota input", () => {
    const result = detectClaudeQuotaExhausted("hello world, all good");
    expect(result.type).toBe("unknown");
    expect(result.confidence).toBe(0);
  });

  it("returns unknown for empty input", () => {
    const result = detectClaudeQuotaExhausted("");
    expect(result.type).toBe("unknown");
  });
});
```

**Como rodar (documentar no README na Tarefa 3):**
```
npx vitest run .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts
```

Conforme D-12: roda standalone via `npx vitest`, não precisa estar integrado ao test runner principal do monorepo.
  </action>

  <verify>
    <automated>test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.ts &amp;&amp; test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts &amp;&amp; npx vitest run .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts --no-coverage</automated>
  </verify>

  <acceptance_criteria>
    - `detect-quota-exhausted.ts` exporta `detectClaudeQuotaExhausted` e tipo `QuotaDetectionResult`
    - `detect-quota-exhausted.test.ts` tem >= 8 casos (6 fixtures + 2 negative cases)
    - Vitest run passa todos os tests
    - Função retorna shape `{ type, confidence, retryAt, source }` conforme D-10
  </acceptance_criteria>

  <done>
    Protótipo classifier funcional, testado contra 6 fixtures stub. Conforme D-09, D-10, D-12.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: Criar prototype/README.md documentando uso</name>
  <files>.planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md</files>

  <read_first>
    - .planning/phases/04-spike-multi-account-claude-code-detection/04-CONTEXT.md (D-09 — natureza descartável)
    - prototype/detect-quota-exhausted.ts (criado em Tarefa 2)
    - prototype/detect-quota-exhausted.test.ts (criado em Tarefa 2)
  </read_first>

  <action>
Criar `.planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md` em pt-br com:

```markdown
# Protótipo: detectClaudeQuotaExhausted

**Spike Phase 4 — código descartável.** NÃO IMPORTAR em produção.

## Propósito

Validar empiricamente uma estrutura de classifier para exhaustão de Claude Code CLI antes de implementar a versão de produção em Phase 5 (`packages/adapters/claude-local/src/server/parse.ts` MULTI-06).

Conforme D-09 (04-CONTEXT.md): protótipo standalone, vive em `.planning/phases/04-*/prototype/`, NÃO em `packages/` ou `scripts/`.

## API

```typescript
import { detectClaudeQuotaExhausted } from "./detect-quota-exhausted";

const result = detectClaudeQuotaExhausted(streamJsonOrStderr);
// { type: "session_5h", confidence: 0.9, retryAt: Date, source: "regex" }
```

**Tipos suportados:** `rpm_transient`, `tpm_transient`, `daily_quota`, `weekly_quota`, `session_5h`, `org_tier`, `unknown`.

## Como rodar os tests

```bash
npx vitest run .planning/phases/04-spike-multi-account-claude-code-detection/prototype/detect-quota-exhausted.test.ts --no-coverage
```

## Estrutura

- `detect-quota-exhausted.ts` — função classifier
- `detect-quota-exhausted.test.ts` — vitest suite (8 cases)
- `fixtures/` — 6 fixtures stub (1 por tipo da taxonomia)

## Fixtures

Conforme D-11: fixtures iniciais são **stubs** baseados em mensagens conhecidas (regex existente em `parse.ts:12-15` + docs Anthropic). Captura real é feita em **HUMAN-UAT-04-01** (ver `04-HUMAN-UAT.md`) e substitui os stubs incrementalmente.

| Fixture | Tipo | Status |
|---------|------|--------|
| `fixtures/rpm_transient.txt` | rpm_transient | stub |
| `fixtures/tpm_transient.txt` | tpm_transient | stub |
| `fixtures/daily_quota.txt` | daily_quota | stub |
| `fixtures/weekly_quota.txt` | weekly_quota | stub |
| `fixtures/org_tier.txt` | org_tier | stub |
| `fixtures/session_5h.txt` | session_5h | stub |

## Relação com produção (Phase 5)

Phase 5 MULTI-06 implementa a versão de produção. O protótipo aqui:
1. Valida que a forma de output `{ type, confidence, retryAt, source }` é viável
2. Demonstra reuso das regex existentes (`CLAUDE_TRANSIENT_UPSTREAM_RE`, `CLAUDE_EXTRA_USAGE_RESET_RE`) em vez de reescrever do zero
3. Identifica edge cases (org_tier partial coverage, RPM/TPM ambiguity) para a implementação real

Quando Phase 5 estiver completa, este diretório pode ser arquivado ou deletado.

## Limitações conhecidas

- **Partial coverage** para `org_tier` e `tpm_transient`: regex top-level dispara, mas discriminator é heurístico. Tests aceitam variações.
- **Headers HTTP não cobertos**: protótipo só processa string (stream-JSON ou stderr blob). Detecção pré-emptiva via `anthropic-ratelimit-*` headers é decisão de Phase 5 (ver `DECISION-DETECTION-STRATEGY.md`).
- **Fixtures são stubs**: HUMAN-UAT-04-01 substitui com captura real quando disponível.

## Referências

- `../CLAUDE_429_TAXONOMY.md` — taxonomia 6-tipos com cobertura por regex existente
- `../DECISION-DETECTION-STRATEGY.md` — decisão arquitetural reativo vs pré-emptivo
- `packages/adapters/claude-local/src/server/parse.ts:12-15` — regex de produção (audit target, READ-ONLY no spike)
```
  </action>

  <verify>
    <automated>test -f .planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md &amp;&amp; grep -q "detectClaudeQuotaExhausted" .planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md &amp;&amp; grep -q "npx vitest" .planning/phases/04-spike-multi-account-claude-code-detection/prototype/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `prototype/README.md` existe e documenta API, como rodar tests, e natureza descartável
    - Cita `detectClaudeQuotaExhausted` por nome
    - Inclui comando `npx vitest run` literal
    - Documento em pt-br
  </acceptance_criteria>

  <done>
    README do protótipo escrito, integrando referências para `CLAUDE_429_TAXONOMY.md`, `DECISION-DETECTION-STRATEGY.md` e parse.ts. Conforme D-09.
  </done>
</task>

</tasks>

<verification>
- 6 fixtures stub criados e identificados
- Protótipo TS exporta `detectClaudeQuotaExhausted` com shape correto
- Vitest passa todos os 8 cases
- README documenta uso e natureza descartável
- Tudo vive em `.planning/phases/04-*/prototype/` (não em `packages/`)
</verification>

<success_criteria>
SPIKE-02 satisfeito: protótipo classifier testado contra 6 fixtures, tipos conformes à taxonomia, código descartável.
</success_criteria>

<output>
After completion, create `.planning/phases/04-spike-multi-account-claude-code-detection/04-03-SUMMARY.md`.
</output>
