---
phase: 02-migra-o-de-storage-para-supabase
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - server/src/auth/better-auth.ts
  - server/src/__tests__/better-auth-supabase-readiness.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Better Auth instance é criada via `createBetterAuthInstance(db, config)` apontando para o `db` Drizzle (que agora resolve para Supabase Postgres via Plano 02-03)"
    - "Cookie prefix é `paperclip-team-shared` (derivado de `PAPERCLIP_INSTANCE_ID=team-shared` via `deriveAuthCookiePrefix`) — todos os devs no time compartilham mesma sessão lógica (AUTH-02)"
    - "BETTER_AUTH_SECRET de `.env.local` é diferente do placeholder `paperclip-dev-secret` (gerado via crypto.randomBytes(32) base64)"
    - "Teste unitário valida que `deriveAuthCookiePrefix('team-shared')` retorna `'paperclip-team-shared'`"
    - "Teste de integração valida que createBetterAuthInstance não lança erro quando recebe um db conectado ao Supabase com schema aplicado (Plano 02-04 deixa schema pronto)"
    - "Modo `authenticated` permanece configurável (AUTH-03 — config.deploymentMode='authenticated' via env var existente do paperclip)"
    - "Email/password signup permanece habilitado (AUTH-04 — `emailAndPassword.enabled: true`, já é o default no código atual)"
  artifacts:
    - path: "server/src/auth/better-auth.ts"
      provides: "Better Auth wired against Supabase Postgres + cookie prefix from PAPERCLIP_INSTANCE_ID"
      contains: "deriveAuthCookiePrefix"
    - path: "server/src/__tests__/better-auth-supabase-readiness.test.ts"
      provides: "Unit + integration tests for cookie prefix + Better Auth instance creation"
      min_lines: 60
  key_links:
    - from: "server/src/auth/better-auth.ts"
      to: "PAPERCLIP_INSTANCE_ID env var"
      via: "deriveAuthCookiePrefix(resolvePaperclipInstanceId())"
      pattern: "deriveAuthCookiePrefix.*resolvePaperclipInstanceId|resolvePaperclipInstanceId.*deriveAuthCookiePrefix"
    - from: "server/src/auth/better-auth.ts"
      to: "drizzleAdapter(db, ...)"
      via: "Better Auth adapter recebe db Drizzle (que agora aponta para Supabase via runtime-config patched)"
      pattern: "drizzleAdapter\\(db"
    - from: "BETTER_AUTH_SECRET"
      to: "Better Auth instance"
      via: "secret = process.env.BETTER_AUTH_SECRET"
      pattern: "BETTER_AUTH_SECRET"
---

<objective>
Validar que o módulo Better Auth do paperclip (`server/src/auth/better-auth.ts`) funciona contra o Postgres do Supabase com cookie prefix derivado de `PAPERCLIP_INSTANCE_ID=team-shared`. NÃO há reescrita — o módulo herdado JÁ está bem desenhado para isso. Esta fase é VERIFICAÇÃO + TESTES, não mudança estrutural.

Purpose: AUTH-01..04 dizem "Better Auth funciona contra Postgres do Supabase". Sem teste explícito que valida o wiring, descobrimos no smoke test E2E (02-06) qualquer regressão silenciosa. Pegar agora, testar ali, é exponencialmente mais barato.

A análise do código (linha 32 de `better-auth.ts`: `deriveAuthCookiePrefix(instanceId = resolvePaperclipInstanceId())`) mostra que o cookie prefix JÁ é derivado de `PAPERCLIP_INSTANCE_ID`. Idem para `drizzleAdapter(db, { provider: "pg", ... })` — que agora recebe um db apontando para Supabase. NADA precisa mudar estruturalmente.

Output: Suite de testes nova validando os comportamentos AUTH-02 (cookie prefix) e AUTH-01 (Better Auth se inicializa contra db Supabase). Apenas commenting / minor doc tweaks no `better-auth.ts` se algo estiver ambíguo.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-03-SUMMARY.md
@.planning/research/STACK.md

@server/src/auth/better-auth.ts
@server/src/home-paths.ts
@packages/db/src/schema/index.ts

<interfaces>
<!-- The current Better Auth wiring (verified by reading better-auth.ts) -->

```ts
// server/src/auth/better-auth.ts (lines 32-38, current)
export function deriveAuthCookiePrefix(instanceId = resolvePaperclipInstanceId()): string {
  const scopedInstanceId = instanceId
    .trim()
    .replace(AUTH_COOKIE_PREFIX_INVALID_SEGMENTS_RE, "-")
    .replace(/^-+|-+$/g, "") || AUTH_COOKIE_PREFIX_FALLBACK;
  return `paperclip-${scopedInstanceId}`;
}
```

`resolvePaperclipInstanceId()` (server/src/home-paths.ts) reads `process.env.PAPERCLIP_INSTANCE_ID` with fallback "default".
With `PAPERCLIP_INSTANCE_ID=team-shared` (set in `.env.local` by Plano 02-03), `deriveAuthCookiePrefix()` returns `"paperclip-team-shared"`. ✓ AUTH-02 satisfied.

`createBetterAuthInstance` (lines 87-127):
```ts
export function createBetterAuthInstance(db: Db, config: Config, trustedOrigins?: string[]): BetterAuthInstance {
  // ...
  const secret = process.env.BETTER_AUTH_SECRET ?? process.env.PAPERCLIP_AGENT_JWT_SECRET;
  if (!secret) throw new Error("BETTER_AUTH_SECRET (...) must be set.");
  // ...
  const authConfig = {
    baseURL: baseUrl,
    secret,
    trustedOrigins: effectiveTrustedOrigins,
    database: drizzleAdapter(db, {
      provider: "pg",
      schema: { user: authUsers, session: authSessions, account: authAccounts, verification: authVerifications },
    }),
    emailAndPassword: { enabled: true, requireEmailVerification: false, disableSignUp: config.authDisableSignUp },
    advanced: buildBetterAuthAdvancedOptions({ disableSecureCookies: isHttpOnly }),
  };
  return betterAuth(authConfig);
}
```
✓ AUTH-01 architecturally ready: db → Drizzle → Supabase Postgres (via 02-03 patches)
✓ AUTH-04 ready: emailAndPassword.enabled: true (default)
✓ AUTH-03: config.deploymentMode='authenticated' is set elsewhere (server/src/config.ts) — NOT this plan's concern; we just confirm Better Auth doesn't preempt it.

The auth.ts schema (packages/db/src/schema/auth.ts) defines Better Auth tables with text IDs. These tables were applied to Supabase by Plano 02-04 (since auth.ts is part of paperclip's 71 migrations).

DECISION: This plan does NOT modify better-auth.ts substantively. It adds:
1. A small comment block at the top of better-auth.ts annotating the AUTH-01..04 mapping (for future readers)
2. A new test suite that validates the wiring works end-to-end
</interfaces>

<dependencies>
- Plano 02-03 deve ter aplicado patches em `packages/db/src/client.ts` (createDb com Supavisor-aware options) e em `runtime-config.ts` (privilegia DATABASE_URL).
- Plano 02-04 deve ter aplicado as 71 migrations no Supabase, incluindo as tabelas Better Auth (`user`, `session`, `account`, `verification`).
- Para os testes de integração contra Supabase real, `.env.local` deve estar populado (já está).
</dependencies>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Suite de testes validando wiring Better Auth → Supabase</name>
  <files>
    server/src/__tests__/better-auth-supabase-readiness.test.ts
  </files>
  <behavior>
Tests cobrindo (em ordem do simples ao integração):

- Test 1 (unit, isolated): `deriveAuthCookiePrefix('team-shared')` retorna exatamente `'paperclip-team-shared'`
- Test 2 (unit): `deriveAuthCookiePrefix('default')` retorna `'paperclip-default'` (fallback semântico)
- Test 3 (unit): `deriveAuthCookiePrefix('with spaces & weird@chars!!')` sanitiza para `'paperclip-with-spaces-weird-chars'` (caracteres inválidos viram `-`, leading/trailing `-` removidos)
- Test 4 (unit): `deriveAuthCookiePrefix('')` retorna `'paperclip-default'` (string vazia → fallback)
- Test 5 (unit, env-driven): com `PAPERCLIP_INSTANCE_ID=team-shared` no env, `deriveAuthCookiePrefix()` (sem args, default usa env) retorna `'paperclip-team-shared'`
- Test 6 (integration, gated): com `.env.local` carregado E Supabase reachable, `createBetterAuthInstance(realDb, mockConfig)` NÃO lança e retorna objeto com `.api`. Caso `SUPABASE_DB_URL` ausente, este teste é SKIPPED com mensagem clara (não falha).

Os testes 1-5 são totalmente unit e rodam em qualquer máquina sem rede. Test 6 só roda com env populado e cobre o "wiring real" — é a validação que difere de "ele compila" para "ele realmente conecta".
  </behavior>
  <action>
Crie `server/src/__tests__/better-auth-supabase-readiness.test.ts`:

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
import { deriveAuthCookiePrefix } from "../auth/better-auth.js";

describe("deriveAuthCookiePrefix (AUTH-02)", () => {
  const originalEnv = process.env.PAPERCLIP_INSTANCE_ID;

  beforeEach(() => {
    delete process.env.PAPERCLIP_INSTANCE_ID;
  });

  afterEach(() => {
    if (originalEnv === undefined) delete process.env.PAPERCLIP_INSTANCE_ID;
    else process.env.PAPERCLIP_INSTANCE_ID = originalEnv;
  });

  it("Test 1: 'team-shared' → 'paperclip-team-shared'", () => {
    expect(deriveAuthCookiePrefix("team-shared")).toBe("paperclip-team-shared");
  });

  it("Test 2: 'default' → 'paperclip-default'", () => {
    expect(deriveAuthCookiePrefix("default")).toBe("paperclip-default");
  });

  it("Test 3: invalid chars are sanitized", () => {
    expect(deriveAuthCookiePrefix("with spaces & weird@chars!!")).toBe(
      "paperclip-with-spaces-weird-chars",
    );
  });

  it("Test 4: empty string falls back to default", () => {
    expect(deriveAuthCookiePrefix("")).toBe("paperclip-default");
  });

  it("Test 5: env var PAPERCLIP_INSTANCE_ID drives default arg", () => {
    process.env.PAPERCLIP_INSTANCE_ID = "team-shared";
    // Re-import to pick up env? deriveAuthCookiePrefix uses resolvePaperclipInstanceId()
    // at CALL time (not import time), so env mutation here is honored.
    expect(deriveAuthCookiePrefix()).toBe("paperclip-team-shared");
  });
});

// Integration test: only runs when .env.local provides SUPABASE_DB_URL
describe("createBetterAuthInstance against real Supabase (AUTH-01) [integration]", () => {
  const supabaseUrl = process.env.SUPABASE_DB_URL ?? process.env.DATABASE_URL;
  const skipReason = !supabaseUrl
    ? "SUPABASE_DB_URL not set — skipping integration test (run with .env.local loaded to enable)"
    : null;

  it.skipIf(skipReason !== null)("Test 6: createBetterAuthInstance succeeds with Supabase-backed db", async () => {
    if (skipReason) return;

    // Dynamic imports because we don't want module-level effects in unit tests
    const { createDb } = await import("@paperclipai/db");
    const { createBetterAuthInstance } = await import("../auth/better-auth.js");

    const db = createDb(supabaseUrl!);

    // Minimal Config shape needed by createBetterAuthInstance.
    // Real Config comes from server/src/config.ts via loadConfig() — but we don't
    // need full config here, just the fields the function reads.
    const fakeConfig = {
      authBaseUrlMode: "implicit" as const,
      authPublicBaseUrl: "",
      authDisableSignUp: false,
      deploymentMode: "authenticated" as const,
      allowedHostnames: [] as string[],
    };

    // Skip if BETTER_AUTH_SECRET not set (env precondition)
    if (!process.env.BETTER_AUTH_SECRET && !process.env.PAPERCLIP_AGENT_JWT_SECRET) {
      console.warn("BETTER_AUTH_SECRET not set; skipping integration test");
      return;
    }

    let instance: ReturnType<typeof createBetterAuthInstance> | null = null;
    let thrown: unknown = null;
    try {
      instance = createBetterAuthInstance(db, fakeConfig as never);
    } catch (e) {
      thrown = e;
    }

    expect(thrown).toBeNull();
    expect(instance).not.toBeNull();
    // Better Auth instance has `.api` namespace with handlers
    expect((instance as { api?: unknown }).api).toBeDefined();
  });
});

describe("Auth schema tables exported from @paperclipai/db (AUTH-01 dependency)", () => {
  it("Test 7: authUsers, authSessions, authAccounts, authVerifications are exported", async () => {
    const dbExports = await import("@paperclipai/db");
    expect(dbExports.authUsers).toBeDefined();
    expect(dbExports.authSessions).toBeDefined();
    expect(dbExports.authAccounts).toBeDefined();
    expect(dbExports.authVerifications).toBeDefined();
  });
});
```

Rode:
```bash
cd D:/projetos/ddd && \
  set -a && source .env.local 2>/dev/null && set +a && \
  pnpm --filter @paperclipai/server exec vitest run src/__tests__/better-auth-supabase-readiness.test.ts --reporter=verbose
```

Espera-se: 6 ou 7 tests passing (Test 6 pode ser skipped se SUPABASE_DB_URL não foi exportado para o ambiente do vitest; isso é OK).

NÃO modifique `better-auth.ts` para tornar os testes passarem. Se algum teste 1-5 falhar, é bug na implementação herdada — abrir um issue, não patchar aqui (escopo).
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && pnpm --filter @paperclipai/server exec vitest run src/__tests__/better-auth-supabase-readiness.test.ts --reporter=verbose 2>&1 | grep -E "(✓|✗|FAIL|PASS|skipped)" | tail -15</automated>
  </verify>
  <done>
- `better-auth-supabase-readiness.test.ts` existe
- 5 testes unit (cookie prefix) passam
- 1 teste de schema exports passa
- 1 teste de integração passa OU é gracefully skipped (com motivo)
- AUTH-02 validado empiricamente
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Anotar `better-auth.ts` com mapeamento de requirements</name>
  <files>
    server/src/auth/better-auth.ts
  </files>
  <action>
Adicione um comment block NO INÍCIO do arquivo (após os imports, antes do primeiro `export type`), documentando o mapeamento de AUTH-01..04 ao código:

```ts
/**
 * Better Auth wiring — Phase 2 (AUTH-01..04).
 *
 * Architecture decisions (per .planning/research/ARCHITECTURE.md):
 *   - Better Auth is preserved in v1 (NOT migrated to Supabase Auth — text-id schema
 *     incompatible with auth.users uuid; migration is HIGH effort with no v1 gain).
 *   - Better Auth runs against the SAME Postgres as the rest of the app (Supabase via
 *     drizzleAdapter). The `db` argument to `createBetterAuthInstance` is created
 *     by `@paperclipai/db`'s `createDb(SUPABASE_URL)` which auto-applies pool config
 *     (`prepare: false` for port 6543, `max: 5`, `idle_timeout: 20`) — see Plan 02-03.
 *
 * Requirement mapping:
 *   AUTH-01: Better Auth functional against Supabase Postgres
 *     → drizzleAdapter(db, { provider: "pg", schema: { user/session/account/verification } })
 *       at line ~105. The `db` injection makes this Supabase-backed transparently.
 *   AUTH-02: Cookie prefix from PAPERCLIP_INSTANCE_ID for shared session across team
 *     → deriveAuthCookiePrefix(instanceId) returns `paperclip-${sanitized(instanceId)}`.
 *       With PAPERCLIP_INSTANCE_ID=team-shared in .env, prefix is `paperclip-team-shared`.
 *   AUTH-03: Mode `authenticated` enforces login on all routes except signup/login
 *     → config.deploymentMode='authenticated' is honored by middleware/auth.ts (NOT this file).
 *   AUTH-04: Email/password signup available
 *     → emailAndPassword: { enabled: true, requireEmailVerification: false } at line ~115.
 *       Disabled by config.authDisableSignUp; off by default.
 *
 * Anti-pattern guards:
 *   - BETTER_AUTH_SECRET MUST be set (line ~91 throws otherwise).
 *   - In dev (HTTP), useSecureCookies is auto-disabled to prevent cookie write failures.
 *   - PAPERCLIP_AGENT_JWT_SECRET is an alias fallback for BETTER_AUTH_SECRET; both refer
 *     to the same signing key.
 *
 * Tests covering this wiring: server/src/__tests__/better-auth-supabase-readiness.test.ts
 */
```

Adicione APENAS este block. NÃO modifique nenhuma outra linha. NÃO renomeie nenhuma função. NÃO remova nenhum export.

Após adicionar, valide typecheck:
```bash
cd D:/projetos/ddd && pnpm --filter @paperclipai/server exec tsc --noEmit 2>&1 | tail -10
```

Sem novos erros.
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const c=fs.readFileSync('server/src/auth/better-auth.ts','utf8');const required=['AUTH-01','AUTH-02','AUTH-03','AUTH-04','PAPERCLIP_INSTANCE_ID','better-auth-supabase-readiness.test.ts','prepare: false'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('missing:',missing);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <done>
- Comment block no topo do `better-auth.ts` mapeia AUTH-01..04 ao código
- Cita o test file que cobre o wiring
- Cita Plano 02-03 (createDb pool config)
- Typecheck do server passa
  </done>
</task>

</tasks>

<verification>
- AUTH-01 ✓ — Better Auth instance se inicializa com db apontando para Supabase (test 6 quando env presente; arquitetura confirmada via leitura de código)
- AUTH-02 ✓ — Cookie prefix `paperclip-team-shared` derivado de `PAPERCLIP_INSTANCE_ID` (testes 1, 5)
- AUTH-03 ✓ — Modo `authenticated` é orthogonal ao Better Auth wiring; config.deploymentMode é setado em outro lugar (config.ts)
- AUTH-04 ✓ — emailAndPassword.enabled: true (default no código herdado, validado por leitura)
</verification>

<success_criteria>
- [ ] `better-auth-supabase-readiness.test.ts` existe e ≥6 testes passam
- [ ] Comment block em `better-auth.ts` mapeia AUTH-01..04 a linhas específicas
- [ ] Typecheck do server passa
- [ ] Cookie prefix `paperclip-team-shared` confirmado empiricamente
- [ ] Schema Better Auth (user/session/account/verification) confirmado disponível em `@paperclipai/db` exports
</success_criteria>

<output>
After completion, create `.planning/phases/02-migra-o-de-storage-para-supabase/02-05-SUMMARY.md` describing:
- Decisão arquitetural: Better Auth preservado, ZERO mudanças estruturais (apenas comment + tests)
- Cookie prefix validado: `paperclip-team-shared`
- Schema Better Auth presente em Supabase (pré-condição: 02-04 aplicou as migrations)
- Tests adicionados (lista os 6-7)
- Próximo: Plano 02-06 (smoke test E2E) pode iniciar — todas as fundações de auth + db estão em pé
</output>
