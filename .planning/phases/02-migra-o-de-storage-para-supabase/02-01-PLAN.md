---
phase: 02-migra-o-de-storage-para-supabase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "MIGRATION_AUDIT.md exists in the phase directory and lists every code-level coupling that may break against Supavisor (Supabase pooler) transaction mode"
    - "Each finding has a concrete file path + line range + decision (mitigate / port / accept-as-is / N/A in our usage)"
    - "Auditoria conclui se as 71 migrations Drizzle existentes podem ser aplicadas as-is contra Supabase ou se há red-flags (ex.: extensions pré-requisito, schemas conflitantes com `auth` interno do Supabase)"
  artifacts:
    - path: ".planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md"
      provides: "Mapa de acoplamentos Postgres-embedded → Supavisor com decisão por achado"
      contains: "## Findings"
  key_links:
    - from: "MIGRATION_AUDIT.md"
      to: "packages/db/src/client.ts"
      via: "Achado documentando uso de prepared statements (postgres-js default) → mitigação `prepare: false` para porta 6543"
      pattern: "prepare.*false|6543"
    - from: "MIGRATION_AUDIT.md"
      to: "server/src/services/plugin-database.ts:414"
      via: "Achado documentando `pg_advisory_xact_lock` (transactional, OK em Supavisor — não é session-level)"
      pattern: "pg_advisory_xact_lock"
    - from: "MIGRATION_AUDIT.md"
      to: "packages/db/src/migrations/"
      via: "Decisão sobre estratégia das 71 migrations: try-apply-or-regenerate-baseline"
      pattern: "71 migrations|baseline"
---

<objective>
Produzir `MIGRATION_AUDIT.md` mapeando todos os pontos de acoplamento entre o código herdado do paperclip e o Postgres embedded, classificando cada achado por risco de quebra contra Supavisor (Supabase connection pooler) em transaction mode (porta 6543) e session mode (porta 5432).

Purpose: Sem este mapa, o swap de conexão da Fase 2 vira "trocar a string e ver o que quebra em produção". Auditoria-primeiro é a única forma defensável de migrar 71 migrations + um app stateful sem regressões silenciosas. PITFALLS Armadilha 2 nomeia exatamente esse modo de falha.

Output: Artefato Markdown único em `.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md` com seções obrigatórias `## Scope`, `## Methodology`, `## Findings` (uma subseção por categoria), `## Migration Strategy Decision`, `## Mitigation Plan`. É o INFRA-01 explícito.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

# Arquivos-fonte cuja superfície precisa ser auditada
@packages/db/src/client.ts
@packages/db/src/runtime-config.ts
@packages/db/drizzle.config.ts
@server/src/index.ts
@server/src/services/plugin-database.ts
@AGENTS.md

<interfaces>
<!-- Pontos de acoplamento já conhecidos por inspeção rápida — devem aparecer no MIGRATION_AUDIT.md como achados pré-identificados. O executor DEVE confirmar com grep e expandir. -->

Achado pré-identificado #1: postgres-js sem `prepare: false`
  File: packages/db/src/client.ts:48-51
  Code:
    export function createDb(url: string) {
      const sql = postgres(url);  // ← sem opções; prepared statements ON por default
      return drizzlePg(sql, { schema });
    }
  Risk: HIGH em transaction mode (porta 6543). Supavisor não suporta prepared statements em transaction mode.
  Decision: Mitigar (passar `prepare: false` quando porta 6543). Será implementado no plano 02-03.

Achado pré-identificado #2: `pg_advisory_xact_lock` em plugin-database.ts
  File: server/src/services/plugin-database.ts:414
  Code: await tx.execute(sql`SELECT pg_advisory_xact_lock(${lockKey})`);
  Risk: LOW. É TRANSACTIONAL (xact), não session-level. Supavisor suporta session locks dentro de uma transação. Não requer mitigação.
  Decision: Aceitar como está. Documentar no audit como "verified compatible".

Achado pré-identificado #3: Auto-migrations no startup
  File: server/src/index.ts:130-183 (função `ensureMigrations`)
  Risk: HIGH em ambiente compartilhado. PITFALLS Armadilha 3.
  Decision: Mitigar (desabilitar por default; opt-in via `PAPERCLIP_MIGRATION_AUTO_APPLY=true` já existe no código mas é prompt-driven; precisa endurecer para falhar fast quando schema desatualizado). Será implementado no plano 02-03.

Achado pré-identificado #4: 71 migrations Drizzle existentes
  Files: packages/db/src/migrations/0000_*.sql ... 0070_*.sql
  Risk: UNKNOWN. Pode haver dependências em extensions que Supabase não tem, ou conflito com schema `auth` (interno do Supabase, não Better Auth). Decisão Travada (CONTEXT.md): tentar aplicar as-is no plano 02-04; só regenerar baseline se falhar.
  Decision: Documentar o plano contingente; deixar a tentativa para 02-04.

Achado pré-identificado #5: embedded-postgres patch
  File: package.json:58-60 (`patchedDependencies` para `embedded-postgres@18.1.0-beta.16`)
  Risk: NONE para Supabase (irrelevante quando não usamos embedded). Mas embedded permanece como fallback opt-in (INFRA-06).
  Decision: Aceitar. Documentar a coexistência.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Levantar achados via grep sistemático contra a base de código</name>
  <files>.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT_FINDINGS.json</files>
  <action>
Este passo NÃO produz o documento final — produz uma lista estruturada de achados crus que a Tarefa 2 vai redigir.

Execute os greps abaixo a partir da raiz do repo `D:\projetos\ddd` e capture cada hit em `MIGRATION_AUDIT_FINDINGS.json` com schema `{ category, pattern, file, line, snippet (≤3 lines), risk_hypothesis }`. Use a ferramenta `Grep` (NÃO `bash grep`).

Categorias e patterns OBRIGATÓRIOS (rode todos, mesmo que zero hits — registrar zero é parte do achado):

A) Coupling com prepared statements explícitos:
   - pattern: `\bPREPARE\b` (case-insensitive)
   - pattern: `prepared statement`
   - pattern: `prepare:\s*true`
   - File globs: `**/*.ts`, `**/*.sql`

B) LISTEN/NOTIFY (incompatível com Supavisor transaction mode):
   - pattern: `\bLISTEN\s+` (case-insensitive, espaço após LISTEN para evitar PAPERCLIP_OPEN_ON_LISTEN)
   - pattern: `\bNOTIFY\s+`
   - pattern: `pg_notify\s*\(`
   - File globs: `**/*.ts`, `**/*.sql`

C) Advisory locks (session vs transactional):
   - pattern: `pg_advisory_lock\b` (session-level — risco em transaction mode)
   - pattern: `pg_advisory_unlock\b`
   - pattern: `pg_advisory_xact_lock\b` (transactional — OK)
   - pattern: `pg_try_advisory_lock\b`
   - File globs: `**/*.ts`, `**/*.sql`

D) Tabelas temporárias e SET LOCAL:
   - pattern: `CREATE\s+TEMP` (case-insensitive)
   - pattern: `CREATE\s+TEMPORARY` (case-insensitive)
   - pattern: `\bSET\s+LOCAL\b` (case-insensitive)
   - File globs: `**/*.ts`, `**/*.sql`

E) Long-lived transactions / idle_in_transaction:
   - pattern: `idle_in_transaction`
   - pattern: `\.transaction\s*\(\s*async`  (encontrar transactions Drizzle longas; analisar caso a caso)
   - File globs: `**/*.ts`

F) Auto-migrations no startup:
   - pattern: `applyPendingMigrations`
   - pattern: `ensureMigrations`
   - pattern: `migratePostgresIfEmpty`
   - File globs: `server/**/*.ts`, `cli/**/*.ts`

G) Connection management e pool config:
   - pattern: `postgres\s*\(`  (procurar todos os call sites de postgres-js)
   - pattern: `max:\s*\d+`
   - pattern: `idle_timeout`
   - File globs: `**/*.ts`

H) Embedded postgres references (para confirmar que o fallback opt-in está bem isolado):
   - pattern: `embedded-postgres`
   - pattern: `EmbeddedPostgres`
   - pattern: `embedded_postgres` (snake_case)
   - File globs: `**/*.ts`, `**/*.json`

I) DATABASE_URL / SUPABASE_DB_URL references existentes:
   - pattern: `DATABASE_URL`
   - pattern: `SUPABASE_DB_URL`
   - pattern: `process\.env\.\w*DATABASE`
   - File globs: `**/*.ts`, `**/*.json`, `**/*.md`

J) Migrations existentes — pré-requisitos suspeitos:
   - pattern: `CREATE\s+EXTENSION` (case-insensitive)
   - pattern: `CREATE\s+SCHEMA` (case-insensitive — conflito com schema `auth` do Supabase?)
   - pattern: `pg_trgm|uuid-ossp|pgcrypto|vector`
   - File globs: `packages/db/src/migrations/*.sql`

Para cada hit:
1. Anote `category` (A-J), `pattern`, `file` (path absoluto), `line` (n da linha), `snippet` (≤3 linhas em volta), `risk_hypothesis` (string curta — uma linha — sua hipótese inicial; será confirmada na Tarefa 2).

Salve o JSON intermediário como `MIGRATION_AUDIT_FINDINGS.json` no `.planning/phases/02-migra-o-de-storage-para-supabase/`. NÃO commite este JSON; ele é descartável após Tarefa 2 produzir o MD final.

Schema do JSON:
```json
{
  "audit_run_at": "ISO-8601 timestamp",
  "categories_searched": ["A", "B", "C", "D", "E", "F", "G", "H", "I", "J"],
  "findings": [
    {
      "category": "F",
      "pattern": "applyPendingMigrations",
      "file": "D:\\projetos\\ddd\\server\\src\\index.ts",
      "line": 168,
      "snippet": "      logger.info({ pendingMigrations: state.pendingMigrations }, ...);\n      await applyPendingMigrations(connectionString);\n      return \"applied (pending migrations)\";",
      "risk_hypothesis": "Auto-apply chamado de dentro do startup do server quando PAPERCLIP_MIGRATION_AUTO_APPLY=true ou TTY answer=y. Risco MEDIUM: 5 devs simultaneamente podem apertar 'y'; pode aplicar migrations duplicado/conflitando."
    }
  ],
  "zero_hit_categories": ["B"]
}
```

Se uma categoria retornar zero hits, registre em `zero_hit_categories` — isso É um achado (significa "não encontrado, sem mitigação necessária").
  </action>
  <verify>
    <automated>node -e "const j=require('D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT_FINDINGS.json'); if(!Array.isArray(j.findings))process.exit(1); if(!j.categories_searched||j.categories_searched.length<10)process.exit(2); if(j.findings.length===0&&(!j.zero_hit_categories||j.zero_hit_categories.length<10))process.exit(3); console.log('findings:',j.findings.length,'zero-hit:',(j.zero_hit_categories||[]).length); process.exit(0)"</automated>
  </verify>
  <done>
- `MIGRATION_AUDIT_FINDINGS.json` existe em `.planning/phases/02-migra-o-de-storage-para-supabase/`
- Contém ≥1 finding (no mínimo o `applyPendingMigrations` em server/src/index.ts) OU zero_hit_categories cobrindo todas as 10 categorias
- Cada finding tem os 6 campos obrigatórios não-vazios
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Redigir MIGRATION_AUDIT.md a partir dos findings + decisões de mitigação</name>
  <files>.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md</files>
  <action>
Lendo o `MIGRATION_AUDIT_FINDINGS.json` produzido na Tarefa 1, escreva `MIGRATION_AUDIT.md` no mesmo diretório seguindo EXATAMENTE este template (preencha as seções):

```markdown
# Migration Audit — Postgres Embedded → Supabase

**Phase:** 02 (Migração de Storage para Supabase)
**Requirement satisfied:** INFRA-01
**Audited:** {ISO-8601 date from MIGRATION_AUDIT_FINDINGS.json}
**Auditor:** Claude (executor de planejamento)
**Status:** Complete

## Scope

Auditoria sistemática do código herdado do paperclip (importado na Fase 1 a partir do `paperclipai/paperclip@master`) procurando acoplamentos com o backend Postgres embedded que possam quebrar — silenciosamente ou ruidosamente — quando o connection string aponta para o Supavisor pooler do Supabase em transaction mode (porta 6543) ou session mode (porta 5432).

**Categorias auditadas (10):**
- A — Prepared statements explícitos
- B — LISTEN/NOTIFY (incompatível com transaction mode)
- C — Advisory locks (session vs transactional)
- D — Tabelas temporárias / SET LOCAL
- E — Long-lived transactions
- F — Auto-migrations no startup
- G — Connection pool config
- H — Embedded postgres references
- I — DATABASE_URL / SUPABASE_DB_URL references
- J — Migrations existentes (extensions, schemas)

## Methodology

Grep sistemático com 10 patterns por categoria contra `**/*.ts`, `**/*.sql`, `**/*.json`, `**/*.md`. Findings registrados em `MIGRATION_AUDIT_FINDINGS.json` (intermediário, descartável). Cada hit confirmado por leitura de contexto (3 linhas em volta) e classificado em risco LOW / MEDIUM / HIGH com base em:

- **HIGH** — quebra silenciosa em Supavisor; corrupção possível
- **MEDIUM** — degradação observável (latência, race) mas detectável
- **LOW** — aceitável; documentar e seguir
- **NONE** — falso positivo ou padrão já compatível

Reference: `.planning/research/PITFALLS.md` Armadilha 2 (acoplamentos escondidos), Armadilha 3 (auto-migrations).

## Findings

{Para cada categoria A-J, incluir uma subseção. Se zero hits, dizer explicitamente "Zero hits — sem ações necessárias".}

### A — Prepared Statements

{Listar findings da categoria A. Para cada um:}

#### A.1 — postgres-js sem `prepare: false`

- **Risk:** HIGH (porta 6543 / transaction mode) | NONE (porta 5432 / session mode)
- **File:** `packages/db/src/client.ts:48-51`
- **Code:**
  ```ts
  export function createDb(url: string) {
    const sql = postgres(url);  // sem opções
    return drizzlePg(sql, { schema });
  }
  ```
- **Why it matters:** postgres-js cacheia prepared statements por default. Em Supavisor transaction mode, statements ganham conexões diferentes a cada query → "prepared statement does not exist" intermitente.
- **Decision:** **Mitigate** — adicionar `prepare: false` quando connection string tem porta 6543. Implementação no Plano 02-03.
- **Source citation:** PITFALLS Armadilha 2; STACK linha 200.

### B — LISTEN/NOTIFY

{...}

### C — Advisory Locks

#### C.1 — `pg_advisory_xact_lock` em plugin-database.ts

- **Risk:** NONE
- **File:** `server/src/services/plugin-database.ts:414`
- **Code:** `await tx.execute(sql\`SELECT pg_advisory_xact_lock(\${lockKey})\`);`
- **Why it matters:** É a versão TRANSACTIONAL (`_xact_`), liberada automaticamente no commit/rollback. Supavisor suporta locks dentro de transação porque a conexão é dedicada por toda duração da transação.
- **Decision:** **Accept as-is** — verified compatible com Supavisor transaction mode.
- **Source citation:** Supabase docs — Supavisor terminology; PITFALLS Armadilha 2 (distinção session vs xact).

{... continuar para D, E, F, G, H, I, J ...}

### F — Auto-Migrations no Startup

#### F.1 — `ensureMigrations` chamado em startServer()

- **Risk:** HIGH (multi-dev shared DB)
- **File:** `server/src/index.ts:130-183` (função local `ensureMigrations`)
- **Code:**
  ```ts
  async function ensureMigrations(connectionString: string, label: string, opts?: EnsureMigrationsOptions): Promise<MigrationSummary> {
    // ...
    await applyPendingMigrations(connectionString);
    return "applied (pending migrations)";
  }
  ```
- **Why it matters:** Durante a Fase 1, isso era inofensivo (cada dev tinha seu embedded local). Agora 5 devs apontam para o mesmo Supabase: dois startups concorrentes podem disputar o lock de migration; pior, uma migration de feature branch pode ser aplicada sem review.
- **Decision:** **Mitigate** — desabilitar auto-apply por default. Startup deve falhar fast com mensagem acionável quando `inspectMigrations` retorna `pendingMigrations`. Honra ao opt-in `PAPERCLIP_MIGRATION_AUTO_APPLY=true` permanece para CI; mas não para devs locais. Implementação no Plano 02-03 (DB-02).
- **Source citation:** PITFALLS Armadilha 3; CONTEXT.md decisão `Auto-migrations no startup desabilitadas`.

### J — Migrations Existentes (71 arquivos)

#### J.1 — Inventário

- **Total:** 71 arquivos `.sql` em `packages/db/src/migrations/0000_*.sql` … `0070_*.sql`
- **Journal:** `packages/db/src/migrations/meta/_journal.json`
- **Risk a investigar:** Cada migration pode referenciar (a) extensions que Supabase não tem por default; (b) tipos / funções que conflitam com schema `auth` interno do Supabase; (c) sintaxe não-suportada em pgBouncer.

#### J.2 — Pré-requisitos detectados via grep `CREATE\s+EXTENSION`

{Listar o que o grep encontrou. Se zero, escrever "Nenhuma migration declara `CREATE EXTENSION`. Supabase já fornece pgcrypto, uuid-ossp, vector, pg_trgm por default — risco BAIXO."}

#### J.3 — Decisão estratégica

- **Decision:** **Try-apply-first, regenerate-baseline-if-fails** (decisão travada em CONTEXT.md)
- **Rationale:** Regenerar baseline é caminho HIGH-effort que rompe o histórico de migrations. Se as 71 aplicarem cleanly, mantemos o histórico (auditável; permite rollback fino). Decisão final é EMPÍRICA, no Plano 02-04.
- **Contingency plan (se a aplicação falhar):**
  1. Capturar a primeira migration que falha + erro completo do Postgres
  2. Investigar: (a) é missing extension? CREATE EXTENSION manual; (b) é conflito com `auth` schema? Renomear schema na migration; (c) é DDL não-suportado em pooler? Mover para session mode (porta 5432)
  3. Se ≥3 migrations falham, regenerar baseline única via `pnpm db:generate` partindo do schema atual

## Migration Strategy Decision

Resumo executivo das decisões consolidadas (entrada para Plano 02-03 e 02-04):

| Aspect | Decision | Plan |
|--------|----------|------|
| Driver config | `prepare: false` quando porta=6543, pool max=5, idle_timeout=20 | 02-03 |
| Auto-migrations | Desabilitadas por default; falha fast em pending; opt-in `PAPERCLIP_MIGRATION_AUTO_APPLY=true` apenas para CI | 02-03 |
| Embedded postgres | Permanece como fallback opt-in via `PAPERCLIP_DB_MODE=embedded-postgres` | 02-03 |
| 71 migrations | Try-apply-first contra Supabase via `pnpm db:migrate`; regenerate-baseline only if blocking failures | 02-04 |
| GitHub Actions migration pipeline | Único caminho legítimo de `pnpm db:migrate` em merge para main | 02-04 |
| Better Auth | Preservado; cookie prefix derivado de PAPERCLIP_INSTANCE_ID | 02-05 |
| Pre-commit hook for service-role detection | Detecta `eyJ...` JWT + `VITE_*SERVICE_ROLE*` em arquivos cliente | 02-02 |

## Mitigation Plan

Mitigações organizadas por plano consumidor:

- **Plano 02-02** consome: A.1 (pre-commit hook é orthogonal mas roda em paralelo; sem dependência de findings)
- **Plano 02-03** consome: A.1, F.1, e qualquer achado HIGH em D-G
- **Plano 02-04** consome: J.1-J.3 (estratégia de migrations)
- **Plano 02-05** consome: nenhum achado direto (auth wiring é independente da auditoria)
- **Plano 02-06** consome: validação E2E confirma que mitigações dos planos anteriores funcionaram

## Discoveries (Empirical Findings to Surface in Planning Notes)

Achados que merecem destaque por divergir de docs públicas:

- **Pooler hostname `aws-1-sa-east-1`** (não `aws-0-`) — não documentado em Supabase docs públicas para sa-east-1. Descoberto empiricamente em 2026-04-26 ao validar conexão com `bxlczioxgizgvtznukwt`. Registrar em `.env.example` Plano 02-03 com comentário inline.

## References

- `.planning/research/PITFALLS.md` — Armadilhas 2, 3, 5
- `.planning/research/STACK.md` — `prepare: false`, pool config
- `.planning/research/ARCHITECTURE.md` — pontos de mudança em packages/db
- `.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md` — decisões travadas
- Supabase docs — [Supavisor terminology](https://supabase.com/docs/guides/troubleshooting/supavisor-and-connection-terminology-explained-9pr_ZO)

---
*Audit complete: {ISO-8601 date}*
*Next plan to consume: 02-03 (DB connection patches + env scaffolding)*
```

REGRAS DURAS de redação:
1. NÃO inventar findings — toda subseção em `## Findings` mapeia para entradas reais em `MIGRATION_AUDIT_FINDINGS.json` da Tarefa 1, OU declara explicitamente "zero hits" para a categoria
2. Risk classification deve ter justificativa — nunca apenas "MEDIUM" sem `Why it matters`
3. Toda `Decision: Mitigate` aponta para o plano consumidor (`02-03`, `02-04`, etc.) — sem mitigações órfãs
4. Cada finding cita fonte (PITFALLS Armadilha N, STACK linha N, doc Supabase, etc.) — sem afirmações soltas
5. NÃO fazer `pnpm install` ou rodar testes — auditoria é puramente baseada em leitura de código

Após escrever o `.md`, DELETE `MIGRATION_AUDIT_FINDINGS.json` (era artefato intermediário descartável):
```bash
rm .planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT_FINDINGS.json
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='D:/projetos/ddd/.planning/phases/02-migra-o-de-storage-para-supabase/MIGRATION_AUDIT.md';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');const required=['## Scope','## Methodology','## Findings','## Migration Strategy Decision','## Mitigation Plan','INFRA-01','prepare: false','pg_advisory_xact_lock','71 migration','aws-1-sa-east-1'];const missing=required.filter(s=>!c.includes(s));if(missing.length){console.error('missing sections:',missing);process.exit(2)}if(c.length<3000){console.error('document too short:',c.length,'chars');process.exit(3)}console.log('OK',c.length,'chars,',(c.match(/^#{2,3} /gm)||[]).length,'sections')"</automated>
  </verify>
  <done>
- `MIGRATION_AUDIT.md` existe em `.planning/phases/02-migra-o-de-storage-para-supabase/`
- Contém todas as 5 seções obrigatórias (Scope, Methodology, Findings, Migration Strategy Decision, Mitigation Plan)
- ≥3 findings com Risk + File + Decision (mínimo: A.1 prepare:false, C.1 advisory_xact_lock, F.1 ensureMigrations)
- Cita INFRA-01 explicitamente como requirement satisfeito
- Cita `aws-1-sa-east-1` como discovery empírica
- `MIGRATION_AUDIT_FINDINGS.json` deletado (intermediário descartável)
  </done>
</task>

</tasks>

<verification>
- `MIGRATION_AUDIT.md` é o entregável único do plano e satisfaz INFRA-01
- Documento é entrada explícita para os planos 02-03 (driver patches), 02-04 (apply migrations), 02-05 (auth) — Section "Mitigation Plan" mapeia cada finding para seu consumidor
- Toda decisão tem rastreabilidade: PITFALLS / STACK / CONTEXT.md cited per finding
- Auditoria valida ou refuta a estratégia "try 71 migrations as-is" — entrada chave para 02-04
</verification>

<success_criteria>
- [ ] `MIGRATION_AUDIT.md` existe e tem ≥3000 caracteres
- [ ] Cobre 10 categorias (A-J), com zero-hit categorias declaradas explicitamente
- [ ] Cada finding HIGH/MEDIUM tem decisão (Mitigate / Accept) + plano consumidor
- [ ] INFRA-01 é citado como requirement satisfeito no frontmatter do MD
- [ ] Pooler hostname empírico `aws-1-sa-east-1` é documentado como discovery
- [ ] `MIGRATION_AUDIT_FINDINGS.json` foi deletado (intermediário)
- [ ] Plano confirma que `pg_advisory_xact_lock` (transactional) é compatible — não é blocker
- [ ] Plano confirma que 71 migrations devem ser tentadas as-is no 02-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-migra-o-de-storage-para-supabase/02-01-SUMMARY.md` describing:
- Quantos findings totais (HIGH + MEDIUM + LOW + NONE)
- Decisão final sobre estratégia das 71 migrations (try-as-is confirmed | regenerate-baseline-needed)
- Pointers explícitos para os planos consumidores (02-03, 02-04, 02-05)
</output>
