---
phase: 02-migra-o-de-storage-para-supabase
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .husky/pre-commit
  - scripts/check-no-service-role-leak.mjs
  - package.json
  - .gitignore
autonomous: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "Pre-commit hook bloqueia commit que adiciona JWT-shaped string (`eyJ...`) em arquivos do diretório `ui/src/**` ou com extensão `.tsx`/`.jsx`"
    - "Pre-commit hook bloqueia commit que adiciona variável com prefixo `VITE_` cujo nome contém `SERVICE_ROLE` ou `SECRET`"
    - "Hook é instalado automaticamente após `pnpm install` (via husky `prepare` script)"
    - "Hook está em `.husky/pre-commit` (committed) e é executável"
    - "`scripts/check-no-service-role-leak.mjs` retorna exit 0 quando staged diff está limpo, exit 1+ com mensagem acionável quando detecta leak"
  artifacts:
    - path: ".husky/pre-commit"
      provides: "Hook git pré-commit que invoca o checker"
      contains: "check-no-service-role-leak"
    - path: "scripts/check-no-service-role-leak.mjs"
      provides: "Detector de JWT/service-role em staged diff"
      exports: ["main entry as default ESM"]
    - path: "package.json"
      provides: "husky em devDependencies + script `prepare`"
      contains: "husky"
  key_links:
    - from: "package.json (prepare script)"
      to: ".husky/pre-commit"
      via: "`husky install` instala git hooks no `.git/hooks/` que delegam para `.husky/`"
      pattern: "\"prepare\".*husky"
    - from: ".husky/pre-commit"
      to: "scripts/check-no-service-role-leak.mjs"
      via: "shell invocation `node scripts/check-no-service-role-leak.mjs`"
      pattern: "check-no-service-role-leak"
---

<objective>
Implementar pre-commit hook que detecta leak acidental de credenciais Supabase (service-role key ou outro JWT) em arquivos client-side antes do commit chegar ao remoto. Satisfaz AUTH-05.

Purpose: Service-role key bypassa toda RLS e dá acesso total ao Supabase compartilhado da equipe. Um único commit vazado obriga rotação imediata da key + atualização sincronizada de 5+ devs. PITFALLS Armadilha 5 documenta o cenário. Pre-commit é a primeira linha de defesa — barata, automática, falha fast.

Output: Hook husky `.husky/pre-commit` invocando `scripts/check-no-service-role-leak.mjs` que faz `git diff --cached` e procura padrões proibidos. Hook é instalado automaticamente após `pnpm install` (sem fricção para devs novos).
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migra-o-de-storage-para-supabase/02-CONTEXT.md
@.planning/research/PITFALLS.md

@package.json
@.gitignore

<interfaces>
<!-- husky 9 API contract — relevant pieces the executor needs -->

husky 9 (latest, 2025+) instala via `prepare` script no package.json:
  "prepare": "husky"
Cria `.husky/_/` (gitignored) com infra; hooks ficam em `.husky/<hookname>` (committed, executable).
Cada hook é shell script. Husky 9 não precisa mais do shebang `#!/usr/bin/env sh` nem do `. "$(dirname -- "$0")/_/husky.sh"` — apenas o comando direto.

Exemplo `.husky/pre-commit` válido em husky 9:
```sh
node scripts/check-no-service-role-leak.mjs
```

Existing devDependencies no package.json (não modificar):
  "@playwright/test": "^1.58.2",
  "cross-env": "^10.1.0",
  "esbuild": "^0.27.3",
  "typescript": "^5.7.3",
  "vitest": "^3.0.5"

Add: husky@^9.1.7

git diff --cached --unified=0 retorna diff de staged changes; linhas adicionadas começam com `+` (mas não `+++` que é file marker).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar e testar `scripts/check-no-service-role-leak.mjs`</name>
  <files>
    scripts/check-no-service-role-leak.mjs
    scripts/check-no-service-role-leak.test.mjs
  </files>
  <behavior>
Tests primeiro (escrever em `scripts/check-no-service-role-leak.test.mjs` como vitest spec) cobrindo:

- Test 1: input com staged diff limpo → exit 0, no output to stderr
- Test 2: staged diff adicionando `+const k = "eyJhbGciOiJIUzI1NiIs..."` em `ui/src/lib/supabase.ts` → exit 1, mensagem stderr cita arquivo + linha + tipo "JWT-shaped literal"
- Test 3: staged diff adicionando `+VITE_SUPABASE_SERVICE_ROLE_KEY=foo` em `.env.example` → exit 1, mensagem cita "VITE_*SERVICE_ROLE*"
- Test 4: staged diff adicionando `+const k = "eyJ..."` em `server/src/auth/supabase.ts` (path NÃO client-side) → exit 0 (server-side é permitido)
- Test 5: staged diff adicionando `+VITE_SUPABASE_SECRET=foo` em `ui/.env.local` → exit 1, mensagem cita "VITE_*SECRET*"
- Test 6: staged diff em `*.tsx` adicionando string literal contendo `eyJ` mas com comprimento <30 chars (falso positivo) → exit 0 (heurística requer comprimento mínimo de JWT real)
- Test 7: staged diff modificando linha existente que JÁ tinha `eyJ...` (linha NÃO começa com `+` no diff) → exit 0 (só barra ADIÇÕES novas; remoção/contexto não deve disparar)

O checker DEVE aceitar input via stdin (mock fácil para test) OU via `git diff --cached --unified=0`. Para testes, PREFER injection: exportar `checkDiff(diffString): { exitCode, errors }` como ESM named export, e usar isso na test suite. O entrypoint principal apenas chama `git diff` e delega.
  </behavior>
  <action>
Crie DOIS arquivos:

**1. `scripts/check-no-service-role-leak.mjs`** (ESM, Node 20+ compatible):

```js
#!/usr/bin/env node
// Pre-commit guard: blocks staged diffs that introduce JWT-shaped literals in
// client-side code or VITE_*SERVICE_ROLE*/VITE_*SECRET* env vars anywhere.
// Satisfies AUTH-05 of phase 02 (Migração de Storage para Supabase).
//
// Usage:
//   - Standalone: `node scripts/check-no-service-role-leak.mjs`
//   - Programmatic: `import { checkDiff } from "./check-no-service-role-leak.mjs"`

import { execSync } from "node:child_process";

// Patterns
// JWT structural: three base64url segments joined by dots, header always starts with `eyJ` (b64 of `{"`).
// Minimum total length 30 to avoid matching `eyJ` substrings in unrelated strings.
const JWT_REGEX = /eyJ[A-Za-z0-9_-]{15,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}/;
// VITE_ prefixed env vars containing forbidden tokens
const VITE_FORBIDDEN_REGEX = /^\+\s*VITE_[A-Z0-9_]*(?:SERVICE_ROLE|SECRET)[A-Z0-9_]*\s*=/;

// Path classification
function isClientSidePath(path) {
  if (!path) return false;
  // Forbidden: ui/src/** OR any *.tsx / *.jsx anywhere
  if (path.startsWith("ui/src/")) return true;
  if (path.endsWith(".tsx") || path.endsWith(".jsx")) return true;
  return false;
}

/**
 * @param {string} diffOutput - output of `git diff --cached --unified=0`
 * @returns {{ exitCode: number, errors: string[] }}
 */
export function checkDiff(diffOutput) {
  const errors = [];
  const lines = diffOutput.split("\n");
  let currentFile = null;
  let lineNumberInNewFile = 0;

  for (const rawLine of lines) {
    // File marker: `diff --git a/path b/path` or `+++ b/path`
    const filePlusMatch = rawLine.match(/^\+\+\+ b\/(.+)$/);
    if (filePlusMatch) {
      currentFile = filePlusMatch[1].trim();
      lineNumberInNewFile = 0;
      continue;
    }
    // Hunk header: `@@ -X,Y +A,B @@` — set lineNumberInNewFile = A
    const hunkMatch = rawLine.match(/^@@ .* \+(\d+)(?:,\d+)? @@/);
    if (hunkMatch) {
      lineNumberInNewFile = Number.parseInt(hunkMatch[1], 10) - 1; // -1 because we increment before use
      continue;
    }
    // File header markers we don't care about
    if (rawLine.startsWith("diff --git ") || rawLine.startsWith("--- ") || rawLine.startsWith("index ")) {
      continue;
    }
    // Context line (starts with space) or removal (starts with `-`): increment counter only for context
    if (rawLine.startsWith(" ")) {
      lineNumberInNewFile += 1;
      continue;
    }
    if (rawLine.startsWith("-") && !rawLine.startsWith("---")) {
      // removal — does not advance new-file counter and is not a leak vector
      continue;
    }
    // Addition line — this is what we scan
    if (rawLine.startsWith("+") && !rawLine.startsWith("+++")) {
      lineNumberInNewFile += 1;
      const addedContent = rawLine.slice(1);

      // Check 1: VITE_*SERVICE_ROLE* / VITE_*SECRET* — applies to ALL files (env files included)
      if (VITE_FORBIDDEN_REGEX.test(rawLine)) {
        errors.push(
          `${currentFile}:${lineNumberInNewFile}: forbidden env var name (VITE_ prefix exposes to client bundle): ${addedContent.trim()}`,
        );
        continue;
      }

      // Check 2: JWT-shaped literal in client-side path
      if (isClientSidePath(currentFile) && JWT_REGEX.test(addedContent)) {
        errors.push(
          `${currentFile}:${lineNumberInNewFile}: JWT-shaped literal in client-side file (service-role/auth tokens MUST live server-only)`,
        );
        continue;
      }
    }
  }

  return { exitCode: errors.length === 0 ? 0 : 1, errors };
}

function main() {
  let diff;
  try {
    diff = execSync("git diff --cached --unified=0", { encoding: "utf8", maxBuffer: 50 * 1024 * 1024 });
  } catch (err) {
    process.stderr.write(`[pre-commit] failed to read staged diff: ${err.message}\n`);
    process.exit(2);
  }

  const { exitCode, errors } = checkDiff(diff);
  if (exitCode !== 0) {
    process.stderr.write("\n[pre-commit] BLOCKED — service-role / JWT leak detected:\n\n");
    for (const e of errors) process.stderr.write(`  - ${e}\n`);
    process.stderr.write(
      "\nFix:\n" +
      "  - JWTs (Supabase service-role key, Better Auth tokens) must NEVER appear in ui/src/** or *.tsx/*.jsx\n" +
      "  - VITE_ prefix exposes value to the Vite client bundle — never use VITE_*SERVICE_ROLE* or VITE_*SECRET*\n" +
      "  - Move secrets to server-only env files; access via process.env in server/ code only\n" +
      "  - If this is a false positive (e.g. fixture data), git commit --no-verify with justification in commit message\n\n",
    );
  }
  process.exit(exitCode);
}

// Only run main() when invoked as CLI (not when imported by tests)
const isMain = import.meta.url === `file://${process.argv[1]?.replaceAll("\\\\", "/")}` ||
               import.meta.url.endsWith(process.argv[1]?.replaceAll("\\\\", "/"));
if (isMain) main();
```

**2. `scripts/check-no-service-role-leak.test.mjs`** (vitest):

```js
import { describe, it, expect } from "vitest";
import { checkDiff } from "./check-no-service-role-leak.mjs";

const fakeJwt = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIn0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c";

describe("check-no-service-role-leak", () => {
  it("Test 1: clean diff exits 0", () => {
    const diff = `diff --git a/server/src/foo.ts b/server/src/foo.ts
+++ b/server/src/foo.ts
@@ -0,0 +1 @@
+export const x = "hello";
`;
    expect(checkDiff(diff)).toEqual({ exitCode: 0, errors: [] });
  });

  it("Test 2: JWT literal in ui/src/** is blocked", () => {
    const diff = `diff --git a/ui/src/lib/supabase.ts b/ui/src/lib/supabase.ts
+++ b/ui/src/lib/supabase.ts
@@ -0,0 +1 @@
+const k = "${fakeJwt}";
`;
    const r = checkDiff(diff);
    expect(r.exitCode).toBe(1);
    expect(r.errors[0]).toMatch(/ui\/src\/lib\/supabase\.ts:1.*JWT-shaped/);
  });

  it("Test 3: VITE_*SERVICE_ROLE* in any file is blocked", () => {
    const diff = `diff --git a/.env.example b/.env.example
+++ b/.env.example
@@ -0,0 +1 @@
+VITE_SUPABASE_SERVICE_ROLE_KEY=foo
`;
    const r = checkDiff(diff);
    expect(r.exitCode).toBe(1);
    expect(r.errors[0]).toMatch(/VITE_.*SERVICE_ROLE/);
  });

  it("Test 4: JWT literal in server/** is allowed", () => {
    const diff = `diff --git a/server/src/auth/supabase.ts b/server/src/auth/supabase.ts
+++ b/server/src/auth/supabase.ts
@@ -0,0 +1 @@
+const k = "${fakeJwt}";
`;
    expect(checkDiff(diff)).toEqual({ exitCode: 0, errors: [] });
  });

  it("Test 5: VITE_*SECRET* in ui env is blocked", () => {
    const diff = `diff --git a/ui/.env.local b/ui/.env.local
+++ b/ui/.env.local
@@ -0,0 +1 @@
+VITE_SUPABASE_SECRET=foo
`;
    const r = checkDiff(diff);
    expect(r.exitCode).toBe(1);
    expect(r.errors[0]).toMatch(/VITE_.*SECRET/);
  });

  it("Test 6: short eyJ literal (false positive) is allowed", () => {
    const diff = `diff --git a/ui/src/lib/foo.tsx b/ui/src/lib/foo.tsx
+++ b/ui/src/lib/foo.tsx
@@ -0,0 +1 @@
+const x = "eyJshort";
`;
    expect(checkDiff(diff)).toEqual({ exitCode: 0, errors: [] });
  });

  it("Test 7: removed line with JWT (line begins with -) is not flagged", () => {
    const diff = `diff --git a/ui/src/lib/supabase.ts b/ui/src/lib/supabase.ts
+++ b/ui/src/lib/supabase.ts
@@ -1 +1 @@
-const k = "${fakeJwt}";
+const k = process.env.PUBLIC_KEY;
`;
    expect(checkDiff(diff)).toEqual({ exitCode: 0, errors: [] });
  });
});
```

Após criar os dois arquivos, faça os testes passarem:
```bash
pnpm exec vitest run scripts/check-no-service-role-leak.test.mjs
```

Se qualquer teste falhar, ajuste o regex/lógica em `check-no-service-role-leak.mjs` (NÃO os testes — testes definem o contrato). Os testes 6 e 7 são casos de borda comuns onde implementações ingênuas falham — preste atenção a eles.

Ajuste o `JWT_REGEX` se Test 6 falhar (provavelmente sua hipótese: quantificadores `{15,}.{10,}.{10,}` evitam match em `eyJshort`).

Make `scripts/check-no-service-role-leak.mjs` executable (chmod +x não funciona em Windows, mas em Linux/macOS sim — incluir no Bash):
```bash
chmod +x scripts/check-no-service-role-leak.mjs 2>/dev/null || true
```
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && pnpm exec vitest run scripts/check-no-service-role-leak.test.mjs --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- Ambos os arquivos existem
- `pnpm exec vitest run scripts/check-no-service-role-leak.test.mjs` passa todos os 7 testes
- `node scripts/check-no-service-role-leak.mjs` executável (não trava em git diff vazio — exit 0 quando staged é vazio)
- `checkDiff` exportado como named ESM
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Instalar husky e ligar pre-commit hook</name>
  <files>
    package.json
    .husky/pre-commit
    .gitignore
  </files>
  <action>
**Passo 1 — Adicionar husky e o script `prepare`:**

Edite `package.json` raiz:
1. Em `devDependencies`, adicione `"husky": "^9.1.7"` (ordenar alfabeticamente entre `esbuild` e `typescript`)
2. Em `scripts`, adicione `"prepare": "husky"` (no início da seção scripts, antes de `preflight:workspace-links`)

NÃO REMOVA NENHUM SCRIPT EXISTENTE.

**Passo 2 — Instalar husky:**
```bash
cd D:/projetos/ddd && pnpm install
```

Esse comando dispara o `prepare` script, que cria `.husky/_/` (gitignored automaticamente) e prepara a infra.

**Passo 3 — Criar `.husky/pre-commit`:**

Conteúdo EXATO:
```sh
node scripts/check-no-service-role-leak.mjs
```

(Sem shebang, sem source — husky 9 dispensa boilerplate. UMA linha apenas.)

Após criar, em Linux/macOS torne executável:
```bash
chmod +x .husky/pre-commit 2>/dev/null || true
```

(Em Windows isso é no-op; husky lida com isso via git's `core.fileMode = false`.)

**Passo 4 — Atualizar `.gitignore`:**

O `.gitignore` atual NÃO precisa entry específico para `.husky/_/` — husky 9 cria `.husky/_/.gitignore` interno automaticamente. Verifique que rodou:
```bash
cat D:/projetos/ddd/.husky/_/.gitignore 2>/dev/null
```

Deve retornar algo (gerado por husky). Se NÃO retornou, adicione manualmente em `.gitignore` raiz:
```
# husky internal (managed by husky 9; safe-net if husky didn't generate)
.husky/_/
```

**Passo 5 — Smoke test do hook:**

Verifique que o hook está conectado:
```bash
cd D:/projetos/ddd && cat .git/hooks/pre-commit
```

Deve referenciar `.husky/pre-commit` (husky 9 escreve um delegator em `.git/hooks/pre-commit`).

Em seguida, simule um commit que DEVE ser bloqueado:
```bash
cd D:/projetos/ddd && \
  echo 'export const k = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.test_signature_long_enough_to_match";' > /tmp/leak-test.tsx && \
  git -c core.hooksPath=.husky add /tmp/leak-test.tsx 2>/dev/null
```

(Em Windows, use `$TEMP` ou `%TEMP%` em vez de `/tmp`. Adapte conforme necessário — o ponto é provar que o hook bloqueia.)

Você NÃO precisa rodar `git commit` realmente; basta confirmar que `.husky/pre-commit` existe E que `node scripts/check-no-service-role-leak.mjs < /dev/null` retorna exit 0 (sem staged changes leak == limpo). Limpe o arquivo de teste:
```bash
rm /tmp/leak-test.tsx 2>/dev/null
git reset HEAD /tmp/leak-test.tsx 2>/dev/null || true
```
  </action>
  <verify>
    <automated>cd D:/projetos/ddd && node -e "const fs=require('fs');const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));if(!pkg.devDependencies?.husky)process.exit(1);if(!pkg.scripts?.prepare?.includes('husky'))process.exit(2);if(!fs.existsSync('.husky/pre-commit'))process.exit(3);const hook=fs.readFileSync('.husky/pre-commit','utf8');if(!hook.includes('check-no-service-role-leak'))process.exit(4);console.log('OK: husky',pkg.devDependencies.husky,'| prepare:',pkg.scripts.prepare,'| hook:',hook.trim())"</automated>
  </verify>
  <done>
- `package.json` tem `husky` em devDependencies (≥9.1.7)
- `package.json` tem `"prepare": "husky"` em scripts
- `pnpm install` foi rodado e disparou o prepare → `.husky/_/` existe
- `.husky/pre-commit` existe, contém UMA linha invocando `node scripts/check-no-service-role-leak.mjs`
- `.git/hooks/pre-commit` foi escrito por husky (delegando para `.husky/`)
- `.gitignore` cobre `.husky/_/` (manual ou automático)
  </done>
</task>

</tasks>

<verification>
- AUTH-05 satisfeito: hook detecta `eyJ...` em client-side + VITE_*SERVICE_ROLE*/SECRET* em qualquer file
- Hook é instalado automaticamente em qualquer máquina nova após `pnpm install` (zero fricção)
- Falsos positivos de tamanho curto (`eyJshort`) NÃO disparam (regex requer 3 segmentos JWT-shaped reais)
- Removals (linhas começando com `-`) NÃO disparam (somente adições)
- Server-side paths (`server/**`, `packages/**`) ficam livres para JWT (legítimo)
</verification>

<success_criteria>
- [ ] `scripts/check-no-service-role-leak.mjs` existe e exporta `checkDiff`
- [ ] `scripts/check-no-service-role-leak.test.mjs` tem 7 testes, todos passam
- [ ] `.husky/pre-commit` existe com 1 linha
- [ ] `package.json` tem husky@^9.1.7 + script `prepare`
- [ ] `pnpm install` instala husky e gera `.husky/_/` automaticamente
- [ ] `.git/hooks/pre-commit` foi escrito por husky e delega para `.husky/`
</success_criteria>

<output>
After completion, create `.planning/phases/02-migra-o-de-storage-para-supabase/02-02-SUMMARY.md` describing:
- Versão exata de husky instalada
- 7 casos de teste cobertos pelo checker
- Caminhos protegidos (`ui/src/**`, `*.tsx`, `*.jsx`) vs caminhos livres (server/, packages/)
- Comandos para validar (`pnpm exec vitest run scripts/check-no-service-role-leak.test.mjs`, `cat .husky/pre-commit`)
</output>
