---
phase: 09-traducao-ui-admin-auth-sistemicas
plan: 01
type: execute
wave: 2
depends_on: ["09-03b"]
files_modified:
  - ui/src/i18n/locales/pt-BR/settings.json
  - ui/src/i18n/locales/en-US/settings.json
  - ui/src/pages/CompanySettings.tsx
  - ui/src/pages/CompanyAccess.tsx
  - ui/src/pages/CompanyInvites.tsx
  - ui/src/pages/JoinRequestQueue.tsx
  - ui/src/pages/OrgChart.tsx
  - ui/src/pages/Org.tsx
  - ui/src/pages/Companies.tsx
  - ui/src/pages/CompanyExport.tsx
  - ui/src/pages/CompanyImport.tsx
  - ui/src/pages/CompanySkills.tsx
  - ui/src/pages/Costs.tsx
  - ui/src/pages/__tests__/CompanySettings.i18n.test.tsx
  - ui/src/pages/__tests__/CompanyAccess.i18n.test.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "User with locale=pt-BR navega CompanySettings (general/appearance/environments/hiring/claude-pool/invites/packages/danger-zone) sem ver string em inglês"
    - "User com locale=pt-BR navega CompanyAccess (humans table, edit member dialog, remove member dialog, pending join requests) em pt-BR"
    - "Telas Companies/Org/OrgChart/CompanyInvites/JoinRequestQueue/CompanyExport/CompanyImport/CompanySkills/Costs renderizam em pt-BR"
    - "Toggle para en-US devolve todas as superfícies admin/company para inglês imediatamente"
    - "Detector missing-keys CI=true exit 0 após migração (sem chaves órfãs)"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/settings.json"
      provides: "Sub-tree settings:company.* extension preservando settings:language.* e settings:claude-accounts.* (Phase 7-8)"
      contains: "company"
    - path: "ui/src/i18n/locales/en-US/settings.json"
      provides: "Mirror en-US"
      contains: "company"
    - path: "ui/src/pages/CompanySettings.tsx"
      provides: "Página admin migrada para t()"
      contains: "useTranslation"
    - path: "ui/src/pages/CompanyAccess.tsx"
      provides: "Página access migrada para t() com ROLE_KEY/STATUS_KEY/PERMISSION_KEY lookups"
      contains: "ROLE_KEY"
    - path: "ui/src/pages/__tests__/CompanySettings.i18n.test.tsx"
      provides: "RTL probe asserindo strings pt-BR ↔ en-US toggle"
      min_lines: 40
    - path: "ui/src/pages/__tests__/CompanyAccess.i18n.test.tsx"
      provides: "RTL probe asserindo dialog edit + remove + pending join card"
      min_lines: 40
  key_links:
    - from: "ui/src/pages/CompanySettings.tsx (e siblings)"
      to: "settings:company.* sub-tree"
      via: "useTranslation([\"settings\", \"common\"])"
      pattern: "useTranslation\\(\\[\"settings\""
    - from: "ui/src/pages/CompanyAccess.tsx ROLE_KEY/PERMISSION_KEY"
      to: "settings:company.access.role.* / settings:company.access.permission.*"
      via: "Static lookup map t(ROLE_KEY[role])"
      pattern: "ROLE_KEY\\["
    - from: "Mutation onError handlers em todas as 11 páginas"
      to: "translateApiError helper (Plano 09-03b)"
      via: "pushToast({...translateApiError(err, t), tone: \"error\"})"
      pattern: "translateApiError"
---

<objective>
Traduzir 11 páginas admin/company de inglês hardcoded para `useTranslation`-driven pt-BR/en-US. Estender `settings.json` com sub-tree `company.*` cobrindo CompanySettings (general/appearance/environments/hiring/claude-pool/invites/packages/danger-zone), CompanyAccess (humans table + edit/remove dialogs + pending join requests + permissions/roles/status), e as 9 páginas siblings (Companies, Org, OrgChart, CompanyInvites, JoinRequestQueue, CompanyExport, CompanyImport, CompanySkills, Costs). Reusa padrões consolidados em Phase 8 (STATUS_KEY enum→kebab, useTranslation(["settings", "common"]), inner-component useTranslation, brand-preserving interpolation, plurals _one/_other) e consome `translateApiError` (do Plano 09-03b) em todos os mutation onError handlers. Preserva integralmente sub-trees `settings:language.*` (Phase 7) e `settings:claude-accounts.*` (Phase 8-03).

Purpose: Cierra UI-04 — superfície admin/company 100% pt-BR. Equipe Paperclip operando em pt-BR não vê mais texto bilíngue em settings, access controls, gestão de membros, pool de contas Claude (já feito em 8-03), cost summary, ou rotation history.

Output: 11 arquivos TSX migrados + settings.json estendida (~280 keys novas) + 2 RTL probe tests.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-CONTEXT.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-RESEARCH.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-VALIDATION.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-03b-SUMMARY.md

@ui/src/pages/ProfileSettings.tsx
@ui/src/pages/ClaudeAccounts.tsx
@ui/src/pages/CompanySettings.tsx
@ui/src/pages/CompanyAccess.tsx
@ui/src/i18n/locales/pt-BR/settings.json
@ui/src/i18n/locales/en-US/settings.json
@packages/shared/src/constants.ts

<interfaces>
<!-- Pattern canônico (verified em ClaudeAccounts.tsx:42-47, ProfileSettings.tsx:26) -->

```typescript
import { useTranslation } from "react-i18next";

export function CompanyAccess() {
  const { t } = useTranslation(["settings", "common"]);
  return <h1>{t("settings:company.access.title")}</h1>;
}
```

<!-- STATUS_KEY enum→kebab pattern (canônico v1.1, do RESEARCH Pattern B linhas 859-872) -->

```typescript
const ROLE_KEY: Record<NonNullable<CompanyMember["membershipRole"]>, string> = {
  owner: "settings:company.access.role.owner",
  admin: "settings:company.access.role.admin",
  operator: "settings:company.access.role.operator",
  viewer: "settings:company.access.role.viewer",
};
// no JSX:
{member.membershipRole ? t(ROLE_KEY[member.membershipRole]) : t("settings:company.access.role.unset")}
```

<!-- translateApiError consumption (do Plano 09-03b) -->

```typescript
import { translateApiError } from "@/lib/translateApiError";
import { useToast } from "@/context/ToastContext";

const { pushToast } = useToast();
const { t } = useTranslation(["settings", "common"]);

const archiveMutation = useMutation({
  mutationFn: (id: string) => companiesApi.archive(id),
  onError: (err) => {
    pushToast({ ...translateApiError(err, t), tone: "error" });
  },
});
```

<!-- Anti-pattern (Pitfall 3 do RESEARCH) -->
<!-- NÃO faça: -->
<!--   const permissionLabels: Record<...> = { "agents:create": "Create agents", ... };
       <span>{permissionLabels[permKey]}</span>
       (string hardcoded em objeto JS, mesmo dentro do componente, não passa por t()) -->
<!-- FAÇA: PERMISSION_KEY com key i18n -->
```typescript
const PERMISSION_KEY: Record<PermissionKey, string> = {
  "agents:create": "settings:company.access.permission.agents-create",
  "users:invite": "settings:company.access.permission.users-invite",
  // ... 8 permissions total (verified em CompanyAccess.tsx:30-39)
};
// no JSX: t(PERMISSION_KEY[permKey])
```

<!-- packages/shared/src/constants.ts:464-469 (HUMAN_COMPANY_MEMBERSHIP_ROLE_LABELS) -->
<!-- NÃO MEXER (decisão Anti-Pattern 1 do RESEARCH) — server usa para logs/exports em inglês -->

<!-- Sub-tree settings:company.* completa (do RESEARCH §"Sample bootstrap" linhas 310-460 + §"Pattern 3" linhas 290-308) -->
<!-- Reproduzido como REFERÊNCIA — implementação literal -->

```json
{
  "language": { /* ...mantém Phase 7 byte-por-byte... */ },
  "instance-settings": { /* ...mantém Phase 8-03... */ },
  "claude-accounts": { /* ...mantém Phase 8-03... */ },
  "profile": { /* ...mantém Phase 8-03... */ },
  "heartbeats": { /* ...mantém Phase 8-03... */ },
  "company": {
    "title": "Configurações da empresa",
    "no-company-selected": "Nenhuma empresa selecionada. Selecione uma empresa no seletor acima.",
    "general": {
      "section": "Geral",
      "name-label": "Nome da empresa",
      "name-hint": "O nome de exibição para sua empresa.",
      "description-label": "Descrição",
      "description-hint": "Descrição opcional mostrada no perfil da empresa.",
      "description-placeholder": "Descrição opcional da empresa"
    },
    "appearance": {
      "section": "Aparência",
      "logo-label": "Logo",
      "logo-hint": "Carregue uma imagem PNG, JPEG, WEBP, GIF ou SVG.",
      "remove-logo": "Remover logo",
      "removing-logo": "Removendo…",
      "logo-upload-failed": "Falha ao enviar logo",
      "uploading-logo": "Enviando logo…",
      "brand-color-label": "Cor da marca",
      "brand-color-hint": "Define a cor do ícone da empresa. Deixe vazio para cor auto-gerada.",
      "brand-color-clear": "Limpar"
    },
    "actions": {
      "save-changes": "Salvar alterações",
      "saving": "Salvando…",
      "saved": "Salvo",
      "save-failed": "Falha ao salvar"
    },
    "environments": { "section": "Ambientes", /* ...placeholders to be expanded by executor based on file Read */ },
    "hiring": {
      "section": "Contratação",
      "require-approval-label": "Exigir aprovação do board para novas contratações",
      "require-approval-hint": "Novas contratações de agente ficam pendentes até aprovação do board."
    },
    "claude-pool": { /* extensão da Phase 8-03 settings:claude-accounts; sub-tree pode ficar aqui ou ser referenciado cross-namespace conforme decisão executor */ },
    "invites": { "section": "Convites", "generate-cta": "Gerar prompt de convite OpenClaw", /* ... */ },
    "packages": { "section": "Pacotes da empresa", /* ... */ },
    "danger-zone": {
      "section": "Zona de perigo",
      "archive-description": "Arquive esta empresa para escondê-la da barra lateral. Isso persiste no banco de dados.",
      "archive-cta": "Arquivar empresa",
      "archive-confirm": "Arquivar empresa \"{{name}}\"? Ela será escondida da barra lateral.",
      "archiving": "Arquivando…"
    },
    "access": {
      "title": "Acesso da empresa",
      "humans-section": "Humanos",
      "table": {
        "user-account": "Conta de usuário",
        "role": "Função",
        "status": "Status",
        "grants": "Permissões",
        "action": "Ação",
        "no-members": "Nenhuma associação encontrada para esta empresa ainda.",
        "edit": "Editar",
        "remove": "Remover"
      },
      "role": {
        "owner": "Proprietário",
        "admin": "Admin",
        "operator": "Operador",
        "viewer": "Visualizador",
        "unset": "Não definido"
      },
      "status": { "active": "ativo", "pending": "pendente", "suspended": "suspenso" },
      "permission": {
        "agents-create": "Criar agentes",
        "users-invite": "Convidar humanos e agentes",
        "users-manage-permissions": "Gerenciar membros e permissões",
        "tasks-assign": "Atribuir tarefas",
        "tasks-assign-scope": "Atribuir tarefas com escopo",
        "tasks-manage-active-checkouts": "Gerenciar checkouts ativos",
        "joins-approve": "Aprovar solicitações de entrada",
        "environments-manage": "Gerenciar ambientes"
      },
      "edit-dialog": { /* ...do RESEARCH linhas 421-433 */ },
      "remove-dialog": { /* ...do RESEARCH linhas 434-448, com plurals _one/_other */ },
      "join-request": { /* ...do RESEARCH linhas 449-457 */ }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Audit das 9 páginas admin não medidas + extender settings.json</name>
  <files>ui/src/i18n/locales/pt-BR/settings.json, ui/src/i18n/locales/en-US/settings.json, ui/src/pages/CompanyInvites.tsx (READ-ONLY audit), ui/src/pages/JoinRequestQueue.tsx (READ-ONLY audit), ui/src/pages/OrgChart.tsx (READ-ONLY audit), ui/src/pages/Org.tsx (READ-ONLY audit), ui/src/pages/Companies.tsx (READ-ONLY audit), ui/src/pages/CompanyExport.tsx (READ-ONLY audit), ui/src/pages/CompanyImport.tsx (READ-ONLY audit), ui/src/pages/CompanySkills.tsx (READ-ONLY audit), ui/src/pages/Costs.tsx (READ-ONLY audit)</files>
  <action>
    **Audit pass (Pitfall 8 do RESEARCH):** As 9 páginas siblings de CompanySettings/CompanyAccess/ClaudeAccounts não foram medidas em RESEARCH. Densidade de strings é estimativa heurística (±50). Antes de migrar, fazer audit detalhado.

    1. Para cada uma das 9 páginas, executar:
       - `Read` o arquivo completo
       - Notar:
         - LOC total
         - Section headers (e.g., `<h1>`, `<h2>`)
         - Form labels (`<label>`, `placeholder=`)
         - Button labels (texto inline em `<button>` ou `<Button>`)
         - Toast strings (`pushToast({title, body})` callsites)
         - Tooltip strings (`title=` ou `aria-label=` em elementos interativos)
         - Empty states (textos em condicionais `&& <p>...`)
         - Dialog/modal copy
         - Hard-coded mensagens de erro/validação inline (e.g., `setError("...")`)

    2. Estender `ui/src/i18n/locales/pt-BR/settings.json`:
       - **PRESERVAR byte-por-byte** as sub-trees existentes: `language.*` (Phase 7), `instance-settings.*` (Phase 8-03), `claude-accounts.*` (Phase 8-03), `profile.*` (Phase 8-03), `heartbeats.*` (Phase 8-03)
       - Adicionar sub-tree `company.*` cobrindo:
         - `general`, `appearance`, `actions` (sub-trees do bloco interfaces)
         - `environments` — section header + table headers (Name, Status, Probe, etc. — confirmar via Read CompanySettings.tsx:763-893) + empty state + draft probe section
         - `hiring`, `claude-pool` (extensão), `invites`, `packages`, `danger-zone`
         - `access.*` — table, role, status, permission, edit-dialog, remove-dialog (com plurals `_one`/`_other`), join-request
       - Adicionar sub-trees adicionais conforme audit das 9 páginas:
         - `org.*` — Org chart (titles, headers, empty states)
         - `companies.*` — Lista de empresas (instance admin)
         - `company-invites.*` — Convites table + actions
         - `join-requests.*` — Queue de approvals
         - `company-export.*`, `company-import.*` — Páginas dedicadas
         - `company-skills.*` — Skills da empresa
         - `costs.*` — Costs page (verificar se Phase 8-03 já cobriu parcialmente em settings:claude-accounts; se sim, usar mesma sub-tree)
       - Plurals: usar i18next built-in `_one`/`_other` suffix (ver Phase 8-03 pattern em settings:heartbeats.* e Phase 8-04 brand-preserving)

    3. Mirror estrutura para `ui/src/i18n/locales/en-US/settings.json`:
       - Mesmas keys, valores em inglês
       - **Crítico:** Detector missing-keys exige paridade estrutural entre locales

    4. Validar JSON: `node -e 'JSON.parse(require("fs").readFileSync("ui/src/i18n/locales/pt-BR/settings.json"))'` e mesma para en-US

    5. Documentar audit no SUMMARY (Tarefa 4) com tabela LOC × estimated strings × actual strings × notes.

    **Anti-pattern:** NÃO traduza `HUMAN_COMPANY_MEMBERSHIP_ROLE_LABELS` em `packages/shared/src/constants.ts` (Anti-Pattern 1 do RESEARCH). Constants permanecem en-US (server logs/exports). UI cria seu próprio lookup `ROLE_KEY` na Tarefa 2.

    Endereça UI-04 (foundation dictionary).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/settings.json')); JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/en-US/settings.json')); console.log('OK')"</automated>
  </verify>
  <done>settings.json (pt-BR + en-US) estendida com sub-tree `company.*` + adicionais conforme audit (org, companies, company-invites, join-requests, company-export, company-import, company-skills, costs); Phase 7-8 sub-trees preservadas byte-por-byte; ambos JSONs válidos; estrutura paridade entre locales; audit doc inclui LOC + string density real para 9 páginas medidas.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Migrar CompanySettings.tsx + CompanyAccess.tsx (núcleo admin) para t()</name>
  <files>ui/src/pages/CompanySettings.tsx, ui/src/pages/CompanyAccess.tsx, ui/src/pages/__tests__/CompanySettings.i18n.test.tsx, ui/src/pages/__tests__/CompanyAccess.i18n.test.tsx</files>
  <action>
    **Núcleo da superfície admin (1480 + 679 LOC = 2159 LOC; ~155 strings).**

    **CompanySettings.tsx (Read primeiro):**

    1. Adicionar `import { useTranslation } from "react-i18next"; import { translateApiError } from "@/lib/translateApiError";` no topo
    2. No componente: `const { t } = useTranslation(["settings", "common"]);`
    3. Migrar section headers (linhas ~614, 643, 763, 1145, 1162, 1209, 1287, 1314 — confirmar via Read):
       - "General" → `t("settings:company.general.section")`
       - "Appearance" → `t("settings:company.appearance.section")`
       - "Environments" → `t("settings:company.environments.section")`
       - "Hiring" → `t("settings:company.hiring.section")`
       - "Claude Account Pool" → manter consumindo `t("settings:claude-accounts.*")` da Phase 8-03 (NÃO duplicar em company.claude-pool); estender Phase 8-03 sub-tree com keys novas se faltarem
       - "Invites" → `t("settings:company.invites.section")`
       - "Company Packages" → `t("settings:company.packages.section")`
       - "Danger Zone" → `t("settings:company.danger-zone.section")`
    4. Field labels/hints (linhas 618-732, 904-1035): cada `<label>` e `<p className="...hint">` traduzido
    5. Toast strings (linhas 363-407): substituir cada `pushToast({title: "...", body: "..."})` por `pushToast({title: t("..."), body: t("...", params)})`
       - Para mutations onError: usar `pushToast({...translateApiError(err, t), tone: "error"})`
    6. window.confirm (linha 1332):
       ```typescript
       const confirmed = window.confirm(t("settings:company.danger-zone.archive-confirm", { name: company.name }));
       ```
    7. Botões "Save changes"/"Saving..."/"Saved"/"Failed to save" (linhas 746-755) → `t("settings:company.actions.save-changes")` etc.
    8. Environment table headers, empty state, list rows (linhas 775-893) → respectivas keys
    9. Generate OpenClaw Invite Prompt section (1208-1283) → `t("settings:company.invites.*")`

    **CompanyAccess.tsx (679 LOC, zero traduzido):**

    1. Adicionar imports + `useTranslation`
    2. **Static lookup maps (Pattern B canônico):**
       ```typescript
       const ROLE_KEY: Record<NonNullable<CompanyMember["membershipRole"]>, string> = {
         owner: "settings:company.access.role.owner",
         admin: "settings:company.access.role.admin",
         operator: "settings:company.access.role.operator",
         viewer: "settings:company.access.role.viewer",
       };
       const STATUS_KEY: Record<CompanyMember["status"], string> = {
         active: "settings:company.access.status.active",
         pending: "settings:company.access.status.pending",
         suspended: "settings:company.access.status.suspended",
       };
       const PERMISSION_KEY: Record<PermissionKey, string> = {
         "agents:create": "settings:company.access.permission.agents-create",
         "users:invite": "settings:company.access.permission.users-invite",
         "users:manage-permissions": "settings:company.access.permission.users-manage-permissions",
         "tasks:assign": "settings:company.access.permission.tasks-assign",
         "tasks:assign-scope": "settings:company.access.permission.tasks-assign-scope",
         "tasks:manage-active-checkouts": "settings:company.access.permission.tasks-manage-active-checkouts",
         "joins:approve": "settings:company.access.permission.joins-approve",
         "environments:manage": "settings:company.access.permission.environments-manage",
       };
       ```
    3. Substituir `permissionLabels` (linhas 30-39, hardcoded) por `PERMISSION_KEY` lookup; uso passa de `permissionLabels[k]` para `t(PERMISSION_KEY[k])`
    4. Substituir `HUMAN_COMPANY_MEMBERSHIP_ROLE_LABELS[role]` por `t(ROLE_KEY[role])` em todos os 4 callsites (linhas 367, 424, 458, 493)
    5. Status badges: `t(STATUS_KEY[member.status])`
    6. Breadcrumb labels (74-77) — `setBreadcrumbs([{ label: t("settings:company.access.title") }])`
    7. Toast strings (115-220) — todos via `t()`; mutation onError via `translateApiError`
    8. Edit member Dialog (403-528): title/description/labels/footer/buttons
    9. Remove member Dialog (530-614): title/description/checking/plurals (`open-issues_one`/`open-issues_other` via `t("...", { count })`)
    10. PendingJoinRequestCard (632-678): submitted-at/no-email/unknown-requester/invite-metadata-unavailable/approve-human/reject-human
    11. Error messages (244-249): `t("...")` ou `translateApiError`

    **Testes RTL:**

    1. Criar `ui/src/pages/__tests__/CompanySettings.i18n.test.tsx`:
       - Render `<CompanySettings />` wrapped em I18nextProvider + QueryClientProvider + CompanyProvider mocks
       - Antes de cada test, `await i18n.changeLanguage(locale)` (pt-BR ou en-US)
       - Test 1: locale=pt-BR, asserta presença de "Configurações da empresa", "Geral", "Aparência", "Salvar alterações"
       - Test 2: locale=en-US, asserta presença de "Company settings", "General", "Appearance", "Save changes"
       - Pattern: ver `ui/src/pages/__tests__/Settings.translation.test.tsx` (Phase 8-03)
       - **Importante (Phase 8-04 fixture-fix Rule 1):** Wrap com I18nextProvider + `beforeAll(() => i18n.changeLanguage("en-US"))` na suite

    2. Criar `ui/src/pages/__tests__/CompanyAccess.i18n.test.tsx`:
       - Render `<CompanyAccess />` com mock CompanyContext fornecendo selectedCompany + members
       - Test 1: locale=pt-BR, asserta "Humanos", "Função", "Status", "Permissões"
       - Test 2: locale=en-US, asserta "Humans", "Role", "Status", "Grants"
       - Test 3: Open edit member dialog (simula click no Edit), asserta "Editar membro" / "Edit member"
       - Test 4: Plural — render PendingJoinRequestCard com count=1 vs count=3 e asserta "1 tarefa atribuída em aberto" vs "3 tarefas atribuídas em aberto"

    3. Run: `pnpm --filter @paperclipai/ui test:run -- CompanySettings.i18n CompanyAccess.i18n`
    4. Run: `CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys` exit 0
    5. Run: `pnpm --filter @paperclipai/ui typecheck` exit 0

    **Pitfall 1 watch:** Se algum teste pré-existente quebrar por fixture i18n (Rule 1 do execute-plan), aplicar fixture-fix protocol — wrap `<I18nextProvider>` + `beforeAll(en-US)` (4ª aplicação no v1.1, padrão consolidado).

    Endereça UI-04 (núcleo).
  </action>
  <verify>
    <automated>pnpm --filter @paperclipai/ui test:run -- CompanySettings.i18n CompanyAccess.i18n</automated>
  </verify>
  <done>CompanySettings.tsx + CompanyAccess.tsx zero strings hardcoded em inglês visíveis ao usuário; 4-6 testes RTL GREEN; PERMISSION_KEY/ROLE_KEY/STATUS_KEY lookups canônicos; HUMAN_COMPANY_MEMBERSHIP_ROLE_LABELS em packages/shared intocado; full UI suite preserva GREEN status; missing-keys CI=true exit 0.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Migrar 9 páginas siblings (Companies, Org, OrgChart, CompanyInvites, JoinRequestQueue, CompanyExport, CompanyImport, CompanySkills, Costs)</name>
  <files>ui/src/pages/Companies.tsx, ui/src/pages/Org.tsx, ui/src/pages/OrgChart.tsx, ui/src/pages/CompanyInvites.tsx, ui/src/pages/JoinRequestQueue.tsx, ui/src/pages/CompanyExport.tsx, ui/src/pages/CompanyImport.tsx, ui/src/pages/CompanySkills.tsx, ui/src/pages/Costs.tsx</files>
  <action>
    **Loop por arquivo** (procedimento idêntico para cada uma):

    1. Read o arquivo completo
    2. Adicionar `import { useTranslation } from "react-i18next";` + `import { translateApiError } from "@/lib/translateApiError";` (este último apenas se houver mutations com onError)
    3. Adicionar `const { t } = useTranslation(["settings", "common"]);` no componente
    4. Substituir cada string hardcoded visível ao usuário por `t("settings:{namespace}.{key}")` usando o sub-tree correspondente da Tarefa 1
    5. Para inner-components (e.g., `OrgChartNode` dentro de `OrgChart.tsx`): adicionar próprio `useTranslation` (Phase 8-03 pattern — inner-component pattern)
    6. Toast onSuccess/onError: success usa `t("...")`; error usa `translateApiError(err, t)`
    7. Empty states e loading states: `t("settings:{namespace}.empty-state")`, `t("settings:{namespace}.loading")`
    8. Brand-preserving interpolation (Phase 8-04): se aparecer "Paperclip", manter literal dentro do template — `t("...", { version })` com template "Paperclip v{{version}}"
    9. Costs.tsx: verificar se já consome `settings:claude-accounts.*` (Phase 8-03 cobriu cost summary table). Se sim, apenas wire-up `useTranslation`. Se não, adicionar sub-tree `settings:costs.*`.
    10. window.confirm: substituir por `window.confirm(t("..."))` se houver

    **Validation incremental:**
    - Após cada arquivo, `pnpm --filter @paperclipai/ui typecheck` (rapid feedback de erro)
    - A cada 3 arquivos, run `CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys` para detectar keys órfãs cedo

    **Final validation:**
    - `pnpm --filter @paperclipai/ui test:run` — full UI suite passa
    - `CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys` exit 0
    - `pnpm --filter @paperclipai/ui typecheck` exit 0
    - Grep `>[A-Z][a-z]` (heuristic — strings inline em JSX começando com uppercase): rodar nos 9 arquivos e verificar manualmente que matches restantes são técnicos (e.g., constants em JSX `{Math.PI}`) e não strings de UI

    **Anti-pattern:** Se um arquivo for trivialmente pequeno (e.g., 50 LOC com 5 strings) e suas strings cobertas por sub-trees existentes (`common:nav.*` Phase 8-04), apenas wire `useTranslation` — não criar sub-tree dedicada.

    Endereça UI-04 (siblings).
  </action>
  <verify>
    <automated>CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys && pnpm --filter @paperclipai/ui typecheck</automated>
  </verify>
  <done>9 arquivos siblings migrados; zero strings hardcoded visíveis em inglês quando locale=pt-BR; settings.json sub-trees preenchidas conforme uso real; missing-keys detector GREEN; full UI suite GREEN.</done>
</task>

<task type="auto">
  <name>Tarefa 4: Verificação consolidada UI-04 + SUMMARY</name>
  <files>.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-01-SUMMARY.md</files>
  <action>
    1. Run final consolidated checks:
       - `pnpm --filter @paperclipai/ui typecheck` exit 0
       - `pnpm --filter @paperclipai/ui test:run` full suite GREEN
       - `CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys` exit 0
       - `pnpm -r typecheck` exit 0 (cross-workspace)

    2. Spot check visual: grep `placeholder="[A-Z]"` ou `>[A-Z][a-z]+` nos 11 arquivos. Documentar matches restantes (devem ser zero ou apenas brand names "Paperclip" / valores técnicos).

    3. Escrever `.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-01-SUMMARY.md` documentando:
       - 11 arquivos migrados
       - Audit results da Tarefa 1 (LOC + string density real para 9 páginas medidas)
       - Sub-trees criadas em settings.json (lista completa)
       - Decisões de granularidade (por que `costs.*` ou `settings:claude-accounts.cost.*`, etc.)
       - Reuso de patterns Phase 8 (STATUS_KEY canônico, inner-component useTranslation, brand-preserving, fixture-fix protocol — quantas aplicações)
       - Open Question #4 resolution (audit fechou estimativa ±20%)
       - Pitfall 8 fechado
       - Próximas dependências: 09-04 toca alguns dos mesmos arquivos para tooltip/toast cross-cutting

    Endereça UI-04 closure.
  </action>
  <verify>
    <automated>pnpm --filter @paperclipai/ui test:run && CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys && pnpm -r typecheck</automated>
  </verify>
  <done>SUMMARY documentado; 3 comandos verification GREEN; UI-04 marcado como satisfied no STATE.md (executor task pós-plano).</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @paperclipai/ui test:run` — full suite GREEN (incluindo 4-6 novos RTL tests + Phase 7-8 preservadas)
2. `CI=true pnpm --filter @paperclipai/ui test:run -- missing-keys` exit 0
3. `pnpm --filter @paperclipai/ui typecheck` exit 0
4. `pnpm -r typecheck` exit 0
5. Spot grep visual nos 11 arquivos: zero strings inglês uppercase visíveis ao usuário
</verification>

<success_criteria>
- 11 admin/company pages migrated to useTranslation
- settings.json (pt-BR + en-US) extended with `company.*` + 8 additional sub-trees, ~280 leaf keys
- Phase 7 (`language.*`) and Phase 8 (`claude-accounts.*`, `instance-settings.*`, etc.) sub-trees preserved byte-by-byte
- ROLE_KEY / STATUS_KEY / PERMISSION_KEY canonical lookups (no hardcoded permissionLabels)
- HUMAN_COMPANY_MEMBERSHIP_ROLE_LABELS in packages/shared untouched (server logs en-US)
- translateApiError consumed in all mutation onError handlers
- 2 RTL probe tests (CompanySettings, CompanyAccess) covering 4-6 cases incl. plurals
- UI-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-traducao-ui-admin-auth-sistemicas/09-01-SUMMARY.md` per Tarefa 4.
</output>
</content>
</invoke>