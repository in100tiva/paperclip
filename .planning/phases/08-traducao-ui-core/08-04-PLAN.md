---
phase: 08-traducao-ui-core
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/i18n/locales/pt-BR/common.json
  - ui/src/i18n/locales/en-US/common.json
  - ui/src/components/Sidebar.tsx
  - ui/src/components/InstanceSidebar.tsx
  - ui/src/components/SidebarAccountMenu.tsx
  - ui/src/components/SidebarCompanyMenu.tsx
  - ui/src/components/SidebarAgents.tsx
  - ui/src/components/SidebarProjects.tsx
  - ui/src/components/BreadcrumbBar.tsx
  - ui/src/components/MobileBottomNav.tsx
  - ui/src/components/Layout.tsx
  - ui/src/components/CompanyRail.tsx
  - ui/src/components/CompanySwitcher.tsx
  - ui/src/components/layout/__tests__/Sidebar.translation.test.tsx
autonomous: true
requirements:
  - UI-05

must_haves:
  truths:
    - "Usuário com locale=pt-BR navega no app e vê todos os itens de sidebar (Dashboard, Inbox, Tarefas, Rotinas, Metas, Workspaces, Organização, Skills, Custos, Atividade, Configurações), section labels (Trabalho/Empresa) e botão 'Nova tarefa' em português"
    - "User menu (SidebarAccountMenu) renderiza Profile/Edit/Settings/Documentation/Switch theme/Sign out + descriptions secundárias em pt-BR; versão 'Paperclip v{{version}}' interpola corretamente"
    - "InstanceSidebar renderiza nav labels (Profile, General, Access, Heartbeats, Experimental, Plugins, Adapters) traduzidos"
    - "BreadcrumbBar.tsx aria-label 'Abrir barra lateral' traduzido; LABELS de breadcrumb passam pelas páginas (não modificar BreadcrumbBar para traduzir labels — Pitfall 2)"
    - "'Paperclip' como brand string permanece literal (NÃO traduzir — D-NAV)"
    - "Detector missing-keys.test.ts permanece GREEN com CI=true após edits"
  artifacts:
    - path: "ui/src/i18n/locales/pt-BR/common.json"
      provides: "Estendido com sub-tree nav.* (sections/items/instance-settings/account-menu/breadcrumb)"
      contains: "nav"
    - path: "ui/src/i18n/locales/en-US/common.json"
      provides: "Estendido espelhado"
      contains: "nav"
    - path: "ui/src/components/layout/__tests__/Sidebar.translation.test.tsx"
      provides: "RTL test asserindo render pt-BR e en-US do Sidebar"
      min_lines: 30
  key_links:
    - from: "ui/src/components/Sidebar.tsx"
      to: "ui/src/i18n/locales/pt-BR/common.json"
      via: "useTranslation(['common']) + t('common:nav.items.*')"
      pattern: "useTranslation\\(\\[\"common\""
    - from: "ui/src/components/SidebarAccountMenu.tsx"
      to: "ui/src/i18n/locales/pt-BR/common.json"
      via: "t('common:nav.account-menu.*') incluindo version interpolation"
      pattern: "t\\(\"common:nav.account-menu"
    - from: "ui/src/components/InstanceSidebar.tsx"
      to: "ui/src/i18n/locales/pt-BR/common.json"
      via: "t('common:nav.instance-settings.items.*')"
      pattern: "t\\(\"common:nav.instance-settings"
---

<objective>
Traduzir a superfície de Navegação (UI-05) para pt-BR e en-US, cobrindo 11 arquivos: `Sidebar.tsx` (131 LOC), `InstanceSidebar.tsx` (58 LOC), `SidebarAccountMenu.tsx` (256 LOC, user menu denso), `SidebarCompanyMenu.tsx`, `SidebarAgents.tsx`, `SidebarProjects.tsx`, `BreadcrumbBar.tsx` (apenas aria-label — labels vêm das páginas), `MobileBottomNav.tsx`, `Layout.tsx`, `CompanyRail.tsx`, `CompanySwitcher.tsx`. Estender `common.json` com sub-tree `nav.*` (~55 chaves novas).

Purpose: UI-05 é o "framing" do app — sidebar/header/menus aparecem em TODA página. Traduzi-los garante experiência sem mistura bilíngue. SidebarAccountMenu tem 256 LOC com pares label/description que dobram a contagem efetiva. "Paperclip" como brand permanece intacto (D-NAV).

Output: 2 arquivos JSON estendidos, 11 arquivos TSX migrados, 1 arquivo de teste RTL.
</objective>

<execution_context>
@./.claude/framework/workflows/execute-plan.md
@./.claude/framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-traducao-ui-core/08-CONTEXT.md
@.planning/phases/08-traducao-ui-core/08-RESEARCH.md
@.planning/phases/08-traducao-ui-core/08-VALIDATION.md
@ui/src/pages/ProfileSettings.tsx
@ui/src/i18n/locales/pt-BR/common.json

<interfaces>
From ui/src/i18n/locales/pt-BR/common.json (Phase 7 existing — DO NOT REMOVE):
```json
{
  "app-name": "Paperclip da Equipe",
  "loading": "Carregando…",
  "save": "Salvar",
  "cancel": "Cancelar"
}
```

From RESEARCH §Pattern 4 (Nav surface mapping) — `nav.*` sub-tree planejado:
```json
{
  "nav": {
    "new-issue": "Nova tarefa",
    "sections": { "work": "Trabalho", "company": "Empresa" },
    "items": { "dashboard": "Dashboard", "inbox": "Inbox", ... },
    "instance-settings": { "title": "...", "items": {...} },
    "account-menu": { "open": "Abrir menu da conta", ..., "version": "Paperclip v{{version}}" },
    "breadcrumb": { "open-sidebar": "Abrir barra lateral" }
  }
}
```

From RESEARCH §Pattern 4 critical decision:
- BreadcrumbBar.tsx tem APENAS 1 string para traduzir (aria-label "Open sidebar" linha 71).
- Crumb labels (Projects, Activity, Inbox, Project, Instance Settings, Heartbeats, General) vêm de `setBreadcrumbs([{ label: "..." }])` chamadas pelas páginas.
- ESTAS labels são traduzidas NA PÁGINA (08-01/08-02/08-03 já fazem isso para suas próprias breadcrumbs); este plano NÃO mexe em setBreadcrumbs de outras páginas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Estender common.json com sub-tree nav.*</name>
  <files>ui/src/i18n/locales/pt-BR/common.json, ui/src/i18n/locales/en-US/common.json</files>
  <read_first>
    - ui/src/i18n/locales/pt-BR/common.json (CRÍTICO — Phase 7 deixou app-name/loading/save/cancel; pode também ter chaves do plan 08-01 se executou primeiro; preservar tudo)
    - ui/src/components/Sidebar.tsx:80-118 (linhas com nav labels)
    - ui/src/components/SidebarAccountMenu.tsx:131-249 (user menu strings + descriptions)
    - ui/src/components/InstanceSidebar.tsx:20-32
    - ui/src/components/BreadcrumbBar.tsx:71 (única string)
  </read_first>
  <action>
    Conforme RESEARCH §Pattern 4 e D-01.

    **CRÍTICO:** Ler estado atual de `common.json` antes de editar (08-01 pode já ter adicionado `actions.*`; preservar todas as chaves existentes).

    Estender `ui/src/i18n/locales/pt-BR/common.json` com sub-tree `nav.*`:

    ```json
    {
      "...": "(MANTER tudo existente: app-name, loading, save, cancel, e quaisquer chaves do 08-01)",
      "nav": {
        "new-issue": "Nova tarefa",
        "sections": {
          "work": "Trabalho",
          "company": "Empresa"
        },
        "items": {
          "dashboard": "Dashboard",
          "inbox": "Inbox",
          "issues": "Tarefas",
          "routines": "Rotinas",
          "goals": "Metas",
          "workspaces": "Workspaces",
          "org": "Organização",
          "skills": "Skills",
          "costs": "Custos",
          "activity": "Atividade",
          "settings": "Configurações"
        },
        "instance-settings": {
          "title": "Configurações da instância",
          "items": {
            "profile": "Perfil",
            "general": "Geral",
            "access": "Acesso",
            "heartbeats": "Heartbeats",
            "experimental": "Experimental",
            "plugins": "Plugins",
            "adapters": "Adaptadores"
          }
        },
        "account-menu": {
          "open": "Abrir menu da conta",
          "view-profile": "Ver perfil",
          "view-profile-description": "Abra seu ledger de atividade, tarefas e uso.",
          "edit-profile": "Editar perfil",
          "edit-profile-description": "Atualize seu nome de exibição e avatar.",
          "instance-settings": "Configurações da instância",
          "instance-settings-description": "Volte para a última página de configurações que abriu.",
          "documentation": "Documentação",
          "documentation-description": "Abra a documentação do Paperclip em uma nova aba.",
          "switch-to-light": "Trocar para modo claro",
          "switch-to-dark": "Trocar para modo escuro",
          "switch-theme-description": "Alterne a aparência do app.",
          "sign-out": "Sair",
          "signing-out": "Saindo…",
          "sign-out-description": "Encerre esta sessão do navegador.",
          "account-badge": "Conta",
          "local-badge": "Local",
          "signed-in": "Conectado",
          "local-board": "Board do workspace local",
          "version": "Paperclip v{{version}}"
        },
        "company-menu": {
          "switch": "Trocar de empresa",
          "create": "Criar empresa"
        },
        "agents-section": {
          "title": "Agentes"
        },
        "projects-section": {
          "title": "Projetos"
        },
        "breadcrumb": {
          "open-sidebar": "Abrir barra lateral"
        },
        "mobile-bottom": {
          "more": "Mais"
        }
      }
    }
    ```

    Estender com chaves adicionais conforme strings descobertas no grep das tarefas seguintes — alvo total ~55 chaves novas.

    Espelhar TODAS as chaves novas em `ui/src/i18n/locales/en-US/common.json` (Open account menu, View profile, etc.).

    **Atenção D-NAV:** "Paperclip" em `account-menu.version` está dentro de string com interpolação ({{version}}); NÃO virar chave própria. "Paperclip da Equipe" é app-name (Phase 7) — ambos permanecem com brand intacto.

    Validar JSON e preservação:
    ```bash
    node -e "const d = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/common.json','utf8')); if (!d['app-name']) throw 'app-name lost'; if (!d.nav?.items?.inbox) throw 'nav.items.inbox missing'; console.log('OK preserved + extended');"
    ```
  </action>
  <verify>
    <automated>node -e "const ptBr = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/pt-BR/common.json','utf8')); const enUs = JSON.parse(require('fs').readFileSync('ui/src/i18n/locales/en-US/common.json','utf8')); if (!ptBr['app-name']) throw 'pt-BR app-name lost'; if (!enUs['app-name']) throw 'en-US app-name lost'; if (!ptBr.nav?.items?.inbox) throw 'nav.items missing'; if (!ptBr.nav?.['account-menu']?.['view-profile']) throw 'account-menu missing'; if (!enUs.nav?.items?.inbox) throw 'en-US nav.items missing'; console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/i18n/locales/pt-BR/common.json contains "app-name" (preserved) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/common.json contains "nav" (new) (grep-checkable)
    - ui/src/i18n/locales/pt-BR/common.json contains "account-menu" (grep-checkable)
    - ui/src/i18n/locales/pt-BR/common.json contains "{{version}}" (interpolation) (grep-checkable)
    - ui/src/i18n/locales/en-US/common.json mirror dos sub-trees
    - 2 arquivos JSON válidos
  </acceptance_criteria>
  <done>2 dicionários common estendidos com nav.*; Phase 7 keys preservadas; conjuntos espelhados</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migrar 11 arquivos de Nav para t()</name>
  <files>ui/src/components/Sidebar.tsx, ui/src/components/InstanceSidebar.tsx, ui/src/components/SidebarAccountMenu.tsx, ui/src/components/SidebarCompanyMenu.tsx, ui/src/components/SidebarAgents.tsx, ui/src/components/SidebarProjects.tsx, ui/src/components/BreadcrumbBar.tsx, ui/src/components/MobileBottomNav.tsx, ui/src/components/Layout.tsx, ui/src/components/CompanyRail.tsx, ui/src/components/CompanySwitcher.tsx</files>
  <read_first>
    - ui/src/pages/ProfileSettings.tsx:3,26 (padrão canônico)
    - ui/src/components/Sidebar.tsx (todo — labels via prop em linhas 80-118)
    - ui/src/components/SidebarAccountMenu.tsx (256 LOC — user menu denso, pares label/description em 131-249)
    - ui/src/components/InstanceSidebar.tsx (58 LOC, nav items em 20-32)
    - ui/src/components/BreadcrumbBar.tsx (121 LOC — APENAS aria-label linha 71)
    - ui/src/components/SidebarCompanyMenu.tsx, SidebarAgents.tsx, SidebarProjects.tsx, MobileBottomNav.tsx, Layout.tsx, CompanyRail.tsx, CompanySwitcher.tsx
  </read_first>
  <action>
    Conforme D-01 e RESEARCH §Pattern 4 + Anti-Patterns.

    Para cada um dos 11 arquivos:

    1. Adicionar import:
       ```typescript
       import { useTranslation } from "react-i18next";
       ```

    2. Hook no início do componente:
       ```typescript
       const { t } = useTranslation(["common"]);
       ```

    3. Substituir strings hardcoded:
       - **Sidebar.tsx**: `label="Inbox"` → `label={t("common:nav.items.inbox")}`. Repetir para todos os 11 nav items + section labels (Work/Company).
       - **InstanceSidebar.tsx**: nav labels via `t("common:nav.instance-settings.items.profile")` etc.
       - **SidebarAccountMenu.tsx**: cada item tem 2 strings (label + description). Ex: `<MenuItem title="View profile" description="Open your activity ledger...">` → `<MenuItem title={t("common:nav.account-menu.view-profile")} description={t("common:nav.account-menu.view-profile-description")}>`. Aplicar para 7 items (view-profile, edit-profile, instance-settings, documentation, switch-theme, sign-out + version footer).
       - **Version footer**: `Paperclip v{version}` → `t("common:nav.account-menu.version", { version })` (interpolação preserva "Paperclip" como literal embedded).
       - **BreadcrumbBar.tsx (linha 71)**: `aria-label="Open sidebar"` → `aria-label={t("common:nav.breadcrumb.open-sidebar")}`. **NÃO MEXER em nada mais** — labels de crumbs vêm das páginas via setBreadcrumbs.
       - **SidebarCompanyMenu.tsx, SidebarAgents.tsx, SidebarProjects.tsx**: section headers + actions traduzidos.
       - **MobileBottomNav.tsx**: labels mobile via `t("common:nav.items.*")` (reusa chaves desktop).
       - **Layout.tsx, CompanyRail.tsx, CompanySwitcher.tsx**: aria-labels gerais e layout-level strings.

    4. **Anti-pattern crítico**: NÃO traduzir "Paperclip" como brand standalone. Em `app-name` (Phase 7) ele permanece "Paperclip da Equipe"; em version footer ele permanece literal dentro do template `{{version}}`.

    5. **Anti-pattern crítico**: NÃO traduzir labels de breadcrumb dentro do BreadcrumbBar.tsx. As labels vêm de cada página chamando `setBreadcrumbs([{ label: "..." }])`. Esses callsites em outras páginas são responsabilidade do plano correspondente (Inbox.tsx → 08-01, Projects.tsx/ProjectDetail.tsx → 08-02, Settings/* → 08-03). Este plano apenas confirma que páginas externas (Goals, Workspaces, Activity etc. — fora do escopo da fase 8) que ainda usam labels em inglês permanecem em inglês até Fase 9.

    6. Para CADA chave nova adicionada via `t()`, garantir existência em pt-BR e en-US JSON.

    Validar:
    ```bash
    cd ui && pnpm typecheck
    cd ui && CI=true pnpm test:run -- missing-keys
    ```
  </action>
  <verify>
    <automated>cd ui && pnpm typecheck && CI=true pnpm test:run -- missing-keys</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/Sidebar.tsx contains "useTranslation" (grep-checkable)
    - ui/src/components/Sidebar.tsx contains 't("common:nav.items.' (grep-checkable, 5+ occurrences)
    - ui/src/components/SidebarAccountMenu.tsx contains 't("common:nav.account-menu.' (grep-checkable, 5+ occurrences)
    - ui/src/components/SidebarAccountMenu.tsx contains '{{ version }}' OR '{ version }' (interpolation usage) (grep-checkable)
    - ui/src/components/InstanceSidebar.tsx contains 't("common:nav.instance-settings.items.' (grep-checkable)
    - ui/src/components/BreadcrumbBar.tsx contains 't("common:nav.breadcrumb.open-sidebar"' (grep-checkable)
    - ui/src/components/Sidebar.tsx does NOT contain "Paperclip" as bare standalone JSX text (it should be inside t() interpolation or app-name key)
    - `cd ui && pnpm typecheck` exits 0
    - `cd ui && CI=true pnpm test:run -- missing-keys` exits 0
  </acceptance_criteria>
  <done>11 arquivos migrados; 7 pares label/description em SidebarAccountMenu cobertos; BreadcrumbBar limitado a 1 string (Pitfall respeitado); brand "Paperclip" preservado em interpolação; missing-keys CI verde</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: RTL test Sidebar.translation.test.tsx</name>
  <files>ui/src/components/layout/__tests__/Sidebar.translation.test.tsx</files>
  <read_first>
    - ui/src/pages/__tests__/ProfileSettings.locale-toggle.test.tsx (provider wrapping pattern)
    - ui/src/components/Sidebar.tsx (props/contexto necessário — geralmente usa CompanyContext, BreadcrumbContext, possivelmente router)
    - ui/src/i18n/locales/pt-BR/common.json (chaves para asserir)
  </read_first>
  <action>
    Conforme VALIDATION.md §Wave 0 Requirements (linha 58).

    Criar diretório `ui/src/components/layout/__tests__/` se necessário.

    Estrutura espelhada de ProfileSettings.locale-toggle.test.tsx:

    ```typescript
    // @vitest-environment jsdom
    import { describe, it, expect, beforeEach } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
    import { I18nextProvider } from "react-i18next";
    import { MemoryRouter } from "react-router-dom";
    import i18n from "@/i18n";
    import { Sidebar } from "@/components/Sidebar";
    // OU SidebarAccountMenu/InstanceSidebar se Sidebar tiver setup complexo

    function renderWithProviders(ui: React.ReactElement) {
      const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
      return render(
        <QueryClientProvider client={queryClient}>
          <I18nextProvider i18n={i18n}>
            <MemoryRouter>{ui}</MemoryRouter>
          </I18nextProvider>
        </QueryClientProvider>
      );
    }

    describe("Sidebar — translation render (UI-05)", () => {
      beforeEach(async () => {
        await i18n.changeLanguage("pt-BR");
      });

      it("renderiza nav items em pt-BR", async () => {
        renderWithProviders(<Sidebar {/* mock props mínimas */} />);
        // Esperado pelo menos 1 dos labels traduzidos
        expect(screen.getByText(/Inbox|Tarefas|Configurações|Atividade/i)).toBeInTheDocument();
      });

      it("renderiza nav items em en-US", async () => {
        await i18n.changeLanguage("en-US");
        renderWithProviders(<Sidebar {/* mock props mínimas */} />);
        expect(screen.getByText(/Inbox|Issues|Settings|Activity/i)).toBeInTheDocument();
      });
    });
    ```

    Se Sidebar tem dependências complexas (CompanyContext, queries hidratantes), alvejar `InstanceSidebar.tsx` (58 LOC, simples) ou `SidebarAccountMenu.tsx` que aceita session via prop.

    Garantir GREEN.
  </action>
  <verify>
    <automated>cd ui && pnpm test:run -- Sidebar.translation</automated>
  </verify>
  <acceptance_criteria>
    - ui/src/components/layout/__tests__/Sidebar.translation.test.tsx exists
    - Test file contains "i18n.changeLanguage" (grep-checkable)
    - Test file contains "pt-BR" AND "en-US"
    - `cd ui && pnpm test:run -- Sidebar.translation` exits 0
  </acceptance_criteria>
  <done>Arquivo de teste criado; teste GREEN; cobertura UI-05 truth #1 em automatizado</done>
</task>

</tasks>

<verification>
- `cd ui && pnpm typecheck` — exit 0
- `cd ui && CI=true pnpm test:run -- missing-keys` — exit 0
- `cd ui && pnpm test:run -- Sidebar.translation` — exit 0
- `cd ui && pnpm test:run` — full UI suite GREEN
- grep manual em Sidebar.tsx por strings em inglês fora de t() — zero matches (exceto strings em comentários)
- grep `"Paperclip"` em arquivos modificados — apenas dentro de strings de interpolação ou em chaves JSON, nunca como JSX text standalone
</verification>

<success_criteria>
- 2 dicionários common estendidos com sub-tree `nav.*` (~55 chaves) preservando Phase 7 keys + chaves adicionadas pelo 08-01
- 11 arquivos TSX migrados (Sidebar/InstanceSidebar/SidebarAccountMenu + 4 sidebar children + BreadcrumbBar restrito + MobileBottomNav + Layout + CompanyRail + CompanySwitcher)
- BreadcrumbBar.tsx só tem aria-label traduzido; labels de crumbs respeitam Pitfall 2 (vêm das páginas)
- Brand "Paperclip" preservado dentro de version interpolation
- 1 teste RTL novo
- Detector missing-keys CI=true GREEN
- UI-05 satisfeito (truth #1: usuário em pt-BR vê navegação global em português)
</success_criteria>

<output>
After completion, create `.planning/phases/08-traducao-ui-core/08-04-SUMMARY.md`.
</output>
